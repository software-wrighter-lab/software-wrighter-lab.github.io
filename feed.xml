<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://software-wrighter-lab.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://software-wrighter-lab.github.io/" rel="alternate" type="text/html" /><updated>2026-04-29T12:07:44-07:00</updated><id>https://software-wrighter-lab.github.io/feed.xml</id><title type="html">Software Wrighter Lab Blog</title><subtitle>AI coding agents, systems programming, and practical machine learning</subtitle><author><name>Mike Wright</name></author><entry><title type="html">Personal Software (10/?): Vibe-Maintenance — When AI Agents Don’t Just Write Code, They Fix Bugs</title><link href="https://software-wrighter-lab.github.io/2026/04/29/personal-software-vibe-maintenance/" rel="alternate" type="text/html" title="Personal Software (10/?): Vibe-Maintenance — When AI Agents Don’t Just Write Code, They Fix Bugs" /><published>2026-04-29T12:00:00-07:00</published><updated>2026-04-29T12:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/29/personal-software-vibe-maintenance</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/29/personal-software-vibe-maintenance/"><![CDATA[<p><img src="/assets/images/posts/block-car-lift.png" class="post-marker no-invert" alt="" style="width: 240px;" /></p>

<div style="overflow: hidden;">

  <p>The car is up on the lift. The mechanic is not building a new car; the mechanic is doing maintenance on a car that already runs — replacing a sensor, chasing a leak, tightening a fitting that worked itself loose. The everyday work of a software lab looks more like that picture than like the more common rendering of “AI builds the thing” — a lab full of in-progress compilers, runtimes, and demos accumulates bugs faster than it accumulates <em>features</em>, and the AI agents that helped write the original code are now spending most of their tokens fixing it.</p>

</div>

<div class="aside-box">

  <p><strong>Why this matters</strong> — “Vibe-coding” gets the headlines because it produces something visible: a new feature, a new demo, a new tool. Vibe-maintenance is the quieter half. It does not show up as a flashy commit message; it shows up as a green “Try it” badge that used to say “In dev,” or as a closed-issues heatmap that is busier than the commits one. If your only frame for AI-assisted development is “the agent writes new code,” you miss the half of the work where the agent is reading existing code, finding the assumption that was wrong, and patching it. Almost every senior engineer who has tried to use AI agents finds maintenance more useful than greenfield work; the post argues for why, and what the human still has to do.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Status dashboard</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/status">sw-embed.github.io/web-sw-cor24-demos/#/status</a></td>
      </tr>
      <tr>
        <td><strong>Demos repo</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      </tr>
      <tr>
        <td><strong>Closed-issues report (raw)</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-demos/blob/main/reports/closed-issues.html">reports/closed-issues.html</a></td>
      </tr>
      <tr>
        <td><strong>Prior Personal Software posts</strong></td>
        <td><a href="/2026/04/28/personal-software-sw-launcher-one-ring/">sw-launcher: One Ring to Rule Them All</a> · <a href="/2026/04/27/sw-checklist-ratchet-ai-coding-agents/">sw-checklist: Reining In AI Coding Agents</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-corollary-to-vibe-coding">The Corollary to Vibe-Coding</h2>

<p>Vibe-coding, as the term gets used: a human describes intent, an AI agent writes the code, the human accepts or redirects. The implicit assumption is that <em>new code</em> is the bottleneck. For a greenfield project, sure — there is nothing to maintain because there is nothing to maintain <em>yet</em>. For a lab whose accumulated output now spans 37 repositories — assemblers, emulators, cross-compilers, p-code VM, native interpreters for BASIC and Forth and APL and Smalltalk and Macrolisp and SNOBOL4, host tooling, the resident-shell trio, web demos for most of them — the bottleneck moved a long time ago. New code lands; new code interacts with old code; old code that was fine on its own now has a corner that nobody exercised; an issue gets opened.</p>

<p>Vibe-maintenance is the same loop with a different verb. The human writes an issue title. The agent reads the relevant code, locates the assumption that was wrong, makes a minimal patch, adds a regression test, and closes the issue. The human’s job is not to write the code; it is <em>to write the issue</em> with enough specificity that the agent has somewhere to start, and to read the diff with enough care that the test does not just pin in the bug under a different name.</p>

<p>The skill the human keeps is not “writing code.” It is <em>symptom description</em>.</p>

<h2 id="the-status-tab-as-visualization">The Status Tab as Visualization</h2>

<p><a href="https://github.com/sw-embed/web-sw-cor24-demos">web-sw-cor24-demos</a> is the landing page for the COR24 ecosystem. Its Status tab makes the lab’s <em>operational</em> state visible at a glance:</p>

<ul>
  <li>A 37-row table of every repo with a colored badge — “Try it” (green), “In dev” (yellow), “In plan” (orange), “Future” (red), “n/a” (neutral) — for repo readiness, web-UI readiness, and AgentRail saga presence.</li>
  <li>An issue chart per repo: open vs. closed counts and a sparkline.</li>
  <li>A <strong>Closed Issues by Repo &amp; Date</strong> heatmap, generated by <code class="language-plaintext highlighter-rouge">scripts/gen-closed-issues.sh</code> from the GitHub API.</li>
  <li>A <strong>Commits by Repo &amp; Hour</strong> heatmap, the same shape one row down.</li>
  <li>A “Gaps” panel calling out the cross-cutting work the lab has <em>not</em> yet done (software floating-point library, native COR24 C compiler, etc.).</li>
</ul>

<p>The headline number on the closed-issues chart, at the time of writing: <strong>28 repos, 141 issues, 18 days</strong>. Eight closed issues a day, sustained, across two dozen active repos. That is not an output a single human writing code by hand achieves. It is also not an output a single human <em>reviewing AI-written code</em> achieves if every issue requires a fresh greenfield design — it is only achievable because most of those 141 issues were <em>bugs in code that already existed</em>, and an agent can fix one of those in a fraction of the time it took to write the original.</p>

<p>The two heatmaps next to each other tell a story: the commits chart shows when work happened (clusters in the morning, fewer at night, weekend bursts when an idea hit); the closed-issues chart shows what work <em>settled</em> (each cell is a problem that has a regression test guarding it). The cells are mostly numbered links, so any cell on the chart is one click from a real PR diff. That traceability is the whole point.</p>

<h2 id="four-patterns-of-vibe-maintenance">Four Patterns of Vibe-Maintenance</h2>

<p>Reading down the closed-issues report by category — not by repo — four kinds of fix dominate.</p>

<h3 id="1-capacity-limit-bumps">1. Capacity-Limit Bumps</h3>

<p>The numerically-largest category. A compiler or interpreter has an internal table whose size was <em>guessed</em> at first commit (<code class="language-plaintext highlighter-rouge">MAX_PROCS = 32</code>, <code class="language-plaintext highlighter-rouge">INPUT_BUF_SIZE = 8192</code>, AST pool of 256 nodes, emit buffer of 32 KiB, string literal table of 32 entries). A real program hits the limit, the agent bumps it, and a follow-up issue raises it again later when an even bigger program lands.</p>

<p>A small sample, all closed in this window, all from <code class="language-plaintext highlighter-rouge">sw-cor24-plsw</code> and <code class="language-plaintext highlighter-rouge">sw-cor24-pascal</code>:</p>

<ul>
  <li>“AST pool exhaustion (256 nodes) causes misleading parse errors.”</li>
  <li>“Source buffer (8 KiB) too small for larger programs.”</li>
  <li>“Emit buffer (32 KiB) too small for programs with large static data.”</li>
  <li>“DEF_MAX (32) too small — %DEFINE silently dropped when exceeded.”</li>
  <li>“Global symbol table limited to 64 entries (SYM_SCOPE_MAX).”</li>
  <li>“Raise MAX_PROCS from 32 to support larger programs.”</li>
  <li>“Raise MAX_STRINGS limit from 16 to support larger programs.”</li>
  <li>“Raise INPUT_BUF_SIZE from 32768 to support larger programs (third bump).”</li>
</ul>

<p>Each one is a one-line const change plus a regression test that compiles a representative-sized input. The third-bump issue is the funny one: the limit is no longer a guess, it is a parameter that grows with the corpus. Eventually the right answer is “the table grows dynamically,” but the lab’s working assumption is that bumping a static limit is a one-cycle fix, while a dynamic table is a one-day refactor that earns its keep only after the same limit has been bumped enough times to justify it. The ratchet is the right tool for this kind of debt.</p>

<h3 id="2-subtle-codegen-bugs">2. Subtle Codegen Bugs</h3>

<p>The most interesting category. These are not “the feature is missing”; these are “the feature is silently wrong.” A few from the same window:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-plsw#8</code>: “BYTE field reads use signed <code class="language-plaintext highlighter-rouge">lb</code> instead of unsigned <code class="language-plaintext highlighter-rouge">lbu</code>.” A one-instruction error; values 128–255 sign-extend to negative integers, breaking pattern matching and arithmetic.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-plsw#31</code>: “Function return corrupts r1 -&gt; jmp to PC=0 (programs that call PUT_DEC re-enter <code class="language-plaintext highlighter-rouge">_start</code>).” A clobbered callee-saved register; the symptom looks like an infinite loop with the program re-running from the top.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-pcode#10</code>: “p24-load: patch_code_relocations incorrectly relocates negative push literals.” A pointer-vs-immediate confusion in the linker; small negative integers get rewritten as garbage addresses.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-x-tinyc#19</code>: “Codegen: integer division with negative dividend returns wrong result.” The C cross-compiler’s sign-handling.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-snobol4#13</code>: “Arithmetic on a pattern-captured string returns garbage on first use.” A type-tag bug in the SNOBOL4 interpreter’s value union.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-pascal#16</code>: “<code class="language-plaintext highlighter-rouge">write(chr(n))</code> outputs integer <code class="language-plaintext highlighter-rouge">n</code> instead of character.” Built-in dispatch on the wrong type.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-cor24-basic#1</code>: “ABS function silently returns wrong value (parsed as variable A).” The lexer treats <code class="language-plaintext highlighter-rouge">ABS</code> as a variable rather than a builtin, so <code class="language-plaintext highlighter-rouge">ABS(-3)</code> parses as <code class="language-plaintext highlighter-rouge">(A) * BS * (-3)</code> — a beautifully evil bug whose title carries the entire diagnosis.</li>
</ul>

<p>These are the issues where vibe-maintenance shines. Each title is a <em>complete reproducer</em> in plain English. The agent reads the title, opens the relevant translation unit, finds the wrong instruction or the wrong dispatch, fixes it, writes a one-program regression test, and closes the issue. The human never wrote a line of code; the human wrote a <em>fifteen-word symptom description</em>.</p>

<h3 id="3-surface-language-features-rolling-in">3. Surface Language Features Rolling In</h3>

<p>Each language interpreter ships with a minimum viable feature set, and demos that exercise more of the historical language drag in features that didn’t make the first cut:</p>

<ul>
  <li><strong>BASIC</strong>: DIM integer arrays, DATA / READ / RESTORE, ON expr GOTO/GOSUB, MOD, bitwise BAND/BOR/BXOR/SHL/SHR, CONT after STOP. (<code class="language-plaintext highlighter-rouge">sw-cor24-basic#2..#7</code>.)</li>
  <li><strong>OCaml</strong>: top-level let bindings, multi-line match expressions, mutable refs, records, list combinators (<code class="language-plaintext highlighter-rouge">map</code>/<code class="language-plaintext highlighter-rouge">fold_left</code>/<code class="language-plaintext highlighter-rouge">filter</code>), char literals, block comments, exceptions. (<code class="language-plaintext highlighter-rouge">sw-cor24-ocaml#3..#11</code>.)</li>
  <li><strong>Forth</strong>: forth-in-forth gets DO/LOOP, ?DO, WHILE/REPEAT, AGAIN, CONSTANT, VARIABLE, hashed dictionary; <code class="language-plaintext highlighter-rouge">:NONAME</code>. (<code class="language-plaintext highlighter-rouge">sw-cor24-forth#1..#5</code>.)</li>
  <li><strong>SNOBOL4</strong>: SIZE/SUBSTR/CHAR builtins, pattern-replacement assignment, case-preserving INPUT mode. (<code class="language-plaintext highlighter-rouge">sw-cor24-snobol4#1..#10</code>.)</li>
  <li><strong>TinyC</strong>: goto + labels, compound literals, designated initializers, <code class="language-plaintext highlighter-rouge">_Noreturn</code>, <code class="language-plaintext highlighter-rouge">restrict</code>, <code class="language-plaintext highlighter-rouge">inline</code>, octal literals, multi-dimensional array declarations. (<code class="language-plaintext highlighter-rouge">sw-cor24-x-tinyc#2..#11</code>.)</li>
</ul>

<p>Each of these is the kind of feature that <em>would</em> take a human a half-day if they had to read the existing parser, find the right place to extend it, and add the right test fixtures. Vibe-coding compresses that to a half-hour of agent work plus a human-written acceptance criterion. The acceptance criterion is the part that does not get cheaper.</p>

<h3 id="4-cross-cutting-tooling-bugs">4. Cross-Cutting Tooling Bugs</h3>

<p>The last category is the meta one: the tooling that <em>generates</em> the dashboards itself has bugs. A representative pair from the demos repo’s commit log, just in the past two weeks:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">fix UTC-to-local date conversion in gen-issue-chart, rebuild and deploy pages</code></li>
  <li><code class="language-plaintext highlighter-rouge">fix timezone in activity reports: convert UTC dates/hours to local, regenerate tables</code></li>
</ul>

<p>Two separate “use Local::now() instead of Utc::now()” commits in two different generators. The cells in the heatmap were shifted by a few hours, which made yesterday’s work look like today’s, which made the dashboard wrong — subtly, in a way that would only catch the eye of someone who <em>knew</em> what they had committed yesterday. The agent fixed both. They are listed in the dashboard the agent generated. The fact that the dashboard works is itself a regression test on its own generators.</p>

<h2 id="the-skill-that-doesnt-go-away">The Skill That Doesn’t Go Away</h2>

<p>Every category above leans on the same human contribution: a <em>precise</em> description of the symptom, often as the issue title.</p>

<p>Compare the two SNOBOL4 issues:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">#1</code>: “Missing string builtins: SIZE, SUBSTR, CHAR return 0 for all inputs.”</li>
  <li><code class="language-plaintext highlighter-rouge">#11</code>: “OUTPUT corruption: concat-OUTPUT in a loop truncates when a different block declares a pattern-match with <code class="language-plaintext highlighter-rouge">:F(forward_label)</code>.”</li>
</ul>

<p>Both are real titles. Both are diagnoses, not just symptoms — they tell the agent <em>exactly</em> what subsystem to read. <code class="language-plaintext highlighter-rouge">#1</code> is mechanical: open the builtins table, see that the entries return 0, fix them. <code class="language-plaintext highlighter-rouge">#11</code> is forensic: the title names the loop, the operator, the block, the directive, and the conditional — the agent has the entire reproducer one paste away from a test fixture.</p>

<p>The bad version of either title would be “OUTPUT is broken.” That title costs the agent half its budget on guessing what “broken” means and produces a fix that may or may not address the real bug.</p>

<p>The skill the lab keeps practicing is <em>writing the issue at the level of detail the agent needs</em>. That is roughly the same skill an engineer uses to file a useful bug report against another engineer. The difference is that the audience is faster and cheaper than the engineer; the title is read inside a second, the diff is back inside a minute, and the regression test is attached. The economics of writing good issues, in a vibe-maintenance world, are vastly more favorable than they were when the audience was a human queue with their own backlog.</p>

<h2 id="the-feedback-loop">The Feedback Loop</h2>

<div class="gutter-section">

  <p><img src="/assets/images/posts/block-mobius-strip.png" class="gutter-img-left no-invert" alt="Möbius strip" /></p>

  <p>The maintenance loop, as it actually runs in this lab, has five steps:</p>

  <ol>
    <li><strong>Symptom.</strong> A demo, test, or build fails. A user reports a wrong output. A regression test caught a regression. CI flagged a gate.</li>
    <li><strong>Issue.</strong> The human (or another agent) writes a one-line title and a short body that names the conditions. Most of the time the title is enough.</li>
    <li><strong>AgentRail saga step.</strong> For non-trivial fixes, the work goes onto an <a href="/2026/04/28/ai-tools-agentrail-rs-mid-saga-flexibility/">AgentRail</a> step — a single session does the diff, commits, and runs <code class="language-plaintext highlighter-rouge">agentrail complete</code>. The session is bounded; the next session is for the next step.</li>
    <li><strong>Regression test.</strong> The diff includes a test that pins the bug fixed. <code class="language-plaintext highlighter-rouge">cargo test</code> / <code class="language-plaintext highlighter-rouge">make test</code> is the contract. If a future change re-introduces the bug, the test catches it.</li>
    <li><strong>Status refresh.</strong> <code class="language-plaintext highlighter-rouge">cargo run -p gen-status</code> and the closed-issues / commits scripts re-pull from the GitHub API; the heatmap moves; the badge in the table tilts greener.</li>
  </ol>

  <p>This is not a novel workflow — it is what every well-run engineering team does. What is <em>new</em> is that the per-issue cost is small enough that the heatmap is <em>busy</em>. Eight issues a day across a constellation of personal projects, sustained for weeks, is the kind of cadence that used to require a small team. One human plus AI agents plus the discipline of writing good issues hits it.</p>

  <p>The artifact, in the end, is not “the AI fixed 141 bugs.” The artifact is the dashboard — 28 repos getting visibly greener, with each cell on the heatmap a clickable link to the diff that closed it. The lab is <em>legible</em>, and being legible makes it possible to do the work at this pace in the first place.</p>

</div>

<h2 id="where-it-sits-in-the-personal-software-toolkit">Where It Sits in the Personal-Software Toolkit</h2>

<p><a href="/2026/04/27/sw-checklist-ratchet-ai-coding-agents/">sw-checklist</a> keeps the <em>shape</em> of the code in line. <a href="/2026/04/28/personal-software-sw-launcher-one-ring/">sw-launcher</a> keeps the <em>shape of the load plan and memory budget</em> in line. <a href="/2026/04/28/ai-tools-agentrail-rs-mid-saga-flexibility/">AgentRail</a> keeps the <em>shape of the work</em> in line — one saga, one step at a time, with a faithful audit trail. The Status tab is the <em>operational dashboard</em> that makes the result of all three legible at a glance. None of those tools, individually, would be enough to keep a 37-repo lab maintainable by one person; together they make vibe-maintenance the steady-state mode of operation.</p>

<p>The car is up on the lift. The mechanic is not building anything new today. The mechanic is going around with a torque wrench, an oil drain, and a parts list, and at the end of the afternoon every gauge is in the green again. AI agents do not change which afternoons that work happens on. They change how many cars fit in the shop.</p>]]></content><author><name>Software Wrighter</name></author><category term="ai-tools" /><category term="cli-tools" /><category term="embedded" /><category term="vibe-maintenance" /><category term="vibe-coding" /><category term="personal-software" /><category term="ai-coding" /><category term="ai-agents" /><category term="cor24" /><category term="sw-embed" /><category term="bug-fixes" /><category term="regression-tests" /><category term="agentrail" /><category term="status-dashboard" /><summary type="html"><![CDATA[The car is up on the lift. The mechanic is not building a new car; the mechanic is doing maintenance on a car that already runs — replacing a sensor, chasing a leak, tightening a fitting that worked itself loose. The everyday work of a software lab looks more like that picture than like the more common rendering of “AI builds the thing” — a lab full of in-progress compilers, runtimes, and demos accumulates bugs faster than it accumulates features, and the AI agents that helped write the original code are now spending most of their tokens fixing it. Why this matters — “Vibe-coding” gets the headlines because it produces something visible: a new feature, a new demo, a new tool. Vibe-maintenance is the quieter half. It does not show up as a flashy commit message; it shows up as a green “Try it” badge that used to say “In dev,” or as a closed-issues heatmap that is busier than the commits one. If your only frame for AI-assisted development is “the agent writes new code,” you miss the half of the work where the agent is reading existing code, finding the assumption that was wrong, and patching it. Almost every senior engineer who has tried to use AI agents finds maintenance more useful than greenfield work; the post argues for why, and what the human still has to do. Resource Link Status dashboard sw-embed.github.io/web-sw-cor24-demos/#/status Demos repo sw-embed/web-sw-cor24-demos Closed-issues report (raw) reports/closed-issues.html Prior Personal Software posts sw-launcher: One Ring to Rule Them All · sw-checklist: Reining In AI Coding Agents Comments Discord The Corollary to Vibe-Coding Vibe-coding, as the term gets used: a human describes intent, an AI agent writes the code, the human accepts or redirects. The implicit assumption is that new code is the bottleneck. For a greenfield project, sure — there is nothing to maintain because there is nothing to maintain yet. For a lab whose accumulated output now spans 37 repositories — assemblers, emulators, cross-compilers, p-code VM, native interpreters for BASIC and Forth and APL and Smalltalk and Macrolisp and SNOBOL4, host tooling, the resident-shell trio, web demos for most of them — the bottleneck moved a long time ago. New code lands; new code interacts with old code; old code that was fine on its own now has a corner that nobody exercised; an issue gets opened. Vibe-maintenance is the same loop with a different verb. The human writes an issue title. The agent reads the relevant code, locates the assumption that was wrong, makes a minimal patch, adds a regression test, and closes the issue. The human’s job is not to write the code; it is to write the issue with enough specificity that the agent has somewhere to start, and to read the diff with enough care that the test does not just pin in the bug under a different name. The skill the human keeps is not “writing code.” It is symptom description. The Status Tab as Visualization web-sw-cor24-demos is the landing page for the COR24 ecosystem. Its Status tab makes the lab’s operational state visible at a glance: A 37-row table of every repo with a colored badge — “Try it” (green), “In dev” (yellow), “In plan” (orange), “Future” (red), “n/a” (neutral) — for repo readiness, web-UI readiness, and AgentRail saga presence. An issue chart per repo: open vs. closed counts and a sparkline. A Closed Issues by Repo &amp; Date heatmap, generated by scripts/gen-closed-issues.sh from the GitHub API. A Commits by Repo &amp; Hour heatmap, the same shape one row down. A “Gaps” panel calling out the cross-cutting work the lab has not yet done (software floating-point library, native COR24 C compiler, etc.). The headline number on the closed-issues chart, at the time of writing: 28 repos, 141 issues, 18 days. Eight closed issues a day, sustained, across two dozen active repos. That is not an output a single human writing code by hand achieves. It is also not an output a single human reviewing AI-written code achieves if every issue requires a fresh greenfield design — it is only achievable because most of those 141 issues were bugs in code that already existed, and an agent can fix one of those in a fraction of the time it took to write the original. The two heatmaps next to each other tell a story: the commits chart shows when work happened (clusters in the morning, fewer at night, weekend bursts when an idea hit); the closed-issues chart shows what work settled (each cell is a problem that has a regression test guarding it). The cells are mostly numbered links, so any cell on the chart is one click from a real PR diff. That traceability is the whole point. Four Patterns of Vibe-Maintenance Reading down the closed-issues report by category — not by repo — four kinds of fix dominate. 1. Capacity-Limit Bumps The numerically-largest category. A compiler or interpreter has an internal table whose size was guessed at first commit (MAX_PROCS = 32, INPUT_BUF_SIZE = 8192, AST pool of 256 nodes, emit buffer of 32 KiB, string literal table of 32 entries). A real program hits the limit, the agent bumps it, and a follow-up issue raises it again later when an even bigger program lands. A small sample, all closed in this window, all from sw-cor24-plsw and sw-cor24-pascal: “AST pool exhaustion (256 nodes) causes misleading parse errors.” “Source buffer (8 KiB) too small for larger programs.” “Emit buffer (32 KiB) too small for programs with large static data.” “DEF_MAX (32) too small — %DEFINE silently dropped when exceeded.” “Global symbol table limited to 64 entries (SYM_SCOPE_MAX).” “Raise MAX_PROCS from 32 to support larger programs.” “Raise MAX_STRINGS limit from 16 to support larger programs.” “Raise INPUT_BUF_SIZE from 32768 to support larger programs (third bump).” Each one is a one-line const change plus a regression test that compiles a representative-sized input. The third-bump issue is the funny one: the limit is no longer a guess, it is a parameter that grows with the corpus. Eventually the right answer is “the table grows dynamically,” but the lab’s working assumption is that bumping a static limit is a one-cycle fix, while a dynamic table is a one-day refactor that earns its keep only after the same limit has been bumped enough times to justify it. The ratchet is the right tool for this kind of debt. 2. Subtle Codegen Bugs The most interesting category. These are not “the feature is missing”; these are “the feature is silently wrong.” A few from the same window: sw-cor24-plsw#8: “BYTE field reads use signed lb instead of unsigned lbu.” A one-instruction error; values 128–255 sign-extend to negative integers, breaking pattern matching and arithmetic. sw-cor24-plsw#31: “Function return corrupts r1 -&gt; jmp to PC=0 (programs that call PUT_DEC re-enter _start).” A clobbered callee-saved register; the symptom looks like an infinite loop with the program re-running from the top. sw-cor24-pcode#10: “p24-load: patch_code_relocations incorrectly relocates negative push literals.” A pointer-vs-immediate confusion in the linker; small negative integers get rewritten as garbage addresses. sw-cor24-x-tinyc#19: “Codegen: integer division with negative dividend returns wrong result.” The C cross-compiler’s sign-handling. sw-cor24-snobol4#13: “Arithmetic on a pattern-captured string returns garbage on first use.” A type-tag bug in the SNOBOL4 interpreter’s value union. sw-cor24-pascal#16: “write(chr(n)) outputs integer n instead of character.” Built-in dispatch on the wrong type. sw-cor24-basic#1: “ABS function silently returns wrong value (parsed as variable A).” The lexer treats ABS as a variable rather than a builtin, so ABS(-3) parses as (A) * BS * (-3) — a beautifully evil bug whose title carries the entire diagnosis. These are the issues where vibe-maintenance shines. Each title is a complete reproducer in plain English. The agent reads the title, opens the relevant translation unit, finds the wrong instruction or the wrong dispatch, fixes it, writes a one-program regression test, and closes the issue. The human never wrote a line of code; the human wrote a fifteen-word symptom description. 3. Surface Language Features Rolling In Each language interpreter ships with a minimum viable feature set, and demos that exercise more of the historical language drag in features that didn’t make the first cut: BASIC: DIM integer arrays, DATA / READ / RESTORE, ON expr GOTO/GOSUB, MOD, bitwise BAND/BOR/BXOR/SHL/SHR, CONT after STOP. (sw-cor24-basic#2..#7.) OCaml: top-level let bindings, multi-line match expressions, mutable refs, records, list combinators (map/fold_left/filter), char literals, block comments, exceptions. (sw-cor24-ocaml#3..#11.) Forth: forth-in-forth gets DO/LOOP, ?DO, WHILE/REPEAT, AGAIN, CONSTANT, VARIABLE, hashed dictionary; :NONAME. (sw-cor24-forth#1..#5.) SNOBOL4: SIZE/SUBSTR/CHAR builtins, pattern-replacement assignment, case-preserving INPUT mode. (sw-cor24-snobol4#1..#10.) TinyC: goto + labels, compound literals, designated initializers, _Noreturn, restrict, inline, octal literals, multi-dimensional array declarations. (sw-cor24-x-tinyc#2..#11.) Each of these is the kind of feature that would take a human a half-day if they had to read the existing parser, find the right place to extend it, and add the right test fixtures. Vibe-coding compresses that to a half-hour of agent work plus a human-written acceptance criterion. The acceptance criterion is the part that does not get cheaper. 4. Cross-Cutting Tooling Bugs The last category is the meta one: the tooling that generates the dashboards itself has bugs. A representative pair from the demos repo’s commit log, just in the past two weeks: fix UTC-to-local date conversion in gen-issue-chart, rebuild and deploy pages fix timezone in activity reports: convert UTC dates/hours to local, regenerate tables Two separate “use Local::now() instead of Utc::now()” commits in two different generators. The cells in the heatmap were shifted by a few hours, which made yesterday’s work look like today’s, which made the dashboard wrong — subtly, in a way that would only catch the eye of someone who knew what they had committed yesterday. The agent fixed both. They are listed in the dashboard the agent generated. The fact that the dashboard works is itself a regression test on its own generators. The Skill That Doesn’t Go Away Every category above leans on the same human contribution: a precise description of the symptom, often as the issue title. Compare the two SNOBOL4 issues: #1: “Missing string builtins: SIZE, SUBSTR, CHAR return 0 for all inputs.” #11: “OUTPUT corruption: concat-OUTPUT in a loop truncates when a different block declares a pattern-match with :F(forward_label).” Both are real titles. Both are diagnoses, not just symptoms — they tell the agent exactly what subsystem to read. #1 is mechanical: open the builtins table, see that the entries return 0, fix them. #11 is forensic: the title names the loop, the operator, the block, the directive, and the conditional — the agent has the entire reproducer one paste away from a test fixture. The bad version of either title would be “OUTPUT is broken.” That title costs the agent half its budget on guessing what “broken” means and produces a fix that may or may not address the real bug. The skill the lab keeps practicing is writing the issue at the level of detail the agent needs. That is roughly the same skill an engineer uses to file a useful bug report against another engineer. The difference is that the audience is faster and cheaper than the engineer; the title is read inside a second, the diff is back inside a minute, and the regression test is attached. The economics of writing good issues, in a vibe-maintenance world, are vastly more favorable than they were when the audience was a human queue with their own backlog. The Feedback Loop The maintenance loop, as it actually runs in this lab, has five steps: Symptom. A demo, test, or build fails. A user reports a wrong output. A regression test caught a regression. CI flagged a gate. Issue. The human (or another agent) writes a one-line title and a short body that names the conditions. Most of the time the title is enough. AgentRail saga step. For non-trivial fixes, the work goes onto an AgentRail step — a single session does the diff, commits, and runs agentrail complete. The session is bounded; the next session is for the next step. Regression test. The diff includes a test that pins the bug fixed. cargo test / make test is the contract. If a future change re-introduces the bug, the test catches it. Status refresh. cargo run -p gen-status and the closed-issues / commits scripts re-pull from the GitHub API; the heatmap moves; the badge in the table tilts greener. This is not a novel workflow — it is what every well-run engineering team does. What is new is that the per-issue cost is small enough that the heatmap is busy. Eight issues a day across a constellation of personal projects, sustained for weeks, is the kind of cadence that used to require a small team. One human plus AI agents plus the discipline of writing good issues hits it. The artifact, in the end, is not “the AI fixed 141 bugs.” The artifact is the dashboard — 28 repos getting visibly greener, with each cell on the heatmap a clickable link to the diff that closed it. The lab is legible, and being legible makes it possible to do the work at this pace in the first place. Where It Sits in the Personal-Software Toolkit sw-checklist keeps the shape of the code in line. sw-launcher keeps the shape of the load plan and memory budget in line. AgentRail keeps the shape of the work in line — one saga, one step at a time, with a faithful audit trail. The Status tab is the operational dashboard that makes the result of all three legible at a glance. None of those tools, individually, would be enough to keep a 37-repo lab maintainable by one person; together they make vibe-maintenance the steady-state mode of operation. The car is up on the lift. The mechanic is not building anything new today. The mechanic is going around with a torque wrench, an oil drain, and a parts list, and at the end of the afternoon every gauge is in the green again. AI agents do not change which afternoons that work happens on. They change how many cars fit in the shop.]]></summary></entry><entry><title type="html">Personal Software (9/?): One Ring to Rule Them All — sw-launcher’s Memory Profiles, Heap Budgets, and a Working Scenario A</title><link href="https://software-wrighter-lab.github.io/2026/04/28/personal-software-sw-launcher-one-ring/" rel="alternate" type="text/html" title="Personal Software (9/?): One Ring to Rule Them All — sw-launcher’s Memory Profiles, Heap Budgets, and a Working Scenario A" /><published>2026-04-28T12:00:00-07:00</published><updated>2026-04-28T12:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/28/personal-software-sw-launcher-one-ring</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/28/personal-software-sw-launcher-one-ring/"><![CDATA[<p><img src="/assets/images/posts/block-one-ring.png" class="post-marker no-invert" alt="" style="width: 220px;" /></p>

<div style="overflow: hidden;">

  <p>The sw-embed monorepos cover ten-plus languages targeting the same COR24 emulator: hand-written assembler, Forth, BASIC, Pascal, PL/SW, Macrolisp, OCaml-on-p-code, Smalltalk, Tuplet, Snobol4, APL — plus a resident-shell trio (monitor, script, yocto-ed) that does not look like a “language” at all but uses the same emulator and the same memory map. Every one of them solved the bottom-of-the-stack problem — <em>get the right bytes into the right addresses, in the right order, with the runtime patched to know where the upper layers live</em> — independently, with a hand-rolled <code class="language-plaintext highlighter-rouge">scripts/run-*.sh</code>. <a href="https://github.com/sw-cli-tools/sw-launcher">sw-launcher</a> is the personal-software CLI that consolidates that ritual into one declarative file with caching, validation, and a vendor lockfile. Phase 0 (the survey) is done, the schema has been revised <em>twice</em> in response to what the survey found, and Phase 1 (Scenario A end-to-end) actually runs.</p>

</div>

<div class="aside-box">

  <p><strong>Why this matters</strong> — AI coding agents working across multiple sw-embed repos do not have the patience or the pattern-matching to get the load plan right by inspection. They will happily write <code class="language-plaintext highlighter-rouge">cor24-run --load-binary out.bin@0 --load-binary app.p24@0x10000 --patch 0x12=0x10000 --entry 0</code> from scratch every time, sometimes inventing flags that don’t exist. The fix is not better agent prompts; it is removing the freedom to invent. <code class="language-plaintext highlighter-rouge">sw-launch run &lt;scenario&gt;</code> is the only verb the agent gets, the TOML is the only place memory-layout decisions live, and the schema makes oversized heaps argue for themselves before the validator accepts them.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repo</strong></td>
        <td><a href="https://github.com/sw-cli-tools/sw-launcher">sw-cli-tools/sw-launcher</a></td>
      </tr>
      <tr>
        <td><strong>13-repo survey</strong></td>
        <td><a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/survey/index.md">docs/survey/index.md</a> · <a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/survey/schema-gaps.md">schema-gaps.md</a></td>
      </tr>
      <tr>
        <td><strong>Memory stance</strong></td>
        <td><a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/memory-stance.md">docs/memory-stance.md</a> · <a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/heap-analysis.md">docs/heap-analysis.md</a></td>
      </tr>
      <tr>
        <td><strong>COR24 emulator</strong></td>
        <td><a href="https://github.com/sw-embed/cor24-rs">sw-embed/cor24-rs</a></td>
      </tr>
      <tr>
        <td><strong>Driven projects</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-pcode">sw-cor24-pcode</a> · <a href="https://github.com/sw-embed/sw-cor24-ocaml">sw-cor24-ocaml</a> · <a href="https://github.com/sw-embed/sw-cor24-pascal">sw-cor24-pascal</a> · <a href="https://github.com/sw-embed/sw-cor24-basic">sw-cor24-basic</a></td>
      </tr>
      <tr>
        <td><strong>Prior Personal Software post</strong></td>
        <td><a href="/2026/04/27/sw-checklist-ratchet-ai-coding-agents/">sw-checklist: Reining In AI Coding Agents With a Code-Metrics Ratchet</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-problem-every-language-has-its-own-loader">The Problem: Every Language Has Its Own Loader</h2>

<p>The COR24 is a 24-bit machine with 1 MiB of SRAM, a 3 KiB EBR hardware stack, and an MMIO aperture at <code class="language-plaintext highlighter-rouge">0xFF0000</code>. The host-side <code class="language-plaintext highlighter-rouge">cor24-run</code> emulator accepts a small surface — <code class="language-plaintext highlighter-rouge">--load-binary path@hex_addr</code>, <code class="language-plaintext highlighter-rouge">--patch hex_addr=hex_value</code>, <code class="language-plaintext highlighter-rouge">--uart-input "..."</code>, <code class="language-plaintext highlighter-rouge">--entry</code>, <code class="language-plaintext highlighter-rouge">--speed</code>, <code class="language-plaintext highlighter-rouge">-n</code> — and that’s the universe.</p>

<p>What changes between repos is <em>what gets loaded where</em>, <em>which runtime word has to be patched to point at the layer above it</em>, and <em>how source and data ride the UART</em>. The Phase 0 survey looked at thirteen working repos and aggregated the patterns. A few from the comparison table:</p>

<table>
  <thead>
    <tr>
      <th>repo</th>
      <th style="text-align: right">loads</th>
      <th style="text-align: right">patches</th>
      <th>UART src</th>
      <th>UART data</th>
      <th>heap</th>
      <th>stack</th>
      <th>approx SRAM</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>basic</td>
      <td style="text-align: right">1</td>
      <td style="text-align: right">0</td>
      <td>yes</td>
      <td>no</td>
      <td>emb</td>
      <td>hw EBR</td>
      <td>~64 KiB</td>
    </tr>
    <tr>
      <td>forth</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">0</td>
      <td>yes</td>
      <td>no</td>
      <td>emb</td>
      <td>hw EBR</td>
      <td>~256 KiB</td>
    </tr>
    <tr>
      <td>macrolisp</td>
      <td style="text-align: right">0–1</td>
      <td style="text-align: right">0</td>
      <td>yes</td>
      <td>snapshot</td>
      <td>emb</td>
      <td>hw EBR</td>
      <td>~512 KiB</td>
    </tr>
    <tr>
      <td>ocaml</td>
      <td style="text-align: right">2</td>
      <td style="text-align: right">2</td>
      <td>yes</td>
      <td>post-EOT</td>
      <td>emb+res</td>
      <td>emb in pvm</td>
      <td>~512 KiB</td>
    </tr>
    <tr>
      <td>pascal</td>
      <td style="text-align: right">1–2</td>
      <td style="text-align: right">1</td>
      <td>yes</td>
      <td>no</td>
      <td>emb</td>
      <td>emb</td>
      <td>~64 KiB</td>
    </tr>
    <tr>
      <td>plsw</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">0</td>
      <td>yes</td>
      <td>no</td>
      <td>emb</td>
      <td>hw EBR</td>
      <td>~1 MiB</td>
    </tr>
    <tr>
      <td>snobol4</td>
      <td style="text-align: right">1–3</td>
      <td style="text-align: right">0</td>
      <td>yes</td>
      <td>mode-flag</td>
      <td>emb</td>
      <td>emb</td>
      <td>~128 KiB</td>
    </tr>
    <tr>
      <td>monitor</td>
      <td style="text-align: right">many</td>
      <td style="text-align: right">0</td>
      <td>no</td>
      <td>no</td>
      <td>emb</td>
      <td>hw EBR</td>
      <td>~64 KiB</td>
    </tr>
    <tr>
      <td>tuplet</td>
      <td style="text-align: right">3</td>
      <td style="text-align: right">2</td>
      <td>yes</td>
      <td>image@0x080000</td>
      <td>res</td>
      <td>emb in pvm</td>
      <td>~768 KiB</td>
    </tr>
  </tbody>
</table>

<p>Every project’s <code class="language-plaintext highlighter-rouge">scripts/run-*.sh</code> re-encodes one of these shapes. None of them validate. None of them cache. None of them notice when the OCaml heap and the DSL heap overlap. And every AI agent that touches these scripts adds its own subtle variation, because the shell script is the spec.</p>

<h2 id="two-axes-five-shapes">Two Axes, Five Shapes</h2>

<p>The original PRD assumed one axis with three points (A: single image, B: runtime+image, C: nested interpreter). The survey says it is actually two axes:</p>

<ul>
  <li><strong>Build axis</strong>: hand-written assembly, compiled from a higher-level language, snapshot rehydrated by host tooling, or composite of N modules linked host-side.</li>
  <li><strong>Run axis</strong>: one-shot batch (kick off and check UART), interactive REPL through UART, interactive shell with a resident process model, or edit-then-run via a resident editor.</li>
</ul>

<p>The cross product yields five primitive shapes that cover everything sw-embed has written so far. The first three were already in the day-zero design; the last two emerged from the survey:</p>

<ol>
  <li><strong>Single image at zero, UART source.</strong> Heap and stack embedded in the image. (apl, basic, forth, plsw, smalltalk-delegated.)</li>
  <li><strong>Runtime + image + patch.</strong> Native COR24 runtime at 0 plus a p-code image at a higher address with a <code class="language-plaintext highlighter-rouge">code_ptr</code>-style patch. (pascal single-unit and multi-unit, the OCaml/tuplet pattern without the heap patch.)</li>
  <li><strong>Nested interpreter with heap-limit patch and UART-after-EOT data.</strong> Adds a second patch (heap limit), and the UART payload is <code class="language-plaintext highlighter-rouge">&lt;source&gt; + EOT + &lt;runtime data&gt;</code>. (ocaml, tuplet.)</li>
  <li><strong>Multi-module composite image.</strong> The launcher loads N independently assembled modules at contiguous bases (snobol4 via <code class="language-plaintext highlighter-rouge">link24</code>) or at fixed slot addresses (macrolisp’s multi-module demo, monitor’s program registry). Linking happens host-side, not via patches.</li>
  <li><strong>Resident shell + paste-and-go.</strong> Monitor at 0, sws shell at <code class="language-plaintext highlighter-rouge">0x20000</code>, programs at fixed slots, all preloaded together; transfer of control happens <em>inside</em> the emulator via a service-vector / trampoline (<code class="language-plaintext highlighter-rouge">mon_invoke_program</code>) and never returns to the host runner. (monitor, script, yocto-ed.)</li>
</ol>

<p>A scenario picks one shape; the schema makes that pick explicit instead of implied by which shell script you happen to run.</p>

<h2 id="schema-v11-partition-grid-considered-then-rejected">Schema v1.1: Partition Grid (Considered, Then Rejected)</h2>

<p>The first revision after the survey, schema v1.1, divided the 1 MiB SRAM into eight fixed <strong>partitions</strong> of 128 KiB and four <strong>regions</strong> per partition (<code class="language-plaintext highlighter-rouge">code</code>/<code class="language-plaintext highlighter-rouge">heap</code>/<code class="language-plaintext highlighter-rouge">spare</code>/<code class="language-plaintext highlighter-rouge">stack</code>, 32 KiB each). Most existing repos already align to obvious partition boundaries (<code class="language-plaintext highlighter-rouge">0x000000</code>, <code class="language-plaintext highlighter-rouge">0x010000</code>, <code class="language-plaintext highlighter-rouge">0x040000</code>, <code class="language-plaintext highlighter-rouge">0x080000</code>, <code class="language-plaintext highlighter-rouge">0x0F0000</code>), so re-stating those addresses in <code class="language-plaintext highlighter-rouge">(partition, region)</code> coordinates was mostly a labeling change.</p>

<p>It was the wrong move. The grid canonized a <em>layout</em> without taking a position on the <em>budgets</em>, which let oversized heaps express themselves as multi-cell claims and call it normal:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># v1.1: OCaml's 252 KiB heap, expressed as four contiguous cells.</span>
<span class="c"># Schema accepts it, validator passes, nothing argues back.</span>
<span class="k">[</span><span class="n">layers</span><span class="k">.</span><span class="n">ocaml_interp</span><span class="k">.</span><span class="n">segments</span><span class="k">.</span><span class="n">value_heap</span><span class="k">]</span>
<span class="n">kind</span>   <span class="o">=</span><span class="w"> </span><span class="s">"heap"</span>
<span class="n">grows</span>  <span class="o">=</span><span class="w"> </span><span class="s">"down"</span>
<span class="n">claims</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span>
  <span class="p">{</span><span class="w"> </span><span class="n">partition</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="n">region</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"spare"</span><span class="w"> </span><span class="p">},</span>
  <span class="p">{</span><span class="w"> </span><span class="n">partition</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="n">region</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"stack"</span><span class="w"> </span><span class="p">},</span>
  <span class="p">{</span><span class="w"> </span><span class="n">partition</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="n">region</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"code"</span><span class="w">  </span><span class="p">},</span>
  <span class="p">{</span><span class="w"> </span><span class="n">partition</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="n">region</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"heap"</span><span class="w">  </span><span class="p">},</span>
<span class="p">]</span>
</code></pre></div></div>

<p>That’s the OCaml interpreter’s current <code class="language-plaintext highlighter-rouge">heap_limit = 0x03F000</code> written as a partition-cell list. Pinning down the layout this way looked like progress. It was actually normalization of the bug.</p>

<h2 id="schema-v12-memory-profiles--heap-budgets">Schema v1.2: Memory Profiles + Heap Budgets</h2>

<p>The second revision flipped the prior. From <a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/memory-stance.md"><code class="language-plaintext highlighter-rouge">docs/memory-stance.md</code></a>:</p>

<blockquote>
  <p>The COR24 board emulator targets 1 MiB SRAM. That is <em>more</em>, not less, than every machine these re-implemented languages were originally designed for: Forth in 4–16 KiB, BASIC in 4 KiB (Altair) to 32 KiB (MS BASIC for IBM PC), APL/360 in &lt;128 KiB per partition, Smalltalk-72/76 in 128–512 KiB <em>including the bitmap display</em>, Macrolisp on a PDP-10 with 256 KiB <em>total</em>. The IBM PC shipped in 1981 with 16–256 KiB. By 1985 measure, 1 MiB and a tiny monitor is a luxurious environment.</p>
</blockquote>

<p>If macrolisp on a PDP-10 fit in 256 KiB total — runtime, interpreter, and program — then the COR24 macrolisp’s ~288 KiB <em>heap</em> is not a constraint problem. Something has gone soft. The 1 MiB ceiling does not need to be raised. The heaps need to be shrunk.</p>

<p>v1.2 makes that the schema’s stance. Three concrete changes:</p>

<p><strong>1. The fixed grid is gone.</strong> Replaced with <strong>named memory profiles</strong>. Each profile is an ordered list of partitions of arbitrary size, each with its own list of named regions of arbitrary kind and size, plus a <em>budget</em> block:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">memory_profiles</span><span class="k">.</span><span class="n">compiled-app</span><span class="k">]</span>
<span class="n">description</span> <span class="o">=</span><span class="w"> </span><span class="s">"Single image at 0; small heap; small stack."</span>

<span class="k">[[</span><span class="n">memory_profiles</span><span class="k">.</span><span class="n">compiled-app</span><span class="k">.</span><span class="n">partitions</span><span class="k">]]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"code"</span>
<span class="n">base</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x000000"</span>
<span class="n">size</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x010000"</span>            <span class="c"># 64 KiB</span>
<span class="n">regions</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span>
  <span class="p">{</span><span class="w"> </span><span class="n">name</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"code"</span><span class="p">,</span><span class="w">   </span><span class="n">kind</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"code"</span><span class="p">,</span><span class="w">  </span><span class="n">size</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"auto"</span><span class="w"> </span><span class="p">},</span>
  <span class="p">{</span><span class="w"> </span><span class="n">name</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"static"</span><span class="p">,</span><span class="w"> </span><span class="n">kind</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"data"</span><span class="p">,</span><span class="w">  </span><span class="n">size</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"auto"</span><span class="w"> </span><span class="p">},</span>
<span class="p">]</span>

<span class="k">[[</span><span class="n">memory_profiles</span><span class="k">.</span><span class="n">compiled-app</span><span class="k">.</span><span class="n">partitions</span><span class="k">]]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"heap"</span>
<span class="n">base</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x010000"</span>
<span class="n">size</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x008000"</span>            <span class="c"># 32 KiB</span>
<span class="n">regions</span> <span class="o">=</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="n">name</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"heap"</span><span class="p">,</span><span class="w"> </span><span class="n">kind</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"heap"</span><span class="p">,</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"0x008000"</span><span class="w"> </span><span class="p">}]</span>

<span class="k">[</span><span class="n">memory_profiles</span><span class="k">.</span><span class="n">compiled-app</span><span class="k">.</span><span class="n">budget</span><span class="k">]</span>
<span class="n">code_max</span>  <span class="o">=</span><span class="w"> </span><span class="s">"0x008000"</span>       <span class="c"># 32 KiB</span>
<span class="n">heap_max</span>  <span class="o">=</span><span class="w"> </span><span class="s">"0x004000"</span>       <span class="c"># 16 KiB</span>
<span class="n">stack_max</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x002000"</span>       <span class="c"># 8 KiB</span>
<span class="n">total_max</span> <span class="o">=</span><span class="w"> </span><span class="s">"0x010000"</span>       <span class="c"># 64 KiB</span>
<span class="n">justification_required</span> <span class="o">=</span><span class="w"> </span><span class="kc">true</span>
</code></pre></div></div>

<p><strong>2. Five default profiles ship with the launcher</strong>, each sized per <a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/heap-analysis.md"><code class="language-plaintext highlighter-rouge">docs/heap-analysis.md</code></a>:</p>

<table>
  <thead>
    <tr>
      <th>profile</th>
      <th>code+data</th>
      <th>heap</th>
      <th>stack</th>
      <th>total</th>
      <th>example use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">compiled-app</code></td>
      <td>&lt;= 32 KiB</td>
      <td>&lt;= 16 KiB</td>
      <td>&lt;= 8 KiB</td>
      <td>&lt;= 64 KiB</td>
      <td>BASIC echo program</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">interpreter-only</code></td>
      <td>&lt;= 64 KiB</td>
      <td>&lt;= 64 KiB</td>
      <td>&lt;= 16 KiB</td>
      <td>&lt;= 160 KiB</td>
      <td>APL, Forth, Smalltalk</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">repl-inline-compile</code></td>
      <td>&lt;= 128 KiB</td>
      <td>&lt;= 256 KiB</td>
      <td>&lt;= 32 KiB</td>
      <td>&lt;= 448 KiB</td>
      <td>OCaml + GC, Tuplet (post-fix)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">compiler-image</code></td>
      <td>&lt;= 256 KiB</td>
      <td>&lt;= 64 KiB</td>
      <td>&lt;= 32 KiB</td>
      <td>&lt;= 384 KiB</td>
      <td>PL/SW (post-fix)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">resident-shell</code></td>
      <td>&lt;= 64 KiB per slot, up to 8 slots</td>
      <td>per-program</td>
      <td>shared 8 KiB</td>
      <td>&lt;= 512 KiB</td>
      <td>monitor + sws + N programs</td>
    </tr>
  </tbody>
</table>

<p>A scenario picks a profile by name; the validator enforces that profile’s budget. Layers cite partitions and regions by <em>name</em>, not by hex.</p>

<p><strong>3. Heaps over 32 KiB must argue for themselves</strong> through a <code class="language-plaintext highlighter-rouge">heap_justification</code> block:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">layers</span><span class="k">.</span><span class="n">ocaml_interp</span><span class="k">.</span><span class="n">heap_justification</span><span class="k">]</span>
<span class="n">category</span> <span class="o">=</span><span class="w"> </span><span class="s">"gc-slack"</span>
<span class="n">note</span>     <span class="o">=</span><span class="w"> </span><span class="s">"Mark/sweep GC; sized for working set + 2x slack."</span>
<span class="n">measured_floor_kib</span> <span class="o">=</span><span class="w"> </span><span class="mi">64</span>
<span class="n">tracking_issue</span>     <span class="o">=</span><span class="w"> </span><span class="s">"sw-cor24-ocaml#28"</span>
</code></pre></div></div>

<p>Five categories, in roughly descending order of merit:</p>

<table>
  <thead>
    <tr>
      <th>category</th>
      <th>accepted?</th>
      <th>meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">algorithmic-floor</code></td>
      <td>yes</td>
      <td>Working set genuinely requires this size.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">bytecode-image</code></td>
      <td>yes</td>
      <td>Heap is mostly read-only data, not allocations.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gc-slack</code></td>
      <td>yes (with <code class="language-plaintext highlighter-rouge">measured_floor_kib</code>)</td>
      <td>Sized for floor + slack between collections.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">dead-leak</code></td>
      <td>warn; rejected by <code class="language-plaintext highlighter-rouge">--strict</code></td>
      <td>Allocations that never get freed.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">algorithmic-bloat</code></td>
      <td>warn; rejected by <code class="language-plaintext highlighter-rouge">--strict</code></td>
      <td>Pointer width, boxing, dispatch tables, etc.</td>
    </tr>
  </tbody>
</table>

<p>The default category for an undocumented oversized heap is <code class="language-plaintext highlighter-rouge">dead-leak</code> — because the heap-analysis pass found that all three of the demanding repos (ocaml, macrolisp, plsw) match exactly that pattern, and the first job of a budget is to refuse to normalize them.</p>

<h2 id="what-the-heap-analysis-found">What the Heap Analysis Found</h2>

<p><a href="https://github.com/sw-cli-tools/sw-launcher/blob/main/docs/heap-analysis.md"><code class="language-plaintext highlighter-rouge">docs/heap-analysis.md</code></a> walks every repo with a claimed heap &gt; 32 KiB and assigns it a category, a historical benchmark, and a shrinkage backlog:</p>

<table>
  <thead>
    <tr>
      <th>repo</th>
      <th>current claim</th>
      <th>category</th>
      <th>historical floor</th>
      <th>post-fix target</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ocaml</td>
      <td>~252 KiB heap</td>
      <td>dead-leak</td>
      <td>OCaml-on-PDP-10 &lt; 256 KiB <em>total</em> (1973)</td>
      <td>&lt;= 64 KiB after GC</td>
    </tr>
    <tr>
      <td>tuplet</td>
      <td>inherits ocaml</td>
      <td>dead-leak</td>
      <td>n/a (downstream)</td>
      <td>shrinks with ocaml</td>
    </tr>
    <tr>
      <td>macrolisp</td>
      <td>~288 KiB BSS</td>
      <td>dead-leak + bloat</td>
      <td>Maclisp 256 KiB <em>total</em></td>
      <td>&lt;= 64 KiB heap</td>
    </tr>
    <tr>
      <td>plsw</td>
      <td>~1 MiB image</td>
      <td>algorithmic-bloat</td>
      <td>UCSD Pascal in 64 KiB; Turbo Pascal 1.0 in 33.5 KiB</td>
      <td>&lt;= 256 KiB image</td>
    </tr>
    <tr>
      <td>snobol4</td>
      <td>~76 KiB internal</td>
      <td>floor + dead-leak</td>
      <td>SNOBOL4 in 64–256 KiB <em>total</em></td>
      <td>&lt;= 64 KiB</td>
    </tr>
    <tr>
      <td>forth</td>
      <td>dictionary</td>
      <td>algorithmic-floor</td>
      <td>Forth kernels in 4–16 KiB</td>
      <td>&lt;= 16 KiB typical</td>
    </tr>
    <tr>
      <td>basic</td>
      <td>embedded DIM 64–128 KiB</td>
      <td>algorithmic-floor</td>
      <td>Altair 4K BASIC</td>
      <td>&lt;= 32 KiB</td>
    </tr>
  </tbody>
</table>

<p>OCaml’s GC work in <code class="language-plaintext highlighter-rouge">sw-cor24-ocaml#28</code> is the one in flight. After it lands, tuplet’s <code class="language-plaintext highlighter-rouge">heap_limit</code> should <em>shrink</em>, not stay where it is. Macrolisp’s mark byte should be a mark <em>bit</em> (8x reduction). PL/SW’s compiler-output redundancy is fixable in one pass through the transpiler. The schema must support the current sizes transitionally, but the analysis doc must not normalize them. The current sizes are evidence of work to do; not the spec for the launcher.</p>

<h2 id="layers-are-composites-not-blobs">Layers Are Composites, Not Blobs</h2>

<p>Every other piece of the schema survives both revisions. A layer is still <code class="language-plaintext highlighter-rouge">(artifact?) + (segments)</code>, and segments still have lifecycles:</p>

<ul>
  <li><strong>Embedded</strong> segments live inside the artifact. <code class="language-plaintext highlighter-rouge">pvm.s</code> reserves <code class="language-plaintext highlighter-rouge">eval_stack</code>, <code class="language-plaintext highlighter-rouge">call_stack</code>, and a small <code class="language-plaintext highlighter-rouge">heap_seg</code> statically; they are part of <code class="language-plaintext highlighter-rouge">pvm.bin</code> and the loader does not allocate them again, but the validator has to <em>know</em> they exist (resolved through the artifact’s listing) so the global overlap check sees them.</li>
  <li><strong>Reserved</strong> segments are allocated by the loader at a configured cell with a configured size and zero-filled. The OCaml interpreter’s value heap, the Pascal eval stack, the DSL heap on top — none of these fit in the COR24’s 3 KiB EBR; they live in high SRAM cells and the runtime gets <em>patched</em> to point at them.</li>
  <li><strong>Patched</strong> is the verb that ties the two together. A reserved segment with <code class="language-plaintext highlighter-rouge">value = "self.address"</code> (or <code class="language-plaintext highlighter-rouge">self.end</code> for down-growing heaps) tells the loader to write its own resolved address into the runtime’s <code class="language-plaintext highlighter-rouge">heap_base</code> / <code class="language-plaintext highlighter-rouge">heap_limit</code> symbol, so the runtime knows where to find the heap the loader just allocated for it.</li>
</ul>

<p>Patches in v1.2 also accept two value forms that the survey demanded:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">value = "sidecar:&lt;path&gt;"</code> reads a build-time-resolved address from a small text file. ocaml and tuplet today write <code class="language-plaintext highlighter-rouge">build/code_ptr_addr.txt</code> and <code class="language-plaintext highlighter-rouge">build/heap_limit_addr.txt</code> during their build; the schema makes that explicit and includes the sidecar in the cache key.</li>
  <li><code class="language-plaintext highlighter-rouge">target = "&lt;upstream-layer&gt;.&lt;symbol&gt;"</code> lets a downstream layer reference a symbol in an upstream layer’s listing — including upstream layers from a <em>vendored</em> repo (Phase 2 step 002 just landed the resolver for this; tuplet wants pvm symbols from sw-cor24-ocaml’s build, not its own).</li>
</ul>

<h2 id="the-wizards-spellbook--sw-launchtoml">The Wizard’s Spellbook — sw-launch.toml</h2>

<div class="gutter-section">

  <p><img src="/assets/images/posts/block-wizard.png" class="gutter-img-right no-invert" alt="Wizard with staff" style="width: 18%;" /></p>

  <p>The whole config is one file at the project root. A trimmed Scenario A example, written against the <code class="language-plaintext highlighter-rouge">compiled-app</code> profile, that <em>actually runs end-to-end</em> on a real <code class="language-plaintext highlighter-rouge">cor24-run</code>:</p>

  <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">scenarios</span><span class="k">.</span><span class="n">echo</span><span class="k">]</span>
<span class="n">target</span>         <span class="o">=</span><span class="w"> </span><span class="s">"cor24"</span>
<span class="n">memory_profile</span> <span class="o">=</span><span class="w"> </span><span class="s">"compiled-app"</span>
<span class="n">layers</span>         <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"program"</span><span class="p">,</span> <span class="s">"stdin"</span><span class="p">]</span>
<span class="n">entry</span>          <span class="o">=</span><span class="w"> </span><span class="s">"0x000000"</span>

<span class="k">[</span><span class="n">scenarios</span><span class="k">.</span><span class="n">echo</span><span class="k">.</span><span class="n">run</span><span class="k">]</span>
<span class="n">timeout_ms</span> <span class="o">=</span><span class="w"> </span><span class="mi">2000</span>
<span class="n">max_cycles</span> <span class="o">=</span><span class="w"> </span><span class="mi">200_000</span>
<span class="n">halt_on</span>    <span class="o">=</span><span class="w"> </span><span class="s">"uart-eot"</span>

<span class="k">[</span><span class="n">scenarios</span><span class="k">.</span><span class="n">echo</span><span class="k">.</span><span class="n">expect</span><span class="k">]</span>
<span class="n">uart_contains</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"A"</span><span class="p">]</span>
<span class="n">exit_code</span>     <span class="o">=</span><span class="w"> </span><span class="mi">0</span>

<span class="k">[</span><span class="n">layers</span><span class="k">.</span><span class="n">program</span><span class="k">]</span>
<span class="n">kind</span>     <span class="o">=</span><span class="w"> </span><span class="s">"assembler"</span>
<span class="n">source</span>   <span class="o">=</span><span class="w"> </span><span class="s">"local"</span>
<span class="n">input</span>    <span class="o">=</span><span class="w"> </span><span class="s">"src/echo.s"</span>
<span class="n">tool</span>     <span class="o">=</span><span class="w"> </span><span class="s">"assembler"</span>
<span class="n">artifact</span> <span class="o">=</span><span class="w"> </span><span class="s">"echo.bin"</span>

<span class="k">[</span><span class="n">layers</span><span class="k">.</span><span class="n">program</span><span class="k">.</span><span class="n">segments</span><span class="k">.</span><span class="n">code</span><span class="k">]</span>
<span class="n">kind</span>   <span class="o">=</span><span class="w"> </span><span class="s">"code"</span>
<span class="n">claims</span> <span class="o">=</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="n">partition</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"code"</span><span class="p">,</span><span class="w"> </span><span class="n">region</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"code"</span><span class="w"> </span><span class="p">}]</span>

<span class="k">[</span><span class="n">layers</span><span class="k">.</span><span class="n">stdin</span><span class="k">]</span>
<span class="n">kind</span> <span class="o">=</span><span class="w"> </span><span class="s">"data"</span>
<span class="n">input</span> <span class="o">=</span><span class="w"> </span><span class="s">"tests/echo-input.txt"</span>
<span class="n">load</span><span class="p">.</span><span class="n">method</span> <span class="o">=</span><span class="w"> </span><span class="s">"uart"</span>
<span class="n">load</span><span class="p">.</span><span class="n">max_bytes</span> <span class="o">=</span><span class="w"> </span><span class="mi">1024</span>
</code></pre></div>  </div>

  <p><code class="language-plaintext highlighter-rouge">sw-launch run echo</code> walks the layer DAG, builds each layer (or pulls it from the in-process memoization cache), assembles the load plan, invokes <code class="language-plaintext highlighter-rouge">cor24-run</code>, captures UART output, and checks the expectations. Today this prints <code class="language-plaintext highlighter-rouge">A</code> and exits 0. The CLI surface is small enough to memorize:</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sw-launch run     &lt;scenario&gt;    Build (with cache) and execute, check expectations.
sw-launch build   &lt;scenario&gt;    Build all layers; do not execute.
sw-launch check   &lt;scenario&gt;    Validate config + lock; no tools run.
sw-launch graph   &lt;scenario&gt;    Print layer DAG (text or --json).
sw-launch cache   list          List cached artifacts.
sw-launch vendor  sync          Resolve and pin all dependencies.
sw-launch doctor                Verify host tools (cor24-run, pa24r, pl24r) found.
</code></pre></div>  </div>

  <p>Every flag agents used to invent is now either a TOML field or a <code class="language-plaintext highlighter-rouge">--profile</code>. There is no <code class="language-plaintext highlighter-rouge">--load-binary</code> to mistype.</p>

</div>

<h2 id="phase-1-what-runs-today">Phase 1: What Runs Today</h2>

<p>The Phase 1 saga (<code class="language-plaintext highlighter-rouge">sw-launcher-phase1</code>, ten steps) closed clean on April 28. End state:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">sw-launch run echo --config tests/fixtures/scenario_a/sw-launch.toml</code> exits 0 and prints <code class="language-plaintext highlighter-rouge">A</code> (the captured UART output).</li>
  <li><code class="language-plaintext highlighter-rouge">sw-launch check echo</code> validates the scenario without spawning the emulator.</li>
  <li><code class="language-plaintext highlighter-rouge">sw-launch build echo</code> assembles every assembler-kind layer under <code class="language-plaintext highlighter-rouge">&lt;config-dir&gt;/.sw-launch/build/&lt;scenario&gt;/&lt;layer&gt;/</code> with a sha256-keyed in-process memoization cache.</li>
  <li>60 tests across 11 binaries, including 2 end-to-end against the real <code class="language-plaintext highlighter-rouge">cor24-run</code> binary, all green.</li>
  <li><code class="language-plaintext highlighter-rouge">validate.rs</code> implements 17 stable error codes, each with a negative test asserting the exact code and span.</li>
</ul>

<p>The integration tests ran against <code class="language-plaintext highlighter-rouge">cor24-run 0.1.0</code>, <code class="language-plaintext highlighter-rouge">rustc 1.94.1</code>, edition 2024, on Darwin 24.6.0. Every one of those versions is recorded in the repo’s <code class="language-plaintext highlighter-rouge">status.md</code> so future-me knows what “Phase 1 worked” actually meant.</p>

<p>Phase 2 (<code class="language-plaintext highlighter-rouge">sw-launcher-phase2</code>) is open and seeded with five steps; step 001 (PCode tool with SourceSpec resolution) and step 002 (cross-layer listing-symbol patches resolve at scenario validate time) just landed. Phase 2’s target is end-to-end Scenario B: COR24 runtime at 0 plus a p-code blob at a higher address with a <code class="language-plaintext highlighter-rouge">code_ptr</code> patch — the smallest meaningful test that the launcher can express the layered shape that today’s <code class="language-plaintext highlighter-rouge">sw-cor24-pcode</code> and <code class="language-plaintext highlighter-rouge">sw-cor24-pascal</code> demos use.</p>

<h2 id="validation-catch-collisions-before-the-emulator-does">Validation: Catch Collisions Before the Emulator Does</h2>

<p>The validator is the most important verb. <code class="language-plaintext highlighter-rouge">sw-launch check</code> reads the TOML and the lockfile, walks every segment of every layer in the scenario, computes absolute address ranges (resolving profile partitions to addresses, embedded segments through the producing layer’s listing, sidecars from disk), and runs the rules. Each rule has a stable error code so agents can match on it without scraping prose.</p>

<p>A subset, selected for what they catch:</p>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Rule</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>E0003</td>
      <td>No two memory ranges overlap (across embedded <em>and</em> reserved segments).</td>
    </tr>
    <tr>
      <td>E0004</td>
      <td>UART layers declare <code class="language-plaintext highlighter-rouge">max_bytes</code> and the input fits.</td>
    </tr>
    <tr>
      <td>E0005</td>
      <td>Layer kind and load method are compatible.</td>
    </tr>
    <tr>
      <td>E0006</td>
      <td>Every patch resolves — symbol exists, segment exists, topo order is right.</td>
    </tr>
    <tr>
      <td>E0011</td>
      <td>Reserved <code class="language-plaintext highlighter-rouge">stack</code>/<code class="language-plaintext highlighter-rouge">heap</code>/<code class="language-plaintext highlighter-rouge">bss</code> lies inside <code class="language-plaintext highlighter-rouge">regions.sram</code>, never touches <code class="language-plaintext highlighter-rouge">regions.ebr_stack</code> or <code class="language-plaintext highlighter-rouge">regions.mmio</code>.</td>
    </tr>
    <tr>
      <td>E0014</td>
      <td><code class="language-plaintext highlighter-rouge">embedded = true</code> segments declare a <code class="language-plaintext highlighter-rouge">symbol</code> that resolves in the producing layer’s listing.</td>
    </tr>
    <tr>
      <td>E0023</td>
      <td>Resident-mode mismatch (a layer claims a slot the resident shell doesn’t expose).</td>
    </tr>
    <tr>
      <td>E0028</td>
      <td>Heap-budget overshoot (heap exceeds the profile’s <code class="language-plaintext highlighter-rouge">heap_max</code>).</td>
    </tr>
    <tr>
      <td>E0029</td>
      <td>Heap &gt;= 80% of <code class="language-plaintext highlighter-rouge">heap_max</code> (warn).</td>
    </tr>
    <tr>
      <td>E0030</td>
      <td>Missing <code class="language-plaintext highlighter-rouge">heap_justification</code> for heap &gt; 32 KiB.</td>
    </tr>
    <tr>
      <td>E0031</td>
      <td>Layer cites a partition or region the profile does not declare.</td>
    </tr>
    <tr>
      <td>E0032</td>
      <td>Profile self-overlap (the profile’s own partitions collide).</td>
    </tr>
    <tr>
      <td>E0033</td>
      <td>Total claimed SRAM &gt; 1 MiB.</td>
    </tr>
    <tr>
      <td>E0034</td>
      <td><code class="language-plaintext highlighter-rouge">heap_justification.category = "dead-leak"</code> or <code class="language-plaintext highlighter-rouge">"algorithmic-bloat"</code> under <code class="language-plaintext highlighter-rouge">--strict</code>.</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">--strict</code> mode promotes most warnings to errors and <em>always</em> rejects <code class="language-plaintext highlighter-rouge">dead-leak</code> and <code class="language-plaintext highlighter-rouge">algorithmic-bloat</code> justifications. CI runs strict; local <code class="language-plaintext highlighter-rouge">check</code> runs lax so the shrinkage work can land incrementally without breaking the build.</p>

<h2 id="caching-vendoring-and-the-lockfile">Caching, Vendoring, and the Lockfile</h2>

<p>The cost of <em>not</em> caching is that every test re-assembles <code class="language-plaintext highlighter-rouge">pvm.s</code>, every demo re-builds the host toolchain, every CI run wastes minutes. Phase 1 ships an in-process memoization cache (sha256 keyed on <code class="language-plaintext highlighter-rouge">(input bytes, tool version, args, output filename)</code>) inside the assembler tool wrapper; Phase 4 will lift that to a persistent on-disk cache at <code class="language-plaintext highlighter-rouge">~/.cache/sw-launch/</code>. The key formula already accounts for the hard cases:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>layer_key = sha256(
    schema_version
  | normalize_toml(layer_config)
  | hash_each(input_files)
  | tool_version_hash
  | dependency_layer_hashes (in topo order)
  | resolved_address_or_uart_marker
  | sidecar_contents (if any)
)
</code></pre></div></div>

<p>A cache hit is <em>sound</em> — if the key matches, the artifact would be byte-identical to a fresh build, so reusing it can never produce a different scenario result. Sidecar contents are in the key because v1.2’s <code class="language-plaintext highlighter-rouge">value = "sidecar:..."</code> patch source needs the cache to invalidate when the sidecar changes.</p>

<p>The vendor side is symmetric. The survey was unflattering — ten of thirteen repos pin nothing at all, and one (tuplet) inherits its pins transitively from sw-cor24-ocaml. Only sw-cor24-ocaml has a real <code class="language-plaintext highlighter-rouge">vendor/&lt;tool&gt;/&lt;version&gt;/active.env</code> model with commit SHAs. v1.2 makes the OCaml-style vendored model the default: <code class="language-plaintext highlighter-rouge">sw-launch vendor sync</code> (Phase 4) will resolve declared dependencies (<code class="language-plaintext highlighter-rouge">sibling:</code>, <code class="language-plaintext highlighter-rouge">vendor:</code>, eventually <code class="language-plaintext highlighter-rouge">git:</code>) and write <code class="language-plaintext highlighter-rouge">sw-launch.lock</code> with each artifact’s commit hash and SHA. <code class="language-plaintext highlighter-rouge">sw-launch doctor</code> will record the <em>observed</em> version of each PATH-resolved tool too, so drift is visible even when nothing is explicitly pinned.</p>

<h2 id="what-this-buys">What This Buys</h2>

<p>The point is not that the TOML is shorter than the shell script — it is often longer. The point is what <em>changes</em> about the system:</p>

<ol>
  <li><strong>One verb for agents.</strong> <code class="language-plaintext highlighter-rouge">sw-launch run &lt;scenario&gt;</code> replaces the ten variants of <code class="language-plaintext highlighter-rouge">cor24-run --load-binary ...</code> that agents kept reinventing.</li>
  <li><strong>Validation before the emulator.</strong> The most expensive failure mode — “ran for forty seconds, traps silently, zero output” — is replaced by a fast <code class="language-plaintext highlighter-rouge">check</code> that names the colliding region and the offending TOML span.</li>
  <li><strong>Heaps argue for themselves.</strong> A 252 KiB heap is not waved through; it has to declare a category, a measured floor, and a tracking issue. <code class="language-plaintext highlighter-rouge">--strict</code> rejects <code class="language-plaintext highlighter-rouge">dead-leak</code> and <code class="language-plaintext highlighter-rouge">algorithmic-bloat</code> outright.</li>
  <li><strong>A schema for memory layout.</strong> Reserved heaps, embedded stacks, profile-named partitions, sidecar patches, cross-layer symbol references — all first-class in TOML. A new language port describes its memory shape; it does not write a new shell loader.</li>
  <li><strong>Vendor versions visible.</strong> Sibling-repo dependencies are pinned by commit hash in the lockfile, not by “whatever was checked out at 3pm.” PATH-resolved tools have their observed versions recorded too.</li>
  <li><strong>Phase 1 actually runs.</strong> The “what runs today” section is not a roadmap; it is what <code class="language-plaintext highlighter-rouge">cargo test</code> exercises, end-to-end, against a real emulator.</li>
</ol>

<p>The wizard metaphor still fits the post marker: thirteen repos, each with its own ring of power, all in the end answering to one. sw-launcher is <em>the one ring</em> in the boring sense — the one place to encode the load plan — not in the corrupting sense, hopefully. But the more accurate metaphor for v1.2 is that the schema is a <em>budget officer</em>: every heap that wants more than 32 KiB has to file paperwork, and the default verdict on the paperwork is “this is a leak; prove otherwise.”</p>

<h2 id="where-it-sits-in-the-personal-software-toolkit">Where It Sits in the Personal-Software Toolkit</h2>

<p><a href="/2026/04/27/sw-checklist-ratchet-ai-coding-agents/">sw-checklist</a>, the subject of the previous post in this series, sits at a different layer of the same problem: AI agents working alone produce too much variety in places where uniformity is cheaper. sw-checklist constrains the <em>shape of the code</em> (function/file/module/crate size limits); sw-launcher constrains the <em>shape of the load plan and the memory budget</em> (which addresses, which patches, how big a heap can grow before it has to file paperwork). Both are accidental complexity in Brooks’s sense. Both are paying rent.</p>

<p>Phase 0 (survey) and Phase 1 (Scenario A end-to-end) are done. Schema v1.1 was a wrong turn that taught the right lesson: do not canonize the layout without taking a position on the budgets. Schema v1.2 takes that position, in writing, with five default profiles sized against historical implementations from the 1970s and 1980s. Phase 2 (Scenario B, runtime + p-code blob with cross-layer patches) is in flight; Phase 3 brings Scenario C (nested interpreter, heap-limit-only patches — the tuplet shape); Phase 4 brings persistent caching and vendor sync; Phase 5 brings the resident-shell composite (D + E).</p>

<p>The wizard is just the post marker. The one ring is just the metaphor. The interesting part is the budget officer.</p>]]></content><author><name>Software Wrighter</name></author><category term="cli-tools" /><category term="rust" /><category term="embedded" /><category term="sw-launcher" /><category term="personal-software" /><category term="rust" /><category term="cli" /><category term="cor24" /><category term="sw-embed" /><category term="emulator" /><category term="memory-layout" /><category term="memory-profiles" /><category term="heap-budgets" /><category term="p-code" /><category term="vendoring" /><category term="agentrail" /><category term="ai-coding" /><summary type="html"><![CDATA[The sw-embed monorepos cover ten-plus languages targeting the same COR24 emulator: hand-written assembler, Forth, BASIC, Pascal, PL/SW, Macrolisp, OCaml-on-p-code, Smalltalk, Tuplet, Snobol4, APL — plus a resident-shell trio (monitor, script, yocto-ed) that does not look like a “language” at all but uses the same emulator and the same memory map. Every one of them solved the bottom-of-the-stack problem — get the right bytes into the right addresses, in the right order, with the runtime patched to know where the upper layers live — independently, with a hand-rolled scripts/run-*.sh. sw-launcher is the personal-software CLI that consolidates that ritual into one declarative file with caching, validation, and a vendor lockfile. Phase 0 (the survey) is done, the schema has been revised twice in response to what the survey found, and Phase 1 (Scenario A end-to-end) actually runs. Why this matters — AI coding agents working across multiple sw-embed repos do not have the patience or the pattern-matching to get the load plan right by inspection. They will happily write cor24-run --load-binary out.bin@0 --load-binary app.p24@0x10000 --patch 0x12=0x10000 --entry 0 from scratch every time, sometimes inventing flags that don’t exist. The fix is not better agent prompts; it is removing the freedom to invent. sw-launch run &lt;scenario&gt; is the only verb the agent gets, the TOML is the only place memory-layout decisions live, and the schema makes oversized heaps argue for themselves before the validator accepts them. Resource Link Repo sw-cli-tools/sw-launcher 13-repo survey docs/survey/index.md · schema-gaps.md Memory stance docs/memory-stance.md · docs/heap-analysis.md COR24 emulator sw-embed/cor24-rs Driven projects sw-cor24-pcode · sw-cor24-ocaml · sw-cor24-pascal · sw-cor24-basic Prior Personal Software post sw-checklist: Reining In AI Coding Agents With a Code-Metrics Ratchet Comments Discord The Problem: Every Language Has Its Own Loader The COR24 is a 24-bit machine with 1 MiB of SRAM, a 3 KiB EBR hardware stack, and an MMIO aperture at 0xFF0000. The host-side cor24-run emulator accepts a small surface — --load-binary path@hex_addr, --patch hex_addr=hex_value, --uart-input "...", --entry, --speed, -n — and that’s the universe. What changes between repos is what gets loaded where, which runtime word has to be patched to point at the layer above it, and how source and data ride the UART. The Phase 0 survey looked at thirteen working repos and aggregated the patterns. A few from the comparison table: repo loads patches UART src UART data heap stack approx SRAM basic 1 0 yes no emb hw EBR ~64 KiB forth 0 0 yes no emb hw EBR ~256 KiB macrolisp 0–1 0 yes snapshot emb hw EBR ~512 KiB ocaml 2 2 yes post-EOT emb+res emb in pvm ~512 KiB pascal 1–2 1 yes no emb emb ~64 KiB plsw 0 0 yes no emb hw EBR ~1 MiB snobol4 1–3 0 yes mode-flag emb emb ~128 KiB monitor many 0 no no emb hw EBR ~64 KiB tuplet 3 2 yes image@0x080000 res emb in pvm ~768 KiB Every project’s scripts/run-*.sh re-encodes one of these shapes. None of them validate. None of them cache. None of them notice when the OCaml heap and the DSL heap overlap. And every AI agent that touches these scripts adds its own subtle variation, because the shell script is the spec. Two Axes, Five Shapes The original PRD assumed one axis with three points (A: single image, B: runtime+image, C: nested interpreter). The survey says it is actually two axes: Build axis: hand-written assembly, compiled from a higher-level language, snapshot rehydrated by host tooling, or composite of N modules linked host-side. Run axis: one-shot batch (kick off and check UART), interactive REPL through UART, interactive shell with a resident process model, or edit-then-run via a resident editor. The cross product yields five primitive shapes that cover everything sw-embed has written so far. The first three were already in the day-zero design; the last two emerged from the survey: Single image at zero, UART source. Heap and stack embedded in the image. (apl, basic, forth, plsw, smalltalk-delegated.) Runtime + image + patch. Native COR24 runtime at 0 plus a p-code image at a higher address with a code_ptr-style patch. (pascal single-unit and multi-unit, the OCaml/tuplet pattern without the heap patch.) Nested interpreter with heap-limit patch and UART-after-EOT data. Adds a second patch (heap limit), and the UART payload is &lt;source&gt; + EOT + &lt;runtime data&gt;. (ocaml, tuplet.) Multi-module composite image. The launcher loads N independently assembled modules at contiguous bases (snobol4 via link24) or at fixed slot addresses (macrolisp’s multi-module demo, monitor’s program registry). Linking happens host-side, not via patches. Resident shell + paste-and-go. Monitor at 0, sws shell at 0x20000, programs at fixed slots, all preloaded together; transfer of control happens inside the emulator via a service-vector / trampoline (mon_invoke_program) and never returns to the host runner. (monitor, script, yocto-ed.) A scenario picks one shape; the schema makes that pick explicit instead of implied by which shell script you happen to run. Schema v1.1: Partition Grid (Considered, Then Rejected) The first revision after the survey, schema v1.1, divided the 1 MiB SRAM into eight fixed partitions of 128 KiB and four regions per partition (code/heap/spare/stack, 32 KiB each). Most existing repos already align to obvious partition boundaries (0x000000, 0x010000, 0x040000, 0x080000, 0x0F0000), so re-stating those addresses in (partition, region) coordinates was mostly a labeling change. It was the wrong move. The grid canonized a layout without taking a position on the budgets, which let oversized heaps express themselves as multi-cell claims and call it normal: # v1.1: OCaml's 252 KiB heap, expressed as four contiguous cells. # Schema accepts it, validator passes, nothing argues back. [layers.ocaml_interp.segments.value_heap] kind = "heap" grows = "down" claims = [ { partition = 0, region = "spare" }, { partition = 0, region = "stack" }, { partition = 1, region = "code" }, { partition = 1, region = "heap" }, ] That’s the OCaml interpreter’s current heap_limit = 0x03F000 written as a partition-cell list. Pinning down the layout this way looked like progress. It was actually normalization of the bug. Schema v1.2: Memory Profiles + Heap Budgets The second revision flipped the prior. From docs/memory-stance.md: The COR24 board emulator targets 1 MiB SRAM. That is more, not less, than every machine these re-implemented languages were originally designed for: Forth in 4–16 KiB, BASIC in 4 KiB (Altair) to 32 KiB (MS BASIC for IBM PC), APL/360 in &lt;128 KiB per partition, Smalltalk-72/76 in 128–512 KiB including the bitmap display, Macrolisp on a PDP-10 with 256 KiB total. The IBM PC shipped in 1981 with 16–256 KiB. By 1985 measure, 1 MiB and a tiny monitor is a luxurious environment. If macrolisp on a PDP-10 fit in 256 KiB total — runtime, interpreter, and program — then the COR24 macrolisp’s ~288 KiB heap is not a constraint problem. Something has gone soft. The 1 MiB ceiling does not need to be raised. The heaps need to be shrunk. v1.2 makes that the schema’s stance. Three concrete changes: 1. The fixed grid is gone. Replaced with named memory profiles. Each profile is an ordered list of partitions of arbitrary size, each with its own list of named regions of arbitrary kind and size, plus a budget block: [memory_profiles.compiled-app] description = "Single image at 0; small heap; small stack." [[memory_profiles.compiled-app.partitions]] name = "code" base = "0x000000" size = "0x010000" # 64 KiB regions = [ { name = "code", kind = "code", size = "auto" }, { name = "static", kind = "data", size = "auto" }, ] [[memory_profiles.compiled-app.partitions]] name = "heap" base = "0x010000" size = "0x008000" # 32 KiB regions = [{ name = "heap", kind = "heap", size = "0x008000" }] [memory_profiles.compiled-app.budget] code_max = "0x008000" # 32 KiB heap_max = "0x004000" # 16 KiB stack_max = "0x002000" # 8 KiB total_max = "0x010000" # 64 KiB justification_required = true 2. Five default profiles ship with the launcher, each sized per docs/heap-analysis.md: profile code+data heap stack total example use compiled-app &lt;= 32 KiB &lt;= 16 KiB &lt;= 8 KiB &lt;= 64 KiB BASIC echo program interpreter-only &lt;= 64 KiB &lt;= 64 KiB &lt;= 16 KiB &lt;= 160 KiB APL, Forth, Smalltalk repl-inline-compile &lt;= 128 KiB &lt;= 256 KiB &lt;= 32 KiB &lt;= 448 KiB OCaml + GC, Tuplet (post-fix) compiler-image &lt;= 256 KiB &lt;= 64 KiB &lt;= 32 KiB &lt;= 384 KiB PL/SW (post-fix) resident-shell &lt;= 64 KiB per slot, up to 8 slots per-program shared 8 KiB &lt;= 512 KiB monitor + sws + N programs A scenario picks a profile by name; the validator enforces that profile’s budget. Layers cite partitions and regions by name, not by hex. 3. Heaps over 32 KiB must argue for themselves through a heap_justification block: [layers.ocaml_interp.heap_justification] category = "gc-slack" note = "Mark/sweep GC; sized for working set + 2x slack." measured_floor_kib = 64 tracking_issue = "sw-cor24-ocaml#28" Five categories, in roughly descending order of merit: category accepted? meaning algorithmic-floor yes Working set genuinely requires this size. bytecode-image yes Heap is mostly read-only data, not allocations. gc-slack yes (with measured_floor_kib) Sized for floor + slack between collections. dead-leak warn; rejected by --strict Allocations that never get freed. algorithmic-bloat warn; rejected by --strict Pointer width, boxing, dispatch tables, etc. The default category for an undocumented oversized heap is dead-leak — because the heap-analysis pass found that all three of the demanding repos (ocaml, macrolisp, plsw) match exactly that pattern, and the first job of a budget is to refuse to normalize them. What the Heap Analysis Found docs/heap-analysis.md walks every repo with a claimed heap &gt; 32 KiB and assigns it a category, a historical benchmark, and a shrinkage backlog: repo current claim category historical floor post-fix target ocaml ~252 KiB heap dead-leak OCaml-on-PDP-10 &lt; 256 KiB total (1973) &lt;= 64 KiB after GC tuplet inherits ocaml dead-leak n/a (downstream) shrinks with ocaml macrolisp ~288 KiB BSS dead-leak + bloat Maclisp 256 KiB total &lt;= 64 KiB heap plsw ~1 MiB image algorithmic-bloat UCSD Pascal in 64 KiB; Turbo Pascal 1.0 in 33.5 KiB &lt;= 256 KiB image snobol4 ~76 KiB internal floor + dead-leak SNOBOL4 in 64–256 KiB total &lt;= 64 KiB forth dictionary algorithmic-floor Forth kernels in 4–16 KiB &lt;= 16 KiB typical basic embedded DIM 64–128 KiB algorithmic-floor Altair 4K BASIC &lt;= 32 KiB OCaml’s GC work in sw-cor24-ocaml#28 is the one in flight. After it lands, tuplet’s heap_limit should shrink, not stay where it is. Macrolisp’s mark byte should be a mark bit (8x reduction). PL/SW’s compiler-output redundancy is fixable in one pass through the transpiler. The schema must support the current sizes transitionally, but the analysis doc must not normalize them. The current sizes are evidence of work to do; not the spec for the launcher. Layers Are Composites, Not Blobs Every other piece of the schema survives both revisions. A layer is still (artifact?) + (segments), and segments still have lifecycles: Embedded segments live inside the artifact. pvm.s reserves eval_stack, call_stack, and a small heap_seg statically; they are part of pvm.bin and the loader does not allocate them again, but the validator has to know they exist (resolved through the artifact’s listing) so the global overlap check sees them. Reserved segments are allocated by the loader at a configured cell with a configured size and zero-filled. The OCaml interpreter’s value heap, the Pascal eval stack, the DSL heap on top — none of these fit in the COR24’s 3 KiB EBR; they live in high SRAM cells and the runtime gets patched to point at them. Patched is the verb that ties the two together. A reserved segment with value = "self.address" (or self.end for down-growing heaps) tells the loader to write its own resolved address into the runtime’s heap_base / heap_limit symbol, so the runtime knows where to find the heap the loader just allocated for it. Patches in v1.2 also accept two value forms that the survey demanded: value = "sidecar:&lt;path&gt;" reads a build-time-resolved address from a small text file. ocaml and tuplet today write build/code_ptr_addr.txt and build/heap_limit_addr.txt during their build; the schema makes that explicit and includes the sidecar in the cache key. target = "&lt;upstream-layer&gt;.&lt;symbol&gt;" lets a downstream layer reference a symbol in an upstream layer’s listing — including upstream layers from a vendored repo (Phase 2 step 002 just landed the resolver for this; tuplet wants pvm symbols from sw-cor24-ocaml’s build, not its own). The Wizard’s Spellbook — sw-launch.toml The whole config is one file at the project root. A trimmed Scenario A example, written against the compiled-app profile, that actually runs end-to-end on a real cor24-run: [scenarios.echo] target = "cor24" memory_profile = "compiled-app" layers = ["program", "stdin"] entry = "0x000000" [scenarios.echo.run] timeout_ms = 2000 max_cycles = 200_000 halt_on = "uart-eot" [scenarios.echo.expect] uart_contains = ["A"] exit_code = 0 [layers.program] kind = "assembler" source = "local" input = "src/echo.s" tool = "assembler" artifact = "echo.bin" [layers.program.segments.code] kind = "code" claims = [{ partition = "code", region = "code" }] [layers.stdin] kind = "data" input = "tests/echo-input.txt" load.method = "uart" load.max_bytes = 1024 sw-launch run echo walks the layer DAG, builds each layer (or pulls it from the in-process memoization cache), assembles the load plan, invokes cor24-run, captures UART output, and checks the expectations. Today this prints A and exits 0. The CLI surface is small enough to memorize: sw-launch run &lt;scenario&gt; Build (with cache) and execute, check expectations. sw-launch build &lt;scenario&gt; Build all layers; do not execute. sw-launch check &lt;scenario&gt; Validate config + lock; no tools run. sw-launch graph &lt;scenario&gt; Print layer DAG (text or --json). sw-launch cache list List cached artifacts. sw-launch vendor sync Resolve and pin all dependencies. sw-launch doctor Verify host tools (cor24-run, pa24r, pl24r) found. Every flag agents used to invent is now either a TOML field or a --profile. There is no --load-binary to mistype. Phase 1: What Runs Today The Phase 1 saga (sw-launcher-phase1, ten steps) closed clean on April 28. End state: sw-launch run echo --config tests/fixtures/scenario_a/sw-launch.toml exits 0 and prints A (the captured UART output). sw-launch check echo validates the scenario without spawning the emulator. sw-launch build echo assembles every assembler-kind layer under &lt;config-dir&gt;/.sw-launch/build/&lt;scenario&gt;/&lt;layer&gt;/ with a sha256-keyed in-process memoization cache. 60 tests across 11 binaries, including 2 end-to-end against the real cor24-run binary, all green. validate.rs implements 17 stable error codes, each with a negative test asserting the exact code and span. The integration tests ran against cor24-run 0.1.0, rustc 1.94.1, edition 2024, on Darwin 24.6.0. Every one of those versions is recorded in the repo’s status.md so future-me knows what “Phase 1 worked” actually meant. Phase 2 (sw-launcher-phase2) is open and seeded with five steps; step 001 (PCode tool with SourceSpec resolution) and step 002 (cross-layer listing-symbol patches resolve at scenario validate time) just landed. Phase 2’s target is end-to-end Scenario B: COR24 runtime at 0 plus a p-code blob at a higher address with a code_ptr patch — the smallest meaningful test that the launcher can express the layered shape that today’s sw-cor24-pcode and sw-cor24-pascal demos use. Validation: Catch Collisions Before the Emulator Does The validator is the most important verb. sw-launch check reads the TOML and the lockfile, walks every segment of every layer in the scenario, computes absolute address ranges (resolving profile partitions to addresses, embedded segments through the producing layer’s listing, sidecars from disk), and runs the rules. Each rule has a stable error code so agents can match on it without scraping prose. A subset, selected for what they catch: Code Rule E0003 No two memory ranges overlap (across embedded and reserved segments). E0004 UART layers declare max_bytes and the input fits. E0005 Layer kind and load method are compatible. E0006 Every patch resolves — symbol exists, segment exists, topo order is right. E0011 Reserved stack/heap/bss lies inside regions.sram, never touches regions.ebr_stack or regions.mmio. E0014 embedded = true segments declare a symbol that resolves in the producing layer’s listing. E0023 Resident-mode mismatch (a layer claims a slot the resident shell doesn’t expose). E0028 Heap-budget overshoot (heap exceeds the profile’s heap_max). E0029 Heap &gt;= 80% of heap_max (warn). E0030 Missing heap_justification for heap &gt; 32 KiB. E0031 Layer cites a partition or region the profile does not declare. E0032 Profile self-overlap (the profile’s own partitions collide). E0033 Total claimed SRAM &gt; 1 MiB. E0034 heap_justification.category = "dead-leak" or "algorithmic-bloat" under --strict. --strict mode promotes most warnings to errors and always rejects dead-leak and algorithmic-bloat justifications. CI runs strict; local check runs lax so the shrinkage work can land incrementally without breaking the build. Caching, Vendoring, and the Lockfile The cost of not caching is that every test re-assembles pvm.s, every demo re-builds the host toolchain, every CI run wastes minutes. Phase 1 ships an in-process memoization cache (sha256 keyed on (input bytes, tool version, args, output filename)) inside the assembler tool wrapper; Phase 4 will lift that to a persistent on-disk cache at ~/.cache/sw-launch/. The key formula already accounts for the hard cases: layer_key = sha256( schema_version | normalize_toml(layer_config) | hash_each(input_files) | tool_version_hash | dependency_layer_hashes (in topo order) | resolved_address_or_uart_marker | sidecar_contents (if any) ) A cache hit is sound — if the key matches, the artifact would be byte-identical to a fresh build, so reusing it can never produce a different scenario result. Sidecar contents are in the key because v1.2’s value = "sidecar:..." patch source needs the cache to invalidate when the sidecar changes. The vendor side is symmetric. The survey was unflattering — ten of thirteen repos pin nothing at all, and one (tuplet) inherits its pins transitively from sw-cor24-ocaml. Only sw-cor24-ocaml has a real vendor/&lt;tool&gt;/&lt;version&gt;/active.env model with commit SHAs. v1.2 makes the OCaml-style vendored model the default: sw-launch vendor sync (Phase 4) will resolve declared dependencies (sibling:, vendor:, eventually git:) and write sw-launch.lock with each artifact’s commit hash and SHA. sw-launch doctor will record the observed version of each PATH-resolved tool too, so drift is visible even when nothing is explicitly pinned. What This Buys The point is not that the TOML is shorter than the shell script — it is often longer. The point is what changes about the system: One verb for agents. sw-launch run &lt;scenario&gt; replaces the ten variants of cor24-run --load-binary ... that agents kept reinventing. Validation before the emulator. The most expensive failure mode — “ran for forty seconds, traps silently, zero output” — is replaced by a fast check that names the colliding region and the offending TOML span. Heaps argue for themselves. A 252 KiB heap is not waved through; it has to declare a category, a measured floor, and a tracking issue. --strict rejects dead-leak and algorithmic-bloat outright. A schema for memory layout. Reserved heaps, embedded stacks, profile-named partitions, sidecar patches, cross-layer symbol references — all first-class in TOML. A new language port describes its memory shape; it does not write a new shell loader. Vendor versions visible. Sibling-repo dependencies are pinned by commit hash in the lockfile, not by “whatever was checked out at 3pm.” PATH-resolved tools have their observed versions recorded too. Phase 1 actually runs. The “what runs today” section is not a roadmap; it is what cargo test exercises, end-to-end, against a real emulator. The wizard metaphor still fits the post marker: thirteen repos, each with its own ring of power, all in the end answering to one. sw-launcher is the one ring in the boring sense — the one place to encode the load plan — not in the corrupting sense, hopefully. But the more accurate metaphor for v1.2 is that the schema is a budget officer: every heap that wants more than 32 KiB has to file paperwork, and the default verdict on the paperwork is “this is a leak; prove otherwise.” Where It Sits in the Personal-Software Toolkit sw-checklist, the subject of the previous post in this series, sits at a different layer of the same problem: AI agents working alone produce too much variety in places where uniformity is cheaper. sw-checklist constrains the shape of the code (function/file/module/crate size limits); sw-launcher constrains the shape of the load plan and the memory budget (which addresses, which patches, how big a heap can grow before it has to file paperwork). Both are accidental complexity in Brooks’s sense. Both are paying rent. Phase 0 (survey) and Phase 1 (Scenario A end-to-end) are done. Schema v1.1 was a wrong turn that taught the right lesson: do not canonize the layout without taking a position on the budgets. Schema v1.2 takes that position, in writing, with five default profiles sized against historical implementations from the 1970s and 1980s. Phase 2 (Scenario B, runtime + p-code blob with cross-layer patches) is in flight; Phase 3 brings Scenario C (nested interpreter, heap-limit-only patches — the tuplet shape); Phase 4 brings persistent caching and vendor sync; Phase 5 brings the resident-shell composite (D + E). The wizard is just the post marker. The one ring is just the metaphor. The interesting part is the budget officer.]]></summary></entry><entry><title type="html">Dogfooding (1/?): YAGNI Until You Do — A Pascal P-Code Bump Allocator Grows a Free List</title><link href="https://software-wrighter-lab.github.io/2026/04/27/embedded-pascal-pcode-yagni-bump-to-reclaim/" rel="alternate" type="text/html" title="Dogfooding (1/?): YAGNI Until You Do — A Pascal P-Code Bump Allocator Grows a Free List" /><published>2026-04-27T09:00:00-07:00</published><updated>2026-04-27T09:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/27/embedded-pascal-pcode-yagni-bump-to-reclaim</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/27/embedded-pascal-pcode-yagni-bump-to-reclaim/"><![CDATA[<p><img src="/assets/images/posts/block-puppy-dogfood-bowl.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>YAGNI — <em>You Ain’t Gonna Need It</em> — is one of those rules that’s easy to quote and hard to time. Skip the feature, ship the demo, move on. It works beautifully <em>until</em> the day a real workload sits down on the MVP and the missing piece is suddenly the only piece that matters. Today is that day for the Pascal p-code VM’s allocator: a bump allocator with a no-op <code class="language-plaintext highlighter-rouge">free</code> was exactly enough for every demo it ever ran, right up to the moment OCaml-in-Pascal tried to lex and parse a non-trivial Tuplet program and walked off the end of the heap.</p>

</div>

<div class="aside-box">

  <p><strong>Why Dogfooding?</strong> — The phrase comes from <em>“eating your own dog food”</em>: shipping software that you yourself rely on for real work. In this lab, dogfooding is also the <em>forcing function</em> that decides when an MVP has earned its build-out. The pattern is: implement the smallest thing that demonstrates the capability, ship a demo, move on — then wait for a downstream project to put real load on the placeholder. This series captures the moments where that load finally arrives, what was missing, and what filling the gap looked like.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>COR24 Pascal toolchain</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-pascal">sw-embed/sw-cor24-pascal</a></td>
      </tr>
      <tr>
        <td><strong>Pascal Demo</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-pascal/">sw-embed.github.io/web-sw-cor24-pascal</a></td>
      </tr>
      <tr>
        <td><strong>Tuplet</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/tuplet">sw-vibe-coding/tuplet</a></td>
      </tr>
      <tr>
        <td><strong>Related Post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): How Much of Forth Can Be Forth?</a></td>
      </tr>
      <tr>
        <td><strong>Related Post</strong></td>
        <td><a href="/2026/04/26/saw-tuplet-smalltalk-forth-from-forth/">Saw (8/?): Tuplet, Smalltalk-on-BASIC, Forth-from-Forth</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="what-the-bump-allocator-bought">What the Bump Allocator Bought</h2>

<p>The Pascal p-code VM is the runtime under <code class="language-plaintext highlighter-rouge">pa24r</code> (the Pascal compiler) and <code class="language-plaintext highlighter-rouge">pl24r</code> (the linker). Pascal source compiles to p-code; p-code executes on a small stack-and-heap VM that targets the COR24 ISA. Heap allocation, from day one, was the simplest thing that could work:</p>

<ul>
  <li>A single contiguous heap region.</li>
  <li>A bump pointer.</li>
  <li>Allocate by advancing the pointer.</li>
  <li><code class="language-plaintext highlighter-rouge">free</code> is a no-op.</li>
  <li>Out-of-heap is a hard fault.</li>
</ul>

<p>That’s it. No metadata per allocation, no headers, no free list, no compaction — just a pointer and a high-water mark. The allocator fits in a handful of p-code instructions, has zero per-allocation overhead, and is trivially correct because there is nothing to be incorrect <em>about</em>. Allocation is O(1) and the worst case equals the best case.</p>

<p>This was the right call. Every Pascal demo on COR24 to date — the BASIC interpreter, small string-handling exercises, the sample programs in the web demo — has a working-set that fits comfortably under whatever heap size the VM is configured with. Nothing on the runway needed reclaim. Building a free list would have been YAGNI-bait: more code, more bugs, more places for a wrong-looking trace to come from, all to solve a problem nobody had.</p>

<h2 id="the-forcing-function-ocaml-in-pascal-meets-tuplet">The Forcing Function: OCaml-in-Pascal Meets Tuplet</h2>

<p>The crossing-over moment came from the Tuplet front-end. <a href="https://github.com/sw-vibe-coding/tuplet">Tuplet</a> is the new glyph-and-whitespace language <a href="/2026/04/26/saw-tuplet-smalltalk-forth-from-forth/">introduced in last weekend’s Saw post</a>; its lexer and parser are written in OCaml, and OCaml itself runs — in the dogfooded version — on top of the Pascal p-code VM.</p>

<p>That stack looks like this:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: right">Layer</th>
      <th>What’s running</th>
      <th>Implemented in</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: right">4</td>
      <td>Tuplet program</td>
      <td>Tuplet source</td>
    </tr>
    <tr>
      <td style="text-align: right">3</td>
      <td>Tuplet lexer/parser</td>
      <td>OCaml</td>
    </tr>
    <tr>
      <td style="text-align: right">2</td>
      <td>OCaml runtime</td>
      <td>Pascal</td>
    </tr>
    <tr>
      <td style="text-align: right">1</td>
      <td>Pascal program</td>
      <td>p-code</td>
    </tr>
    <tr>
      <td style="text-align: right">0</td>
      <td>p-code VM</td>
      <td>COR24 / native host</td>
    </tr>
  </tbody>
</table>

<p>Lex-and-parse is exactly the workload a bump allocator does <em>not</em> like. Lexers churn out token objects; parsers build AST nodes by the thousand and discard most of the intermediate scaffolding (one-shot list cells, temporary closures, exception-carrying error paths). The amount of <em>live</em> data at any instant is small. The amount of <em>allocated</em> data over the lifetime of a parse is enormous. Bump allocation treats those two numbers as the same number.</p>

<h2 id="doubling-the-heap-once-is-a-fix-twice-is-a-signal">Doubling the Heap, Once Is a Fix, Twice Is a Signal</h2>

<p>For the first few inputs, the bump allocator was fine: small Tuplet sources parsed cleanly. Then the prelude grew, the test corpus grew, and parses started ending the same way — <em>out of heap</em>. The first response was the cheap one: double the heap. That bought one more input. Double again. One more input. The growth rate of the corpus was outrunning the growth rate of the heap, and every doubling pushed the failure later in the parse rather than removing it.</p>

<p>That’s the YAGNI exit signal. The next doubling wasn’t going to fix anything; it was just going to delay the same fault by another constant factor while consuming memory the host didn’t have to spare. The shape of the workload was demanding reclaim, and the allocator had to grow up.</p>

<h2 id="why-yagni-worked-and-why-it-stopped-working">Why YAGNI Worked, and Why It Stopped Working</h2>

<p>It’s worth being precise about what changed, because the instinct after a story like this is <em>“I should have built the free list from the start.”</em> That’s the wrong lesson. The bump allocator was load-bearing for almost a year of demo work, during which:</p>

<ul>
  <li>The runtime stayed small enough to read in one sitting.</li>
  <li>Memory bugs were impossible by construction (you can’t double-free a no-op).</li>
  <li>Every new Pascal feature landed against an allocator that had nothing to break.</li>
  <li>The build-out budget went into things that <em>did</em> matter for shipping demos — string handling, control flow, the p-code instruction set, the assembler, the linker.</li>
</ul>

<p>The cost of building reclaim earlier wouldn’t just have been the code; it would have been all the <em>other</em> code that didn’t get written because the free list was eating the calendar. YAGNI bought a year of velocity. It paid for itself the first time, and again the second time, and would have paid for itself again if Tuplet had stayed small.</p>

<p>What expired wasn’t the allocator’s correctness. It was the assumption that the workload-set was bounded by <em>demo</em> shapes. A self-hosted compiler front-end is not a demo; it’s a real program with its own internal allocation discipline, and that discipline assumes there’s something on the other end of the heap willing to take memory back. The first time the VM hosts that kind of workload is the first time YAGNI doesn’t apply.</p>

<h2 id="what-grow-up-looks-like">What “Grow Up” Looks Like</h2>

<div class="gutter-section">

  <p><img src="/assets/images/posts/block-jenga-tower.png" class="gutter-img-left no-invert" alt="Jenga tower with a missing block" /></p>

  <p>The reclaim work in flight is deliberately <em>minimum-viable</em> in the same spirit as the bump allocator was — pull out the smallest piece the workload demands, leave the tower standing, and don’t add anything that doesn’t pay for itself the moment it lands. The shape:</p>

  <ul>
    <li>Add a per-allocation header carrying size and a one-bit free flag.</li>
    <li>Add an explicit <code class="language-plaintext highlighter-rouge">free(p)</code> p-code instruction that flips the flag and coalesces with adjacent free neighbors.</li>
    <li>Maintain a single free list (or a small handful of size classes — still TBD).</li>
    <li>Allocation prefers free-list reuse, falls back to bumping the high-water mark, faults only when both fail.</li>
    <li>No compaction, no GC, no tracing — the OCaml runtime knows when it’s done with a value and is willing to call <code class="language-plaintext highlighter-rouge">free</code> explicitly.</li>
  </ul>

  <p>That’s still a lot smaller than a real allocator. There’s no defragmentation, no concurrent allocation, no statistics, no debug poisoning, no quarantine. Each of those is a future YAGNI test — if the workload demands it, build it; otherwise leave it out. The new floor is <em>“reclaim works”</em>, not <em>“the allocator is finished.”</em></p>

  <p>The interesting question, once this lands, is whether the OCaml runtime’s allocation pattern is friendly enough to a free-list allocator that fragmentation never bites, or whether some later workload will bite hard enough to force compaction. I genuinely don’t know. That’s fine. The next forcing function will tell me.</p>

</div>

<h2 id="takeaways">Takeaways</h2>

<ul>
  <li>A bump allocator with a no-op <code class="language-plaintext highlighter-rouge">free</code> is the right call when the workload-set is bounded by demos. It’s fast, small, and impossible to corrupt.</li>
  <li>“Real users” expire the assumption. A self-hosted compiler front-end is the smallest realistic workload that <em>requires</em> reclaim, because the working-set is small but the lifetime allocation is unbounded.</li>
  <li>Doubling the heap is a perfectly good response to a memory bug — <em>once</em>. Two doublings is a signal. Three is a confession.</li>
  <li>The next allocator should be exactly as small as the workload demands, and no smaller. Free list, header bit, coalesce on free; defer everything else until something asks for it.</li>
</ul>

<p>YAGNI didn’t fail here. It cashed out. The allocator was a placeholder all along; today is the day it was supposed to be replaced. The trick was knowing it was a placeholder, and trusting the dogfooding loop to ring the bell when the placeholder ran out.</p>

<p>Future Dogfooding posts will follow the same pattern — a placeholder that earned its keep, the workload that finally outgrew it, and what filling the gap actually looked like. Next up: a postmortem on the reclaim implementation itself, once the OCaml-on-Pascal Tuplet parse can run end-to-end without a heap fault.</p>]]></content><author><name>Software Wrighter</name></author><category term="embedded" /><category term="programming-languages" /><category term="compilers" /><category term="dogfooding" /><category term="dogfooding" /><category term="yagni" /><category term="pascal" /><category term="p-code" /><category term="vm" /><category term="allocator" /><category term="bump-allocator" /><category term="free-list" /><category term="ocaml" /><category term="tuplet" /><category term="mvp" /><category term="vibe-coding" /><summary type="html"><![CDATA[YAGNI — You Ain’t Gonna Need It — is one of those rules that’s easy to quote and hard to time. Skip the feature, ship the demo, move on. It works beautifully until the day a real workload sits down on the MVP and the missing piece is suddenly the only piece that matters. Today is that day for the Pascal p-code VM’s allocator: a bump allocator with a no-op free was exactly enough for every demo it ever ran, right up to the moment OCaml-in-Pascal tried to lex and parse a non-trivial Tuplet program and walked off the end of the heap. Why Dogfooding? — The phrase comes from “eating your own dog food”: shipping software that you yourself rely on for real work. In this lab, dogfooding is also the forcing function that decides when an MVP has earned its build-out. The pattern is: implement the smallest thing that demonstrates the capability, ship a demo, move on — then wait for a downstream project to put real load on the placeholder. This series captures the moments where that load finally arrives, what was missing, and what filling the gap looked like. Resource Link COR24 Pascal toolchain sw-embed/sw-cor24-pascal Pascal Demo sw-embed.github.io/web-sw-cor24-pascal Tuplet sw-vibe-coding/tuplet Related Post Embedded (3/?): How Much of Forth Can Be Forth? Related Post Saw (8/?): Tuplet, Smalltalk-on-BASIC, Forth-from-Forth Comments Discord What the Bump Allocator Bought The Pascal p-code VM is the runtime under pa24r (the Pascal compiler) and pl24r (the linker). Pascal source compiles to p-code; p-code executes on a small stack-and-heap VM that targets the COR24 ISA. Heap allocation, from day one, was the simplest thing that could work: A single contiguous heap region. A bump pointer. Allocate by advancing the pointer. free is a no-op. Out-of-heap is a hard fault. That’s it. No metadata per allocation, no headers, no free list, no compaction — just a pointer and a high-water mark. The allocator fits in a handful of p-code instructions, has zero per-allocation overhead, and is trivially correct because there is nothing to be incorrect about. Allocation is O(1) and the worst case equals the best case. This was the right call. Every Pascal demo on COR24 to date — the BASIC interpreter, small string-handling exercises, the sample programs in the web demo — has a working-set that fits comfortably under whatever heap size the VM is configured with. Nothing on the runway needed reclaim. Building a free list would have been YAGNI-bait: more code, more bugs, more places for a wrong-looking trace to come from, all to solve a problem nobody had. The Forcing Function: OCaml-in-Pascal Meets Tuplet The crossing-over moment came from the Tuplet front-end. Tuplet is the new glyph-and-whitespace language introduced in last weekend’s Saw post; its lexer and parser are written in OCaml, and OCaml itself runs — in the dogfooded version — on top of the Pascal p-code VM. That stack looks like this: Layer What’s running Implemented in 4 Tuplet program Tuplet source 3 Tuplet lexer/parser OCaml 2 OCaml runtime Pascal 1 Pascal program p-code 0 p-code VM COR24 / native host Lex-and-parse is exactly the workload a bump allocator does not like. Lexers churn out token objects; parsers build AST nodes by the thousand and discard most of the intermediate scaffolding (one-shot list cells, temporary closures, exception-carrying error paths). The amount of live data at any instant is small. The amount of allocated data over the lifetime of a parse is enormous. Bump allocation treats those two numbers as the same number. Doubling the Heap, Once Is a Fix, Twice Is a Signal For the first few inputs, the bump allocator was fine: small Tuplet sources parsed cleanly. Then the prelude grew, the test corpus grew, and parses started ending the same way — out of heap. The first response was the cheap one: double the heap. That bought one more input. Double again. One more input. The growth rate of the corpus was outrunning the growth rate of the heap, and every doubling pushed the failure later in the parse rather than removing it. That’s the YAGNI exit signal. The next doubling wasn’t going to fix anything; it was just going to delay the same fault by another constant factor while consuming memory the host didn’t have to spare. The shape of the workload was demanding reclaim, and the allocator had to grow up. Why YAGNI Worked, and Why It Stopped Working It’s worth being precise about what changed, because the instinct after a story like this is “I should have built the free list from the start.” That’s the wrong lesson. The bump allocator was load-bearing for almost a year of demo work, during which: The runtime stayed small enough to read in one sitting. Memory bugs were impossible by construction (you can’t double-free a no-op). Every new Pascal feature landed against an allocator that had nothing to break. The build-out budget went into things that did matter for shipping demos — string handling, control flow, the p-code instruction set, the assembler, the linker. The cost of building reclaim earlier wouldn’t just have been the code; it would have been all the other code that didn’t get written because the free list was eating the calendar. YAGNI bought a year of velocity. It paid for itself the first time, and again the second time, and would have paid for itself again if Tuplet had stayed small. What expired wasn’t the allocator’s correctness. It was the assumption that the workload-set was bounded by demo shapes. A self-hosted compiler front-end is not a demo; it’s a real program with its own internal allocation discipline, and that discipline assumes there’s something on the other end of the heap willing to take memory back. The first time the VM hosts that kind of workload is the first time YAGNI doesn’t apply. What “Grow Up” Looks Like The reclaim work in flight is deliberately minimum-viable in the same spirit as the bump allocator was — pull out the smallest piece the workload demands, leave the tower standing, and don’t add anything that doesn’t pay for itself the moment it lands. The shape: Add a per-allocation header carrying size and a one-bit free flag. Add an explicit free(p) p-code instruction that flips the flag and coalesces with adjacent free neighbors. Maintain a single free list (or a small handful of size classes — still TBD). Allocation prefers free-list reuse, falls back to bumping the high-water mark, faults only when both fail. No compaction, no GC, no tracing — the OCaml runtime knows when it’s done with a value and is willing to call free explicitly. That’s still a lot smaller than a real allocator. There’s no defragmentation, no concurrent allocation, no statistics, no debug poisoning, no quarantine. Each of those is a future YAGNI test — if the workload demands it, build it; otherwise leave it out. The new floor is “reclaim works”, not “the allocator is finished.” The interesting question, once this lands, is whether the OCaml runtime’s allocation pattern is friendly enough to a free-list allocator that fragmentation never bites, or whether some later workload will bite hard enough to force compaction. I genuinely don’t know. That’s fine. The next forcing function will tell me. Takeaways A bump allocator with a no-op free is the right call when the workload-set is bounded by demos. It’s fast, small, and impossible to corrupt. “Real users” expire the assumption. A self-hosted compiler front-end is the smallest realistic workload that requires reclaim, because the working-set is small but the lifetime allocation is unbounded. Doubling the heap is a perfectly good response to a memory bug — once. Two doublings is a signal. Three is a confession. The next allocator should be exactly as small as the workload demands, and no smaller. Free list, header bit, coalesce on free; defer everything else until something asks for it. YAGNI didn’t fail here. It cashed out. The allocator was a placeholder all along; today is the day it was supposed to be replaced. The trick was knowing it was a placeholder, and trusting the dogfooding loop to ring the bell when the placeholder ran out. Future Dogfooding posts will follow the same pattern — a placeholder that earned its keep, the workload that finally outgrew it, and what filling the gap actually looked like. Next up: a postmortem on the reclaim implementation itself, once the OCaml-on-Pascal Tuplet parse can run end-to-end without a heap fault.]]></summary></entry><entry><title type="html">sw-checklist: Reining In AI Coding Agents With a Code-Metrics Ratchet</title><link href="https://software-wrighter-lab.github.io/2026/04/27/sw-checklist-ratchet-ai-coding-agents/" rel="alternate" type="text/html" title="sw-checklist: Reining In AI Coding Agents With a Code-Metrics Ratchet" /><published>2026-04-27T02:00:00-07:00</published><updated>2026-04-27T02:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/27/sw-checklist-ratchet-ai-coding-agents</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/27/sw-checklist-ratchet-ai-coding-agents/"><![CDATA[<p><img src="/assets/images/posts/block-twisted-ropes.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>When I say “vibe-coding,” the quotes are doing real work. I am not turning the AI loose and accepting whatever lands. I am using AI agents the same way I’d use a sharp tool with a guard on it: deliberately, with constraints that are themselves additional work. The constraints are <em>accidental</em> complexity in Brooks’s sense — they come from how I choose to build, not from the problem itself — and yet I’d argue they are the only reason the code stays focused on the <em>essential</em> complexity that actually matters. <a href="https://github.com/softwarewrighter/sw-checklist">sw-checklist</a> is the personal-software tool I wrote to keep that discipline in the loop.</p>

</div>

<div class="aside-box">

  <p><strong>Why this matters</strong> — It is easy to confuse “the AI is fast” with “the AI is producing good code.” Without forcing functions — code metrics, a linter, a TDD loop — a generative agent will happily emit a 600-line file with 12 functions per module and 9 modules per crate, none of which are technically wrong, all of which are technically a mess. The interesting question is not whether to spend on accidental complexity, but <em>which</em> accidental complexity earns its keep.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>sw-checklist</strong></td>
        <td><a href="https://github.com/softwarewrighter/sw-checklist">softwarewrighter/sw-checklist</a></td>
      </tr>
      <tr>
        <td><strong>No Silver Bullet (Wikipedia)</strong></td>
        <td><a href="https://en.wikipedia.org/wiki/No_Silver_Bullet">en.wikipedia.org/wiki/No_Silver_Bullet</a></td>
      </tr>
      <tr>
        <td><strong>Rich Hickey — Simple Made Easy</strong></td>
        <td><a href="https://www.infoq.com/presentations/Simple-Made-Easy/">infoq.com/presentations/Simple-Made-Easy</a></td>
      </tr>
      <tr>
        <td><strong>Prior Personal Software post</strong></td>
        <td><a href="/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion/">pjmai-rs: Navigation History and Fuzzy Completion</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="brooks-essential-vs-accidental-complexity">Brooks: Essential vs Accidental Complexity</h2>

<p>Fred Brooks’s <em>No Silver Bullet</em> (1986, later folded into the 20th-anniversary edition of <em>The Mythical Man-Month</em>) draws the line that has framed this argument for forty years:</p>

<ul>
  <li><strong>Essential complexity</strong> is the complexity inherent in the problem itself. Modeling tax law is hard because tax law is hard. There is no clever framework that erases the irregularities of the rules.</li>
  <li><strong>Accidental complexity</strong> is the complexity introduced by the tools, languages, and processes we use to attack the problem. CRUD boilerplate, build-system friction, framework idioms — none of it is part of the problem; all of it is part of the cost of solving the problem with the tools at hand.</li>
</ul>

<p>Brooks’s punchline was that decades of progress had eaten most of the <em>accidental</em> complexity (assemblers, then high-level languages, then garbage collection, then better debuggers), and that future productivity gains would have to come from attacking <em>essential</em> complexity — which is much harder, because it sits inside the problem and refuses to be abstracted away.</p>

<p>That framing still holds. What it does not say — and what is the interesting modern question — is that <em>not all accidental complexity is waste</em>. Some of it is investment. Some of it pays rent.</p>

<h2 id="hickey-simple-vs-easy-complect-vs-decomplect">Hickey: Simple vs Easy, Complect vs Decomplect</h2>

<p>Rich Hickey’s <em>Simple Made Easy</em> (Strange Loop 2011) sharpens the same axis from a different angle. Restating it briefly — and this is my paraphrase, not a quote:</p>

<ul>
  <li><strong>Simple</strong> is <em>un-complected</em>: one role, one task, one concept, not braided with anything else. The Latin root is <em>simplex</em> — one fold.</li>
  <li><strong>Easy</strong> is <em>familiar</em> and <em>near-at-hand</em>: requires little new learning, fits the muscles you already have.</li>
  <li><strong>Complex</strong> is <em>complected</em> — braided, interleaved, two or more concerns sharing a single piece of code.</li>
</ul>

<p>Hickey’s central claim is that we mistake <em>easy</em> for <em>simple</em>. Reaching for the familiar tool is easy, but it often produces complected code: classes that hold both state and identity, functions that mix decisions with effects, modules that interleave domain logic with transport. Easy now, complex later. <em>Simple</em>, by contrast, is often <em>not</em> easy — it requires more upfront thought to keep concerns separated — but the resulting code is decomplected and stays decomplected as it grows.</p>

<p>Brooks tells you <em>what kind</em> of complexity you are paying. Hickey tells you <em>how the payment compounds</em>. Together they suggest a strategy: accept some accidental cost up front if and only if the payment buys you <em>simple</em> — decomplected, single-role — code.</p>

<h2 id="the-investment-thesis">The Investment Thesis</h2>

<p>This is the move I want to defend: <em>some accidental complexity is the cheapest known way to preserve the focus on essential complexity</em>.</p>

<p>A linter is accidental. It rejects code that the language would otherwise compile. The cost is real — the AI agent burns tokens fixing line lengths, the human burns minutes reading diagnostics. The payment is that the next reader can recognize patterns instantly because the surface is uniform.</p>

<p>A TDD loop is accidental. It demands two passes for every line of behavior — the test that fails, then the code that makes it pass. The cost is doubled output. The payment is that essential changes become localized: when the model of the world is wrong, the test names tell you exactly which assumption broke.</p>

<p>Code metrics are accidental. There is nothing wrong, in the language sense, with a 600-line file or a module that holds 12 functions. The payment is that thresholds force the <em>next</em> level of decomposition — a 25-line function ceiling makes you name the sub-step; a 4-functions-per-module warning makes you ask whether two of those functions are really one thing braided with another. Hickey would call that <em>decomplecting</em>.</p>

<p>In all three cases the accidental complexity is paying rent on essential clarity.</p>

<h2 id="sw-checklist-as-a-forcing-function-on-ai-agents">sw-checklist as a Forcing Function on AI Agents</h2>

<p><a href="https://github.com/softwarewrighter/sw-checklist">sw-checklist</a> is a Rust CLI I run against a project to check conformance. It auto-detects project type — Rust crate, workspace, CLI tool, web UI — and runs the appropriate checks. The interesting checks for this post are the <em>modularity</em> ones:</p>

<table>
  <thead>
    <tr>
      <th>Check</th>
      <th style="text-align: right">Warn</th>
      <th style="text-align: right">Fail</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Function lines of code</td>
      <td style="text-align: right">&gt; 25</td>
      <td style="text-align: right">&gt; 50</td>
    </tr>
    <tr>
      <td>File lines of code</td>
      <td style="text-align: right">&gt; 350</td>
      <td style="text-align: right">&gt; 500</td>
    </tr>
    <tr>
      <td>Functions per module</td>
      <td style="text-align: right">&gt; 4</td>
      <td style="text-align: right">&gt; 7</td>
    </tr>
    <tr>
      <td>Modules per crate</td>
      <td style="text-align: right">&gt; 4</td>
      <td style="text-align: right">&gt; 7</td>
    </tr>
    <tr>
      <td>Crates per project</td>
      <td style="text-align: right">&gt; 4</td>
      <td style="text-align: right">&gt; 7</td>
    </tr>
  </tbody>
</table>

<p>The numbers themselves are debatable. What is not debatable is what they do to the <em>agent’s</em> behavior over time. There are two regimes:</p>

<ul>
  <li><strong>Reactive.</strong> The agent writes whatever it wants, runs the checklist, sees a failure on “module X has 9 functions,” and rewrites to split. This costs tokens. It also produces good code — the split is often the decomplecting step that the agent would have skipped on its own.</li>
  <li><strong>Anticipatory.</strong> Eventually the agent starts splitting <em>before</em> the checklist runs. New code lands closer to the thresholds on the first pass, with named sub-modules and small functions, because the agent has internalized the constraint as a writing style rather than a post-hoc fix-up.</li>
</ul>

<p>The transition is the interesting moment. It does not happen on day one. It happens after enough cycles that the model has built up a gradient toward the shape of the constraint. From then on, the accidental cost shrinks because the agent is no longer paying for re-writes — it is writing decomplected code on the first try.</p>

<p>The linter and the TDD loop have the same shape. Each one is an accidental cost up front and a <em>style</em> eventually. Each one decomplects a different axis: the linter normalizes the surface, TDD localizes the model, sw-checklist enforces the decomposition.</p>

<h2 id="the-ratchet">The Ratchet</h2>

<div class="gutter-section">

  <p><img src="/assets/images/posts/block-ratchet-wrench.png" class="gutter-img-left no-invert" alt="Ratchet wrench" /></p>

  <p>Tech debt grows when <em>anything</em> is allowed to slide. The naive policy — “we’ll clean it up later” — reliably produces a heap. The opposite policy — “no debt, ever” — reliably produces paralysis.</p>

  <p>The policy I aim for is a ratchet:</p>

  <ul>
    <li>The wrench turns one way.</li>
    <li>Sometimes a tooth slips back — a temporary hack, a function over the threshold, a test marked <code class="language-plaintext highlighter-rouge">ignore</code> while a bigger refactor lands.</li>
    <li>The next click <em>must</em> be forward. Slips are bounded; the trend is monotone.</li>
  </ul>

  <p>A ratchet does not promise that every commit reduces debt. It promises that the <em>cumulative</em> direction is reduction. That promise is enforceable: at every cycle, ask “is the metric better or worse than last commit?” and refuse to merge a regression unless it is consciously, visibly, paying for a larger forward turn.</p>

  <p>This is the connecting tissue between the Brooks framing and the day-to-day. If accidental complexity is investment, the ratchet is what protects the investment from being clawed back by the next sprint’s deadline.</p>

</div>

<h2 id="de-complexifying-as-a-practice">De-complexifying as a Practice</h2>

<p>Pull the threads together:</p>

<ol>
  <li>Brooks: distinguish accidental from essential complexity, and <em>spend on accidental cost</em> only where it preserves essential clarity.</li>
  <li>Hickey: aim for <em>simple</em> (decomplected), not just <em>easy</em> (familiar). The two are not the same and confusing them is how complected code grows.</li>
  <li>The investment thesis: linter, TDD, and code metrics are accidental costs that pay rent in decomplecting — they make essential complexity stay visible.</li>
  <li>The forcing function: AI agents respond to constraints first reactively, then anticipatorily. The sooner the constraint is in the loop, the sooner the agent’s output moves from messy-and-correct to focused-and-correct.</li>
  <li>The ratchet: tech debt may slip a tooth but the wrench only turns one way. Cumulative direction matters more than per-commit purity.</li>
</ol>

<p>The phrase I keep coming back to is <em>de-complexify</em>. It is not the same as <em>simplify</em>. Simplifying is reducing scope; de-complexifying is keeping the same scope while pulling the strands apart so each strand has one job. Hickey’s word for the move is <em>decomplect</em>. Brooks would call the result a code base where the essential complexity is <em>visible</em> and the accidental complexity is <em>bounded</em>.</p>

<p>Vibe-coding done well is exactly that: AI agents producing focused, decomplected code, fast, because the constraints are doing the de-complexifying work in the background and the agent has learned to write inside them.</p>

<p>The constraints cost something. They are accidental complexity. They are also the only way I have found to keep the essential complexity in view long enough to actually solve it. sw-checklist is one piece of that personal-software toolkit — a small CLI whose only job is to make the next decomplecting step impossible to ignore.</p>]]></content><author><name>Software Wrighter</name></author><category term="cli-tools" /><category term="rust" /><category term="ai-tools" /><category term="sw-checklist" /><category term="personal-software" /><category term="rust" /><category term="cli" /><category term="code-metrics" /><category term="accidental-complexity" /><category term="essential-complexity" /><category term="brooks" /><category term="mythical-man-month" /><category term="no-silver-bullet" /><category term="hickey" /><category term="simple-made-easy" /><category term="tech-debt" /><category term="ratchet" /><category term="vibe-coding" /><category term="ai-coding" /><category term="tdd" /><category term="linter" /><category term="forcing-function" /><summary type="html"><![CDATA[When I say “vibe-coding,” the quotes are doing real work. I am not turning the AI loose and accepting whatever lands. I am using AI agents the same way I’d use a sharp tool with a guard on it: deliberately, with constraints that are themselves additional work. The constraints are accidental complexity in Brooks’s sense — they come from how I choose to build, not from the problem itself — and yet I’d argue they are the only reason the code stays focused on the essential complexity that actually matters. sw-checklist is the personal-software tool I wrote to keep that discipline in the loop. Why this matters — It is easy to confuse “the AI is fast” with “the AI is producing good code.” Without forcing functions — code metrics, a linter, a TDD loop — a generative agent will happily emit a 600-line file with 12 functions per module and 9 modules per crate, none of which are technically wrong, all of which are technically a mess. The interesting question is not whether to spend on accidental complexity, but which accidental complexity earns its keep. Resource Link sw-checklist softwarewrighter/sw-checklist No Silver Bullet (Wikipedia) en.wikipedia.org/wiki/No_Silver_Bullet Rich Hickey — Simple Made Easy infoq.com/presentations/Simple-Made-Easy Prior Personal Software post pjmai-rs: Navigation History and Fuzzy Completion Comments Discord Brooks: Essential vs Accidental Complexity Fred Brooks’s No Silver Bullet (1986, later folded into the 20th-anniversary edition of The Mythical Man-Month) draws the line that has framed this argument for forty years: Essential complexity is the complexity inherent in the problem itself. Modeling tax law is hard because tax law is hard. There is no clever framework that erases the irregularities of the rules. Accidental complexity is the complexity introduced by the tools, languages, and processes we use to attack the problem. CRUD boilerplate, build-system friction, framework idioms — none of it is part of the problem; all of it is part of the cost of solving the problem with the tools at hand. Brooks’s punchline was that decades of progress had eaten most of the accidental complexity (assemblers, then high-level languages, then garbage collection, then better debuggers), and that future productivity gains would have to come from attacking essential complexity — which is much harder, because it sits inside the problem and refuses to be abstracted away. That framing still holds. What it does not say — and what is the interesting modern question — is that not all accidental complexity is waste. Some of it is investment. Some of it pays rent. Hickey: Simple vs Easy, Complect vs Decomplect Rich Hickey’s Simple Made Easy (Strange Loop 2011) sharpens the same axis from a different angle. Restating it briefly — and this is my paraphrase, not a quote: Simple is un-complected: one role, one task, one concept, not braided with anything else. The Latin root is simplex — one fold. Easy is familiar and near-at-hand: requires little new learning, fits the muscles you already have. Complex is complected — braided, interleaved, two or more concerns sharing a single piece of code. Hickey’s central claim is that we mistake easy for simple. Reaching for the familiar tool is easy, but it often produces complected code: classes that hold both state and identity, functions that mix decisions with effects, modules that interleave domain logic with transport. Easy now, complex later. Simple, by contrast, is often not easy — it requires more upfront thought to keep concerns separated — but the resulting code is decomplected and stays decomplected as it grows. Brooks tells you what kind of complexity you are paying. Hickey tells you how the payment compounds. Together they suggest a strategy: accept some accidental cost up front if and only if the payment buys you simple — decomplected, single-role — code. The Investment Thesis This is the move I want to defend: some accidental complexity is the cheapest known way to preserve the focus on essential complexity. A linter is accidental. It rejects code that the language would otherwise compile. The cost is real — the AI agent burns tokens fixing line lengths, the human burns minutes reading diagnostics. The payment is that the next reader can recognize patterns instantly because the surface is uniform. A TDD loop is accidental. It demands two passes for every line of behavior — the test that fails, then the code that makes it pass. The cost is doubled output. The payment is that essential changes become localized: when the model of the world is wrong, the test names tell you exactly which assumption broke. Code metrics are accidental. There is nothing wrong, in the language sense, with a 600-line file or a module that holds 12 functions. The payment is that thresholds force the next level of decomposition — a 25-line function ceiling makes you name the sub-step; a 4-functions-per-module warning makes you ask whether two of those functions are really one thing braided with another. Hickey would call that decomplecting. In all three cases the accidental complexity is paying rent on essential clarity. sw-checklist as a Forcing Function on AI Agents sw-checklist is a Rust CLI I run against a project to check conformance. It auto-detects project type — Rust crate, workspace, CLI tool, web UI — and runs the appropriate checks. The interesting checks for this post are the modularity ones: Check Warn Fail Function lines of code &gt; 25 &gt; 50 File lines of code &gt; 350 &gt; 500 Functions per module &gt; 4 &gt; 7 Modules per crate &gt; 4 &gt; 7 Crates per project &gt; 4 &gt; 7 The numbers themselves are debatable. What is not debatable is what they do to the agent’s behavior over time. There are two regimes: Reactive. The agent writes whatever it wants, runs the checklist, sees a failure on “module X has 9 functions,” and rewrites to split. This costs tokens. It also produces good code — the split is often the decomplecting step that the agent would have skipped on its own. Anticipatory. Eventually the agent starts splitting before the checklist runs. New code lands closer to the thresholds on the first pass, with named sub-modules and small functions, because the agent has internalized the constraint as a writing style rather than a post-hoc fix-up. The transition is the interesting moment. It does not happen on day one. It happens after enough cycles that the model has built up a gradient toward the shape of the constraint. From then on, the accidental cost shrinks because the agent is no longer paying for re-writes — it is writing decomplected code on the first try. The linter and the TDD loop have the same shape. Each one is an accidental cost up front and a style eventually. Each one decomplects a different axis: the linter normalizes the surface, TDD localizes the model, sw-checklist enforces the decomposition. The Ratchet Tech debt grows when anything is allowed to slide. The naive policy — “we’ll clean it up later” — reliably produces a heap. The opposite policy — “no debt, ever” — reliably produces paralysis. The policy I aim for is a ratchet: The wrench turns one way. Sometimes a tooth slips back — a temporary hack, a function over the threshold, a test marked ignore while a bigger refactor lands. The next click must be forward. Slips are bounded; the trend is monotone. A ratchet does not promise that every commit reduces debt. It promises that the cumulative direction is reduction. That promise is enforceable: at every cycle, ask “is the metric better or worse than last commit?” and refuse to merge a regression unless it is consciously, visibly, paying for a larger forward turn. This is the connecting tissue between the Brooks framing and the day-to-day. If accidental complexity is investment, the ratchet is what protects the investment from being clawed back by the next sprint’s deadline. De-complexifying as a Practice Pull the threads together: Brooks: distinguish accidental from essential complexity, and spend on accidental cost only where it preserves essential clarity. Hickey: aim for simple (decomplected), not just easy (familiar). The two are not the same and confusing them is how complected code grows. The investment thesis: linter, TDD, and code metrics are accidental costs that pay rent in decomplecting — they make essential complexity stay visible. The forcing function: AI agents respond to constraints first reactively, then anticipatorily. The sooner the constraint is in the loop, the sooner the agent’s output moves from messy-and-correct to focused-and-correct. The ratchet: tech debt may slip a tooth but the wrench only turns one way. Cumulative direction matters more than per-commit purity. The phrase I keep coming back to is de-complexify. It is not the same as simplify. Simplifying is reducing scope; de-complexifying is keeping the same scope while pulling the strands apart so each strand has one job. Hickey’s word for the move is decomplect. Brooks would call the result a code base where the essential complexity is visible and the accidental complexity is bounded. Vibe-coding done well is exactly that: AI agents producing focused, decomplected code, fast, because the constraints are doing the de-complexifying work in the background and the agent has learned to write inside them. The constraints cost something. They are accidental complexity. They are also the only way I have found to keep the essential complexity in view long enough to actually solve it. sw-checklist is one piece of that personal-software toolkit — a small CLI whose only job is to make the next decomplecting step impossible to ignore.]]></summary></entry><entry><title type="html">Saw (8/?): Tuplet, Smalltalk-on-BASIC, Forth-from-Forth, sw-MLPL Split, and I2C on COR24</title><link href="https://software-wrighter-lab.github.io/2026/04/26/saw-tuplet-smalltalk-forth-from-forth/" rel="alternate" type="text/html" title="Saw (8/?): Tuplet, Smalltalk-on-BASIC, Forth-from-Forth, sw-MLPL Split, and I2C on COR24" /><published>2026-04-26T09:30:00-07:00</published><updated>2026-04-26T09:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/26/saw-tuplet-smalltalk-forth-from-forth</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/26/saw-tuplet-smalltalk-forth-from-forth/"><![CDATA[<p><img src="/assets/images/posts/block-gray-beard-sharpen-saw.png" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>Eighth Sharpen the Saw update. <a href="/2026/04/19/saw-prolog-many-agents-self-hosting-mlpl/">Last time</a> the theme was <em>controlled scale</em>—more agents, more languages, more layers, with infrastructure underneath to keep growth reliable. This week the theme is <em>forcing functions</em>: writing real programs in a language exposes missing language features, and outgrowing a single laptop exposes missing project structure. Either way, what breaks drives the next round of work.</p>

  <p>Six threads, one idea: dogfood the stack and let the gaps—missing features, missing input methods, missing peripherals, an over-large build—drive what gets sharpened next.</p>

</div>

<div class="aside-box">

  <p><strong>Why Sharpen the Saw?</strong> — The name comes from Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">Habit 7</a>: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains, language platforms—so the feature work on top goes faster.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Tuplet</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/tuplet">github.com/sw-vibe-coding/tuplet</a></td>
      </tr>
      <tr>
        <td><strong>Smalltalk on COR24 BASIC</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-smalltalk">github.com/sw-embed/sw-cor24-smalltalk</a></td>
      </tr>
      <tr>
        <td><strong>Smalltalk video (YouTube Short)</strong></td>
        <td><a href="https://www.youtube.com/shorts/fL5NLSKkLoU">youtube.com/shorts/fL5NLSKkLoU</a></td>
      </tr>
      <tr>
        <td><strong>Forth-from-Forth (proto-forth-wasm)</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/sw-fth-wasm">github.com/sw-vibe-coding/sw-fth-wasm</a> — <a href="https://sw-vibe-coding.github.io/sw-fth-wasm/">live demo</a></td>
      </tr>
      <tr>
        <td><strong>COR24 Demo Hub (Status tab)</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">sw-embed.github.io/web-sw-cor24-demos</a></td>
      </tr>
      <tr>
        <td><strong>Repos &amp; Live Demos</strong></td>
        <td><a href="#repos-and-live-demos">Table below</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/04/19/saw-prolog-many-agents-self-hosting-mlpl/">Saw (7/?): Prolog, Many-Agent Isolation, Self-Hosting Assembler, and MLPL</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="tuplet-a-glyph-and-whitespace-language-playground">Tuplet: A Glyph-and-Whitespace Language Playground</h2>

<p><img src="/assets/images/posts/tuplet-badge.png" class="post-marker no-invert" alt="Tuplet badge" style="width: 180px;" /></p>

<p><a href="https://github.com/sw-vibe-coding/tuplet">Tuplet</a> is a new experimental infix language with <strong>first-class named tuple bundles</strong>, <strong>multi-output verbs</strong>, <strong>call-site argument splicing</strong>, and a <strong>glyph-heavy surface syntax</strong>. The kernel is small (~10 forms); everything else—<code class="language-plaintext highlighter-rouge">if/then/else</code>, <code class="language-plaintext highlighter-rouge">while</code>, every operator, every helper—is defined in a prelude using a single mechanism called <strong>mint</strong> (<code class="language-plaintext highlighter-rouge">▪</code>, BLACK SMALL SQUARE, U+25AA). User code can read, replace, or extend the same definitions the prelude uses. Tuplet compiles to Forth and runs on the COR24 runtime.</p>

<p>A taste of the surface syntax—a <code class="language-plaintext highlighter-rouge">Power</code> verb that uses Tuplet glyphs for type signature, assignment, and a piecewise definition:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                                      ⎧ 1 →
   ▪ Power (n : ℤ  e : ℤ) → (p : ℤ) ← ⎨ loop e times    iff e is positive
                                      ⎩ n (×) →
</code></pre></div></div>

<p>The glyphs in that fragment are: ▪ (BLACK SMALL SQUARE, U+25AA, mint), ℤ (DOUBLE-STRUCK Z, U+2124, integer type), → (RIGHTWARDS ARROW, U+2192, map / signature), ← (LEFTWARDS ARROW, U+2190, assign), ⎧⎨⎩ (LEFT CURLY BRACKET UPPER HOOK + MIDDLE PIECE + LOWER HOOK, U+23A7/U+23A8/U+23A9, multi-line piecewise group), and × (MULTIPLICATION SIGN, U+00D7, multiplication). Every glyph has an ASCII fallback the lexer accepts, and the lexer folds Unicode forms to ASCII canonicals at lex time so AST output and error messages stay portable. See <a href="https://github.com/sw-vibe-coding/tuplet/blob/main/docs/glyphs.md"><code class="language-plaintext highlighter-rouge">docs/glyphs.md</code></a> for the full alphabet.</p>

<p>Two things make Tuplet immediately practical:</p>

<ul>
  <li><strong>Implementation language</strong>: Tuplet’s host implementation is an OCaml subset that runs on <a href="https://github.com/sw-embed/sw-cor24-ocaml">sw-cor24-ocaml</a>. Using OCaml as a load-bearing part of the toolchain forced a round of new features in the OCaml interpreter—details in the Smalltalk-and-Tuplet section below.</li>
  <li><strong>Compilation target</strong>: Tuplet lowers to Forth, so it inherits whatever the COR24 Forth and Forth-from-Forth efforts deliver. New Forth features cascade into Tuplet for free.</li>
</ul>

<h2 id="glyph-input-infrastructure-espanso-and-emacs">Glyph Input Infrastructure: Espanso and Emacs</h2>

<p>A glyph language is unusable if you can’t type it. The glyph-entry layer is its own piece of the toolchain, and it now has two parts:</p>

<ul>
  <li><strong><a href="https://espanso.org/">Espanso</a></strong> is a cross-platform text expander. Installing and configuring Espanso gives every editor and terminal a shared way to type Tuplet glyphs by short trigger sequences. The same configuration also covers <a href="https://www.gnu.org/software/apl/">GNU APL</a> glyph entry—APL has the canonical “language with non-ASCII glyphs” problem, and Espanso solves both at once. See <a href="https://github.com/sw-vibe-coding/tuplet/blob/main/docs/cli-inputs.md"><code class="language-plaintext highlighter-rouge">docs/cli-inputs.md</code></a> for the Tuplet trigger conventions.</li>
  <li><strong>Emacs</strong> has its own glyph-entry configuration so the same triggers work natively in Emacs buffers without going through Espanso. TeX-style input methods, Agda-style mappings, and a custom Quail input method are all options—see <a href="https://github.com/sw-vibe-coding/tuplet/blob/main/docs/emacs-inputs.md"><code class="language-plaintext highlighter-rouge">docs/emacs-inputs.md</code></a>. This keeps the editing experience consistent whether you’re in a terminal REPL, a browser-based demo, or an Emacs buffer.</li>
</ul>

<p>Glyph entry counts as Sharpen the Saw work because it is <em>infrastructure for future experiments</em>: every new language that wants non-ASCII syntax—Tuplet, GNU APL, the next experiment—rides on the same input layer.</p>

<h2 id="smalltalk-on-cor24-basic-dogfooding-as-a-forcing-function">Smalltalk on COR24 BASIC: Dogfooding as a Forcing Function</h2>

<div class="post-aside-right" style="width: 200px;">
<img src="/assets/images/posts/smalltalk-badge.png" alt="COR24 Smalltalk badge" style="width: 100%;" />
</div>

<p>A small <strong>integer/toy Smalltalk</strong> is now running, implemented in <strong>COR24 BASIC v1</strong>—a 26-variable, no-array, integer-only BASIC interpreter. The Smalltalk is in the spirit of the ~1000-line BASIC-hosted Smalltalk evaluator that Alan Kay’s group built in 1972. It has a tagged-pointer object encoding (low-bit-1 SmallIntegers, low-bit-0 heap pointers), 14 bytecodes, 6 primitives in v0, and a real <code class="language-plaintext highlighter-rouge">CLASSOF -&gt; LOOKUP -&gt; ACTIVATE -&gt; primitive-or-frame-push</code> dispatch loop. The whole heap, method table, and frames live in 1024 24-bit words of <code class="language-plaintext highlighter-rouge">PEEK</code>/<code class="language-plaintext highlighter-rouge">POKE</code> scratch RAM. Three nested fetch/decode/execute loops (p-code VM, BASIC interpreter, Tinytalk VM) run at all times.</p>

<p><a href="https://www.youtube.com/shorts/fL5NLSKkLoU">Video walkthrough (YouTube Short): https://www.youtube.com/shorts/fL5NLSKkLoU</a></p>

<p>The point is not speed. The point is to make the OO mental model <em>visible</em>—every dispatch step is a numbered BASIC line you can single-step through—and to <strong>dogfood</strong> COR24 BASIC by writing a real, recognizable system in it. The forcing function worked: writing Smalltalk in BASIC immediately revealed gaps. Six BASIC feature requests landed and closed this week:</p>

<div class="post-aside-right" style="width: 180px;">
<img src="/assets/images/posts/basic-badge.png" alt="COR24 BASIC badge" style="width: 100%;" />
</div>

<table>
  <thead>
    <tr>
      <th>BASIC issue</th>
      <th>Feature</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/3">FR-1</a></td>
      <td>DIM integer arrays</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/2">FR-2</a></td>
      <td>DATA / READ / RESTORE statements</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/4">FR-3</a></td>
      <td>ON expr GOTO/GOSUB statements</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/5">FR-4</a></td>
      <td>MOD operator</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/6">FR-5</a></td>
      <td>Bitwise operators (BAND/BOR/BXOR/SHL/SHR)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic/issues/7">FR-6</a></td>
      <td>CONT after STOP</td>
    </tr>
  </tbody>
</table>

<p>Tagged-pointer dispatch needs the bitwise ops; method tables and bytecode arrays want <code class="language-plaintext highlighter-rouge">DIM</code>; <code class="language-plaintext highlighter-rouge">DATA</code>/<code class="language-plaintext highlighter-rouge">READ</code> is the natural way to ship the boot image; <code class="language-plaintext highlighter-rouge">ON expr GOTO</code> is the dispatch-jump that powers the bytecode interpreter. Each was a real shortcoming Smalltalk hit, not a speculative wishlist item. The <a href="https://sw-embed.github.io/web-sw-cor24-basic/">BASIC live demo</a> ships them all.</p>

<p>The same forcing-function pattern played out for Tuplet on the OCaml side. Eleven OCaml interpreter issues closed in the last 24 hours, almost all driven by Tuplet’s host code:</p>

<div class="post-aside-right" style="width: 180px;">
<img src="/assets/images/posts/ocaml-badge.png" alt="COR24 OCaml badge" style="width: 100%;" />
</div>

<table>
  <thead>
    <tr>
      <th>OCaml issue</th>
      <th>Feature</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/1">#1</a></td>
      <td>TRAP 2 in <code class="language-plaintext highlighter-rouge">demo_adventure</code> (round-trip bug)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/2">#2</a></td>
      <td>User-defined variant types (algebraic data types)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/3">#3</a></td>
      <td>Top-level <code class="language-plaintext highlighter-rouge">let</code> bindings (without <code class="language-plaintext highlighter-rouge">in EXPR</code>)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/4">#4</a></td>
      <td>String escapes (<code class="language-plaintext highlighter-rouge">\n</code>, <code class="language-plaintext highlighter-rouge">\t</code>, …) and arbitrary tuples (3+)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/5">#5</a></td>
      <td>Multi-line <code class="language-plaintext highlighter-rouge">match</code> expressions</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/6">#6</a></td>
      <td>Mutable references (<code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">:=</code>)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/7">#7</a></td>
      <td>Record types and field access</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/8">#8</a></td>
      <td>List combinators (<code class="language-plaintext highlighter-rouge">List.map</code>, <code class="language-plaintext highlighter-rouge">List.fold_left</code>, <code class="language-plaintext highlighter-rouge">List.filter</code>)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/9">#9</a></td>
      <td>Block comments <code class="language-plaintext highlighter-rouge">(* ... *)</code></td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/10">#10</a></td>
      <td>Char literals <code class="language-plaintext highlighter-rouge">'a'</code> + <code class="language-plaintext highlighter-rouge">Char.code</code> / <code class="language-plaintext highlighter-rouge">Char.chr</code></td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml/issues/11">#11</a></td>
      <td>Exceptions (<code class="language-plaintext highlighter-rouge">raise</code> / <code class="language-plaintext highlighter-rouge">try</code> / <code class="language-plaintext highlighter-rouge">with</code>) or a <code class="language-plaintext highlighter-rouge">Result</code> type</td>
    </tr>
  </tbody>
</table>

<p>A lexer, AST, and IR lowering pipeline needs records, variants, top-level lets, multi-line match, mutable refs, and exceptions just to be writable. The <a href="https://sw-embed.github.io/web-sw-cor24-ocaml/">OCaml live demo</a> now ships every one of those. The Forth side picked up the matching upgrades—<code class="language-plaintext highlighter-rouge">:NONAME</code> for anonymous colon definitions and a <code class="language-plaintext highlighter-rouge">SEE-CFA</code> truncation fix—in <a href="https://github.com/sw-embed/sw-cor24-forth">sw-cor24-forth</a> over the same window.</p>

<h2 id="forth-from-forth-a-browser-native-forth-via-wasm">Forth-from-Forth: A Browser-Native Forth via WASM</h2>

<p><a href="https://github.com/sw-vibe-coding/sw-fth-wasm">Forth-from-Forth</a> (<code class="language-plaintext highlighter-rouge">proto-forth-wasm</code>) is a browser-native Forth running on Rust/WASM. The Rust <code class="language-plaintext highlighter-rouge">Machine</code> owns the data stack, return stack, memory, dictionary, tokenizer, compiler, and VM loop; colon definitions compile to an opcode IR; user-word execution runs on an iterative VM loop with no host-stack recursion for nested calls. The vocabulary already covers arithmetic, stack shuffling, comparisons, structured control flow (<code class="language-plaintext highlighter-rouge">IF/ELSE/THEN</code>, <code class="language-plaintext highlighter-rouge">BEGIN/UNTIL</code>, <code class="language-plaintext highlighter-rouge">BEGIN/WHILE/REPEAT</code>, <code class="language-plaintext highlighter-rouge">DO/LOOP</code> with <code class="language-plaintext highlighter-rouge">I</code>), memory (<code class="language-plaintext highlighter-rouge">VARIABLE</code>, <code class="language-plaintext highlighter-rouge">CONSTANT</code>, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">+!</code>, <code class="language-plaintext highlighter-rouge">ALLOT</code>), the return stack, I/O, and introspection (<code class="language-plaintext highlighter-rouge">SEE</code>, <code class="language-plaintext highlighter-rouge">WORDS</code>).</p>

<p>The <a href="https://sw-vibe-coding.github.io/sw-fth-wasm/">live demo</a> puts the REPL, source pane, stack, dictionary, output, history, and trace all on one page. WASM matters here for the same reason a self-hosting toolchain matters elsewhere: zero-install distribution. A reader can land on the page and have a working Forth in seconds.</p>

<p>Two Forth-in-Forth issues closed this week pulled the project closer to self-hosting at the language level: <a href="https://github.com/sw-embed/sw-cor24-forth/issues/1">#1</a> added a hashed dictionary to speed up <code class="language-plaintext highlighter-rouge">FIND</code>, and <a href="https://github.com/sw-embed/sw-cor24-forth/issues/2">#2</a> added <code class="language-plaintext highlighter-rouge">DO/LOOP</code>, <code class="language-plaintext highlighter-rouge">?DO</code>, <code class="language-plaintext highlighter-rouge">WHILE/REPEAT</code>, <code class="language-plaintext highlighter-rouge">AGAIN</code>, <code class="language-plaintext highlighter-rouge">CONSTANT</code>, and <code class="language-plaintext highlighter-rouge">VARIABLE</code> to the Forth-in-Forth implementation. Open issue <a href="https://github.com/sw-embed/sw-cor24-forth/issues/3">#3</a> tracks the remaining standard words: <code class="language-plaintext highlighter-rouge">+LOOP/J/LEAVE</code>, <code class="language-plaintext highlighter-rouge">DOES&gt;</code>, <code class="language-plaintext highlighter-rouge">RECURSE</code>, <code class="language-plaintext highlighter-rouge">PICK/ROLL/?DUP/MIN/MAX/&lt;=/&gt;=/&lt;&gt;</code>.</p>

<p>A <strong>Forth-builder</strong> is in planning—a small kit (inner interpreter, dictionary, I/O backend, target word set) that composes Forth systems by configuration instead of by fork. Once the builder exists, spinning up a new Forth flavor for an experiment becomes a build-time decision.</p>

<h2 id="sw-mlpl-35-gb-forces-a-split-into-parallelizable-pieces">sw-MLPL: 35 GB Forces a Split into Parallelizable Pieces</h2>

<div class="post-aside-left" style="width: 200px;">
<img src="/assets/images/posts/mlpl-badge.png" alt="sw-MLPL badge" style="width: 100%;" />
</div>

<p><a href="https://github.com/sw-ml-study/sw-mlpl">sw-MLPL</a> (<a href="https://sw-ml-study.github.io/sw-mlpl/">live demo</a>) hit a different kind of forcing function this week: its build directory blew past <strong>35 GB on a MacBook</strong>, and that is <em>without</em> the CUDA backend wired in yet. A single repo trying to hold a CPU runtime, the <a href="https://ml-explore.github.io/mlx/">MLX</a> backend, a future CUDA backend, an interpreter, a compiler, a REPL, and a Web UI is not a structure that fits on one laptop.</p>

<p>The fix is to split:</p>

<ul>
  <li><strong>Linux/CUDA only</strong> — one fork that targets <a href="https://developer.nvidia.com/cuda-toolkit">NVIDIA CUDA</a> and lives on a host with the disk space and toolchain to support it.</li>
  <li><strong>Mac/MLX only</strong> — a sibling fork that stays on Apple Silicon and the MLX unified-memory path, where the build stays small enough to iterate on a laptop.</li>
  <li><strong>Web UI separated</strong> — the in-browser demo carved out into its own repo, decoupled from the runtime build cycle.</li>
  <li><strong>Compiler and REPL separated</strong> — the two have different dependency profiles and different test harnesses; separating them lets each grow without dragging the other’s build along.</li>
</ul>

<p>The win is not just disk space. Once the project is split, different hosts can develop different pieces in parallel: a Linux/CUDA box pushes the GPU backend while a MacBook iterates on MLX or the compiler, and neither waits on the other’s artifacts. The Web UI repo’s CI doesn’t care which backend any given commit targets. This is the same pattern the COR24 stack uses by default—small, single-purpose, parallelizable repos—applied retroactively to a project that grew past the point of fitting in one place.</p>

<h2 id="i2c-on-the-cor24-emulator">I2C on the COR24 Emulator</h2>

<div class="post-aside-right" style="width: 200px;">
<img src="/assets/images/posts/block-bmp280.png" alt="BMP280 I2C sensor breakout" style="width: 100%;" />
</div>

<p>The <a href="https://github.com/sw-embed/sw-cor24-emulator">COR24 emulator</a> is gaining <strong>I2C</strong> support, with example programs to drive it. I2C unlocks the obvious next layer of peripheral experiments—temperature/pressure sensors (the <a href="https://github.com/sw-embed/bmp280">BMP280 repo</a> is already in the tree), small EEPROMs, OLED displays, and any of the usual hobbyist breakout boards. I2C is the right first bus for the emulator: low pin count, well-documented protocol, plenty of devices to talk to, and the bring-up code is small enough that the Smalltalk and Tuplet languages above could plausibly drive a real sensor in a future demo.</p>

<p>The pattern is the same as everywhere else in this post: build the platform piece (the bus, the examples) so that future feature work has somewhere to land.</p>

<h2 id="demo-site-status-tab-tracks-the-new-languages">Demo Site: Status Tab Tracks the New Languages</h2>

<p>The COR24 demo hub now has a <strong>Status tab</strong> that tracks the languages, their recent commits, and their open issues in one place. Adding Tuplet and Smalltalk to the lineup made the per-language pages harder to skim, so the Status tab consolidates:</p>

<ul>
  <li>The current language list (now including <strong>Tuplet</strong> and <strong>Smalltalk</strong>, alongside PL/SW, SNOBOL4, MLPL, BASIC, Forth, OCaml, Pascal, APL, MacroLisp, P-code, TinyC, and the assemblers).</li>
  <li>Recent commits per language—a single cross-repo activity feed.</li>
  <li>Open issues per language, so the in-flight work is visible without bouncing between GitHub repos.</li>
</ul>

<p><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub (and Status tab): sw-embed.github.io/web-sw-cor24-demos/#/</a></p>

<p>The Status tab is the same kind of investment as the glyph-input layer and the I2C bus: <em>infrastructure for moving faster</em>, not a feature in any one language. New languages plug into it; the cost of adding the next one is now bounded.</p>

<h2 id="repos-and-live-demos">Repos and Live Demos</h2>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>GitHub</th>
      <th>Live Demo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Tuplet</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/tuplet">sw-vibe-coding/tuplet</a></td>
      <td><em>in development</em></td>
    </tr>
    <tr>
      <td><strong>Smalltalk on COR24 BASIC</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-smalltalk">sw-embed/sw-cor24-smalltalk</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-smalltalk/">sw-embed.github.io/web-sw-cor24-smalltalk</a></td>
    </tr>
    <tr>
      <td><strong>Forth-from-Forth (WASM)</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/sw-fth-wasm">sw-vibe-coding/sw-fth-wasm</a></td>
      <td><a href="https://sw-vibe-coding.github.io/sw-fth-wasm/">sw-vibe-coding.github.io/sw-fth-wasm</a></td>
    </tr>
    <tr>
      <td><strong>COR24 BASIC</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic">sw-embed/sw-cor24-basic</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-basic/">sw-embed.github.io/web-sw-cor24-basic</a></td>
    </tr>
    <tr>
      <td><strong>COR24 Forth</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">sw-embed.github.io/web-sw-cor24-forth</a></td>
    </tr>
    <tr>
      <td><strong>COR24 OCaml</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-ocaml">sw-embed/sw-cor24-ocaml</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-ocaml/">sw-embed.github.io/web-sw-cor24-ocaml</a></td>
    </tr>
    <tr>
      <td><strong>COR24 Emulator (I2C)</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-emulator">sw-embed/sw-cor24-emulator</a></td>
      <td><a href="https://sw-embed.github.io/cor24-rs/">sw-embed.github.io/cor24-rs</a></td>
    </tr>
    <tr>
      <td><strong>sw-MLPL</strong></td>
      <td><a href="https://github.com/sw-ml-study/sw-mlpl">sw-ml-study/sw-mlpl</a></td>
      <td><a href="https://sw-ml-study.github.io/sw-mlpl/">sw-ml-study.github.io/sw-mlpl</a></td>
    </tr>
    <tr>
      <td><strong>COR24 Demo Hub</strong></td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub</a></td>
    </tr>
  </tbody>
</table>

<h2 id="whats-next">What’s Next</h2>

<p><strong>Tuplet</strong>: Get the Tuplet live demo working alongside the other COR24 language demos, and add a way to enter glyphs directly in the Tuplet web UI (so visitors don’t need Espanso or Emacs configured locally to try the demo).</p>

<p><strong>Smalltalk on COR24 BASIC</strong>: Get the Smalltalk live demo working—a browser-hosted version of the BASIC interpreter running the Tinytalk image, so the YouTube walkthrough has a hands-on counterpart.</p>

<p><strong>Glyph Input + Emacs</strong>: Publish concrete Emacs configuration examples (init snippets, Quail rules, TeX-style mappings) drawn from <code class="language-plaintext highlighter-rouge">docs/emacs-inputs.md</code>, and add <strong>org-mode support</strong>—which means babel blocks for Tuplet <em>and</em> every other COR24 language (BASIC, Forth, OCaml, PL/SW, SNOBOL4, etc.). Babel-blocks across the language set turns org-mode into a real notebook for the COR24 stack.</p>

<p><strong>Forth-from-Forth</strong>: Close the remaining standard Forth words (<a href="https://github.com/sw-embed/sw-cor24-forth/issues/3">sw-cor24-forth #3</a>) and start the Forth-builder kit so new Forth flavors compose by configuration.</p>

<p><strong>sw-MLPL Split</strong>: Land the four-way split (Linux/CUDA, Mac/MLX, Web UI, separated Compiler vs. REPL) and confirm each piece builds cleanly on a host that doesn’t have the others’ dependencies. Republish the live demo from the new Web UI repo.</p>

<p><strong>COR24 Emulator I2C</strong>: Land the first round of I2C example programs end to end (sensor reads, EEPROM round-trip), then expose I2C from the higher-level languages so a Tuplet or Smalltalk program can drive a peripheral.</p>

<hr />

<p><em>Forcing functions sharpen languages faster than feature lists do. Follow for more Sharpen the Saw updates.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="languages" /><category term="compilers" /><category term="forth" /><category term="smalltalk" /><category term="emacs" /><category term="machine-learning" /><category term="sharpen-the-saw" /><category term="tuplet" /><category term="smalltalk" /><category term="cor24" /><category term="basic" /><category term="forth" /><category term="forth-from-forth" /><category term="espanso" /><category term="gnu-apl" /><category term="emacs" /><category term="glyphs" /><category term="wasm" /><category term="dogfooding" /><category term="ocaml" /><category term="i2c" /><category term="mlpl" /><category term="mlx" /><category term="cuda" /><summary type="html"><![CDATA[Eighth Sharpen the Saw update. Last time the theme was controlled scale—more agents, more languages, more layers, with infrastructure underneath to keep growth reliable. This week the theme is forcing functions: writing real programs in a language exposes missing language features, and outgrowing a single laptop exposes missing project structure. Either way, what breaks drives the next round of work. Six threads, one idea: dogfood the stack and let the gaps—missing features, missing input methods, missing peripherals, an over-large build—drive what gets sharpened next. Why Sharpen the Saw? — The name comes from Covey’s Habit 7: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains, language platforms—so the feature work on top goes faster. Resource Link Tuplet github.com/sw-vibe-coding/tuplet Smalltalk on COR24 BASIC github.com/sw-embed/sw-cor24-smalltalk Smalltalk video (YouTube Short) youtube.com/shorts/fL5NLSKkLoU Forth-from-Forth (proto-forth-wasm) github.com/sw-vibe-coding/sw-fth-wasm — live demo COR24 Demo Hub (Status tab) sw-embed.github.io/web-sw-cor24-demos Repos &amp; Live Demos Table below Prior Post Saw (7/?): Prolog, Many-Agent Isolation, Self-Hosting Assembler, and MLPL Comments Discord Tuplet: A Glyph-and-Whitespace Language Playground Tuplet is a new experimental infix language with first-class named tuple bundles, multi-output verbs, call-site argument splicing, and a glyph-heavy surface syntax. The kernel is small (~10 forms); everything else—if/then/else, while, every operator, every helper—is defined in a prelude using a single mechanism called mint (▪, BLACK SMALL SQUARE, U+25AA). User code can read, replace, or extend the same definitions the prelude uses. Tuplet compiles to Forth and runs on the COR24 runtime. A taste of the surface syntax—a Power verb that uses Tuplet glyphs for type signature, assignment, and a piecewise definition: ⎧ 1 → ▪ Power (n : ℤ e : ℤ) → (p : ℤ) ← ⎨ loop e times iff e is positive ⎩ n (×) → The glyphs in that fragment are: ▪ (BLACK SMALL SQUARE, U+25AA, mint), ℤ (DOUBLE-STRUCK Z, U+2124, integer type), → (RIGHTWARDS ARROW, U+2192, map / signature), ← (LEFTWARDS ARROW, U+2190, assign), ⎧⎨⎩ (LEFT CURLY BRACKET UPPER HOOK + MIDDLE PIECE + LOWER HOOK, U+23A7/U+23A8/U+23A9, multi-line piecewise group), and × (MULTIPLICATION SIGN, U+00D7, multiplication). Every glyph has an ASCII fallback the lexer accepts, and the lexer folds Unicode forms to ASCII canonicals at lex time so AST output and error messages stay portable. See docs/glyphs.md for the full alphabet. Two things make Tuplet immediately practical: Implementation language: Tuplet’s host implementation is an OCaml subset that runs on sw-cor24-ocaml. Using OCaml as a load-bearing part of the toolchain forced a round of new features in the OCaml interpreter—details in the Smalltalk-and-Tuplet section below. Compilation target: Tuplet lowers to Forth, so it inherits whatever the COR24 Forth and Forth-from-Forth efforts deliver. New Forth features cascade into Tuplet for free. Glyph Input Infrastructure: Espanso and Emacs A glyph language is unusable if you can’t type it. The glyph-entry layer is its own piece of the toolchain, and it now has two parts: Espanso is a cross-platform text expander. Installing and configuring Espanso gives every editor and terminal a shared way to type Tuplet glyphs by short trigger sequences. The same configuration also covers GNU APL glyph entry—APL has the canonical “language with non-ASCII glyphs” problem, and Espanso solves both at once. See docs/cli-inputs.md for the Tuplet trigger conventions. Emacs has its own glyph-entry configuration so the same triggers work natively in Emacs buffers without going through Espanso. TeX-style input methods, Agda-style mappings, and a custom Quail input method are all options—see docs/emacs-inputs.md. This keeps the editing experience consistent whether you’re in a terminal REPL, a browser-based demo, or an Emacs buffer. Glyph entry counts as Sharpen the Saw work because it is infrastructure for future experiments: every new language that wants non-ASCII syntax—Tuplet, GNU APL, the next experiment—rides on the same input layer. Smalltalk on COR24 BASIC: Dogfooding as a Forcing Function A small integer/toy Smalltalk is now running, implemented in COR24 BASIC v1—a 26-variable, no-array, integer-only BASIC interpreter. The Smalltalk is in the spirit of the ~1000-line BASIC-hosted Smalltalk evaluator that Alan Kay’s group built in 1972. It has a tagged-pointer object encoding (low-bit-1 SmallIntegers, low-bit-0 heap pointers), 14 bytecodes, 6 primitives in v0, and a real CLASSOF -&gt; LOOKUP -&gt; ACTIVATE -&gt; primitive-or-frame-push dispatch loop. The whole heap, method table, and frames live in 1024 24-bit words of PEEK/POKE scratch RAM. Three nested fetch/decode/execute loops (p-code VM, BASIC interpreter, Tinytalk VM) run at all times. Video walkthrough (YouTube Short): https://www.youtube.com/shorts/fL5NLSKkLoU The point is not speed. The point is to make the OO mental model visible—every dispatch step is a numbered BASIC line you can single-step through—and to dogfood COR24 BASIC by writing a real, recognizable system in it. The forcing function worked: writing Smalltalk in BASIC immediately revealed gaps. Six BASIC feature requests landed and closed this week: BASIC issue Feature FR-1 DIM integer arrays FR-2 DATA / READ / RESTORE statements FR-3 ON expr GOTO/GOSUB statements FR-4 MOD operator FR-5 Bitwise operators (BAND/BOR/BXOR/SHL/SHR) FR-6 CONT after STOP Tagged-pointer dispatch needs the bitwise ops; method tables and bytecode arrays want DIM; DATA/READ is the natural way to ship the boot image; ON expr GOTO is the dispatch-jump that powers the bytecode interpreter. Each was a real shortcoming Smalltalk hit, not a speculative wishlist item. The BASIC live demo ships them all. The same forcing-function pattern played out for Tuplet on the OCaml side. Eleven OCaml interpreter issues closed in the last 24 hours, almost all driven by Tuplet’s host code: OCaml issue Feature #1 TRAP 2 in demo_adventure (round-trip bug) #2 User-defined variant types (algebraic data types) #3 Top-level let bindings (without in EXPR) #4 String escapes (\n, \t, …) and arbitrary tuples (3+) #5 Multi-line match expressions #6 Mutable references (ref, !, :=) #7 Record types and field access #8 List combinators (List.map, List.fold_left, List.filter) #9 Block comments (* ... *) #10 Char literals 'a' + Char.code / Char.chr #11 Exceptions (raise / try / with) or a Result type A lexer, AST, and IR lowering pipeline needs records, variants, top-level lets, multi-line match, mutable refs, and exceptions just to be writable. The OCaml live demo now ships every one of those. The Forth side picked up the matching upgrades—:NONAME for anonymous colon definitions and a SEE-CFA truncation fix—in sw-cor24-forth over the same window. Forth-from-Forth: A Browser-Native Forth via WASM Forth-from-Forth (proto-forth-wasm) is a browser-native Forth running on Rust/WASM. The Rust Machine owns the data stack, return stack, memory, dictionary, tokenizer, compiler, and VM loop; colon definitions compile to an opcode IR; user-word execution runs on an iterative VM loop with no host-stack recursion for nested calls. The vocabulary already covers arithmetic, stack shuffling, comparisons, structured control flow (IF/ELSE/THEN, BEGIN/UNTIL, BEGIN/WHILE/REPEAT, DO/LOOP with I), memory (VARIABLE, CONSTANT, @, !, +!, ALLOT), the return stack, I/O, and introspection (SEE, WORDS). The live demo puts the REPL, source pane, stack, dictionary, output, history, and trace all on one page. WASM matters here for the same reason a self-hosting toolchain matters elsewhere: zero-install distribution. A reader can land on the page and have a working Forth in seconds. Two Forth-in-Forth issues closed this week pulled the project closer to self-hosting at the language level: #1 added a hashed dictionary to speed up FIND, and #2 added DO/LOOP, ?DO, WHILE/REPEAT, AGAIN, CONSTANT, and VARIABLE to the Forth-in-Forth implementation. Open issue #3 tracks the remaining standard words: +LOOP/J/LEAVE, DOES&gt;, RECURSE, PICK/ROLL/?DUP/MIN/MAX/&lt;=/&gt;=/&lt;&gt;. A Forth-builder is in planning—a small kit (inner interpreter, dictionary, I/O backend, target word set) that composes Forth systems by configuration instead of by fork. Once the builder exists, spinning up a new Forth flavor for an experiment becomes a build-time decision. sw-MLPL: 35 GB Forces a Split into Parallelizable Pieces sw-MLPL (live demo) hit a different kind of forcing function this week: its build directory blew past 35 GB on a MacBook, and that is without the CUDA backend wired in yet. A single repo trying to hold a CPU runtime, the MLX backend, a future CUDA backend, an interpreter, a compiler, a REPL, and a Web UI is not a structure that fits on one laptop. The fix is to split: Linux/CUDA only — one fork that targets NVIDIA CUDA and lives on a host with the disk space and toolchain to support it. Mac/MLX only — a sibling fork that stays on Apple Silicon and the MLX unified-memory path, where the build stays small enough to iterate on a laptop. Web UI separated — the in-browser demo carved out into its own repo, decoupled from the runtime build cycle. Compiler and REPL separated — the two have different dependency profiles and different test harnesses; separating them lets each grow without dragging the other’s build along. The win is not just disk space. Once the project is split, different hosts can develop different pieces in parallel: a Linux/CUDA box pushes the GPU backend while a MacBook iterates on MLX or the compiler, and neither waits on the other’s artifacts. The Web UI repo’s CI doesn’t care which backend any given commit targets. This is the same pattern the COR24 stack uses by default—small, single-purpose, parallelizable repos—applied retroactively to a project that grew past the point of fitting in one place. I2C on the COR24 Emulator The COR24 emulator is gaining I2C support, with example programs to drive it. I2C unlocks the obvious next layer of peripheral experiments—temperature/pressure sensors (the BMP280 repo is already in the tree), small EEPROMs, OLED displays, and any of the usual hobbyist breakout boards. I2C is the right first bus for the emulator: low pin count, well-documented protocol, plenty of devices to talk to, and the bring-up code is small enough that the Smalltalk and Tuplet languages above could plausibly drive a real sensor in a future demo. The pattern is the same as everywhere else in this post: build the platform piece (the bus, the examples) so that future feature work has somewhere to land. Demo Site: Status Tab Tracks the New Languages The COR24 demo hub now has a Status tab that tracks the languages, their recent commits, and their open issues in one place. Adding Tuplet and Smalltalk to the lineup made the per-language pages harder to skim, so the Status tab consolidates: The current language list (now including Tuplet and Smalltalk, alongside PL/SW, SNOBOL4, MLPL, BASIC, Forth, OCaml, Pascal, APL, MacroLisp, P-code, TinyC, and the assemblers). Recent commits per language—a single cross-repo activity feed. Open issues per language, so the in-flight work is visible without bouncing between GitHub repos. Demo Hub (and Status tab): sw-embed.github.io/web-sw-cor24-demos/#/ The Status tab is the same kind of investment as the glyph-input layer and the I2C bus: infrastructure for moving faster, not a feature in any one language. New languages plug into it; the cost of adding the next one is now bounded. Repos and Live Demos Project GitHub Live Demo Tuplet sw-vibe-coding/tuplet in development Smalltalk on COR24 BASIC sw-embed/sw-cor24-smalltalk sw-embed.github.io/web-sw-cor24-smalltalk Forth-from-Forth (WASM) sw-vibe-coding/sw-fth-wasm sw-vibe-coding.github.io/sw-fth-wasm COR24 BASIC sw-embed/sw-cor24-basic sw-embed.github.io/web-sw-cor24-basic COR24 Forth sw-embed/sw-cor24-forth sw-embed.github.io/web-sw-cor24-forth COR24 OCaml sw-embed/sw-cor24-ocaml sw-embed.github.io/web-sw-cor24-ocaml COR24 Emulator (I2C) sw-embed/sw-cor24-emulator sw-embed.github.io/cor24-rs sw-MLPL sw-ml-study/sw-mlpl sw-ml-study.github.io/sw-mlpl COR24 Demo Hub sw-embed/web-sw-cor24-demos Demo Hub What’s Next Tuplet: Get the Tuplet live demo working alongside the other COR24 language demos, and add a way to enter glyphs directly in the Tuplet web UI (so visitors don’t need Espanso or Emacs configured locally to try the demo). Smalltalk on COR24 BASIC: Get the Smalltalk live demo working—a browser-hosted version of the BASIC interpreter running the Tinytalk image, so the YouTube walkthrough has a hands-on counterpart. Glyph Input + Emacs: Publish concrete Emacs configuration examples (init snippets, Quail rules, TeX-style mappings) drawn from docs/emacs-inputs.md, and add org-mode support—which means babel blocks for Tuplet and every other COR24 language (BASIC, Forth, OCaml, PL/SW, SNOBOL4, etc.). Babel-blocks across the language set turns org-mode into a real notebook for the COR24 stack. Forth-from-Forth: Close the remaining standard Forth words (sw-cor24-forth #3) and start the Forth-builder kit so new Forth flavors compose by configuration. sw-MLPL Split: Land the four-way split (Linux/CUDA, Mac/MLX, Web UI, separated Compiler vs. REPL) and confirm each piece builds cleanly on a host that doesn’t have the others’ dependencies. Republish the live demo from the new Web UI repo. COR24 Emulator I2C: Land the first round of I2C example programs end to end (sensor reads, EEPROM round-trip), then expose I2C from the higher-level languages so a Tuplet or Smalltalk program can drive a peripheral. Forcing functions sharpen languages faster than feature lists do. Follow for more Sharpen the Saw updates.]]></summary></entry><entry><title type="html">Rabbit-hole (5/?): FORTH — Retargetable Codegen and the Forward-Reference Problem</title><link href="https://software-wrighter-lab.github.io/2026/04/26/rabbit-hole-forth-retargetable-codegen-forward-references/" rel="alternate" type="text/html" title="Rabbit-hole (5/?): FORTH — Retargetable Codegen and the Forward-Reference Problem" /><published>2026-04-26T00:15:00-07:00</published><updated>2026-04-26T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/26/rabbit-hole-forth-retargetable-codegen-forward-references</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/26/rabbit-hole-forth-retargetable-codegen-forward-references/"><![CDATA[<p><img src="/assets/images/posts/block-rabbit-tree-sword-left.png" class="post-marker no-invert" alt="" style="width: 220px;" /></p>

<div style="overflow: hidden;">

  <p>Dictionary compaction was the vertical problem: one Forth source tree, many images, each with a different amount of development machinery stripped away. Retargetable code generation is the horizontal problem: one source tree, many machines, each with a different instruction set, calling convention, memory model, and set of awkward things the compiler cannot pretend are the same.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Prior post</strong></td>
        <td><a href="/2026/04/25/rabbit-hole-forth-dictionary-compaction-specialized-images/">Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images</a></td>
      </tr>
      <tr>
        <td><strong>Overview post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-vertical-split-was-only-half-the-problem">The Vertical Split Was Only Half the Problem</h2>

<p>Part 4 framed the Forth composer as a way to build different <em>profiles</em> from the same source:</p>

<table>
  <thead>
    <tr>
      <th>Profile</th>
      <th>Keeps</th>
      <th>Drops</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>dev</strong></td>
      <td>names, <code class="language-plaintext highlighter-rouge">FIND</code>, <code class="language-plaintext highlighter-rouge">CREATE</code>, REPL, instrumentation</td>
      <td>almost nothing</td>
    </tr>
    <tr>
      <td><strong>runtime</strong></td>
      <td>compiled CFAs, primitives, inner loop</td>
      <td>names, compiler, REPL, dead words</td>
    </tr>
    <tr>
      <td><strong>debug-runtime</strong></td>
      <td>compiled CFAs plus symbols and trace hooks</td>
      <td>full interactive machinery</td>
    </tr>
  </tbody>
</table>

<p>That is vertical specialization. The target machine stays the same; the amount of Forth you carry into the final artifact changes.</p>

<p>Retargetable codegen asks a different question:</p>

<blockquote>
  <p>What if the <em>profile</em> is the same, but the output machine changes?</p>
</blockquote>

<p>COR24 wants one shape of branch. WASM wants another. RV32I has registers and load/store rules that do not resemble a tiny threaded-code VM. S/360 brings condition codes, base registers, and a whole cultural memory of what “assembly” means.</p>

<p>If the compiler is self-hosted, this cannot be solved by hiding everything behind a giant external backend forever. Eventually the Forth system itself needs to describe what it emits.</p>

<h2 id="the-tempting-bad-design">The Tempting Bad Design</h2>

<p>The easiest design to imagine is also the one that rots first:</p>

<pre><code class="language-forth">: EMIT-CALL
  TARGET-COR24 IF ... THEN
  TARGET-WASM  IF ... THEN
  TARGET-RV32I IF ... THEN
  TARGET-S360  IF ... THEN ;
</code></pre>

<p>Do that for <code class="language-plaintext highlighter-rouge">CALL</code>, <code class="language-plaintext highlighter-rouge">BRANCH</code>, literals, stack effects, memory access, returns, labels, and relocations, and the compiler becomes a pile of target checks. Every new target edits shared code. Every feature test is now a backend integration test. The source language has no clean boundary from the machines it targets.</p>

<p>That is not a retargetable compiler. That is a compiler with a target-shaped rash.</p>

<h2 id="a-better-boundary-operations-not-instructions">A Better Boundary: Operations, Not Instructions</h2>

<p>The useful split is between <em>semantic operations</em> and <em>target encodings</em>.</p>

<p>The front half of the Forth compiler should talk in operations:</p>

<table>
  <thead>
    <tr>
      <th>Operation</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-call word</code></td>
      <td>transfer to a known word and return</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-lit value</code></td>
      <td>place a literal on the data stack</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-branch label</code></td>
      <td>unconditional control flow</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-0branch label</code></td>
      <td>branch if top of stack is false</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-load addr</code></td>
      <td>fetch from memory</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-store addr</code></td>
      <td>store to memory</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">op-exit</code></td>
      <td>return from colon definition</td>
    </tr>
  </tbody>
</table>

<p>The backend owns how those operations become bytes, cells, threaded CFAs, WASM instructions, or assembly text.</p>

<p>That boundary is what lets COR24 stay simple while a future WASM target does something entirely different.</p>

<h2 id="the-forward-reference-problem">The Forward-Reference Problem</h2>

<p>Forth normally likes definitions to appear before use. A simple single-pass compiler can get very far with that rule:</p>

<pre><code class="language-forth">: SQUARE DUP * ;
: AREA SQUARE * ;
</code></pre>

<p><code class="language-plaintext highlighter-rouge">AREA</code> can call <code class="language-plaintext highlighter-rouge">SQUARE</code> because <code class="language-plaintext highlighter-rouge">SQUARE</code> already exists.</p>

<p>But real systems eventually want cycles:</p>

<pre><code class="language-forth">: EVEN? DUP 0= IF DROP TRUE EXIT THEN 1- ODD? ;
: ODD?  DUP 0= IF DROP FALSE EXIT THEN 1- EVEN? ;
</code></pre>

<p>At the moment <code class="language-plaintext highlighter-rouge">EVEN?</code> is compiled, <code class="language-plaintext highlighter-rouge">ODD?</code> does not exist yet. A name lookup cannot produce a CFA because there is no CFA to find.</p>

<p>There are three classic ways out:</p>

<table>
  <thead>
    <tr>
      <th>Strategy</th>
      <th>Tradeoff</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Require ordering</td>
      <td>simple compiler, awkward programs</td>
    </tr>
    <tr>
      <td>Add declarations</td>
      <td>more syntax, better compile-time knowledge</td>
    </tr>
    <tr>
      <td>Emit unresolved references</td>
      <td>needs fixups, enables natural structure</td>
    </tr>
  </tbody>
</table>

<p>For a self-hosting Forth that wants retargetable codegen, unresolved references are the interesting path. They turn the compiler into a small linker.</p>

<style>
  .post-content .gutter-section { position: relative; }
  .gutter-img-left,
  .gutter-img-right {
    position: absolute;
    top: 0;
    width: 72%;
    max-width: none;
    height: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
  }
  .gutter-img-left { right: calc(100% + 1.25rem); }
  .gutter-img-right { left: calc(100% + 1.25rem); }
  @media (max-width: 900px) {
    .gutter-img-left,
    .gutter-img-right {
      position: static;
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0.5em auto 1em;
    }
  }
</style>

<h2 id="fixups-are-the-linker-hiding-in-the-compiler">Fixups Are the Linker Hiding in the Compiler</h2>

<div class="gutter-section">

  <p><img src="/assets/images/posts/block-forth-spiral.png" class="gutter-img-left no-invert" alt="" /></p>

  <p>When the compiler sees a reference to a word it cannot resolve yet, it can emit a placeholder and record a fixup:</p>

  <table>
    <thead>
      <tr>
        <th>Field</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>unresolved name</td>
        <td><code class="language-plaintext highlighter-rouge">ODD?</code></td>
      </tr>
      <tr>
        <td>use site</td>
        <td>byte/cell offset inside <code class="language-plaintext highlighter-rouge">EVEN?</code></td>
      </tr>
      <tr>
        <td>relocation kind</td>
        <td>call target, branch target, literal address</td>
      </tr>
      <tr>
        <td>target profile</td>
        <td>dev, runtime, debug-runtime</td>
      </tr>
      <tr>
        <td>backend</td>
        <td>COR24, WASM, RV32I, S/360</td>
      </tr>
    </tbody>
  </table>

  <p>Later, when <code class="language-plaintext highlighter-rouge">ODD?</code> is defined, the linker pass patches every recorded use site.</p>

  <p>This is not only for mutually recursive colon definitions. The same mechanism handles branch labels, separately compiled modules, runtime-image compaction, and target backends whose branch instruction cannot be encoded until the distance is known.</p>

</div>

<h2 id="why-this-belongs-with-the-composer">Why This Belongs With the Composer</h2>

<p>Part 4’s composer decided which words survive into an image.</p>

<p>Part 5’s composer has to decide where surviving words land, what unresolved references point to, and how target-specific relocation works.</p>

<p>That suggests one pipeline:</p>

<ol>
  <li>Parse and compile source into target-neutral operations.</li>
  <li>Record unresolved words and labels as fixups.</li>
  <li>Resolve names after each module or full source load.</li>
  <li>Choose a profile: dev, runtime, debug-runtime.</li>
  <li>Compact the dictionary for that profile.</li>
  <li>Hand surviving operations and fixups to the target backend.</li>
  <li>Emit the final artifact and a manifest of what was resolved.</li>
</ol>

<p>The same manifest that makes dictionary compaction auditable also makes retargeting auditable. If a runtime image drops a word that a backend still needs, the build should fail with a name and a fixup site, not a mystery crash.</p>

<h2 id="target-notes">Target Notes</h2>

<p>This section needs measurements and examples from actual backend sketches.</p>

<h3 id="cor24">COR24</h3>

<p>COR24 is the baseline: small, explicit, close to the current Forth image model.</p>

<p>Notes to fill in:</p>

<ul>
  <li>direct-threaded vs subroutine-threaded options</li>
  <li>branch range and call encoding</li>
  <li>how much relocation state is needed for compacted runtime images</li>
</ul>

<h3 id="wasm">WASM</h3>

<p>WASM is structured, typed, and not just “assembly with different mnemonics.”</p>

<p>Notes to fill in:</p>

<ul>
  <li>structured control flow vs arbitrary branch labels</li>
  <li>stack machine overlap with Forth’s data stack</li>
  <li>imports/exports for host integration</li>
</ul>

<h3 id="rv32i">RV32I</h3>

<p>RV32I is useful because it forces the compiler to be honest about registers and memory.</p>

<p>Notes to fill in:</p>

<ul>
  <li>stack pointer conventions</li>
  <li>immediate range limits</li>
  <li>call/return sequence</li>
</ul>

<h3 id="s360">S/360</h3>

<p>S/360 is useful because it makes relocation and base registers visible.</p>

<p>Notes to fill in:</p>

<ul>
  <li>base register setup</li>
  <li>literal pools</li>
  <li>condition code mapping</li>
</ul>

<h2 id="what-comes-next">What Comes Next</h2>

<p>The next rabbit-hole after this one should probably stop talking about compiler structure and show an actual thin backend: take one tiny Forth source file, emit two target artifacts, and compare the fixup manifests side by side.</p>

<p>The composer idea only matters if it survives contact with a second target.</p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="codegen" /><category term="retargetable-compiler" /><category term="forward-references" /><category term="mutual-recursion" /><category term="linker" /><category term="assembler" /><category term="self-hosting" /><category term="compiler-backend" /><summary type="html"><![CDATA[Dictionary compaction was the vertical problem: one Forth source tree, many images, each with a different amount of development machinery stripped away. Retargetable code generation is the horizontal problem: one source tree, many machines, each with a different instruction set, calling convention, memory model, and set of awkward things the compiler cannot pretend are the same. Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Prior post Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images Overview post Embedded (3/?): Self-Hosting Spectrum Comments Discord The Vertical Split Was Only Half the Problem Part 4 framed the Forth composer as a way to build different profiles from the same source: Profile Keeps Drops dev names, FIND, CREATE, REPL, instrumentation almost nothing runtime compiled CFAs, primitives, inner loop names, compiler, REPL, dead words debug-runtime compiled CFAs plus symbols and trace hooks full interactive machinery That is vertical specialization. The target machine stays the same; the amount of Forth you carry into the final artifact changes. Retargetable codegen asks a different question: What if the profile is the same, but the output machine changes? COR24 wants one shape of branch. WASM wants another. RV32I has registers and load/store rules that do not resemble a tiny threaded-code VM. S/360 brings condition codes, base registers, and a whole cultural memory of what “assembly” means. If the compiler is self-hosted, this cannot be solved by hiding everything behind a giant external backend forever. Eventually the Forth system itself needs to describe what it emits. The Tempting Bad Design The easiest design to imagine is also the one that rots first: : EMIT-CALL TARGET-COR24 IF ... THEN TARGET-WASM IF ... THEN TARGET-RV32I IF ... THEN TARGET-S360 IF ... THEN ; Do that for CALL, BRANCH, literals, stack effects, memory access, returns, labels, and relocations, and the compiler becomes a pile of target checks. Every new target edits shared code. Every feature test is now a backend integration test. The source language has no clean boundary from the machines it targets. That is not a retargetable compiler. That is a compiler with a target-shaped rash. A Better Boundary: Operations, Not Instructions The useful split is between semantic operations and target encodings. The front half of the Forth compiler should talk in operations: Operation Meaning op-call word transfer to a known word and return op-lit value place a literal on the data stack op-branch label unconditional control flow op-0branch label branch if top of stack is false op-load addr fetch from memory op-store addr store to memory op-exit return from colon definition The backend owns how those operations become bytes, cells, threaded CFAs, WASM instructions, or assembly text. That boundary is what lets COR24 stay simple while a future WASM target does something entirely different. The Forward-Reference Problem Forth normally likes definitions to appear before use. A simple single-pass compiler can get very far with that rule: : SQUARE DUP * ; : AREA SQUARE * ; AREA can call SQUARE because SQUARE already exists. But real systems eventually want cycles: : EVEN? DUP 0= IF DROP TRUE EXIT THEN 1- ODD? ; : ODD? DUP 0= IF DROP FALSE EXIT THEN 1- EVEN? ; At the moment EVEN? is compiled, ODD? does not exist yet. A name lookup cannot produce a CFA because there is no CFA to find. There are three classic ways out: Strategy Tradeoff Require ordering simple compiler, awkward programs Add declarations more syntax, better compile-time knowledge Emit unresolved references needs fixups, enables natural structure For a self-hosting Forth that wants retargetable codegen, unresolved references are the interesting path. They turn the compiler into a small linker. Fixups Are the Linker Hiding in the Compiler When the compiler sees a reference to a word it cannot resolve yet, it can emit a placeholder and record a fixup: Field Example unresolved name ODD? use site byte/cell offset inside EVEN? relocation kind call target, branch target, literal address target profile dev, runtime, debug-runtime backend COR24, WASM, RV32I, S/360 Later, when ODD? is defined, the linker pass patches every recorded use site. This is not only for mutually recursive colon definitions. The same mechanism handles branch labels, separately compiled modules, runtime-image compaction, and target backends whose branch instruction cannot be encoded until the distance is known. Why This Belongs With the Composer Part 4’s composer decided which words survive into an image. Part 5’s composer has to decide where surviving words land, what unresolved references point to, and how target-specific relocation works. That suggests one pipeline: Parse and compile source into target-neutral operations. Record unresolved words and labels as fixups. Resolve names after each module or full source load. Choose a profile: dev, runtime, debug-runtime. Compact the dictionary for that profile. Hand surviving operations and fixups to the target backend. Emit the final artifact and a manifest of what was resolved. The same manifest that makes dictionary compaction auditable also makes retargeting auditable. If a runtime image drops a word that a backend still needs, the build should fail with a name and a fixup site, not a mystery crash. Target Notes This section needs measurements and examples from actual backend sketches. COR24 COR24 is the baseline: small, explicit, close to the current Forth image model. Notes to fill in: direct-threaded vs subroutine-threaded options branch range and call encoding how much relocation state is needed for compacted runtime images WASM WASM is structured, typed, and not just “assembly with different mnemonics.” Notes to fill in: structured control flow vs arbitrary branch labels stack machine overlap with Forth’s data stack imports/exports for host integration RV32I RV32I is useful because it forces the compiler to be honest about registers and memory. Notes to fill in: stack pointer conventions immediate range limits call/return sequence S/360 S/360 is useful because it makes relocation and base registers visible. Notes to fill in: base register setup literal pools condition code mapping What Comes Next The next rabbit-hole after this one should probably stop talking about compiler structure and show an actual thin backend: take one tiny Forth source file, emit two target artifacts, and compare the fixup manifests side by side. The composer idea only matters if it survives contact with a second target.]]></summary></entry><entry><title type="html">Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images</title><link href="https://software-wrighter-lab.github.io/2026/04/25/rabbit-hole-forth-dictionary-compaction-specialized-images/" rel="alternate" type="text/html" title="Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images" /><published>2026-04-25T00:15:00-07:00</published><updated>2026-04-25T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/25/rabbit-hole-forth-dictionary-compaction-specialized-images</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/25/rabbit-hole-forth-dictionary-compaction-specialized-images/"><![CDATA[<p><img src="/assets/images/posts/block-rabbit-tree-sword-right.png" class="post-marker no-invert" alt="" style="width: 220px;" /></p>

<div style="overflow: hidden;">

  <p>Third FORTH post in the rabbit hole. <a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Part 2</a> dropped the XMX hash. <a href="/2026/04/24/rabbit-hole-forth-life-after-hashing/">Part 3</a> replaced it with a numeric fast path, a recent-hit cache, and a hot-token cache — tuning FIND in the places where FIND still runs. This post asks a different question: <em>can we make FIND not run at all?</em></p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Prior post</strong></td>
        <td><a href="/2026/04/24/rabbit-hole-forth-life-after-hashing/">Rabbit-hole (3/?): FORTH — Life After Hashing</a></td>
      </tr>
      <tr>
        <td><strong>Overview post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="two-different-forths-same-source">Two Different Forths, Same Source</h2>

<p>A working Forth environment does a lot of things at once:</p>

<ul>
  <li>Reads source text, tokenizes, looks up names → interpret/compile</li>
  <li>Holds a live dictionary, walked by <code class="language-plaintext highlighter-rouge">FIND</code>, grown by <code class="language-plaintext highlighter-rouge">CREATE</code></li>
  <li>Exposes a REPL for interactive use</li>
  <li>Runs user-level compiled code (the actual <em>work</em>)</li>
  <li>Sometimes: profiles, instruments, debugs</li>
</ul>

<p>In a development Forth, all of this matters. You’re compiling new words, reloading <code class="language-plaintext highlighter-rouge">.fth</code> files, poking at things via <code class="language-plaintext highlighter-rouge">.S</code> and <code class="language-plaintext highlighter-rouge">WORDS</code>. The dictionary <em>must</em> be searchable by name, CREATE <em>must</em> work, the REPL <em>must</em> respond.</p>

<p>In a deployed Forth — the kind you’d flash onto an FPGA or ship in a firmware image — most of that is overhead. The deployed program already exists as compiled bytecode. It doesn’t need FIND. It doesn’t need CREATE. It doesn’t need the REPL. It doesn’t even need the names of the words it calls, because the calls are already resolved.</p>

<p><strong>Dictionary compaction</strong> is the name for the gap between those two Forths. Specialized-image building is how you cross it.</p>

<h2 id="three-optimization-layers">Three Optimization Layers</h2>

<p>Posts 2 and 3 sat entirely in layer 1. This post introduces layers 2 and 3.</p>

<table>
  <thead>
    <tr>
      <th>Layer</th>
      <th>Target</th>
      <th>Examples</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>1. Dev-time lookup</strong></td>
      <td>Make FIND fast in the dev image</td>
      <td>Numeric fast path · recent-hit cache · hot-token cache (Part 3)</td>
    </tr>
    <tr>
      <td><strong>2. Dictionary compaction</strong></td>
      <td>Shrink what gets shipped</td>
      <td>Drop shadowed words · strip instrumentation · remove dead code</td>
    </tr>
    <tr>
      <td><strong>3. Whole-app specialization</strong></td>
      <td>Eliminate the interpreter for production</td>
      <td>Drop FIND · drop CREATE · drop REPL · ship only compiled CFAs</td>
    </tr>
  </tbody>
</table>

<p>Each layer is <strong>opt-in</strong>. Each is a different build artifact produced from the same source tree. A composer sits at the top and chooses which layers to apply for a given target profile.</p>

<h2 id="a-forth-composer">A Forth Composer</h2>

<p>The mental shift, once you’ve done this a few times:</p>

<blockquote>
  <p><em>You’re not building “a Forth.” You’re building a Forth system generator that can assemble different Forth flavors for different use cases.</em></p>
</blockquote>

<p>Named profiles for common builds:</p>

<table>
  <thead>
    <tr>
      <th>Profile</th>
      <th>Lookup subsystem</th>
      <th>Instrumentation</th>
      <th>Image</th>
      <th>Good for</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">simple</code></strong></td>
      <td>Linear walk only</td>
      <td>Off</td>
      <td>Full dev dict</td>
      <td>Teaching, debugging</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">instrumented</code></strong></td>
      <td>Linear + recent cache + counters</td>
      <td>On</td>
      <td>Full dev dict</td>
      <td>Profiling real workloads</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">optimizing</code></strong></td>
      <td>Linear + both caches</td>
      <td>On, driving reorder</td>
      <td>Compacted dev dict</td>
      <td>Interactive, fast boot</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">runtime</code></strong></td>
      <td>None (no FIND)</td>
      <td>Off</td>
      <td>Compiled CFAs only</td>
      <td>Deployed app</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">debug-runtime</code></strong></td>
      <td>None</td>
      <td>Minimal (<code class="language-plaintext highlighter-rouge">assert</code>, <code class="language-plaintext highlighter-rouge">trace</code>)</td>
      <td>Compiled CFAs + symbols</td>
      <td>Crash-reproducible deploy</td>
    </tr>
  </tbody>
</table>

<p>The core is the same in every profile: stacks, threading, primitives, inner loop. Everything above that is a composition of optional modules and policies. The composer’s job is to know which modules are required, which conflict, and how to wire them together for the requested profile.</p>

<p>This is cleaner than “one Forth tries to be everything.” It matches the spirit of the <a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">self-hosting spectrum</a> post’s phased migration: different axes are orthogonal, so treat them that way.</p>

<h2 id="what-compaction-actually-removes">What Compaction Actually Removes</h2>

<p>For profile <strong><code class="language-plaintext highlighter-rouge">runtime</code></strong> (the most aggressive), the compactor walks the live dictionary and drops:</p>

<p><strong>Shadowed redefinitions.</strong> Forth lets you redefine a word. The old definition stays in the dictionary, unreachable by name (FIND walks newest-to-oldest, hits the new one first) but still taking space. In a runtime image, shadowed entries are pure dead weight. Drop them.</p>

<p><strong>Bootstrap-only words.</strong> Words that exist <em>only</em> to load the source. Things like <code class="language-plaintext highlighter-rouge">,DOCOL</code>, <code class="language-plaintext highlighter-rouge">CONSTANT</code>, <code class="language-plaintext highlighter-rouge">VARIABLE</code>, <code class="language-plaintext highlighter-rouge">CREATE</code>, <code class="language-plaintext highlighter-rouge">:</code>, <code class="language-plaintext highlighter-rouge">;</code>, <code class="language-plaintext highlighter-rouge">IMMEDIATE</code>. If the runtime image doesn’t compile new words at runtime, none of these are needed — they were scaffolding used during loading.</p>

<p><strong>Interpreter loop.</strong> <code class="language-plaintext highlighter-rouge">INTERPRET</code>, <code class="language-plaintext highlighter-rouge">QUIT</code>, <code class="language-plaintext highlighter-rouge">WORD</code>, <code class="language-plaintext highlighter-rouge">FIND</code>, <code class="language-plaintext highlighter-rouge">NUMBER</code>. A runtime image that runs one entry point (<code class="language-plaintext highlighter-rouge">MAIN</code> or whatever) doesn’t read text, doesn’t tokenize, doesn’t look things up. The whole outer loop is gone.</p>

<p><strong>Instrumentation.</strong> Any counters, profiling hooks, trace points. These were for building the optimized image; they don’t go into the deliverable.</p>

<p><strong>Developer-only helpers.</strong> <code class="language-plaintext highlighter-rouge">.S</code>, <code class="language-plaintext highlighter-rouge">DUMP</code>, <code class="language-plaintext highlighter-rouge">WORDS</code>, <code class="language-plaintext highlighter-rouge">SEE</code>, <code class="language-plaintext highlighter-rouge">DEPTH</code>, diagnostic decorations. Useful at the REPL; useless in deployment.</p>

<p><strong>Name strings.</strong> The aggressive move: drop the name field on every dictionary entry. Runtime callers reach words by CFA, not by name, so the strings are only readable by a human via <code class="language-plaintext highlighter-rouge">SEE</code> and <code class="language-plaintext highlighter-rouge">WORDS</code> — both dropped in the previous step. 40-char headers become 4-char (flags byte + link + CFA).</p>

<p>What’s <em>left</em> after all that: the data stack, the return stack, a handful of primitives still needed by compiled code (<code class="language-plaintext highlighter-rouge">+</code>, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">DUP</code>, etc.), the inner loop (<code class="language-plaintext highlighter-rouge">NEXT</code>), and the compiled CFAs of words the application actually calls. On COR24, that’s the difference between a ~3KB dev image and a ~600-byte runtime image.</p>

<style>
  /* Stagger sections occupy cols 1-3 / 2-4 / 3-5 of a 5-col grid (60% width).
     Gutter images fill the remaining 2 cols (40%) next to the stagger paragraph. */
  .post-content .stagger { position: relative; }
  .gutter-img-left,
  .gutter-img-right {
    position: absolute;
    top: 0;
    /* 66.67% of a 60%-wide section = 40% of the container = 2 of 5 cols */
    width: 66.67%;
    max-width: none;
    /* Cap height to section so image can't overflow into the next section;
       contain preserves aspect ratio (no crop). */
    height: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
  }
  .gutter-img-left  { right: calc(100% + 1.25rem); }
  .gutter-img-right { left:  calc(100% + 1.25rem); }
  @media (max-width: 900px) {
    .gutter-img-left,
    .gutter-img-right {
      position: static;
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0.5em auto 1em;
    }
  }
</style>

<h2 id="the-real-implementation-burden-pointers">The Real Implementation Burden: Pointers</h2>

<p><img src="/assets/images/posts/block-forth-in-stone-regress.png" class="gutter-img-left no-invert" alt="" /></p>

<p>Removing words is the easy part. Rewriting all references <em>to</em> those words is where dictionary compaction earns its reputation.</p>

<p>Every compiled colon definition is a sequence of CFA pointers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SQUARE: DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT
</code></pre></div></div>

<p>If the compactor removes a shadowed <code class="language-plaintext highlighter-rouge">*</code> and the remaining <code class="language-plaintext highlighter-rouge">*</code> lives at a different address, every single compiled definition that points at the old <code class="language-plaintext highlighter-rouge">*</code> now points at a stale address. You have to find them all and rewrite them.</p>

<p>That requires:</p>

<ol>
  <li><strong>An authoritative record of which word each CFA originally named.</strong> Names are dropped in the final image but must exist during compaction.</li>
  <li><strong>A walk of every compiled body</strong>, looking at each cell, asking “is this a CFA pointer?” and if so “which word does it refer to now?” and rewriting accordingly.</li>
  <li><strong>Fixups for literal addresses</strong> (from <code class="language-plaintext highlighter-rouge">LIT</code>), branch targets (from <code class="language-plaintext highlighter-rouge">BRANCH</code> / <code class="language-plaintext highlighter-rouge">0BRANCH</code>), and any word-to-word references stored in data cells.</li>
  <li><strong>A reproducible layout algorithm</strong> so two runs over the same source produce the same image. This matters for CI.</li>
</ol>

<p>Under the hood, this is the same relocation problem a linker solves. The Forth composer is a linker for Forth objects. It just happens to be written in Forth.</p>

<p>Two techniques help:</p>

<ul>
  <li><strong>Build a <em>new</em> image rather than editing the old one.</strong> Start from a clean allocation, stream surviving words through one-by-one, fix up pointers as you go. This is much easier than trying to edit-in-place and much easier to test.</li>
  <li><strong>Do not reorder words until all compaction is done.</strong> The pointer-rewrite pass is simpler when you know the old → new CFA mapping is stable. Reordering for cache locality, if you do it, is a separate pass over the already-compacted image.</li>
</ul>

<h2 id="profile-driven-optimization">Profile-Driven Optimization</h2>

<p>Compaction without data is guesswork. What goes, what stays, what gets reordered — the composer needs a <em>profile</em> that captures real usage.</p>

<p>The right counters to capture during a profiling run:</p>

<table>
  <thead>
    <tr>
      <th>Counter</th>
      <th>What it measures</th>
      <th>Drives</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">source-interpret-count</code></td>
      <td>Times word looked up at REPL</td>
      <td>Recent cache tuning</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">source-compile-count</code></td>
      <td>Times word looked up during source loading</td>
      <td>Hot cache seeding</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">runtime-execute-count</code></td>
      <td>Times compiled CFA executed</td>
      <td>Dictionary reordering · inlining</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">last-used-pass</code></td>
      <td>Which build step last referenced word</td>
      <td>Shadowed / dead-code detection</td>
    </tr>
  </tbody>
</table>

<p>The PROFILE-ON / PROFILE-OFF mental model from the composer:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PROFILE-ON
( load source, run demo workload, exercise edge cases )
PROFILE-OFF
DUMP-COUNTS   \ writes the profile to an external file
BUILD-OPT-IMAGE use-profile=demo.prof target=runtime
</code></pre></div></div>

<p>Counts are written external to the dictionary so the composer can re-use them across builds and compare policies. The same profile data can drive multiple output images (a runtime target + an instrumented dev target) without recollecting measurements.</p>

<h2 id="the-overfitting-trap">The Overfitting Trap</h2>

<p>Optimizing against a demo corpus is seductive, and it’s wrong in the same ways micro-benchmark tuning is wrong.</p>

<p>If the profile comes from 25 example <code class="language-plaintext highlighter-rouge">.fth</code> files and those are the <em>only</em> workloads the deployed image will ever see: perfect, overfit freely. The deployed image is a fixed-function artifact; fitting it to its workload is the whole job.</p>

<p>If the deployed image will see workload <code class="language-plaintext highlighter-rouge">#26</code> that looks different: compaction that was fine against the 25 can silently drop words that <code class="language-plaintext highlighter-rouge">#26</code> needs, or reorder the dictionary in ways that make <code class="language-plaintext highlighter-rouge">#26</code> slow. The composer can’t catch this unless <code class="language-plaintext highlighter-rouge">#26</code> is in the profile.</p>

<p>Mitigations:</p>

<ul>
  <li><strong>Keep a canary <code class="language-plaintext highlighter-rouge">.fth</code></strong> full of standard Forth idioms that the compacted image must still run. Regression-check it on every build.</li>
  <li><strong>Have at least one “safe” profile</strong> that retains more than you strictly need, for deployments whose workload is uncertain.</li>
  <li><strong>Be explicit about what got cut.</strong> The compactor should emit a manifest of which words it dropped, which it kept, and why. Surprises in deployment trace back to surprises in that manifest.</li>
</ul>

<p>The classic advice: <em>optimize the workload you have, guard against the workload you don’t.</em> The composer makes both easier — different profiles for different risk tolerances — but it doesn’t automate the judgment call.</p>

<h2 id="where-this-lands">Where This Lands</h2>

<p>Three layers of optimization, one composer selecting profiles. The runtime profile ships a Forth that doesn’t look much like Forth: no REPL, no interpreter, no dictionary-by-name. Just compiled CFAs and a tiny inner loop. Some people call this “a runtime,” not a Forth. Either framing is fine. The point is that the <strong>same source</strong> produces both the interactive dev Forth and the minimal runtime deliverable — by composition, not by a separate rewrite.</p>

<p>The phase-4 <code class="language-plaintext highlighter-rouge">forth-from-forth/</code> work naturally wants this framing. A Forth-hosted cross-compiler already has to reason about “which words end up in the output image.” Making that reasoning explicit — <em>the composer decides</em> — turns an implicit dependency tree into an explicit build pipeline.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Compaction collapses <em>vertically</em> — strip unused words, drop unused features. The next rabbit-hole post asks about <em>horizontal</em> compaction: when the same Forth source has to target different ISAs (COR24, WASM, RV32I, S/360), how do you partition the compiler so only the backend changes? And what happens when the compiler has to reference words that don’t yet exist — the forward-reference / mutual-recursion problem that every self-hosted compiler eventually faces.</p>

<p>That’s <em>Rabbit-hole (5/?): FORTH — Retargetable Codegen and the Forward-Reference Problem</em>.</p>

<hr />

<p><em>One source, many artifacts. The Forth composer is just the recognition that different deployments want different Forths — and the discipline to stop pretending otherwise.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="dictionary" /><category term="compaction" /><category term="image-building" /><category term="runtime-image" /><category term="dev-image" /><category term="composer" /><category term="self-hosting" /><category term="profile-driven-optimization" /><summary type="html"><![CDATA[Third FORTH post in the rabbit hole. Part 2 dropped the XMX hash. Part 3 replaced it with a numeric fast path, a recent-hit cache, and a hot-token cache — tuning FIND in the places where FIND still runs. This post asks a different question: can we make FIND not run at all? Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Prior post Rabbit-hole (3/?): FORTH — Life After Hashing Overview post Embedded (3/?): Self-Hosting Spectrum Comments Discord Two Different Forths, Same Source A working Forth environment does a lot of things at once: Reads source text, tokenizes, looks up names → interpret/compile Holds a live dictionary, walked by FIND, grown by CREATE Exposes a REPL for interactive use Runs user-level compiled code (the actual work) Sometimes: profiles, instruments, debugs In a development Forth, all of this matters. You’re compiling new words, reloading .fth files, poking at things via .S and WORDS. The dictionary must be searchable by name, CREATE must work, the REPL must respond. In a deployed Forth — the kind you’d flash onto an FPGA or ship in a firmware image — most of that is overhead. The deployed program already exists as compiled bytecode. It doesn’t need FIND. It doesn’t need CREATE. It doesn’t need the REPL. It doesn’t even need the names of the words it calls, because the calls are already resolved. Dictionary compaction is the name for the gap between those two Forths. Specialized-image building is how you cross it. Three Optimization Layers Posts 2 and 3 sat entirely in layer 1. This post introduces layers 2 and 3. Layer Target Examples 1. Dev-time lookup Make FIND fast in the dev image Numeric fast path · recent-hit cache · hot-token cache (Part 3) 2. Dictionary compaction Shrink what gets shipped Drop shadowed words · strip instrumentation · remove dead code 3. Whole-app specialization Eliminate the interpreter for production Drop FIND · drop CREATE · drop REPL · ship only compiled CFAs Each layer is opt-in. Each is a different build artifact produced from the same source tree. A composer sits at the top and chooses which layers to apply for a given target profile. A Forth Composer The mental shift, once you’ve done this a few times: You’re not building “a Forth.” You’re building a Forth system generator that can assemble different Forth flavors for different use cases. Named profiles for common builds: Profile Lookup subsystem Instrumentation Image Good for simple Linear walk only Off Full dev dict Teaching, debugging instrumented Linear + recent cache + counters On Full dev dict Profiling real workloads optimizing Linear + both caches On, driving reorder Compacted dev dict Interactive, fast boot runtime None (no FIND) Off Compiled CFAs only Deployed app debug-runtime None Minimal (assert, trace) Compiled CFAs + symbols Crash-reproducible deploy The core is the same in every profile: stacks, threading, primitives, inner loop. Everything above that is a composition of optional modules and policies. The composer’s job is to know which modules are required, which conflict, and how to wire them together for the requested profile. This is cleaner than “one Forth tries to be everything.” It matches the spirit of the self-hosting spectrum post’s phased migration: different axes are orthogonal, so treat them that way. What Compaction Actually Removes For profile runtime (the most aggressive), the compactor walks the live dictionary and drops: Shadowed redefinitions. Forth lets you redefine a word. The old definition stays in the dictionary, unreachable by name (FIND walks newest-to-oldest, hits the new one first) but still taking space. In a runtime image, shadowed entries are pure dead weight. Drop them. Bootstrap-only words. Words that exist only to load the source. Things like ,DOCOL, CONSTANT, VARIABLE, CREATE, :, ;, IMMEDIATE. If the runtime image doesn’t compile new words at runtime, none of these are needed — they were scaffolding used during loading. Interpreter loop. INTERPRET, QUIT, WORD, FIND, NUMBER. A runtime image that runs one entry point (MAIN or whatever) doesn’t read text, doesn’t tokenize, doesn’t look things up. The whole outer loop is gone. Instrumentation. Any counters, profiling hooks, trace points. These were for building the optimized image; they don’t go into the deliverable. Developer-only helpers. .S, DUMP, WORDS, SEE, DEPTH, diagnostic decorations. Useful at the REPL; useless in deployment. Name strings. The aggressive move: drop the name field on every dictionary entry. Runtime callers reach words by CFA, not by name, so the strings are only readable by a human via SEE and WORDS — both dropped in the previous step. 40-char headers become 4-char (flags byte + link + CFA). What’s left after all that: the data stack, the return stack, a handful of primitives still needed by compiled code (+, @, !, DUP, etc.), the inner loop (NEXT), and the compiled CFAs of words the application actually calls. On COR24, that’s the difference between a ~3KB dev image and a ~600-byte runtime image. The Real Implementation Burden: Pointers Removing words is the easy part. Rewriting all references to those words is where dictionary compaction earns its reputation. Every compiled colon definition is a sequence of CFA pointers: SQUARE: DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT If the compactor removes a shadowed * and the remaining * lives at a different address, every single compiled definition that points at the old * now points at a stale address. You have to find them all and rewrite them. That requires: An authoritative record of which word each CFA originally named. Names are dropped in the final image but must exist during compaction. A walk of every compiled body, looking at each cell, asking “is this a CFA pointer?” and if so “which word does it refer to now?” and rewriting accordingly. Fixups for literal addresses (from LIT), branch targets (from BRANCH / 0BRANCH), and any word-to-word references stored in data cells. A reproducible layout algorithm so two runs over the same source produce the same image. This matters for CI. Under the hood, this is the same relocation problem a linker solves. The Forth composer is a linker for Forth objects. It just happens to be written in Forth. Two techniques help: Build a new image rather than editing the old one. Start from a clean allocation, stream surviving words through one-by-one, fix up pointers as you go. This is much easier than trying to edit-in-place and much easier to test. Do not reorder words until all compaction is done. The pointer-rewrite pass is simpler when you know the old → new CFA mapping is stable. Reordering for cache locality, if you do it, is a separate pass over the already-compacted image. Profile-Driven Optimization Compaction without data is guesswork. What goes, what stays, what gets reordered — the composer needs a profile that captures real usage. The right counters to capture during a profiling run: Counter What it measures Drives source-interpret-count Times word looked up at REPL Recent cache tuning source-compile-count Times word looked up during source loading Hot cache seeding runtime-execute-count Times compiled CFA executed Dictionary reordering · inlining last-used-pass Which build step last referenced word Shadowed / dead-code detection The PROFILE-ON / PROFILE-OFF mental model from the composer: PROFILE-ON ( load source, run demo workload, exercise edge cases ) PROFILE-OFF DUMP-COUNTS \ writes the profile to an external file BUILD-OPT-IMAGE use-profile=demo.prof target=runtime Counts are written external to the dictionary so the composer can re-use them across builds and compare policies. The same profile data can drive multiple output images (a runtime target + an instrumented dev target) without recollecting measurements. The Overfitting Trap Optimizing against a demo corpus is seductive, and it’s wrong in the same ways micro-benchmark tuning is wrong. If the profile comes from 25 example .fth files and those are the only workloads the deployed image will ever see: perfect, overfit freely. The deployed image is a fixed-function artifact; fitting it to its workload is the whole job. If the deployed image will see workload #26 that looks different: compaction that was fine against the 25 can silently drop words that #26 needs, or reorder the dictionary in ways that make #26 slow. The composer can’t catch this unless #26 is in the profile. Mitigations: Keep a canary .fth full of standard Forth idioms that the compacted image must still run. Regression-check it on every build. Have at least one “safe” profile that retains more than you strictly need, for deployments whose workload is uncertain. Be explicit about what got cut. The compactor should emit a manifest of which words it dropped, which it kept, and why. Surprises in deployment trace back to surprises in that manifest. The classic advice: optimize the workload you have, guard against the workload you don’t. The composer makes both easier — different profiles for different risk tolerances — but it doesn’t automate the judgment call. Where This Lands Three layers of optimization, one composer selecting profiles. The runtime profile ships a Forth that doesn’t look much like Forth: no REPL, no interpreter, no dictionary-by-name. Just compiled CFAs and a tiny inner loop. Some people call this “a runtime,” not a Forth. Either framing is fine. The point is that the same source produces both the interactive dev Forth and the minimal runtime deliverable — by composition, not by a separate rewrite. The phase-4 forth-from-forth/ work naturally wants this framing. A Forth-hosted cross-compiler already has to reason about “which words end up in the output image.” Making that reasoning explicit — the composer decides — turns an implicit dependency tree into an explicit build pipeline. What’s Next Compaction collapses vertically — strip unused words, drop unused features. The next rabbit-hole post asks about horizontal compaction: when the same Forth source has to target different ISAs (COR24, WASM, RV32I, S/360), how do you partition the compiler so only the backend changes? And what happens when the compiler has to reference words that don’t yet exist — the forward-reference / mutual-recursion problem that every self-hosted compiler eventually faces. That’s Rabbit-hole (5/?): FORTH — Retargetable Codegen and the Forward-Reference Problem. One source, many artifacts. The Forth composer is just the recognition that different deployments want different Forths — and the discipline to stop pretending otherwise.]]></summary></entry><entry><title type="html">Large-Language-Monkeys: Scaling Inference, Not Models</title><link href="https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference/" rel="alternate" type="text/html" title="Large-Language-Monkeys: Scaling Inference, Not Models" /><published>2026-04-24T00:15:00-07:00</published><updated>2026-04-24T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference/"><![CDATA[<p><img src="/assets/images/posts/block-monkeys-typing.png" class="post-marker no-invert" alt="" style="width: 340px;" /></p>

<div style="overflow: hidden;">

  <p>“Large Language Monkeys” is Brown et al.’s (2024) nod to the infinite-monkey theorem, reframed for inference-time compute: if you let a model take many attempts at a problem — and you have an automatic verifier to pick the right one — the odds of at least one attempt being correct climb faster than almost any intuition predicts. Coverage (the fraction of problems solved by <em>any</em> sample) scales log-linearly with the number of samples over <strong>four orders of magnitude</strong>. Small models stop being second-class. Inference becomes a first-class scaling axis.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Paper</strong></td>
        <td><a href="https://arxiv.org/abs/2407.21787">arXiv 2407.21787</a> (Brown et al., 2024)</td>
      </tr>
      <tr>
        <td><strong>Code (forked)</strong></td>
        <td><a href="https://github.com/sw-ml-study/Repeated-Sampling">sw-ml-study/Repeated-Sampling</a></td>
      </tr>
      <tr>
        <td><strong>Code (original)</strong></td>
        <td><a href="https://github.com/weagan/Repeated-Sampling">weagan/Repeated-Sampling</a></td>
      </tr>
      <tr>
        <td><strong>Demo notebook</strong></td>
        <td><a href="https://github.com/sw-ml-study/Repeated-Sampling/blob/main/llm_monkeys_function_demo.ipynb">llm_monkeys_function_demo.ipynb</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-reframing">The Reframing</h2>

<p>The last decade of scaling has been about <em>making the model bigger</em> — more parameters, more training data, more pretraining compute. Inference, by contrast, has stayed boringly fixed: prompt in, answer out, one attempt, take it or leave it.</p>

<p>This paper asks: what if the <em>attempts</em> are a scaling axis?</p>

<table>
  <thead>
    <tr>
      <th>Traditional scaling</th>
      <th>Inference-time scaling</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Bigger model</td>
      <td>More attempts per problem</td>
    </tr>
    <tr>
      <td>More training data</td>
      <td>Repeated sampling</td>
    </tr>
    <tr>
      <td>Single-shot answer</td>
      <td>Multi-sample + verification</td>
    </tr>
  </tbody>
</table>

<p>The mental model shifts from <em>“the model knows the answer”</em> to <em>“the model generates candidate answers; we search for a correct one.”</em> Under that lens, an LLM isn’t an oracle — it’s a stochastic proposer wired to a verifier.</p>

<h2 id="the-core-loop">The Core Loop</h2>

<p>Strip it to essentials:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for i in 1..N:
    sample_i = model(prompt, temperature=T)
    score_i  = verifier(sample_i)
return best(sample_i)
</code></pre></div></div>

<p>Two components:</p>

<ol>
  <li><strong>Generator</strong> — an LLM producing candidate solutions. Temperature drives diversity.</li>
  <li><strong>Verifier / selector</strong> — unit tests (code), symbolic checker (math), heuristic scorer. Picks the right answer from the pile.</li>
</ol>

<p>The verifier is the load-bearing piece. When it’s cheap and deterministic (code → run tests; math proof → check with Lean), repeated sampling is devastatingly effective. When it isn’t (essays, subjective tasks, open-ended reasoning), the scaling story gets thornier.</p>

<h2 id="what-the-numbers-look-like">What the Numbers Look Like</h2>

<p>The paper’s headline result on SWE-bench Lite:</p>

<table>
  <thead>
    <tr>
      <th>Setup</th>
      <th style="text-align: right">Solve rate</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct, 1 sample</td>
      <td style="text-align: right">15.9%</td>
    </tr>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct, 250 samples</td>
      <td style="text-align: right"><strong>56.0%</strong></td>
    </tr>
    <tr>
      <td>Single-sample SOTA at time of writing</td>
      <td style="text-align: right">43.0%</td>
    </tr>
  </tbody>
</table>

<p>Repeated sampling from a <em>non-frontier</em> model blew past the frontier single-sample SOTA on one of the hardest real-world coding benchmarks. Not by a little — by 13 percentage points.</p>

<p>The cost story hits even harder:</p>

<table>
  <thead>
    <tr>
      <th>Configuration</th>
      <th style="text-align: right">Solve rate</th>
      <th style="text-align: right">Cost</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct × 5 samples</td>
      <td style="text-align: right">29.62%</td>
      <td style="text-align: right"><strong>$10.80</strong></td>
    </tr>
    <tr>
      <td>GPT-4o × 1 sample</td>
      <td style="text-align: right">24.00%</td>
      <td style="text-align: right">$39</td>
    </tr>
    <tr>
      <td>Claude 3.5 Sonnet × 1 sample</td>
      <td style="text-align: right">26.70%</td>
      <td style="text-align: right">$51</td>
    </tr>
  </tbody>
</table>

<p>Five DeepSeek attempts beat one attempt from each frontier model at roughly <strong>4× lower cost</strong>. Repeated sampling changes the economics of deploying AI on verifiable tasks.</p>

<h2 id="the-coverage-scaling-law">The Coverage Scaling Law</h2>

<p>The paper models coverage (<code class="language-plaintext highlighter-rouge">c</code>, probability at least one of <code class="language-plaintext highlighter-rouge">k</code> samples is correct) as an <strong>exponentiated power law</strong>:</p>

<div style="text-align: center; font-family: Georgia, 'Times New Roman', serif; font-size: 1.3em; font-style: italic; margin: 1.25em 0;">
  c(k) &nbsp;≈&nbsp; exp(a &middot; k<sup>b</sup>)
</div>

<p>where <code class="language-plaintext highlighter-rouge">k</code> is the number of samples and <code class="language-plaintext highlighter-rouge">a</code>, <code class="language-plaintext highlighter-rouge">b</code> are fitted parameters. Across GSM8K, MATH, MiniF2F, SWE-bench Lite, and CodeContests, coverage scales log-linearly with the sample count over four orders of magnitude. On CodeContests with Gemma-2B, coverage climbs from <strong>0.02% at 1 sample to 7.1% at 10k samples</strong> — that’s more than a 350× improvement, purely from adding attempts.</p>

<p>The shape matters. A log-linear curve means every <em>multiplicative</em> increase in samples yields a fixed <em>additive</em> gain in coverage. Doubling samples → constant boost in solve rate. This is a scaling <em>law</em>, not a diminishing-returns saturation. It’s why the paper’s title matters: enough monkeys, enough time, and Shakespeare emerges from chance. The quantitative claim is that the “enough” is measurable and predictable.</p>

<style>
  /* Stagger sections occupy cols 1-3 / 2-4 / 3-5 of a 5-col grid (60% width).
     Gutter images fill the remaining 2 cols (40%) next to the stagger paragraph. */
  .post-content .stagger { position: relative; }
  .gutter-img-left,
  .gutter-img-right {
    position: absolute;
    top: 0;
    /* 66.67% of a 60%-wide section = 40% of the container = 2 of 5 cols */
    width: 66.67%;
    max-width: none;
    /* Match paragraph height exactly; scale-to-fit without cropping */
    height: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
  }
  .gutter-img-left  { right: calc(100% + 1.25rem); }
  .gutter-img-right { left:  calc(100% + 1.25rem); }
  @media (max-width: 900px) {
    .gutter-img-left,
    .gutter-img-right {
      position: static;
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0.5em auto 1em;
    }
  }
</style>

<h2 id="weak-models-smart-sampling">Weak Models, Smart Sampling</h2>

<p><img src="/assets/images/posts/block-flying-monkeys-brain-statue.png" class="gutter-img-left no-invert" alt="" /></p>

<p>The most important empirical finding for practitioners: <em>smaller models outperform larger ones at matched compute</em>, on the right tasks.</p>

<p>On FLOP-matched comparisons across MATH, GSM8K, and MiniF2F, Llama-3-8B-Instruct with more samples achieves higher coverage than Llama-3-70B-Instruct — at the <em>same</em> total inference FLOPs. The 8B model, sampled more aggressively, beats the 70B sampled once.</p>

<p>There are caveats:</p>

<ul>
  <li><strong>CodeContests is an exception</strong> — the 70B model stays more cost-effective; the smaller-wins pattern is task-dependent.</li>
  <li><strong>Verifier quality gates everything.</strong> Coverage gains don’t translate into solve-rate gains without a sharp verifier.</li>
  <li><strong>Majority voting and reward models plateau</strong> beyond a few hundred samples in domains lacking automatic verification. Selection, not generation, is the bottleneck.</li>
</ul>

<p>But for code, math, and formal proofs — the workloads where you can <em>prove</em> correctness of an individual sample — the “more small samples” strategy is cheaper <em>and</em> better.</p>

<h2 id="reproducing-the-shape-binary-search-fix">Reproducing the Shape: Binary Search Fix</h2>

<p>The accompanying notebook (forked to <a href="https://github.com/sw-ml-study/Repeated-Sampling">sw-ml-study/Repeated-Sampling</a>, original by <a href="https://github.com/weagan/Repeated-Sampling">weagan</a>) makes the paper’s claim tangible on a small budget.</p>

<p>The task: fix a buggy <code class="language-plaintext highlighter-rouge">binary_search</code> that has an off-by-one (<code class="language-plaintext highlighter-rouge">lo &lt; hi</code> instead of <code class="language-plaintext highlighter-rouge">&lt;=</code>) and must return the <strong>first occurrence</strong> of duplicates. Tested against 24 hidden cases.</p>

<p>Two configurations:</p>

<ul>
  <li><strong>Big model</strong>: <code class="language-plaintext highlighter-rouge">llama-3.3-70b-versatile</code>, temperature 0.0, 1 sample.</li>
  <li><strong>Small model</strong>: <code class="language-plaintext highlighter-rouge">llama-3.1-8b-instant</code>, temperature 0.8, N samples (where N ∈ {1, 5, 10, 15}).</li>
</ul>

<p>The core loop:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">all_fixes</span><span class="p">,</span> <span class="n">all_results</span><span class="p">,</span> <span class="n">coverage</span> <span class="o">=</span> <span class="p">[],</span> <span class="p">[],</span> <span class="p">[]</span>

<span class="c1"># Generate
</span><span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">max</span><span class="p">(</span><span class="n">sample_counts</span><span class="p">)):</span>
    <span class="n">fix</span> <span class="o">=</span> <span class="nf">generate_fix</span><span class="p">(</span><span class="n">BINARY_SEARCH_TASK</span><span class="p">,</span> <span class="n">SMALL_MODEL</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.8</span><span class="p">)</span>
    <span class="n">all_fixes</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">fix</span><span class="p">)</span>

<span class="c1"># Verify each
</span><span class="k">for</span> <span class="n">fix</span> <span class="ow">in</span> <span class="n">all_fixes</span><span class="p">:</span>
    <span class="n">passed</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="nf">test_function</span><span class="p">(</span><span class="n">fix</span><span class="p">,</span> <span class="n">BINARY_SEARCH_TASK</span><span class="p">)</span>
    <span class="n">all_results</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">passed</span><span class="p">)</span>

<span class="c1"># Cumulative coverage at each N
</span><span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="n">sample_counts</span><span class="p">:</span>
    <span class="n">coverage</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">any</span><span class="p">(</span><span class="n">all_results</span><span class="p">[:</span><span class="n">n</span><span class="p">]))</span>
</code></pre></div></div>

<p>Separation of generation from verification is the shape of every repeated-sampling system you’ll build after this: a batch of stochastic proposers, a deterministic scorer, a selection pass.</p>

<h3 id="observed-results">Observed results</h3>

<ul>
  <li>70B × 1 sample: passes (375 characters of code).</li>
  <li>8B × 15 samples: <strong>3 out of 15 pass</strong> (20%), but since “coverage” is “at least one passes,” the 8B model <em>wins at every tested N</em>.</li>
  <li>Cost: 70B @ 1 = $0.00034; 8B @ 15 = $0.00047 (marginally more, for a 15× robustness margin); 8B @ 10 = $0.00031 (cheaper than the single 70B call).</li>
</ul>

<h3 id="the-temperature-sweep">The temperature sweep</h3>

<p>Running 10 samples of the 8B model across six temperatures:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Temperature</th>
      <th style="text-align: center">Success rate (10 samples)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0.0</td>
      <td style="text-align: center">0%</td>
    </tr>
    <tr>
      <td style="text-align: center">0.5</td>
      <td style="text-align: center">50%</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>0.8</strong></td>
      <td style="text-align: center"><strong>60%</strong></td>
    </tr>
    <tr>
      <td style="text-align: center">1.0</td>
      <td style="text-align: center">30%</td>
    </tr>
    <tr>
      <td style="text-align: center">1.2</td>
      <td style="text-align: center">40%</td>
    </tr>
  </tbody>
</table>

<p>Temperature 0.8 is the sweet spot. Colder, and the model produces the same wrong answer every time — all monkeys compose identical gibberish, so coverage stays at zero. Hotter, and diversity starts corrupting the structure faster than it finds the solution — monkeys hammering ∞ Shakespeares in parallel but none completing a line. Exploration vs. exploitation, in one experiment, with a neat inverted-U.</p>

<p>This is a pedagogical gem: a single knob controls the trade-off and the wrong setting invalidates the whole approach.</p>

<h2 id="where-this-connects">Where This Connects</h2>

<p>Once you see LLMs as stochastic proposers + verifiers, a lot of modern infrastructure makes more sense:</p>

<ul>
  <li><strong>CI/CD coding agents.</strong> Tests are the verifier; parallel agent attempts are the samples. “Run the test suite 10 times with 10 different patches” is <em>exactly</em> repeated sampling.</li>
  <li><strong>Multi-agent orchestration.</strong> Each agent is one monkey. Diversity across agents (different prompts, tools, temperatures) is the dispersion that fuels coverage.</li>
  <li><strong>Tool-augmented LLMs.</strong> Tools execute, execution results verify, model iterates. Three laps around the generator/verifier loop.</li>
  <li><strong>Cascading models.</strong> Cheap model attempts many times → if all fail, escalate to an expensive model. Cost-tuned coverage scaling.</li>
</ul>

<p>The paper is, quietly, a manifesto for a particular kind of system: <em>stop buying smarter models, start building smarter ways to use the ones you have</em>.</p>

<h2 id="what-breaks">What Breaks</h2>

<p>The limitations are worth naming:</p>

<table>
  <thead>
    <tr>
      <th>Issue</th>
      <th>Consequence</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>No verifier</strong></td>
      <td>Majority voting and reward-model scoring plateau beyond a few hundred samples; coverage doesn’t translate to solve rate</td>
    </tr>
    <tr>
      <td><strong>i.i.d. sampling</strong></td>
      <td>The paper does <em>not</em> explore diversity-forcing strategies (re-prompting, retrieval, temperature schedules) — that’s future work</td>
    </tr>
    <tr>
      <td><strong>No feedback loop</strong></td>
      <td>Each sample is independent; failed samples don’t inform later ones. Agent systems that do use feedback are already departing from pure repeated sampling</td>
    </tr>
    <tr>
      <td><strong>Latency</strong></td>
      <td>Total wall-clock time grows unless samples run in parallel. Inference infrastructure has to favor throughput over per-request latency — a different shape than chatbot serving</td>
    </tr>
    <tr>
      <td><strong>Task-dependence</strong></td>
      <td>CodeContests shows the 70B still wins; smaller-beats-larger isn’t universal</td>
    </tr>
  </tbody>
</table>

<p>Selection is the choke point. Generation scales logarithmically “for free”; turning coverage into answers needs a verifier that itself scales.</p>

<h2 id="why-it-matters">Why It Matters</h2>

<p>Three shifts follow from taking this paper seriously:</p>

<ol>
  <li><strong>Smaller models become more useful.</strong> A 7B–13B model with a good verifier and parallel infrastructure can out-solve a 70B–400B single-shot on verifiable workloads.</li>
  <li><strong>Systems beat individual models.</strong> The pipeline (generator + verifier + selector + orchestrator) is the product. The model is a component.</li>
  <li><strong>Compute orchestration is a core skill.</strong> Batching, scheduling, cost-aware sample allocation, adaptive early-stopping — these are more leverage-able than model choice on most production workloads.</li>
</ol>

<p>The paper’s closing framing — <em>intelligence = search + verification over stochastic outputs</em> — isn’t a complete theory of intelligence. But it’s a useful reduction for the subset of problems where correctness is checkable: which turns out to be a surprisingly large and growing fraction of what we ask LLMs to do.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Open directions the paper gestures at:</p>

<ul>
  <li><strong>Learned verifiers.</strong> What happens when the scorer itself is a model, trained to rank candidates? (Reward models plateau; can better selectors scale?)</li>
  <li><strong>Self-consistency voting</strong> as a middle-ground between pure verification and reward models.</li>
  <li><strong>Tool-augmented sampling</strong>: samples don’t just propose text, they propose <em>actions</em> that get executed and verified.</li>
  <li><strong>Adaptive sample budgets</strong>: spend more where uncertainty is high, less where the first sample already looks good.</li>
</ul>

<p>The repeated-sampling axis is new enough that most of the engineering is yet to be done. The scaling law is the load-bearing result; the systems that exploit it are still being built.</p>

<hr />

<p><em>The paper title is a reference, but not a joke — with enough attempts and a reliable scorer, an arbitrarily weak model becomes reliable. The leverage is in the scorer. The monkeys are easy.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="llm" /><category term="machine-learning" /><category term="research" /><category term="machine-learning" /><category term="large-language-monkeys" /><category term="repeated-sampling" /><category term="inference-scaling" /><category term="verification" /><category term="swe-bench" /><category term="small-models" /><category term="agents" /><category term="coverage" /><category term="scaling-laws" /><summary type="html"><![CDATA[“Large Language Monkeys” is Brown et al.’s (2024) nod to the infinite-monkey theorem, reframed for inference-time compute: if you let a model take many attempts at a problem — and you have an automatic verifier to pick the right one — the odds of at least one attempt being correct climb faster than almost any intuition predicts. Coverage (the fraction of problems solved by any sample) scales log-linearly with the number of samples over four orders of magnitude. Small models stop being second-class. Inference becomes a first-class scaling axis. Resource Link Paper arXiv 2407.21787 (Brown et al., 2024) Code (forked) sw-ml-study/Repeated-Sampling Code (original) weagan/Repeated-Sampling Demo notebook llm_monkeys_function_demo.ipynb Comments Discord The Reframing The last decade of scaling has been about making the model bigger — more parameters, more training data, more pretraining compute. Inference, by contrast, has stayed boringly fixed: prompt in, answer out, one attempt, take it or leave it. This paper asks: what if the attempts are a scaling axis? Traditional scaling Inference-time scaling Bigger model More attempts per problem More training data Repeated sampling Single-shot answer Multi-sample + verification The mental model shifts from “the model knows the answer” to “the model generates candidate answers; we search for a correct one.” Under that lens, an LLM isn’t an oracle — it’s a stochastic proposer wired to a verifier. The Core Loop Strip it to essentials: for i in 1..N: sample_i = model(prompt, temperature=T) score_i = verifier(sample_i) return best(sample_i) Two components: Generator — an LLM producing candidate solutions. Temperature drives diversity. Verifier / selector — unit tests (code), symbolic checker (math), heuristic scorer. Picks the right answer from the pile. The verifier is the load-bearing piece. When it’s cheap and deterministic (code → run tests; math proof → check with Lean), repeated sampling is devastatingly effective. When it isn’t (essays, subjective tasks, open-ended reasoning), the scaling story gets thornier. What the Numbers Look Like The paper’s headline result on SWE-bench Lite: Setup Solve rate DeepSeek-Coder-V2-Instruct, 1 sample 15.9% DeepSeek-Coder-V2-Instruct, 250 samples 56.0% Single-sample SOTA at time of writing 43.0% Repeated sampling from a non-frontier model blew past the frontier single-sample SOTA on one of the hardest real-world coding benchmarks. Not by a little — by 13 percentage points. The cost story hits even harder: Configuration Solve rate Cost DeepSeek-Coder-V2-Instruct × 5 samples 29.62% $10.80 GPT-4o × 1 sample 24.00% $39 Claude 3.5 Sonnet × 1 sample 26.70% $51 Five DeepSeek attempts beat one attempt from each frontier model at roughly 4× lower cost. Repeated sampling changes the economics of deploying AI on verifiable tasks. The Coverage Scaling Law The paper models coverage (c, probability at least one of k samples is correct) as an exponentiated power law: c(k) &nbsp;≈&nbsp; exp(a &middot; kb) where k is the number of samples and a, b are fitted parameters. Across GSM8K, MATH, MiniF2F, SWE-bench Lite, and CodeContests, coverage scales log-linearly with the sample count over four orders of magnitude. On CodeContests with Gemma-2B, coverage climbs from 0.02% at 1 sample to 7.1% at 10k samples — that’s more than a 350× improvement, purely from adding attempts. The shape matters. A log-linear curve means every multiplicative increase in samples yields a fixed additive gain in coverage. Doubling samples → constant boost in solve rate. This is a scaling law, not a diminishing-returns saturation. It’s why the paper’s title matters: enough monkeys, enough time, and Shakespeare emerges from chance. The quantitative claim is that the “enough” is measurable and predictable. Weak Models, Smart Sampling The most important empirical finding for practitioners: smaller models outperform larger ones at matched compute, on the right tasks. On FLOP-matched comparisons across MATH, GSM8K, and MiniF2F, Llama-3-8B-Instruct with more samples achieves higher coverage than Llama-3-70B-Instruct — at the same total inference FLOPs. The 8B model, sampled more aggressively, beats the 70B sampled once. There are caveats: CodeContests is an exception — the 70B model stays more cost-effective; the smaller-wins pattern is task-dependent. Verifier quality gates everything. Coverage gains don’t translate into solve-rate gains without a sharp verifier. Majority voting and reward models plateau beyond a few hundred samples in domains lacking automatic verification. Selection, not generation, is the bottleneck. But for code, math, and formal proofs — the workloads where you can prove correctness of an individual sample — the “more small samples” strategy is cheaper and better. Reproducing the Shape: Binary Search Fix The accompanying notebook (forked to sw-ml-study/Repeated-Sampling, original by weagan) makes the paper’s claim tangible on a small budget. The task: fix a buggy binary_search that has an off-by-one (lo &lt; hi instead of &lt;=) and must return the first occurrence of duplicates. Tested against 24 hidden cases. Two configurations: Big model: llama-3.3-70b-versatile, temperature 0.0, 1 sample. Small model: llama-3.1-8b-instant, temperature 0.8, N samples (where N ∈ {1, 5, 10, 15}). The core loop: all_fixes, all_results, coverage = [], [], [] # Generate for i in range(max(sample_counts)): fix = generate_fix(BINARY_SEARCH_TASK, SMALL_MODEL, temperature=0.8) all_fixes.append(fix) # Verify each for fix in all_fixes: passed, _ = test_function(fix, BINARY_SEARCH_TASK) all_results.append(passed) # Cumulative coverage at each N for n in sample_counts: coverage.append(any(all_results[:n])) Separation of generation from verification is the shape of every repeated-sampling system you’ll build after this: a batch of stochastic proposers, a deterministic scorer, a selection pass. Observed results 70B × 1 sample: passes (375 characters of code). 8B × 15 samples: 3 out of 15 pass (20%), but since “coverage” is “at least one passes,” the 8B model wins at every tested N. Cost: 70B @ 1 = $0.00034; 8B @ 15 = $0.00047 (marginally more, for a 15× robustness margin); 8B @ 10 = $0.00031 (cheaper than the single 70B call). The temperature sweep Running 10 samples of the 8B model across six temperatures: Temperature Success rate (10 samples) 0.0 0% 0.5 50% 0.8 60% 1.0 30% 1.2 40% Temperature 0.8 is the sweet spot. Colder, and the model produces the same wrong answer every time — all monkeys compose identical gibberish, so coverage stays at zero. Hotter, and diversity starts corrupting the structure faster than it finds the solution — monkeys hammering ∞ Shakespeares in parallel but none completing a line. Exploration vs. exploitation, in one experiment, with a neat inverted-U. This is a pedagogical gem: a single knob controls the trade-off and the wrong setting invalidates the whole approach. Where This Connects Once you see LLMs as stochastic proposers + verifiers, a lot of modern infrastructure makes more sense: CI/CD coding agents. Tests are the verifier; parallel agent attempts are the samples. “Run the test suite 10 times with 10 different patches” is exactly repeated sampling. Multi-agent orchestration. Each agent is one monkey. Diversity across agents (different prompts, tools, temperatures) is the dispersion that fuels coverage. Tool-augmented LLMs. Tools execute, execution results verify, model iterates. Three laps around the generator/verifier loop. Cascading models. Cheap model attempts many times → if all fail, escalate to an expensive model. Cost-tuned coverage scaling. The paper is, quietly, a manifesto for a particular kind of system: stop buying smarter models, start building smarter ways to use the ones you have. What Breaks The limitations are worth naming: Issue Consequence No verifier Majority voting and reward-model scoring plateau beyond a few hundred samples; coverage doesn’t translate to solve rate i.i.d. sampling The paper does not explore diversity-forcing strategies (re-prompting, retrieval, temperature schedules) — that’s future work No feedback loop Each sample is independent; failed samples don’t inform later ones. Agent systems that do use feedback are already departing from pure repeated sampling Latency Total wall-clock time grows unless samples run in parallel. Inference infrastructure has to favor throughput over per-request latency — a different shape than chatbot serving Task-dependence CodeContests shows the 70B still wins; smaller-beats-larger isn’t universal Selection is the choke point. Generation scales logarithmically “for free”; turning coverage into answers needs a verifier that itself scales. Why It Matters Three shifts follow from taking this paper seriously: Smaller models become more useful. A 7B–13B model with a good verifier and parallel infrastructure can out-solve a 70B–400B single-shot on verifiable workloads. Systems beat individual models. The pipeline (generator + verifier + selector + orchestrator) is the product. The model is a component. Compute orchestration is a core skill. Batching, scheduling, cost-aware sample allocation, adaptive early-stopping — these are more leverage-able than model choice on most production workloads. The paper’s closing framing — intelligence = search + verification over stochastic outputs — isn’t a complete theory of intelligence. But it’s a useful reduction for the subset of problems where correctness is checkable: which turns out to be a surprisingly large and growing fraction of what we ask LLMs to do. What’s Next Open directions the paper gestures at: Learned verifiers. What happens when the scorer itself is a model, trained to rank candidates? (Reward models plateau; can better selectors scale?) Self-consistency voting as a middle-ground between pure verification and reward models. Tool-augmented sampling: samples don’t just propose text, they propose actions that get executed and verified. Adaptive sample budgets: spend more where uncertainty is high, less where the first sample already looks good. The repeated-sampling axis is new enough that most of the engineering is yet to be done. The scaling law is the load-bearing result; the systems that exploit it are still being built. The paper title is a reference, but not a joke — with enough attempts and a reliable scorer, an arbitrarily weak model becomes reliable. The leverage is in the scorer. The monkeys are easy.]]></summary></entry><entry><title type="html">Rabbit-hole (3/?): FORTH — Life After Hashing</title><link href="https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing/" rel="alternate" type="text/html" title="Rabbit-hole (3/?): FORTH — Life After Hashing" /><published>2026-04-24T00:15:00-07:00</published><updated>2026-04-24T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing/"><![CDATA[<p><img src="/assets/images/posts/block-armed-rabbit-with-bow.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>Second FORTH post in the rabbit hole. <a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Part 1</a> explained why phase 4 (<code class="language-plaintext highlighter-rouge">forth-from-forth</code>) is deleting the XMX hash and the 256-bucket table along with the asm <code class="language-plaintext highlighter-rouge">do_find</code> that used them. This post answers the obvious follow-up: <strong>if not a hash, then what?</strong> Because “no optimization” isn’t quite right either. FIND still runs thousands of times during bootstrap and source loading. The asm-era tools are gone, but the interactive hot path deserves something.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Prior post</strong></td>
        <td><a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</a></td>
      </tr>
      <tr>
        <td><strong>Overview post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-post-hash-toolbox">The Post-Hash Toolbox</h2>

<p>Three optimizations, all cheaper than a hash, all written in pure Forth. None of them require a side table, none require touching dictionary headers, none require boot-time setup. Each one trades a little code for a better hit rate on the patterns that actually show up in source files.</p>

<table>
  <thead>
    <tr>
      <th>Optimization</th>
      <th>Cost</th>
      <th>Hit pattern</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Numeric fast path</strong></td>
      <td>~5 lines of Forth</td>
      <td>Any token starting with a digit or <code class="language-plaintext highlighter-rouge">-&lt;digit&gt;</code></td>
    </tr>
    <tr>
      <td><strong>Recent-hit cache</strong> (N entries)</td>
      <td>~10 lines + N × 2 cells of memory</td>
      <td>Tokens repeated within a short window</td>
    </tr>
    <tr>
      <td><strong>Hot-token cache</strong> (top-K)</td>
      <td>~20 lines + K × 2 cells + some accounting</td>
      <td>The handful of names that appear 100s of times</td>
    </tr>
  </tbody>
</table>

<p>They’re orthogonal. You can ship any one, any two, or all three. The order below is the order I’d install them in: each next one is a little more work for a little more payoff.</p>

<h2 id="optimization-1-numeric-fast-path">Optimization 1: Numeric Fast Path</h2>

<p>Every <code class="language-plaintext highlighter-rouge">.fth</code> source file has numbers in it. In the all-asm INTERPRET, each number makes FIND walk the dictionary, fail to find a match, and <em>then</em> fall through to NUMBER. That’s wasted work — you can tell by the first character that no dictionary word will match.</p>

<p>The fix is a one-line pre-check at the top of INTERPRET:</p>

<pre><code class="language-forth">: INTERPRET
    WORD DUP C@                   \ token (addr len) first-char
    [CHAR] 0 [CHAR] 9 WITHIN
    IF  NUMBER  EXIT  THEN        \ starts with a digit — skip FIND
    ... (normal FIND path) ...
;
</code></pre>

<p><em>(Handle <code class="language-plaintext highlighter-rouge">-&lt;digit&gt;</code> with one more check. Handle <code class="language-plaintext highlighter-rouge">$</code> or <code class="language-plaintext highlighter-rouge">0x</code> hex prefix if your Forth takes those.)</em></p>

<p>On a <code class="language-plaintext highlighter-rouge">.fth</code> source file like <code class="language-plaintext highlighter-rouge">examples/fib.fth</code>:</p>

<pre><code class="language-forth">: FIB ( n -- n' )
    DUP 2 &lt; IF EXIT THEN
    DUP 1- RECURSE
    SWAP 2- RECURSE
    + ;
</code></pre>

<p>The tokens <code class="language-plaintext highlighter-rouge">2</code>, <code class="language-plaintext highlighter-rouge">1-</code>, <code class="language-plaintext highlighter-rouge">2-</code> all start with digits (well, <code class="language-plaintext highlighter-rouge">1-</code> and <code class="language-plaintext highlighter-rouge">2-</code> <em>look like</em> numbers until the dash is consumed and the remaining part doesn’t parse — some Forths special-case <code class="language-plaintext highlighter-rouge">1-</code> as a word anyway; take the simple path and let FIND confirm after the numeric parse fails). The main point: literal numbers like <code class="language-plaintext highlighter-rouge">2</code> get a direct routing to NUMBER and skip FIND entirely.</p>

<p><strong>Measurement shape.</strong> On the COR24 core loading <code class="language-plaintext highlighter-rouge">examples/*.fth</code>, numeric tokens are ~20% of the input stream. Fast-pathing them avoids the equivalent fraction of dictionary walks. One branch, huge payoff.</p>

<p><strong>Caveat.</strong> If your Forth lets words start with digits (<code class="language-plaintext highlighter-rouge">2dup</code>, <code class="language-plaintext highlighter-rouge">2drop</code>, <code class="language-plaintext highlighter-rouge">4th</code>), the fast path has to fall back to FIND on “might-be-a-word” failures. In practice that means: try NUMBER first, and if NUMBER rejects the whole token, <em>then</em> FIND it. The rejection is fast — NUMBER aborts on the first non-digit.</p>

<h2 id="optimization-2-recent-hit-cache">Optimization 2: Recent-Hit Cache</h2>

<p>The circular-buffer design. Keep the last N successful <code class="language-plaintext highlighter-rouge">(hash, cfa, flags)</code> tuples — call it 8 or 16 entries. Every FIND first probes the circular buffer by hash; on hit, return immediately. On miss, walk the dictionary, then insert the result into the next slot of the buffer (overwriting the oldest).</p>

<p>Rough structure in Forth:</p>

<pre><code class="language-forth">16 CONSTANT RECENT-SIZE
CREATE recent-hash  RECENT-SIZE CELLS ALLOT
CREATE recent-cfa   RECENT-SIZE CELLS ALLOT
CREATE recent-flags RECENT-SIZE CELLS ALLOT
VARIABLE recent-next   \ index where the next insert lands

: recent-lookup ( h -- cfa flags | 0 0 )
    RECENT-SIZE 0 DO
        DUP recent-hash I CELLS + @ =
        IF  DROP
            recent-cfa   I CELLS + @
            recent-flags I CELLS + @
            UNLOOP EXIT
        THEN
    LOOP  DROP 0 0 ;

: recent-insert ( h cfa flags -- )
    recent-next @  &gt;R
    R@ recent-flags + !
    R@ recent-cfa   + !
    R@ recent-hash  + !
    recent-next @ 1+  RECENT-SIZE MOD  recent-next ! ;
</code></pre>

<p>A FIND now becomes:</p>

<pre><code class="language-forth">: FIND  ( addr len -- cfa flags )
    hash-name recent-lookup  ?DUP IF EXIT THEN  DROP
    dict-walk  ( returns cfa flags or 0 0 )
    DUP IF  \ found — remember it
        2DUP hash-name -ROT recent-insert
    THEN ;
</code></pre>

<p>Where <code class="language-plaintext highlighter-rouge">hash-name</code> is a <em>cheap</em> hash (just XOR the first 3 characters, for example) — the full XMX is overkill here because we only need to distinguish N=16 candidates, not partition 256 buckets.</p>

<p><strong>Why it works.</strong> Temporal locality. In a typical <code class="language-plaintext highlighter-rouge">.fth</code> block:</p>

<pre><code class="language-forth">: SQUARE DUP * ;
: CUBE DUP SQUARE * ;
: FOURTH SQUARE SQUARE ;
</code></pre>

<p><code class="language-plaintext highlighter-rouge">DUP</code>, <code class="language-plaintext highlighter-rouge">*</code>, <code class="language-plaintext highlighter-rouge">SQUARE</code>, and <code class="language-plaintext highlighter-rouge">;</code> each get looked up multiple times within a few-token window. The recent-hit buffer catches all of it. Buffer size N=8 is enough for most Forth source; N=16 is generous.</p>

<p><strong>Cost.</strong> <code class="language-plaintext highlighter-rouge">N × 3 cells ≈ 72 bytes</code> for a 16-entry cache on COR24 (3-byte cells). Linear scan of 16 entries is ~48 COR24 instructions worst case, fewer on hit. Compare to a hash-table miss walking a chain of 15 — the circular buffer wins whenever the hit rate is above ~25%.</p>

<h2 id="optimization-3-hot-token-cache">Optimization 3: Hot-Token Cache</h2>

<p>The recent-hit cache catches <em>local</em> repetition. What it misses: tokens that appear in many different contexts — <code class="language-plaintext highlighter-rouge">DUP</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">IF</code>, <code class="language-plaintext highlighter-rouge">;</code>. These words show up hundreds of times across a full core-loading pass, but rarely in back-to-back tokens. The recent buffer evicts them between hits.</p>

<p>The hot-token cache handles that shape. Top-K by frequency. K is small — 4 or 8 entries is plenty — because Forth’s name distribution is severely top-heavy: the top 10 words typically account for 40–60% of all lookups in a core.</p>

<p>Structure is essentially the recent cache plus frequency counters:</p>

<pre><code class="language-forth">8 CONSTANT HOT-SIZE
CREATE hot-hash  HOT-SIZE CELLS ALLOT
CREATE hot-cfa   HOT-SIZE CELLS ALLOT
CREATE hot-flags HOT-SIZE CELLS ALLOT
CREATE hot-count HOT-SIZE CELLS ALLOT
</code></pre>

<p>On every dictionary walk that finds a word:</p>
<ul>
  <li>Increment the count for that word’s bucket.</li>
  <li>If any bucket’s count exceeds the minimum in the top-K, promote the new entry and evict the former minimum.</li>
</ul>

<p>Lookup probes the hot cache <em>first</em>, then the recent cache, then does the dictionary walk.</p>

<p><strong>Why it works.</strong> Forth’s word-frequency distribution is Zipf-like. The top 8 names cover roughly half of all lookups in a typical <code class="language-plaintext highlighter-rouge">.fth</code> corpus; the top 32 cover nearly all of them. A tiny K-wide cache pins the most-frequent names in place regardless of temporal locality.</p>

<p><strong>Pitfall.</strong> If you promote aggressively on every increment, the top-K churns on early workloads before settling. Add a small hysteresis (only promote when the count exceeds the minimum by some margin), or seed the cache after a profiling pass has accumulated stable counts.</p>

<h2 id="the-instrumentation-question">The Instrumentation Question</h2>

<p>You can’t build the hot-token cache honestly without counting. And the counter has to count the <strong>right</strong> thing.</p>

<p>There are three plausible counts, and they’re <strong>not the same</strong>:</p>

<table>
  <thead>
    <tr>
      <th>Counter</th>
      <th>What it measures</th>
      <th>Useful for</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Source-interpret count</strong></td>
      <td>How often a name is looked up while reading <code class="language-plaintext highlighter-rouge">.fth</code> source in interpret mode</td>
      <td>Tuning the recent cache for REPL use</td>
    </tr>
    <tr>
      <td><strong>Source-compile count</strong></td>
      <td>How often a name is looked up while reading <code class="language-plaintext highlighter-rouge">.fth</code> source in compile mode</td>
      <td>Tuning the recent/hot cache for <code class="language-plaintext highlighter-rouge">.fth</code> loading</td>
    </tr>
    <tr>
      <td><strong>Runtime execution count</strong></td>
      <td>How often a compiled word runs</td>
      <td>Dictionary reordering, inlining decisions</td>
    </tr>
  </tbody>
</table>

<p>Naive instrumentation counts <em>executions</em> because that’s what’s easy to hook into <code class="language-plaintext highlighter-rouge">NEXT</code>. But execution count is wrong for lookup optimization. <code class="language-plaintext highlighter-rouge">DUP</code> may execute millions of times inside compiled loops but only be looked up a few dozen times during source-reading. What you want to cache is the lookup pattern, not the runtime pattern.</p>

<p>That distinction matters even more for phase-4-style systems that will eventually drop the interpreter entirely in production builds. Lookup frequency is a <em>boot-time</em> cost. Execution frequency is a <em>runtime</em> cost. Different optimizations target different costs.</p>

<h2 id="why-linear-walk--cache-often-beats-hash">Why Linear Walk + Cache Often Beats Hash</h2>

<p>Adding up the options against the phase 2 hash:</p>

<ul>
  <li>Numeric fast path: zero collision risk, saves the 20% of tokens that are numbers.</li>
  <li>Recent-hit cache: catches local repetition, which is the dominant lookup pattern in structured Forth code.</li>
  <li>Hot-token cache: catches global high-frequency names — the “Zipf tail” that’s always hot.</li>
</ul>

<p>Three orthogonal wins, none of which need:</p>
<ul>
  <li>A 768-byte bucket table.</li>
  <li>A second link field in every dictionary entry.</li>
  <li>Boot-time hash population.</li>
  <li>CREATE-time bucket splicing.</li>
  <li>Re-implementing any of that in high-level Forth.</li>
</ul>

<p>The hash was a <em>general</em> answer to <em>amortize all FIND cost via bucket distribution</em>. The layered caches are <em>specific</em> answers to the <em>specific</em> patterns that happen in real Forth code. Specific answers are usually cheaper when the patterns are well-understood — and Forth’s patterns are very well-understood, because the language has been alive for 50 years and its word-frequency distributions don’t really change.</p>

<p>The classic rule of thumb applies: <strong>optimize the actual workload, not the worst case</strong>. A hash protects you against adversarial inputs and dictionaries thousands of words deep. A layered cache protects you against the real workload of a few hundred words and strong locality. For a bootstrapping Forth, the real workload is the only workload.</p>

<h2 id="the-good-enough-threshold">The “Good Enough” Threshold</h2>

<p>Here’s the pragmatic shape of the decision:</p>

<table>
  <thead>
    <tr>
      <th>Workload</th>
      <th>What to ship</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Just loading a small core (~50 words, one pass)</td>
      <td>Linear walk, nothing else</td>
    </tr>
    <tr>
      <td>Full core + extended <code class="language-plaintext highlighter-rouge">.fth</code> library (~200 words)</td>
      <td>Add numeric fast path</td>
    </tr>
    <tr>
      <td>Interactive REPL + <code class="language-plaintext highlighter-rouge">.fth</code> loading</td>
      <td>Add recent-hit cache (16 entries)</td>
    </tr>
    <tr>
      <td>Profiling shows unbalanced hot words</td>
      <td>Add hot-token cache (8 entries)</td>
    </tr>
    <tr>
      <td>Dictionary exceeds 500 words, lookup dominates</td>
      <td>Revisit: first-char bucket or XMX</td>
    </tr>
  </tbody>
</table>

<p>The insight from phase 2’s hash work was that <em>the threshold for “hash is worth it”</em> turned out to be a larger dictionary than COR24 Forth actually has. Phase 4 takes that insight seriously: don’t carry the hash subsystem if the actual workload doesn’t need it. Install the cheap wins. Measure. Only reach for heavier mechanism if measurement shows the cheap wins ran out.</p>

<h2 id="whats-next">What’s Next</h2>

<p>The layered caches make lookup <em>fast</em>. The next rabbit-hole post asks a different question: <em>can we make lookup go away entirely?</em> Dictionary compaction, shadowed-word removal, splitting development images from production images — the point where “optimize FIND” becomes “eliminate FIND for this deployment”. That’s <em>Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images</em>.</p>

<hr />

<p><em>Forth’s patterns are stable because Forth is old. Optimization strategies that assume stable patterns pay off handsomely. Optimization strategies that assume adversarial inputs carry weight you don’t need.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="dictionary" /><category term="find" /><category term="lookup-cache" /><category term="numeric-fast-path" /><category term="recent-hit-cache" /><category term="hot-token-cache" /><category term="instrumentation" /><category term="optimization" /><category term="self-hosting" /><summary type="html"><![CDATA[Second FORTH post in the rabbit hole. Part 1 explained why phase 4 (forth-from-forth) is deleting the XMX hash and the 256-bucket table along with the asm do_find that used them. This post answers the obvious follow-up: if not a hash, then what? Because “no optimization” isn’t quite right either. FIND still runs thousands of times during bootstrap and source loading. The asm-era tools are gone, but the interactive hot path deserves something. Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Prior post Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name Overview post Embedded (3/?): Self-Hosting Spectrum Comments Discord The Post-Hash Toolbox Three optimizations, all cheaper than a hash, all written in pure Forth. None of them require a side table, none require touching dictionary headers, none require boot-time setup. Each one trades a little code for a better hit rate on the patterns that actually show up in source files. Optimization Cost Hit pattern Numeric fast path ~5 lines of Forth Any token starting with a digit or -&lt;digit&gt; Recent-hit cache (N entries) ~10 lines + N × 2 cells of memory Tokens repeated within a short window Hot-token cache (top-K) ~20 lines + K × 2 cells + some accounting The handful of names that appear 100s of times They’re orthogonal. You can ship any one, any two, or all three. The order below is the order I’d install them in: each next one is a little more work for a little more payoff. Optimization 1: Numeric Fast Path Every .fth source file has numbers in it. In the all-asm INTERPRET, each number makes FIND walk the dictionary, fail to find a match, and then fall through to NUMBER. That’s wasted work — you can tell by the first character that no dictionary word will match. The fix is a one-line pre-check at the top of INTERPRET: : INTERPRET WORD DUP C@ \ token (addr len) first-char [CHAR] 0 [CHAR] 9 WITHIN IF NUMBER EXIT THEN \ starts with a digit — skip FIND ... (normal FIND path) ... ; (Handle -&lt;digit&gt; with one more check. Handle $ or 0x hex prefix if your Forth takes those.) On a .fth source file like examples/fib.fth: : FIB ( n -- n' ) DUP 2 &lt; IF EXIT THEN DUP 1- RECURSE SWAP 2- RECURSE + ; The tokens 2, 1-, 2- all start with digits (well, 1- and 2- look like numbers until the dash is consumed and the remaining part doesn’t parse — some Forths special-case 1- as a word anyway; take the simple path and let FIND confirm after the numeric parse fails). The main point: literal numbers like 2 get a direct routing to NUMBER and skip FIND entirely. Measurement shape. On the COR24 core loading examples/*.fth, numeric tokens are ~20% of the input stream. Fast-pathing them avoids the equivalent fraction of dictionary walks. One branch, huge payoff. Caveat. If your Forth lets words start with digits (2dup, 2drop, 4th), the fast path has to fall back to FIND on “might-be-a-word” failures. In practice that means: try NUMBER first, and if NUMBER rejects the whole token, then FIND it. The rejection is fast — NUMBER aborts on the first non-digit. Optimization 2: Recent-Hit Cache The circular-buffer design. Keep the last N successful (hash, cfa, flags) tuples — call it 8 or 16 entries. Every FIND first probes the circular buffer by hash; on hit, return immediately. On miss, walk the dictionary, then insert the result into the next slot of the buffer (overwriting the oldest). Rough structure in Forth: 16 CONSTANT RECENT-SIZE CREATE recent-hash RECENT-SIZE CELLS ALLOT CREATE recent-cfa RECENT-SIZE CELLS ALLOT CREATE recent-flags RECENT-SIZE CELLS ALLOT VARIABLE recent-next \ index where the next insert lands : recent-lookup ( h -- cfa flags | 0 0 ) RECENT-SIZE 0 DO DUP recent-hash I CELLS + @ = IF DROP recent-cfa I CELLS + @ recent-flags I CELLS + @ UNLOOP EXIT THEN LOOP DROP 0 0 ; : recent-insert ( h cfa flags -- ) recent-next @ &gt;R R@ recent-flags + ! R@ recent-cfa + ! R@ recent-hash + ! recent-next @ 1+ RECENT-SIZE MOD recent-next ! ; A FIND now becomes: : FIND ( addr len -- cfa flags ) hash-name recent-lookup ?DUP IF EXIT THEN DROP dict-walk ( returns cfa flags or 0 0 ) DUP IF \ found — remember it 2DUP hash-name -ROT recent-insert THEN ; Where hash-name is a cheap hash (just XOR the first 3 characters, for example) — the full XMX is overkill here because we only need to distinguish N=16 candidates, not partition 256 buckets. Why it works. Temporal locality. In a typical .fth block: : SQUARE DUP * ; : CUBE DUP SQUARE * ; : FOURTH SQUARE SQUARE ; DUP, *, SQUARE, and ; each get looked up multiple times within a few-token window. The recent-hit buffer catches all of it. Buffer size N=8 is enough for most Forth source; N=16 is generous. Cost. N × 3 cells ≈ 72 bytes for a 16-entry cache on COR24 (3-byte cells). Linear scan of 16 entries is ~48 COR24 instructions worst case, fewer on hit. Compare to a hash-table miss walking a chain of 15 — the circular buffer wins whenever the hit rate is above ~25%. Optimization 3: Hot-Token Cache The recent-hit cache catches local repetition. What it misses: tokens that appear in many different contexts — DUP, !, @, IF, ;. These words show up hundreds of times across a full core-loading pass, but rarely in back-to-back tokens. The recent buffer evicts them between hits. The hot-token cache handles that shape. Top-K by frequency. K is small — 4 or 8 entries is plenty — because Forth’s name distribution is severely top-heavy: the top 10 words typically account for 40–60% of all lookups in a core. Structure is essentially the recent cache plus frequency counters: 8 CONSTANT HOT-SIZE CREATE hot-hash HOT-SIZE CELLS ALLOT CREATE hot-cfa HOT-SIZE CELLS ALLOT CREATE hot-flags HOT-SIZE CELLS ALLOT CREATE hot-count HOT-SIZE CELLS ALLOT On every dictionary walk that finds a word: Increment the count for that word’s bucket. If any bucket’s count exceeds the minimum in the top-K, promote the new entry and evict the former minimum. Lookup probes the hot cache first, then the recent cache, then does the dictionary walk. Why it works. Forth’s word-frequency distribution is Zipf-like. The top 8 names cover roughly half of all lookups in a typical .fth corpus; the top 32 cover nearly all of them. A tiny K-wide cache pins the most-frequent names in place regardless of temporal locality. Pitfall. If you promote aggressively on every increment, the top-K churns on early workloads before settling. Add a small hysteresis (only promote when the count exceeds the minimum by some margin), or seed the cache after a profiling pass has accumulated stable counts. The Instrumentation Question You can’t build the hot-token cache honestly without counting. And the counter has to count the right thing. There are three plausible counts, and they’re not the same: Counter What it measures Useful for Source-interpret count How often a name is looked up while reading .fth source in interpret mode Tuning the recent cache for REPL use Source-compile count How often a name is looked up while reading .fth source in compile mode Tuning the recent/hot cache for .fth loading Runtime execution count How often a compiled word runs Dictionary reordering, inlining decisions Naive instrumentation counts executions because that’s what’s easy to hook into NEXT. But execution count is wrong for lookup optimization. DUP may execute millions of times inside compiled loops but only be looked up a few dozen times during source-reading. What you want to cache is the lookup pattern, not the runtime pattern. That distinction matters even more for phase-4-style systems that will eventually drop the interpreter entirely in production builds. Lookup frequency is a boot-time cost. Execution frequency is a runtime cost. Different optimizations target different costs. Why Linear Walk + Cache Often Beats Hash Adding up the options against the phase 2 hash: Numeric fast path: zero collision risk, saves the 20% of tokens that are numbers. Recent-hit cache: catches local repetition, which is the dominant lookup pattern in structured Forth code. Hot-token cache: catches global high-frequency names — the “Zipf tail” that’s always hot. Three orthogonal wins, none of which need: A 768-byte bucket table. A second link field in every dictionary entry. Boot-time hash population. CREATE-time bucket splicing. Re-implementing any of that in high-level Forth. The hash was a general answer to amortize all FIND cost via bucket distribution. The layered caches are specific answers to the specific patterns that happen in real Forth code. Specific answers are usually cheaper when the patterns are well-understood — and Forth’s patterns are very well-understood, because the language has been alive for 50 years and its word-frequency distributions don’t really change. The classic rule of thumb applies: optimize the actual workload, not the worst case. A hash protects you against adversarial inputs and dictionaries thousands of words deep. A layered cache protects you against the real workload of a few hundred words and strong locality. For a bootstrapping Forth, the real workload is the only workload. The “Good Enough” Threshold Here’s the pragmatic shape of the decision: Workload What to ship Just loading a small core (~50 words, one pass) Linear walk, nothing else Full core + extended .fth library (~200 words) Add numeric fast path Interactive REPL + .fth loading Add recent-hit cache (16 entries) Profiling shows unbalanced hot words Add hot-token cache (8 entries) Dictionary exceeds 500 words, lookup dominates Revisit: first-char bucket or XMX The insight from phase 2’s hash work was that the threshold for “hash is worth it” turned out to be a larger dictionary than COR24 Forth actually has. Phase 4 takes that insight seriously: don’t carry the hash subsystem if the actual workload doesn’t need it. Install the cheap wins. Measure. Only reach for heavier mechanism if measurement shows the cheap wins ran out. What’s Next The layered caches make lookup fast. The next rabbit-hole post asks a different question: can we make lookup go away entirely? Dictionary compaction, shadowed-word removal, splitting development images from production images — the point where “optimize FIND” becomes “eliminate FIND for this deployment”. That’s Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images. Forth’s patterns are stable because Forth is old. Optimization strategies that assume stable patterns pay off handsomely. Optimization strategies that assume adversarial inputs carry weight you don’t need.]]></summary></entry><entry><title type="html">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</title><link href="https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/" rel="alternate" type="text/html" title="Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name" /><published>2026-04-23T00:15:00-07:00</published><updated>2026-04-23T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/"><![CDATA[<p><img src="/assets/images/posts/block-rabbit-hole-watch.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>First FORTH post in this rabbit hole — more are planned. The <a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">self-hosting spectrum post</a> laid out four phases (all-asm kernel → forth-in-forth → forth-on-forthish → forth-from-forth) and caught the project mid-stride: phase 2 shipped, phase 3 with its first subsets just landing, phase 4 still on the horizon.</p>

  <p><strong>Since then, phase 3 has completed</strong> — <code class="language-plaintext highlighter-rouge">forth-on-forthish</code> finished subsets through 21, pulling <code class="language-plaintext highlighter-rouge">WORD</code>, <code class="language-plaintext highlighter-rouge">FIND</code>, <code class="language-plaintext highlighter-rouge">NUMBER</code>, <code class="language-plaintext highlighter-rouge">INTERPRET</code>, and <code class="language-plaintext highlighter-rouge">QUIT</code> into Forth and leaving ~22 irreducible assembly primitives. That moves the project’s active edge onto phase 4, <code class="language-plaintext highlighter-rouge">forth-from-forth</code> — a Forth-hosted cross-compiler that emits the kernel’s <code class="language-plaintext highlighter-rouge">.s</code> file as a build artifact. Working toward it surfaced a set of design questions that don’t fit in a single post: how lookup really works, what to optimize once the asm <code class="language-plaintext highlighter-rouge">do_find</code> goes away, how to build specialized images, whether to structure the system as a composer, how to retarget the codegen. Each of those is a rabbit hole of its own, and this post is the first.</p>

  <p>This post goes one level deeper on the single operation that gets executed more than any other during bootstrap, source loading, and the REPL: <strong>dictionary lookup</strong>.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Web Demo Repo</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-forth">sw-embed/web-sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>WASM Side-Experiment</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/sw-fth-wasm">sw-vibe-coding/sw-fth-wasm</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="what-find-does">What FIND Does</h2>

<p>FIND is the operation that turns a name into something you can execute. Given a word like <code class="language-plaintext highlighter-rouge">DUP</code>, it walks the dictionary and returns the address of the entry — the code field address, or <strong>CFA</strong> — along with whether the word is IMMEDIATE (needs to run during compilation) or normal.</p>

<p>If that sounds simple, remember the frequency. Every token the REPL reads. Every word in every <code class="language-plaintext highlighter-rouge">.fth</code> file loaded at boot. Every reference inside a colon definition as it’s being compiled. FIND runs thousands of times before you see the first prompt.</p>

<p>That’s why it’s an obvious target for optimization. It’s also why it’s a trap: optimizing it changes the shape of the dictionary, and the dictionary is touched by almost everything.</p>

<h2 id="two-worlds-interpret-mode-and-compile-mode">Two Worlds: Interpret Mode and Compile Mode</h2>

<p>Before getting into FIND’s internals, the crucial split: <strong>FIND gets called from two different contexts, and they use the result differently.</strong></p>

<p>In <strong>interpret mode</strong>, the outer loop (<code class="language-plaintext highlighter-rouge">QUIT</code> in classic Forth) is doing the hot thing you expect:</p>

<ol>
  <li>Read a token with <code class="language-plaintext highlighter-rouge">WORD</code>.</li>
  <li>Call <code class="language-plaintext highlighter-rouge">FIND</code> on it.</li>
  <li>If found, <code class="language-plaintext highlighter-rouge">EXECUTE</code> the CFA — the word runs <em>now</em>.</li>
  <li>If not found, try <code class="language-plaintext highlighter-rouge">NUMBER</code> — if it parses, push the value.</li>
  <li>If neither, fail.</li>
</ol>

<p>In <strong>compile mode</strong> (triggered by <code class="language-plaintext highlighter-rouge">:</code> which flips <code class="language-plaintext highlighter-rouge">STATE</code> to non-zero), the outer loop does something subtly different:</p>

<ol>
  <li>Read a token with <code class="language-plaintext highlighter-rouge">WORD</code>.</li>
  <li>Call <code class="language-plaintext highlighter-rouge">FIND</code> on it.</li>
  <li>If found, check the IMMEDIATE flag.
    <ul>
      <li>IMMEDIATE words execute <em>now</em> (e.g. <code class="language-plaintext highlighter-rouge">;</code>, <code class="language-plaintext highlighter-rouge">IF</code>, <code class="language-plaintext highlighter-rouge">THEN</code>).</li>
      <li>Normal words get <strong>compiled</strong> — their CFA is written into the growing definition, not executed.</li>
    </ul>
  </li>
  <li>If not found, try <code class="language-plaintext highlighter-rouge">NUMBER</code> — compile as a literal.</li>
</ol>

<p>The key point: <strong>compilation doesn’t skip FIND.</strong> In fact compilation calls FIND <em>more</em> — once per token in the source definition. Optimizing lookup helps compile-time as much as it helps interactive REPL use. And on a bootstrapping Forth, almost everything is compile-time, because loading <code class="language-plaintext highlighter-rouge">.fth</code> files is one long colon-definition parade.</p>

<h2 id="walkthrough-5-dup--in-interpret-mode">Walkthrough: <code class="language-plaintext highlighter-rouge">5 DUP .</code> in Interpret Mode</h2>

<p>Three tokens, three different shapes of work:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token   FIND result          NUMBER   Action
-----   ------------------   ------   --------------------------
5       not found            → 5      push 5
DUP     found, not immediate  —       execute DUP (duplicates TOS)
.       found, not immediate  —       execute . (pops and prints)
</code></pre></div></div>

<p>Stack evolution, with <code class="language-plaintext highlighter-rouge">|</code> separating layers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Start:                 (empty)
After 5:               5
After DUP:             5 5
After .:               5           (prints "5")
</code></pre></div></div>

<p>The outer loop runs FIND three times. Twice it hits. Once it misses and falls through to NUMBER. Every token is an independent lookup.</p>

<h2 id="walkthrough--square-dup---in-compile-mode">Walkthrough: <code class="language-plaintext highlighter-rouge">: SQUARE DUP * ;</code> in Compile Mode</h2>

<p>Same shape of work, different consumer:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token    FIND result                    Action
------   ----------------------------   ------------------------
:        found, IMMEDIATE-like           run : (create header,
                                         flip STATE to compile)
SQUARE   (:  reads the next token as a
          name, not as a dictionary lookup — SQUARE is the new word)
DUP      found, not immediate            COMPILE DUP's CFA into body
*        found, not immediate            COMPILE *'s CFA into body
;        found, IMMEDIATE                run ; (compile EXIT, flip
                                         STATE back to interpret)
</code></pre></div></div>

<p>After this, <code class="language-plaintext highlighter-rouge">SQUARE</code>’s body in the dictionary looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT
</code></pre></div></div>

<p>When <code class="language-plaintext highlighter-rouge">5 SQUARE .</code> runs later, the interpreter pushes <code class="language-plaintext highlighter-rouge">5</code>, finds <code class="language-plaintext highlighter-rouge">SQUARE</code>, executes it. Inside <code class="language-plaintext highlighter-rouge">SQUARE</code>, the inner loop (<code class="language-plaintext highlighter-rouge">NEXT</code>) walks the compiled CFAs one at a time. <strong>No FIND happens at runtime.</strong> The names <code class="language-plaintext highlighter-rouge">DUP</code> and <code class="language-plaintext highlighter-rouge">*</code> were resolved once, at compile time, and baked into the body as direct CFA references.</p>

<p>This is the core insight that drives all the lookup-optimization decisions: <strong>FIND’s cost is concentrated in the text-reading phase, not in running compiled code.</strong> Once a definition is compiled, the cost is gone forever.</p>

<h2 id="the-all-asm-find-linear-walk">The All-Asm FIND: Linear Walk</h2>

<p>The reference phase-1 kernel does FIND the obvious way — walk the LATEST chain backwards, compare names, return the first hit. A Forth dictionary entry header looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>link (3B)   → flags|namelen (1B)   → name (N bytes)   → CFA
</code></pre></div></div>

<p>Walking is simple:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cursor = LATEST
while cursor != 0:
    length = (cursor + 3) &amp; 0x1F     ; strip flags
    if length == target_length and
       memcmp(cursor + 4, target_name, length) == 0:
        return cursor + 4 + length   ; CFA
    cursor = *cursor                  ; next link
return 0  ; not found
</code></pre></div></div>

<p>On a dictionary of ~50 words, average hit is ~25 comparisons. Each comparison is a length check plus a short string compare. Fast enough for one-off tokens. Painful when you’re loading <code class="language-plaintext highlighter-rouge">highlevel.fth</code> and every line has six tokens that each need resolution.</p>

<p>Bootstrap timing on the web demo showed this clearly: the all-asm kernel would spend a full second or more just parsing its own Forth core on a slow laptop. Visible lag. Time to speed it up.</p>

<h2 id="the-phase-2-optimization-2-round-xmx-hash">The Phase 2 Optimization: 2-Round XMX Hash</h2>

<p>The first real optimization added a hash table. Three pieces working together:</p>

<p><strong>1. A hash function.</strong> 2-Round XMX, chosen because it fits COR24’s 24-bit native arithmetic:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = 0
for each byte c of the name:
    h = h XOR c
    h = h * 0xDEADB5        ; 24-bit multiply, naturally truncates
    h = h XOR (h SRL 12)    ; avalanche: fold high half into low half
bucket = h AND 0xFF
</code></pre></div></div>

<p>Each step is cheap on COR24:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">XOR</code> and <code class="language-plaintext highlighter-rouge">SRL</code> are single cycles.</li>
  <li>The <code class="language-plaintext highlighter-rouge">* 0xDEADB5</code> is a 24-bit multiply whose overflow is discarded for free (it’s the word size).</li>
  <li><code class="language-plaintext highlighter-rouge">AND 0xFF</code> is a byte mask.</li>
</ul>

<p>The shift-by-12 is doing the real work. XMX (<em>xor-multiply-xor</em>) hashes need an avalanche step so that 1–3 character names like <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">DUP</code> produce well-distributed bucket indices. Without the shift, short names cluster badly. With it, short names are indistinguishable from long ones, from the hash’s point of view.</p>

<p><strong>2. A hash table.</strong> 256 buckets, 3 bytes each, 768 bytes total. Each bucket holds a pointer to the most-recent dictionary entry whose name hashes into that bucket.</p>

<p><strong>3. A per-entry hash chain.</strong> Every dictionary entry grew a <em>second</em> link, independent of the chronological <code class="language-plaintext highlighter-rouge">link</code> field. This one points to the next-older entry in the same hash bucket.</p>

<p>Lookup becomes:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = xmx(name)
bucket = h AND 0xFF
cursor = hashtab[bucket]
while cursor != 0:
    if name matches entry:
        return cursor.cfa
    cursor = cursor.hash_link   ; NOT the LATEST link
return 0
</code></pre></div></div>

<p>On the real dictionary, this cut linear-scan collisions from 47 (average walk depth) down to 11–15 per bucket. Measurably faster. Boot dropped from ~2 seconds to under one. The hashing work document in the repo profiles the distribution in detail.</p>

<h2 id="the-phase-2-optimization-part-2-lookaside-cache">The Phase 2 Optimization, Part 2: Lookaside Cache</h2>

<p>Even with hashing, one pattern remained: <code class="language-plaintext highlighter-rouge">DUP DUP DROP</code>, <code class="language-plaintext highlighter-rouge">SWAP OVER +</code>, the same few names repeated in tight succession during source loading. For those, even one hash-plus-bucket-walk is waste.</p>

<p>The cache is a 1-entry lookaside holding the most recent successful lookup as a tuple:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(hash, cfa, flags)
</code></pre></div></div>

<p>The memento is keyed by the full XMX hash, not by the name — hashing is already cheap, and comparing a 24-bit value is one instruction. On a hit, the lookup short-circuits before even dereferencing the hash table.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = xmx(name)
if h == cache.hash:
    return (cache.cfa, cache.flags)
; otherwise normal hash lookup, then update cache on success
</code></pre></div></div>

<p>One entry is enough because Forth source has strong temporal locality — when you see <code class="language-plaintext highlighter-rouge">DUP</code> once, you’ll see it again in the next few tokens more often than not. The cache doesn’t pretend to solve the general working-set problem; it just skips the table lookup on the very-hottest path.</p>

<h2 id="why-phase-4-is-dropping-all-of-it">Why Phase 4 Is Dropping All of It</h2>

<p>Phase 4 (<code class="language-plaintext highlighter-rouge">forth-from-forth</code>) builds the kernel via a Forth-hosted cross-compiler. One of its design goals: no hand-written <code class="language-plaintext highlighter-rouge">.s</code> file in the source tree. Everything comes from Forth.</p>

<p>That means FIND itself moves to Forth. The high-level FIND in <code class="language-plaintext highlighter-rouge">highlevel.fth</code> already exists — it’s a plain linear walk. No hash. No lookaside cache. No second link in dictionary entries.</p>

<p>Against that, the hashed FIND looks like dead weight:</p>

<table>
  <thead>
    <tr>
      <th>Cost</th>
      <th>What you’d need to keep</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>768 bytes of bucket table</td>
      <td>Boot-time setup code to zero and populate it</td>
    </tr>
    <tr>
      <td>~40 instructions of <code class="language-plaintext highlighter-rouge">compute_hash</code></td>
      <td>Re-implement in Forth (or keep in asm, which contradicts the goal)</td>
    </tr>
    <tr>
      <td>Per-entry hash link</td>
      <td>Extra 3 bytes on every dictionary entry</td>
    </tr>
    <tr>
      <td>CREATE-time splice</td>
      <td>Every new word has to hash itself and thread into the right bucket</td>
    </tr>
    <tr>
      <td>Boot population</td>
      <td>Run compute_hash on every existing word at startup</td>
    </tr>
  </tbody>
</table>

<p>The only customer of all this mechanism was the asm <code class="language-plaintext highlighter-rouge">do_find</code>. Phase 4 deletes <code class="language-plaintext highlighter-rouge">do_find</code>. The customer is gone.</p>

<p>And boot <em>already</em> fits under a second without the hash, because the web demo’s adaptive pump-loop optimization (<a href="https://github.com/sw-embed/web-sw-cor24-forth">commit f757800 in web-sw-cor24-forth</a>) did the work from a different direction. The hash was solving a visible problem; once the pump-loop fixed the problem, the hash was solving nothing.</p>

<h2 id="the-staged-optimization-lesson">The Staged-Optimization Lesson</h2>

<p>The phase 4 plan doesn’t say “drop hashing and never look back.” It says this:</p>

<blockquote>
  <p>Fallback order if “no hash” turns out wrong: linear walk → first-char hash (one-instruction, 2–3× speedup, 47 collisions) → 2-Round XMX (proven winner, 15 collisions, ~10 instructions/char). Don’t jump straight to XMX on speculation.</p>
</blockquote>

<p>Three graded fallbacks, in order of complexity added. Each one you only reach for if the previous one proved insufficient under measurement. The XMX hash is fine engineering — it’s well-matched to COR24, the avalanche step is well-chosen, the collision count is low. But it’s also a <strong>complete subsystem</strong>: function + table + per-entry link + CREATE-time maintenance + boot-time initialization.</p>

<p>The instinct that led to adding it in phase 2 — <em>FIND is in the hot path, therefore FIND should be hashed</em> — was correct for that phase. The instinct that says <em>keep what works</em> is wrong for phase 4, because the customer is leaving. The right instinct is: <strong>when the caller changes, re-evaluate the callee</strong>.</p>

<p>Said another way: the XMX hash was the right answer to the question <em>“how do we make the asm do_find faster?”</em>. It is not automatically the right answer to <em>“how do we make high-level Forth FIND faster?”</em> — because the question has a different shape now. FIND is in Forth. The dictionary is walked by Forth. Anything you add, you add to Forth. And linear walks through a reverse-chronological list are Forth’s natural shape.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Phase 4 ships without the hash. But that doesn’t mean nothing gets optimized — it means the <em>kinds</em> of optimizations change. Numeric fast paths. Recent-hit caches. Hot-token caches. Instrumentation to know which words actually matter. That’s the subject of the next post in this rabbit hole: <em>Rabbit-hole (3/?): FORTH — Life After Hashing</em>.</p>

<hr />

<p><em>If you’ve been reading this on the <a href="https://sw-embed.github.io/web-sw-cor24-forth/">web demo</a>, switch to the <strong>forth-on-forthish</strong> tab and type <code class="language-plaintext highlighter-rouge">WORDS</code> — every word printed there was looked up during boot.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="dictionary" /><category term="find" /><category term="hashing" /><category term="xmx" /><category term="lookaside-cache" /><category term="interpret-mode" /><category term="compile-mode" /><category term="self-hosting" /><summary type="html"><![CDATA[First FORTH post in this rabbit hole — more are planned. The self-hosting spectrum post laid out four phases (all-asm kernel → forth-in-forth → forth-on-forthish → forth-from-forth) and caught the project mid-stride: phase 2 shipped, phase 3 with its first subsets just landing, phase 4 still on the horizon. Since then, phase 3 has completed — forth-on-forthish finished subsets through 21, pulling WORD, FIND, NUMBER, INTERPRET, and QUIT into Forth and leaving ~22 irreducible assembly primitives. That moves the project’s active edge onto phase 4, forth-from-forth — a Forth-hosted cross-compiler that emits the kernel’s .s file as a build artifact. Working toward it surfaced a set of design questions that don’t fit in a single post: how lookup really works, what to optimize once the asm do_find goes away, how to build specialized images, whether to structure the system as a composer, how to retarget the codegen. Each of those is a rabbit hole of its own, and this post is the first. This post goes one level deeper on the single operation that gets executed more than any other during bootstrap, source loading, and the REPL: dictionary lookup. Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Web Demo Repo sw-embed/web-sw-cor24-forth WASM Side-Experiment sw-vibe-coding/sw-fth-wasm Prior Post Embedded (3/?): Self-Hosting Spectrum Comments Discord What FIND Does FIND is the operation that turns a name into something you can execute. Given a word like DUP, it walks the dictionary and returns the address of the entry — the code field address, or CFA — along with whether the word is IMMEDIATE (needs to run during compilation) or normal. If that sounds simple, remember the frequency. Every token the REPL reads. Every word in every .fth file loaded at boot. Every reference inside a colon definition as it’s being compiled. FIND runs thousands of times before you see the first prompt. That’s why it’s an obvious target for optimization. It’s also why it’s a trap: optimizing it changes the shape of the dictionary, and the dictionary is touched by almost everything. Two Worlds: Interpret Mode and Compile Mode Before getting into FIND’s internals, the crucial split: FIND gets called from two different contexts, and they use the result differently. In interpret mode, the outer loop (QUIT in classic Forth) is doing the hot thing you expect: Read a token with WORD. Call FIND on it. If found, EXECUTE the CFA — the word runs now. If not found, try NUMBER — if it parses, push the value. If neither, fail. In compile mode (triggered by : which flips STATE to non-zero), the outer loop does something subtly different: Read a token with WORD. Call FIND on it. If found, check the IMMEDIATE flag. IMMEDIATE words execute now (e.g. ;, IF, THEN). Normal words get compiled — their CFA is written into the growing definition, not executed. If not found, try NUMBER — compile as a literal. The key point: compilation doesn’t skip FIND. In fact compilation calls FIND more — once per token in the source definition. Optimizing lookup helps compile-time as much as it helps interactive REPL use. And on a bootstrapping Forth, almost everything is compile-time, because loading .fth files is one long colon-definition parade. Walkthrough: 5 DUP . in Interpret Mode Three tokens, three different shapes of work: Token FIND result NUMBER Action ----- ------------------ ------ -------------------------- 5 not found → 5 push 5 DUP found, not immediate — execute DUP (duplicates TOS) . found, not immediate — execute . (pops and prints) Stack evolution, with | separating layers: Start: (empty) After 5: 5 After DUP: 5 5 After .: 5 (prints "5") The outer loop runs FIND three times. Twice it hits. Once it misses and falls through to NUMBER. Every token is an independent lookup. Walkthrough: : SQUARE DUP * ; in Compile Mode Same shape of work, different consumer: Token FIND result Action ------ ---------------------------- ------------------------ : found, IMMEDIATE-like run : (create header, flip STATE to compile) SQUARE (: reads the next token as a name, not as a dictionary lookup — SQUARE is the new word) DUP found, not immediate COMPILE DUP's CFA into body * found, not immediate COMPILE *'s CFA into body ; found, IMMEDIATE run ; (compile EXIT, flip STATE back to interpret) After this, SQUARE’s body in the dictionary looks like: DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT When 5 SQUARE . runs later, the interpreter pushes 5, finds SQUARE, executes it. Inside SQUARE, the inner loop (NEXT) walks the compiled CFAs one at a time. No FIND happens at runtime. The names DUP and * were resolved once, at compile time, and baked into the body as direct CFA references. This is the core insight that drives all the lookup-optimization decisions: FIND’s cost is concentrated in the text-reading phase, not in running compiled code. Once a definition is compiled, the cost is gone forever. The All-Asm FIND: Linear Walk The reference phase-1 kernel does FIND the obvious way — walk the LATEST chain backwards, compare names, return the first hit. A Forth dictionary entry header looks like: link (3B) → flags|namelen (1B) → name (N bytes) → CFA Walking is simple: cursor = LATEST while cursor != 0: length = (cursor + 3) &amp; 0x1F ; strip flags if length == target_length and memcmp(cursor + 4, target_name, length) == 0: return cursor + 4 + length ; CFA cursor = *cursor ; next link return 0 ; not found On a dictionary of ~50 words, average hit is ~25 comparisons. Each comparison is a length check plus a short string compare. Fast enough for one-off tokens. Painful when you’re loading highlevel.fth and every line has six tokens that each need resolution. Bootstrap timing on the web demo showed this clearly: the all-asm kernel would spend a full second or more just parsing its own Forth core on a slow laptop. Visible lag. Time to speed it up. The Phase 2 Optimization: 2-Round XMX Hash The first real optimization added a hash table. Three pieces working together: 1. A hash function. 2-Round XMX, chosen because it fits COR24’s 24-bit native arithmetic: h = 0 for each byte c of the name: h = h XOR c h = h * 0xDEADB5 ; 24-bit multiply, naturally truncates h = h XOR (h SRL 12) ; avalanche: fold high half into low half bucket = h AND 0xFF Each step is cheap on COR24: XOR and SRL are single cycles. The * 0xDEADB5 is a 24-bit multiply whose overflow is discarded for free (it’s the word size). AND 0xFF is a byte mask. The shift-by-12 is doing the real work. XMX (xor-multiply-xor) hashes need an avalanche step so that 1–3 character names like @, !, DUP produce well-distributed bucket indices. Without the shift, short names cluster badly. With it, short names are indistinguishable from long ones, from the hash’s point of view. 2. A hash table. 256 buckets, 3 bytes each, 768 bytes total. Each bucket holds a pointer to the most-recent dictionary entry whose name hashes into that bucket. 3. A per-entry hash chain. Every dictionary entry grew a second link, independent of the chronological link field. This one points to the next-older entry in the same hash bucket. Lookup becomes: h = xmx(name) bucket = h AND 0xFF cursor = hashtab[bucket] while cursor != 0: if name matches entry: return cursor.cfa cursor = cursor.hash_link ; NOT the LATEST link return 0 On the real dictionary, this cut linear-scan collisions from 47 (average walk depth) down to 11–15 per bucket. Measurably faster. Boot dropped from ~2 seconds to under one. The hashing work document in the repo profiles the distribution in detail. The Phase 2 Optimization, Part 2: Lookaside Cache Even with hashing, one pattern remained: DUP DUP DROP, SWAP OVER +, the same few names repeated in tight succession during source loading. For those, even one hash-plus-bucket-walk is waste. The cache is a 1-entry lookaside holding the most recent successful lookup as a tuple: (hash, cfa, flags) The memento is keyed by the full XMX hash, not by the name — hashing is already cheap, and comparing a 24-bit value is one instruction. On a hit, the lookup short-circuits before even dereferencing the hash table. h = xmx(name) if h == cache.hash: return (cache.cfa, cache.flags) ; otherwise normal hash lookup, then update cache on success One entry is enough because Forth source has strong temporal locality — when you see DUP once, you’ll see it again in the next few tokens more often than not. The cache doesn’t pretend to solve the general working-set problem; it just skips the table lookup on the very-hottest path. Why Phase 4 Is Dropping All of It Phase 4 (forth-from-forth) builds the kernel via a Forth-hosted cross-compiler. One of its design goals: no hand-written .s file in the source tree. Everything comes from Forth. That means FIND itself moves to Forth. The high-level FIND in highlevel.fth already exists — it’s a plain linear walk. No hash. No lookaside cache. No second link in dictionary entries. Against that, the hashed FIND looks like dead weight: Cost What you’d need to keep 768 bytes of bucket table Boot-time setup code to zero and populate it ~40 instructions of compute_hash Re-implement in Forth (or keep in asm, which contradicts the goal) Per-entry hash link Extra 3 bytes on every dictionary entry CREATE-time splice Every new word has to hash itself and thread into the right bucket Boot population Run compute_hash on every existing word at startup The only customer of all this mechanism was the asm do_find. Phase 4 deletes do_find. The customer is gone. And boot already fits under a second without the hash, because the web demo’s adaptive pump-loop optimization (commit f757800 in web-sw-cor24-forth) did the work from a different direction. The hash was solving a visible problem; once the pump-loop fixed the problem, the hash was solving nothing. The Staged-Optimization Lesson The phase 4 plan doesn’t say “drop hashing and never look back.” It says this: Fallback order if “no hash” turns out wrong: linear walk → first-char hash (one-instruction, 2–3× speedup, 47 collisions) → 2-Round XMX (proven winner, 15 collisions, ~10 instructions/char). Don’t jump straight to XMX on speculation. Three graded fallbacks, in order of complexity added. Each one you only reach for if the previous one proved insufficient under measurement. The XMX hash is fine engineering — it’s well-matched to COR24, the avalanche step is well-chosen, the collision count is low. But it’s also a complete subsystem: function + table + per-entry link + CREATE-time maintenance + boot-time initialization. The instinct that led to adding it in phase 2 — FIND is in the hot path, therefore FIND should be hashed — was correct for that phase. The instinct that says keep what works is wrong for phase 4, because the customer is leaving. The right instinct is: when the caller changes, re-evaluate the callee. Said another way: the XMX hash was the right answer to the question “how do we make the asm do_find faster?”. It is not automatically the right answer to “how do we make high-level Forth FIND faster?” — because the question has a different shape now. FIND is in Forth. The dictionary is walked by Forth. Anything you add, you add to Forth. And linear walks through a reverse-chronological list are Forth’s natural shape. What’s Next Phase 4 ships without the hash. But that doesn’t mean nothing gets optimized — it means the kinds of optimizations change. Numeric fast paths. Recent-hit caches. Hot-token caches. Instrumentation to know which words actually matter. That’s the subject of the next post in this rabbit hole: Rabbit-hole (3/?): FORTH — Life After Hashing. If you’ve been reading this on the web demo, switch to the forth-on-forthish tab and type WORDS — every word printed there was looked up during boot.]]></summary></entry></feed>