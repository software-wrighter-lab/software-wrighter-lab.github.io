<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://software-wrighter-lab.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://software-wrighter-lab.github.io/" rel="alternate" type="text/html" /><updated>2026-03-19T10:14:35-07:00</updated><id>https://software-wrighter-lab.github.io/feed.xml</id><title type="html">Software Wrighter Lab Blog</title><subtitle>AI coding agents, systems programming, and practical machine learning</subtitle><author><name>Mike Wright</name></author><entry><title type="html">TBT (7/?): reg-rs - Regression Testing from C++ to Java to Rust</title><link href="https://software-wrighter-lab.github.io/2026/03/19/tbt-reg-rs-regression-testing/" rel="alternate" type="text/html" title="TBT (7/?): reg-rs - Regression Testing from C++ to Java to Rust" /><published>2026-03-19T01:15:00-07:00</published><updated>2026-03-19T01:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/19/tbt-reg-rs-regression-testing</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/19/tbt-reg-rs-regression-testing/"><![CDATA[<p><img src="/assets/images/posts/block-checks-and-x.png" class="post-marker no-invert" alt="" /></p>

<p>You ship a fix. Tests pass. Three weeks later a customer reports that a flag you didn’t touch now produces different output. Nothing in your test suite catches it because your tests check behavior, not output. What you needed was a regression test—a snapshot of what the command <em>actually produced</em>, compared against what it produces now.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/watch?v=CC1GvHk5hL4">reg-rs: Snapshot Regression Testing</a><br /><a href="https://www.youtube.com/watch?v=CC1GvHk5hL4"><img src="https://img.youtube.com/vi/CC1GvHk5hL4/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Repo</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs">sw-cli-tools/reg-rs</a></td>
      </tr>
      <tr>
        <td><strong>Motivation</strong></td>
        <td><a href="#motivation">Why I Built This</a></td>
      </tr>
      <tr>
        <td><strong>References</strong></td>
        <td><a href="#historical-context">Historical Context</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<div style="clear: both;"></div>

<h2 id="what-is-reg-rs">What is reg-rs?</h2>

<p>reg-rs (regress) is a CLI tool that captures the output of shell commands—stdout, stderr, and exit code—as “golden” baselines, then re-runs those commands and compares the results to detect regressions. Think of it as snapshot testing for any command-line tool.</p>

<h2 id="quick-start-with-aliases">Quick Start with Aliases</h2>

<p>reg-rs ships with shell aliases that make the workflow fast. Source them once (or add to your <code class="language-plaintext highlighter-rouge">.zshrc</code>/<code class="language-plaintext highlighter-rouge">.bashrc</code>):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> /path/to/reg-rs/bin/source-rg.sh
</code></pre></div></div>

<p>Then the full create-run-check-update cycle looks like this:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Create the thing you're testing</span>
<span class="nb">echo</span> <span class="s1">'echo "Hello, World!"'</span> <span class="o">&gt;</span> greet.sh

<span class="c"># 2. Create a regression test — captures the output as the baseline</span>
adrg greet <span class="s1">'bash greet.sh'</span>

<span class="c"># 3. Run the test — compares current output against the saved baseline</span>
rnrg greet
<span class="c">#   PASS</span>

<span class="c"># 4. See the results</span>
lsrg greet
<span class="c">#   PASS   greet   bash greet.sh</span>

<span class="c"># 5. Now change greet.sh (simulate a code change)</span>
<span class="nb">echo</span> <span class="s1">'echo "Hey there!"'</span> <span class="o">&gt;</span> greet.sh

<span class="c"># 6. Run the test again</span>
rnrg greet
<span class="c">#   FAIL</span>

<span class="c"># 7. See what changed</span>
shrg greet <span class="nt">-vv</span>
<span class="c">#   baseline: "Hello, World!"</span>
<span class="c">#   latest:   "Hey there!"</span>
<span class="c">#   diff:     - Hello, World!</span>
<span class="c">#             + Hey there!</span>

<span class="c"># 8. You decide this change is intentional — accept the new output</span>
uprg greet

<span class="c"># 9. Run again — passes with the new baseline</span>
rnrg greet
<span class="c">#   PASS</span>
</code></pre></div></div>

<p>The aliases: <code class="language-plaintext highlighter-rouge">adrg</code> (add), <code class="language-plaintext highlighter-rouge">rnrg</code> (run), <code class="language-plaintext highlighter-rouge">lsrg</code> (list), <code class="language-plaintext highlighter-rouge">shrg</code> (show), <code class="language-plaintext highlighter-rouge">uprg</code> (update/rebase), <code class="language-plaintext highlighter-rouge">rmrg</code> (remove), <code class="language-plaintext highlighter-rouge">rsrg</code> (reset), <code class="language-plaintext highlighter-rouge">strg</code> (status server), <code class="language-plaintext highlighter-rouge">hlrg</code> (help). Tab completion is included.</p>

<p>Or use the full commands: <code class="language-plaintext highlighter-rouge">reg-rs create</code>, <code class="language-plaintext highlighter-rouge">reg-rs run</code>, <code class="language-plaintext highlighter-rouge">reg-rs list</code>, <code class="language-plaintext highlighter-rouge">reg-rs show</code>, <code class="language-plaintext highlighter-rouge">reg-rs rebase</code>, etc.</p>

<h2 id="text-based-test-format-rgt">Text-Based Test Format (.rgt)</h2>

<p>A recent major change: tests are now stored as plain text files instead of binary SQLite databases. Each test has up to three files:</p>

<table>
  <thead>
    <tr>
      <th>File</th>
      <th>Purpose</th>
      <th style="text-align: center">Git-tracked?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.rgt</code></td>
      <td>TOML spec (command, timeout, metadata)</td>
      <td style="text-align: center">Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.out</code></td>
      <td>Expected stdout baseline</td>
      <td style="text-align: center">Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.err</code></td>
      <td>Expected stderr baseline (absent if empty)</td>
      <td style="text-align: center">Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.tdb</code></td>
      <td>Runtime cache (latest results, diffs)</td>
      <td style="text-align: center">No</td>
    </tr>
  </tbody>
</table>

<p>An <code class="language-plaintext highlighter-rouge">.rgt</code> file looks like:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">command</span> <span class="o">=</span><span class="w"> </span><span class="s">"git --version"</span>
<span class="n">timeout</span> <span class="o">=</span><span class="w"> </span><span class="mi">10</span>
<span class="n">exit_code</span> <span class="o">=</span><span class="w"> </span><span class="mi">0</span>
<span class="n">desc</span> <span class="o">=</span><span class="w"> </span><span class="s">"Version string format check"</span>
<span class="n">expects</span> <span class="o">=</span><span class="w"> </span><span class="s">"Prints version in semver format"</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">.out</code> file is just the golden output, plain text:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git version 2.44.0
</code></pre></div></div>

<p>This makes tests git-friendly—baselines show up in diffs, code reviews, and blame. The <code class="language-plaintext highlighter-rouge">.tdb</code> cache is gitignored; it only stores runtime results for reporting. If you have existing <code class="language-plaintext highlighter-rouge">.tdb</code> tests, <code class="language-plaintext highlighter-rouge">reg-rs migrate</code> (or <code class="language-plaintext highlighter-rouge">mgrg</code>) converts them to the new format.</p>

<h2 id="detecting-a-regression">Detecting a Regression</h2>

<p>Here’s a concrete example of reg-rs catching a version change:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Set up a baseline</span>
<span class="nb">echo</span> <span class="s1">'version 1.0.0'</span> <span class="o">&gt;</span> version.txt
adrg version_test <span class="s1">'cat version.txt'</span>

<span class="c"># Run it again---passes, output matches</span>
rnrg version_test
<span class="c"># PASS</span>

<span class="c"># Simulate a change</span>
<span class="nb">echo</span> <span class="s1">'version 2.0.0'</span> <span class="o">&gt;</span> version.txt

<span class="c"># Run again---regression detected</span>
rnrg version_test
<span class="c"># FAIL</span>

<span class="c"># See exactly what changed</span>
shrg version_test <span class="nt">-vv</span>
<span class="c"># stdout differences:</span>
<span class="c">#   - version 1.0.0</span>
<span class="c">#   + version 2.0.0</span>

<span class="c"># Intentional change? Accept the new baseline</span>
uprg version_test
</code></pre></div></div>

<h2 id="dogfooding-reg-rs-tests-itself">Dogfooding: reg-rs Tests Itself</h2>

<p>reg-rs uses itself to regression-test its own CLI. The test directory contains golden baselines for every subcommand’s help output. After any code change, <code class="language-plaintext highlighter-rouge">rnrg</code> checks that no help text, flag names, or usage strings changed accidentally. The demo scripts that exercise this workflow run automatically as part of <code class="language-plaintext highlighter-rouge">cargo test</code>.</p>

<h2 id="monitor-web-dashboard">Monitor: Web Dashboard</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reg-rs status <span class="nt">-p</span> <span class="nb">test</span>
<span class="c"># or: strg test</span>
</code></pre></div></div>

<p>This launches an Axum web server on port 4740 with a live dashboard. The landing page shows summary counts (pass/fail/pending) across all projects, updating in real time via Server-Sent Events (SSE)—no polling, no page refresh. The SSE stream sends JSON payloads and the client updates the DOM directly, so you see pass counts climb and pending counts drop as each test completes.</p>

<p>The detail view has collapsible sections for failures, passes, and pending tests. Failed tests show inline character-level diffs—expected baseline in green, actual output in yellow—so you can see exactly what changed and decide whether to investigate or rebase. A JSON API at <code class="language-plaintext highlighter-rouge">/api/status</code> is available for programmatic access.</p>

<div class="resource-box">

  <h3 id="motivation">Motivation</h3>

  <p>I’ve used regression testing tools for over 25 years, starting with regress at Forte Software in Oakland around 2000. The idea is simple and powerful: capture what a command produces, then verify it hasn’t changed. When I started learning Rust in 2020, I created a private implementation called rtt1. I’ve now forked and open-sourced it as reg-rs under an MIT license, with AI features already implemented: –describe generates test commands from natural language, and analyze triages failures using Claude. The <a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/prd.md">PRD</a> and <a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/subject-study.md">subject study</a> document the full roadmap and real-world testing patterns.</p>

</div>

<h2 id="the-throwback">The Throwback</h2>

<p>In 2000, I was working at Forte Software in Oakland, California. Forte had a C/C++-based regression testing tool called regress. The concept was straightforward: run a command, save the output, run it again later, diff the results. Simple, but it caught real bugs that unit tests missed—the kind where the output format changed, or an error message got reworded, or a flag silently started behaving differently.</p>

<p>Sun Microsystems acquired Forte, and since Sun was focused on Java, I wrote <strong>jregress</strong> over the next year—a clean-room implementation, not a port. It was partly a learning exercise, partly practical: the Java development teams and QA needed a regression tool that lived in their ecosystem, and writing it in Java meant I could maintain it myself and add features as QA requested them. Oracle acquired Sun in 2010, and as far as I know, jregress is still being maintained and used there today. There may have been an attempt to open-source it, but I haven’t found it online.</p>

<table>
  <thead>
    <tr>
      <th>Era</th>
      <th>Tool</th>
      <th>Language</th>
      <th>Context</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>2000</td>
      <td>regress</td>
      <td>C/C++</td>
      <td>Forte Software, Oakland</td>
    </tr>
    <tr>
      <td>~2001</td>
      <td>jregress</td>
      <td>Java</td>
      <td>Sun Microsystems (clean-room rewrite)</td>
    </tr>
    <tr>
      <td>2010+</td>
      <td>jregress</td>
      <td>Java</td>
      <td>Oracle (still maintained?)</td>
    </tr>
    <tr>
      <td>2020</td>
      <td>rtt1</td>
      <td>Rust</td>
      <td>Private learning project</td>
    </tr>
    <tr>
      <td>2026</td>
      <td>reg-rs</td>
      <td>Rust</td>
      <td>Open-sourced, MIT license</td>
    </tr>
  </tbody>
</table>

<p>The concept hasn’t changed in 25 years. What’s changed is the tooling around it: Rust gives you single-binary distribution, text-based <code class="language-plaintext highlighter-rouge">.rgt</code> files make tests git-friendly, clap gives you a polished CLI with shell aliases and tab completion, and Axum gives you a live monitoring dashboard with SSE. The next evolution is AI—using language models to generate test cases, explain regressions, and maintain baselines as code evolves.</p>

<h2 id="advanced-features">Advanced Features</h2>

<p>The basics—add, run, list/show—cover simple cases. Real CLI tools present harder challenges: non-deterministic output, interactive prompts, binary files, and slow test suites. reg-rs has features for all of these.</p>

<h3 id="taming-non-deterministic-output">Taming Non-Deterministic Output</h3>

<p>CLI output often contains timestamps, temp paths, PIDs, and version strings that change between runs. reg-rs provides two mechanisms to handle this.</p>

<p><strong><code class="language-plaintext highlighter-rouge">--preprocess</code> (<code class="language-plaintext highlighter-rouge">-P</code>)</strong>: Pipe stdout/stderr through a shell command before diffing:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Mask temp directory paths (macOS resolves /tmp to /private/var/...)</span>
adrg my_test <span class="s1">'my_tool run'</span> <span class="se">\</span>
  <span class="nt">-P</span> <span class="s2">"sed 's|/tmp/[^ ]*|&lt;TMPDIR&gt;|g; s|/private/var/[^ ]*|&lt;TMPDIR&gt;|g'"</span>
</code></pre></div></div>

<p><strong><code class="language-plaintext highlighter-rouge">--diff-mode</code> (<code class="language-plaintext highlighter-rouge">-M</code>)</strong>: Built-in normalization for common formats:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># JSON: sorts keys and pretty-prints before diffing</span>
adrg api_test <span class="s1">'curl -s localhost:8080/status'</span> <span class="nt">-M</span> json

<span class="c"># Lines-unordered: sorts lines before diffing</span>
adrg completions <span class="s1">'mytool complete commands'</span> <span class="nt">-M</span> lines-unordered
</code></pre></div></div>

<h3 id="command-timeouts">Command Timeouts</h3>

<p>Interactive CLIs that prompt for input will hang indefinitely in non-interactive shells. The <code class="language-plaintext highlighter-rouge">--timeout</code> flag (in seconds) makes them fail fast:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adrg pjmai_help <span class="s1">'pjmai-rs --help'</span> <span class="nt">--timeout</span> 10
</code></pre></div></div>

<h3 id="self-documenting-tests">Self-Documenting Tests</h3>

<p>Tests can carry their own documentation, stored in the <code class="language-plaintext highlighter-rouge">.rgt</code> file:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adrg pjmai_help <span class="s1">'pjmai-rs --help'</span> <span class="nt">--timeout</span> 10 <span class="se">\</span>
  <span class="nt">--desc</span> <span class="s2">"Verifies help text is stable"</span> <span class="se">\</span>
  <span class="nt">--expects</span> <span class="s2">"Standard clap help output"</span> <span class="se">\</span>
  <span class="nt">--flaky-note</span> <span class="s2">"None - deterministic"</span>
</code></pre></div></div>

<p>These metadata fields appear in failure reports at <code class="language-plaintext highlighter-rouge">-vv</code> verbosity and are consumed by the <code class="language-plaintext highlighter-rouge">analyze</code> subcommand for AI-powered triage.</p>

<h3 id="parallel-execution">Parallel Execution</h3>

<p>The <code class="language-plaintext highlighter-rouge">--parallel</code> flag runs all matching tests concurrently, one thread per test:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rnrg pjmai <span class="nt">--parallel</span>
</code></pre></div></div>

<p>Each test has its own independent files, so there are no concurrency conflicts.</p>

<h3 id="testing-binary-output">Testing Binary Output</h3>

<p>Not all CLI tools produce text. <a href="https://github.com/sw-cli-tools/favicon">favicon</a> generates PNG and ICO images—binary output where line diffs are meaningless. The <a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/subject-study.md">subject study</a> documents approaches including SHA-256 checksums, base64 encoding, and hybrid strategies for visual comparison.</p>

<h2 id="ai-features">AI Features</h2>

<p>Several AI features are implemented (not just planned). All require <code class="language-plaintext highlighter-rouge">ANTHROPIC_API_KEY</code>.</p>

<h3 id="ai-powered-test-creation---describe">AI-Powered Test Creation (<code class="language-plaintext highlighter-rouge">--describe</code>)</h3>

<p>Describe what you want to test in natural language instead of writing the shell command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reg-rs create <span class="nt">-t</span> status <span class="nt">-D</span> <span class="s2">"show git status of current directory"</span>
<span class="c"># AI generates: git status</span>
</code></pre></div></div>

<p>Add <code class="language-plaintext highlighter-rouge">--context</code> (<code class="language-plaintext highlighter-rouge">-C</code>) to feed the AI existing help text for better command generation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reg-rs create <span class="nt">-t</span> pjmai_list <span class="se">\</span>
  <span class="nt">-D</span> <span class="s2">"test the list subcommand with no projects"</span> <span class="se">\</span>
  <span class="nt">-C</span> <span class="s2">"pjmai-rs --help"</span>
</code></pre></div></div>

<h3 id="ai-failure-analysis-analyze">AI Failure Analysis (<code class="language-plaintext highlighter-rouge">analyze</code>)</h3>

<p>When tests fail, the <code class="language-plaintext highlighter-rouge">analyze</code> subcommand sends the original output, latest output, and diff to Claude for triage:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reg-rs analyze <span class="nt">-p</span> my_failing_test
</code></pre></div></div>

<p>It classifies failures as true regressions, flaky tests, environmental changes, or stale baselines—helping you decide whether to investigate or rebase.</p>

<h2 id="getting-started">Getting Started</h2>

<p>Clone and build:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/sw-cli-tools/reg-rs.git
<span class="nb">cd </span>reg-rs
cargo build <span class="nt">--release</span>
</code></pre></div></div>

<p>Set up aliases:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source </span>bin/source-rg.sh
</code></pre></div></div>

<p>Create your first test:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adrg hello <span class="s1">'echo hello world'</span>
rnrg hello
lsrg hello
</code></pre></div></div>

<div class="references-section">

  <h2 id="references">References</h2>

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>reg-rs Repository</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs">github.com/sw-cli-tools/reg-rs</a></td>
      </tr>
      <tr>
        <td><strong>User Guide</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/user-guide.md">docs/user-guide.md</a></td>
      </tr>
      <tr>
        <td><strong>Subject Study</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/subject-study.md">Testing CLI tools with reg-rs</a></td>
      </tr>
      <tr>
        <td><strong>PRD</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs/blob/main/docs/prd.md">Product Requirements</a></td>
      </tr>
    </tbody>
  </table>

  <h3 id="historical-context">Historical Context</h3>

  <table>
    <thead>
      <tr>
        <th>Era</th>
        <th>Resource</th>
        <th>Notes</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>2000</td>
        <td><strong>Forte Software / Sun</strong></td>
        <td>regress was an internal C/C++ regression testing tool</td>
      </tr>
      <tr>
        <td>2000s</td>
        <td><strong>jregress</strong></td>
        <td>Clean-room Java implementation at Sun Microsystems</td>
      </tr>
      <tr>
        <td>2010</td>
        <td><strong>Oracle acquires Sun</strong></td>
        <td>jregress continues in use internally</td>
      </tr>
      <tr>
        <td>2020</td>
        <td><strong>rtt1</strong></td>
        <td>Private Rust implementation, learning project</td>
      </tr>
      <tr>
        <td>2026</td>
        <td><strong>reg-rs</strong></td>
        <td>Open-sourced fork under MIT license</td>
      </tr>
    </tbody>
  </table>

</div>

<hr />

<p><em>The best test is the one that catches the change nobody expected. Regression testing has been doing that for decades—now with better tools.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="tbt" /><category term="cli-tools" /><category term="rust" /><category term="testing" /><category term="tbt" /><category term="regression-testing" /><category term="rust" /><category term="cli" /><category term="sqlite" /><category term="testing" /><category term="developer-tools" /><summary type="html"><![CDATA[You ship a fix. Tests pass. Three weeks later a customer reports that a flag you didn’t touch now produces different output. Nothing in your test suite catches it because your tests check behavior, not output. What you needed was a regression test—a snapshot of what the command actually produced, compared against what it produces now. Resource Link Video reg-rs: Snapshot Regression Testing Repo sw-cli-tools/reg-rs Motivation Why I Built This References Historical Context Comments Discord What is reg-rs? reg-rs (regress) is a CLI tool that captures the output of shell commands—stdout, stderr, and exit code—as “golden” baselines, then re-runs those commands and compares the results to detect regressions. Think of it as snapshot testing for any command-line tool. Quick Start with Aliases reg-rs ships with shell aliases that make the workflow fast. Source them once (or add to your .zshrc/.bashrc): source /path/to/reg-rs/bin/source-rg.sh Then the full create-run-check-update cycle looks like this: # 1. Create the thing you're testing echo 'echo "Hello, World!"' &gt; greet.sh # 2. Create a regression test — captures the output as the baseline adrg greet 'bash greet.sh' # 3. Run the test — compares current output against the saved baseline rnrg greet # PASS # 4. See the results lsrg greet # PASS greet bash greet.sh # 5. Now change greet.sh (simulate a code change) echo 'echo "Hey there!"' &gt; greet.sh # 6. Run the test again rnrg greet # FAIL # 7. See what changed shrg greet -vv # baseline: "Hello, World!" # latest: "Hey there!" # diff: - Hello, World! # + Hey there! # 8. You decide this change is intentional — accept the new output uprg greet # 9. Run again — passes with the new baseline rnrg greet # PASS The aliases: adrg (add), rnrg (run), lsrg (list), shrg (show), uprg (update/rebase), rmrg (remove), rsrg (reset), strg (status server), hlrg (help). Tab completion is included. Or use the full commands: reg-rs create, reg-rs run, reg-rs list, reg-rs show, reg-rs rebase, etc. Text-Based Test Format (.rgt) A recent major change: tests are now stored as plain text files instead of binary SQLite databases. Each test has up to three files: File Purpose Git-tracked? test.rgt TOML spec (command, timeout, metadata) Yes test.out Expected stdout baseline Yes test.err Expected stderr baseline (absent if empty) Yes test.tdb Runtime cache (latest results, diffs) No An .rgt file looks like: command = "git --version" timeout = 10 exit_code = 0 desc = "Version string format check" expects = "Prints version in semver format" The .out file is just the golden output, plain text: git version 2.44.0 This makes tests git-friendly—baselines show up in diffs, code reviews, and blame. The .tdb cache is gitignored; it only stores runtime results for reporting. If you have existing .tdb tests, reg-rs migrate (or mgrg) converts them to the new format. Detecting a Regression Here’s a concrete example of reg-rs catching a version change: # Set up a baseline echo 'version 1.0.0' &gt; version.txt adrg version_test 'cat version.txt' # Run it again---passes, output matches rnrg version_test # PASS # Simulate a change echo 'version 2.0.0' &gt; version.txt # Run again---regression detected rnrg version_test # FAIL # See exactly what changed shrg version_test -vv # stdout differences: # - version 1.0.0 # + version 2.0.0 # Intentional change? Accept the new baseline uprg version_test Dogfooding: reg-rs Tests Itself reg-rs uses itself to regression-test its own CLI. The test directory contains golden baselines for every subcommand’s help output. After any code change, rnrg checks that no help text, flag names, or usage strings changed accidentally. The demo scripts that exercise this workflow run automatically as part of cargo test. Monitor: Web Dashboard reg-rs status -p test # or: strg test This launches an Axum web server on port 4740 with a live dashboard. The landing page shows summary counts (pass/fail/pending) across all projects, updating in real time via Server-Sent Events (SSE)—no polling, no page refresh. The SSE stream sends JSON payloads and the client updates the DOM directly, so you see pass counts climb and pending counts drop as each test completes. The detail view has collapsible sections for failures, passes, and pending tests. Failed tests show inline character-level diffs—expected baseline in green, actual output in yellow—so you can see exactly what changed and decide whether to investigate or rebase. A JSON API at /api/status is available for programmatic access. Motivation I’ve used regression testing tools for over 25 years, starting with regress at Forte Software in Oakland around 2000. The idea is simple and powerful: capture what a command produces, then verify it hasn’t changed. When I started learning Rust in 2020, I created a private implementation called rtt1. I’ve now forked and open-sourced it as reg-rs under an MIT license, with AI features already implemented: –describe generates test commands from natural language, and analyze triages failures using Claude. The PRD and subject study document the full roadmap and real-world testing patterns. The Throwback In 2000, I was working at Forte Software in Oakland, California. Forte had a C/C++-based regression testing tool called regress. The concept was straightforward: run a command, save the output, run it again later, diff the results. Simple, but it caught real bugs that unit tests missed—the kind where the output format changed, or an error message got reworded, or a flag silently started behaving differently. Sun Microsystems acquired Forte, and since Sun was focused on Java, I wrote jregress over the next year—a clean-room implementation, not a port. It was partly a learning exercise, partly practical: the Java development teams and QA needed a regression tool that lived in their ecosystem, and writing it in Java meant I could maintain it myself and add features as QA requested them. Oracle acquired Sun in 2010, and as far as I know, jregress is still being maintained and used there today. There may have been an attempt to open-source it, but I haven’t found it online. Era Tool Language Context 2000 regress C/C++ Forte Software, Oakland ~2001 jregress Java Sun Microsystems (clean-room rewrite) 2010+ jregress Java Oracle (still maintained?) 2020 rtt1 Rust Private learning project 2026 reg-rs Rust Open-sourced, MIT license The concept hasn’t changed in 25 years. What’s changed is the tooling around it: Rust gives you single-binary distribution, text-based .rgt files make tests git-friendly, clap gives you a polished CLI with shell aliases and tab completion, and Axum gives you a live monitoring dashboard with SSE. The next evolution is AI—using language models to generate test cases, explain regressions, and maintain baselines as code evolves. Advanced Features The basics—add, run, list/show—cover simple cases. Real CLI tools present harder challenges: non-deterministic output, interactive prompts, binary files, and slow test suites. reg-rs has features for all of these. Taming Non-Deterministic Output CLI output often contains timestamps, temp paths, PIDs, and version strings that change between runs. reg-rs provides two mechanisms to handle this. --preprocess (-P): Pipe stdout/stderr through a shell command before diffing: # Mask temp directory paths (macOS resolves /tmp to /private/var/...) adrg my_test 'my_tool run' \ -P "sed 's|/tmp/[^ ]*|&lt;TMPDIR&gt;|g; s|/private/var/[^ ]*|&lt;TMPDIR&gt;|g'" --diff-mode (-M): Built-in normalization for common formats: # JSON: sorts keys and pretty-prints before diffing adrg api_test 'curl -s localhost:8080/status' -M json # Lines-unordered: sorts lines before diffing adrg completions 'mytool complete commands' -M lines-unordered Command Timeouts Interactive CLIs that prompt for input will hang indefinitely in non-interactive shells. The --timeout flag (in seconds) makes them fail fast: adrg pjmai_help 'pjmai-rs --help' --timeout 10 Self-Documenting Tests Tests can carry their own documentation, stored in the .rgt file: adrg pjmai_help 'pjmai-rs --help' --timeout 10 \ --desc "Verifies help text is stable" \ --expects "Standard clap help output" \ --flaky-note "None - deterministic" These metadata fields appear in failure reports at -vv verbosity and are consumed by the analyze subcommand for AI-powered triage. Parallel Execution The --parallel flag runs all matching tests concurrently, one thread per test: rnrg pjmai --parallel Each test has its own independent files, so there are no concurrency conflicts. Testing Binary Output Not all CLI tools produce text. favicon generates PNG and ICO images—binary output where line diffs are meaningless. The subject study documents approaches including SHA-256 checksums, base64 encoding, and hybrid strategies for visual comparison. AI Features Several AI features are implemented (not just planned). All require ANTHROPIC_API_KEY. AI-Powered Test Creation (--describe) Describe what you want to test in natural language instead of writing the shell command: reg-rs create -t status -D "show git status of current directory" # AI generates: git status Add --context (-C) to feed the AI existing help text for better command generation: reg-rs create -t pjmai_list \ -D "test the list subcommand with no projects" \ -C "pjmai-rs --help" AI Failure Analysis (analyze) When tests fail, the analyze subcommand sends the original output, latest output, and diff to Claude for triage: reg-rs analyze -p my_failing_test It classifies failures as true regressions, flaky tests, environmental changes, or stale baselines—helping you decide whether to investigate or rebase. Getting Started Clone and build: git clone https://github.com/sw-cli-tools/reg-rs.git cd reg-rs cargo build --release Set up aliases: source bin/source-rg.sh Create your first test: adrg hello 'echo hello world' rnrg hello lsrg hello References Resource Link reg-rs Repository github.com/sw-cli-tools/reg-rs User Guide docs/user-guide.md Subject Study Testing CLI tools with reg-rs PRD Product Requirements Historical Context Era Resource Notes 2000 Forte Software / Sun regress was an internal C/C++ regression testing tool 2000s jregress Clean-room Java implementation at Sun Microsystems 2010 Oracle acquires Sun jregress continues in use internally 2020 rtt1 Private Rust implementation, learning project 2026 reg-rs Open-sourced fork under MIT license The best test is the one that catches the change nobody expected. Regression testing has been doing that for decades—now with better tools.]]></summary></entry><entry><title type="html">AI Tools (1/?): XSkill — A Memory Layer for Multimodal Agents</title><link href="https://software-wrighter-lab.github.io/2026/03/17/ai-tools-xskill-memory-layer-multimodal-agents/" rel="alternate" type="text/html" title="AI Tools (1/?): XSkill — A Memory Layer for Multimodal Agents" /><published>2026-03-17T01:15:00-07:00</published><updated>2026-03-17T01:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/17/ai-tools-xskill-memory-layer-multimodal-agents</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/17/ai-tools-xskill-memory-layer-multimodal-agents/"><![CDATA[<p><img src="/assets/images/posts/block-x.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>Most AI agents can use tools. Far fewer can remember how to use them better next time. XSkill addresses that gap with a structured memory layer that accumulates know-how from past runs—without retraining the model.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Paper</strong></td>
        <td><a href="https://arxiv.org/abs/2603.12056">arXiv 2603.12056</a></td>
      </tr>
      <tr>
        <td><strong>Project</strong></td>
        <td><a href="https://xskill-agent.github.io/xskill_page/">XSkill Project Page</a></td>
      </tr>
      <tr>
        <td><strong>Code</strong></td>
        <td><a href="https://github.com/XSkill-Agent/XSkill">GitHub (MIT)</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/shorts/QelUyffoo1g">XSkill: Memory Layer</a><br /><a href="https://www.youtube.com/shorts/QelUyffoo1g"><img src="https://img.youtube.com/vi/QelUyffoo1g/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-problem-isolated-episodes">The Problem: Isolated Episodes</h2>

<p>Multimodal agents solve complex visual and tool-heavy tasks, but each run starts from scratch. An agent might figure out a multi-step workflow for extracting color data from an image—only to lose that knowledge when the next task begins. Useful lessons evaporate between sessions.</p>

<h2 id="two-kinds-of-memory">Two Kinds of Memory</h2>

<p>XSkill introduces a dual-memory architecture that separates strategic knowledge from tactical knowledge:</p>

<p><strong>Skills</strong> are structured Markdown documents containing workflows and tool templates for a class of tasks. A skill says: <em>here is the overall approach for this kind of problem.</em></p>

<p><strong>Experiences</strong> are smaller tactical lessons with triggering conditions, recommended actions, and failure notes. An experience says: <em>when this specific pattern appears, use this tactic instead of guessing.</em></p>

<p>That split matters. Ablation analysis shows that removing either type hurts performance—skills alone aren’t enough, and experiences alone aren’t enough.</p>

<h2 id="two-phase-framework">Two-Phase Framework</h2>

<p>The framework operates in a loop:</p>

<p><strong>Phase 1 — Accumulation.</strong> After completing rollout batches, the agent reviews past trajectories through visually-grounded summarization, cross-rollout critique, and hierarchical consolidation. This produces skill documents and experience items stored in persistent banks.</p>

<p><strong>Phase 2 — Inference.</strong> Given a new task, the agent decomposes it, retrieves relevant skills and experiences via semantic search, adapts them to the current visual context, and injects them into the system prompt.</p>

<p>The key claim: agents improve through memory accumulation and retrieval, not parameter updates. No fine-tuning required.</p>

<h2 id="results">Results</h2>

<p>Evaluated across five benchmarks spanning visual tool use (VisualToolBench, TIR-Bench), multimodal search (MMSearch-Plus, MMBrowseComp), and comprehensive agent tasks (AgentVista):</p>

<table>
  <thead>
    <tr>
      <th>Backbone</th>
      <th>Avg@4</th>
      <th>Pass@4</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Gemini-3-Flash + XSkill</td>
      <td>40.34</td>
      <td>58.95</td>
    </tr>
    <tr>
      <td>Gemini-2.5-Pro + XSkill</td>
      <td>28.63</td>
      <td>46.38</td>
    </tr>
    <tr>
      <td>o4-mini + XSkill</td>
      <td>23.72</td>
      <td>39.07</td>
    </tr>
    <tr>
      <td>GPT-4o-mini + XSkill</td>
      <td>23.19</td>
      <td>38.90</td>
    </tr>
  </tbody>
</table>

<p>Average gains of 2.6 to 6.7 points over baselines (Agent Workflow Memory, Dynamic CheatSheet, Agent-KB). Performance consistently improves as rollout count increases from 1 to 4.</p>

<p>Practical impact: syntax errors drop from 20.3% to 11.4% with skills, and tool name errors fall from 2.85% to 0.32%.</p>

<h2 id="concrete-example">Concrete Example</h2>

<p>A visual task requires identifying the color of a region behind specific text in an image. Without memory, the agent guesses. With XSkill, it retrieves a structured workflow: locate the text, isolate the region, sample pixels via code interpreter, and infer the color from actual data. Code interpreter usage increases from 66.6% to 77.0% on VisualToolBench—the agent learns to measure instead of guess.</p>

<h2 id="why-this-matters">Why This Matters</h2>

<p>XSkill sits at the intersection of agents, tools, multimodal reasoning, and continual improvement. The practical takeaway isn’t just that memory helps—it’s that <em>different kinds</em> of memory help in different ways. Strategic workflows and situational tactics serve complementary roles.</p>

<p>Not a bigger model. A smarter memory layer.</p>

<hr />

<h2 id="references">References</h2>

<table>
  <thead>
    <tr>
      <th>Reference</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>XSkill paper</td>
      <td><a href="https://arxiv.org/abs/2603.12056">arXiv 2603.12056</a></td>
    </tr>
    <tr>
      <td>Project page</td>
      <td><a href="https://xskill-agent.github.io/xskill_page/">xskill-agent.github.io</a></td>
    </tr>
    <tr>
      <td>GitHub repo (MIT)</td>
      <td><a href="https://github.com/XSkill-Agent/XSkill">XSkill-Agent/XSkill</a></td>
    </tr>
  </tbody>
</table>]]></content><author><name>Software Wrighter</name></author><category term="ai-agents" /><category term="research" /><category term="tools" /><category term="ai-tools" /><category term="xskill" /><category term="multimodal" /><category term="continual-learning" /><category term="memory" /><category term="tool-use" /><category term="agents" /><summary type="html"><![CDATA[Most AI agents can use tools. Far fewer can remember how to use them better next time. XSkill addresses that gap with a structured memory layer that accumulates know-how from past runs—without retraining the model. Resource Link Paper arXiv 2603.12056 Project XSkill Project Page Code GitHub (MIT) Video XSkill: Memory Layer Comments Discord The Problem: Isolated Episodes Multimodal agents solve complex visual and tool-heavy tasks, but each run starts from scratch. An agent might figure out a multi-step workflow for extracting color data from an image—only to lose that knowledge when the next task begins. Useful lessons evaporate between sessions. Two Kinds of Memory XSkill introduces a dual-memory architecture that separates strategic knowledge from tactical knowledge: Skills are structured Markdown documents containing workflows and tool templates for a class of tasks. A skill says: here is the overall approach for this kind of problem. Experiences are smaller tactical lessons with triggering conditions, recommended actions, and failure notes. An experience says: when this specific pattern appears, use this tactic instead of guessing. That split matters. Ablation analysis shows that removing either type hurts performance—skills alone aren’t enough, and experiences alone aren’t enough. Two-Phase Framework The framework operates in a loop: Phase 1 — Accumulation. After completing rollout batches, the agent reviews past trajectories through visually-grounded summarization, cross-rollout critique, and hierarchical consolidation. This produces skill documents and experience items stored in persistent banks. Phase 2 — Inference. Given a new task, the agent decomposes it, retrieves relevant skills and experiences via semantic search, adapts them to the current visual context, and injects them into the system prompt. The key claim: agents improve through memory accumulation and retrieval, not parameter updates. No fine-tuning required. Results Evaluated across five benchmarks spanning visual tool use (VisualToolBench, TIR-Bench), multimodal search (MMSearch-Plus, MMBrowseComp), and comprehensive agent tasks (AgentVista): Backbone Avg@4 Pass@4 Gemini-3-Flash + XSkill 40.34 58.95 Gemini-2.5-Pro + XSkill 28.63 46.38 o4-mini + XSkill 23.72 39.07 GPT-4o-mini + XSkill 23.19 38.90 Average gains of 2.6 to 6.7 points over baselines (Agent Workflow Memory, Dynamic CheatSheet, Agent-KB). Performance consistently improves as rollout count increases from 1 to 4. Practical impact: syntax errors drop from 20.3% to 11.4% with skills, and tool name errors fall from 2.85% to 0.32%. Concrete Example A visual task requires identifying the color of a region behind specific text in an image. Without memory, the agent guesses. With XSkill, it retrieves a structured workflow: locate the text, isolate the region, sample pixels via code interpreter, and infer the color from actual data. Code interpreter usage increases from 66.6% to 77.0% on VisualToolBench—the agent learns to measure instead of guess. Why This Matters XSkill sits at the intersection of agents, tools, multimodal reasoning, and continual improvement. The practical takeaway isn’t just that memory helps—it’s that different kinds of memory help in different ways. Strategic workflows and situational tactics serve complementary roles. Not a bigger model. A smarter memory layer. References Reference Link XSkill paper arXiv 2603.12056 Project page xskill-agent.github.io GitHub repo (MIT) XSkill-Agent/XSkill]]></summary></entry><entry><title type="html">Embedded (1/?): BMP280 Driver — From Prototype to Patent Proof-of-Concept</title><link href="https://software-wrighter-lab.github.io/2026/03/17/embedded-bmp280-pressure-sensor-driver/" rel="alternate" type="text/html" title="Embedded (1/?): BMP280 Driver — From Prototype to Patent Proof-of-Concept" /><published>2026-03-17T01:15:00-07:00</published><updated>2026-03-17T01:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/17/embedded-bmp280-pressure-sensor-driver</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/17/embedded-bmp280-pressure-sensor-driver/"><![CDATA[<p><img src="/assets/images/posts/block-bmp280.png" class="post-marker" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>A colleague had a patent application that needed empirical data: pressure measurements from multiple sensors at two physical locations, with enough redundancy to establish statistical confidence. Off-the-shelf solutions weren’t flexible enough, so we built the whole stack in Rust—from the sensor driver up through data collection, analysis, and plotting.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Source</strong></td>
        <td><a href="https://github.com/sw-embed/bmp280">GitHub</a></td>
      </tr>
      <tr>
        <td><strong>Docs</strong></td>
        <td><a href="https://github.com/sw-embed/bmp280/blob/master/docs/multiple-sensor-support.md">Multi-sensor guide</a></td>
      </tr>
      <tr>
        <td><strong>References</strong></td>
        <td><a href="#references">Datasheets, patent, hardware</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<hr />

<h2 id="the-problem">The Problem</h2>

<p>We needed high-resolution barometric pressure data from dozens of sensors split across two physical locations. Each location needed multiple sensors—not just for coverage, but because averaging across several readings reduces noise and gives more trustworthy measurements. A single BMP280 reading has enough jitter that you want redundancy.</p>

<p>The existing BMP280 driver crate worked fine for one sensor at the default address. We needed it to support both I2C addresses so we could put two sensors on each bus—and then use multiplexers to scale that to many.</p>

<hr />

<h2 id="why-fork-the-driver">Why Fork the Driver?</h2>

<p>The original <code class="language-plaintext highlighter-rouge">bmp280-ehal</code> crate is a platform-agnostic, <code class="language-plaintext highlighter-rouge">no_std</code> Rust driver built on <code class="language-plaintext highlighter-rouge">embedded-hal</code> traits. It runs on anything from bare-metal microcontrollers to Raspberry Pi. But it had a limitation: it assumed the default I2C address (<code class="language-plaintext highlighter-rouge">0x76</code>) and had no way to talk to a second sensor at the alternate address (<code class="language-plaintext highlighter-rouge">0x77</code>).</p>

<p>I needed the driver to address both <code class="language-plaintext highlighter-rouge">0x76</code> and <code class="language-plaintext highlighter-rouge">0x77</code>—two sensors per bus instead of one. That change alone doubled capacity, and multiplexers would scale it from there. So I forked the driver and made three changes:</p>

<h3 id="commit-1-refactor-constructor-0b66fda">Commit 1: Refactor constructor (<code class="language-plaintext highlighter-rouge">0b66fda</code>)</h3>

<p>Simplified the API by removing the implicit address tracking. The old constructor tied the driver to a single address at creation time. The refactored version makes <code class="language-plaintext highlighter-rouge">read_calibration()</code> public and removes the stored address, laying the groundwork for multi-address support.</p>

<h3 id="commit-2-per-sensor-calibration-d393bd6">Commit 2: Per-sensor calibration (<code class="language-plaintext highlighter-rouge">d393bd6</code>)</h3>

<p>The structural change that enables multi-sensor support. Instead of flat calibration fields on the driver struct, this commit introduces:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">TempComp</code></strong> and <strong><code class="language-plaintext highlighter-rouge">PressureComp</code></strong> structs for temperature and pressure compensation data</li>
  <li><strong><code class="language-plaintext highlighter-rouge">Sensors</code></strong> container holding <code class="language-plaintext highlighter-rouge">Option&lt;TempComp&gt;</code> and <code class="language-plaintext highlighter-rouge">Option&lt;PressureComp&gt;</code> for both addresses (<code class="language-plaintext highlighter-rouge">0x76</code> and <code class="language-plaintext highlighter-rouge">0x77</code>)</li>
  <li>Every reading method now takes an explicit address parameter</li>
</ul>

<p>Each BMP280 ships with unique factory calibration coefficients baked into its NVM. The driver reads 24 bytes of calibration data per sensor and stores it independently. This matters because the compensation polynomial uses 12 coefficients—get them wrong and your pressure reading is meaningless.</p>

<h3 id="commit-3-documentation-and-multi-sensor-guide-21f16dc">Commit 3: Documentation and multi-sensor guide (<code class="language-plaintext highlighter-rouge">21f16dc</code>)</h3>

<p>Updated the README and added comprehensive documentation in <code class="language-plaintext highlighter-rouge">docs/multiple-sensor-support.md</code> covering:</p>

<ul>
  <li>Two sensors on one bus (differential pressure)</li>
  <li>I2C multiplexer topologies (TCA9548A, PCA9546A, TCA9543A)</li>
  <li>Cascading mux trees for large sensor arrays</li>
  <li>I2C extenders for long-distance distribution</li>
</ul>

<hr />

<h2 id="the-hardware-setup">The Hardware Setup</h2>

<h3 id="doubling-capacity-with-dual-addresses">Doubling capacity with dual addresses</h3>

<p>The BMP280 supports two I2C addresses (<code class="language-plaintext highlighter-rouge">0x76</code> and <code class="language-plaintext highlighter-rouge">0x77</code>), selected by the SDO pin. The old driver only tracked one. The new driver stores calibration for both, doubling capacity in every topology:</p>

<table>
  <thead>
    <tr>
      <th>Topology</th>
      <th>Old driver</th>
      <th>New driver</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Single I2C bus</td>
      <td>1</td>
      <td><strong>2</strong></td>
    </tr>
    <tr>
      <td>8-channel mux</td>
      <td>8</td>
      <td><strong>16</strong></td>
    </tr>
    <tr>
      <td>8 muxes × 8 channels</td>
      <td>64</td>
      <td><strong>128</strong></td>
    </tr>
  </tbody>
</table>

<p><img src="/assets/images/posts/i2c-mux.png" alt="I2C multiplexer" style="width: 200px; float: right; margin: 0 0 1em 1em;" /></p>

<h3 id="multiplexer-arrays">Multiplexer arrays</h3>

<p>For the patent prototype, we used TCA9548A 8-channel I2C multiplexers. Each mux channel is an electrically isolated I2C bus, so sensors on different channels can share the same address:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                          ┌─── ch 0 ─── BMP280 (0x76) + BMP280 (0x77)
  Pi ── I2C ── TCA9548A ──┼─── ch 1 ─── BMP280 (0x76) + BMP280 (0x77)
               (addr 0x70) ├─── ch 2 ─── BMP280 (0x76) + BMP280 (0x77)
                          └─── ...         (up to 16 sensors per mux)
</code></pre></div></div>

<p>A Raspberry Pi at each location ran a Rust application on Linux that cycled through mux channels, read calibrated pressure/temperature data from every sensor, and logged the raw values. A separate PC-based Rust application pulled the log files for analysis, producing plots and spreadsheets.</p>

<hr />

<p><img src="/assets/images/posts/bmp580.png" alt="BMP581 sensor" style="width: 200px; float: right; margin: 0 0 1em 1em;" /></p>

<h2 id="what-we-learned-bmp280--bmp581">What We Learned: BMP280 → BMP581</h2>

<p>The BMP280 worked for the initial proof-of-concept, but we hit its limits. The sensor’s resolution wasn’t granular enough for the pressure differentials we needed to measure. The BMP581—Bosch’s newer generation—offers significantly better resolution and noise characteristics.</p>

<p>We also changed the architecture. Instead of running I2C extenders to reach distant sensor locations from a single controller, we gave each location its own Raspberry Pi with its own mux and BMP581 sensor array. The Pi boards communicate over gigabit LAN, which is simpler, more reliable, and eliminates the signal integrity issues that come with long I2C runs.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Location A:  Pi ── I2C ── TCA9548A ── BMP581s ──┐
                                                  ├── Gigabit LAN
Location B:  Pi ── I2C ── TCA9548A ── BMP581s ──┘
                                                  │
                                        Analysis PC (Rust)
</code></pre></div></div>

<hr />

<h2 id="using-the-driver">Using the Driver</h2>

<p>Basic usage with a single sensor:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">bmp280_ehal</span><span class="p">::</span><span class="n">BMP280</span><span class="p">;</span>

<span class="k">let</span> <span class="k">mut</span> <span class="n">bmp</span> <span class="o">=</span> <span class="nn">BMP280</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">i2c</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>
<span class="k">let</span> <span class="n">temp</span> <span class="o">=</span> <span class="n">bmp</span><span class="nf">.temp</span><span class="p">(</span><span class="mi">0x76</span><span class="p">);</span>     <span class="c1">// Celsius</span>
<span class="k">let</span> <span class="n">pres</span> <span class="o">=</span> <span class="n">bmp</span><span class="nf">.pressure</span><span class="p">(</span><span class="mi">0x76</span><span class="p">);</span> <span class="c1">// Pascals</span>
</code></pre></div></div>

<p>Two sensors on one bus:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="k">mut</span> <span class="n">bmp</span> <span class="o">=</span> <span class="nn">BMP280</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">i2c</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>
<span class="n">bmp</span><span class="nf">.read_calibration</span><span class="p">(</span><span class="mi">0x77</span><span class="p">);</span>

<span class="k">let</span> <span class="n">delta_p</span> <span class="o">=</span> <span class="n">bmp</span><span class="nf">.pressure</span><span class="p">(</span><span class="mi">0x76</span><span class="p">)</span> <span class="o">-</span> <span class="n">bmp</span><span class="nf">.pressure</span><span class="p">(</span><span class="mi">0x77</span><span class="p">);</span>
</code></pre></div></div>

<p>With a multiplexer:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="n">select_channel</span><span class="o">&lt;</span><span class="n">I2C</span><span class="p">,</span> <span class="n">E</span><span class="o">&gt;</span><span class="p">(</span><span class="n">i2c</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">mut</span> <span class="n">I2C</span><span class="p">,</span> <span class="n">ch</span><span class="p">:</span> <span class="nb">u8</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(),</span> <span class="n">E</span><span class="o">&gt;</span>
<span class="k">where</span> <span class="n">I2C</span><span class="p">:</span> <span class="nn">embedded_hal</span><span class="p">::</span><span class="nn">blocking</span><span class="p">::</span><span class="nn">i2c</span><span class="p">::</span><span class="n">Write</span><span class="o">&lt;</span><span class="n">Error</span> <span class="o">=</span> <span class="n">E</span><span class="o">&gt;</span>
<span class="p">{</span>
    <span class="n">i2c</span><span class="nf">.write</span><span class="p">(</span><span class="mi">0x70</span><span class="p">,</span> <span class="o">&amp;</span><span class="p">[</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">ch</span><span class="p">])</span>
<span class="p">}</span>
</code></pre></div></div>

<p>See the <a href="https://github.com/sw-embed/bmp280/blob/master/docs/multiple-sensor-support.md">multi-sensor documentation</a> for shared-bus patterns and cascaded mux topologies.</p>

<blockquote class="fun-fact">
  <p><strong>Fun Fact:</strong> Some of the Raspberry Pi boards with I2C multiplexers and BMP581 sensor arrays were submerged in diving bell-like enclosures with low power and LAN cables tethered.</p>
</blockquote>

<hr />

<h2 id="references">References</h2>

<table>
  <thead>
    <tr>
      <th>Reference</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>BMP280 datasheet</td>
      <td><a href="https://www.bosch-sensortec.com/media/boschsensortec/downloads/product_flyer/bst-bmp280-fl000.pdf">Bosch Sensortec (PDF)</a></td>
    </tr>
    <tr>
      <td>BMP581 product page</td>
      <td><a href="https://www.bosch-sensortec.com/en/products/environmental-sensors/pressure-sensors/bmp581">Bosch Sensortec</a></td>
    </tr>
    <tr>
      <td>MIKROE Pressure 21 Click</td>
      <td><a href="https://www.mikroe.com/pressure-21-click">mikroe.com</a></td>
    </tr>
    <tr>
      <td>I2C Multiplexer (TCA9548A)</td>
      <td><a href="https://github.com/sparkfun/SparkFun_I2C_Mux_Arduino_Library?tab=readme-ov-file">SparkFun I2C Mux</a></td>
    </tr>
    <tr>
      <td>US Patent 12,188,836 B1</td>
      <td><a href="https://patents.google.com/patent/US12188836B1/en">Google Patents</a></td>
    </tr>
    <tr>
      <td>Multi-sensor docs</td>
      <td><a href="https://github.com/sw-embed/bmp280/blob/master/docs/multiple-sensor-support.md">GitHub</a></td>
    </tr>
  </tbody>
</table>]]></content><author><name>Software Wrighter</name></author><category term="embedded-systems" /><category term="rust" /><category term="embedded" /><category term="bmp280" /><category term="i2c" /><category term="pressure-sensor" /><category term="raspberry-pi" /><category term="no_std" /><category term="embedded-hal" /><category term="multiplexer" /><summary type="html"><![CDATA[A colleague had a patent application that needed empirical data: pressure measurements from multiple sensors at two physical locations, with enough redundancy to establish statistical confidence. Off-the-shelf solutions weren’t flexible enough, so we built the whole stack in Rust—from the sensor driver up through data collection, analysis, and plotting. Resource Link Source GitHub Docs Multi-sensor guide References Datasheets, patent, hardware Comments Discord The Problem We needed high-resolution barometric pressure data from dozens of sensors split across two physical locations. Each location needed multiple sensors—not just for coverage, but because averaging across several readings reduces noise and gives more trustworthy measurements. A single BMP280 reading has enough jitter that you want redundancy. The existing BMP280 driver crate worked fine for one sensor at the default address. We needed it to support both I2C addresses so we could put two sensors on each bus—and then use multiplexers to scale that to many. Why Fork the Driver? The original bmp280-ehal crate is a platform-agnostic, no_std Rust driver built on embedded-hal traits. It runs on anything from bare-metal microcontrollers to Raspberry Pi. But it had a limitation: it assumed the default I2C address (0x76) and had no way to talk to a second sensor at the alternate address (0x77). I needed the driver to address both 0x76 and 0x77—two sensors per bus instead of one. That change alone doubled capacity, and multiplexers would scale it from there. So I forked the driver and made three changes: Commit 1: Refactor constructor (0b66fda) Simplified the API by removing the implicit address tracking. The old constructor tied the driver to a single address at creation time. The refactored version makes read_calibration() public and removes the stored address, laying the groundwork for multi-address support. Commit 2: Per-sensor calibration (d393bd6) The structural change that enables multi-sensor support. Instead of flat calibration fields on the driver struct, this commit introduces: TempComp and PressureComp structs for temperature and pressure compensation data Sensors container holding Option&lt;TempComp&gt; and Option&lt;PressureComp&gt; for both addresses (0x76 and 0x77) Every reading method now takes an explicit address parameter Each BMP280 ships with unique factory calibration coefficients baked into its NVM. The driver reads 24 bytes of calibration data per sensor and stores it independently. This matters because the compensation polynomial uses 12 coefficients—get them wrong and your pressure reading is meaningless. Commit 3: Documentation and multi-sensor guide (21f16dc) Updated the README and added comprehensive documentation in docs/multiple-sensor-support.md covering: Two sensors on one bus (differential pressure) I2C multiplexer topologies (TCA9548A, PCA9546A, TCA9543A) Cascading mux trees for large sensor arrays I2C extenders for long-distance distribution The Hardware Setup Doubling capacity with dual addresses The BMP280 supports two I2C addresses (0x76 and 0x77), selected by the SDO pin. The old driver only tracked one. The new driver stores calibration for both, doubling capacity in every topology: Topology Old driver New driver Single I2C bus 1 2 8-channel mux 8 16 8 muxes × 8 channels 64 128 Multiplexer arrays For the patent prototype, we used TCA9548A 8-channel I2C multiplexers. Each mux channel is an electrically isolated I2C bus, so sensors on different channels can share the same address: ┌─── ch 0 ─── BMP280 (0x76) + BMP280 (0x77) Pi ── I2C ── TCA9548A ──┼─── ch 1 ─── BMP280 (0x76) + BMP280 (0x77) (addr 0x70) ├─── ch 2 ─── BMP280 (0x76) + BMP280 (0x77) └─── ... (up to 16 sensors per mux) A Raspberry Pi at each location ran a Rust application on Linux that cycled through mux channels, read calibrated pressure/temperature data from every sensor, and logged the raw values. A separate PC-based Rust application pulled the log files for analysis, producing plots and spreadsheets. What We Learned: BMP280 → BMP581 The BMP280 worked for the initial proof-of-concept, but we hit its limits. The sensor’s resolution wasn’t granular enough for the pressure differentials we needed to measure. The BMP581—Bosch’s newer generation—offers significantly better resolution and noise characteristics. We also changed the architecture. Instead of running I2C extenders to reach distant sensor locations from a single controller, we gave each location its own Raspberry Pi with its own mux and BMP581 sensor array. The Pi boards communicate over gigabit LAN, which is simpler, more reliable, and eliminates the signal integrity issues that come with long I2C runs. Location A: Pi ── I2C ── TCA9548A ── BMP581s ──┐ ├── Gigabit LAN Location B: Pi ── I2C ── TCA9548A ── BMP581s ──┘ │ Analysis PC (Rust) Using the Driver Basic usage with a single sensor: use bmp280_ehal::BMP280; let mut bmp = BMP280::new(i2c)?; let temp = bmp.temp(0x76); // Celsius let pres = bmp.pressure(0x76); // Pascals Two sensors on one bus: let mut bmp = BMP280::new(i2c)?; bmp.read_calibration(0x77); let delta_p = bmp.pressure(0x76) - bmp.pressure(0x77); With a multiplexer: fn select_channel&lt;I2C, E&gt;(i2c: &amp;mut I2C, ch: u8) -&gt; Result&lt;(), E&gt; where I2C: embedded_hal::blocking::i2c::Write&lt;Error = E&gt; { i2c.write(0x70, &amp;[1 &lt;&lt; ch]) } See the multi-sensor documentation for shared-bus patterns and cascaded mux topologies. Fun Fact: Some of the Raspberry Pi boards with I2C multiplexers and BMP581 sensor arrays were submerged in diving bell-like enclosures with low power and LAN cables tethered. References Reference Link BMP280 datasheet Bosch Sensortec (PDF) BMP581 product page Bosch Sensortec MIKROE Pressure 21 Click mikroe.com I2C Multiplexer (TCA9548A) SparkFun I2C Mux US Patent 12,188,836 B1 Google Patents Multi-sensor docs GitHub]]></summary></entry><entry><title type="html">ML Frontier #02: In-Context Reinforcement Learning</title><link href="https://software-wrighter-lab.github.io/2026/03/16/mlf-02-icrl-in-context-reinforcement-learning/" rel="alternate" type="text/html" title="ML Frontier #02: In-Context Reinforcement Learning" /><published>2026-03-16T01:15:00-07:00</published><updated>2026-03-16T01:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/16/mlf-02-icrl-in-context-reinforcement-learning</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/16/mlf-02-icrl-in-context-reinforcement-learning/"><![CDATA[<p><img src="/assets/images/posts/block-graph.png" class="post-marker" alt="" style="width: 120px;" /></p>

<p>Second ML Frontier episode. This one covers In-Context Reinforcement Learning—how transformers learn decision policies from trajectory examples in the prompt, without weight updates.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Papers</strong></td>
        <td><a href="#papers">5 papers covered</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/shorts/D4KrvmqOwEY">ML Frontier 2: ICRL</a><br /><a href="https://www.youtube.com/shorts/D4KrvmqOwEY"><img src="https://img.youtube.com/vi/D4KrvmqOwEY/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Related</strong></td>
        <td><a href="/2026/03/15/saw-reg-rs-avoid-compaction-agentrail/">Saw (2/?): agentrail-rs</a> — practical ICRL application</td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="what-is-in-context-reinforcement-learning">What is In-Context Reinforcement Learning?</h2>

<p>The model observes sequences of states, actions, and rewards stored in the prompt. Instead of updating weights through gradient descent, the transformer approximates a policy from those trajectory examples.</p>

<p>Think of it like learning to cook by reading a journal of recipes that worked—and ones that didn’t—with notes on what went wrong.</p>

<h2 id="why-does-this-matter">Why Does This Matter?</h2>

<p>AI agents often lose procedural knowledge when context is truncated between sessions. They know the goal but forget which API to call, which flags to use, which client library to reference, or how to validate output.</p>

<p>The traditional approach—writing instructions in markdown files—isn’t reliable. Agents ignore rules even when they’re present. ICRL offers a different path: instead of telling the agent what to do, show it what worked and what didn’t, with reward signals attached.</p>

<p>By embedding successful execution traces in the prompt, agents can reuse proven approaches instead of improvising from scratch.</p>

<h2 id="research-evidence">Research Evidence</h2>

<h3 id="decision-transformer-chen-et-al-2021">Decision Transformer (Chen et al., 2021)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2106.01345">arXiv 2106.01345</a></p>

<p><strong>In brief:</strong> The paper that started it all. Instead of training an RL agent with value functions and policy gradients, just frame the problem as sequence prediction. Feed the transformer trajectories of (return-to-go, state, action) and let it predict the next action conditioned on the desired return. The transformer learns a policy by modeling sequences—no Bellman equations needed.</p>

<p><strong>Why it matters:</strong> Reframed RL as something transformers already do well: sequence modeling.</p>

<h3 id="transformers-learn-td-methods-wang-et-al-iclr-2025">Transformers Learn TD Methods (Wang et al., ICLR 2025)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2405.13861">arXiv 2405.13861</a></p>

<p><strong>In brief:</strong> This paper shows that transformers don’t just pattern-match on trajectories—they actually approximate temporal-difference (TD) learning algorithms during the forward pass. The model internally implements something resembling TD updates, without being explicitly trained to do so.</p>

<p><strong>Why it matters:</strong> Transformers aren’t just memorizing trajectories. They’re learning the underlying RL algorithm in-context.</p>

<h3 id="omnirl-2025">OmniRL (2025)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2502.02869">arXiv 2502.02869</a></p>

<p><strong>In brief:</strong> OmniRL proposes a transformer architecture that emulates actor-critic RL in-context, improving decision quality across multiple tasks. Rather than specializing in one environment, the model generalizes its in-context RL capabilities across diverse settings.</p>

<p><strong>Why it matters:</strong> ICRL isn’t limited to one task—it scales across environments.</p>

<h3 id="reflexion-shinn-et-al-neurips-2023">Reflexion (Shinn et al., NeurIPS 2023)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2303.11366">arXiv 2303.11366</a></p>

<p><strong>In brief:</strong> Reflexion takes a different angle: instead of embedding raw trajectories, the agent generates verbal reflections on its failures and successes. These self-critiques are stored and injected into future prompts. The agent learns from its own written analysis of what went wrong.</p>

<p><strong>Why it matters:</strong> Shows that trajectory-based learning doesn’t require structured (state, action, reward) tuples—natural language reflections work too.</p>

<h3 id="voyager-wang-et-al-2023">Voyager (Wang et al., 2023)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2305.16291">arXiv 2305.16291</a></p>

<p><strong>In brief:</strong> An open-ended Minecraft agent that builds a skill library from successful code executions. When Voyager solves a task, it stores the working code as a reusable skill. Future tasks can retrieve and compose these skills. The agent explores, learns, and accumulates capabilities without any weight updates.</p>

<p><strong>Why it matters:</strong> Demonstrates ICRL at scale—an agent that gets better over time by accumulating proven solutions.</p>

<h2 id="papers">Papers</h2>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Paper</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Jun 2021</td>
      <td>Decision Transformer: RL via Sequence Modeling</td>
      <td><a href="https://arxiv.org/abs/2106.01345">arXiv 2106.01345</a></td>
    </tr>
    <tr>
      <td>Mar 2023</td>
      <td>Reflexion: Language Agents with Verbal Reinforcement Learning</td>
      <td><a href="https://arxiv.org/abs/2303.11366">arXiv 2303.11366</a></td>
    </tr>
    <tr>
      <td>May 2023</td>
      <td>Voyager: Open-Ended Embodied Agent with LLMs</td>
      <td><a href="https://arxiv.org/abs/2305.16291">arXiv 2305.16291</a></td>
    </tr>
    <tr>
      <td>May 2024</td>
      <td>Transformers Learn TD Methods for In-Context RL</td>
      <td><a href="https://arxiv.org/abs/2405.13861">arXiv 2405.13861</a></td>
    </tr>
    <tr>
      <td>Feb 2025</td>
      <td>OmniRL: In-Context RL Across Multiple Tasks</td>
      <td><a href="https://arxiv.org/abs/2502.02869">arXiv 2502.02869</a></td>
    </tr>
  </tbody>
</table>

<h2 id="practical-application-agentrail-rs">Practical Application: agentrail-rs</h2>

<p>This isn’t just theory. I’m building <a href="https://github.com/sw-vibe-coding/agentrail-rs">agentrail-rs</a> to apply ICRL to AI coding agents used for non-coding tasks—TTS generation, video compositing, file manipulation. The tool records trajectories (state, action, result, reward) and injects successful examples into future agent prompts. Early days, but the research says this should work.</p>

<p>See <a href="/2026/03/15/saw-reg-rs-avoid-compaction-agentrail/">Saw (2/?): agentrail-rs</a> for more on the engineering side.</p>

<h2 id="key-takeaways">Key Takeaways</h2>

<table>
  <thead>
    <tr>
      <th>Concept</th>
      <th>One-liner</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ICRL</strong></td>
      <td>Learn RL policies from trajectory examples in the prompt</td>
    </tr>
    <tr>
      <td><strong>No Weight Updates</strong></td>
      <td>The transformer adapts during inference, not training</td>
    </tr>
    <tr>
      <td><strong>TD in the Forward Pass</strong></td>
      <td>Transformers approximate RL algorithms internally</td>
    </tr>
    <tr>
      <td><strong>Verbal Reflection</strong></td>
      <td>Natural language self-critique works as trajectory data</td>
    </tr>
    <tr>
      <td><strong>Skill Libraries</strong></td>
      <td>Accumulate proven solutions for reuse across sessions</td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>In-Context RL turns agents from amnesiacs into learners. Follow for more ML Frontier episodes exploring research at the edge.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="machine-learning" /><category term="research" /><category term="explainers" /><category term="ml-frontier" /><category term="icrl" /><category term="reinforcement-learning" /><category term="transformers" /><category term="decision-transformer" /><category term="in-context-learning" /><category term="ai-agents" /><summary type="html"><![CDATA[Second ML Frontier episode. This one covers In-Context Reinforcement Learning—how transformers learn decision policies from trajectory examples in the prompt, without weight updates. Resource Link Papers 5 papers covered Video ML Frontier 2: ICRL Related Saw (2/?): agentrail-rs — practical ICRL application Comments Discord What is In-Context Reinforcement Learning? The model observes sequences of states, actions, and rewards stored in the prompt. Instead of updating weights through gradient descent, the transformer approximates a policy from those trajectory examples. Think of it like learning to cook by reading a journal of recipes that worked—and ones that didn’t—with notes on what went wrong. Why Does This Matter? AI agents often lose procedural knowledge when context is truncated between sessions. They know the goal but forget which API to call, which flags to use, which client library to reference, or how to validate output. The traditional approach—writing instructions in markdown files—isn’t reliable. Agents ignore rules even when they’re present. ICRL offers a different path: instead of telling the agent what to do, show it what worked and what didn’t, with reward signals attached. By embedding successful execution traces in the prompt, agents can reuse proven approaches instead of improvising from scratch. Research Evidence Decision Transformer (Chen et al., 2021) Paper: arXiv 2106.01345 In brief: The paper that started it all. Instead of training an RL agent with value functions and policy gradients, just frame the problem as sequence prediction. Feed the transformer trajectories of (return-to-go, state, action) and let it predict the next action conditioned on the desired return. The transformer learns a policy by modeling sequences—no Bellman equations needed. Why it matters: Reframed RL as something transformers already do well: sequence modeling. Transformers Learn TD Methods (Wang et al., ICLR 2025) Paper: arXiv 2405.13861 In brief: This paper shows that transformers don’t just pattern-match on trajectories—they actually approximate temporal-difference (TD) learning algorithms during the forward pass. The model internally implements something resembling TD updates, without being explicitly trained to do so. Why it matters: Transformers aren’t just memorizing trajectories. They’re learning the underlying RL algorithm in-context. OmniRL (2025) Paper: arXiv 2502.02869 In brief: OmniRL proposes a transformer architecture that emulates actor-critic RL in-context, improving decision quality across multiple tasks. Rather than specializing in one environment, the model generalizes its in-context RL capabilities across diverse settings. Why it matters: ICRL isn’t limited to one task—it scales across environments. Reflexion (Shinn et al., NeurIPS 2023) Paper: arXiv 2303.11366 In brief: Reflexion takes a different angle: instead of embedding raw trajectories, the agent generates verbal reflections on its failures and successes. These self-critiques are stored and injected into future prompts. The agent learns from its own written analysis of what went wrong. Why it matters: Shows that trajectory-based learning doesn’t require structured (state, action, reward) tuples—natural language reflections work too. Voyager (Wang et al., 2023) Paper: arXiv 2305.16291 In brief: An open-ended Minecraft agent that builds a skill library from successful code executions. When Voyager solves a task, it stores the working code as a reusable skill. Future tasks can retrieve and compose these skills. The agent explores, learns, and accumulates capabilities without any weight updates. Why it matters: Demonstrates ICRL at scale—an agent that gets better over time by accumulating proven solutions. Papers Date Paper Link Jun 2021 Decision Transformer: RL via Sequence Modeling arXiv 2106.01345 Mar 2023 Reflexion: Language Agents with Verbal Reinforcement Learning arXiv 2303.11366 May 2023 Voyager: Open-Ended Embodied Agent with LLMs arXiv 2305.16291 May 2024 Transformers Learn TD Methods for In-Context RL arXiv 2405.13861 Feb 2025 OmniRL: In-Context RL Across Multiple Tasks arXiv 2502.02869 Practical Application: agentrail-rs This isn’t just theory. I’m building agentrail-rs to apply ICRL to AI coding agents used for non-coding tasks—TTS generation, video compositing, file manipulation. The tool records trajectories (state, action, result, reward) and injects successful examples into future agent prompts. Early days, but the research says this should work. See Saw (2/?): agentrail-rs for more on the engineering side. Key Takeaways Concept One-liner ICRL Learn RL policies from trajectory examples in the prompt No Weight Updates The transformer adapts during inference, not training TD in the Forward Pass Transformers approximate RL algorithms internally Verbal Reflection Natural language self-critique works as trajectory data Skill Libraries Accumulate proven solutions for reuse across sessions In-Context RL turns agents from amnesiacs into learners. Follow for more ML Frontier episodes exploring research at the edge.]]></summary></entry><entry><title type="html">Saw (2/?): reg-rs, avoid-compaction, and agentrail-rs</title><link href="https://software-wrighter-lab.github.io/2026/03/15/saw-reg-rs-avoid-compaction-agentrail/" rel="alternate" type="text/html" title="Saw (2/?): reg-rs, avoid-compaction, and agentrail-rs" /><published>2026-03-15T11:00:00-07:00</published><updated>2026-03-15T11:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/15/saw-reg-rs-avoid-compaction-agentrail</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/15/saw-reg-rs-avoid-compaction-agentrail/"><![CDATA[<p><img src="/assets/images/posts/block-saw-circle-handle.png" class="post-marker no-invert" alt="" /></p>

<p>Welcome back to the <a href="/series/#sharpen-the-saw-sundays">Sharpen the Saw</a> series, where I maintain existing tools, vibe-code new ones, and try new approaches to development workflows. Three tools, one pattern: each one hit a ceiling that required rethinking how it stores and shares information. This week covers reg-rs migrating from binary to text-based test definitions, avoid-compaction structuring multi-session AI agent workflows, and agentrail-rs adding reinforcement learning from an agent’s own success history.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repos</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs">sw-cli-tools/reg-rs</a>, <a href="https://github.com/softwarewrighter/avoid-compaction">softwarewrighter/avoid-compaction</a>, <a href="https://github.com/sw-vibe-coding/agentrail-rs">sw-vibe-coding/agentrail-rs</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/watch?v=TkdNCJi2jBA">Sharpening 3 Rust Dev Tools</a></td>
      </tr>
      <tr>
        <td><strong>References</strong></td>
        <td><a href="#references">Links and Resources</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="reg-rs-three-improvements-to-regression-testing">reg-rs: Three Improvements to Regression Testing</h2>

<p><a href="https://github.com/sw-cli-tools/reg-rs">reg-rs</a> captures command output as golden baselines and flags regressions on re-run. This round of sharpening addressed three friction points: clunky commands, opaque binary storage, and noisy output.</p>

<h3 id="shell-aliases">Shell Aliases</h3>

<p>The full command syntax (<code class="language-plaintext highlighter-rouge">reg-rs run -p my_test -v</code>) gets old fast. Shell aliases in <code class="language-plaintext highlighter-rouge">source-rg.sh</code> cut it to 4 characters with tab completion in zsh and bash:</p>

<table>
  <thead>
    <tr>
      <th>Alias</th>
      <th>Action</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rnrg</code></td>
      <td>Run tests</td>
      <td><code class="language-plaintext highlighter-rouge">rnrg my_test -v</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">adrg</code></td>
      <td>Create test</td>
      <td><code class="language-plaintext highlighter-rouge">adrg my_test 'echo hi'</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">lsrg</code></td>
      <td>List tests</td>
      <td><code class="language-plaintext highlighter-rouge">lsrg</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">shrg</code></td>
      <td>Show details</td>
      <td><code class="language-plaintext highlighter-rouge">shrg my_test -vv</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">uprg</code></td>
      <td>Rebase baselines</td>
      <td><code class="language-plaintext highlighter-rouge">uprg my_test</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rsrg</code></td>
      <td>Reset results</td>
      <td><code class="language-plaintext highlighter-rouge">rsrg my_test</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rmrg</code></td>
      <td>Remove test</td>
      <td><code class="language-plaintext highlighter-rouge">rmrg old_test</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mgrg</code></td>
      <td>Migrate .tdb to .rgt</td>
      <td><code class="language-plaintext highlighter-rouge">mgrg</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">strg</code></td>
      <td>Status dashboard</td>
      <td><code class="language-plaintext highlighter-rouge">strg</code></td>
    </tr>
  </tbody>
</table>

<p>Muscle memory builds fast. <code class="language-plaintext highlighter-rouge">hlrg</code> prints the full cheat sheet with examples.</p>

<h3 id="git-friendly-rgt-format">Git-Friendly .rgt Format</h3>

<p>The legacy <code class="language-plaintext highlighter-rouge">.tdb</code> format stored tests in SQLite binary files. <code class="language-plaintext highlighter-rouge">git diff</code> showed noise, merge conflicts were unresolvable, and new developers needed setup scripts. Regression tests are documentation—they define what your CLI <em>actually does</em>—so hiding them in binary blobs defeated the purpose.</p>

<p>The new <code class="language-plaintext highlighter-rouge">.rgt</code> format splits each test across git-tracked text files:</p>

<table>
  <thead>
    <tr>
      <th>File</th>
      <th>Contents</th>
      <th>Tracked?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.rgt</code></td>
      <td>TOML spec (command, timeout, preprocessing)</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.out</code></td>
      <td>Expected stdout baseline</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.err</code></td>
      <td>Expected stderr (only if non-empty)</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test.tdb</code></td>
      <td>Runtime cache</td>
      <td>No (gitignored)</td>
    </tr>
  </tbody>
</table>

<p>A test definition reads like documentation:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">command</span> <span class="o">=</span><span class="w"> </span><span class="s">"myapp --version"</span>
<span class="n">timeout</span> <span class="o">=</span><span class="w"> </span><span class="mi">10</span>
<span class="n">exit_code</span> <span class="o">=</span><span class="w"> </span><span class="mi">0</span>
<span class="n">desc</span> <span class="o">=</span><span class="w"> </span><span class="s">"Version string format check"</span>
<span class="n">preprocess</span> <span class="o">=</span><span class="w"> </span><span class="s">"jq --sort-keys"</span>
<span class="n">diff_mode</span> <span class="o">=</span><span class="w"> </span><span class="s">"json"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">reg-rs create</code> now writes <code class="language-plaintext highlighter-rouge">.rgt</code> directly—no intermediate <code class="language-plaintext highlighter-rouge">.tdb</code> step. Existing tests migrate with <code class="language-plaintext highlighter-rouge">mgrg</code>. PR reviewers see exactly what changed and why, <code class="language-plaintext highlighter-rouge">git clone</code> inherits every test, and merge conflicts resolve with standard tools.</p>

<h3 id="output-verbosity-controls">Output Verbosity Controls</h3>

<p>Previously, running tests dumped SQL debug info and full diffs regardless of context. Now output scales to what you need:</p>

<table>
  <thead>
    <tr>
      <th>Flag</th>
      <th>Output</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><em>(none)</em></td>
      <td>Summary line: <code class="language-plaintext highlighter-rouge">3 passed, 1 failed (of 4 total)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code></td>
      <td>+ failure details (diff counts per test)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-vv</code></td>
      <td>+ full diff output</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-q</code></td>
      <td>Nothing—exit code only</td>
    </tr>
  </tbody>
</table>

<p>Exit codes are now meaningful too: <code class="language-plaintext highlighter-rouge">0</code> for all pass, <code class="language-plaintext highlighter-rouge">1</code> for regressions detected, <code class="language-plaintext highlighter-rouge">2</code> for errors. This makes reg-rs usable in CI pipelines where you check <code class="language-plaintext highlighter-rouge">$?</code> rather than parse output.</p>

<div class="resource-box">

  <p><strong>Sharpen the Saw</strong> — Habit 7 from Stephen Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People"><em>The 7 Habits of Highly Effective People</em></a> is about preserving and enhancing your greatest asset: yourself and your tools. In software, that means taking time to fix accumulated friction, update dependencies, and learn new frameworks—even when shipping features feels more urgent. The payoff compounds: every hour spent sharpening saves many more down the line.</p>

</div>

<h2 id="avoid-compaction-structured-multi-session-agent-workflows">avoid-compaction: Structured Multi-Session Agent Workflows</h2>

<p><a href="https://github.com/softwarewrighter/avoid-compaction">avoid-compaction</a> solves a problem anyone using AI coding agents hits eventually: context death. Long conversations get automatically compacted—the system summarizes older messages to make room for new ones, losing decisions, constraints, and procedural knowledge along the way.</p>

<h3 id="the-insight">The Insight</h3>

<p>Rather than fighting compaction with longer context windows, avoid-compaction embraces frequent restarts as a feature. Each restart gives the agent a full, fresh context window. The trick is making handoffs explicit and structured so nothing is lost between sessions.</p>

<h3 id="the-sagastep-model">The Saga/Step Model</h3>

<p>Work is organized into <strong>sagas</strong> (projects) composed of <strong>steps</strong> (focused units of work):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.avoid-compaction/
  saga.toml                    # name, status, current step
  plan.md                      # evolving project plan
  planned-steps.md             # upcoming steps preview
  steps/001-add-routes/
    step.toml                  # status, description, context files
    prompt.md                  # what the agent was told to do
    summary.md                 # what the agent actually did
  steps/002-add-tests/
    ...
</code></pre></div></div>

<p>Each session follows the same loop:</p>

<ol>
  <li>New Claude session starts with fresh context</li>
  <li>Agent runs <code class="language-plaintext highlighter-rouge">next</code> to see the current step’s prompt and context</li>
  <li>Agent does the work</li>
  <li>Agent runs <code class="language-plaintext highlighter-rouge">complete</code> with a summary and next-step definition</li>
  <li>User restarts Claude</li>
  <li>Next session picks up exactly where the last left off</li>
</ol>

<h3 id="why-this-matters">Why This Matters</h3>

<p>The difference is reliability. Without structured handoffs, session 4 of a complex feature often forgets constraints from session 1. The agent improvises, makes contradictory decisions, or redoes work. With avoid-compaction:</p>

<ul>
  <li><strong>Every session starts with full context</strong> for its specific task</li>
  <li><strong>Summaries accumulate</strong> so later sessions can reference earlier decisions</li>
  <li><strong>The plan evolves</strong> as work reveals new insights</li>
  <li><strong>Nothing is lost</strong> to compaction—it’s all in the filesystem</li>
</ul>

<h3 id="current-improvements">Current Improvements</h3>

<p>The tool is going through a refactoring sequence to meet code quality standards:</p>

<ol>
  <li><strong>Merging small command modules</strong> into larger, cohesive modules</li>
  <li><strong>Extracting shared display logic</strong> into reusable formatters</li>
  <li><strong>Workspace conversion</strong> to split concerns across crates</li>
  <li><strong>Session crate extraction</strong> for reusable JSONL handling</li>
</ol>

<p>Each spike is low-to-medium risk, guided by the principle that smaller modules with clear responsibilities are easier to test, review, and extend.</p>

<h2 id="agentrail-rs-learning-from-success">agentrail-rs: Learning from Success</h2>

<p><a href="https://github.com/sw-vibe-coding/agentrail-rs">agentrail-rs</a> is the evolution of avoid-compaction, adding a critical capability: <strong>In-Context Reinforcement Learning (ICRL)</strong>. Where avoid-compaction structures handoffs, agentrail-rs teaches agents from their own history.</p>

<h3 id="the-75-problem">The 75% Problem</h3>

<p>I use AI coding agents for more than coding—TTS audio generation, video compositing, file manipulation, and other multi-step production tasks. In practice, agents succeed about 75% of the time on these workflows. The failures aren’t random—they’re procedural: the agent forgets which API to call, which flags to use, which client library to reference, or how to validate output.</p>

<p>The traditional approach—writing instructions in markdown files like <code class="language-plaintext highlighter-rouge">AGENTS.md</code> or <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>—isn’t reliable. Even when rules, instructions, and prohibitions are present, agents often ignore them. Claude, when called out, will say <em>“You’re right, I should have done that”</em>—and a few moments later make the same kind of mistake. Bigger prompts and more examples hit diminishing returns. The agent needs to learn from <em>reward-based examples</em>—both good and bad—delivered in-context, not static documentation it may or may not follow. That’s the core idea behind ICRL: show the agent what worked, what didn’t, and let the rewards guide its next attempt.</p>

<h3 id="how-icrl-works">How ICRL Works</h3>

<p>After each step, agentrail-rs records a trajectory:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>state → action → result → reward
</code></pre></div></div>

<p>Successful trajectories are stored at <code class="language-plaintext highlighter-rouge">.agentrail/trajectories/{task_type}/run_NNN.json</code>. When the agent hits the same task type in a new session, the CLI retrieves the top N successful trajectories and injects them into the prompt: <em>“Here’s how you succeeded at this before.”</em></p>

<p>The agent reads its own success patterns and self-corrects—no weight updates, no fine-tuning, no training pipeline. Just examples from its own history, delivered in context.</p>

<h3 id="four-step-types">Four Step Types</h3>

<p>agentrail-rs distinguishes what needs an agent from what doesn’t:</p>

<table>
  <thead>
    <tr>
      <th>Step Type</th>
      <th>Who Executes</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Meta</strong></td>
      <td>Agent</td>
      <td>Prepare handoff packets with success examples</td>
    </tr>
    <tr>
      <td><strong>Production</strong></td>
      <td>Agent</td>
      <td>Execute semantic work using prepared context</td>
    </tr>
    <tr>
      <td><strong>Deterministic</strong></td>
      <td>Machine</td>
      <td>Run TTS generation, video composition (no agent needed)</td>
    </tr>
    <tr>
      <td><strong>Validation</strong></td>
      <td>Machine</td>
      <td>Check outputs, record rewards for ICRL</td>
    </tr>
  </tbody>
</table>

<p>Deterministic steps can’t fail due to agent forgetfulness—they’re hard-specified. Validation steps create the reward signals that make ICRL work.</p>

<h3 id="architecture">Architecture</h3>

<p>The project is structured as a five-crate Cargo workspace:</p>

<table>
  <thead>
    <tr>
      <th>Crate</th>
      <th>Purpose</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">agentrail-core</code></td>
      <td>Domain model, trajectories, handoff packets</td>
      <td>Complete</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">agentrail-store</code></td>
      <td>Persistence (saga, step, trajectory, session)</td>
      <td>Complete</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">agentrail-cli</code></td>
      <td>CLI commands</td>
      <td>Stub</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">agentrail-exec</code></td>
      <td>Deterministic job executors</td>
      <td>Stub</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">agentrail-validate</code></td>
      <td>Output validators</td>
      <td>Stub</td>
    </tr>
  </tbody>
</table>

<p>The core and store crates are done. The next phase wires up the CLI, then deterministic execution, then the full ICRL retrieval and injection loop.</p>

<h3 id="the-expected-payoff">The Expected Payoff</h3>

<p>Once the trajectory system is live (I just started vibe-coding it today), agents working on repetitive task types should see reliability climb from ~75% toward deterministic. Each success makes the next attempt more likely to succeed, without any manual intervention. The goal is a self-improving loop: agents learn their own procedures through experience.</p>

<h2 id="three-problems-three-approaches">Three Problems, Three Approaches</h2>

<p>These projects aren’t related by a common architecture or shared abstraction. They’re related because each one solves a different productivity problem I keep hitting:</p>

<ul>
  <li><strong>reg-rs</strong> catches regressions that slip in whenever a feature is added or a fix applied—the kind of silent breakage that unit tests don’t cover because they test behavior, not actual output.</li>
  <li><strong>avoid-compaction</strong> is a direct reaction to Claude Code auto-compacting multiple times per day, with noticeable performance degradation after each compaction. Structured restarts with explicit handoffs beat a slowly decaying context window.</li>
  <li><strong>agentrail-rs</strong> tackles the opposite problem: not forgetting, but <em>improvising</em>. LLMs are probabilistic, and Claude keeps trying new (failing) approaches to routine tasks instead of sticking with the proven-working ones it has used and documented before. ICRL feeds successful trajectories back into context so the agent repeats what works.</li>
</ul>

<p>Different problems, different solutions, same goal: spend less time fighting tools and more time building.</p>

<div class="references-section">

  <h2 id="references">References</h2>

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>reg-rs</strong></td>
        <td><a href="https://github.com/sw-cli-tools/reg-rs">github.com/sw-cli-tools/reg-rs</a></td>
      </tr>
      <tr>
        <td><strong>avoid-compaction</strong></td>
        <td><a href="https://github.com/softwarewrighter/avoid-compaction">github.com/softwarewrighter/avoid-compaction</a></td>
      </tr>
      <tr>
        <td><strong>agentrail-rs</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/agentrail-rs">github.com/sw-vibe-coding/agentrail-rs</a></td>
      </tr>
      <tr>
        <td><strong>Decision Transformer</strong></td>
        <td><a href="https://arxiv.org/abs/2106.01345">Chen et al., 2021</a> — framing RL as sequence modeling</td>
      </tr>
      <tr>
        <td><strong>Transformers Learn TD Methods</strong></td>
        <td><a href="https://arxiv.org/abs/2405.13861">Wang et al., ICLR 2025</a> — transformers simulate temporal-difference learning in-context</td>
      </tr>
      <tr>
        <td><strong>OmniRL</strong></td>
        <td><a href="https://arxiv.org/abs/2502.02869">2025</a> — transformer architecture emulating actor-critic RL in-context</td>
      </tr>
      <tr>
        <td><strong>Reflexion</strong></td>
        <td><a href="https://arxiv.org/abs/2303.11366">Shinn et al., NeurIPS 2023</a> — verbal self-reflection for agent improvement</td>
      </tr>
      <tr>
        <td><strong>Voyager</strong></td>
        <td><a href="https://arxiv.org/abs/2305.16291">Wang et al., 2023</a> — open-ended learning agent with skill library</td>
      </tr>
      <tr>
        <td><strong>“Sharpen the Saw”</strong></td>
        <td><a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">The 7 Habits of Highly Effective People</a> (Stephen Covey)</td>
      </tr>
    </tbody>
  </table>

</div>

<hr />

<p><em>Habit 7: Sharpen the Saw. Spend less time fighting tools, more time building.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rust" /><category term="tools" /><category term="developer-workflow" /><category term="rust" /><category term="reg-rs" /><category term="avoid-compaction" /><category term="agentrail-rs" /><category term="sharpen-the-saw" /><category term="cli" /><category term="developer-tools" /><category term="ai-agents" /><category term="regression-testing" /><category term="workflow" /><summary type="html"><![CDATA[Welcome back to the Sharpen the Saw series, where I maintain existing tools, vibe-code new ones, and try new approaches to development workflows. Three tools, one pattern: each one hit a ceiling that required rethinking how it stores and shares information. This week covers reg-rs migrating from binary to text-based test definitions, avoid-compaction structuring multi-session AI agent workflows, and agentrail-rs adding reinforcement learning from an agent’s own success history. Resource Link Repos sw-cli-tools/reg-rs, softwarewrighter/avoid-compaction, sw-vibe-coding/agentrail-rs Video Sharpening 3 Rust Dev Tools References Links and Resources Comments Discord reg-rs: Three Improvements to Regression Testing reg-rs captures command output as golden baselines and flags regressions on re-run. This round of sharpening addressed three friction points: clunky commands, opaque binary storage, and noisy output. Shell Aliases The full command syntax (reg-rs run -p my_test -v) gets old fast. Shell aliases in source-rg.sh cut it to 4 characters with tab completion in zsh and bash: Alias Action Example rnrg Run tests rnrg my_test -v adrg Create test adrg my_test 'echo hi' lsrg List tests lsrg shrg Show details shrg my_test -vv uprg Rebase baselines uprg my_test rsrg Reset results rsrg my_test rmrg Remove test rmrg old_test mgrg Migrate .tdb to .rgt mgrg strg Status dashboard strg Muscle memory builds fast. hlrg prints the full cheat sheet with examples. Git-Friendly .rgt Format The legacy .tdb format stored tests in SQLite binary files. git diff showed noise, merge conflicts were unresolvable, and new developers needed setup scripts. Regression tests are documentation—they define what your CLI actually does—so hiding them in binary blobs defeated the purpose. The new .rgt format splits each test across git-tracked text files: File Contents Tracked? test.rgt TOML spec (command, timeout, preprocessing) Yes test.out Expected stdout baseline Yes test.err Expected stderr (only if non-empty) Yes test.tdb Runtime cache No (gitignored) A test definition reads like documentation: command = "myapp --version" timeout = 10 exit_code = 0 desc = "Version string format check" preprocess = "jq --sort-keys" diff_mode = "json" reg-rs create now writes .rgt directly—no intermediate .tdb step. Existing tests migrate with mgrg. PR reviewers see exactly what changed and why, git clone inherits every test, and merge conflicts resolve with standard tools. Output Verbosity Controls Previously, running tests dumped SQL debug info and full diffs regardless of context. Now output scales to what you need: Flag Output (none) Summary line: 3 passed, 1 failed (of 4 total) -v + failure details (diff counts per test) -vv + full diff output -q Nothing—exit code only Exit codes are now meaningful too: 0 for all pass, 1 for regressions detected, 2 for errors. This makes reg-rs usable in CI pipelines where you check $? rather than parse output. Sharpen the Saw — Habit 7 from Stephen Covey’s The 7 Habits of Highly Effective People is about preserving and enhancing your greatest asset: yourself and your tools. In software, that means taking time to fix accumulated friction, update dependencies, and learn new frameworks—even when shipping features feels more urgent. The payoff compounds: every hour spent sharpening saves many more down the line. avoid-compaction: Structured Multi-Session Agent Workflows avoid-compaction solves a problem anyone using AI coding agents hits eventually: context death. Long conversations get automatically compacted—the system summarizes older messages to make room for new ones, losing decisions, constraints, and procedural knowledge along the way. The Insight Rather than fighting compaction with longer context windows, avoid-compaction embraces frequent restarts as a feature. Each restart gives the agent a full, fresh context window. The trick is making handoffs explicit and structured so nothing is lost between sessions. The Saga/Step Model Work is organized into sagas (projects) composed of steps (focused units of work): .avoid-compaction/ saga.toml # name, status, current step plan.md # evolving project plan planned-steps.md # upcoming steps preview steps/001-add-routes/ step.toml # status, description, context files prompt.md # what the agent was told to do summary.md # what the agent actually did steps/002-add-tests/ ... Each session follows the same loop: New Claude session starts with fresh context Agent runs next to see the current step’s prompt and context Agent does the work Agent runs complete with a summary and next-step definition User restarts Claude Next session picks up exactly where the last left off Why This Matters The difference is reliability. Without structured handoffs, session 4 of a complex feature often forgets constraints from session 1. The agent improvises, makes contradictory decisions, or redoes work. With avoid-compaction: Every session starts with full context for its specific task Summaries accumulate so later sessions can reference earlier decisions The plan evolves as work reveals new insights Nothing is lost to compaction—it’s all in the filesystem Current Improvements The tool is going through a refactoring sequence to meet code quality standards: Merging small command modules into larger, cohesive modules Extracting shared display logic into reusable formatters Workspace conversion to split concerns across crates Session crate extraction for reusable JSONL handling Each spike is low-to-medium risk, guided by the principle that smaller modules with clear responsibilities are easier to test, review, and extend. agentrail-rs: Learning from Success agentrail-rs is the evolution of avoid-compaction, adding a critical capability: In-Context Reinforcement Learning (ICRL). Where avoid-compaction structures handoffs, agentrail-rs teaches agents from their own history. The 75% Problem I use AI coding agents for more than coding—TTS audio generation, video compositing, file manipulation, and other multi-step production tasks. In practice, agents succeed about 75% of the time on these workflows. The failures aren’t random—they’re procedural: the agent forgets which API to call, which flags to use, which client library to reference, or how to validate output. The traditional approach—writing instructions in markdown files like AGENTS.md or CLAUDE.md—isn’t reliable. Even when rules, instructions, and prohibitions are present, agents often ignore them. Claude, when called out, will say “You’re right, I should have done that”—and a few moments later make the same kind of mistake. Bigger prompts and more examples hit diminishing returns. The agent needs to learn from reward-based examples—both good and bad—delivered in-context, not static documentation it may or may not follow. That’s the core idea behind ICRL: show the agent what worked, what didn’t, and let the rewards guide its next attempt. How ICRL Works After each step, agentrail-rs records a trajectory: state → action → result → reward Successful trajectories are stored at .agentrail/trajectories/{task_type}/run_NNN.json. When the agent hits the same task type in a new session, the CLI retrieves the top N successful trajectories and injects them into the prompt: “Here’s how you succeeded at this before.” The agent reads its own success patterns and self-corrects—no weight updates, no fine-tuning, no training pipeline. Just examples from its own history, delivered in context. Four Step Types agentrail-rs distinguishes what needs an agent from what doesn’t: Step Type Who Executes Example Meta Agent Prepare handoff packets with success examples Production Agent Execute semantic work using prepared context Deterministic Machine Run TTS generation, video composition (no agent needed) Validation Machine Check outputs, record rewards for ICRL Deterministic steps can’t fail due to agent forgetfulness—they’re hard-specified. Validation steps create the reward signals that make ICRL work. Architecture The project is structured as a five-crate Cargo workspace: Crate Purpose Status agentrail-core Domain model, trajectories, handoff packets Complete agentrail-store Persistence (saga, step, trajectory, session) Complete agentrail-cli CLI commands Stub agentrail-exec Deterministic job executors Stub agentrail-validate Output validators Stub The core and store crates are done. The next phase wires up the CLI, then deterministic execution, then the full ICRL retrieval and injection loop. The Expected Payoff Once the trajectory system is live (I just started vibe-coding it today), agents working on repetitive task types should see reliability climb from ~75% toward deterministic. Each success makes the next attempt more likely to succeed, without any manual intervention. The goal is a self-improving loop: agents learn their own procedures through experience. Three Problems, Three Approaches These projects aren’t related by a common architecture or shared abstraction. They’re related because each one solves a different productivity problem I keep hitting: reg-rs catches regressions that slip in whenever a feature is added or a fix applied—the kind of silent breakage that unit tests don’t cover because they test behavior, not actual output. avoid-compaction is a direct reaction to Claude Code auto-compacting multiple times per day, with noticeable performance degradation after each compaction. Structured restarts with explicit handoffs beat a slowly decaying context window. agentrail-rs tackles the opposite problem: not forgetting, but improvising. LLMs are probabilistic, and Claude keeps trying new (failing) approaches to routine tasks instead of sticking with the proven-working ones it has used and documented before. ICRL feeds successful trajectories back into context so the agent repeats what works. Different problems, different solutions, same goal: spend less time fighting tools and more time building. References Resource Link reg-rs github.com/sw-cli-tools/reg-rs avoid-compaction github.com/softwarewrighter/avoid-compaction agentrail-rs github.com/sw-vibe-coding/agentrail-rs Decision Transformer Chen et al., 2021 — framing RL as sequence modeling Transformers Learn TD Methods Wang et al., ICLR 2025 — transformers simulate temporal-difference learning in-context OmniRL 2025 — transformer architecture emulating actor-critic RL in-context Reflexion Shinn et al., NeurIPS 2023 — verbal self-reflection for agent improvement Voyager Wang et al., 2023 — open-ended learning agent with skill library “Sharpen the Saw” The 7 Habits of Highly Effective People (Stephen Covey) Habit 7: Sharpen the Saw. Spend less time fighting tools, more time building.]]></summary></entry><entry><title type="html">Rabbit-hole (1/?): Poor Man’s Rust-to-Unsupported-ISA Translator</title><link href="https://software-wrighter-lab.github.io/2026/03/13/rabbit-hole-rust-to-unsupported-isa/" rel="alternate" type="text/html" title="Rabbit-hole (1/?): Poor Man’s Rust-to-Unsupported-ISA Translator" /><published>2026-03-13T15:00:00-07:00</published><updated>2026-03-13T15:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/13/rabbit-hole-rust-to-unsupported-isa</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/13/rabbit-hole-rust-to-unsupported-isa/"><![CDATA[<p><img src="/assets/images/posts/block-rabbit-hole.png" class="post-marker" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>You want to write Rust for a CPU that <code class="language-plaintext highlighter-rouge">rustc</code> doesn’t know about. There’s no LLVM backend, no target triple, no supported tier. You could write a compiler backend from scratch—or you could cheat.</p>

  <p>This post traces the rabbit hole from Rust source code down to registers on the <a href="https://makerlisp.com">COR24</a>, a 24-bit RISC CPU that exists on real FPGA hardware. The trick: target a CPU that <code class="language-plaintext highlighter-rouge">rustc</code> <em>does</em> support (MSP430, a 16-bit TI microcontroller), then translate the assembly output.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Live Demo</strong></td>
        <td><a href="https://sw-embed.github.io/cor24-rs/">COR24 Assembly Emulator</a></td>
      </tr>
      <tr>
        <td><strong>Source</strong></td>
        <td><a href="https://github.com/sw-embed/cor24-rs">GitHub</a></td>
      </tr>
      <tr>
        <td><strong>MakerLisp</strong></td>
        <td><a href="https://makerlisp.com">makerlisp.com</a> (COR24 creators)</td>
      </tr>
      <tr>
        <td><strong>Disclaimer</strong></td>
        <td><a href="#disclaimer">Proof of concept only</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<hr />

<h2 id="the-problem">The Problem</h2>

<p>COR24 is a 24-bit RISC soft CPU designed by <a href="https://makerlisp.com">MakerLisp</a> for FPGAs. It has 3 general-purpose registers, a 24-bit address space, and runs at 101 MHz on inexpensive Lattice FPGAs. It’s MIT-licensed, well-documented, and has real hardware you can buy.</p>

<p>But LLVM doesn’t have a COR24 backend. Neither does GCC. Writing a full compiler backend could take a lot more effort. We need another way in.</p>

<hr />

<h2 id="the-trick-borrow-a-target">The Trick: Borrow a Target</h2>

<p><code class="language-plaintext highlighter-rouge">rustc</code> supports MSP430, a 16-bit TI microcontroller, via LLVM’s MSP430 backend. It’s a nightly-only target (<code class="language-plaintext highlighter-rouge">msp430-none-elf</code>), but it works. The key insight: MSP430’s instruction set is close enough to COR24 that a <em>translator</em> can bridge the gap.</p>

<p>The full pipeline:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Rust Source (.rs)
     ↓  rustc --target msp430-none-elf --emit asm
MSP430 Assembly (.msp430.s)
     ↓  msp430-to-cor24 translator
COR24 Assembly (.cor24.s)
     ↓  COR24 assembler
Machine Code → COR24 Emulator (or real FPGA hardware)
</code></pre></div></div>

<p>No custom compiler. No modified LLVM. Just Rust’s nightly toolchain and a ~1,800-line translator written in Rust. This is a proof-of-concept—an educational demo, not a production tool for real COR24 hardware.</p>

<div class="resource-box">

  <h3 id="disclaimer">Disclaimer</h3>

  <p>This is a proof-of-concept and educational demo. The MSP430-to-COR24 translator is not intended for production use on real COR24 hardware. It demonstrates that the approach is feasible, not that it’s complete or reliable. If you’re building something real on COR24, use the native assembler and toolchain from <a href="https://makerlisp.com">MakerLisp</a>.</p>

</div>

<hr />

<h2 id="level-1-the-compiler-optimizes-away-your-code">Level 1: The Compiler Optimizes Away Your Code</h2>

<p>Let’s start simple. Three numbers, one add:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#![no_std]</span>

<span class="k">const</span> <span class="n">RESULT_ADDR</span><span class="p">:</span> <span class="nb">u16</span> <span class="o">=</span> <span class="mi">0x0100</span><span class="p">;</span>

<span class="nd">#[inline(never)]</span>
<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">fn</span> <span class="nf">demo_add</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">u16</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">a</span><span class="p">:</span> <span class="nb">u16</span> <span class="o">=</span> <span class="mi">100</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">b</span><span class="p">:</span> <span class="nb">u16</span> <span class="o">=</span> <span class="mi">200</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">c</span><span class="p">:</span> <span class="nb">u16</span> <span class="o">=</span> <span class="mi">42</span><span class="p">;</span>
    <span class="n">a</span> <span class="o">+</span> <span class="n">b</span> <span class="o">+</span> <span class="n">c</span>  <span class="c1">// Returns 342</span>
<span class="p">}</span>

<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">unsafe</span> <span class="k">fn</span> <span class="nf">start</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="o">!</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">result</span> <span class="o">=</span> <span class="nf">demo_add</span><span class="p">();</span>
    <span class="nn">core</span><span class="p">::</span><span class="nn">ptr</span><span class="p">::</span><span class="nf">write_volatile</span><span class="p">(</span><span class="n">RESULT_ADDR</span> <span class="k">as</span> <span class="o">*</span><span class="k">mut</span> <span class="nb">u16</span><span class="p">,</span> <span class="n">result</span><span class="p">);</span>
    <span class="k">loop</span> <span class="p">{}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Compile to MSP430 and the rabbit hole opens immediately. Here’s what <code class="language-plaintext highlighter-rouge">rustc</code> emits for <code class="language-plaintext highlighter-rouge">demo_add</code>:</p>

<pre><code class="language-asm">demo_add:
    mov     #342, r12
    ret
</code></pre>

<p>Two instructions. LLVM constant-folded <code class="language-plaintext highlighter-rouge">100 + 200 + 42</code> into <code class="language-plaintext highlighter-rouge">342</code> at compile time. The addition doesn’t exist in the output—the compiler proved the answer is always the same and replaced the computation with a constant load.</p>

<p>The translator converts this to COR24:</p>

<pre><code class="language-asm">demo_add:
    la      r0, 0x000156    ; load 342 (24-bit)
    jmp     (r1)            ; return via r1
</code></pre>

<p>Run it in the emulator (<code class="language-plaintext highlighter-rouge">scripts/demo-add.sh</code>):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Executed 3 instructions
CPU halted (self-branch detected)

=== Registers ===
  r0:  0x000156  (     342)
</code></pre></div></div>

<p>Two instructions in <code class="language-plaintext highlighter-rouge">demo_add</code>, three total to reach halt. The “add” demo that doesn’t add.</p>

<hr />

<h2 id="level-15-more-variables-than-registers">Level 1.5: More Variables Than Registers</h2>

<p>What happens when Rust needs more live variables than COR24 has registers? MSP430 has 12 general-purpose registers. COR24 has 3. The translator has to spill the extras to the stack.</p>

<p>The <code class="language-plaintext highlighter-rouge">accumulate</code> function keeps 5 values alive simultaneously:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[inline(never)]</span>
<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">unsafe</span> <span class="k">fn</span> <span class="nf">accumulate</span><span class="p">(</span><span class="n">seed</span><span class="p">:</span> <span class="nb">u16</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">u16</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">a</span> <span class="o">=</span> <span class="n">seed</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">b</span> <span class="o">=</span> <span class="n">a</span> <span class="o">+</span> <span class="n">seed</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">c</span> <span class="o">=</span> <span class="n">b</span> <span class="o">+</span> <span class="n">a</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">d</span> <span class="o">=</span> <span class="n">c</span> <span class="o">+</span> <span class="n">b</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">e</span> <span class="o">=</span> <span class="n">d</span> <span class="o">+</span> <span class="n">c</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">result</span> <span class="o">=</span> <span class="n">a</span> <span class="o">^</span> <span class="n">b</span> <span class="o">^</span> <span class="n">c</span> <span class="o">^</span> <span class="n">d</span> <span class="o">^</span> <span class="n">e</span><span class="p">;</span>
    <span class="nf">mem_write</span><span class="p">(</span><span class="n">RESULT_ADDR</span><span class="p">,</span> <span class="n">result</span> <span class="k">as</span> <span class="nb">u8</span><span class="p">);</span>
    <span class="nf">uart_putc</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>
    <span class="nf">uart_putc</span><span class="p">(</span><span class="n">b</span><span class="p">);</span>
    <span class="nf">uart_putc</span><span class="p">(</span><span class="n">c</span><span class="p">);</span>
    <span class="nf">uart_putc</span><span class="p">(</span><span class="n">d</span><span class="p">);</span>
    <span class="nf">uart_putc</span><span class="p">(</span><span class="n">e</span><span class="p">);</span>
    <span class="k">loop</span> <span class="p">{}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The MSP430 assembly uses registers <code class="language-plaintext highlighter-rouge">r6</code> through <code class="language-plaintext highlighter-rouge">r10</code>—five registers that don’t exist on COR24:</p>

<pre><code class="language-asm">accumulate:
    push    r6
    push    r7
    push    r8
    push    r9
    push    r10             ; save 5 callee-saved registers
    mov     r12, r10        ; seed
    mov     r10, r6
    inc     r6              ; a = seed + 1
    add     r6, r10         ; b = a + seed
    mov     r10, r9
    add     r6, r9          ; c = b + a
    ...
</code></pre>

<p>The translator maps these to frame-pointer-relative stack slots, each 3 bytes (one COR24 word). Where MSP430 writes <code class="language-plaintext highlighter-rouge">mov r10, r6</code>, COR24 must load from one spill slot, operate, and store to another:</p>

<pre><code class="language-asm">accumulate:
    sw      r0, 30(fp)     ; spill seed (r10 → offset 30)
    lw      r0, 6(fp)      ; save spill slot for r6
    push    r0
    ...
    lw      r0, 18(fp)     ; load r10 (seed)
    sw      r0, 6(fp)      ; copy to r6 slot
    lw      r0, 6(fp)      ; load r6
    add     r0, 1          ; a = seed + 1
    sw      r0, 6(fp)      ; store r6 back
    ...
    la      r0, 0xFF0000   ; RESULT_ADDR
    ; call mmio_write
    push    r1
    la      r2, mmio_write
    jal     r1, (r2)       ; jal saves return addr in r1
    pop     r1
</code></pre>

<p>It’s verbose—the COR24 output is much longer than the MSP430 input. But it’s correct. The emulator confirms the computation with 148 instructions and the XOR result stored to memory at <code class="language-plaintext highlighter-rouge">0x0100</code>.</p>

<hr />

<h2 id="level-2-the-compiler-writes-your-destructor">Level 2: The Compiler Writes Your Destructor</h2>

<p>Rust’s Drop trait guarantees cleanup when a value goes out of scope. Does that work on a CPU with no OS, no allocator, no runtime?</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">struct</span> <span class="n">Guard</span> <span class="p">{</span> <span class="n">addr</span><span class="p">:</span> <span class="nb">u16</span> <span class="p">}</span>

<span class="k">impl</span> <span class="n">Guard</span> <span class="p">{</span>
    <span class="nd">#[inline(never)]</span>
    <span class="nd">#[no_mangle]</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">guard_new</span><span class="p">(</span><span class="n">addr</span><span class="p">:</span> <span class="nb">u16</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="n">Guard</span> <span class="p">{</span>
        <span class="k">unsafe</span> <span class="p">{</span> <span class="nf">mem_write</span><span class="p">(</span><span class="n">addr</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span> <span class="p">}</span>  <span class="c1">// mark: alive</span>
        <span class="n">Guard</span> <span class="p">{</span> <span class="n">addr</span> <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="k">impl</span> <span class="nb">Drop</span> <span class="k">for</span> <span class="n">Guard</span> <span class="p">{</span>
    <span class="nd">#[inline(never)]</span>
    <span class="k">fn</span> <span class="nf">drop</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="k">self</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">unsafe</span> <span class="p">{</span> <span class="nf">mem_write</span><span class="p">(</span><span class="k">self</span><span class="py">.addr</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span> <span class="p">}</span>  <span class="c1">// mark: gone</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">unsafe</span> <span class="k">fn</span> <span class="nf">start</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="o">!</span> <span class="p">{</span>
    <span class="p">{</span>
        <span class="k">let</span> <span class="n">_g</span> <span class="o">=</span> <span class="nn">Guard</span><span class="p">::</span><span class="nf">guard_new</span><span class="p">(</span><span class="n">STATUS_ADDR</span><span class="p">);</span>
        <span class="c1">// STATUS_ADDR = 1 (guard is alive)</span>
    <span class="p">}</span>
    <span class="c1">// STATUS_ADDR = 0 (compiler called drop here)</span>

    <span class="nf">mem_write</span><span class="p">(</span><span class="n">STATUS_ADDR</span><span class="p">,</span> <span class="mi">0xFF</span><span class="p">);</span>  <span class="c1">// proof we continued</span>
    <span class="k">loop</span> <span class="p">{}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Look at the MSP430 assembly for <code class="language-plaintext highlighter-rouge">start</code>—the compiler inserted the drop call:</p>

<pre><code class="language-asm">start:
    sub     #2, r1              ; allocate stack space
    mov     #256, r12           ; STATUS_ADDR
    call    #guard_new          ; create guard → writes 1
    mov     #256, 0(r1)         ; store Guard on stack
    mov     r1, r12             ; pass &amp;Guard to drop
    call    #&lt;Guard::drop&gt;      ; compiler-inserted! → writes 0
    mov     #256, r12
    mov     #255, r13
    call    #mem_write          ; writes 0xFF
.LBB4_1:
    jmp     .LBB4_1             ; halt
</code></pre>

<p>The <code class="language-plaintext highlighter-rouge">call #&lt;Guard::drop&gt;</code> on line 6 is the compiler honoring the Drop contract. You didn’t write that call—<code class="language-plaintext highlighter-rouge">rustc</code> did. Memory at <code class="language-plaintext highlighter-rouge">STATUS_ADDR</code> goes: <code class="language-plaintext highlighter-rouge">0 → 1 → 0 → 0xFF</code>, proving the destructor ran at the right moment.</p>

<p>The translated COR24 assembly preserves this structure—each <code class="language-plaintext highlighter-rouge">call</code> becomes a <code class="language-plaintext highlighter-rouge">jal</code> (jump-and-link), which saves the return address in <code class="language-plaintext highlighter-rouge">r1</code>:</p>

<pre><code class="language-asm">start:
    sub     sp, 3               ; allocate stack space
    la      r0, 0x000100        ; STATUS_ADDR
    ; call guard_new
    push    r1
    la      r2, guard_new
    jal     r1, (r2)            ; create guard → writes 1
    pop     r1
    ...
    ; call &lt;Guard::drop&gt;        ; compiler-inserted!
    push    r1
    la      r2, &lt;Guard::drop&gt;
    jal     r1, (r2)            ; → writes 0
    pop     r1
</code></pre>

<p>RAII works on bare metal, on an architecture the Rust compiler has never heard of.</p>

<hr />

<h2 id="level-3-interrupts-via-asm-passthrough">Level 3: Interrupts via <code class="language-plaintext highlighter-rouge">asm!</code> Passthrough</h2>

<p>Here’s where it gets interesting. COR24’s interrupt mechanism uses hardware registers that MSP430 doesn’t have:</p>

<ul>
  <li><strong>r6 (iv)</strong>: Interrupt vector—CPU jumps here when an interrupt fires</li>
  <li><strong>r7 (ir)</strong>: Interrupt return—saved PC to return to after the ISR</li>
  <li><strong><code class="language-plaintext highlighter-rouge">jmp (ir)</code></strong>: Return from interrupt</li>
</ul>

<p>There’s no MSP430 equivalent—the LLVM backend has no concept of these registers. But Rust’s <code class="language-plaintext highlighter-rouge">asm!</code> macro combined with the translator’s passthrough mechanism can handle it.</p>

<p>The <code class="language-plaintext highlighter-rouge">demo_echo_v2</code> example splits the problem: application logic in pure Rust, interrupt plumbing in <code class="language-plaintext highlighter-rouge">asm!</code> passthrough:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#![feature(asm_experimental_arch)]</span>

<span class="cd">/// Application logic --- pure Rust, compiled normally</span>
<span class="nd">#[inline(never)]</span>
<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">fn</span> <span class="nf">to_upper</span><span class="p">(</span><span class="n">ch</span><span class="p">:</span> <span class="nb">u16</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">u16</span> <span class="p">{</span>
    <span class="k">if</span> <span class="n">ch</span> <span class="o">&gt;=</span> <span class="mi">0x61</span> <span class="o">&amp;&amp;</span> <span class="n">ch</span> <span class="o">&lt;=</span> <span class="mi">0x7A</span> <span class="p">{</span>
        <span class="n">ch</span> <span class="o">&amp;</span> <span class="mi">0xDF</span>  <span class="c1">// clear bit 5</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="n">ch</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="nd">#[inline(never)]</span>
<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">unsafe</span> <span class="k">fn</span> <span class="nf">handle_rx</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">ch</span> <span class="o">=</span> <span class="nf">mmio_read</span><span class="p">(</span><span class="n">UART_DATA</span><span class="p">);</span>
    <span class="k">if</span> <span class="n">ch</span> <span class="o">==</span> <span class="mi">0x21</span> <span class="p">{</span>               <span class="c1">// '!'</span>
        <span class="nf">mmio_write</span><span class="p">(</span><span class="n">HALT_FLAG</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="nf">uart_putc</span><span class="p">(</span><span class="nf">to_upper</span><span class="p">(</span><span class="n">ch</span><span class="p">));</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The ISR wrapper uses <code class="language-plaintext highlighter-rouge">asm!</code> with a <code class="language-plaintext highlighter-rouge">@cor24:</code> prefix that the translator passes through verbatim:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">unsafe</span> <span class="k">fn</span> <span class="nf">isr_handler</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// Save COR24 state (asm! --- no Rust equivalent)</span>
    <span class="nn">core</span><span class="p">::</span><span class="nn">arch</span><span class="p">::</span><span class="nd">asm!</span><span class="p">(</span>
        <span class="s">"; @cor24: push r0"</span><span class="p">,</span>
        <span class="s">"; @cor24: push r1"</span><span class="p">,</span>
        <span class="s">"; @cor24: push r2"</span><span class="p">,</span>
        <span class="s">"; @cor24: mov r2, c"</span><span class="p">,</span>      <span class="c1">// save condition flag</span>
        <span class="s">"; @cor24: push r2"</span><span class="p">,</span>
    <span class="p">);</span>

    <span class="nf">handle_rx</span><span class="p">();</span>  <span class="c1">// ← pure Rust, compiled through the pipeline</span>

    <span class="c1">// Restore state and return from interrupt</span>
    <span class="nn">core</span><span class="p">::</span><span class="nn">arch</span><span class="p">::</span><span class="nd">asm!</span><span class="p">(</span>
        <span class="s">"; @cor24: pop r2"</span><span class="p">,</span>
        <span class="s">"; @cor24: clu z, r2"</span><span class="p">,</span>      <span class="c1">// restore condition flag</span>
        <span class="s">"; @cor24: pop r2"</span><span class="p">,</span>
        <span class="s">"; @cor24: pop r1"</span><span class="p">,</span>
        <span class="s">"; @cor24: pop r0"</span><span class="p">,</span>
        <span class="s">"; @cor24: jmp (ir)"</span><span class="p">,</span>       <span class="c1">// return from interrupt</span>
        <span class="nf">options</span><span class="p">(</span><span class="n">noreturn</span><span class="p">)</span>
    <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">"; @cor24: ..."</code> lines look like MSP430 comments (so <code class="language-plaintext highlighter-rouge">rustc</code> ignores them), but the translator recognizes the prefix and emits them as real COR24 instructions. In the final COR24 assembly:</p>

<pre><code class="language-asm">isr_handler:
    push r0
    push r1
    push r2
    mov r2, c                     ; save condition flag
    push r2
    ; call handle_rx              ← compiled Rust
    push    r1
    la      r2, handle_rx
    jal     r1, (r2)              ; jal saves return addr in r1
    pop     r1
    pop r2
    clu z, r2                     ; restore condition flag
    pop r2
    pop r1
    pop r0
    jmp (ir)                      ← hardware interrupt return
</code></pre>

<p>The <code class="language-plaintext highlighter-rouge">start</code> function sets up the interrupt vector and enables UART reception:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">core</span><span class="p">::</span><span class="nn">arch</span><span class="p">::</span><span class="nd">asm!</span><span class="p">(</span>
    <span class="s">"; @cor24: la r0, isr_handler"</span><span class="p">,</span>
    <span class="s">"; @cor24: mov r6, r0"</span><span class="p">,</span>         <span class="c1">// r6 = interrupt vector register</span>
    <span class="s">"; @cor24: lc r0, 1"</span><span class="p">,</span>
    <span class="s">"; @cor24: la r1, 0xFF0010"</span><span class="p">,</span>    <span class="c1">// UART interrupt enable register</span>
    <span class="s">"; @cor24: sb r0, 0(r1)"</span><span class="p">,</span>       <span class="c1">// enable UART RX interrupt</span>
<span class="p">);</span>
</code></pre></div></div>

<p>Type a letter, the hardware fires an interrupt, the ISR saves registers, calls <code class="language-plaintext highlighter-rouge">handle_rx</code> (compiled Rust), converts to uppercase, echoes it via UART, restores registers, and returns. The boundary between Rust and hardware is exactly where you’d expect it.</p>

<hr />

<h2 id="what-the-translator-actually-does">What the Translator Actually Does</h2>

<p>The <code class="language-plaintext highlighter-rouge">msp430-to-cor24</code> translator (~1,800 lines of Rust) handles the mechanical differences:</p>

<table>
  <thead>
    <tr>
      <th>Concern</th>
      <th>MSP430</th>
      <th>COR24</th>
      <th>Translation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Word size</strong></td>
      <td>16-bit</td>
      <td>24-bit</td>
      <td>Stack slots: 2 bytes → 3 bytes</td>
    </tr>
    <tr>
      <td><strong>Registers</strong></td>
      <td>r12-r14 (args)</td>
      <td>r0-r2 (args)</td>
      <td>Direct mapping</td>
    </tr>
    <tr>
      <td><strong>Spilled regs</strong></td>
      <td>r4-r11</td>
      <td>None</td>
      <td>Frame-pointer relative loads/stores</td>
    </tr>
    <tr>
      <td><strong>I/O addresses</strong></td>
      <td>16-bit (0xFF00)</td>
      <td>24-bit (0xFF0000)</td>
      <td>Address remapping</td>
    </tr>
    <tr>
      <td><strong>Call convention</strong></td>
      <td><code class="language-plaintext highlighter-rouge">call #func</code> / <code class="language-plaintext highlighter-rouge">ret</code></td>
      <td><code class="language-plaintext highlighter-rouge">jal r1, (r2)</code> / <code class="language-plaintext highlighter-rouge">jmp (r1)</code></td>
      <td>Uses COR24’s jump-and-link</td>
    </tr>
    <tr>
      <td><strong>Tail calls</strong></td>
      <td><code class="language-plaintext highlighter-rouge">call</code> + <code class="language-plaintext highlighter-rouge">ret</code> pattern</td>
      <td><code class="language-plaintext highlighter-rouge">jmp (r2)</code></td>
      <td>Direct jump, no link</td>
    </tr>
    <tr>
      <td><strong>Entry point</strong></td>
      <td>Section order</td>
      <td>Reset vector at addr 0</td>
      <td><code class="language-plaintext highlighter-rouge">la r0, start</code> + <code class="language-plaintext highlighter-rouge">jmp (r0)</code></td>
    </tr>
  </tbody>
</table>

<p><strong>COR24’s calling convention</strong> centers on the <code class="language-plaintext highlighter-rouge">jal</code> (jump-and-link) instruction. <code class="language-plaintext highlighter-rouge">jal r1, (r2)</code> jumps to the address in <code class="language-plaintext highlighter-rouge">r2</code> and saves the return address in <code class="language-plaintext highlighter-rouge">r1</code>. The callee returns with <code class="language-plaintext highlighter-rouge">jmp (r1)</code>. Since the translator re-uses <code class="language-plaintext highlighter-rouge">r1</code> for the return address, it saves and restores <code class="language-plaintext highlighter-rouge">r1</code> around each call with <code class="language-plaintext highlighter-rouge">push r1</code> / <code class="language-plaintext highlighter-rouge">pop r1</code>. COR24’s native C compiler convention uses a standard prologue (<code class="language-plaintext highlighter-rouge">push fp; push r2; push r1; mov fp,sp</code>) and passes arguments on the stack—the translator doesn’t follow that full protocol, but it does use the same <code class="language-plaintext highlighter-rouge">jal</code>/<code class="language-plaintext highlighter-rouge">jmp (r1)</code> mechanism for call and return.</p>

<p>The entry point handling is worth noting. <code class="language-plaintext highlighter-rouge">rustc</code> emits functions in alphabetical section order, so the panic handler often lands at address 0. Every demo uses a <code class="language-plaintext highlighter-rouge">#[no_mangle] pub unsafe fn start() -&gt; !</code> as its entry point—a convention I chose for this project. The translator looks for a <code class="language-plaintext highlighter-rouge">start</code> label and emits a reset vector prologue (<code class="language-plaintext highlighter-rouge">la r0, start</code> + <code class="language-plaintext highlighter-rouge">jmp (r0)</code> at address 0), mimicking how real microcontrollers boot. This isn’t a Rust or MSP430 convention; it’s a project-level rule that keeps the translator simple and every demo consistent.</p>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Prerequisites</span>
rustup toolchain <span class="nb">install </span>nightly
rustup target add msp430-none-elf <span class="nt">--toolchain</span> nightly

<span class="c"># Clone and build</span>
git clone https://github.com/sw-embed/cor24-rs.git
<span class="nb">cd </span>cor24-rs/rust-to-cor24

<span class="c"># Run the add demo (full pipeline)</span>
bash scripts/demo-add.sh

<span class="c"># Run the UART hello demo</span>
bash scripts/demo-uart-hello.sh

<span class="c"># Or compile any demo project</span>
cargo run <span class="nt">--bin</span> msp430-to-cor24 <span class="nt">--</span> <span class="nt">--compile</span> demos/demo_drop
</code></pre></div></div>

<p>Each demo script traces the full pipeline: Rust source → MSP430 assembly → COR24 assembly → emulator output with register dumps.</p>

<hr />

<h2 id="key-takeaways">Key Takeaways</h2>

<ol>
  <li>
    <p><strong>You don’t need a compiler backend</strong> to target a new architecture. If a similar-enough target exists, a translator can bridge the gap.</p>
  </li>
  <li>
    <p><strong>The Rust compiler is surprisingly good</strong> at bare-metal code. Constant folding, dead code elimination, and Drop all work correctly even when the output gets re-targeted to an architecture LLVM has never seen.</p>
  </li>
  <li>
    <p><strong><code class="language-plaintext highlighter-rouge">asm!</code> passthrough is the escape hatch.</strong> Hardware-specific operations (interrupt setup, condition flag save/restore) bypass the translation layer entirely using comment-prefix conventions.</p>
  </li>
  <li>
    <p><strong>The pipeline is auditable.</strong> Every intermediate artifact (<code class="language-plaintext highlighter-rouge">.msp430.s</code>, <code class="language-plaintext highlighter-rouge">.cor24.s</code>, register dumps) is human-readable. You can trace any behavior from source to silicon.</p>
  </li>
</ol>

<hr />

<p><em>The rabbit hole goes deeper. Next time: what happens when the 16-bit intermediate can’t express a 24-bit value.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="embedded-systems" /><category term="rust" /><category term="rust" /><category term="assembly" /><category term="msp430" /><category term="cor24" /><category term="embedded" /><category term="compiler" /><category term="translation" /><summary type="html"><![CDATA[You want to write Rust for a CPU that rustc doesn’t know about. There’s no LLVM backend, no target triple, no supported tier. You could write a compiler backend from scratch—or you could cheat. This post traces the rabbit hole from Rust source code down to registers on the COR24, a 24-bit RISC CPU that exists on real FPGA hardware. The trick: target a CPU that rustc does support (MSP430, a 16-bit TI microcontroller), then translate the assembly output. Resource Link Live Demo COR24 Assembly Emulator Source GitHub MakerLisp makerlisp.com (COR24 creators) Disclaimer Proof of concept only Comments Discord The Problem COR24 is a 24-bit RISC soft CPU designed by MakerLisp for FPGAs. It has 3 general-purpose registers, a 24-bit address space, and runs at 101 MHz on inexpensive Lattice FPGAs. It’s MIT-licensed, well-documented, and has real hardware you can buy. But LLVM doesn’t have a COR24 backend. Neither does GCC. Writing a full compiler backend could take a lot more effort. We need another way in. The Trick: Borrow a Target rustc supports MSP430, a 16-bit TI microcontroller, via LLVM’s MSP430 backend. It’s a nightly-only target (msp430-none-elf), but it works. The key insight: MSP430’s instruction set is close enough to COR24 that a translator can bridge the gap. The full pipeline: Rust Source (.rs) ↓ rustc --target msp430-none-elf --emit asm MSP430 Assembly (.msp430.s) ↓ msp430-to-cor24 translator COR24 Assembly (.cor24.s) ↓ COR24 assembler Machine Code → COR24 Emulator (or real FPGA hardware) No custom compiler. No modified LLVM. Just Rust’s nightly toolchain and a ~1,800-line translator written in Rust. This is a proof-of-concept—an educational demo, not a production tool for real COR24 hardware. Disclaimer This is a proof-of-concept and educational demo. The MSP430-to-COR24 translator is not intended for production use on real COR24 hardware. It demonstrates that the approach is feasible, not that it’s complete or reliable. If you’re building something real on COR24, use the native assembler and toolchain from MakerLisp. Level 1: The Compiler Optimizes Away Your Code Let’s start simple. Three numbers, one add: #![no_std] const RESULT_ADDR: u16 = 0x0100; #[inline(never)] #[no_mangle] pub fn demo_add() -&gt; u16 { let a: u16 = 100; let b: u16 = 200; let c: u16 = 42; a + b + c // Returns 342 } #[no_mangle] pub unsafe fn start() -&gt; ! { let result = demo_add(); core::ptr::write_volatile(RESULT_ADDR as *mut u16, result); loop {} } Compile to MSP430 and the rabbit hole opens immediately. Here’s what rustc emits for demo_add: demo_add: mov #342, r12 ret Two instructions. LLVM constant-folded 100 + 200 + 42 into 342 at compile time. The addition doesn’t exist in the output—the compiler proved the answer is always the same and replaced the computation with a constant load. The translator converts this to COR24: demo_add: la r0, 0x000156 ; load 342 (24-bit) jmp (r1) ; return via r1 Run it in the emulator (scripts/demo-add.sh): Executed 3 instructions CPU halted (self-branch detected) === Registers === r0: 0x000156 ( 342) Two instructions in demo_add, three total to reach halt. The “add” demo that doesn’t add. Level 1.5: More Variables Than Registers What happens when Rust needs more live variables than COR24 has registers? MSP430 has 12 general-purpose registers. COR24 has 3. The translator has to spill the extras to the stack. The accumulate function keeps 5 values alive simultaneously: #[inline(never)] #[no_mangle] pub unsafe fn accumulate(seed: u16) -&gt; u16 { let a = seed + 1; let b = a + seed; let c = b + a; let d = c + b; let e = d + c; let result = a ^ b ^ c ^ d ^ e; mem_write(RESULT_ADDR, result as u8); uart_putc(a); uart_putc(b); uart_putc(c); uart_putc(d); uart_putc(e); loop {} } The MSP430 assembly uses registers r6 through r10—five registers that don’t exist on COR24: accumulate: push r6 push r7 push r8 push r9 push r10 ; save 5 callee-saved registers mov r12, r10 ; seed mov r10, r6 inc r6 ; a = seed + 1 add r6, r10 ; b = a + seed mov r10, r9 add r6, r9 ; c = b + a ... The translator maps these to frame-pointer-relative stack slots, each 3 bytes (one COR24 word). Where MSP430 writes mov r10, r6, COR24 must load from one spill slot, operate, and store to another: accumulate: sw r0, 30(fp) ; spill seed (r10 → offset 30) lw r0, 6(fp) ; save spill slot for r6 push r0 ... lw r0, 18(fp) ; load r10 (seed) sw r0, 6(fp) ; copy to r6 slot lw r0, 6(fp) ; load r6 add r0, 1 ; a = seed + 1 sw r0, 6(fp) ; store r6 back ... la r0, 0xFF0000 ; RESULT_ADDR ; call mmio_write push r1 la r2, mmio_write jal r1, (r2) ; jal saves return addr in r1 pop r1 It’s verbose—the COR24 output is much longer than the MSP430 input. But it’s correct. The emulator confirms the computation with 148 instructions and the XOR result stored to memory at 0x0100. Level 2: The Compiler Writes Your Destructor Rust’s Drop trait guarantees cleanup when a value goes out of scope. Does that work on a CPU with no OS, no allocator, no runtime? pub struct Guard { addr: u16 } impl Guard { #[inline(never)] #[no_mangle] pub fn guard_new(addr: u16) -&gt; Guard { unsafe { mem_write(addr, 1); } // mark: alive Guard { addr } } } impl Drop for Guard { #[inline(never)] fn drop(&amp;mut self) { unsafe { mem_write(self.addr, 0); } // mark: gone } } #[no_mangle] pub unsafe fn start() -&gt; ! { { let _g = Guard::guard_new(STATUS_ADDR); // STATUS_ADDR = 1 (guard is alive) } // STATUS_ADDR = 0 (compiler called drop here) mem_write(STATUS_ADDR, 0xFF); // proof we continued loop {} } Look at the MSP430 assembly for start—the compiler inserted the drop call: start: sub #2, r1 ; allocate stack space mov #256, r12 ; STATUS_ADDR call #guard_new ; create guard → writes 1 mov #256, 0(r1) ; store Guard on stack mov r1, r12 ; pass &amp;Guard to drop call #&lt;Guard::drop&gt; ; compiler-inserted! → writes 0 mov #256, r12 mov #255, r13 call #mem_write ; writes 0xFF .LBB4_1: jmp .LBB4_1 ; halt The call #&lt;Guard::drop&gt; on line 6 is the compiler honoring the Drop contract. You didn’t write that call—rustc did. Memory at STATUS_ADDR goes: 0 → 1 → 0 → 0xFF, proving the destructor ran at the right moment. The translated COR24 assembly preserves this structure—each call becomes a jal (jump-and-link), which saves the return address in r1: start: sub sp, 3 ; allocate stack space la r0, 0x000100 ; STATUS_ADDR ; call guard_new push r1 la r2, guard_new jal r1, (r2) ; create guard → writes 1 pop r1 ... ; call &lt;Guard::drop&gt; ; compiler-inserted! push r1 la r2, &lt;Guard::drop&gt; jal r1, (r2) ; → writes 0 pop r1 RAII works on bare metal, on an architecture the Rust compiler has never heard of. Level 3: Interrupts via asm! Passthrough Here’s where it gets interesting. COR24’s interrupt mechanism uses hardware registers that MSP430 doesn’t have: r6 (iv): Interrupt vector—CPU jumps here when an interrupt fires r7 (ir): Interrupt return—saved PC to return to after the ISR jmp (ir): Return from interrupt There’s no MSP430 equivalent—the LLVM backend has no concept of these registers. But Rust’s asm! macro combined with the translator’s passthrough mechanism can handle it. The demo_echo_v2 example splits the problem: application logic in pure Rust, interrupt plumbing in asm! passthrough: #![feature(asm_experimental_arch)] /// Application logic --- pure Rust, compiled normally #[inline(never)] #[no_mangle] pub fn to_upper(ch: u16) -&gt; u16 { if ch &gt;= 0x61 &amp;&amp; ch &lt;= 0x7A { ch &amp; 0xDF // clear bit 5 } else { ch } } #[inline(never)] #[no_mangle] pub unsafe fn handle_rx() { let ch = mmio_read(UART_DATA); if ch == 0x21 { // '!' mmio_write(HALT_FLAG, 1); } else { uart_putc(to_upper(ch)); } } The ISR wrapper uses asm! with a @cor24: prefix that the translator passes through verbatim: #[no_mangle] pub unsafe fn isr_handler() { // Save COR24 state (asm! --- no Rust equivalent) core::arch::asm!( "; @cor24: push r0", "; @cor24: push r1", "; @cor24: push r2", "; @cor24: mov r2, c", // save condition flag "; @cor24: push r2", ); handle_rx(); // ← pure Rust, compiled through the pipeline // Restore state and return from interrupt core::arch::asm!( "; @cor24: pop r2", "; @cor24: clu z, r2", // restore condition flag "; @cor24: pop r2", "; @cor24: pop r1", "; @cor24: pop r0", "; @cor24: jmp (ir)", // return from interrupt options(noreturn) ); } The "; @cor24: ..." lines look like MSP430 comments (so rustc ignores them), but the translator recognizes the prefix and emits them as real COR24 instructions. In the final COR24 assembly: isr_handler: push r0 push r1 push r2 mov r2, c ; save condition flag push r2 ; call handle_rx ← compiled Rust push r1 la r2, handle_rx jal r1, (r2) ; jal saves return addr in r1 pop r1 pop r2 clu z, r2 ; restore condition flag pop r2 pop r1 pop r0 jmp (ir) ← hardware interrupt return The start function sets up the interrupt vector and enables UART reception: core::arch::asm!( "; @cor24: la r0, isr_handler", "; @cor24: mov r6, r0", // r6 = interrupt vector register "; @cor24: lc r0, 1", "; @cor24: la r1, 0xFF0010", // UART interrupt enable register "; @cor24: sb r0, 0(r1)", // enable UART RX interrupt ); Type a letter, the hardware fires an interrupt, the ISR saves registers, calls handle_rx (compiled Rust), converts to uppercase, echoes it via UART, restores registers, and returns. The boundary between Rust and hardware is exactly where you’d expect it. What the Translator Actually Does The msp430-to-cor24 translator (~1,800 lines of Rust) handles the mechanical differences: Concern MSP430 COR24 Translation Word size 16-bit 24-bit Stack slots: 2 bytes → 3 bytes Registers r12-r14 (args) r0-r2 (args) Direct mapping Spilled regs r4-r11 None Frame-pointer relative loads/stores I/O addresses 16-bit (0xFF00) 24-bit (0xFF0000) Address remapping Call convention call #func / ret jal r1, (r2) / jmp (r1) Uses COR24’s jump-and-link Tail calls call + ret pattern jmp (r2) Direct jump, no link Entry point Section order Reset vector at addr 0 la r0, start + jmp (r0) COR24’s calling convention centers on the jal (jump-and-link) instruction. jal r1, (r2) jumps to the address in r2 and saves the return address in r1. The callee returns with jmp (r1). Since the translator re-uses r1 for the return address, it saves and restores r1 around each call with push r1 / pop r1. COR24’s native C compiler convention uses a standard prologue (push fp; push r2; push r1; mov fp,sp) and passes arguments on the stack—the translator doesn’t follow that full protocol, but it does use the same jal/jmp (r1) mechanism for call and return. The entry point handling is worth noting. rustc emits functions in alphabetical section order, so the panic handler often lands at address 0. Every demo uses a #[no_mangle] pub unsafe fn start() -&gt; ! as its entry point—a convention I chose for this project. The translator looks for a start label and emits a reset vector prologue (la r0, start + jmp (r0) at address 0), mimicking how real microcontrollers boot. This isn’t a Rust or MSP430 convention; it’s a project-level rule that keeps the translator simple and every demo consistent. Try It Yourself # Prerequisites rustup toolchain install nightly rustup target add msp430-none-elf --toolchain nightly # Clone and build git clone https://github.com/sw-embed/cor24-rs.git cd cor24-rs/rust-to-cor24 # Run the add demo (full pipeline) bash scripts/demo-add.sh # Run the UART hello demo bash scripts/demo-uart-hello.sh # Or compile any demo project cargo run --bin msp430-to-cor24 -- --compile demos/demo_drop Each demo script traces the full pipeline: Rust source → MSP430 assembly → COR24 assembly → emulator output with register dumps. Key Takeaways You don’t need a compiler backend to target a new architecture. If a similar-enough target exists, a translator can bridge the gap. The Rust compiler is surprisingly good at bare-metal code. Constant folding, dead code elimination, and Drop all work correctly even when the output gets re-targeted to an architecture LLVM has never seen. asm! passthrough is the escape hatch. Hardware-specific operations (interrupt setup, condition flag save/restore) bypass the translation layer entirely using comment-prefix conventions. The pipeline is auditable. Every intermediate artifact (.msp430.s, .cor24.s, register dumps) is human-readable. You can trace any behavior from source to silicon. The rabbit hole goes deeper. Next time: what happens when the 16-bit intermediate can’t express a 24-bit value.]]></summary></entry><entry><title type="html">ML Frontier #01: Neural Collapse</title><link href="https://software-wrighter-lab.github.io/2026/03/09/mlf-01-neural-collapse/" rel="alternate" type="text/html" title="ML Frontier #01: Neural Collapse" /><published>2026-03-09T01:15:00-07:00</published><updated>2026-03-09T01:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/09/mlf-01-neural-collapse</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/09/mlf-01-neural-collapse/"><![CDATA[<p><img src="/assets/images/posts/block-neural-collapse.png" class="post-marker" alt="" style="width: 120px;" /></p>

<p>First Machine Learning Mondays post. ML Frontier explores cutting-edge research in machine learning.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Papers</strong></td>
        <td><a href="#papers">5 papers covered</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/shorts/ElFwTSuHjnM">ML Frontier #01: Neural Collapse</a><br /><a href="https://www.youtube.com/shorts/ElFwTSuHjnM"><img src="https://img.youtube.com/vi/ElFwTSuHjnM/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="what-is-neural-collapse">What is Neural Collapse?</h2>

<p>During the final phase of training, deep network representations converge to a specific geometric pattern. Class means become equidistant and form a <strong>symmetric simplex structure</strong> in the feature space.</p>

<p>Think of it like points arranging themselves at equal distances on a sphere.</p>

<h2 id="why-does-this-happen">Why Does This Happen?</h2>

<p>When networks are trained past zero training error, representations continue simplifying. The network finds the most symmetric way to separate classes, forming equal angles between all class centers.</p>

<p>This isn’t random—it’s mathematically optimal.</p>

<h2 id="2024-2025-breakthroughs">2024-2025 Breakthroughs</h2>

<p>Recent research proves neural collapse is globally optimal in deep transformers and ResNets with regularization. As depth increases, the network provably converges to this collapsed geometry.</p>

<p>This changes how we think about deep learning. Collapse explains why overparameterized networks generalize well. It also guides continual learning, where progressive collapse prevents catastrophic forgetting.</p>

<h2 id="papers">Papers</h2>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Paper</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Sep 2024</td>
      <td>Beyond Unconstrained Features: Neural Collapse for Shallow Networks</td>
      <td><a href="https://arxiv.org/abs/2409.01832">arXiv 2409.01832</a></td>
    </tr>
    <tr>
      <td>Oct 2024</td>
      <td>Wide Neural Networks with Weight Decay Provably Exhibit Neural Collapse</td>
      <td><a href="https://arxiv.org/abs/2410.04887">arXiv 2410.04887</a></td>
    </tr>
    <tr>
      <td>Jan 2025</td>
      <td>Neural Collapse Beyond the Unconstrained Features Model</td>
      <td><a href="https://arxiv.org/abs/2501.19104">arXiv 2501.19104</a></td>
    </tr>
    <tr>
      <td>May 2025</td>
      <td>Rethinking Continual Learning with Progressive Neural Collapse</td>
      <td><a href="https://arxiv.org/abs/2505.24254">arXiv 2505.24254</a></td>
    </tr>
    <tr>
      <td>May 2025</td>
      <td>Neural Collapse is Globally Optimal in Deep Regularized ResNets and Transformers</td>
      <td><a href="https://arxiv.org/abs/2505.15239">arXiv 2505.15239</a></td>
    </tr>
  </tbody>
</table>

<h2 id="paper-summaries">Paper Summaries</h2>

<h3 id="beyond-unconstrained-features-sep-2024">Beyond Unconstrained Features (Sep 2024)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2409.01832">Hong &amp; Ling</a></p>

<p><strong>In brief:</strong> Most neural collapse theory assumes you can put class markers anywhere you want—like sticking Post-it notes anywhere on a wall. But real shallow networks have limits. This paper shows neural collapse still emerges even in small networks with real data constraints, not just idealized deep networks.</p>

<p><strong>Why it matters:</strong> Neural collapse isn’t just a “big model” phenomenon—it happens in smaller, practical architectures too.</p>

<h3 id="wide-networks-with-weight-decay-oct-2024">Wide Networks with Weight Decay (Oct 2024)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2410.04887">Jacot, Súkeník, Wang &amp; Mondelli</a></p>

<p><strong>In brief:</strong> If you train a wide neural network with weight decay (a common regularization trick), this paper proves the network <em>will</em> exhibit neural collapse. It’s the first proof that end-to-end training (not just theory) leads to collapse.</p>

<p><strong>Why it matters:</strong> Weight decay isn’t just preventing overfitting—it’s actively pushing the network toward optimal geometry.</p>

<h3 id="beyond-the-unconstrained-features-model-jan-2025">Beyond the Unconstrained Features Model (Jan 2025)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2501.19104">arXiv 2501.19104</a></p>

<p><strong>In brief:</strong> The “unconstrained features model” assumes networks can place representations anywhere. Real networks have architectural constraints. This paper extends neural collapse theory to realistic settings where the architecture limits what’s possible.</p>

<p><strong>Why it matters:</strong> The theory holds up in real-world conditions, not just toy examples.</p>

<h3 id="progressive-neural-collapse-for-continual-learning-may-2025">Progressive Neural Collapse for Continual Learning (May 2025)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2505.24254">arXiv 2505.24254</a></p>

<p><strong>In brief:</strong> When you teach a network new things, it often forgets old things (catastrophic forgetting). This paper uses neural collapse to fix that: by carefully managing how class representations “collapse” over time, the network can keep learning new tasks without losing old knowledge.</p>

<p><strong>Why it matters:</strong> Neural collapse isn’t just a curiosity—it’s a tool for building better learning systems.</p>

<h3 id="globally-optimal-in-transformers-and-resnets-may-2025">Globally Optimal in Transformers and ResNets (May 2025)</h3>

<p><strong>Paper:</strong> <a href="https://arxiv.org/abs/2505.15239">Súkeník et al.</a></p>

<p><strong>In brief:</strong> Imagine you have a box of crayons and need to organize them so each color is as far from every other color as possible. This paper proves that deep neural networks automatically find the <em>best possible</em> arrangement—not just a good one, but the mathematically perfect one. And this happens in both transformers (like GPT) and ResNets (like image classifiers).</p>

<p><strong>Why it matters:</strong> It’s not a coincidence that networks learn this way. It’s provably optimal.</p>

<h2 id="key-takeaways">Key Takeaways</h2>

<table>
  <thead>
    <tr>
      <th>Concept</th>
      <th>One-liner</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Neural Collapse</strong></td>
      <td>Class representations converge to symmetric simplex geometry</td>
    </tr>
    <tr>
      <td><strong>Why It Happens</strong></td>
      <td>Training past zero error simplifies representations maximally</td>
    </tr>
    <tr>
      <td><strong>Optimal Geometry</strong></td>
      <td>Provably the best configuration in deep networks</td>
    </tr>
    <tr>
      <td><strong>Practical Impact</strong></td>
      <td>Explains generalization and enables continual learning</td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>Neural collapse reveals the hidden geometry of learning. Follow for more ML Frontier episodes exploring cutting-edge research.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="machine-learning" /><category term="research" /><category term="explainers" /><category term="ml-frontier" /><category term="neural-collapse" /><category term="deep-learning" /><category term="representation-learning" /><category term="transformers" /><category term="resnets" /><category term="geometry" /><summary type="html"><![CDATA[First Machine Learning Mondays post. ML Frontier explores cutting-edge research in machine learning. Resource Link Papers 5 papers covered Video ML Frontier #01: Neural Collapse Comments Discord What is Neural Collapse? During the final phase of training, deep network representations converge to a specific geometric pattern. Class means become equidistant and form a symmetric simplex structure in the feature space. Think of it like points arranging themselves at equal distances on a sphere. Why Does This Happen? When networks are trained past zero training error, representations continue simplifying. The network finds the most symmetric way to separate classes, forming equal angles between all class centers. This isn’t random—it’s mathematically optimal. 2024-2025 Breakthroughs Recent research proves neural collapse is globally optimal in deep transformers and ResNets with regularization. As depth increases, the network provably converges to this collapsed geometry. This changes how we think about deep learning. Collapse explains why overparameterized networks generalize well. It also guides continual learning, where progressive collapse prevents catastrophic forgetting. Papers Date Paper Link Sep 2024 Beyond Unconstrained Features: Neural Collapse for Shallow Networks arXiv 2409.01832 Oct 2024 Wide Neural Networks with Weight Decay Provably Exhibit Neural Collapse arXiv 2410.04887 Jan 2025 Neural Collapse Beyond the Unconstrained Features Model arXiv 2501.19104 May 2025 Rethinking Continual Learning with Progressive Neural Collapse arXiv 2505.24254 May 2025 Neural Collapse is Globally Optimal in Deep Regularized ResNets and Transformers arXiv 2505.15239 Paper Summaries Beyond Unconstrained Features (Sep 2024) Paper: Hong &amp; Ling In brief: Most neural collapse theory assumes you can put class markers anywhere you want—like sticking Post-it notes anywhere on a wall. But real shallow networks have limits. This paper shows neural collapse still emerges even in small networks with real data constraints, not just idealized deep networks. Why it matters: Neural collapse isn’t just a “big model” phenomenon—it happens in smaller, practical architectures too. Wide Networks with Weight Decay (Oct 2024) Paper: Jacot, Súkeník, Wang &amp; Mondelli In brief: If you train a wide neural network with weight decay (a common regularization trick), this paper proves the network will exhibit neural collapse. It’s the first proof that end-to-end training (not just theory) leads to collapse. Why it matters: Weight decay isn’t just preventing overfitting—it’s actively pushing the network toward optimal geometry. Beyond the Unconstrained Features Model (Jan 2025) Paper: arXiv 2501.19104 In brief: The “unconstrained features model” assumes networks can place representations anywhere. Real networks have architectural constraints. This paper extends neural collapse theory to realistic settings where the architecture limits what’s possible. Why it matters: The theory holds up in real-world conditions, not just toy examples. Progressive Neural Collapse for Continual Learning (May 2025) Paper: arXiv 2505.24254 In brief: When you teach a network new things, it often forgets old things (catastrophic forgetting). This paper uses neural collapse to fix that: by carefully managing how class representations “collapse” over time, the network can keep learning new tasks without losing old knowledge. Why it matters: Neural collapse isn’t just a curiosity—it’s a tool for building better learning systems. Globally Optimal in Transformers and ResNets (May 2025) Paper: Súkeník et al. In brief: Imagine you have a box of crayons and need to organize them so each color is as far from every other color as possible. This paper proves that deep neural networks automatically find the best possible arrangement—not just a good one, but the mathematically perfect one. And this happens in both transformers (like GPT) and ResNets (like image classifiers). Why it matters: It’s not a coincidence that networks learn this way. It’s provably optimal. Key Takeaways Concept One-liner Neural Collapse Class representations converge to symmetric simplex geometry Why It Happens Training past zero error simplifies representations maximally Optimal Geometry Provably the best configuration in deep networks Practical Impact Explains generalization and enables continual learning Neural collapse reveals the hidden geometry of learning. Follow for more ML Frontier episodes exploring cutting-edge research.]]></summary></entry><entry><title type="html">Saw (1/?): pjmai-rs, Rig, and langchain-rust</title><link href="https://software-wrighter-lab.github.io/2026/03/08/saw-pjmai-rs-rig-langchain/" rel="alternate" type="text/html" title="Saw (1/?): pjmai-rs, Rig, and langchain-rust" /><published>2026-03-08T11:00:00-07:00</published><updated>2026-03-08T11:00:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/03/08/saw-pjmai-rs-rig-langchain</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/08/saw-pjmai-rs-rig-langchain/"><![CDATA[<p><img src="/assets/images/posts/block-sharp-tools.png" class="post-marker no-invert" alt="" /></p>

<p>Three tools, one theme: sharpening the foundation. This week covers pjmai-rs bug fixes and new features, plus a first look at two Rust frameworks for building LLM-powered applications—Rig and langchain-rust.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repos</strong></td>
        <td><a href="https://github.com/sw-cli-tools/pjmai-rs">sw-cli-tools/pjmai-rs</a>, <a href="https://github.com/softwarewrighter/try-rig">try-rig</a>, <a href="https://github.com/softwarewrighter/try-langchain-rust">try-langchain-rust</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://youtu.be/CqdBxXXOeUs">Explainer</a></td>
      </tr>
      <tr>
        <td><strong>References</strong></td>
        <td><a href="#references">Links and Resources</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="pjmai-rs-fixing-the-foundation">pjmai-rs: Fixing the Foundation</h2>

<p>Before adding AI features to any tool, you need a solid foundation. Since the last update, pjmai-rs received critical fixes and practical new features.</p>

<h3 id="the-rust-2024-edition-bug">The Rust 2024 Edition Bug</h3>

<p>Upgrading to Rust edition 2024 broke project removal silently. The <code class="language-plaintext highlighter-rouge">IndexMap::remove()</code> method changed semantics—it now preserves insertion order differently. The fix:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Rust 2024 broke this</span>
<span class="o">-</span> <span class="n">projects</span><span class="nf">.remove</span><span class="p">(</span><span class="n">name</span><span class="p">)</span>

<span class="c1">// shift_remove maintains expected behavior</span>
<span class="o">+</span> <span class="n">projects</span><span class="nf">.shift_remove</span><span class="p">(</span><span class="n">name</span><span class="p">)</span>
</code></pre></div></div>

<p>A one-line fix, but the kind that silently corrupts data if you miss it. The 2024 edition migration guide mentions this change, but it’s easy to overlook in a large codebase.</p>

<h3 id="shell-integration-improvements">Shell Integration Improvements</h3>

<ul>
  <li><strong>Help flags</strong>: All aliases (<code class="language-plaintext highlighter-rouge">chpj</code>, <code class="language-plaintext highlighter-rouge">hypj</code>, <code class="language-plaintext highlighter-rouge">stpj</code>, etc.) now properly pass <code class="language-plaintext highlighter-rouge">--help</code> through</li>
  <li><strong>After-help messages</strong>: Every subcommand shows examples and related commands</li>
  <li><strong>Version matching</strong>: <code class="language-plaintext highlighter-rouge">--version</code> output now matches <code class="language-plaintext highlighter-rouge">Cargo.toml</code></li>
  <li><strong>Argument validation</strong>: Better error messages for invalid flag combinations</li>
</ul>

<h3 id="new-capabilities">New Capabilities</h3>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>Command</th>
      <th>What It Does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stack navigation</td>
      <td><code class="language-plaintext highlighter-rouge">stpj</code></td>
      <td>Push/pop project context with visibility</td>
    </tr>
    <tr>
      <td>History tracking</td>
      <td><code class="language-plaintext highlighter-rouge">hypj</code></td>
      <td>Revisit recently-visited projects by number</td>
    </tr>
    <tr>
      <td>Fuzzy completion</td>
      <td><code class="language-plaintext highlighter-rouge">chpj &lt;TAB&gt;</code></td>
      <td>Prefix &gt; segment &gt; substring, sorted by recency</td>
    </tr>
    <tr>
      <td>Environment config</td>
      <td><code class="language-plaintext highlighter-rouge">evpj</code></td>
      <td>Per-project env vars with auto-detection (Python, Node, Rust)</td>
    </tr>
    <tr>
      <td>Bulk operations</td>
      <td><code class="language-plaintext highlighter-rouge">rmpj --all</code></td>
      <td>Batch management with confirmation</td>
    </tr>
    <tr>
      <td>Subdirectory nav</td>
      <td><code class="language-plaintext highlighter-rouge">chpj proj src/</code></td>
      <td>Tab-complete into subdirs</td>
    </tr>
  </tbody>
</table>

<p>These features are covered in detail in the <a href="/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion/">Personal Software post</a>.</p>

<div class="resource-box">

  <p><strong>Sharpen the Saw</strong> — Habit 7 from Stephen Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People"><em>The 7 Habits of Highly Effective People</em></a> is about preserving and enhancing your greatest asset: yourself and your tools. In software, that means taking time to fix accumulated friction, update dependencies, and learn new frameworks—even when shipping features feels more urgent. The payoff compounds: every hour spent sharpening saves many more down the line.</p>

</div>

<h2 id="rig-type-safe-ai-agents-in-rust">Rig: Type-Safe AI Agents in Rust</h2>

<p><a href="https://rig.rs/">Rig</a> (<code class="language-plaintext highlighter-rouge">rig-core</code> 0.32) is a Rust library for building LLM applications with a unified API across providers. I built <a href="https://github.com/softwarewrighter/try-rig">try-rig</a> to explore it hands-on with Ollama running locally—no cloud API keys needed.</p>

<h3 id="a-simple-agent">A Simple Agent</h3>

<p>The builder pattern makes agent construction readable:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">rig</span><span class="p">::</span><span class="nn">providers</span><span class="p">::</span><span class="n">ollama</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">rig</span><span class="p">::</span><span class="nn">client</span><span class="p">::</span><span class="n">Nothing</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">rig</span><span class="p">::</span><span class="nn">completion</span><span class="p">::</span><span class="n">Prompt</span><span class="p">;</span>

<span class="k">let</span> <span class="n">client</span> <span class="o">=</span> <span class="nn">ollama</span><span class="p">::</span><span class="nn">Client</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">Nothing</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">agent</span> <span class="o">=</span> <span class="n">client</span>
    <span class="nf">.agent</span><span class="p">(</span><span class="s">"llama3.2"</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"You are a helpful assistant. Be concise."</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>

<span class="k">let</span> <span class="n">response</span> <span class="o">=</span> <span class="n">agent</span><span class="nf">.prompt</span><span class="p">(</span><span class="s">"What is Rust?"</span><span class="p">)</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>Swap <code class="language-plaintext highlighter-rouge">ollama::Client</code> for <code class="language-plaintext highlighter-rouge">openai::Client</code> or <code class="language-plaintext highlighter-rouge">anthropic::Client</code> and the rest stays the same.</p>

<h3 id="tool-equipped-agents">Tool-Equipped Agents</h3>

<p>Where Rig gets interesting is tools. Define a tool by implementing the <code class="language-plaintext highlighter-rouge">Tool</code> trait with typed args:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(Deserialize,</span> <span class="nd">Serialize)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">Calculator</span><span class="p">;</span>

<span class="k">impl</span> <span class="n">Tool</span> <span class="k">for</span> <span class="n">Calculator</span> <span class="p">{</span>
    <span class="k">const</span> <span class="n">NAME</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">'static</span> <span class="nb">str</span> <span class="o">=</span> <span class="s">"calculator"</span><span class="p">;</span>
    <span class="k">type</span> <span class="n">Error</span> <span class="o">=</span> <span class="n">CalcError</span><span class="p">;</span>
    <span class="k">type</span> <span class="n">Args</span> <span class="o">=</span> <span class="n">CalcArgs</span><span class="p">;</span>
    <span class="k">type</span> <span class="n">Output</span> <span class="o">=</span> <span class="nb">f64</span><span class="p">;</span>

    <span class="k">async</span> <span class="k">fn</span> <span class="nf">definition</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">_prompt</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="n">ToolDefinition</span> <span class="p">{</span>
        <span class="n">ToolDefinition</span> <span class="p">{</span>
            <span class="n">name</span><span class="p">:</span> <span class="s">"calculator"</span><span class="nf">.to_string</span><span class="p">(),</span>
            <span class="n">description</span><span class="p">:</span> <span class="s">"Perform arithmetic: add, subtract, multiply, divide"</span><span class="nf">.to_string</span><span class="p">(),</span>
            <span class="n">parameters</span><span class="p">:</span> <span class="nd">json!</span><span class="p">({</span> <span class="cm">/* JSON Schema */</span> <span class="p">}),</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="k">async</span> <span class="k">fn</span> <span class="nf">call</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">args</span><span class="p">:</span> <span class="k">Self</span><span class="p">::</span><span class="n">Args</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="k">Self</span><span class="p">::</span><span class="n">Output</span><span class="p">,</span> <span class="k">Self</span><span class="p">::</span><span class="n">Error</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">match</span> <span class="n">args</span><span class="py">.operation</span><span class="nf">.as_str</span><span class="p">()</span> <span class="p">{</span>
            <span class="s">"add"</span> <span class="k">=&gt;</span> <span class="nf">Ok</span><span class="p">(</span><span class="n">args</span><span class="py">.x</span> <span class="o">+</span> <span class="n">args</span><span class="py">.y</span><span class="p">),</span>
            <span class="s">"multiply"</span> <span class="k">=&gt;</span> <span class="nf">Ok</span><span class="p">(</span><span class="n">args</span><span class="py">.x</span> <span class="o">*</span> <span class="n">args</span><span class="py">.y</span><span class="p">),</span>
            <span class="c1">// ...</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Then chain tools onto the agent builder:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">agent</span> <span class="o">=</span> <span class="n">client</span>
    <span class="nf">.agent</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"Use tools for math, weather, files, date/time, and text."</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">Calculator</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">WeatherLookup</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">FileSearch</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">DateTime</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">StringTool</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>
</code></pre></div></div>

<p>The compiler verifies all tool types at build time. No runtime surprises from mismatched schemas.</p>

<h3 id="rag-with-embeddings">RAG with Embeddings</h3>

<p>Rig has built-in vector store support. The RAG agent in try-rig uses <code class="language-plaintext highlighter-rouge">nomic-embed-text</code> via Ollama for fully local embeddings:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">embedding_model</span> <span class="o">=</span> <span class="n">client</span><span class="nf">.embedding_model_with_ndims</span><span class="p">(</span><span class="s">"nomic-embed-text"</span><span class="p">,</span> <span class="mi">768</span><span class="p">);</span>

<span class="k">let</span> <span class="n">embeddings</span> <span class="o">=</span> <span class="nn">EmbeddingsBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">embedding_model</span><span class="nf">.clone</span><span class="p">())</span>
    <span class="nf">.documents</span><span class="p">(</span><span class="n">knowledge_entries</span><span class="p">)</span><span class="o">?</span>
    <span class="nf">.build</span><span class="p">()</span>
    <span class="k">.await</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">vector_store</span> <span class="o">=</span> <span class="nn">InMemoryVectorStore</span><span class="p">::</span><span class="nf">from_documents</span><span class="p">(</span><span class="n">embeddings</span><span class="p">);</span>
<span class="k">let</span> <span class="n">index</span> <span class="o">=</span> <span class="n">vector_store</span><span class="nf">.index</span><span class="p">(</span><span class="n">embedding_model</span><span class="p">);</span>

<span class="k">let</span> <span class="n">rag_agent</span> <span class="o">=</span> <span class="n">client</span>
    <span class="nf">.agent</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"Use the provided context to answer accurately."</span><span class="p">)</span>
    <span class="nf">.dynamic_context</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="n">index</span><span class="p">)</span>  <span class="c1">// inject top 2 results</span>
    <span class="nf">.build</span><span class="p">();</span>
</code></pre></div></div>

<h3 id="multi-agent-orchestration">Multi-Agent Orchestration</h3>

<p>Rig agents can be used as tools for other agents. The try-rig demo builds a math specialist and a weather specialist, then hands both to an orchestrator:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">calc_agent</span> <span class="o">=</span> <span class="n">client</span><span class="nf">.agent</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"You are a math specialist."</span><span class="p">)</span>
    <span class="nf">.name</span><span class="p">(</span><span class="s">"math_agent"</span><span class="p">)</span>
    <span class="nf">.description</span><span class="p">(</span><span class="s">"Arithmetic: add, subtract, multiply, divide."</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">Calculator</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>

<span class="k">let</span> <span class="n">weather_agent</span> <span class="o">=</span> <span class="n">client</span><span class="nf">.agent</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"You are a weather specialist."</span><span class="p">)</span>
    <span class="nf">.name</span><span class="p">(</span><span class="s">"weather_agent"</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">WeatherLookup</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>

<span class="k">let</span> <span class="n">orchestrator</span> <span class="o">=</span> <span class="n">client</span><span class="nf">.agent</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"Route questions to math_agent or weather_agent."</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">calc_agent</span><span class="p">)</span>
    <span class="nf">.tool</span><span class="p">(</span><span class="n">weather_agent</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>
</code></pre></div></div>

<p>The orchestrator decides which specialist to call based on the question. Agents as tools—composable all the way down.</p>

<h3 id="typed-extraction">Typed Extraction</h3>

<p>Rig can also extract structured data from unstructured text using <code class="language-plaintext highlighter-rouge">schemars</code>:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(Debug,</span> <span class="nd">Deserialize,</span> <span class="nd">Serialize,</span> <span class="nd">JsonSchema)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">ContactInfo</span> <span class="p">{</span>
    <span class="k">pub</span> <span class="n">name</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span>
    <span class="k">pub</span> <span class="n">email</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span>
    <span class="k">pub</span> <span class="n">phone</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span>
<span class="p">}</span>

<span class="k">let</span> <span class="n">extractor</span> <span class="o">=</span> <span class="n">client</span>
    <span class="py">.extractor</span><span class="p">::</span><span class="o">&lt;</span><span class="n">ContactInfo</span><span class="o">&gt;</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
    <span class="nf">.preamble</span><span class="p">(</span><span class="s">"Extract contact information from text."</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">();</span>

<span class="k">let</span> <span class="n">contact</span> <span class="o">=</span> <span class="n">extractor</span><span class="nf">.extract</span><span class="p">(</span><span class="s">"Call Jane at 555-1234 or jane@example.com"</span><span class="p">)</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>The output is a proper Rust struct, not a string you have to parse.</p>

<h3 id="the-try-rig-cli">The try-rig CLI</h3>

<p>All of these patterns are runnable from <a href="https://github.com/softwarewrighter/try-rig">try-rig</a>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>try-rig ask <span class="s2">"What is Rust?"</span>           <span class="c"># Simple agent</span>
try-rig tools <span class="s2">"What is 42 * 17?"</span>      <span class="c"># Tool calling</span>
try-rig rag <span class="s2">"Explain Rust ownership"</span>   <span class="c"># RAG with embeddings</span>
try-rig multi <span class="s2">"Weather in Tokyo?"</span>      <span class="c"># Multi-agent routing</span>
try-rig extract <span class="s2">"Call Jane at 555-1234"</span><span class="c"># Typed extraction</span>
try-rig stream <span class="s2">"Explain TCP/IP"</span>        <span class="c"># Streaming response</span>
</code></pre></div></div>

<p>Five times less memory than equivalent Python, zero Python dependencies, and the compiler catches your mistakes before runtime.</p>

<h2 id="langchain-rust-chain-abstractions-for-rust">langchain-rust: Chain Abstractions for Rust</h2>

<p><a href="https://crates.io/crates/langchain-rust">langchain-rust</a> (v4.6.0) brings LangChain’s composable chain architecture to Rust. Where Rig focuses on type-safe agents, langchain-rust focuses on chain orchestration. The <a href="https://github.com/softwarewrighter/try-langchain-rust">try-langchain-rust</a> repo has 13 runnable examples across the full feature set.</p>

<h3 id="llm-chains-and-prompt-templates">LLM Chains and Prompt Templates</h3>

<p>The chain builder composes prompts and LLMs into reusable pipelines:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">langchain_rust</span><span class="p">::{</span>
    <span class="nn">chain</span><span class="p">::{</span><span class="n">Chain</span><span class="p">,</span> <span class="n">LLMChainBuilder</span><span class="p">},</span>
    <span class="n">fmt_message</span><span class="p">,</span> <span class="n">fmt_template</span><span class="p">,</span> <span class="n">message_formatter</span><span class="p">,</span>
    <span class="nn">prompt</span><span class="p">::</span><span class="n">HumanMessagePromptTemplate</span><span class="p">,</span>
    <span class="n">prompt_args</span><span class="p">,</span> <span class="nn">schemas</span><span class="p">::</span><span class="nn">messages</span><span class="p">::</span><span class="n">Message</span><span class="p">,</span> <span class="n">template_fstring</span><span class="p">,</span>
<span class="p">};</span>

<span class="k">let</span> <span class="n">prompt</span> <span class="o">=</span> <span class="nd">message_formatter!</span><span class="p">[</span>
    <span class="nd">fmt_message!</span><span class="p">(</span><span class="nn">Message</span><span class="p">::</span><span class="nf">new_system_message</span><span class="p">(</span><span class="s">"You are a concise technical writer."</span><span class="p">)),</span>
    <span class="nd">fmt_template!</span><span class="p">(</span><span class="nn">HumanMessagePromptTemplate</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="nd">template_fstring!</span><span class="p">(</span>
        <span class="s">"Explain {topic} in 2-3 sentences."</span><span class="p">,</span> <span class="s">"topic"</span>
    <span class="p">)))</span>
<span class="p">];</span>

<span class="k">let</span> <span class="n">chain</span> <span class="o">=</span> <span class="nn">LLMChainBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.prompt</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="nf">.llm</span><span class="p">(</span><span class="n">llm</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">result</span> <span class="o">=</span> <span class="n">chain</span><span class="nf">.invoke</span><span class="p">(</span><span class="nd">prompt_args!</span> <span class="p">{</span> <span class="s">"topic"</span> <span class="k">=&gt;</span> <span class="s">"ownership in Rust"</span> <span class="p">})</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>Ollama works the same way—swap the LLM and everything else stays identical:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">ollama</span> <span class="o">=</span> <span class="nn">Ollama</span><span class="p">::</span><span class="nf">default</span><span class="p">()</span><span class="nf">.with_model</span><span class="p">(</span><span class="s">"llama3.2"</span><span class="p">);</span>
<span class="c1">// use ollama in place of llm above</span>
</code></pre></div></div>

<h3 id="sequential-chains">Sequential Chains</h3>

<p>Pipe one chain’s output into the next. This example generates a story concept, then a title, then an opening line:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">concept_chain</span> <span class="o">=</span> <span class="nn">LLMChainBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.prompt</span><span class="p">(</span><span class="cm">/* "Create a concept about " */</span><span class="p">)</span>
    <span class="nf">.llm</span><span class="p">(</span><span class="n">llm</span><span class="nf">.clone</span><span class="p">())</span>
    <span class="nf">.output_key</span><span class="p">(</span><span class="s">"concept"</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">title_chain</span> <span class="o">=</span> <span class="nn">LLMChainBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.prompt</span><span class="p">(</span><span class="cm">/* "Suggest a title for " */</span><span class="p">)</span>
    <span class="nf">.llm</span><span class="p">(</span><span class="n">llm</span><span class="nf">.clone</span><span class="p">())</span>
    <span class="nf">.output_key</span><span class="p">(</span><span class="s">"title"</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">chain</span> <span class="o">=</span> <span class="nd">sequential_chain!</span><span class="p">(</span><span class="n">concept_chain</span><span class="p">,</span> <span class="n">title_chain</span><span class="p">,</span> <span class="n">opening_chain</span><span class="p">);</span>

<span class="k">let</span> <span class="n">output</span> <span class="o">=</span> <span class="n">chain</span><span class="nf">.execute</span><span class="p">(</span><span class="nd">prompt_args!</span> <span class="p">{</span> <span class="s">"topic"</span> <span class="k">=&gt;</span> <span class="s">"a robot that learns to paint"</span> <span class="p">})</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
<span class="nd">println!</span><span class="p">(</span><span class="s">"Title: {}"</span><span class="p">,</span> <span class="n">output</span><span class="p">[</span><span class="s">"title"</span><span class="p">]);</span>
</code></pre></div></div>

<h3 id="conversational-memory">Conversational Memory</h3>

<p>Multi-turn dialogue with automatic context retention:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">chain</span> <span class="o">=</span> <span class="nn">ConversationalChainBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.llm</span><span class="p">(</span><span class="n">llm</span><span class="p">)</span>
    <span class="nf">.memory</span><span class="p">(</span><span class="nn">SimpleMemory</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span><span class="nf">.into</span><span class="p">())</span>
    <span class="nf">.build</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>

<span class="n">chain</span><span class="nf">.invoke</span><span class="p">(</span><span class="nd">prompt_args!</span> <span class="p">{</span> <span class="s">"input"</span> <span class="k">=&gt;</span> <span class="s">"My name is Alice and I'm learning Rust."</span> <span class="p">})</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
<span class="c1">// Turn 2: chain remembers Alice and Rust</span>
<span class="n">chain</span><span class="nf">.invoke</span><span class="p">(</span><span class="nd">prompt_args!</span> <span class="p">{</span> <span class="s">"input"</span> <span class="k">=&gt;</span> <span class="s">"What's my name?"</span> <span class="p">})</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="rag-with-vector-store">RAG with Vector Store</h3>

<p>The conversational retriever chain combines memory, vector search, and LLM generation. The try-langchain-rust demo uses SQLite for the vector store:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">store</span> <span class="o">=</span> <span class="nn">StoreBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.embedder</span><span class="p">(</span><span class="nn">OpenAiEmbedder</span><span class="p">::</span><span class="nf">default</span><span class="p">())</span>
    <span class="nf">.connection_url</span><span class="p">(</span><span class="s">"sqlite::memory:"</span><span class="p">)</span>
    <span class="nf">.table</span><span class="p">(</span><span class="s">"documents"</span><span class="p">)</span>
    <span class="nf">.vector_dimensions</span><span class="p">(</span><span class="mi">1536</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>

<span class="n">store</span><span class="nf">.initialize</span><span class="p">()</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
<span class="nd">add_documents!</span><span class="p">(</span><span class="n">store</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">documents</span><span class="p">)</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">chain</span> <span class="o">=</span> <span class="nn">ConversationalRetrieverChainBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.llm</span><span class="p">(</span><span class="n">llm</span><span class="p">)</span>
    <span class="nf">.rephrase_question</span><span class="p">(</span><span class="k">true</span><span class="p">)</span>
    <span class="nf">.memory</span><span class="p">(</span><span class="nn">SimpleMemory</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span><span class="nf">.into</span><span class="p">())</span>
    <span class="nf">.retriever</span><span class="p">(</span><span class="nn">Retriever</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">store</span><span class="p">,</span> <span class="mi">3</span><span class="p">))</span>
    <span class="nf">.prompt</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>Multi-turn RAG conversations work out of the box—the chain rephrases follow-up questions using conversation history before searching the vector store.</p>

<h3 id="agents-and-semantic-routing">Agents and Semantic Routing</h3>

<p>Agents select tools autonomously. The demo uses a <code class="language-plaintext highlighter-rouge">CommandExecutor</code> tool:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">agent</span> <span class="o">=</span> <span class="nn">ConversationalAgentBuilder</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
    <span class="nf">.tools</span><span class="p">(</span><span class="o">&amp;</span><span class="p">[</span><span class="nn">Arc</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="nn">CommandExecutor</span><span class="p">::</span><span class="nf">default</span><span class="p">())])</span>
    <span class="nf">.build</span><span class="p">(</span><span class="n">llm</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">executor</span> <span class="o">=</span> <span class="nn">AgentExecutor</span><span class="p">::</span><span class="nf">from_agent</span><span class="p">(</span><span class="n">agent</span><span class="p">)</span>
    <span class="nf">.with_memory</span><span class="p">(</span><span class="nn">SimpleMemory</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span><span class="nf">.into</span><span class="p">());</span>

<span class="n">executor</span><span class="nf">.invoke</span><span class="p">(</span><span class="nd">prompt_args!</span> <span class="p">{</span> <span class="s">"input"</span> <span class="k">=&gt;</span> <span class="s">"List the files in the current directory"</span> <span class="p">})</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>Semantic routing dispatches queries by meaning—define example utterances for each route and the router classifies new inputs:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">coding_route</span> <span class="o">=</span> <span class="nn">Router</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="s">"coding"</span><span class="p">,</span> <span class="o">&amp;</span><span class="p">[</span>
    <span class="s">"How do I write a function in Rust?"</span><span class="p">,</span>
    <span class="s">"Explain generics in programming"</span><span class="p">,</span>
<span class="p">]);</span>
<span class="k">let</span> <span class="n">devops_route</span> <span class="o">=</span> <span class="nn">Router</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="s">"devops"</span><span class="p">,</span> <span class="o">&amp;</span><span class="p">[</span>
    <span class="s">"Set up a CI/CD pipeline"</span><span class="p">,</span>
    <span class="s">"Configure Docker containers"</span><span class="p">,</span>
<span class="p">]);</span>

<span class="k">let</span> <span class="n">router</span> <span class="o">=</span> <span class="nn">RouteLayerBuilder</span><span class="p">::</span><span class="nf">default</span><span class="p">()</span>
    <span class="nf">.embedder</span><span class="p">(</span><span class="nn">OpenAiEmbedder</span><span class="p">::</span><span class="nf">default</span><span class="p">())</span>
    <span class="nf">.add_route</span><span class="p">(</span><span class="n">coding_route</span><span class="p">)</span>
    <span class="nf">.add_route</span><span class="p">(</span><span class="n">devops_route</span><span class="p">)</span>
    <span class="nf">.threshold</span><span class="p">(</span><span class="mf">0.80</span><span class="p">)</span>
    <span class="nf">.build</span><span class="p">()</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>

<span class="k">let</span> <span class="n">route</span> <span class="o">=</span> <span class="n">router</span><span class="nf">.call</span><span class="p">(</span><span class="s">"Explain the borrow checker"</span><span class="p">)</span><span class="k">.await</span><span class="o">?</span><span class="p">;</span>
<span class="c1">// → "coding"</span>
</code></pre></div></div>

<h3 id="the-try-langchain-rust-examples">The try-langchain-rust Examples</h3>

<p>All 13 examples are runnable from <a href="https://github.com/softwarewrighter/try-langchain-rust">try-langchain-rust</a>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cargo run <span class="nt">--example</span> llm_chain         <span class="c"># Prompt templates + LLM chain</span>
cargo run <span class="nt">--example</span> conversational    <span class="c"># Multi-turn memory</span>
cargo run <span class="nt">--example</span> ollama            <span class="c"># Local LLM (no API key)</span>
cargo run <span class="nt">--example</span> streaming         <span class="c"># Token-by-token output</span>
cargo run <span class="nt">--example</span> vector_store      <span class="c"># SQLite similarity search</span>
cargo run <span class="nt">--example</span> doc_loader        <span class="c"># Text/CSV loading + splitting</span>
cargo run <span class="nt">--example</span> qa_chain          <span class="c"># Q&amp;A over documents</span>
cargo run <span class="nt">--example</span> rag_chat          <span class="c"># Conversational RAG</span>
cargo run <span class="nt">--example</span> agent             <span class="c"># Agent with tools</span>
cargo run <span class="nt">--example</span> sequential        <span class="c"># Chained pipelines</span>
cargo run <span class="nt">--example</span> semantic_router   <span class="c"># Route by meaning</span>
</code></pre></div></div>

<h2 id="rig-vs-langchain-rust">Rig vs. langchain-rust</h2>

<table>
  <thead>
    <tr>
      <th>Dimension</th>
      <th>Rig</th>
      <th>langchain-rust</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Focus</strong></td>
      <td>Agent construction</td>
      <td>Chain orchestration</td>
    </tr>
    <tr>
      <td><strong>Type safety</strong></td>
      <td>Strong (Tool trait, typed extraction)</td>
      <td>Moderate (macro-based prompt building)</td>
    </tr>
    <tr>
      <td><strong>RAG</strong></td>
      <td>In-memory vector store, embeddings</td>
      <td>SQLite/Postgres/Qdrant + document loaders</td>
    </tr>
    <tr>
      <td><strong>Multi-agent</strong></td>
      <td>Agents as tools (composable)</td>
      <td>Agent executor with tool selection</td>
    </tr>
    <tr>
      <td><strong>Memory</strong></td>
      <td>Manual history management</td>
      <td>Built-in SimpleMemory, auto context</td>
    </tr>
    <tr>
      <td><strong>Chains</strong></td>
      <td>Single agent pipelines</td>
      <td>Sequential chains, conversational retriever</td>
    </tr>
    <tr>
      <td><strong>Maturity</strong></td>
      <td>v0.32, active development</td>
      <td>v4.6.0, stable API</td>
    </tr>
    <tr>
      <td><strong>Local LLM</strong></td>
      <td>Ollama native</td>
      <td>Ollama supported</td>
    </tr>
    <tr>
      <td><strong>Best for</strong></td>
      <td>Type-safe agents, tool calling</td>
      <td>Multi-step pipelines, RAG, document ingestion</td>
    </tr>
  </tbody>
</table>

<p>They’re complementary more than competing. A project could use Rig for the agent layer and langchain-rust for document ingestion and retrieval.</p>

<h2 id="whats-next-for-pjmai-rs">What’s Next for pjmai-rs</h2>

<p>The <a href="https://github.com/sw-cli-tools/pjmai-rs">Phase 4 roadmap</a> for pjmai-rs includes AI integration:</p>

<ul>
  <li><strong>AI context injection</strong>: <code class="language-plaintext highlighter-rouge">ctpj --for-agent</code> already outputs project metadata as JSON for AI prompts</li>
  <li><strong>Restricted PATH mode</strong>: Sandboxed environments for autonomous agents</li>
  <li><strong>AI-assisted discovery</strong>: Let agents find and register projects automatically</li>
</ul>

<p>The question isn’t whether pjmai-rs will use Rig or langchain-rust—it’s which patterns from each framework make sense for a CLI tool that helps AI agents navigate codebases.</p>

<div class="references-section">

  <h2 id="references">References</h2>

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Rig Framework</strong></td>
        <td><a href="https://rig.rs/">rig.rs</a></td>
      </tr>
      <tr>
        <td><strong>Rig Docs</strong></td>
        <td><a href="https://docs.rs/rig-core">docs.rs/rig-core</a></td>
      </tr>
      <tr>
        <td><strong>langchain-rust</strong></td>
        <td><a href="https://crates.io/crates/langchain-rust">crates.io/crates/langchain-rust</a></td>
      </tr>
      <tr>
        <td><strong>langchain-rust Source</strong></td>
        <td><a href="https://github.com/Abraxas-365/langchain-rust">github.com/Abraxas-365/langchain-rust</a></td>
      </tr>
      <tr>
        <td><strong>Rust 2024 Edition Guide</strong></td>
        <td><a href="https://doc.rust-lang.org/edition-guide/">doc.rust-lang.org/edition-guide</a></td>
      </tr>
      <tr>
        <td><strong>“Sharpen the Saw”</strong></td>
        <td><a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">The 7 Habits of Highly Effective People</a> (Stephen Covey)</td>
      </tr>
      <tr>
        <td><strong>pjmai-rs Background</strong></td>
        <td><a href="/2026/03/05/tbt-pjmai-rs-project-manager/">TBT: PJMAI-RS</a></td>
      </tr>
      <tr>
        <td><strong>pjmai-rs Features</strong></td>
        <td><a href="/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion/">Navigation History and Fuzzy Completion</a></td>
      </tr>
    </tbody>
  </table>

</div>

<hr />

<p><em>Habit 7: Sharpen the Saw. Fix the foundation first, then build higher.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rust" /><category term="tools" /><category term="ai" /><category term="rust" /><category term="pjmai" /><category term="rig" /><category term="langchain" /><category term="ai-agents" /><category term="llm" /><category term="sharpen-the-saw" /><category term="cli" /><category term="developer-tools" /><summary type="html"><![CDATA[Three tools, one theme: sharpening the foundation. This week covers pjmai-rs bug fixes and new features, plus a first look at two Rust frameworks for building LLM-powered applications—Rig and langchain-rust. Resource Link Repos sw-cli-tools/pjmai-rs, try-rig, try-langchain-rust Video Explainer References Links and Resources Comments Discord pjmai-rs: Fixing the Foundation Before adding AI features to any tool, you need a solid foundation. Since the last update, pjmai-rs received critical fixes and practical new features. The Rust 2024 Edition Bug Upgrading to Rust edition 2024 broke project removal silently. The IndexMap::remove() method changed semantics—it now preserves insertion order differently. The fix: // Rust 2024 broke this - projects.remove(name) // shift_remove maintains expected behavior + projects.shift_remove(name) A one-line fix, but the kind that silently corrupts data if you miss it. The 2024 edition migration guide mentions this change, but it’s easy to overlook in a large codebase. Shell Integration Improvements Help flags: All aliases (chpj, hypj, stpj, etc.) now properly pass --help through After-help messages: Every subcommand shows examples and related commands Version matching: --version output now matches Cargo.toml Argument validation: Better error messages for invalid flag combinations New Capabilities Feature Command What It Does Stack navigation stpj Push/pop project context with visibility History tracking hypj Revisit recently-visited projects by number Fuzzy completion chpj &lt;TAB&gt; Prefix &gt; segment &gt; substring, sorted by recency Environment config evpj Per-project env vars with auto-detection (Python, Node, Rust) Bulk operations rmpj --all Batch management with confirmation Subdirectory nav chpj proj src/ Tab-complete into subdirs These features are covered in detail in the Personal Software post. Sharpen the Saw — Habit 7 from Stephen Covey’s The 7 Habits of Highly Effective People is about preserving and enhancing your greatest asset: yourself and your tools. In software, that means taking time to fix accumulated friction, update dependencies, and learn new frameworks—even when shipping features feels more urgent. The payoff compounds: every hour spent sharpening saves many more down the line. Rig: Type-Safe AI Agents in Rust Rig (rig-core 0.32) is a Rust library for building LLM applications with a unified API across providers. I built try-rig to explore it hands-on with Ollama running locally—no cloud API keys needed. A Simple Agent The builder pattern makes agent construction readable: use rig::providers::ollama; use rig::client::Nothing; use rig::completion::Prompt; let client = ollama::Client::new(Nothing)?; let agent = client .agent("llama3.2") .preamble("You are a helpful assistant. Be concise.") .build(); let response = agent.prompt("What is Rust?").await?; Swap ollama::Client for openai::Client or anthropic::Client and the rest stays the same. Tool-Equipped Agents Where Rig gets interesting is tools. Define a tool by implementing the Tool trait with typed args: #[derive(Deserialize, Serialize)] pub struct Calculator; impl Tool for Calculator { const NAME: &amp;'static str = "calculator"; type Error = CalcError; type Args = CalcArgs; type Output = f64; async fn definition(&amp;self, _prompt: String) -&gt; ToolDefinition { ToolDefinition { name: "calculator".to_string(), description: "Perform arithmetic: add, subtract, multiply, divide".to_string(), parameters: json!({ /* JSON Schema */ }), } } async fn call(&amp;self, args: Self::Args) -&gt; Result&lt;Self::Output, Self::Error&gt; { match args.operation.as_str() { "add" =&gt; Ok(args.x + args.y), "multiply" =&gt; Ok(args.x * args.y), // ... } } } Then chain tools onto the agent builder: let agent = client .agent(model) .preamble("Use tools for math, weather, files, date/time, and text.") .tool(Calculator) .tool(WeatherLookup) .tool(FileSearch) .tool(DateTime) .tool(StringTool) .build(); The compiler verifies all tool types at build time. No runtime surprises from mismatched schemas. RAG with Embeddings Rig has built-in vector store support. The RAG agent in try-rig uses nomic-embed-text via Ollama for fully local embeddings: let embedding_model = client.embedding_model_with_ndims("nomic-embed-text", 768); let embeddings = EmbeddingsBuilder::new(embedding_model.clone()) .documents(knowledge_entries)? .build() .await?; let vector_store = InMemoryVectorStore::from_documents(embeddings); let index = vector_store.index(embedding_model); let rag_agent = client .agent(model) .preamble("Use the provided context to answer accurately.") .dynamic_context(2, index) // inject top 2 results .build(); Multi-Agent Orchestration Rig agents can be used as tools for other agents. The try-rig demo builds a math specialist and a weather specialist, then hands both to an orchestrator: let calc_agent = client.agent(model) .preamble("You are a math specialist.") .name("math_agent") .description("Arithmetic: add, subtract, multiply, divide.") .tool(Calculator) .build(); let weather_agent = client.agent(model) .preamble("You are a weather specialist.") .name("weather_agent") .tool(WeatherLookup) .build(); let orchestrator = client.agent(model) .preamble("Route questions to math_agent or weather_agent.") .tool(calc_agent) .tool(weather_agent) .build(); The orchestrator decides which specialist to call based on the question. Agents as tools—composable all the way down. Typed Extraction Rig can also extract structured data from unstructured text using schemars: #[derive(Debug, Deserialize, Serialize, JsonSchema)] pub struct ContactInfo { pub name: Option&lt;String&gt;, pub email: Option&lt;String&gt;, pub phone: Option&lt;String&gt;, } let extractor = client .extractor::&lt;ContactInfo&gt;(model) .preamble("Extract contact information from text.") .build(); let contact = extractor.extract("Call Jane at 555-1234 or jane@example.com").await?; The output is a proper Rust struct, not a string you have to parse. The try-rig CLI All of these patterns are runnable from try-rig: try-rig ask "What is Rust?" # Simple agent try-rig tools "What is 42 * 17?" # Tool calling try-rig rag "Explain Rust ownership" # RAG with embeddings try-rig multi "Weather in Tokyo?" # Multi-agent routing try-rig extract "Call Jane at 555-1234"# Typed extraction try-rig stream "Explain TCP/IP" # Streaming response Five times less memory than equivalent Python, zero Python dependencies, and the compiler catches your mistakes before runtime. langchain-rust: Chain Abstractions for Rust langchain-rust (v4.6.0) brings LangChain’s composable chain architecture to Rust. Where Rig focuses on type-safe agents, langchain-rust focuses on chain orchestration. The try-langchain-rust repo has 13 runnable examples across the full feature set. LLM Chains and Prompt Templates The chain builder composes prompts and LLMs into reusable pipelines: use langchain_rust::{ chain::{Chain, LLMChainBuilder}, fmt_message, fmt_template, message_formatter, prompt::HumanMessagePromptTemplate, prompt_args, schemas::messages::Message, template_fstring, }; let prompt = message_formatter![ fmt_message!(Message::new_system_message("You are a concise technical writer.")), fmt_template!(HumanMessagePromptTemplate::new(template_fstring!( "Explain {topic} in 2-3 sentences.", "topic" ))) ]; let chain = LLMChainBuilder::new() .prompt(prompt) .llm(llm) .build()?; let result = chain.invoke(prompt_args! { "topic" =&gt; "ownership in Rust" }).await?; Ollama works the same way—swap the LLM and everything else stays identical: let ollama = Ollama::default().with_model("llama3.2"); // use ollama in place of llm above Sequential Chains Pipe one chain’s output into the next. This example generates a story concept, then a title, then an opening line: let concept_chain = LLMChainBuilder::new() .prompt(/* "Create a concept about " */) .llm(llm.clone()) .output_key("concept") .build()?; let title_chain = LLMChainBuilder::new() .prompt(/* "Suggest a title for " */) .llm(llm.clone()) .output_key("title") .build()?; let chain = sequential_chain!(concept_chain, title_chain, opening_chain); let output = chain.execute(prompt_args! { "topic" =&gt; "a robot that learns to paint" }).await?; println!("Title: {}", output["title"]); Conversational Memory Multi-turn dialogue with automatic context retention: let chain = ConversationalChainBuilder::new() .llm(llm) .memory(SimpleMemory::new().into()) .build()?; chain.invoke(prompt_args! { "input" =&gt; "My name is Alice and I'm learning Rust." }).await?; // Turn 2: chain remembers Alice and Rust chain.invoke(prompt_args! { "input" =&gt; "What's my name?" }).await?; RAG with Vector Store The conversational retriever chain combines memory, vector search, and LLM generation. The try-langchain-rust demo uses SQLite for the vector store: let store = StoreBuilder::new() .embedder(OpenAiEmbedder::default()) .connection_url("sqlite::memory:") .table("documents") .vector_dimensions(1536) .build().await?; store.initialize().await?; add_documents!(store, &amp;documents).await?; let chain = ConversationalRetrieverChainBuilder::new() .llm(llm) .rephrase_question(true) .memory(SimpleMemory::new().into()) .retriever(Retriever::new(store, 3)) .prompt(prompt) .build()?; Multi-turn RAG conversations work out of the box—the chain rephrases follow-up questions using conversation history before searching the vector store. Agents and Semantic Routing Agents select tools autonomously. The demo uses a CommandExecutor tool: let agent = ConversationalAgentBuilder::new() .tools(&amp;[Arc::new(CommandExecutor::default())]) .build(llm)?; let executor = AgentExecutor::from_agent(agent) .with_memory(SimpleMemory::new().into()); executor.invoke(prompt_args! { "input" =&gt; "List the files in the current directory" }).await?; Semantic routing dispatches queries by meaning—define example utterances for each route and the router classifies new inputs: let coding_route = Router::new("coding", &amp;[ "How do I write a function in Rust?", "Explain generics in programming", ]); let devops_route = Router::new("devops", &amp;[ "Set up a CI/CD pipeline", "Configure Docker containers", ]); let router = RouteLayerBuilder::default() .embedder(OpenAiEmbedder::default()) .add_route(coding_route) .add_route(devops_route) .threshold(0.80) .build().await?; let route = router.call("Explain the borrow checker").await?; // → "coding" The try-langchain-rust Examples All 13 examples are runnable from try-langchain-rust: cargo run --example llm_chain # Prompt templates + LLM chain cargo run --example conversational # Multi-turn memory cargo run --example ollama # Local LLM (no API key) cargo run --example streaming # Token-by-token output cargo run --example vector_store # SQLite similarity search cargo run --example doc_loader # Text/CSV loading + splitting cargo run --example qa_chain # Q&amp;A over documents cargo run --example rag_chat # Conversational RAG cargo run --example agent # Agent with tools cargo run --example sequential # Chained pipelines cargo run --example semantic_router # Route by meaning Rig vs. langchain-rust Dimension Rig langchain-rust Focus Agent construction Chain orchestration Type safety Strong (Tool trait, typed extraction) Moderate (macro-based prompt building) RAG In-memory vector store, embeddings SQLite/Postgres/Qdrant + document loaders Multi-agent Agents as tools (composable) Agent executor with tool selection Memory Manual history management Built-in SimpleMemory, auto context Chains Single agent pipelines Sequential chains, conversational retriever Maturity v0.32, active development v4.6.0, stable API Local LLM Ollama native Ollama supported Best for Type-safe agents, tool calling Multi-step pipelines, RAG, document ingestion They’re complementary more than competing. A project could use Rig for the agent layer and langchain-rust for document ingestion and retrieval. What’s Next for pjmai-rs The Phase 4 roadmap for pjmai-rs includes AI integration: AI context injection: ctpj --for-agent already outputs project metadata as JSON for AI prompts Restricted PATH mode: Sandboxed environments for autonomous agents AI-assisted discovery: Let agents find and register projects automatically The question isn’t whether pjmai-rs will use Rig or langchain-rust—it’s which patterns from each framework make sense for a CLI tool that helps AI agents navigate codebases. References Resource Link Rig Framework rig.rs Rig Docs docs.rs/rig-core langchain-rust crates.io/crates/langchain-rust langchain-rust Source github.com/Abraxas-365/langchain-rust Rust 2024 Edition Guide doc.rust-lang.org/edition-guide “Sharpen the Saw” The 7 Habits of Highly Effective People (Stephen Covey) pjmai-rs Background TBT: PJMAI-RS pjmai-rs Features Navigation History and Fuzzy Completion Habit 7: Sharpen the Saw. Fix the foundation first, then build higher.]]></summary></entry><entry><title type="html">pjmai-rs: Navigation History and Fuzzy Completion</title><link href="https://software-wrighter-lab.github.io/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion/" rel="alternate" type="text/html" title="pjmai-rs: Navigation History and Fuzzy Completion" /><published>2026-03-07T00:15:00-08:00</published><updated>2026-03-07T00:15:00-08:00</updated><id>https://software-wrighter-lab.github.io/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/07/pjmai-rs-navigation-history-and-fuzzy-completion/"><![CDATA[<p><img src="/assets/images/posts/block-contemplate.png" class="post-marker" alt="" /></p>

<p>Since the <a href="/2026/03/05/tbt-pjmai-rs-project-manager/">TBT post on pjmai-rs</a>, development has continued. This post covers the new features that make project navigation even faster.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repo</strong></td>
        <td><a href="https://github.com/sw-cli-tools/pjmai-rs">sw-cli-tools/pjmai-rs</a></td>
      </tr>
      <tr>
        <td><strong>Background</strong></td>
        <td><a href="/2026/03/05/tbt-pjmai-rs-project-manager/">TBT: PJMAI-RS</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="navigation-history">Navigation History</h2>

<p>The new <code class="language-plaintext highlighter-rouge">hypj</code> command (or <code class="language-plaintext highlighter-rouge">pjmai history</code>) shows where you’ve been:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hypj
<span class="c"># Output:</span>
<span class="c"># 1. webapp      ~/code/webapp</span>
<span class="c"># 2. api         ~/code/api</span>
<span class="c"># 3. config      ~/code/config</span>
<span class="c"># 4. docs        ~/code/docs</span>
</code></pre></div></div>

<p>Jump directly to a history entry:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hypj 3    <span class="c"># Jump to config (entry 3)</span>
</code></pre></div></div>

<p>This is faster than remembering project names when you’re bouncing between several repos.</p>

<h2 id="stack-management">Stack Management</h2>

<p>The push/pop workflow now has explicit stack visibility:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>stpj              <span class="c"># Show current stack</span>
stpj clear        <span class="c"># Clear the stack (with confirmation)</span>
</code></pre></div></div>

<p>When you use <code class="language-plaintext highlighter-rouge">chpj</code> (direct navigation) instead of push/pop, the stack is automatically cleared. This prevents confusion when mixing navigation styles.</p>

<p>The <code class="language-plaintext highlighter-rouge">popj</code> command now shows context:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>popj
<span class="c"># Output: Returning to webapp (1 remaining)</span>
</code></pre></div></div>

<h2 id="subdirectory-navigation">Subdirectory Navigation</h2>

<p>Navigate directly into subdirectories with tab completion:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chpj myproject&lt;TAB&gt;           <span class="c"># Complete project name</span>
chpj myproject &lt;TAB&gt;          <span class="c"># Complete subdirs: src, tests, docs</span>
chpj myproject src/&lt;TAB&gt;      <span class="c"># Complete nested: lib, bin</span>
chpj myproject src/lib&lt;ENTER&gt; <span class="c"># cd to ~/code/myproject/src/lib</span>
</code></pre></div></div>

<p>Both space and slash syntax work:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chpj myproject src lib        <span class="c"># Space-separated</span>
chpj myproject src/lib        <span class="c"># Slash-separated</span>
</code></pre></div></div>

<p>Helpful error messages:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chpj myproject nonexistent
<span class="c"># Error: subdirectory 'nonexistent' not found in project 'myproject'</span>

chpj myproject README.md
<span class="c"># Error: 'README.md' is a file, not a directory</span>
</code></pre></div></div>

<h2 id="smarter-fuzzy-completion">Smarter Fuzzy Completion</h2>

<p>Tab completion now uses tiered matching:</p>

<ol>
  <li><strong>Prefix matches first</strong>: <code class="language-plaintext highlighter-rouge">web</code> → <code class="language-plaintext highlighter-rouge">webapp</code>, <code class="language-plaintext highlighter-rouge">webapi</code></li>
  <li><strong>Segment matches second</strong>: After <code class="language-plaintext highlighter-rouge">-</code> boundaries, so <code class="language-plaintext highlighter-rouge">api</code> matches <code class="language-plaintext highlighter-rouge">my-api</code></li>
  <li><strong>Substring matches last</strong>: <code class="language-plaintext highlighter-rouge">app</code> finds <code class="language-plaintext highlighter-rouge">webapp</code></li>
</ol>

<p>Within each tier, results are sorted by <strong>most recently used</strong>. The project you switched to five minutes ago appears before one you haven’t touched in weeks.</p>

<h2 id="bulk-operations">Bulk Operations</h2>

<p>Two new flags for batch management:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rmpj <span class="nt">--all</span>        <span class="c"># Remove all projects (with confirmation)</span>
scpj <span class="nt">--reset</span>      <span class="c"># Clear registry and re-scan (fresh start)</span>
</code></pre></div></div>

<p>The scan command also handles nickname collisions better now. Instead of numeric suffixes (<code class="language-plaintext highlighter-rouge">webapp2</code>), it uses owner-prefixed names (<code class="language-plaintext highlighter-rouge">acme-webapp</code>) based on the git remote.</p>

<h2 id="new-commands-summary">New Commands Summary</h2>

<table>
  <thead>
    <tr>
      <th>Command</th>
      <th>Alias</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pjmai history [N]</code></td>
      <td><code class="language-plaintext highlighter-rouge">hypj</code></td>
      <td>Show or jump to navigation history</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pjmai stack show</code></td>
      <td><code class="language-plaintext highlighter-rouge">stpj</code></td>
      <td>Show the project stack</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pjmai stack clear</code></td>
      <td><code class="language-plaintext highlighter-rouge">stpj clear</code></td>
      <td>Clear the stack</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pjmai remove --all</code></td>
      <td><code class="language-plaintext highlighter-rouge">rmpj --all</code></td>
      <td>Remove all projects</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pjmai scan --reset</code></td>
      <td><code class="language-plaintext highlighter-rouge">scpj --reset</code></td>
      <td>Fresh re-scan</td>
    </tr>
  </tbody>
</table>

<h2 id="whats-next">What’s Next</h2>

<p>The focus continues on making project context switching invisible. Upcoming work:</p>

<ul>
  <li><strong>Nono integration</strong>: Sandboxing untrusted projects</li>
  <li><strong>AI agent restricted mode</strong>: Curated PATH for autonomous agents</li>
  <li><strong>Multi-machine sync</strong>: Share project registry across machines</li>
</ul>

<hr />

<p><em>The best developer tools are the ones you stop noticing.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="cli-tools" /><category term="rust" /><category term="pjmai" /><category term="rust" /><category term="cli" /><category term="project-management" /><category term="shell" /><category term="developer-tools" /><category term="personal-software" /><summary type="html"><![CDATA[Since the TBT post on pjmai-rs, development has continued. This post covers the new features that make project navigation even faster. Resource Link Repo sw-cli-tools/pjmai-rs Background TBT: PJMAI-RS Comments Discord Navigation History The new hypj command (or pjmai history) shows where you’ve been: hypj # Output: # 1. webapp ~/code/webapp # 2. api ~/code/api # 3. config ~/code/config # 4. docs ~/code/docs Jump directly to a history entry: hypj 3 # Jump to config (entry 3) This is faster than remembering project names when you’re bouncing between several repos. Stack Management The push/pop workflow now has explicit stack visibility: stpj # Show current stack stpj clear # Clear the stack (with confirmation) When you use chpj (direct navigation) instead of push/pop, the stack is automatically cleared. This prevents confusion when mixing navigation styles. The popj command now shows context: popj # Output: Returning to webapp (1 remaining) Subdirectory Navigation Navigate directly into subdirectories with tab completion: chpj myproject&lt;TAB&gt; # Complete project name chpj myproject &lt;TAB&gt; # Complete subdirs: src, tests, docs chpj myproject src/&lt;TAB&gt; # Complete nested: lib, bin chpj myproject src/lib&lt;ENTER&gt; # cd to ~/code/myproject/src/lib Both space and slash syntax work: chpj myproject src lib # Space-separated chpj myproject src/lib # Slash-separated Helpful error messages: chpj myproject nonexistent # Error: subdirectory 'nonexistent' not found in project 'myproject' chpj myproject README.md # Error: 'README.md' is a file, not a directory Smarter Fuzzy Completion Tab completion now uses tiered matching: Prefix matches first: web → webapp, webapi Segment matches second: After - boundaries, so api matches my-api Substring matches last: app finds webapp Within each tier, results are sorted by most recently used. The project you switched to five minutes ago appears before one you haven’t touched in weeks. Bulk Operations Two new flags for batch management: rmpj --all # Remove all projects (with confirmation) scpj --reset # Clear registry and re-scan (fresh start) The scan command also handles nickname collisions better now. Instead of numeric suffixes (webapp2), it uses owner-prefixed names (acme-webapp) based on the git remote. New Commands Summary Command Alias Description pjmai history [N] hypj Show or jump to navigation history pjmai stack show stpj Show the project stack pjmai stack clear stpj clear Clear the stack pjmai remove --all rmpj --all Remove all projects pjmai scan --reset scpj --reset Fresh re-scan What’s Next The focus continues on making project context switching invisible. Upcoming work: Nono integration: Sandboxing untrusted projects AI agent restricted mode: Curated PATH for autonomous agents Multi-machine sync: Share project registry across machines The best developer tools are the ones you stop noticing.]]></summary></entry><entry><title type="html">rank-wav: Ranking Audio Files by Acoustic Quality</title><link href="https://software-wrighter-lab.github.io/2026/03/06/rank-wav-audio-quality-ranking/" rel="alternate" type="text/html" title="rank-wav: Ranking Audio Files by Acoustic Quality" /><published>2026-03-06T00:15:00-08:00</published><updated>2026-03-06T00:15:00-08:00</updated><id>https://software-wrighter-lab.github.io/2026/03/06/rank-wav-audio-quality-ranking</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/03/06/rank-wav-audio-quality-ranking/"><![CDATA[<p><img src="/assets/images/posts/block-boombox.png" class="post-marker" alt="" /></p>

<p>You’ve generated 50 variations of a synthesized sound. Or you’ve downloaded a sample pack with hundreds of one-shots. Now what? Listening to each one is tedious. You need a way to rank them—fast.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/watch?v=F8iP4JLzQ80">rank-wav Demo</a><br /><a href="https://www.youtube.com/watch?v=F8iP4JLzQ80"><img src="https://img.youtube.com/vi/F8iP4JLzQ80/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Original Repo</strong></td>
        <td><a href="https://github.com/sw-cli-tools/rank-wav-rs">sw-cli-tools/rank-wav-rs</a></td>
      </tr>
      <tr>
        <td><strong>Current Dev Repo</strong></td>
        <td><a href="https://github.com/sw-music-tools/rank-wav-rs">sw-music-tools/rank-wav-rs</a></td>
      </tr>
      <tr>
        <td><strong>Motivation</strong></td>
        <td><a href="#motivation">Why I Built This</a></td>
      </tr>
      <tr>
        <td><strong>Related</strong></td>
        <td><a href="#related-projects">Music Generation Tools</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<div style="clear: both;"></div>

<h2 id="what-is-rank-wav">What is rank-wav?</h2>

<p>rank-wav is a Rust CLI that scans directories for WAV files and ranks them by acoustic features correlated with perceived sound quality. It extracts features like RMS energy, spectral centroid, and bandwidth, then computes two scores:</p>

<ul>
  <li><strong>Pleasing</strong>: Favors warm, smooth sounds (low brightness, moderate energy)</li>
  <li><strong>Best</strong>: Favors clear, present sounds (balanced spectrum, strong signal)</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>rank-wav ./samples <span class="nt">--sort</span> pleasing

+---+------------------------+--------+--------+----------+-----------+----------+--------+
| <span class="c"># | File                   | RMS    | ZCR    | Centroid | Bandwidth | Pleasing | Best   |</span>
+---+------------------------+--------+--------+----------+-----------+----------+--------+
| 1 | motif-warm.wav         | 0.0271 | 0.0190 | 763      | 1079      | 0.812    | 0.641  |
| 2 | motif-balanced.wav     | 0.0647 | 0.0480 | 1502     | 1515      | 0.487    | 0.844  |
| 3 | motif-bright.wav       | 0.0361 | 0.0530 | 1782     | 1469      | 0.362    | 0.611  |
+---+------------------------+--------+--------+----------+-----------+----------+--------+
</code></pre></div></div>

<h2 id="the-features">The Features</h2>

<h3 id="basic-metrics">Basic Metrics</h3>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>What It Measures</th>
      <th>Quality Correlation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>RMS</strong></td>
      <td>Signal strength/loudness</td>
      <td>Present vs weak</td>
    </tr>
    <tr>
      <td><strong>ZCR</strong></td>
      <td>Zero-crossing rate</td>
      <td>Noisiness</td>
    </tr>
    <tr>
      <td><strong>Centroid</strong></td>
      <td>Spectral center of mass</td>
      <td>Brightness</td>
    </tr>
    <tr>
      <td><strong>Bandwidth</strong></td>
      <td>Spectral spread</td>
      <td>Complexity</td>
    </tr>
  </tbody>
</table>

<h3 id="extended-metrics">Extended Metrics</h3>

<p>With the <code class="language-plaintext highlighter-rouge">-e</code> flag, rank-wav also computes:</p>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>What It Measures</th>
      <th>Quality Correlation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Rolloff</strong></td>
      <td>Frequency below which 85% of energy lies</td>
      <td>High-frequency content</td>
    </tr>
    <tr>
      <td><strong>Flatness</strong></td>
      <td>How noise-like vs tonal (0-1)</td>
      <td>Tonal quality</td>
    </tr>
    <tr>
      <td><strong>Crest</strong></td>
      <td>Peak to RMS ratio (dB)</td>
      <td>Dynamic range</td>
    </tr>
  </tbody>
</table>

<h2 id="how-scoring-works">How Scoring Works</h2>

<h3 id="the-pleasing-score">The “Pleasing” Score</h3>

<p>The pleasing score favors sounds that are warm and easy to listen to:</p>

<ul>
  <li><strong>Lower spectral centroid</strong> (less harsh, less bright)</li>
  <li><strong>Lower spectral bandwidth</strong> (less complex, more focused)</li>
  <li><strong>Lower zero-crossing rate</strong> (less noisy)</li>
  <li><strong>Moderate RMS</strong> (present but not aggressive)</li>
</ul>

<p>This is useful for: background music, ambient sounds, relaxation audio.</p>

<h3 id="the-best-score">The “Best” Score</h3>

<p>The best score favors sounds that are clear and impactful:</p>

<ul>
  <li><strong>Strong RMS</strong> (present, not weak)</li>
  <li><strong>Moderate spectral centroid</strong> (balanced brightness)</li>
  <li><strong>Moderate bandwidth</strong> (neither thin nor muddy)</li>
  <li><strong>Low zero-crossing rate</strong> (clean signal)</li>
</ul>

<p>This is useful for: sound design, music production, sample selection.</p>

<h2 id="use-cases">Use Cases</h2>

<h3 id="procedural-audio-triage">Procedural Audio Triage</h3>

<p>You’ve generated 100 variations of a procedural sound. Instead of listening to all of them:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rank-wav ./generated <span class="nt">-r</span> <span class="nt">--sort</span> best | <span class="nb">head</span> <span class="nt">-20</span>
</code></pre></div></div>

<p>Listen to the top 20. If none work, adjust your synthesis parameters and try again.</p>

<h3 id="sample-library-organization">Sample Library Organization</h3>

<p>A 500-sample library is overwhelming. Rank by pleasing score to find the smoothest, warmest options first:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rank-wav ./samples <span class="nt">-r</span> <span class="nt">--sort</span> pleasing <span class="nt">--json</span> <span class="o">&gt;</span> ranked.json
</code></pre></div></div>

<p>Then use the JSON to build a playlist of just the top tier.</p>

<h3 id="ab-testing-synthesis-parameters">A/B Testing Synthesis Parameters</h3>

<p>Compare two batches of outputs:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rank-wav ./batch-a <span class="nt">-r</span> <span class="nt">--sort</span> best
rank-wav ./batch-b <span class="nt">-r</span> <span class="nt">--sort</span> best
</code></pre></div></div>

<p>Which batch has higher average scores? That tells you which parameter set produces better results.</p>

<div class="resource-box">

  <h3 id="motivation">Motivation</h3>

  <p>I am trying to automate video production and I want unique music intros/outros for most videos. I do not want to manually specify inputs to music generation or manually review the outputs to choose one. Instead I want an AI Agent to generate different audio wav files, based on an idea or description from me, and the AI Agent can pick the best ones for the preview I review before uploading.</p>

</div>

<h2 id="technical-implementation">Technical Implementation</h2>

<h3 id="pure-rust">Pure Rust</h3>

<p>rank-wav uses only Rust crates with no C dependencies:</p>

<ul>
  <li><strong>hound</strong> for WAV parsing (8/16/24/32-bit int, 32-bit float)</li>
  <li><strong>rustfft</strong> for FFT-based spectral analysis</li>
  <li><strong>clap</strong> for CLI parsing</li>
  <li><strong>tabled</strong> for formatted output</li>
</ul>

<p>No system library dependencies—clone, build, and run.</p>

<h3 id="windowed-fft">Windowed FFT</h3>

<p>To compute spectral features, rank-wav:</p>

<ol>
  <li>Extracts the center segment of the audio (up to 16384 samples)</li>
  <li>Applies a Hann window to reduce spectral leakage</li>
  <li>Computes the FFT</li>
  <li>Calculates centroid, bandwidth, rolloff, and flatness from the magnitude spectrum</li>
</ol>

<h3 id="batch-normalization">Batch Normalization</h3>

<p>All features are normalized relative to the current batch. This means:</p>

<ul>
  <li>Scores are meaningful within a comparison set</li>
  <li>No need for absolute calibration</li>
  <li>Rankings work regardless of overall loudness</li>
</ul>

<p>The trade-off: scores from different runs aren’t directly comparable.</p>

<h2 id="installation">Installation</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/sw-music-tools/rank-wav-rs
<span class="nb">cd </span>rank-wav-rs
cargo <span class="nb">install</span> <span class="nt">--path</span> <span class="nb">.</span>
</code></pre></div></div>

<p>The binary installs as <code class="language-plaintext highlighter-rouge">rank-wav</code>.</p>

<h2 id="example-workflow">Example Workflow</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Scan recursively, sort by best, output JSON</span>
rank-wav ./my-samples <span class="nt">-r</span> <span class="nt">-e</span> <span class="nt">--sort</span> best <span class="nt">--json</span> <span class="o">&gt;</span> results.json

<span class="c"># Quick table of top pleasing sounds</span>
rank-wav ./my-samples <span class="nt">-r</span> <span class="nt">--sort</span> pleasing

<span class="c"># Check a single directory (non-recursive)</span>
rank-wav ./one-shots
</code></pre></div></div>

<h2 id="why-not-just-listen">Why Not Just Listen?</h2>

<p>You should still listen—but to the top candidates, not all of them. rank-wav is a filter that surfaces the most promising files based on acoustic characteristics. It’s not a replacement for your ears; it’s a tool to make your ears more productive.</p>

<h2 id="related-projects">Related Projects</h2>

<p>For generating the WAV files that rank-wav ranks:</p>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://github.com/softwarewrighter/midi-cli-rs">midi-cli-rs</a></td>
      <td>CLI for MIDI file manipulation and synthesis</td>
    </tr>
    <tr>
      <td><a href="https://github.com/softwarewrighter/music-pipe-rs">music-pipe-rs</a></td>
      <td>Pipeline for AI-driven music generation</td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>When you have too many sounds to listen to, let the math do the first pass.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="cli-tools" /><category term="rust" /><category term="audio" /><category term="rust" /><category term="cli" /><category term="audio" /><category term="fft" /><category term="signal-processing" /><category term="wav" /><summary type="html"><![CDATA[You’ve generated 50 variations of a synthesized sound. Or you’ve downloaded a sample pack with hundreds of one-shots. Now what? Listening to each one is tedious. You need a way to rank them—fast. Resource Link Video rank-wav Demo Original Repo sw-cli-tools/rank-wav-rs Current Dev Repo sw-music-tools/rank-wav-rs Motivation Why I Built This Related Music Generation Tools Comments Discord What is rank-wav? rank-wav is a Rust CLI that scans directories for WAV files and ranks them by acoustic features correlated with perceived sound quality. It extracts features like RMS energy, spectral centroid, and bandwidth, then computes two scores: Pleasing: Favors warm, smooth sounds (low brightness, moderate energy) Best: Favors clear, present sounds (balanced spectrum, strong signal) $ rank-wav ./samples --sort pleasing +---+------------------------+--------+--------+----------+-----------+----------+--------+ | # | File | RMS | ZCR | Centroid | Bandwidth | Pleasing | Best | +---+------------------------+--------+--------+----------+-----------+----------+--------+ | 1 | motif-warm.wav | 0.0271 | 0.0190 | 763 | 1079 | 0.812 | 0.641 | | 2 | motif-balanced.wav | 0.0647 | 0.0480 | 1502 | 1515 | 0.487 | 0.844 | | 3 | motif-bright.wav | 0.0361 | 0.0530 | 1782 | 1469 | 0.362 | 0.611 | +---+------------------------+--------+--------+----------+-----------+----------+--------+ The Features Basic Metrics Feature What It Measures Quality Correlation RMS Signal strength/loudness Present vs weak ZCR Zero-crossing rate Noisiness Centroid Spectral center of mass Brightness Bandwidth Spectral spread Complexity Extended Metrics With the -e flag, rank-wav also computes: Feature What It Measures Quality Correlation Rolloff Frequency below which 85% of energy lies High-frequency content Flatness How noise-like vs tonal (0-1) Tonal quality Crest Peak to RMS ratio (dB) Dynamic range How Scoring Works The “Pleasing” Score The pleasing score favors sounds that are warm and easy to listen to: Lower spectral centroid (less harsh, less bright) Lower spectral bandwidth (less complex, more focused) Lower zero-crossing rate (less noisy) Moderate RMS (present but not aggressive) This is useful for: background music, ambient sounds, relaxation audio. The “Best” Score The best score favors sounds that are clear and impactful: Strong RMS (present, not weak) Moderate spectral centroid (balanced brightness) Moderate bandwidth (neither thin nor muddy) Low zero-crossing rate (clean signal) This is useful for: sound design, music production, sample selection. Use Cases Procedural Audio Triage You’ve generated 100 variations of a procedural sound. Instead of listening to all of them: rank-wav ./generated -r --sort best | head -20 Listen to the top 20. If none work, adjust your synthesis parameters and try again. Sample Library Organization A 500-sample library is overwhelming. Rank by pleasing score to find the smoothest, warmest options first: rank-wav ./samples -r --sort pleasing --json &gt; ranked.json Then use the JSON to build a playlist of just the top tier. A/B Testing Synthesis Parameters Compare two batches of outputs: rank-wav ./batch-a -r --sort best rank-wav ./batch-b -r --sort best Which batch has higher average scores? That tells you which parameter set produces better results. Motivation I am trying to automate video production and I want unique music intros/outros for most videos. I do not want to manually specify inputs to music generation or manually review the outputs to choose one. Instead I want an AI Agent to generate different audio wav files, based on an idea or description from me, and the AI Agent can pick the best ones for the preview I review before uploading. Technical Implementation Pure Rust rank-wav uses only Rust crates with no C dependencies: hound for WAV parsing (8/16/24/32-bit int, 32-bit float) rustfft for FFT-based spectral analysis clap for CLI parsing tabled for formatted output No system library dependencies—clone, build, and run. Windowed FFT To compute spectral features, rank-wav: Extracts the center segment of the audio (up to 16384 samples) Applies a Hann window to reduce spectral leakage Computes the FFT Calculates centroid, bandwidth, rolloff, and flatness from the magnitude spectrum Batch Normalization All features are normalized relative to the current batch. This means: Scores are meaningful within a comparison set No need for absolute calibration Rankings work regardless of overall loudness The trade-off: scores from different runs aren’t directly comparable. Installation git clone https://github.com/sw-music-tools/rank-wav-rs cd rank-wav-rs cargo install --path . The binary installs as rank-wav. Example Workflow # Scan recursively, sort by best, output JSON rank-wav ./my-samples -r -e --sort best --json &gt; results.json # Quick table of top pleasing sounds rank-wav ./my-samples -r --sort pleasing # Check a single directory (non-recursive) rank-wav ./one-shots Why Not Just Listen? You should still listen—but to the top candidates, not all of them. rank-wav is a filter that surfaces the most promising files based on acoustic characteristics. It’s not a replacement for your ears; it’s a tool to make your ears more productive. Related Projects For generating the WAV files that rank-wav ranks: Project Description midi-cli-rs CLI for MIDI file manipulation and synthesis music-pipe-rs Pipeline for AI-driven music generation When you have too many sounds to listen to, let the math do the first pass.]]></summary></entry></feed>