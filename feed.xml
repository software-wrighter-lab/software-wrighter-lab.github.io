<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://software-wrighter-lab.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://software-wrighter-lab.github.io/" rel="alternate" type="text/html" /><updated>2026-04-24T08:42:35-07:00</updated><id>https://software-wrighter-lab.github.io/feed.xml</id><title type="html">Software Wrighter Lab Blog</title><subtitle>AI coding agents, systems programming, and practical machine learning</subtitle><author><name>Mike Wright</name></author><entry><title type="html">Large-Language-Monkeys: Scaling Inference, Not Models</title><link href="https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference/" rel="alternate" type="text/html" title="Large-Language-Monkeys: Scaling Inference, Not Models" /><published>2026-04-24T00:15:00-07:00</published><updated>2026-04-24T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/24/large-language-monkeys-scaling-inference/"><![CDATA[<p><img src="/assets/images/posts/block-monkeys-typing.png" class="post-marker no-invert" alt="" style="width: 340px;" /></p>

<div style="overflow: hidden;">

  <p>“Large Language Monkeys” is Brown et al.’s (2024) nod to the infinite-monkey theorem, reframed for inference-time compute: if you let a model take many attempts at a problem — and you have an automatic verifier to pick the right one — the odds of at least one attempt being correct climb faster than almost any intuition predicts. Coverage (the fraction of problems solved by <em>any</em> sample) scales log-linearly with the number of samples over <strong>four orders of magnitude</strong>. Small models stop being second-class. Inference becomes a first-class scaling axis.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Paper</strong></td>
        <td><a href="https://arxiv.org/abs/2407.21787">arXiv 2407.21787</a> (Brown et al., 2024)</td>
      </tr>
      <tr>
        <td><strong>Code (forked)</strong></td>
        <td><a href="https://github.com/sw-ml-study/Repeated-Sampling">sw-ml-study/Repeated-Sampling</a></td>
      </tr>
      <tr>
        <td><strong>Code (original)</strong></td>
        <td><a href="https://github.com/weagan/Repeated-Sampling">weagan/Repeated-Sampling</a></td>
      </tr>
      <tr>
        <td><strong>Demo notebook</strong></td>
        <td><a href="https://github.com/sw-ml-study/Repeated-Sampling/blob/main/llm_monkeys_function_demo.ipynb">llm_monkeys_function_demo.ipynb</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-reframing">The Reframing</h2>

<p>The last decade of scaling has been about <em>making the model bigger</em> — more parameters, more training data, more pretraining compute. Inference, by contrast, has stayed boringly fixed: prompt in, answer out, one attempt, take it or leave it.</p>

<p>This paper asks: what if the <em>attempts</em> are a scaling axis?</p>

<table>
  <thead>
    <tr>
      <th>Traditional scaling</th>
      <th>Inference-time scaling</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Bigger model</td>
      <td>More attempts per problem</td>
    </tr>
    <tr>
      <td>More training data</td>
      <td>Repeated sampling</td>
    </tr>
    <tr>
      <td>Single-shot answer</td>
      <td>Multi-sample + verification</td>
    </tr>
  </tbody>
</table>

<p>The mental model shifts from <em>“the model knows the answer”</em> to <em>“the model generates candidate answers; we search for a correct one.”</em> Under that lens, an LLM isn’t an oracle — it’s a stochastic proposer wired to a verifier.</p>

<h2 id="the-core-loop">The Core Loop</h2>

<p>Strip it to essentials:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for i in 1..N:
    sample_i = model(prompt, temperature=T)
    score_i  = verifier(sample_i)
return best(sample_i)
</code></pre></div></div>

<p>Two components:</p>

<ol>
  <li><strong>Generator</strong> — an LLM producing candidate solutions. Temperature drives diversity.</li>
  <li><strong>Verifier / selector</strong> — unit tests (code), symbolic checker (math), heuristic scorer. Picks the right answer from the pile.</li>
</ol>

<p>The verifier is the load-bearing piece. When it’s cheap and deterministic (code → run tests; math proof → check with Lean), repeated sampling is devastatingly effective. When it isn’t (essays, subjective tasks, open-ended reasoning), the scaling story gets thornier.</p>

<h2 id="what-the-numbers-look-like">What the Numbers Look Like</h2>

<p>The paper’s headline result on SWE-bench Lite:</p>

<table>
  <thead>
    <tr>
      <th>Setup</th>
      <th style="text-align: right">Solve rate</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct, 1 sample</td>
      <td style="text-align: right">15.9%</td>
    </tr>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct, 250 samples</td>
      <td style="text-align: right"><strong>56.0%</strong></td>
    </tr>
    <tr>
      <td>Single-sample SOTA at time of writing</td>
      <td style="text-align: right">43.0%</td>
    </tr>
  </tbody>
</table>

<p>Repeated sampling from a <em>non-frontier</em> model blew past the frontier single-sample SOTA on one of the hardest real-world coding benchmarks. Not by a little — by 13 percentage points.</p>

<p>The cost story hits even harder:</p>

<table>
  <thead>
    <tr>
      <th>Configuration</th>
      <th style="text-align: right">Solve rate</th>
      <th style="text-align: right">Cost</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DeepSeek-Coder-V2-Instruct × 5 samples</td>
      <td style="text-align: right">29.62%</td>
      <td style="text-align: right"><strong>$10.80</strong></td>
    </tr>
    <tr>
      <td>GPT-4o × 1 sample</td>
      <td style="text-align: right">24.00%</td>
      <td style="text-align: right">$39</td>
    </tr>
    <tr>
      <td>Claude 3.5 Sonnet × 1 sample</td>
      <td style="text-align: right">26.70%</td>
      <td style="text-align: right">$51</td>
    </tr>
  </tbody>
</table>

<p>Five DeepSeek attempts beat one attempt from each frontier model at roughly <strong>4× lower cost</strong>. Repeated sampling changes the economics of deploying AI on verifiable tasks.</p>

<h2 id="the-coverage-scaling-law">The Coverage Scaling Law</h2>

<p>The paper models coverage (<code class="language-plaintext highlighter-rouge">c</code>, probability at least one of <code class="language-plaintext highlighter-rouge">k</code> samples is correct) as an <strong>exponentiated power law</strong>:</p>

<div style="text-align: center; font-family: Georgia, 'Times New Roman', serif; font-size: 1.3em; font-style: italic; margin: 1.25em 0;">
  c(k) &nbsp;≈&nbsp; exp(a &middot; k<sup>b</sup>)
</div>

<p>where <code class="language-plaintext highlighter-rouge">k</code> is the number of samples and <code class="language-plaintext highlighter-rouge">a</code>, <code class="language-plaintext highlighter-rouge">b</code> are fitted parameters. Across GSM8K, MATH, MiniF2F, SWE-bench Lite, and CodeContests, coverage scales log-linearly with the sample count over four orders of magnitude. On CodeContests with Gemma-2B, coverage climbs from <strong>0.02% at 1 sample to 7.1% at 10k samples</strong> — that’s more than a 350× improvement, purely from adding attempts.</p>

<p>The shape matters. A log-linear curve means every <em>multiplicative</em> increase in samples yields a fixed <em>additive</em> gain in coverage. Doubling samples → constant boost in solve rate. This is a scaling <em>law</em>, not a diminishing-returns saturation. It’s why the paper’s title matters: enough monkeys, enough time, and Shakespeare emerges from chance. The quantitative claim is that the “enough” is measurable and predictable.</p>

<style>
  /* Stagger sections occupy cols 1-3 / 2-4 / 3-5 of a 5-col grid (60% width).
     Gutter images fill the remaining 2 cols (40%) next to the stagger paragraph. */
  .post-content .stagger { position: relative; }
  .gutter-img-left,
  .gutter-img-right {
    position: absolute;
    top: 0;
    /* 66.67% of a 60%-wide section = 40% of the container = 2 of 5 cols */
    width: 66.67%;
    max-width: none;
    /* Match paragraph height exactly; scale-to-fit without cropping */
    height: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
  }
  .gutter-img-left  { right: calc(100% + 1.25rem); }
  .gutter-img-right { left:  calc(100% + 1.25rem); }
  @media (max-width: 900px) {
    .gutter-img-left,
    .gutter-img-right {
      position: static;
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0.5em auto 1em;
    }
  }
</style>

<h2 id="weak-models-smart-sampling">Weak Models, Smart Sampling</h2>

<p><img src="/assets/images/posts/block-flying-monkeys-brain-statue.png" class="gutter-img-left no-invert" alt="" /></p>

<p>The most important empirical finding for practitioners: <em>smaller models outperform larger ones at matched compute</em>, on the right tasks.</p>

<p>On FLOP-matched comparisons across MATH, GSM8K, and MiniF2F, Llama-3-8B-Instruct with more samples achieves higher coverage than Llama-3-70B-Instruct — at the <em>same</em> total inference FLOPs. The 8B model, sampled more aggressively, beats the 70B sampled once.</p>

<p>There are caveats:</p>

<ul>
  <li><strong>CodeContests is an exception</strong> — the 70B model stays more cost-effective; the smaller-wins pattern is task-dependent.</li>
  <li><strong>Verifier quality gates everything.</strong> Coverage gains don’t translate into solve-rate gains without a sharp verifier.</li>
  <li><strong>Majority voting and reward models plateau</strong> beyond a few hundred samples in domains lacking automatic verification. Selection, not generation, is the bottleneck.</li>
</ul>

<p>But for code, math, and formal proofs — the workloads where you can <em>prove</em> correctness of an individual sample — the “more small samples” strategy is cheaper <em>and</em> better.</p>

<h2 id="reproducing-the-shape-binary-search-fix">Reproducing the Shape: Binary Search Fix</h2>

<p>The accompanying notebook (forked to <a href="https://github.com/sw-ml-study/Repeated-Sampling">sw-ml-study/Repeated-Sampling</a>, original by <a href="https://github.com/weagan/Repeated-Sampling">weagan</a>) makes the paper’s claim tangible on a small budget.</p>

<p>The task: fix a buggy <code class="language-plaintext highlighter-rouge">binary_search</code> that has an off-by-one (<code class="language-plaintext highlighter-rouge">lo &lt; hi</code> instead of <code class="language-plaintext highlighter-rouge">&lt;=</code>) and must return the <strong>first occurrence</strong> of duplicates. Tested against 24 hidden cases.</p>

<p>Two configurations:</p>

<ul>
  <li><strong>Big model</strong>: <code class="language-plaintext highlighter-rouge">llama-3.3-70b-versatile</code>, temperature 0.0, 1 sample.</li>
  <li><strong>Small model</strong>: <code class="language-plaintext highlighter-rouge">llama-3.1-8b-instant</code>, temperature 0.8, N samples (where N ∈ {1, 5, 10, 15}).</li>
</ul>

<p>The core loop:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">all_fixes</span><span class="p">,</span> <span class="n">all_results</span><span class="p">,</span> <span class="n">coverage</span> <span class="o">=</span> <span class="p">[],</span> <span class="p">[],</span> <span class="p">[]</span>

<span class="c1"># Generate
</span><span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">max</span><span class="p">(</span><span class="n">sample_counts</span><span class="p">)):</span>
    <span class="n">fix</span> <span class="o">=</span> <span class="nf">generate_fix</span><span class="p">(</span><span class="n">BINARY_SEARCH_TASK</span><span class="p">,</span> <span class="n">SMALL_MODEL</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.8</span><span class="p">)</span>
    <span class="n">all_fixes</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">fix</span><span class="p">)</span>

<span class="c1"># Verify each
</span><span class="k">for</span> <span class="n">fix</span> <span class="ow">in</span> <span class="n">all_fixes</span><span class="p">:</span>
    <span class="n">passed</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="nf">test_function</span><span class="p">(</span><span class="n">fix</span><span class="p">,</span> <span class="n">BINARY_SEARCH_TASK</span><span class="p">)</span>
    <span class="n">all_results</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">passed</span><span class="p">)</span>

<span class="c1"># Cumulative coverage at each N
</span><span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="n">sample_counts</span><span class="p">:</span>
    <span class="n">coverage</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">any</span><span class="p">(</span><span class="n">all_results</span><span class="p">[:</span><span class="n">n</span><span class="p">]))</span>
</code></pre></div></div>

<p>Separation of generation from verification is the shape of every repeated-sampling system you’ll build after this: a batch of stochastic proposers, a deterministic scorer, a selection pass.</p>

<h3 id="observed-results">Observed results</h3>

<ul>
  <li>70B × 1 sample: passes (375 characters of code).</li>
  <li>8B × 15 samples: <strong>3 out of 15 pass</strong> (20%), but since “coverage” is “at least one passes,” the 8B model <em>wins at every tested N</em>.</li>
  <li>Cost: 70B @ 1 = $0.00034; 8B @ 15 = $0.00047 (marginally more, for a 15× robustness margin); 8B @ 10 = $0.00031 (cheaper than the single 70B call).</li>
</ul>

<h3 id="the-temperature-sweep">The temperature sweep</h3>

<p>Running 10 samples of the 8B model across six temperatures:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Temperature</th>
      <th style="text-align: center">Success rate (10 samples)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0.0</td>
      <td style="text-align: center">0%</td>
    </tr>
    <tr>
      <td style="text-align: center">0.5</td>
      <td style="text-align: center">50%</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>0.8</strong></td>
      <td style="text-align: center"><strong>60%</strong></td>
    </tr>
    <tr>
      <td style="text-align: center">1.0</td>
      <td style="text-align: center">30%</td>
    </tr>
    <tr>
      <td style="text-align: center">1.2</td>
      <td style="text-align: center">40%</td>
    </tr>
  </tbody>
</table>

<p>Temperature 0.8 is the sweet spot. Colder, and the model produces the same wrong answer every time — all monkeys compose identical gibberish, so coverage stays at zero. Hotter, and diversity starts corrupting the structure faster than it finds the solution — monkeys hammering ∞ Shakespeares in parallel but none completing a line. Exploration vs. exploitation, in one experiment, with a neat inverted-U.</p>

<p>This is a pedagogical gem: a single knob controls the trade-off and the wrong setting invalidates the whole approach.</p>

<h2 id="where-this-connects">Where This Connects</h2>

<p>Once you see LLMs as stochastic proposers + verifiers, a lot of modern infrastructure makes more sense:</p>

<ul>
  <li><strong>CI/CD coding agents.</strong> Tests are the verifier; parallel agent attempts are the samples. “Run the test suite 10 times with 10 different patches” is <em>exactly</em> repeated sampling.</li>
  <li><strong>Multi-agent orchestration.</strong> Each agent is one monkey. Diversity across agents (different prompts, tools, temperatures) is the dispersion that fuels coverage.</li>
  <li><strong>Tool-augmented LLMs.</strong> Tools execute, execution results verify, model iterates. Three laps around the generator/verifier loop.</li>
  <li><strong>Cascading models.</strong> Cheap model attempts many times → if all fail, escalate to an expensive model. Cost-tuned coverage scaling.</li>
</ul>

<p>The paper is, quietly, a manifesto for a particular kind of system: <em>stop buying smarter models, start building smarter ways to use the ones you have</em>.</p>

<h2 id="what-breaks">What Breaks</h2>

<p>The limitations are worth naming:</p>

<table>
  <thead>
    <tr>
      <th>Issue</th>
      <th>Consequence</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>No verifier</strong></td>
      <td>Majority voting and reward-model scoring plateau beyond a few hundred samples; coverage doesn’t translate to solve rate</td>
    </tr>
    <tr>
      <td><strong>i.i.d. sampling</strong></td>
      <td>The paper does <em>not</em> explore diversity-forcing strategies (re-prompting, retrieval, temperature schedules) — that’s future work</td>
    </tr>
    <tr>
      <td><strong>No feedback loop</strong></td>
      <td>Each sample is independent; failed samples don’t inform later ones. Agent systems that do use feedback are already departing from pure repeated sampling</td>
    </tr>
    <tr>
      <td><strong>Latency</strong></td>
      <td>Total wall-clock time grows unless samples run in parallel. Inference infrastructure has to favor throughput over per-request latency — a different shape than chatbot serving</td>
    </tr>
    <tr>
      <td><strong>Task-dependence</strong></td>
      <td>CodeContests shows the 70B still wins; smaller-beats-larger isn’t universal</td>
    </tr>
  </tbody>
</table>

<p>Selection is the choke point. Generation scales logarithmically “for free”; turning coverage into answers needs a verifier that itself scales.</p>

<h2 id="why-it-matters">Why It Matters</h2>

<p>Three shifts follow from taking this paper seriously:</p>

<ol>
  <li><strong>Smaller models become more useful.</strong> A 7B–13B model with a good verifier and parallel infrastructure can out-solve a 70B–400B single-shot on verifiable workloads.</li>
  <li><strong>Systems beat individual models.</strong> The pipeline (generator + verifier + selector + orchestrator) is the product. The model is a component.</li>
  <li><strong>Compute orchestration is a core skill.</strong> Batching, scheduling, cost-aware sample allocation, adaptive early-stopping — these are more leverage-able than model choice on most production workloads.</li>
</ol>

<p>The paper’s closing framing — <em>intelligence = search + verification over stochastic outputs</em> — isn’t a complete theory of intelligence. But it’s a useful reduction for the subset of problems where correctness is checkable: which turns out to be a surprisingly large and growing fraction of what we ask LLMs to do.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Open directions the paper gestures at:</p>

<ul>
  <li><strong>Learned verifiers.</strong> What happens when the scorer itself is a model, trained to rank candidates? (Reward models plateau; can better selectors scale?)</li>
  <li><strong>Self-consistency voting</strong> as a middle-ground between pure verification and reward models.</li>
  <li><strong>Tool-augmented sampling</strong>: samples don’t just propose text, they propose <em>actions</em> that get executed and verified.</li>
  <li><strong>Adaptive sample budgets</strong>: spend more where uncertainty is high, less where the first sample already looks good.</li>
</ul>

<p>The repeated-sampling axis is new enough that most of the engineering is yet to be done. The scaling law is the load-bearing result; the systems that exploit it are still being built.</p>

<hr />

<p><em>The paper title is a reference, but not a joke — with enough attempts and a reliable scorer, an arbitrarily weak model becomes reliable. The leverage is in the scorer. The monkeys are easy.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="llm" /><category term="machine-learning" /><category term="research" /><category term="machine-learning" /><category term="large-language-monkeys" /><category term="repeated-sampling" /><category term="inference-scaling" /><category term="verification" /><category term="swe-bench" /><category term="small-models" /><category term="agents" /><category term="coverage" /><category term="scaling-laws" /><summary type="html"><![CDATA[“Large Language Monkeys” is Brown et al.’s (2024) nod to the infinite-monkey theorem, reframed for inference-time compute: if you let a model take many attempts at a problem — and you have an automatic verifier to pick the right one — the odds of at least one attempt being correct climb faster than almost any intuition predicts. Coverage (the fraction of problems solved by any sample) scales log-linearly with the number of samples over four orders of magnitude. Small models stop being second-class. Inference becomes a first-class scaling axis. Resource Link Paper arXiv 2407.21787 (Brown et al., 2024) Code (forked) sw-ml-study/Repeated-Sampling Code (original) weagan/Repeated-Sampling Demo notebook llm_monkeys_function_demo.ipynb Comments Discord The Reframing The last decade of scaling has been about making the model bigger — more parameters, more training data, more pretraining compute. Inference, by contrast, has stayed boringly fixed: prompt in, answer out, one attempt, take it or leave it. This paper asks: what if the attempts are a scaling axis? Traditional scaling Inference-time scaling Bigger model More attempts per problem More training data Repeated sampling Single-shot answer Multi-sample + verification The mental model shifts from “the model knows the answer” to “the model generates candidate answers; we search for a correct one.” Under that lens, an LLM isn’t an oracle — it’s a stochastic proposer wired to a verifier. The Core Loop Strip it to essentials: for i in 1..N: sample_i = model(prompt, temperature=T) score_i = verifier(sample_i) return best(sample_i) Two components: Generator — an LLM producing candidate solutions. Temperature drives diversity. Verifier / selector — unit tests (code), symbolic checker (math), heuristic scorer. Picks the right answer from the pile. The verifier is the load-bearing piece. When it’s cheap and deterministic (code → run tests; math proof → check with Lean), repeated sampling is devastatingly effective. When it isn’t (essays, subjective tasks, open-ended reasoning), the scaling story gets thornier. What the Numbers Look Like The paper’s headline result on SWE-bench Lite: Setup Solve rate DeepSeek-Coder-V2-Instruct, 1 sample 15.9% DeepSeek-Coder-V2-Instruct, 250 samples 56.0% Single-sample SOTA at time of writing 43.0% Repeated sampling from a non-frontier model blew past the frontier single-sample SOTA on one of the hardest real-world coding benchmarks. Not by a little — by 13 percentage points. The cost story hits even harder: Configuration Solve rate Cost DeepSeek-Coder-V2-Instruct × 5 samples 29.62% $10.80 GPT-4o × 1 sample 24.00% $39 Claude 3.5 Sonnet × 1 sample 26.70% $51 Five DeepSeek attempts beat one attempt from each frontier model at roughly 4× lower cost. Repeated sampling changes the economics of deploying AI on verifiable tasks. The Coverage Scaling Law The paper models coverage (c, probability at least one of k samples is correct) as an exponentiated power law: c(k) &nbsp;≈&nbsp; exp(a &middot; kb) where k is the number of samples and a, b are fitted parameters. Across GSM8K, MATH, MiniF2F, SWE-bench Lite, and CodeContests, coverage scales log-linearly with the sample count over four orders of magnitude. On CodeContests with Gemma-2B, coverage climbs from 0.02% at 1 sample to 7.1% at 10k samples — that’s more than a 350× improvement, purely from adding attempts. The shape matters. A log-linear curve means every multiplicative increase in samples yields a fixed additive gain in coverage. Doubling samples → constant boost in solve rate. This is a scaling law, not a diminishing-returns saturation. It’s why the paper’s title matters: enough monkeys, enough time, and Shakespeare emerges from chance. The quantitative claim is that the “enough” is measurable and predictable. Weak Models, Smart Sampling The most important empirical finding for practitioners: smaller models outperform larger ones at matched compute, on the right tasks. On FLOP-matched comparisons across MATH, GSM8K, and MiniF2F, Llama-3-8B-Instruct with more samples achieves higher coverage than Llama-3-70B-Instruct — at the same total inference FLOPs. The 8B model, sampled more aggressively, beats the 70B sampled once. There are caveats: CodeContests is an exception — the 70B model stays more cost-effective; the smaller-wins pattern is task-dependent. Verifier quality gates everything. Coverage gains don’t translate into solve-rate gains without a sharp verifier. Majority voting and reward models plateau beyond a few hundred samples in domains lacking automatic verification. Selection, not generation, is the bottleneck. But for code, math, and formal proofs — the workloads where you can prove correctness of an individual sample — the “more small samples” strategy is cheaper and better. Reproducing the Shape: Binary Search Fix The accompanying notebook (forked to sw-ml-study/Repeated-Sampling, original by weagan) makes the paper’s claim tangible on a small budget. The task: fix a buggy binary_search that has an off-by-one (lo &lt; hi instead of &lt;=) and must return the first occurrence of duplicates. Tested against 24 hidden cases. Two configurations: Big model: llama-3.3-70b-versatile, temperature 0.0, 1 sample. Small model: llama-3.1-8b-instant, temperature 0.8, N samples (where N ∈ {1, 5, 10, 15}). The core loop: all_fixes, all_results, coverage = [], [], [] # Generate for i in range(max(sample_counts)): fix = generate_fix(BINARY_SEARCH_TASK, SMALL_MODEL, temperature=0.8) all_fixes.append(fix) # Verify each for fix in all_fixes: passed, _ = test_function(fix, BINARY_SEARCH_TASK) all_results.append(passed) # Cumulative coverage at each N for n in sample_counts: coverage.append(any(all_results[:n])) Separation of generation from verification is the shape of every repeated-sampling system you’ll build after this: a batch of stochastic proposers, a deterministic scorer, a selection pass. Observed results 70B × 1 sample: passes (375 characters of code). 8B × 15 samples: 3 out of 15 pass (20%), but since “coverage” is “at least one passes,” the 8B model wins at every tested N. Cost: 70B @ 1 = $0.00034; 8B @ 15 = $0.00047 (marginally more, for a 15× robustness margin); 8B @ 10 = $0.00031 (cheaper than the single 70B call). The temperature sweep Running 10 samples of the 8B model across six temperatures: Temperature Success rate (10 samples) 0.0 0% 0.5 50% 0.8 60% 1.0 30% 1.2 40% Temperature 0.8 is the sweet spot. Colder, and the model produces the same wrong answer every time — all monkeys compose identical gibberish, so coverage stays at zero. Hotter, and diversity starts corrupting the structure faster than it finds the solution — monkeys hammering ∞ Shakespeares in parallel but none completing a line. Exploration vs. exploitation, in one experiment, with a neat inverted-U. This is a pedagogical gem: a single knob controls the trade-off and the wrong setting invalidates the whole approach. Where This Connects Once you see LLMs as stochastic proposers + verifiers, a lot of modern infrastructure makes more sense: CI/CD coding agents. Tests are the verifier; parallel agent attempts are the samples. “Run the test suite 10 times with 10 different patches” is exactly repeated sampling. Multi-agent orchestration. Each agent is one monkey. Diversity across agents (different prompts, tools, temperatures) is the dispersion that fuels coverage. Tool-augmented LLMs. Tools execute, execution results verify, model iterates. Three laps around the generator/verifier loop. Cascading models. Cheap model attempts many times → if all fail, escalate to an expensive model. Cost-tuned coverage scaling. The paper is, quietly, a manifesto for a particular kind of system: stop buying smarter models, start building smarter ways to use the ones you have. What Breaks The limitations are worth naming: Issue Consequence No verifier Majority voting and reward-model scoring plateau beyond a few hundred samples; coverage doesn’t translate to solve rate i.i.d. sampling The paper does not explore diversity-forcing strategies (re-prompting, retrieval, temperature schedules) — that’s future work No feedback loop Each sample is independent; failed samples don’t inform later ones. Agent systems that do use feedback are already departing from pure repeated sampling Latency Total wall-clock time grows unless samples run in parallel. Inference infrastructure has to favor throughput over per-request latency — a different shape than chatbot serving Task-dependence CodeContests shows the 70B still wins; smaller-beats-larger isn’t universal Selection is the choke point. Generation scales logarithmically “for free”; turning coverage into answers needs a verifier that itself scales. Why It Matters Three shifts follow from taking this paper seriously: Smaller models become more useful. A 7B–13B model with a good verifier and parallel infrastructure can out-solve a 70B–400B single-shot on verifiable workloads. Systems beat individual models. The pipeline (generator + verifier + selector + orchestrator) is the product. The model is a component. Compute orchestration is a core skill. Batching, scheduling, cost-aware sample allocation, adaptive early-stopping — these are more leverage-able than model choice on most production workloads. The paper’s closing framing — intelligence = search + verification over stochastic outputs — isn’t a complete theory of intelligence. But it’s a useful reduction for the subset of problems where correctness is checkable: which turns out to be a surprisingly large and growing fraction of what we ask LLMs to do. What’s Next Open directions the paper gestures at: Learned verifiers. What happens when the scorer itself is a model, trained to rank candidates? (Reward models plateau; can better selectors scale?) Self-consistency voting as a middle-ground between pure verification and reward models. Tool-augmented sampling: samples don’t just propose text, they propose actions that get executed and verified. Adaptive sample budgets: spend more where uncertainty is high, less where the first sample already looks good. The repeated-sampling axis is new enough that most of the engineering is yet to be done. The scaling law is the load-bearing result; the systems that exploit it are still being built. The paper title is a reference, but not a joke — with enough attempts and a reliable scorer, an arbitrarily weak model becomes reliable. The leverage is in the scorer. The monkeys are easy.]]></summary></entry><entry><title type="html">Rabbit-hole (3/?): FORTH — Life After Hashing</title><link href="https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing/" rel="alternate" type="text/html" title="Rabbit-hole (3/?): FORTH — Life After Hashing" /><published>2026-04-24T00:15:00-07:00</published><updated>2026-04-24T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/24/rabbit-hole-forth-life-after-hashing/"><![CDATA[<p><img src="/assets/images/posts/block-armed-rabbit-with-bow.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>Second FORTH post in the rabbit hole. <a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Part 1</a> explained why phase 4 (<code class="language-plaintext highlighter-rouge">forth-from-forth</code>) is deleting the XMX hash and the 256-bucket table along with the asm <code class="language-plaintext highlighter-rouge">do_find</code> that used them. This post answers the obvious follow-up: <strong>if not a hash, then what?</strong> Because “no optimization” isn’t quite right either. FIND still runs thousands of times during bootstrap and source loading. The asm-era tools are gone, but the interactive hot path deserves something.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Prior post</strong></td>
        <td><a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</a></td>
      </tr>
      <tr>
        <td><strong>Overview post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-post-hash-toolbox">The Post-Hash Toolbox</h2>

<p>Three optimizations, all cheaper than a hash, all written in pure Forth. None of them require a side table, none require touching dictionary headers, none require boot-time setup. Each one trades a little code for a better hit rate on the patterns that actually show up in source files.</p>

<table>
  <thead>
    <tr>
      <th>Optimization</th>
      <th>Cost</th>
      <th>Hit pattern</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Numeric fast path</strong></td>
      <td>~5 lines of Forth</td>
      <td>Any token starting with a digit or <code class="language-plaintext highlighter-rouge">-&lt;digit&gt;</code></td>
    </tr>
    <tr>
      <td><strong>Recent-hit cache</strong> (N entries)</td>
      <td>~10 lines + N × 2 cells of memory</td>
      <td>Tokens repeated within a short window</td>
    </tr>
    <tr>
      <td><strong>Hot-token cache</strong> (top-K)</td>
      <td>~20 lines + K × 2 cells + some accounting</td>
      <td>The handful of names that appear 100s of times</td>
    </tr>
  </tbody>
</table>

<p>They’re orthogonal. You can ship any one, any two, or all three. The order below is the order I’d install them in: each next one is a little more work for a little more payoff.</p>

<h2 id="optimization-1-numeric-fast-path">Optimization 1: Numeric Fast Path</h2>

<p>Every <code class="language-plaintext highlighter-rouge">.fth</code> source file has numbers in it. In the all-asm INTERPRET, each number makes FIND walk the dictionary, fail to find a match, and <em>then</em> fall through to NUMBER. That’s wasted work — you can tell by the first character that no dictionary word will match.</p>

<p>The fix is a one-line pre-check at the top of INTERPRET:</p>

<pre><code class="language-forth">: INTERPRET
    WORD DUP C@                   \ token (addr len) first-char
    [CHAR] 0 [CHAR] 9 WITHIN
    IF  NUMBER  EXIT  THEN        \ starts with a digit — skip FIND
    ... (normal FIND path) ...
;
</code></pre>

<p><em>(Handle <code class="language-plaintext highlighter-rouge">-&lt;digit&gt;</code> with one more check. Handle <code class="language-plaintext highlighter-rouge">$</code> or <code class="language-plaintext highlighter-rouge">0x</code> hex prefix if your Forth takes those.)</em></p>

<p>On a <code class="language-plaintext highlighter-rouge">.fth</code> source file like <code class="language-plaintext highlighter-rouge">examples/fib.fth</code>:</p>

<pre><code class="language-forth">: FIB ( n -- n' )
    DUP 2 &lt; IF EXIT THEN
    DUP 1- RECURSE
    SWAP 2- RECURSE
    + ;
</code></pre>

<p>The tokens <code class="language-plaintext highlighter-rouge">2</code>, <code class="language-plaintext highlighter-rouge">1-</code>, <code class="language-plaintext highlighter-rouge">2-</code> all start with digits (well, <code class="language-plaintext highlighter-rouge">1-</code> and <code class="language-plaintext highlighter-rouge">2-</code> <em>look like</em> numbers until the dash is consumed and the remaining part doesn’t parse — some Forths special-case <code class="language-plaintext highlighter-rouge">1-</code> as a word anyway; take the simple path and let FIND confirm after the numeric parse fails). The main point: literal numbers like <code class="language-plaintext highlighter-rouge">2</code> get a direct routing to NUMBER and skip FIND entirely.</p>

<p><strong>Measurement shape.</strong> On the COR24 core loading <code class="language-plaintext highlighter-rouge">examples/*.fth</code>, numeric tokens are ~20% of the input stream. Fast-pathing them avoids the equivalent fraction of dictionary walks. One branch, huge payoff.</p>

<p><strong>Caveat.</strong> If your Forth lets words start with digits (<code class="language-plaintext highlighter-rouge">2dup</code>, <code class="language-plaintext highlighter-rouge">2drop</code>, <code class="language-plaintext highlighter-rouge">4th</code>), the fast path has to fall back to FIND on “might-be-a-word” failures. In practice that means: try NUMBER first, and if NUMBER rejects the whole token, <em>then</em> FIND it. The rejection is fast — NUMBER aborts on the first non-digit.</p>

<h2 id="optimization-2-recent-hit-cache">Optimization 2: Recent-Hit Cache</h2>

<p>The circular-buffer design. Keep the last N successful <code class="language-plaintext highlighter-rouge">(hash, cfa, flags)</code> tuples — call it 8 or 16 entries. Every FIND first probes the circular buffer by hash; on hit, return immediately. On miss, walk the dictionary, then insert the result into the next slot of the buffer (overwriting the oldest).</p>

<p>Rough structure in Forth:</p>

<pre><code class="language-forth">16 CONSTANT RECENT-SIZE
CREATE recent-hash  RECENT-SIZE CELLS ALLOT
CREATE recent-cfa   RECENT-SIZE CELLS ALLOT
CREATE recent-flags RECENT-SIZE CELLS ALLOT
VARIABLE recent-next   \ index where the next insert lands

: recent-lookup ( h -- cfa flags | 0 0 )
    RECENT-SIZE 0 DO
        DUP recent-hash I CELLS + @ =
        IF  DROP
            recent-cfa   I CELLS + @
            recent-flags I CELLS + @
            UNLOOP EXIT
        THEN
    LOOP  DROP 0 0 ;

: recent-insert ( h cfa flags -- )
    recent-next @  &gt;R
    R@ recent-flags + !
    R@ recent-cfa   + !
    R@ recent-hash  + !
    recent-next @ 1+  RECENT-SIZE MOD  recent-next ! ;
</code></pre>

<p>A FIND now becomes:</p>

<pre><code class="language-forth">: FIND  ( addr len -- cfa flags )
    hash-name recent-lookup  ?DUP IF EXIT THEN  DROP
    dict-walk  ( returns cfa flags or 0 0 )
    DUP IF  \ found — remember it
        2DUP hash-name -ROT recent-insert
    THEN ;
</code></pre>

<p>Where <code class="language-plaintext highlighter-rouge">hash-name</code> is a <em>cheap</em> hash (just XOR the first 3 characters, for example) — the full XMX is overkill here because we only need to distinguish N=16 candidates, not partition 256 buckets.</p>

<p><strong>Why it works.</strong> Temporal locality. In a typical <code class="language-plaintext highlighter-rouge">.fth</code> block:</p>

<pre><code class="language-forth">: SQUARE DUP * ;
: CUBE DUP SQUARE * ;
: FOURTH SQUARE SQUARE ;
</code></pre>

<p><code class="language-plaintext highlighter-rouge">DUP</code>, <code class="language-plaintext highlighter-rouge">*</code>, <code class="language-plaintext highlighter-rouge">SQUARE</code>, and <code class="language-plaintext highlighter-rouge">;</code> each get looked up multiple times within a few-token window. The recent-hit buffer catches all of it. Buffer size N=8 is enough for most Forth source; N=16 is generous.</p>

<p><strong>Cost.</strong> <code class="language-plaintext highlighter-rouge">N × 3 cells ≈ 72 bytes</code> for a 16-entry cache on COR24 (3-byte cells). Linear scan of 16 entries is ~48 COR24 instructions worst case, fewer on hit. Compare to a hash-table miss walking a chain of 15 — the circular buffer wins whenever the hit rate is above ~25%.</p>

<h2 id="optimization-3-hot-token-cache">Optimization 3: Hot-Token Cache</h2>

<p>The recent-hit cache catches <em>local</em> repetition. What it misses: tokens that appear in many different contexts — <code class="language-plaintext highlighter-rouge">DUP</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">IF</code>, <code class="language-plaintext highlighter-rouge">;</code>. These words show up hundreds of times across a full core-loading pass, but rarely in back-to-back tokens. The recent buffer evicts them between hits.</p>

<p>The hot-token cache handles that shape. Top-K by frequency. K is small — 4 or 8 entries is plenty — because Forth’s name distribution is severely top-heavy: the top 10 words typically account for 40–60% of all lookups in a core.</p>

<p>Structure is essentially the recent cache plus frequency counters:</p>

<pre><code class="language-forth">8 CONSTANT HOT-SIZE
CREATE hot-hash  HOT-SIZE CELLS ALLOT
CREATE hot-cfa   HOT-SIZE CELLS ALLOT
CREATE hot-flags HOT-SIZE CELLS ALLOT
CREATE hot-count HOT-SIZE CELLS ALLOT
</code></pre>

<p>On every dictionary walk that finds a word:</p>
<ul>
  <li>Increment the count for that word’s bucket.</li>
  <li>If any bucket’s count exceeds the minimum in the top-K, promote the new entry and evict the former minimum.</li>
</ul>

<p>Lookup probes the hot cache <em>first</em>, then the recent cache, then does the dictionary walk.</p>

<p><strong>Why it works.</strong> Forth’s word-frequency distribution is Zipf-like. The top 8 names cover roughly half of all lookups in a typical <code class="language-plaintext highlighter-rouge">.fth</code> corpus; the top 32 cover nearly all of them. A tiny K-wide cache pins the most-frequent names in place regardless of temporal locality.</p>

<p><strong>Pitfall.</strong> If you promote aggressively on every increment, the top-K churns on early workloads before settling. Add a small hysteresis (only promote when the count exceeds the minimum by some margin), or seed the cache after a profiling pass has accumulated stable counts.</p>

<h2 id="the-instrumentation-question">The Instrumentation Question</h2>

<p>You can’t build the hot-token cache honestly without counting. And the counter has to count the <strong>right</strong> thing.</p>

<p>There are three plausible counts, and they’re <strong>not the same</strong>:</p>

<table>
  <thead>
    <tr>
      <th>Counter</th>
      <th>What it measures</th>
      <th>Useful for</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Source-interpret count</strong></td>
      <td>How often a name is looked up while reading <code class="language-plaintext highlighter-rouge">.fth</code> source in interpret mode</td>
      <td>Tuning the recent cache for REPL use</td>
    </tr>
    <tr>
      <td><strong>Source-compile count</strong></td>
      <td>How often a name is looked up while reading <code class="language-plaintext highlighter-rouge">.fth</code> source in compile mode</td>
      <td>Tuning the recent/hot cache for <code class="language-plaintext highlighter-rouge">.fth</code> loading</td>
    </tr>
    <tr>
      <td><strong>Runtime execution count</strong></td>
      <td>How often a compiled word runs</td>
      <td>Dictionary reordering, inlining decisions</td>
    </tr>
  </tbody>
</table>

<p>Naive instrumentation counts <em>executions</em> because that’s what’s easy to hook into <code class="language-plaintext highlighter-rouge">NEXT</code>. But execution count is wrong for lookup optimization. <code class="language-plaintext highlighter-rouge">DUP</code> may execute millions of times inside compiled loops but only be looked up a few dozen times during source-reading. What you want to cache is the lookup pattern, not the runtime pattern.</p>

<p>That distinction matters even more for phase-4-style systems that will eventually drop the interpreter entirely in production builds. Lookup frequency is a <em>boot-time</em> cost. Execution frequency is a <em>runtime</em> cost. Different optimizations target different costs.</p>

<h2 id="why-linear-walk--cache-often-beats-hash">Why Linear Walk + Cache Often Beats Hash</h2>

<p>Adding up the options against the phase 2 hash:</p>

<ul>
  <li>Numeric fast path: zero collision risk, saves the 20% of tokens that are numbers.</li>
  <li>Recent-hit cache: catches local repetition, which is the dominant lookup pattern in structured Forth code.</li>
  <li>Hot-token cache: catches global high-frequency names — the “Zipf tail” that’s always hot.</li>
</ul>

<p>Three orthogonal wins, none of which need:</p>
<ul>
  <li>A 768-byte bucket table.</li>
  <li>A second link field in every dictionary entry.</li>
  <li>Boot-time hash population.</li>
  <li>CREATE-time bucket splicing.</li>
  <li>Re-implementing any of that in high-level Forth.</li>
</ul>

<p>The hash was a <em>general</em> answer to <em>amortize all FIND cost via bucket distribution</em>. The layered caches are <em>specific</em> answers to the <em>specific</em> patterns that happen in real Forth code. Specific answers are usually cheaper when the patterns are well-understood — and Forth’s patterns are very well-understood, because the language has been alive for 50 years and its word-frequency distributions don’t really change.</p>

<p>The classic rule of thumb applies: <strong>optimize the actual workload, not the worst case</strong>. A hash protects you against adversarial inputs and dictionaries thousands of words deep. A layered cache protects you against the real workload of a few hundred words and strong locality. For a bootstrapping Forth, the real workload is the only workload.</p>

<h2 id="the-good-enough-threshold">The “Good Enough” Threshold</h2>

<p>Here’s the pragmatic shape of the decision:</p>

<table>
  <thead>
    <tr>
      <th>Workload</th>
      <th>What to ship</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Just loading a small core (~50 words, one pass)</td>
      <td>Linear walk, nothing else</td>
    </tr>
    <tr>
      <td>Full core + extended <code class="language-plaintext highlighter-rouge">.fth</code> library (~200 words)</td>
      <td>Add numeric fast path</td>
    </tr>
    <tr>
      <td>Interactive REPL + <code class="language-plaintext highlighter-rouge">.fth</code> loading</td>
      <td>Add recent-hit cache (16 entries)</td>
    </tr>
    <tr>
      <td>Profiling shows unbalanced hot words</td>
      <td>Add hot-token cache (8 entries)</td>
    </tr>
    <tr>
      <td>Dictionary exceeds 500 words, lookup dominates</td>
      <td>Revisit: first-char bucket or XMX</td>
    </tr>
  </tbody>
</table>

<p>The insight from phase 2’s hash work was that <em>the threshold for “hash is worth it”</em> turned out to be a larger dictionary than COR24 Forth actually has. Phase 4 takes that insight seriously: don’t carry the hash subsystem if the actual workload doesn’t need it. Install the cheap wins. Measure. Only reach for heavier mechanism if measurement shows the cheap wins ran out.</p>

<h2 id="whats-next">What’s Next</h2>

<p>The layered caches make lookup <em>fast</em>. The next rabbit-hole post asks a different question: <em>can we make lookup go away entirely?</em> Dictionary compaction, shadowed-word removal, splitting development images from production images — the point where “optimize FIND” becomes “eliminate FIND for this deployment”. That’s <em>Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images</em>.</p>

<hr />

<p><em>Forth’s patterns are stable because Forth is old. Optimization strategies that assume stable patterns pay off handsomely. Optimization strategies that assume adversarial inputs carry weight you don’t need.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="dictionary" /><category term="find" /><category term="lookup-cache" /><category term="numeric-fast-path" /><category term="recent-hit-cache" /><category term="hot-token-cache" /><category term="instrumentation" /><category term="optimization" /><category term="self-hosting" /><summary type="html"><![CDATA[Second FORTH post in the rabbit hole. Part 1 explained why phase 4 (forth-from-forth) is deleting the XMX hash and the 256-bucket table along with the asm do_find that used them. This post answers the obvious follow-up: if not a hash, then what? Because “no optimization” isn’t quite right either. FIND still runs thousands of times during bootstrap and source loading. The asm-era tools are gone, but the interactive hot path deserves something. Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Prior post Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name Overview post Embedded (3/?): Self-Hosting Spectrum Comments Discord The Post-Hash Toolbox Three optimizations, all cheaper than a hash, all written in pure Forth. None of them require a side table, none require touching dictionary headers, none require boot-time setup. Each one trades a little code for a better hit rate on the patterns that actually show up in source files. Optimization Cost Hit pattern Numeric fast path ~5 lines of Forth Any token starting with a digit or -&lt;digit&gt; Recent-hit cache (N entries) ~10 lines + N × 2 cells of memory Tokens repeated within a short window Hot-token cache (top-K) ~20 lines + K × 2 cells + some accounting The handful of names that appear 100s of times They’re orthogonal. You can ship any one, any two, or all three. The order below is the order I’d install them in: each next one is a little more work for a little more payoff. Optimization 1: Numeric Fast Path Every .fth source file has numbers in it. In the all-asm INTERPRET, each number makes FIND walk the dictionary, fail to find a match, and then fall through to NUMBER. That’s wasted work — you can tell by the first character that no dictionary word will match. The fix is a one-line pre-check at the top of INTERPRET: : INTERPRET WORD DUP C@ \ token (addr len) first-char [CHAR] 0 [CHAR] 9 WITHIN IF NUMBER EXIT THEN \ starts with a digit — skip FIND ... (normal FIND path) ... ; (Handle -&lt;digit&gt; with one more check. Handle $ or 0x hex prefix if your Forth takes those.) On a .fth source file like examples/fib.fth: : FIB ( n -- n' ) DUP 2 &lt; IF EXIT THEN DUP 1- RECURSE SWAP 2- RECURSE + ; The tokens 2, 1-, 2- all start with digits (well, 1- and 2- look like numbers until the dash is consumed and the remaining part doesn’t parse — some Forths special-case 1- as a word anyway; take the simple path and let FIND confirm after the numeric parse fails). The main point: literal numbers like 2 get a direct routing to NUMBER and skip FIND entirely. Measurement shape. On the COR24 core loading examples/*.fth, numeric tokens are ~20% of the input stream. Fast-pathing them avoids the equivalent fraction of dictionary walks. One branch, huge payoff. Caveat. If your Forth lets words start with digits (2dup, 2drop, 4th), the fast path has to fall back to FIND on “might-be-a-word” failures. In practice that means: try NUMBER first, and if NUMBER rejects the whole token, then FIND it. The rejection is fast — NUMBER aborts on the first non-digit. Optimization 2: Recent-Hit Cache The circular-buffer design. Keep the last N successful (hash, cfa, flags) tuples — call it 8 or 16 entries. Every FIND first probes the circular buffer by hash; on hit, return immediately. On miss, walk the dictionary, then insert the result into the next slot of the buffer (overwriting the oldest). Rough structure in Forth: 16 CONSTANT RECENT-SIZE CREATE recent-hash RECENT-SIZE CELLS ALLOT CREATE recent-cfa RECENT-SIZE CELLS ALLOT CREATE recent-flags RECENT-SIZE CELLS ALLOT VARIABLE recent-next \ index where the next insert lands : recent-lookup ( h -- cfa flags | 0 0 ) RECENT-SIZE 0 DO DUP recent-hash I CELLS + @ = IF DROP recent-cfa I CELLS + @ recent-flags I CELLS + @ UNLOOP EXIT THEN LOOP DROP 0 0 ; : recent-insert ( h cfa flags -- ) recent-next @ &gt;R R@ recent-flags + ! R@ recent-cfa + ! R@ recent-hash + ! recent-next @ 1+ RECENT-SIZE MOD recent-next ! ; A FIND now becomes: : FIND ( addr len -- cfa flags ) hash-name recent-lookup ?DUP IF EXIT THEN DROP dict-walk ( returns cfa flags or 0 0 ) DUP IF \ found — remember it 2DUP hash-name -ROT recent-insert THEN ; Where hash-name is a cheap hash (just XOR the first 3 characters, for example) — the full XMX is overkill here because we only need to distinguish N=16 candidates, not partition 256 buckets. Why it works. Temporal locality. In a typical .fth block: : SQUARE DUP * ; : CUBE DUP SQUARE * ; : FOURTH SQUARE SQUARE ; DUP, *, SQUARE, and ; each get looked up multiple times within a few-token window. The recent-hit buffer catches all of it. Buffer size N=8 is enough for most Forth source; N=16 is generous. Cost. N × 3 cells ≈ 72 bytes for a 16-entry cache on COR24 (3-byte cells). Linear scan of 16 entries is ~48 COR24 instructions worst case, fewer on hit. Compare to a hash-table miss walking a chain of 15 — the circular buffer wins whenever the hit rate is above ~25%. Optimization 3: Hot-Token Cache The recent-hit cache catches local repetition. What it misses: tokens that appear in many different contexts — DUP, !, @, IF, ;. These words show up hundreds of times across a full core-loading pass, but rarely in back-to-back tokens. The recent buffer evicts them between hits. The hot-token cache handles that shape. Top-K by frequency. K is small — 4 or 8 entries is plenty — because Forth’s name distribution is severely top-heavy: the top 10 words typically account for 40–60% of all lookups in a core. Structure is essentially the recent cache plus frequency counters: 8 CONSTANT HOT-SIZE CREATE hot-hash HOT-SIZE CELLS ALLOT CREATE hot-cfa HOT-SIZE CELLS ALLOT CREATE hot-flags HOT-SIZE CELLS ALLOT CREATE hot-count HOT-SIZE CELLS ALLOT On every dictionary walk that finds a word: Increment the count for that word’s bucket. If any bucket’s count exceeds the minimum in the top-K, promote the new entry and evict the former minimum. Lookup probes the hot cache first, then the recent cache, then does the dictionary walk. Why it works. Forth’s word-frequency distribution is Zipf-like. The top 8 names cover roughly half of all lookups in a typical .fth corpus; the top 32 cover nearly all of them. A tiny K-wide cache pins the most-frequent names in place regardless of temporal locality. Pitfall. If you promote aggressively on every increment, the top-K churns on early workloads before settling. Add a small hysteresis (only promote when the count exceeds the minimum by some margin), or seed the cache after a profiling pass has accumulated stable counts. The Instrumentation Question You can’t build the hot-token cache honestly without counting. And the counter has to count the right thing. There are three plausible counts, and they’re not the same: Counter What it measures Useful for Source-interpret count How often a name is looked up while reading .fth source in interpret mode Tuning the recent cache for REPL use Source-compile count How often a name is looked up while reading .fth source in compile mode Tuning the recent/hot cache for .fth loading Runtime execution count How often a compiled word runs Dictionary reordering, inlining decisions Naive instrumentation counts executions because that’s what’s easy to hook into NEXT. But execution count is wrong for lookup optimization. DUP may execute millions of times inside compiled loops but only be looked up a few dozen times during source-reading. What you want to cache is the lookup pattern, not the runtime pattern. That distinction matters even more for phase-4-style systems that will eventually drop the interpreter entirely in production builds. Lookup frequency is a boot-time cost. Execution frequency is a runtime cost. Different optimizations target different costs. Why Linear Walk + Cache Often Beats Hash Adding up the options against the phase 2 hash: Numeric fast path: zero collision risk, saves the 20% of tokens that are numbers. Recent-hit cache: catches local repetition, which is the dominant lookup pattern in structured Forth code. Hot-token cache: catches global high-frequency names — the “Zipf tail” that’s always hot. Three orthogonal wins, none of which need: A 768-byte bucket table. A second link field in every dictionary entry. Boot-time hash population. CREATE-time bucket splicing. Re-implementing any of that in high-level Forth. The hash was a general answer to amortize all FIND cost via bucket distribution. The layered caches are specific answers to the specific patterns that happen in real Forth code. Specific answers are usually cheaper when the patterns are well-understood — and Forth’s patterns are very well-understood, because the language has been alive for 50 years and its word-frequency distributions don’t really change. The classic rule of thumb applies: optimize the actual workload, not the worst case. A hash protects you against adversarial inputs and dictionaries thousands of words deep. A layered cache protects you against the real workload of a few hundred words and strong locality. For a bootstrapping Forth, the real workload is the only workload. The “Good Enough” Threshold Here’s the pragmatic shape of the decision: Workload What to ship Just loading a small core (~50 words, one pass) Linear walk, nothing else Full core + extended .fth library (~200 words) Add numeric fast path Interactive REPL + .fth loading Add recent-hit cache (16 entries) Profiling shows unbalanced hot words Add hot-token cache (8 entries) Dictionary exceeds 500 words, lookup dominates Revisit: first-char bucket or XMX The insight from phase 2’s hash work was that the threshold for “hash is worth it” turned out to be a larger dictionary than COR24 Forth actually has. Phase 4 takes that insight seriously: don’t carry the hash subsystem if the actual workload doesn’t need it. Install the cheap wins. Measure. Only reach for heavier mechanism if measurement shows the cheap wins ran out. What’s Next The layered caches make lookup fast. The next rabbit-hole post asks a different question: can we make lookup go away entirely? Dictionary compaction, shadowed-word removal, splitting development images from production images — the point where “optimize FIND” becomes “eliminate FIND for this deployment”. That’s Rabbit-hole (4/?): FORTH — Dictionary Compaction and Specialized Images. Forth’s patterns are stable because Forth is old. Optimization strategies that assume stable patterns pay off handsomely. Optimization strategies that assume adversarial inputs carry weight you don’t need.]]></summary></entry><entry><title type="html">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</title><link href="https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/" rel="alternate" type="text/html" title="Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name" /><published>2026-04-23T00:15:00-07:00</published><updated>2026-04-23T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/"><![CDATA[<p><img src="/assets/images/posts/block-rabbit-hole-watch.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<div style="overflow: hidden;">

  <p>First FORTH post in this rabbit hole — more are planned. The <a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">self-hosting spectrum post</a> laid out four phases (all-asm kernel → forth-in-forth → forth-on-forthish → forth-from-forth) and caught the project mid-stride: phase 2 shipped, phase 3 with its first subsets just landing, phase 4 still on the horizon.</p>

  <p><strong>Since then, phase 3 has completed</strong> — <code class="language-plaintext highlighter-rouge">forth-on-forthish</code> finished subsets through 21, pulling <code class="language-plaintext highlighter-rouge">WORD</code>, <code class="language-plaintext highlighter-rouge">FIND</code>, <code class="language-plaintext highlighter-rouge">NUMBER</code>, <code class="language-plaintext highlighter-rouge">INTERPRET</code>, and <code class="language-plaintext highlighter-rouge">QUIT</code> into Forth and leaving ~22 irreducible assembly primitives. That moves the project’s active edge onto phase 4, <code class="language-plaintext highlighter-rouge">forth-from-forth</code> — a Forth-hosted cross-compiler that emits the kernel’s <code class="language-plaintext highlighter-rouge">.s</code> file as a build artifact. Working toward it surfaced a set of design questions that don’t fit in a single post: how lookup really works, what to optimize once the asm <code class="language-plaintext highlighter-rouge">do_find</code> goes away, how to build specialized images, whether to structure the system as a composer, how to retarget the codegen. Each of those is a rabbit hole of its own, and this post is the first.</p>

  <p>This post goes one level deeper on the single operation that gets executed more than any other during bootstrap, source loading, and the REPL: <strong>dictionary lookup</strong>.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a></td>
      </tr>
      <tr>
        <td><strong>Kernel Repo</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Web Demo Repo</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-forth">sw-embed/web-sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>WASM Side-Experiment</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/sw-fth-wasm">sw-vibe-coding/sw-fth-wasm</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/04/20/embedded-forth-self-hosting-spectrum/">Embedded (3/?): Self-Hosting Spectrum</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="what-find-does">What FIND Does</h2>

<p>FIND is the operation that turns a name into something you can execute. Given a word like <code class="language-plaintext highlighter-rouge">DUP</code>, it walks the dictionary and returns the address of the entry — the code field address, or <strong>CFA</strong> — along with whether the word is IMMEDIATE (needs to run during compilation) or normal.</p>

<p>If that sounds simple, remember the frequency. Every token the REPL reads. Every word in every <code class="language-plaintext highlighter-rouge">.fth</code> file loaded at boot. Every reference inside a colon definition as it’s being compiled. FIND runs thousands of times before you see the first prompt.</p>

<p>That’s why it’s an obvious target for optimization. It’s also why it’s a trap: optimizing it changes the shape of the dictionary, and the dictionary is touched by almost everything.</p>

<h2 id="two-worlds-interpret-mode-and-compile-mode">Two Worlds: Interpret Mode and Compile Mode</h2>

<p>Before getting into FIND’s internals, the crucial split: <strong>FIND gets called from two different contexts, and they use the result differently.</strong></p>

<p>In <strong>interpret mode</strong>, the outer loop (<code class="language-plaintext highlighter-rouge">QUIT</code> in classic Forth) is doing the hot thing you expect:</p>

<ol>
  <li>Read a token with <code class="language-plaintext highlighter-rouge">WORD</code>.</li>
  <li>Call <code class="language-plaintext highlighter-rouge">FIND</code> on it.</li>
  <li>If found, <code class="language-plaintext highlighter-rouge">EXECUTE</code> the CFA — the word runs <em>now</em>.</li>
  <li>If not found, try <code class="language-plaintext highlighter-rouge">NUMBER</code> — if it parses, push the value.</li>
  <li>If neither, fail.</li>
</ol>

<p>In <strong>compile mode</strong> (triggered by <code class="language-plaintext highlighter-rouge">:</code> which flips <code class="language-plaintext highlighter-rouge">STATE</code> to non-zero), the outer loop does something subtly different:</p>

<ol>
  <li>Read a token with <code class="language-plaintext highlighter-rouge">WORD</code>.</li>
  <li>Call <code class="language-plaintext highlighter-rouge">FIND</code> on it.</li>
  <li>If found, check the IMMEDIATE flag.
    <ul>
      <li>IMMEDIATE words execute <em>now</em> (e.g. <code class="language-plaintext highlighter-rouge">;</code>, <code class="language-plaintext highlighter-rouge">IF</code>, <code class="language-plaintext highlighter-rouge">THEN</code>).</li>
      <li>Normal words get <strong>compiled</strong> — their CFA is written into the growing definition, not executed.</li>
    </ul>
  </li>
  <li>If not found, try <code class="language-plaintext highlighter-rouge">NUMBER</code> — compile as a literal.</li>
</ol>

<p>The key point: <strong>compilation doesn’t skip FIND.</strong> In fact compilation calls FIND <em>more</em> — once per token in the source definition. Optimizing lookup helps compile-time as much as it helps interactive REPL use. And on a bootstrapping Forth, almost everything is compile-time, because loading <code class="language-plaintext highlighter-rouge">.fth</code> files is one long colon-definition parade.</p>

<h2 id="walkthrough-5-dup--in-interpret-mode">Walkthrough: <code class="language-plaintext highlighter-rouge">5 DUP .</code> in Interpret Mode</h2>

<p>Three tokens, three different shapes of work:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token   FIND result          NUMBER   Action
-----   ------------------   ------   --------------------------
5       not found            → 5      push 5
DUP     found, not immediate  —       execute DUP (duplicates TOS)
.       found, not immediate  —       execute . (pops and prints)
</code></pre></div></div>

<p>Stack evolution, with <code class="language-plaintext highlighter-rouge">|</code> separating layers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Start:                 (empty)
After 5:               5
After DUP:             5 5
After .:               5           (prints "5")
</code></pre></div></div>

<p>The outer loop runs FIND three times. Twice it hits. Once it misses and falls through to NUMBER. Every token is an independent lookup.</p>

<h2 id="walkthrough--square-dup---in-compile-mode">Walkthrough: <code class="language-plaintext highlighter-rouge">: SQUARE DUP * ;</code> in Compile Mode</h2>

<p>Same shape of work, different consumer:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token    FIND result                    Action
------   ----------------------------   ------------------------
:        found, IMMEDIATE-like           run : (create header,
                                         flip STATE to compile)
SQUARE   (:  reads the next token as a
          name, not as a dictionary lookup — SQUARE is the new word)
DUP      found, not immediate            COMPILE DUP's CFA into body
*        found, not immediate            COMPILE *'s CFA into body
;        found, IMMEDIATE                run ; (compile EXIT, flip
                                         STATE back to interpret)
</code></pre></div></div>

<p>After this, <code class="language-plaintext highlighter-rouge">SQUARE</code>’s body in the dictionary looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT
</code></pre></div></div>

<p>When <code class="language-plaintext highlighter-rouge">5 SQUARE .</code> runs later, the interpreter pushes <code class="language-plaintext highlighter-rouge">5</code>, finds <code class="language-plaintext highlighter-rouge">SQUARE</code>, executes it. Inside <code class="language-plaintext highlighter-rouge">SQUARE</code>, the inner loop (<code class="language-plaintext highlighter-rouge">NEXT</code>) walks the compiled CFAs one at a time. <strong>No FIND happens at runtime.</strong> The names <code class="language-plaintext highlighter-rouge">DUP</code> and <code class="language-plaintext highlighter-rouge">*</code> were resolved once, at compile time, and baked into the body as direct CFA references.</p>

<p>This is the core insight that drives all the lookup-optimization decisions: <strong>FIND’s cost is concentrated in the text-reading phase, not in running compiled code.</strong> Once a definition is compiled, the cost is gone forever.</p>

<h2 id="the-all-asm-find-linear-walk">The All-Asm FIND: Linear Walk</h2>

<p>The reference phase-1 kernel does FIND the obvious way — walk the LATEST chain backwards, compare names, return the first hit. A Forth dictionary entry header looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>link (3B)   → flags|namelen (1B)   → name (N bytes)   → CFA
</code></pre></div></div>

<p>Walking is simple:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cursor = LATEST
while cursor != 0:
    length = (cursor + 3) &amp; 0x1F     ; strip flags
    if length == target_length and
       memcmp(cursor + 4, target_name, length) == 0:
        return cursor + 4 + length   ; CFA
    cursor = *cursor                  ; next link
return 0  ; not found
</code></pre></div></div>

<p>On a dictionary of ~50 words, average hit is ~25 comparisons. Each comparison is a length check plus a short string compare. Fast enough for one-off tokens. Painful when you’re loading <code class="language-plaintext highlighter-rouge">highlevel.fth</code> and every line has six tokens that each need resolution.</p>

<p>Bootstrap timing on the web demo showed this clearly: the all-asm kernel would spend a full second or more just parsing its own Forth core on a slow laptop. Visible lag. Time to speed it up.</p>

<h2 id="the-phase-2-optimization-2-round-xmx-hash">The Phase 2 Optimization: 2-Round XMX Hash</h2>

<p>The first real optimization added a hash table. Three pieces working together:</p>

<p><strong>1. A hash function.</strong> 2-Round XMX, chosen because it fits COR24’s 24-bit native arithmetic:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = 0
for each byte c of the name:
    h = h XOR c
    h = h * 0xDEADB5        ; 24-bit multiply, naturally truncates
    h = h XOR (h SRL 12)    ; avalanche: fold high half into low half
bucket = h AND 0xFF
</code></pre></div></div>

<p>Each step is cheap on COR24:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">XOR</code> and <code class="language-plaintext highlighter-rouge">SRL</code> are single cycles.</li>
  <li>The <code class="language-plaintext highlighter-rouge">* 0xDEADB5</code> is a 24-bit multiply whose overflow is discarded for free (it’s the word size).</li>
  <li><code class="language-plaintext highlighter-rouge">AND 0xFF</code> is a byte mask.</li>
</ul>

<p>The shift-by-12 is doing the real work. XMX (<em>xor-multiply-xor</em>) hashes need an avalanche step so that 1–3 character names like <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">DUP</code> produce well-distributed bucket indices. Without the shift, short names cluster badly. With it, short names are indistinguishable from long ones, from the hash’s point of view.</p>

<p><strong>2. A hash table.</strong> 256 buckets, 3 bytes each, 768 bytes total. Each bucket holds a pointer to the most-recent dictionary entry whose name hashes into that bucket.</p>

<p><strong>3. A per-entry hash chain.</strong> Every dictionary entry grew a <em>second</em> link, independent of the chronological <code class="language-plaintext highlighter-rouge">link</code> field. This one points to the next-older entry in the same hash bucket.</p>

<p>Lookup becomes:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = xmx(name)
bucket = h AND 0xFF
cursor = hashtab[bucket]
while cursor != 0:
    if name matches entry:
        return cursor.cfa
    cursor = cursor.hash_link   ; NOT the LATEST link
return 0
</code></pre></div></div>

<p>On the real dictionary, this cut linear-scan collisions from 47 (average walk depth) down to 11–15 per bucket. Measurably faster. Boot dropped from ~2 seconds to under one. The hashing work document in the repo profiles the distribution in detail.</p>

<h2 id="the-phase-2-optimization-part-2-lookaside-cache">The Phase 2 Optimization, Part 2: Lookaside Cache</h2>

<p>Even with hashing, one pattern remained: <code class="language-plaintext highlighter-rouge">DUP DUP DROP</code>, <code class="language-plaintext highlighter-rouge">SWAP OVER +</code>, the same few names repeated in tight succession during source loading. For those, even one hash-plus-bucket-walk is waste.</p>

<p>The cache is a 1-entry lookaside holding the most recent successful lookup as a tuple:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(hash, cfa, flags)
</code></pre></div></div>

<p>The memento is keyed by the full XMX hash, not by the name — hashing is already cheap, and comparing a 24-bit value is one instruction. On a hit, the lookup short-circuits before even dereferencing the hash table.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h = xmx(name)
if h == cache.hash:
    return (cache.cfa, cache.flags)
; otherwise normal hash lookup, then update cache on success
</code></pre></div></div>

<p>One entry is enough because Forth source has strong temporal locality — when you see <code class="language-plaintext highlighter-rouge">DUP</code> once, you’ll see it again in the next few tokens more often than not. The cache doesn’t pretend to solve the general working-set problem; it just skips the table lookup on the very-hottest path.</p>

<h2 id="why-phase-4-is-dropping-all-of-it">Why Phase 4 Is Dropping All of It</h2>

<p>Phase 4 (<code class="language-plaintext highlighter-rouge">forth-from-forth</code>) builds the kernel via a Forth-hosted cross-compiler. One of its design goals: no hand-written <code class="language-plaintext highlighter-rouge">.s</code> file in the source tree. Everything comes from Forth.</p>

<p>That means FIND itself moves to Forth. The high-level FIND in <code class="language-plaintext highlighter-rouge">highlevel.fth</code> already exists — it’s a plain linear walk. No hash. No lookaside cache. No second link in dictionary entries.</p>

<p>Against that, the hashed FIND looks like dead weight:</p>

<table>
  <thead>
    <tr>
      <th>Cost</th>
      <th>What you’d need to keep</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>768 bytes of bucket table</td>
      <td>Boot-time setup code to zero and populate it</td>
    </tr>
    <tr>
      <td>~40 instructions of <code class="language-plaintext highlighter-rouge">compute_hash</code></td>
      <td>Re-implement in Forth (or keep in asm, which contradicts the goal)</td>
    </tr>
    <tr>
      <td>Per-entry hash link</td>
      <td>Extra 3 bytes on every dictionary entry</td>
    </tr>
    <tr>
      <td>CREATE-time splice</td>
      <td>Every new word has to hash itself and thread into the right bucket</td>
    </tr>
    <tr>
      <td>Boot population</td>
      <td>Run compute_hash on every existing word at startup</td>
    </tr>
  </tbody>
</table>

<p>The only customer of all this mechanism was the asm <code class="language-plaintext highlighter-rouge">do_find</code>. Phase 4 deletes <code class="language-plaintext highlighter-rouge">do_find</code>. The customer is gone.</p>

<p>And boot <em>already</em> fits under a second without the hash, because the web demo’s adaptive pump-loop optimization (<a href="https://github.com/sw-embed/web-sw-cor24-forth">commit f757800 in web-sw-cor24-forth</a>) did the work from a different direction. The hash was solving a visible problem; once the pump-loop fixed the problem, the hash was solving nothing.</p>

<h2 id="the-staged-optimization-lesson">The Staged-Optimization Lesson</h2>

<p>The phase 4 plan doesn’t say “drop hashing and never look back.” It says this:</p>

<blockquote>
  <p>Fallback order if “no hash” turns out wrong: linear walk → first-char hash (one-instruction, 2–3× speedup, 47 collisions) → 2-Round XMX (proven winner, 15 collisions, ~10 instructions/char). Don’t jump straight to XMX on speculation.</p>
</blockquote>

<p>Three graded fallbacks, in order of complexity added. Each one you only reach for if the previous one proved insufficient under measurement. The XMX hash is fine engineering — it’s well-matched to COR24, the avalanche step is well-chosen, the collision count is low. But it’s also a <strong>complete subsystem</strong>: function + table + per-entry link + CREATE-time maintenance + boot-time initialization.</p>

<p>The instinct that led to adding it in phase 2 — <em>FIND is in the hot path, therefore FIND should be hashed</em> — was correct for that phase. The instinct that says <em>keep what works</em> is wrong for phase 4, because the customer is leaving. The right instinct is: <strong>when the caller changes, re-evaluate the callee</strong>.</p>

<p>Said another way: the XMX hash was the right answer to the question <em>“how do we make the asm do_find faster?”</em>. It is not automatically the right answer to <em>“how do we make high-level Forth FIND faster?”</em> — because the question has a different shape now. FIND is in Forth. The dictionary is walked by Forth. Anything you add, you add to Forth. And linear walks through a reverse-chronological list are Forth’s natural shape.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Phase 4 ships without the hash. But that doesn’t mean nothing gets optimized — it means the <em>kinds</em> of optimizations change. Numeric fast paths. Recent-hit caches. Hot-token caches. Instrumentation to know which words actually matter. That’s the subject of the next post in this rabbit hole: <em>Rabbit-hole (3/?): FORTH — Life After Hashing</em>.</p>

<hr />

<p><em>If you’ve been reading this on the <a href="https://sw-embed.github.io/web-sw-cor24-forth/">web demo</a>, switch to the <strong>forth-on-forthish</strong> tab and type <code class="language-plaintext highlighter-rouge">WORDS</code> — every word printed there was looked up during boot.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rabbit-hole" /><category term="programming-languages" /><category term="forth" /><category term="deep-dive" /><category term="rabbit-hole" /><category term="forth" /><category term="cor24" /><category term="dictionary" /><category term="find" /><category term="hashing" /><category term="xmx" /><category term="lookaside-cache" /><category term="interpret-mode" /><category term="compile-mode" /><category term="self-hosting" /><summary type="html"><![CDATA[First FORTH post in this rabbit hole — more are planned. The self-hosting spectrum post laid out four phases (all-asm kernel → forth-in-forth → forth-on-forthish → forth-from-forth) and caught the project mid-stride: phase 2 shipped, phase 3 with its first subsets just landing, phase 4 still on the horizon. Since then, phase 3 has completed — forth-on-forthish finished subsets through 21, pulling WORD, FIND, NUMBER, INTERPRET, and QUIT into Forth and leaving ~22 irreducible assembly primitives. That moves the project’s active edge onto phase 4, forth-from-forth — a Forth-hosted cross-compiler that emits the kernel’s .s file as a build artifact. Working toward it surfaced a set of design questions that don’t fit in a single post: how lookup really works, what to optimize once the asm do_find goes away, how to build specialized images, whether to structure the system as a composer, how to retarget the codegen. Each of those is a rabbit hole of its own, and this post is the first. This post goes one level deeper on the single operation that gets executed more than any other during bootstrap, source loading, and the REPL: dictionary lookup. Resource Link Play in Browser COR24 Forth Demo Kernel Repo sw-embed/sw-cor24-forth Web Demo Repo sw-embed/web-sw-cor24-forth WASM Side-Experiment sw-vibe-coding/sw-fth-wasm Prior Post Embedded (3/?): Self-Hosting Spectrum Comments Discord What FIND Does FIND is the operation that turns a name into something you can execute. Given a word like DUP, it walks the dictionary and returns the address of the entry — the code field address, or CFA — along with whether the word is IMMEDIATE (needs to run during compilation) or normal. If that sounds simple, remember the frequency. Every token the REPL reads. Every word in every .fth file loaded at boot. Every reference inside a colon definition as it’s being compiled. FIND runs thousands of times before you see the first prompt. That’s why it’s an obvious target for optimization. It’s also why it’s a trap: optimizing it changes the shape of the dictionary, and the dictionary is touched by almost everything. Two Worlds: Interpret Mode and Compile Mode Before getting into FIND’s internals, the crucial split: FIND gets called from two different contexts, and they use the result differently. In interpret mode, the outer loop (QUIT in classic Forth) is doing the hot thing you expect: Read a token with WORD. Call FIND on it. If found, EXECUTE the CFA — the word runs now. If not found, try NUMBER — if it parses, push the value. If neither, fail. In compile mode (triggered by : which flips STATE to non-zero), the outer loop does something subtly different: Read a token with WORD. Call FIND on it. If found, check the IMMEDIATE flag. IMMEDIATE words execute now (e.g. ;, IF, THEN). Normal words get compiled — their CFA is written into the growing definition, not executed. If not found, try NUMBER — compile as a literal. The key point: compilation doesn’t skip FIND. In fact compilation calls FIND more — once per token in the source definition. Optimizing lookup helps compile-time as much as it helps interactive REPL use. And on a bootstrapping Forth, almost everything is compile-time, because loading .fth files is one long colon-definition parade. Walkthrough: 5 DUP . in Interpret Mode Three tokens, three different shapes of work: Token FIND result NUMBER Action ----- ------------------ ------ -------------------------- 5 not found → 5 push 5 DUP found, not immediate — execute DUP (duplicates TOS) . found, not immediate — execute . (pops and prints) Stack evolution, with | separating layers: Start: (empty) After 5: 5 After DUP: 5 5 After .: 5 (prints "5") The outer loop runs FIND three times. Twice it hits. Once it misses and falls through to NUMBER. Every token is an independent lookup. Walkthrough: : SQUARE DUP * ; in Compile Mode Same shape of work, different consumer: Token FIND result Action ------ ---------------------------- ------------------------ : found, IMMEDIATE-like run : (create header, flip STATE to compile) SQUARE (: reads the next token as a name, not as a dictionary lookup — SQUARE is the new word) DUP found, not immediate COMPILE DUP's CFA into body * found, not immediate COMPILE *'s CFA into body ; found, IMMEDIATE run ; (compile EXIT, flip STATE back to interpret) After this, SQUARE’s body in the dictionary looks like: DOCOL | &lt;DUP's CFA&gt; | &lt;*'s CFA&gt; | EXIT When 5 SQUARE . runs later, the interpreter pushes 5, finds SQUARE, executes it. Inside SQUARE, the inner loop (NEXT) walks the compiled CFAs one at a time. No FIND happens at runtime. The names DUP and * were resolved once, at compile time, and baked into the body as direct CFA references. This is the core insight that drives all the lookup-optimization decisions: FIND’s cost is concentrated in the text-reading phase, not in running compiled code. Once a definition is compiled, the cost is gone forever. The All-Asm FIND: Linear Walk The reference phase-1 kernel does FIND the obvious way — walk the LATEST chain backwards, compare names, return the first hit. A Forth dictionary entry header looks like: link (3B) → flags|namelen (1B) → name (N bytes) → CFA Walking is simple: cursor = LATEST while cursor != 0: length = (cursor + 3) &amp; 0x1F ; strip flags if length == target_length and memcmp(cursor + 4, target_name, length) == 0: return cursor + 4 + length ; CFA cursor = *cursor ; next link return 0 ; not found On a dictionary of ~50 words, average hit is ~25 comparisons. Each comparison is a length check plus a short string compare. Fast enough for one-off tokens. Painful when you’re loading highlevel.fth and every line has six tokens that each need resolution. Bootstrap timing on the web demo showed this clearly: the all-asm kernel would spend a full second or more just parsing its own Forth core on a slow laptop. Visible lag. Time to speed it up. The Phase 2 Optimization: 2-Round XMX Hash The first real optimization added a hash table. Three pieces working together: 1. A hash function. 2-Round XMX, chosen because it fits COR24’s 24-bit native arithmetic: h = 0 for each byte c of the name: h = h XOR c h = h * 0xDEADB5 ; 24-bit multiply, naturally truncates h = h XOR (h SRL 12) ; avalanche: fold high half into low half bucket = h AND 0xFF Each step is cheap on COR24: XOR and SRL are single cycles. The * 0xDEADB5 is a 24-bit multiply whose overflow is discarded for free (it’s the word size). AND 0xFF is a byte mask. The shift-by-12 is doing the real work. XMX (xor-multiply-xor) hashes need an avalanche step so that 1–3 character names like @, !, DUP produce well-distributed bucket indices. Without the shift, short names cluster badly. With it, short names are indistinguishable from long ones, from the hash’s point of view. 2. A hash table. 256 buckets, 3 bytes each, 768 bytes total. Each bucket holds a pointer to the most-recent dictionary entry whose name hashes into that bucket. 3. A per-entry hash chain. Every dictionary entry grew a second link, independent of the chronological link field. This one points to the next-older entry in the same hash bucket. Lookup becomes: h = xmx(name) bucket = h AND 0xFF cursor = hashtab[bucket] while cursor != 0: if name matches entry: return cursor.cfa cursor = cursor.hash_link ; NOT the LATEST link return 0 On the real dictionary, this cut linear-scan collisions from 47 (average walk depth) down to 11–15 per bucket. Measurably faster. Boot dropped from ~2 seconds to under one. The hashing work document in the repo profiles the distribution in detail. The Phase 2 Optimization, Part 2: Lookaside Cache Even with hashing, one pattern remained: DUP DUP DROP, SWAP OVER +, the same few names repeated in tight succession during source loading. For those, even one hash-plus-bucket-walk is waste. The cache is a 1-entry lookaside holding the most recent successful lookup as a tuple: (hash, cfa, flags) The memento is keyed by the full XMX hash, not by the name — hashing is already cheap, and comparing a 24-bit value is one instruction. On a hit, the lookup short-circuits before even dereferencing the hash table. h = xmx(name) if h == cache.hash: return (cache.cfa, cache.flags) ; otherwise normal hash lookup, then update cache on success One entry is enough because Forth source has strong temporal locality — when you see DUP once, you’ll see it again in the next few tokens more often than not. The cache doesn’t pretend to solve the general working-set problem; it just skips the table lookup on the very-hottest path. Why Phase 4 Is Dropping All of It Phase 4 (forth-from-forth) builds the kernel via a Forth-hosted cross-compiler. One of its design goals: no hand-written .s file in the source tree. Everything comes from Forth. That means FIND itself moves to Forth. The high-level FIND in highlevel.fth already exists — it’s a plain linear walk. No hash. No lookaside cache. No second link in dictionary entries. Against that, the hashed FIND looks like dead weight: Cost What you’d need to keep 768 bytes of bucket table Boot-time setup code to zero and populate it ~40 instructions of compute_hash Re-implement in Forth (or keep in asm, which contradicts the goal) Per-entry hash link Extra 3 bytes on every dictionary entry CREATE-time splice Every new word has to hash itself and thread into the right bucket Boot population Run compute_hash on every existing word at startup The only customer of all this mechanism was the asm do_find. Phase 4 deletes do_find. The customer is gone. And boot already fits under a second without the hash, because the web demo’s adaptive pump-loop optimization (commit f757800 in web-sw-cor24-forth) did the work from a different direction. The hash was solving a visible problem; once the pump-loop fixed the problem, the hash was solving nothing. The Staged-Optimization Lesson The phase 4 plan doesn’t say “drop hashing and never look back.” It says this: Fallback order if “no hash” turns out wrong: linear walk → first-char hash (one-instruction, 2–3× speedup, 47 collisions) → 2-Round XMX (proven winner, 15 collisions, ~10 instructions/char). Don’t jump straight to XMX on speculation. Three graded fallbacks, in order of complexity added. Each one you only reach for if the previous one proved insufficient under measurement. The XMX hash is fine engineering — it’s well-matched to COR24, the avalanche step is well-chosen, the collision count is low. But it’s also a complete subsystem: function + table + per-entry link + CREATE-time maintenance + boot-time initialization. The instinct that led to adding it in phase 2 — FIND is in the hot path, therefore FIND should be hashed — was correct for that phase. The instinct that says keep what works is wrong for phase 4, because the customer is leaving. The right instinct is: when the caller changes, re-evaluate the callee. Said another way: the XMX hash was the right answer to the question “how do we make the asm do_find faster?”. It is not automatically the right answer to “how do we make high-level Forth FIND faster?” — because the question has a different shape now. FIND is in Forth. The dictionary is walked by Forth. Anything you add, you add to Forth. And linear walks through a reverse-chronological list are Forth’s natural shape. What’s Next Phase 4 ships without the hash. But that doesn’t mean nothing gets optimized — it means the kinds of optimizations change. Numeric fast paths. Recent-hit caches. Hot-token caches. Instrumentation to know which words actually matter. That’s the subject of the next post in this rabbit hole: Rabbit-hole (3/?): FORTH — Life After Hashing. If you’ve been reading this on the web demo, switch to the forth-on-forthish tab and type WORDS — every word printed there was looked up during boot.]]></summary></entry><entry><title type="html">AI Tools (2/?): AgentRail Mid-Saga — Insert, Reorder, Reopen, Recover</title><link href="https://software-wrighter-lab.github.io/2026/04/23/ai-tools-agentrail-rs-mid-saga-flexibility/" rel="alternate" type="text/html" title="AI Tools (2/?): AgentRail Mid-Saga — Insert, Reorder, Reopen, Recover" /><published>2026-04-23T00:15:00-07:00</published><updated>2026-04-23T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/23/ai-tools-agentrail-rs-mid-saga-flexibility</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/23/ai-tools-agentrail-rs-mid-saga-flexibility/"><![CDATA[<p><img src="/assets/images/posts/block-switchyard.png" class="post-marker no-invert" alt="" style="width: 240px;" /></p>

<div style="overflow: hidden;">

  <p>AgentRail’s Phase 0–5 feature list looked complete on paper. Then I started running it every day, across parallel repos, and every real-world friction point produced a new command. This post is a tour of what daily use teaches you that planning doesn’t.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repo</strong></td>
        <td><a href="https://github.com/sw-vibe-coding/agentrail-rs">sw-vibe-coding/agentrail-rs</a></td>
      </tr>
      <tr>
        <td><strong>Daily-use repos</strong></td>
        <td><a href="https://github.com/orgs/sw-embed/repositories">sw-embed</a> · <a href="https://github.com/orgs/sw-vibe-coding/repositories">sw-vibe-coding</a> · <a href="https://github.com/orgs/sw-ml-study/repositories">sw-ml-study</a></td>
      </tr>
      <tr>
        <td><strong>Prior posts</strong></td>
        <td><a href="/2026/03/17/ai-tools-xskill-memory-layer-multimodal-agents/">AI Tools (1/?): XSkill</a><br /><a href="/2026/03/22/saw-agentrail-rs-icrl-dual-memory/">Saw (3/?): agentrail-rs Dual Memory</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-plan-bending-problem">The Plan-Bending Problem</h2>

<p>A saga is a linear sequence of steps: plan it, walk it, finish it. That model works when the plan survives contact with reality. Two things tend to ruin it:</p>

<ul>
  <li><strong>A dependent repo breaks</strong> halfway through step 5. You need to bounce over, fix it, and come back — without abandoning the saga you were in.</li>
  <li><strong>A step reveals a flaw in a later step.</strong> Step 3 uncovers something that means step 7 has to change, or needs to happen <em>before</em> step 4, or needs to be re-done after you thought it was done.</li>
</ul>

<p>With only <code class="language-plaintext highlighter-rouge">begin</code> / <code class="language-plaintext highlighter-rouge">complete</code>, the options are bad: abandon the saga and re-plan, or pretend the detour didn’t happen and let the plan drift out of sync with what you actually did. Neither preserves the thing sagas are for: a faithful, auditable record of what was done.</p>

<p>The April features exist because this kept happening, every day, across multiple repos.</p>

<h2 id="insert--reorder--reopen-mid-saga-flexibility"><code class="language-plaintext highlighter-rouge">insert</code> / <code class="language-plaintext highlighter-rouge">reorder</code> / <code class="language-plaintext highlighter-rouge">reopen</code>: Mid-Saga Flexibility</h2>

<p>Three commands, each mapped to a real pattern:</p>

<table>
  <colgroup>
    <col style="width: 16em;" />
    <col />
  </colgroup>
  <thead>
    <tr><th>Command</th><th>Scenario</th></tr>
  </thead>
  <tbody>
    <tr>
      <td style="white-space: nowrap;"><code>agentrail insert --after N</code></td>
      <td>A surprise lands (blocker, bug in dependent repo, unplanned dependency). Slot a new pending step at N+1; later pending/in-progress steps shift up by one.</td>
    </tr>
    <tr>
      <td style="white-space: nowrap;"><code>agentrail reorder N --to M</code></td>
      <td>Priorities change. Move a pending or in-progress step; intervening steps shift the other way.</td>
    </tr>
    <tr>
      <td style="white-space: nowrap;"><code>agentrail reopen N</code></td>
      <td>A completed or blocked step turns out to be wrong. Transition it back to in-progress, clear <code>completed_at</code>, re-focus the cursor.</td>
    </tr>
  </tbody>
</table>

<p>The invariant that makes this safe:</p>

<p><strong>Completed steps never renumber.</strong> They are anchored to git history via their <code class="language-plaintext highlighter-rouge">commits</code> array. Any operation that would renumber a completed step is rejected. Reopen preserves the step’s <code class="language-plaintext highlighter-rouge">commits</code> so the git-history linkage stays intact.</p>

<p>This is why <code class="language-plaintext highlighter-rouge">insert</code> and <code class="language-plaintext highlighter-rouge">reorder</code> only move <em>pending</em> and <em>in-progress</em> steps. The past is immutable; the future is negotiable.</p>

<h2 id="cursor-preemption">Cursor Preemption</h2>

<p>A small but important follow-up. When <code class="language-plaintext highlighter-rouge">insert</code> drops a blocker at slot N+1, the cursor used to stay attached to its original step by identity — the new step was there, but <code class="language-plaintext highlighter-rouge">agentrail next</code> still surfaced the old focus. Same story for <code class="language-plaintext highlighter-rouge">reorder</code> pulling a later step forward.</p>

<p>Both now apply a preemption rule: <strong>if the new or moved step lands at or ahead of the cursor’s slot, focus follows it.</strong> Steps placed behind the cursor are still queued without disturbing focus. The result: <code class="language-plaintext highlighter-rouge">agentrail next</code> shows you the thing you just said was more urgent, instead of making you re-focus manually.</p>

<h2 id="maintenance-mode-add--ad-hoc-tasks">Maintenance Mode: <code class="language-plaintext highlighter-rouge">add</code> + Ad-Hoc Tasks</h2>

<p>Not every session is a planned saga. Sometimes the shape of the work is a <em>todo stream</em>, not a roadmap:</p>

<ul>
  <li>User types a task.</li>
  <li>Agent calls <code class="language-plaintext highlighter-rouge">agentrail add</code> to create a step.</li>
  <li>Agent runs <code class="language-plaintext highlighter-rouge">begin</code> / work / <code class="language-plaintext highlighter-rouge">complete</code>.</li>
  <li>Repeat.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">agentrail add --slug &lt;slug&gt; --prompt &lt;text&gt;</code> records the step without going through the plan-first flow. Combined with a CLAUDE.md maintenance protocol, this turns AgentRail into a daily driver for ad-hoc work, not just a tool for staged roadmaps. Agents can also use <code class="language-plaintext highlighter-rouge">add</code> mid-session to enqueue related work they discovered but shouldn’t pursue right now.</p>

<style>
  /* Stagger sections occupy 3 cols of a 5-col grid (60% of width):
       stagger-0 = cols 1-3 (left 60%)
       stagger-1 = cols 2-4 (middle 60%)
       stagger-2 = cols 3-5 (right 60%)
     These images sit in the 40% gutter next to the stagger paragraph:
       .gutter-img-left  → fills cols 1-2 next to a right-justified stagger-2 section
       .gutter-img-right → fills cols 4-5 next to a left-justified stagger-0 section */
  .post-content .stagger { position: relative; }
  .gutter-img-left,
  .gutter-img-right {
    position: absolute;
    top: 0;
    /* 66.67% of a 60%-wide section = 40% of the container (= 2 of 5 cols) */
    width: 66.67%;
    max-width: none;
    /* Cap height to section (so it can't overflow into the next section) but
       preserve aspect ratio — object-fit: contain scales the image down and
       letterboxes rather than cropping. */
    height: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
  }
  .gutter-img-left  { right: calc(100% + 1.25rem); }
  .gutter-img-right { left:  calc(100% + 1.25rem); }
  @media (max-width: 900px) {
    /* Stagger collapses at this width, so fall back to in-flow centered image */
    .gutter-img-left,
    .gutter-img-right {
      position: static;
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0.5em auto 1em;
    }
  }
</style>

<h2 id="when-things-go-wrong-audit--snapshot">When Things Go Wrong: <code class="language-plaintext highlighter-rouge">audit</code> + <code class="language-plaintext highlighter-rouge">snapshot</code></h2>

<p><img src="/assets/images/posts/block-red-signal.png" class="gutter-img-left no-invert" alt="" /></p>

<p>These two were motivated by a real incident: an agent deleted untracked <code class="language-plaintext highlighter-rouge">.agentrail/</code> files. Nothing in the reflog. No recovery path. That one hurt enough to build infrastructure for.</p>

<p><strong><code class="language-plaintext highlighter-rouge">agentrail snapshot</code></strong> writes a git commit of <code class="language-plaintext highlighter-rouge">.agentrail/</code> (and <code class="language-plaintext highlighter-rouge">.agentrail-archive/</code> when present) under <code class="language-plaintext highlighter-rouge">refs/agentrail/snapshots/&lt;timestamp&gt;</code>. Implementation detail that matters: it uses <code class="language-plaintext highlighter-rouge">GIT_INDEX_FILE</code> pointed at a throwaway temp file, so the user’s real index is never touched — no staged-file surprises, <code class="language-plaintext highlighter-rouge">git status</code> is unchanged before and after, and pre-commit hooks don’t race. Because a named ref holds the commit, blobs survive <code class="language-plaintext highlighter-rouge">git gc</code>. Restore is left to the user via plain git: <code class="language-plaintext highlighter-rouge">git restore --source=&lt;ref&gt; -- .agentrail .agentrail-archive</code>. AgentRail never writes to the working tree on your behalf.</p>

<p><strong><code class="language-plaintext highlighter-rouge">agentrail audit</code></strong> compares git history against saga history and reports three categories: matched commits, orphan commits (in git but not claimed by any step), and orphan steps (claimed commits that don’t exist). With <code class="language-plaintext highlighter-rouge">--emit-commands</code>, it prints a shell script of <code class="language-plaintext highlighter-rouge">agentrail add</code> lines seeded from commit subjects — a reconstruction scaffold you review before running.</p>

<p>Two schema changes make audit exact instead of heuristic:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">StepConfig.commits: Vec&lt;String&gt;</code> is populated by <code class="language-plaintext highlighter-rouge">complete</code> from HEAD, so step &lt;-&gt; commit linkage is recorded at the moment of truth.</li>
  <li><code class="language-plaintext highlighter-rouge">SagaConfig.retroactive: bool</code> lets you mark a saga as reconstructed-after-the-fact, so audits treat its commits as claimed.</li>
</ul>

<p>One related bug worth calling out. <code class="language-plaintext highlighter-rouge">agentrail add --commit &lt;ref&gt;</code> used to record whatever string you passed — short hashes, tags, <code class="language-plaintext highlighter-rouge">HEAD~N</code>. Audit compares against full 40-char SHAs from <code class="language-plaintext highlighter-rouge">git log %H</code>, so non-canonical entries never matched and every short-hash commit was flagged as an orphan. A retroactive saga in <code class="language-plaintext highlighter-rouge">sw-cor24-snobol4</code> produced 8 phantom orphans this way. Fixed by routing every <code class="language-plaintext highlighter-rouge">--commit</code> value through <code class="language-plaintext highlighter-rouge">git rev-parse --verify &lt;ref&gt;^{commit}</code> and storing only the full SHA. Unresolvable refs now fail at <code class="language-plaintext highlighter-rouge">add</code> time with a clear error, instead of silently producing orphans at audit time.</p>

<h2 id="archive-closing-one-saga-opening-the-next"><code class="language-plaintext highlighter-rouge">archive</code>: Closing One Saga, Opening the Next</h2>

<p><img src="/assets/images/posts/block-curves-trains.png" class="gutter-img-right no-invert" alt="" /></p>

<p><code class="language-plaintext highlighter-rouge">agentrail archive</code> moves <code class="language-plaintext highlighter-rouge">.agentrail/</code> into <code class="language-plaintext highlighter-rouge">.agentrail-archive/&lt;name&gt;-&lt;timestamp&gt;/</code> and clears the way for a new <code class="language-plaintext highlighter-rouge">init</code>. Optional <code class="language-plaintext highlighter-rouge">--reason</code> writes <code class="language-plaintext highlighter-rouge">archive-reason.txt</code> alongside. Collisions get a <code class="language-plaintext highlighter-rouge">-2</code>, <code class="language-plaintext highlighter-rouge">-3</code> suffix. It sounds small; in practice it’s what makes AgentRail work across many small sagas in the same repo instead of one ever-growing one.</p>

<h2 id="gen-agents-doc-portable-rules"><code class="language-plaintext highlighter-rouge">gen-agents-doc</code>: Portable Rules</h2>

<p>Dogfooding across N repos only works if the safety rules travel. <code class="language-plaintext highlighter-rouge">agentrail gen-agents-doc</code> writes <code class="language-plaintext highlighter-rouge">AGENTS.example.md</code> — a self-contained template covering the session protocol, <code class="language-plaintext highlighter-rouge">.agentrail/</code> handling rules, and <code class="language-plaintext highlighter-rouge">audit</code> recovery guidance. Drop it in any project, rename to <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>, <code class="language-plaintext highlighter-rouge">.cursorrules</code>, whatever your agent reads. The template is embedded via <code class="language-plaintext highlighter-rouge">include_str!</code>, so the binary is self-sufficient.</p>

<h2 id="what-dogfooding-teaches">What Dogfooding Teaches</h2>

<p><img src="/assets/images/posts/block-puppy-food-bowl.png" class="post-marker no-invert" alt="" style="width: 200px;" /></p>

<p>Three patterns keep recurring in the April commit log:</p>

<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Surprises need a <em>slot</em>, not a restart</td>
      <td><code class="language-plaintext highlighter-rouge">insert</code> / <code class="language-plaintext highlighter-rouge">reorder</code> / <code class="language-plaintext highlighter-rouge">reopen</code></td>
    </tr>
    <tr>
      <td>Recovery tools must exist <em>before</em> you need them</td>
      <td><code class="language-plaintext highlighter-rouge">audit</code> / <code class="language-plaintext highlighter-rouge">snapshot</code> — built after an incident, not during one</td>
    </tr>
    <tr>
      <td>Bootstrapping friction kills adoption across repos</td>
      <td><code class="language-plaintext highlighter-rouge">setup</code> / <code class="language-plaintext highlighter-rouge">gen-agents-doc</code> / <code class="language-plaintext highlighter-rouge">archive</code></td>
    </tr>
  </tbody>
</table>

<p>The meta-lesson: the only way to find these problems is to use the tool on real work, every day, across the kind of messy, parallel, interrupt-driven development that real projects actually look like. A plan-first feature list will never surface the cursor-preemption bug, the short-hash audit false-positives, or the “I need a todo stream, not a roadmap” mode. Only use does.</p>

<p>Next up: porting these conventions into a couple of the other sw-vibe-coding repos so more of the saga history is recoverable by default.</p>

<hr />

<p><em>AgentRail is one command at a time. Follow for more AI Tools posts on the small tools that keep agents honest.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="ai-agents" /><category term="cli-tools" /><category term="rust" /><category term="ai-tools" /><category term="agentrail" /><category term="rust" /><category term="cli" /><category term="ai-agents" /><category term="vibe-coding" /><category term="dogfooding" /><category term="workflow" /><category term="saga" /><category term="parallel-development" /><summary type="html"><![CDATA[AgentRail’s Phase 0–5 feature list looked complete on paper. Then I started running it every day, across parallel repos, and every real-world friction point produced a new command. This post is a tour of what daily use teaches you that planning doesn’t. Resource Link Repo sw-vibe-coding/agentrail-rs Daily-use repos sw-embed · sw-vibe-coding · sw-ml-study Prior posts AI Tools (1/?): XSkillSaw (3/?): agentrail-rs Dual Memory Comments Discord The Plan-Bending Problem A saga is a linear sequence of steps: plan it, walk it, finish it. That model works when the plan survives contact with reality. Two things tend to ruin it: A dependent repo breaks halfway through step 5. You need to bounce over, fix it, and come back — without abandoning the saga you were in. A step reveals a flaw in a later step. Step 3 uncovers something that means step 7 has to change, or needs to happen before step 4, or needs to be re-done after you thought it was done. With only begin / complete, the options are bad: abandon the saga and re-plan, or pretend the detour didn’t happen and let the plan drift out of sync with what you actually did. Neither preserves the thing sagas are for: a faithful, auditable record of what was done. The April features exist because this kept happening, every day, across multiple repos. insert / reorder / reopen: Mid-Saga Flexibility Three commands, each mapped to a real pattern: CommandScenario agentrail insert --after N A surprise lands (blocker, bug in dependent repo, unplanned dependency). Slot a new pending step at N+1; later pending/in-progress steps shift up by one. agentrail reorder N --to M Priorities change. Move a pending or in-progress step; intervening steps shift the other way. agentrail reopen N A completed or blocked step turns out to be wrong. Transition it back to in-progress, clear completed_at, re-focus the cursor. The invariant that makes this safe: Completed steps never renumber. They are anchored to git history via their commits array. Any operation that would renumber a completed step is rejected. Reopen preserves the step’s commits so the git-history linkage stays intact. This is why insert and reorder only move pending and in-progress steps. The past is immutable; the future is negotiable. Cursor Preemption A small but important follow-up. When insert drops a blocker at slot N+1, the cursor used to stay attached to its original step by identity — the new step was there, but agentrail next still surfaced the old focus. Same story for reorder pulling a later step forward. Both now apply a preemption rule: if the new or moved step lands at or ahead of the cursor’s slot, focus follows it. Steps placed behind the cursor are still queued without disturbing focus. The result: agentrail next shows you the thing you just said was more urgent, instead of making you re-focus manually. Maintenance Mode: add + Ad-Hoc Tasks Not every session is a planned saga. Sometimes the shape of the work is a todo stream, not a roadmap: User types a task. Agent calls agentrail add to create a step. Agent runs begin / work / complete. Repeat. agentrail add --slug &lt;slug&gt; --prompt &lt;text&gt; records the step without going through the plan-first flow. Combined with a CLAUDE.md maintenance protocol, this turns AgentRail into a daily driver for ad-hoc work, not just a tool for staged roadmaps. Agents can also use add mid-session to enqueue related work they discovered but shouldn’t pursue right now. When Things Go Wrong: audit + snapshot These two were motivated by a real incident: an agent deleted untracked .agentrail/ files. Nothing in the reflog. No recovery path. That one hurt enough to build infrastructure for. agentrail snapshot writes a git commit of .agentrail/ (and .agentrail-archive/ when present) under refs/agentrail/snapshots/&lt;timestamp&gt;. Implementation detail that matters: it uses GIT_INDEX_FILE pointed at a throwaway temp file, so the user’s real index is never touched — no staged-file surprises, git status is unchanged before and after, and pre-commit hooks don’t race. Because a named ref holds the commit, blobs survive git gc. Restore is left to the user via plain git: git restore --source=&lt;ref&gt; -- .agentrail .agentrail-archive. AgentRail never writes to the working tree on your behalf. agentrail audit compares git history against saga history and reports three categories: matched commits, orphan commits (in git but not claimed by any step), and orphan steps (claimed commits that don’t exist). With --emit-commands, it prints a shell script of agentrail add lines seeded from commit subjects — a reconstruction scaffold you review before running. Two schema changes make audit exact instead of heuristic: StepConfig.commits: Vec&lt;String&gt; is populated by complete from HEAD, so step &lt;-&gt; commit linkage is recorded at the moment of truth. SagaConfig.retroactive: bool lets you mark a saga as reconstructed-after-the-fact, so audits treat its commits as claimed. One related bug worth calling out. agentrail add --commit &lt;ref&gt; used to record whatever string you passed — short hashes, tags, HEAD~N. Audit compares against full 40-char SHAs from git log %H, so non-canonical entries never matched and every short-hash commit was flagged as an orphan. A retroactive saga in sw-cor24-snobol4 produced 8 phantom orphans this way. Fixed by routing every --commit value through git rev-parse --verify &lt;ref&gt;^{commit} and storing only the full SHA. Unresolvable refs now fail at add time with a clear error, instead of silently producing orphans at audit time. archive: Closing One Saga, Opening the Next agentrail archive moves .agentrail/ into .agentrail-archive/&lt;name&gt;-&lt;timestamp&gt;/ and clears the way for a new init. Optional --reason writes archive-reason.txt alongside. Collisions get a -2, -3 suffix. It sounds small; in practice it’s what makes AgentRail work across many small sagas in the same repo instead of one ever-growing one. gen-agents-doc: Portable Rules Dogfooding across N repos only works if the safety rules travel. agentrail gen-agents-doc writes AGENTS.example.md — a self-contained template covering the session protocol, .agentrail/ handling rules, and audit recovery guidance. Drop it in any project, rename to AGENTS.md, CLAUDE.md, .cursorrules, whatever your agent reads. The template is embedded via include_str!, so the binary is self-sufficient. What Dogfooding Teaches Three patterns keep recurring in the April commit log: Pattern Example Surprises need a slot, not a restart insert / reorder / reopen Recovery tools must exist before you need them audit / snapshot — built after an incident, not during one Bootstrapping friction kills adoption across repos setup / gen-agents-doc / archive The meta-lesson: the only way to find these problems is to use the tool on real work, every day, across the kind of messy, parallel, interrupt-driven development that real projects actually look like. A plan-first feature list will never surface the cursor-preemption bug, the short-hash audit false-positives, or the “I need a todo stream, not a roadmap” mode. Only use does. Next up: porting these conventions into a couple of the other sw-vibe-coding repos so more of the saga history is recoverable by default. AgentRail is one command at a time. Follow for more AI Tools posts on the small tools that keep agents honest.]]></summary></entry><entry><title type="html">ML Frontier #05: Grokking — Delayed Generalization</title><link href="https://software-wrighter-lab.github.io/2026/04/23/mlf-05-grokking-delayed-generalization/" rel="alternate" type="text/html" title="ML Frontier #05: Grokking — Delayed Generalization" /><published>2026-04-23T00:15:00-07:00</published><updated>2026-04-23T00:15:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/23/mlf-05-grokking-delayed-generalization</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/23/mlf-05-grokking-delayed-generalization/"><![CDATA[<p><img src="/assets/images/posts/thinker-grokking.png" class="post-marker no-invert" alt="" style="width: 120px;" /></p>

<p>Fifth ML Frontier episode. You train a small network on a math task. Training loss drops to zero. Test accuracy stays at random chance. Classical intuition says you’re done — the model has overfit. But you keep training, and thousands of steps later, something surprising happens.</p>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Papers</strong></td>
        <td><a href="#papers">4 papers</a></td>
      </tr>
      <tr>
        <td><strong>Video</strong></td>
        <td><a href="https://www.youtube.com/shorts/GVLnOhmHYGc">ML Frontier 5: Grokking</a><br /><a href="https://www.youtube.com/shorts/GVLnOhmHYGc"><img src="https://img.youtube.com/vi/GVLnOhmHYGc/mqdefault.jpg" alt="Video" class="video-thumb" /></a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-phenomenon">The Phenomenon</h2>

<p>Power et al. (2022) reported something that looked like a bug. They trained small transformers on modular arithmetic tasks and watched training loss crash to zero while test accuracy sat at random chance. By every classical signal, the models had overfit.</p>

<p>Then they left training running. Thousands of optimization steps past convergence — long after any reasonable early-stopping rule would have halted the run — test accuracy suddenly jumped from random to nearly perfect. The model hadn’t just memorized. It had discovered the underlying rule.</p>

<p>They called this <em>grokking</em>: delayed generalization.</p>

<h2 id="competing-circuits-inside-the-network">Competing Circuits Inside the Network</h2>

<p>Nanda et al. (2023) opened the network up to find out what was actually happening. Using mechanistic interpretability, they tracked the internal structure of a small transformer learning modular addition across training.</p>

<p>What they found: two solutions coexist and compete. A memorization circuit dominates early — it fits the training data by rote but doesn’t generalize. Underneath it, a generalizing solution slowly forms. For modular arithmetic, that solution turns out to be a Fourier-style trigonometric circuit that represents numbers on a circle and adds by rotating.</p>

<p>Generalization wasn’t absent during the long plateau. It was being constructed, gradually, while memorization held the foreground.</p>

<h2 id="why-regularization-wins">Why Regularization Wins</h2>

<p>The handoff from memorization to generalization isn’t automatic. It needs pressure.</p>

<p>Weight decay is the key. Memorization circuits are large and brittle — they spread signal across many weights to pin down specific examples. The generalizing circuit is simpler and uses smaller weights. Weight decay taxes the memorization circuit more heavily than the general one, so over many steps it suppresses memorization and lets the simpler solution take over.</p>

<p>Omnigrok (Liu et al., 2022) generalized this picture beyond modular arithmetic, showing grokking-like dynamics across more varied tasks when the right regularization and initialization are in play. Kumar et al. (2024) reframes grokking as a transition from <strong>lazy</strong> training dynamics (features barely move from initialization) to <strong>rich</strong> training dynamics (features reorganize meaningfully) — tying delayed generalization to well-studied concepts in deep learning theory.</p>

<h2 id="generalization-as-phase-transition">Generalization as Phase Transition</h2>

<p>The uncomfortable lesson: generalization doesn’t always emerge smoothly as loss decreases. It can appear suddenly, like a phase transition, after a long latent period where the model looks stuck.</p>

<table>
  <thead>
    <tr>
      <th>Phase</th>
      <th>Train loss</th>
      <th>Test accuracy</th>
      <th>What’s happening inside</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Memorization</td>
      <td>Drops to ~0</td>
      <td>Random</td>
      <td>Network fits training set by rote</td>
    </tr>
    <tr>
      <td>Plateau</td>
      <td>~0</td>
      <td>Still random</td>
      <td>Generalizing circuit slowly forming under regularization pressure</td>
    </tr>
    <tr>
      <td>Grokking</td>
      <td>~0</td>
      <td>Jumps to ~100%</td>
      <td>Generalizing circuit overtakes memorization</td>
    </tr>
  </tbody>
</table>

<p>Classical early stopping would have halted training in phase 1 or 2 and declared the model a failure. The generalizing solution existed but hadn’t yet taken over the output.</p>

<h2 id="the-open-question-for-large-models">The Open Question for Large Models</h2>

<p>Grokking is most cleanly documented on small networks, small algorithmic datasets, with aggressive weight decay. The frontier question is whether a version of this happens quietly inside large language models during pretraining — on subsets of their data, on specific capabilities, hidden inside aggregate loss curves that look smooth.</p>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Does grokking occur inside large-scale LLMs during pretraining?</td>
      <td>Open — aggregate loss hides per-capability dynamics</td>
    </tr>
    <tr>
      <td>Is grokking universal or tied to specific regimes?</td>
      <td>Evidence leans toward regime-dependent (weight decay + small tasks), but lazy-to-rich framing broadens it</td>
    </tr>
    <tr>
      <td>Can we detect grokking in practice?</td>
      <td>Mechanistic progress measures help on small models; scaling them is open</td>
    </tr>
    <tr>
      <td>Can we accelerate grokking?</td>
      <td>Regularization tuning and initialization choices influence timing</td>
    </tr>
  </tbody>
</table>

<p>If LLM pretraining hides local grokking events — capabilities that appear abruptly after a long latent phase — then loss curves alone aren’t enough to decide when a model is “done.” Something richer would be needed to know what it has actually learned.</p>

<h2 id="papers">Papers</h2>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Paper</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Jan 2022</td>
      <td>Grokking: Generalization Beyond Overfitting on Small Algorithmic Datasets (Power et al.)</td>
      <td><a href="https://arxiv.org/abs/2201.02177">arXiv 2201.02177</a></td>
    </tr>
    <tr>
      <td>Oct 2022</td>
      <td>Omnigrok: Grokking Beyond Algorithmic Data (Liu et al.)</td>
      <td><a href="https://arxiv.org/abs/2210.01117">arXiv 2210.01117</a></td>
    </tr>
    <tr>
      <td>Jan 2023</td>
      <td>Progress Measures for Grokking via Mechanistic Interpretability (Nanda et al.)</td>
      <td><a href="https://arxiv.org/abs/2301.05217">arXiv 2301.05217</a></td>
    </tr>
    <tr>
      <td>Oct 2023</td>
      <td>Grokking as the Transition from Lazy to Rich Training Dynamics (Kumar et al.)</td>
      <td><a href="https://arxiv.org/abs/2310.06110">arXiv 2310.06110</a></td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>Generalization can be quiet, slow, and sudden all at once. Follow for more ML Frontier episodes exploring research at the edge.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="machine-learning" /><category term="research" /><category term="explainers" /><category term="ml-frontier" /><category term="grokking" /><category term="generalization" /><category term="mechanistic-interpretability" /><category term="weight-decay" /><category term="phase-transition" /><category term="deep-learning" /><summary type="html"><![CDATA[Fifth ML Frontier episode. You train a small network on a math task. Training loss drops to zero. Test accuracy stays at random chance. Classical intuition says you’re done — the model has overfit. But you keep training, and thousands of steps later, something surprising happens. Resource Link Papers 4 papers Video ML Frontier 5: Grokking Comments Discord The Phenomenon Power et al. (2022) reported something that looked like a bug. They trained small transformers on modular arithmetic tasks and watched training loss crash to zero while test accuracy sat at random chance. By every classical signal, the models had overfit. Then they left training running. Thousands of optimization steps past convergence — long after any reasonable early-stopping rule would have halted the run — test accuracy suddenly jumped from random to nearly perfect. The model hadn’t just memorized. It had discovered the underlying rule. They called this grokking: delayed generalization. Competing Circuits Inside the Network Nanda et al. (2023) opened the network up to find out what was actually happening. Using mechanistic interpretability, they tracked the internal structure of a small transformer learning modular addition across training. What they found: two solutions coexist and compete. A memorization circuit dominates early — it fits the training data by rote but doesn’t generalize. Underneath it, a generalizing solution slowly forms. For modular arithmetic, that solution turns out to be a Fourier-style trigonometric circuit that represents numbers on a circle and adds by rotating. Generalization wasn’t absent during the long plateau. It was being constructed, gradually, while memorization held the foreground. Why Regularization Wins The handoff from memorization to generalization isn’t automatic. It needs pressure. Weight decay is the key. Memorization circuits are large and brittle — they spread signal across many weights to pin down specific examples. The generalizing circuit is simpler and uses smaller weights. Weight decay taxes the memorization circuit more heavily than the general one, so over many steps it suppresses memorization and lets the simpler solution take over. Omnigrok (Liu et al., 2022) generalized this picture beyond modular arithmetic, showing grokking-like dynamics across more varied tasks when the right regularization and initialization are in play. Kumar et al. (2024) reframes grokking as a transition from lazy training dynamics (features barely move from initialization) to rich training dynamics (features reorganize meaningfully) — tying delayed generalization to well-studied concepts in deep learning theory. Generalization as Phase Transition The uncomfortable lesson: generalization doesn’t always emerge smoothly as loss decreases. It can appear suddenly, like a phase transition, after a long latent period where the model looks stuck. Phase Train loss Test accuracy What’s happening inside Memorization Drops to ~0 Random Network fits training set by rote Plateau ~0 Still random Generalizing circuit slowly forming under regularization pressure Grokking ~0 Jumps to ~100% Generalizing circuit overtakes memorization Classical early stopping would have halted training in phase 1 or 2 and declared the model a failure. The generalizing solution existed but hadn’t yet taken over the output. The Open Question for Large Models Grokking is most cleanly documented on small networks, small algorithmic datasets, with aggressive weight decay. The frontier question is whether a version of this happens quietly inside large language models during pretraining — on subsets of their data, on specific capabilities, hidden inside aggregate loss curves that look smooth. Question Status Does grokking occur inside large-scale LLMs during pretraining? Open — aggregate loss hides per-capability dynamics Is grokking universal or tied to specific regimes? Evidence leans toward regime-dependent (weight decay + small tasks), but lazy-to-rich framing broadens it Can we detect grokking in practice? Mechanistic progress measures help on small models; scaling them is open Can we accelerate grokking? Regularization tuning and initialization choices influence timing If LLM pretraining hides local grokking events — capabilities that appear abruptly after a long latent phase — then loss curves alone aren’t enough to decide when a model is “done.” Something richer would be needed to know what it has actually learned. Papers Date Paper Link Jan 2022 Grokking: Generalization Beyond Overfitting on Small Algorithmic Datasets (Power et al.) arXiv 2201.02177 Oct 2022 Omnigrok: Grokking Beyond Algorithmic Data (Liu et al.) arXiv 2210.01117 Jan 2023 Progress Measures for Grokking via Mechanistic Interpretability (Nanda et al.) arXiv 2301.05217 Oct 2023 Grokking as the Transition from Lazy to Rich Training Dynamics (Kumar et al.) arXiv 2310.06110 Generalization can be quiet, slow, and sudden all at once. Follow for more ML Frontier episodes exploring research at the edge.]]></summary></entry><entry><title type="html">Embedded (3/?): How Much of Forth Can Be Forth? A Kernel Self-Hosting Spectrum</title><link href="https://software-wrighter-lab.github.io/2026/04/20/embedded-forth-self-hosting-spectrum/" rel="alternate" type="text/html" title="Embedded (3/?): How Much of Forth Can Be Forth? A Kernel Self-Hosting Spectrum" /><published>2026-04-20T18:30:00-07:00</published><updated>2026-04-20T18:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/20/embedded-forth-self-hosting-spectrum</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/20/embedded-forth-self-hosting-spectrum/"><![CDATA[<p><img src="/assets/images/posts/block-questions-marks-escher.png" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>How much of a Forth kernel can be written in Forth instead of assembly? The question has an obvious answer (<em>“as much as possible”</em>) and a less obvious answer (<em>“it depends on which phase of the bootstrap you’re in”</em>). This post walks through four points along that spectrum for the <a href="https://github.com/sw-embed/sw-cor24-forth">COR24 Forth</a> kernel: two phases shipped, a third in progress with its first subsets landing, and a fourth on the horizon.</p>

  <p>It’s a deep dive — every movement of a word from <code class="language-plaintext highlighter-rouge">.s</code> to <code class="language-plaintext highlighter-rouge">.fth</code> changes the bootstrap ordering, the primitive set, and what the next movement looks like. Forth is an unusually good language for showing its own self-extending nature, and reading the phases in sequence reads like one of Escher’s drawings: each hand sketching the other.</p>

</div>

<div class="aside-box">

  <p><strong>Why this matters</strong> — Self-hosting is the final test that a language is expressive enough for systems work. Moving Forth words from assembly into Forth itself shows exactly where the irreducible floor is: the primitives that <em>must</em> be machine code. Everything above that floor can, in principle, live in <code class="language-plaintext highlighter-rouge">.fth</code> source.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth Demo</a> — three tabs: <em>forth.s</em> (phase 1), <em>forth-in-forth</em> (phase 2, default), <em>forth-on-forthish</em> (phase 3 in progress)</td>
      </tr>
      <tr>
        <td><strong>Forth Interpreter (CLI)</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Web Demo</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-forth">sw-embed/web-sw-cor24-forth</a></td>
      </tr>
      <tr>
        <td><strong>Approach Comparison</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/future.md">docs/future.md</a></td>
      </tr>
      <tr>
        <td><strong>Phase 2 Status</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/forth-in-forth/docs/status.md">forth-in-forth/docs/status.md</a></td>
      </tr>
      <tr>
        <td><strong>Closed Issues</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-forth/issues/1">#1 hashed dictionary</a> · <a href="https://github.com/sw-embed/sw-cor24-forth/issues/2">#2 DO/LOOP &amp; friends</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/03/22/cor24-rs-assembly-emulator/">Embedded (2/?): COR24 Assembly Emulator</a></td>
      </tr>
      <tr>
        <td><strong>Follow-on post</strong></td>
        <td><a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="the-four-approaches">The Four Approaches</h2>

<p>A single axis: what fraction of the kernel is hand-written assembly, and what fraction is Forth? Four labeled points along it:</p>

<table>
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Directory</th>
      <th>Where the kernel comes from</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>All-asm kernel</td>
      <td><code class="language-plaintext highlighter-rouge">./</code> (repo root)</td>
      <td>Hand-written <code class="language-plaintext highlighter-rouge">.s</code></td>
    </tr>
    <tr>
      <td>2</td>
      <td>Tiered Forth on a slimmed kernel</td>
      <td><code class="language-plaintext highlighter-rouge">./forth-in-forth/</code></td>
      <td>Hand-written <code class="language-plaintext highlighter-rouge">.s</code> + hand-written <code class="language-plaintext highlighter-rouge">.fth</code></td>
    </tr>
    <tr>
      <td>3</td>
      <td>Minimal-primitive kernel</td>
      <td><code class="language-plaintext highlighter-rouge">./forth-on-forthish/</code></td>
      <td>Smaller <code class="language-plaintext highlighter-rouge">.s</code> (a Forth-<em>ish</em> primitive substrate) + larger <code class="language-plaintext highlighter-rouge">.fth</code></td>
    </tr>
    <tr>
      <td>4</td>
      <td>Self-hosted via cross-compiler</td>
      <td><code class="language-plaintext highlighter-rouge">./forth-from-forth/</code></td>
      <td>Hand-written <em>Forth</em> compiler emits the <code class="language-plaintext highlighter-rouge">.s</code></td>
    </tr>
  </tbody>
</table>

<p>The preposition family — <em>in / on-ish / from</em> — signals what the kernel <strong>is</strong> to the Forth code on top of it:</p>

<ul>
  <li>In approach 2, Forth runs <strong>in</strong> a slimmed asm host.</li>
  <li>In approach 3, the substrate is so reduced it’s barely asm any more — Forth runs on something that’s already Forth-<strong>ish</strong>.</li>
  <li>In approach 4, the kernel itself comes <strong>from</strong> Forth (Forth source emits the <code class="language-plaintext highlighter-rouge">.s</code>).</li>
</ul>

<h2 id="phase-1-all-asm-kernel--where-we-started">Phase 1: All-Asm Kernel — Where We Started</h2>

<p><code class="language-plaintext highlighter-rouge">forth.s</code> as a single self-contained file. Every word is assembly, including <code class="language-plaintext highlighter-rouge">IF</code>/<code class="language-plaintext highlighter-rouge">THEN</code>, <code class="language-plaintext highlighter-rouge">.</code>, <code class="language-plaintext highlighter-rouge">WORDS</code>, <code class="language-plaintext highlighter-rouge">.S</code>, <code class="language-plaintext highlighter-rouge">\</code>, <code class="language-plaintext highlighter-rouge">(</code>, and so on. About 3000 lines of asm, 3879 bytes assembled. Still the canonical kernel for the web frontend and the existing <a href="https://github.com/sw-vibe-coding/agentrail-rs">reg-rs</a> regression tests.</p>

<p>This was the right starting point. A single-file kernel is easy to debug, easy to load, and explicit about every mechanism. The cost: it doesn’t show Forth’s most characteristic feature — <em>self-extension</em> — because everything is already defined in asm. There’s no moment where Forth makes itself bigger.</p>

<h2 id="phase-2-forth-in-forth--shipped-today">Phase 2: forth-in-forth — Shipped Today</h2>

<p><code class="language-plaintext highlighter-rouge">forth-in-forth/kernel.s</code> plus four tiered <code class="language-plaintext highlighter-rouge">.fth</code> files: <code class="language-plaintext highlighter-rouge">core/minimal.fth</code>, <code class="language-plaintext highlighter-rouge">lowlevel.fth</code>, <code class="language-plaintext highlighter-rouge">midlevel.fth</code>, <code class="language-plaintext highlighter-rouge">highlevel.fth</code>. The kernel keeps only what <em>must</em> be asm — the threading layer, ALU primitives, hardware I/O, the dict-text triplet (<code class="language-plaintext highlighter-rouge">WORD</code>/<code class="language-plaintext highlighter-rouge">FIND</code>/<code class="language-plaintext highlighter-rouge">NUMBER</code>), the outer loop (<code class="language-plaintext highlighter-rouge">INTERPRET</code>/<code class="language-plaintext highlighter-rouge">QUIT</code>), and <code class="language-plaintext highlighter-rouge">:</code>/<code class="language-plaintext highlighter-rouge">;</code>. Everything else moved to <code class="language-plaintext highlighter-rouge">.fth</code>.</p>

<p>The migration happened in 11 subsets, each a single commit:</p>

<table>
  <thead>
    <tr>
      <th>Subset</th>
      <th>Description</th>
      <th>Commit</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Baseline fib example, demo, reg-rs test</td>
      <td><code class="language-plaintext highlighter-rouge">86edf74</code></td>
    </tr>
    <tr>
      <td>2</td>
      <td>Scaffold <code class="language-plaintext highlighter-rouge">forth-in-forth/</code> directory</td>
      <td><code class="language-plaintext highlighter-rouge">94e76b2</code></td>
    </tr>
    <tr>
      <td>3</td>
      <td>Move <code class="language-plaintext highlighter-rouge">IF</code>/<code class="language-plaintext highlighter-rouge">THEN</code>/<code class="language-plaintext highlighter-rouge">ELSE</code>/<code class="language-plaintext highlighter-rouge">BEGIN</code>/<code class="language-plaintext highlighter-rouge">UNTIL</code> to Forth</td>
      <td><code class="language-plaintext highlighter-rouge">686c65f</code></td>
    </tr>
    <tr>
      <td>4</td>
      <td>Move <code class="language-plaintext highlighter-rouge">\</code> and <code class="language-plaintext highlighter-rouge">(</code> to Forth (add <code class="language-plaintext highlighter-rouge">EOL!</code> primitive)</td>
      <td><code class="language-plaintext highlighter-rouge">71e1627</code></td>
    </tr>
    <tr>
      <td>5</td>
      <td>Stack &amp; arith helpers in <code class="language-plaintext highlighter-rouge">core/lowlevel.fth</code></td>
      <td><code class="language-plaintext highlighter-rouge">7d0037c</code></td>
    </tr>
    <tr>
      <td>6</td>
      <td><code class="language-plaintext highlighter-rouge">=</code> and <code class="language-plaintext highlighter-rouge">0=</code> via <code class="language-plaintext highlighter-rouge">XOR</code></td>
      <td><code class="language-plaintext highlighter-rouge">ce57489</code></td>
    </tr>
    <tr>
      <td>7</td>
      <td><code class="language-plaintext highlighter-rouge">CR</code> <code class="language-plaintext highlighter-rouge">SPACE</code> <code class="language-plaintext highlighter-rouge">HEX</code> <code class="language-plaintext highlighter-rouge">DECIMAL</code> to Forth</td>
      <td><code class="language-plaintext highlighter-rouge">06a8dca</code></td>
    </tr>
    <tr>
      <td>8</td>
      <td><code class="language-plaintext highlighter-rouge">.</code> to Forth (hide asm <code class="language-plaintext highlighter-rouge">.</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">12de5b1</code></td>
    </tr>
    <tr>
      <td>9</td>
      <td><code class="language-plaintext highlighter-rouge">DEPTH</code> / <code class="language-plaintext highlighter-rouge">.S</code> to Forth (add <code class="language-plaintext highlighter-rouge">SP@</code> primitive)</td>
      <td><code class="language-plaintext highlighter-rouge">d65ae26</code></td>
    </tr>
    <tr>
      <td>10</td>
      <td><code class="language-plaintext highlighter-rouge">WORDS</code> <code class="language-plaintext highlighter-rouge">VER</code> <code class="language-plaintext highlighter-rouge">SEE</code> to Forth (add <code class="language-plaintext highlighter-rouge">'</code>, <code class="language-plaintext highlighter-rouge">&gt;NAME</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">c908615</code></td>
    </tr>
    <tr>
      <td>11</td>
      <td><code class="language-plaintext highlighter-rouge">repl.sh</code> and <code class="language-plaintext highlighter-rouge">see-demo.sh</code></td>
      <td><code class="language-plaintext highlighter-rouge">8c9104a</code></td>
    </tr>
  </tbody>
</table>

<p>The net movement was 18 words <strong>out</strong> of asm, 3 new asm primitives <strong>in</strong>, and 19 brand-new Forth words <strong>added on top</strong>:</p>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>Words</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Moved asm → Forth</strong> (18)</td>
      <td>IF, THEN, ELSE, BEGIN, UNTIL, <code class="language-plaintext highlighter-rouge">\</code>, <code class="language-plaintext highlighter-rouge">(</code>, <code class="language-plaintext highlighter-rouge">=</code>, <code class="language-plaintext highlighter-rouge">0=</code>, CR, SPACE, HEX, DECIMAL, <code class="language-plaintext highlighter-rouge">.</code>, DEPTH, .S, WORDS, VER</td>
    </tr>
    <tr>
      <td><strong>New asm primitives</strong> (3)</td>
      <td><code class="language-plaintext highlighter-rouge">[']</code> (needed for Forth IF/THEN to compile BRANCH/0BRANCH at compile time), <code class="language-plaintext highlighter-rouge">EOL!</code> (needed for Forth <code class="language-plaintext highlighter-rouge">\</code> to end the input line), <code class="language-plaintext highlighter-rouge">SP@</code> (needed for Forth <code class="language-plaintext highlighter-rouge">DEPTH</code>/<code class="language-plaintext highlighter-rouge">.S</code> to inspect the stack pointer)</td>
    </tr>
    <tr>
      <td><strong>New Forth words</strong> (19)</td>
      <td>NIP, TUCK, ROT, -ROT, 2DUP, 2DROP, 2SWAP, 2OVER, 1+, 1-, NEGATE, ABS, /, MOD, 0&lt; (lowlevel); <code class="language-plaintext highlighter-rouge">'</code>, PRINT-NAME, &gt;NAME, SEE (highlevel)</td>
    </tr>
  </tbody>
</table>

<p>The headline numbers after phase 2:</p>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th style="text-align: right">Before</th>
      <th style="text-align: right">After</th>
      <th style="text-align: right">Δ</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>asm dictionary entries</td>
      <td style="text-align: right">65</td>
      <td style="text-align: right">50</td>
      <td style="text-align: right"><strong>−15</strong></td>
    </tr>
    <tr>
      <td>asm lines (<code class="language-plaintext highlighter-rouge">kernel.s</code>)</td>
      <td style="text-align: right">2852</td>
      <td style="text-align: right">2239</td>
      <td style="text-align: right"><strong>−613 (−22%)</strong></td>
    </tr>
    <tr>
      <td>Assembled binary bytes</td>
      <td style="text-align: right">3879</td>
      <td style="text-align: right">2786</td>
      <td style="text-align: right"><strong>−1093 (−28%)</strong></td>
    </tr>
    <tr>
      <td>Forth colon defs (<code class="language-plaintext highlighter-rouge">core/*.fth</code>)</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">37</td>
      <td style="text-align: right"><strong>+37</strong></td>
    </tr>
    <tr>
      <td><strong>Total vocabulary visible at REPL</strong></td>
      <td style="text-align: right"><strong>62</strong></td>
      <td style="text-align: right"><strong>86</strong></td>
      <td style="text-align: right"><strong>+24</strong></td>
    </tr>
  </tbody>
</table>

<p>Forth words, by tier:</p>

<table>
  <thead>
    <tr>
      <th>Tier</th>
      <th style="text-align: right">Count</th>
      <th>Words</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>minimal.fth</td>
      <td style="text-align: right">9</td>
      <td>BEGIN UNTIL IF THEN ELSE <code class="language-plaintext highlighter-rouge">0=</code> <code class="language-plaintext highlighter-rouge">=</code> <code class="language-plaintext highlighter-rouge">(</code> <code class="language-plaintext highlighter-rouge">\</code></td>
    </tr>
    <tr>
      <td>lowlevel.fth</td>
      <td style="text-align: right">15</td>
      <td>NIP TUCK ROT -ROT 2DUP 2DROP 2SWAP 2OVER <code class="language-plaintext highlighter-rouge">0&lt;</code> 1+ 1- NEGATE ABS <code class="language-plaintext highlighter-rouge">/</code> MOD</td>
    </tr>
    <tr>
      <td>midlevel.fth</td>
      <td style="text-align: right">5</td>
      <td>CR SPACE HEX DECIMAL <code class="language-plaintext highlighter-rouge">.</code></td>
    </tr>
    <tr>
      <td>highlevel.fth</td>
      <td style="text-align: right">8</td>
      <td>DEPTH .S <code class="language-plaintext highlighter-rouge">'</code> PRINT-NAME WORDS VER &gt;NAME SEE</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">SEE SQUARE</code> now prints <code class="language-plaintext highlighter-rouge">DUP * ;</code>. <code class="language-plaintext highlighter-rouge">SEE CUBE</code> prints <code class="language-plaintext highlighter-rouge">DUP SQUARE * ;</code>. The machinery for decompiling a colon definition lives in Forth, because <code class="language-plaintext highlighter-rouge">SEE</code> itself is Forth. That’s the self-extending story the all-asm kernel couldn’t tell.</p>

<h3 id="why-phase-2-stopped-where-it-did">Why Phase 2 Stopped Where It Did</h3>

<p>Three categories of word resist moving to Forth, and together they explain the ~50 asm primitives left:</p>

<ol>
  <li><strong>Threading-layer primitives are below Forth’s level.</strong> <code class="language-plaintext highlighter-rouge">NEXT</code>, <code class="language-plaintext highlighter-rouge">DOCOL</code>, <code class="language-plaintext highlighter-rouge">EXIT</code>, <code class="language-plaintext highlighter-rouge">LIT</code>, <code class="language-plaintext highlighter-rouge">BRANCH</code>, <code class="language-plaintext highlighter-rouge">0BRANCH</code>, <code class="language-plaintext highlighter-rouge">EXECUTE</code> define <em>how</em> threaded code runs. They can’t themselves be threaded code — the CPU has to jump to them.</li>
  <li><strong>Some primitives need hardware/ALU/memory access.</strong> <code class="language-plaintext highlighter-rouge">+</code>, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">!</code>, <code class="language-plaintext highlighter-rouge">KEY</code>, <code class="language-plaintext highlighter-rouge">EMIT</code>, <code class="language-plaintext highlighter-rouge">LED!</code>, <code class="language-plaintext highlighter-rouge">SW?</code> ultimately compile to native instructions. Forth can wrap them, but something has to execute the actual <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">lw</code>, <code class="language-plaintext highlighter-rouge">sw</code>, or memory-mapped UART access.</li>
  <li><strong>Bootstrap-phase primitives need to exist <em>before</em> any <code class="language-plaintext highlighter-rouge">.fth</code> source loads.</strong> <code class="language-plaintext highlighter-rouge">WORD</code>, <code class="language-plaintext highlighter-rouge">FIND</code>, <code class="language-plaintext highlighter-rouge">NUMBER</code>, <code class="language-plaintext highlighter-rouge">:</code>, <code class="language-plaintext highlighter-rouge">;</code>, <code class="language-plaintext highlighter-rouge">INTERPRET</code>, <code class="language-plaintext highlighter-rouge">QUIT</code> are all <em>used</em> by the outer interpreter that reads <code class="language-plaintext highlighter-rouge">.fth</code> source. They could be Forth in principle — but only if a <em>smaller</em> bootstrap interpreter runs first. Phase 2 sidesteps the recursion by keeping them in asm.</li>
</ol>

<p>Phase 3 doesn’t dodge category 3. It attacks it head-on.</p>

<h2 id="what-the-web-port-taught-us">What the Web Port Taught Us</h2>

<p>Building the phase 2 tab in <a href="https://sw-embed.github.io/web-sw-cor24-forth/">web-sw-cor24-forth</a> — alongside the phase 1 <code class="language-plaintext highlighter-rouge">forth.s</code> tab, and now joined by a phase 3 <code class="language-plaintext highlighter-rouge">forth-on-forthish</code> tab — surfaced two categories of learning: <strong>performance</strong> and <strong>vocabulary</strong>.</p>

<p>The performance thread spans three hash functions, a 1-entry lookaside cache, an adaptive web pump-loop, and a build-time bootstrap snapshot (infrastructure shipped but gated off for measurement cleanliness). The vocabulary thread surfaced when the phase-2 tab’s Forth sat side-by-side with standard Forth idioms in teaching material. Both threads shipped fixes, some explicit deferrals, and one feature-flagged fast-path that stays off until the kernel-side work finishes.</p>

<h3 id="making-it-fast-part-1-hashing-find--three-attempts">Making It Fast, Part 1: Hashing <code class="language-plaintext highlighter-rouge">FIND</code> — Three Attempts</h3>

<p>The obvious suspect for slow bootstrap was <code class="language-plaintext highlighter-rouge">FIND</code>: a linear O(N) walk of the <code class="language-plaintext highlighter-rouge">LATEST</code> link chain, called for every token in every <code class="language-plaintext highlighter-rouge">.fth</code> line. At <strong>90 dictionary entries</strong> (50 asm + 40 Forth colon defs), the constant factor should add up. That hypothesis drove three successive attempts, documented in detail in <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/hashing-analysis.md"><code class="language-plaintext highlighter-rouge">docs/hashing-analysis.md</code></a>.</p>

<p>A glance at the first-letter distribution explains why the first attempt was in trouble:</p>

<table>
  <thead>
    <tr>
      <th>First char</th>
      <th>Words (count)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">S</code></td>
      <td>SWAP STATE SW? SP@ SEE-CFA SEE SPACE (7)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">E</code></td>
      <td>EMIT EXIT EXECUTE EOL! ELSE (5)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">D</code></td>
      <td>DROP DUP DEPTH DUMP-ALL DECIMAL (5)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">C</code></td>
      <td>C@ C! C, CREATE CR (5)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">B</code></td>
      <td>BRANCH BASE BYE BEGIN (4)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">2</code></td>
      <td>2DUP 2DROP 2SWAP 2OVER (4)</td>
    </tr>
  </tbody>
</table>

<p>Only ~43 distinct first-letter classes across 90 words. Any scheme that hashes on first char alone saturates long before 256 buckets.</p>

<h4 id="attempt-1--first-char-buckets-shipped">Attempt 1 — First-char buckets (shipped)</h4>

<p>A 256-bucket first-character table (tracked in <a href="https://github.com/sw-embed/sw-cor24-forth/issues/1">sw-cor24-forth#1</a>, commit <code class="language-plaintext highlighter-rouge">a3a63f0</code>). Populated at <code class="language-plaintext highlighter-rouge">_start</code> by walking <code class="language-plaintext highlighter-rouge">LATEST</code> newest-first, maintained by <code class="language-plaintext highlighter-rouge">do_create</code> on every new header, with linear fallback on bucket miss. Correctness held — all reg-rs tests pass, <code class="language-plaintext highlighter-rouge">SEE</code>, <code class="language-plaintext highlighter-rouge">DUMP-ALL</code>, every example produced identical UART output.</p>

<p>The measurement was humbling:</p>

<blockquote>
  <p><strong>CLI speedup on fib-demo compile: ~0% within timestamp resolution.</strong> <code class="language-plaintext highlighter-rouge">cor24-run</code> reports instruction timestamps rounded to 10K. Last UART TX for fib complete: 61.17M inst with hash vs 61.17M inst without.</p>
</blockquote>

<p>Profiling showed why. <code class="language-plaintext highlighter-rouge">FIND</code> is only ~0.3% of compile time. The other 99.7% splits between <code class="language-plaintext highlighter-rouge">KEY</code>’s UART busy-wait (spinning while <code class="language-plaintext highlighter-rouge">cor24-run</code> delivers the next input byte) and the threaded-code overhead of Forth-defined IMMEDIATE words (<code class="language-plaintext highlighter-rouge">IF</code>, <code class="language-plaintext highlighter-rouge">BEGIN</code>, <code class="language-plaintext highlighter-rouge">UNTIL</code>, <code class="language-plaintext highlighter-rouge">\</code>, <code class="language-plaintext highlighter-rouge">(</code>). Shrinking <code class="language-plaintext highlighter-rouge">FIND</code> from ~250 inst to ~50 inst per lookup saves ~200K inst, which disappears into the 61M total.</p>

<p>Still, WASM might behave differently. And with <code class="language-plaintext highlighter-rouge">EMIT</code>/<code class="language-plaintext highlighter-rouge">EXIT</code>, <code class="language-plaintext highlighter-rouge">OVER</code>/<code class="language-plaintext highlighter-rouge">OR</code>, and similar first-letter twins all sharing buckets, the fallback was doing more work than it should have been. Time for a better hash.</p>

<h4 id="attempt-2--len-seeded-mult33-shipped-with-a-detour">Attempt 2 — <code class="language-plaintext highlighter-rouge">len-seeded mult33</code> (shipped, with a detour)</h4>

<p>An <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/forth-in-forth/scripts/hash-collision-analysis.py">offline collision analysis</a> ran nine hash functions against all 90 known dictionary words, at bucket sizes 64/128/256/512. Full data lives in <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/hashing-analysis.md"><code class="language-plaintext highlighter-rouge">docs/hashing-analysis.md</code></a>; the summary:</p>

<table>
  <thead>
    <tr>
      <th>Hash function</th>
      <th style="text-align: right">64</th>
      <th style="text-align: right">128</th>
      <th style="text-align: right">256</th>
      <th style="text-align: right">512</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">first_char</code></td>
      <td style="text-align: right">47</td>
      <td style="text-align: right">47</td>
      <td style="text-align: right">47</td>
      <td style="text-align: right">47</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">len + first + last</code></td>
      <td style="text-align: right">47</td>
      <td style="text-align: right">34</td>
      <td style="text-align: right">34</td>
      <td style="text-align: right">34</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">len*31 + first + last</code></td>
      <td style="text-align: right">42</td>
      <td style="text-align: right">28</td>
      <td style="text-align: right">17</td>
      <td style="text-align: right">—</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">djb2</code></td>
      <td style="text-align: right">44</td>
      <td style="text-align: right">29</td>
      <td style="text-align: right">17</td>
      <td style="text-align: right">—</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mult33</code> (no seed)</td>
      <td style="text-align: right">44</td>
      <td style="text-align: right">31</td>
      <td style="text-align: right">21</td>
      <td style="text-align: right">—</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">fnv1a</code></td>
      <td style="text-align: right">44</td>
      <td style="text-align: right">28</td>
      <td style="text-align: right">17</td>
      <td style="text-align: right">—</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">len-seeded mult33</code></strong></td>
      <td style="text-align: right"><strong>34</strong></td>
      <td style="text-align: right"><strong>25</strong></td>
      <td style="text-align: right"><strong>11</strong></td>
      <td style="text-align: right">9</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">2-Round XMX</code></strong></td>
      <td style="text-align: right">—</td>
      <td style="text-align: right">23</td>
      <td style="text-align: right"><strong>15</strong></td>
      <td style="text-align: right"><strong>8</strong></td>
    </tr>
  </tbody>
</table>

<p>Len-seeded mult33 (<code class="language-plaintext highlighter-rouge">h = length; for c in name: h = h*33 + c</code>) won at 256 buckets with 11 collisions — a 35% improvement over the closest classical competitor. The length seed perturbs the initial state so short words spread out early in the iteration.</p>

<p>The rollout itself was instructive. Commit <code class="language-plaintext highlighter-rouge">485f36f</code> landed mult33 without the full example-suite check and broke the web agent. Commit <code class="language-plaintext highlighter-rouge">ab9817f</code> reverted. Commit <code class="language-plaintext highlighter-rouge">9bd4b10</code> re-landed it properly — all 15 examples byte-identical to the first-char version on CLI, then tested on WASM. WASM verdict: <strong>works, but wall-clock still not fast enough</strong>. A better hash doesn’t rescue a cold-boot that spends the majority of its time not in <code class="language-plaintext highlighter-rouge">FIND</code> at all.</p>

<p>That measurement effectively closed out hashing as a standalone fix. If bootstrap speed mattered on WASM — and it did, because the “forth-in-forth” tab felt visibly slower than the <code class="language-plaintext highlighter-rouge">forth.s</code> tab — something more fundamental than a hash swap was needed. The “Build-Time Bootstrap Dump” section below describes that answer.</p>

<p>Attempt 3 is still worth running, though, for reasons specific to this ISA.</p>

<h4 id="attempt-3--2-round-24-bit-xmx-shipped">Attempt 3 — 2-Round 24-bit XMX (shipped)</h4>

<p>The updated <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/hashing.txt"><code class="language-plaintext highlighter-rouge">docs/hashing.txt</code></a> design notes — a Gemini-assisted survey of 2025–2026 hashing research — surface three recent developments that change the tradeoffs:</p>

<ul>
  <li><strong>Krapivin’s optimal open addressing (2025).</strong> Probe sequence keeps lookups near-constant-time even at 99% table occupancy. Probe 2 becomes <code class="language-plaintext highlighter-rouge">(index + (hash &gt;&gt; 12) + 1) &amp; mask</code> instead of <code class="language-plaintext highlighter-rouge">+1</code> — a tiny asm change that avoids the clustering cliff classical linear probing hits when tables fill.</li>
  <li><strong>Learned / data-aware hashing.</strong> For a static Forth core with a known vocabulary at build time, a <a href="https://en.wikipedia.org/wiki/Perfect_hash_function">perfect-hash-function generator</a> can emit a hash with <strong>zero collisions</strong> on the core dictionary, lookup collapsing to a single multiply-shift.</li>
  <li><strong>SSHash cache-locality hashing (2024–2026).</strong> Order-preserving hashing for short strings (Forth word names are shaped like bioinformatics k-mers). Keeps related words physically close in RAM so the CPU prefetcher stays effective.</li>
</ul>

<p>For COR24’s constraints — 24-bit words, ~8 GPRs, sometimes no hardware multiplier — the pick was <strong>2-Round 24-bit XMX</strong> (Xor, Multiply, Xor), which shipped in commit <code class="language-plaintext highlighter-rouge">fdae7dd</code>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\ R0 = running hash (24 bits, native word size)
\ R1 = next character (or temp during avalanche)
\ R2 = MAGIC = 0xDEADB5, loaded once
\ Per character:
XOR              \ R0 ^= R1            (mix char into hash)
24_BIT_MUL       \ R0 *= R2            (native 24-bit truncation)
DUP 12 RSHIFT    \ R1 = R0 &gt;&gt; 12
XOR              \ R0 ^= R1            (spread high bits into low bits)
</code></pre></div></div>

<p>Two registers, no overflow waste (every bit of the 24-bit GPR carries signal), and the <code class="language-plaintext highlighter-rouge">h ^ (h &gt;&gt; 12)</code> avalanche step is the most bit-distributing operation tested. In the collision analysis XMX tied mult33’s worst-bucket depth at 256 (3) and beat it at 512 (2 vs 3). Per-character cost: ~10 COR24 ops vs ~4 for mult33 (roughly 2.5× slower per char), but for typical 4-character word names that’s ~24K extra instructions across a full bootstrap — noise against 61M.</p>

<p>All 15 example files and <code class="language-plaintext highlighter-rouge">scripts/see-demo.sh</code> produce byte-identical UART output vs the <code class="language-plaintext highlighter-rouge">first_char</code> baseline. Verified correctness, shipped, moved on.</p>

<h3 id="making-it-fast-part-2-a-1-entry-lookaside-cache">Making It Fast, Part 2: A 1-Entry Lookaside Cache</h3>

<p>A better hash function still does <code class="language-plaintext highlighter-rouge">compute_hash → bucket probe → name compare</code> for every token. Most colon-def bodies repeat words back-to-back (<code class="language-plaintext highlighter-rouge">DUP DUP</code>, <code class="language-plaintext highlighter-rouge">DROP DROP</code>, a word used twice in the same definition). Why recompute?</p>

<p>Commit <code class="language-plaintext highlighter-rouge">4ea2f79</code> added a <strong>1-entry lookaside cache</strong> (classic memento pattern). After every successful <code class="language-plaintext highlighter-rouge">FIND</code>, the kernel stashes a single triple — <code class="language-plaintext highlighter-rouge">(full 24-bit XMX hash, CFA, flag)</code> — in fixed memory. The next <code class="language-plaintext highlighter-rouge">FIND</code> that produces the same full hash skips the bucket probe and the name compare entirely, pushes the cached <code class="language-plaintext highlighter-rouge">(cfa, flag)</code>, and returns.</p>

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Choice</th>
      <th>Why</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Cache size</td>
      <td>1 entry</td>
      <td>Simplest possible memento; covers the “same word twice” pattern which is the common case.</td>
    </tr>
    <tr>
      <td>Cache key</td>
      <td>Full 24-bit XMX hash (not just the 8-bit bucket index)</td>
      <td>24-bit keyspace is effectively collision-free across a 90-word dict. False positives (returning the wrong CFA on a spurious hit) are astronomically unlikely.</td>
    </tr>
    <tr>
      <td>Cache update</td>
      <td>In <code class="language-plaintext highlighter-rouge">find_push_flag</code> just before the <code class="language-plaintext highlighter-rouge">NEXT</code> jump</td>
      <td>Reads flag + CFA off the data stack via <code class="language-plaintext highlighter-rouge">mov fp, sp; lw rX, off(fp)</code> without disturbing DS. Three <code class="language-plaintext highlighter-rouge">sw</code>s to store flag/cfa/hash.</td>
    </tr>
    <tr>
      <td>Cache NOT-FOUND?</td>
      <td>No</td>
      <td>Would cause incorrect stale hits when the user later defines the previously-failed word. Only successful lookups are cached.</td>
    </tr>
    <tr>
      <td>Invalidation</td>
      <td>Implicit — cfa=0 slot treated as empty; overwritten on next successful FIND</td>
      <td>Simple and correct; a user-defined <code class="language-plaintext highlighter-rouge">FORGET</code> that removes the cached word would need to clear the slot, but that isn’t currently implemented.</td>
    </tr>
  </tbody>
</table>

<p>Binary size went from 3893 → 3981 bytes (+88 bytes of asm). All 15 example files and <code class="language-plaintext highlighter-rouge">scripts/see-demo.sh</code> remained byte-identical.</p>

<p>CLI measurement once again showed no improvement — <code class="language-plaintext highlighter-rouge">cor24-run</code> timestamps quantize to 10,000 cycles, and the per-FIND savings (~30–50 inst per cache hit, ~15–25K across ~1000 lookups) are below that resolution. But this is a <strong>measurement-infrastructure limitation</strong>, not evidence the cache does nothing: WASM wall-clock has millisecond resolution over a multi-second bootstrap, and that’s where the cumulative savings of hash + lookaside become visible.</p>

<h4 id="implementation-history">Implementation history</h4>

<table>
  <thead>
    <tr>
      <th>Commit</th>
      <th>Hash</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">a3a63f0</code></td>
      <td><code class="language-plaintext highlighter-rouge">first_char</code></td>
      <td>First hash landed. 47 collisions. Poor distribution but correct.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">485f36f</code></td>
      <td><code class="language-plaintext highlighter-rouge">len-seeded mult33</code></td>
      <td>First try at a better hash. Pushed without full test suite; web agent reported broken.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ab9817f</code></td>
      <td><em>(revert)</em></td>
      <td>Reverted to <code class="language-plaintext highlighter-rouge">first_char</code> after bug report.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">9bd4b10</code></td>
      <td><code class="language-plaintext highlighter-rouge">len-seeded mult33</code></td>
      <td>Re-landed after all 15 examples went byte-identical. WASM-tested: works, still not fast enough.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">fdae7dd</code></td>
      <td><strong>2-Round XMX</strong></td>
      <td>Per <code class="language-plaintext highlighter-rouge">hashing.txt</code> recommendation for 24-bit GPR ISAs. Shipped.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">4ea2f79</code></td>
      <td><strong>XMX + 1-entry lookaside</strong></td>
      <td>Memento-pattern cache on top of XMX; +88 bytes. Shipped.</td>
    </tr>
  </tbody>
</table>

<h3 id="making-it-fast-part-3-the-web-tab-goes-snappy">Making It Fast, Part 3: The Web Tab Goes Snappy</h3>

<p>With the kernel-side hash + lookaside work landing, the web side had its own journey. The web agent tried two approaches in parallel — one shipped disabled, the other turned out to be the real winner.</p>

<h4 id="the-adaptive-pump-loop-shipped-the-actual-fix">The adaptive pump-loop (shipped, the actual fix)</h4>

<p><a href="https://github.com/sw-embed/web-sw-cor24-forth/blob/main/src/repl.rs"><code class="language-plaintext highlighter-rouge">web-sw-cor24-forth/src/repl.rs</code></a> runs the emulator in batches between UART byte feeds. The old scheme was a fixed <strong>20k instructions per byte</strong> — but for cheap-byte cases (where a single input byte triggers maybe 500 instructions of compile work before the next <code class="language-plaintext highlighter-rouge">KEY</code> poll), that meant burning ~19,500 cycles spinning in <code class="language-plaintext highlighter-rouge">key_poll</code> waiting for the next byte that the scheduler hadn’t delivered yet.</p>

<p>Commit <code class="language-plaintext highlighter-rouge">f757800</code> reworked the pump to inspect the CPU’s PC each iteration and adapt:</p>

<table>
  <thead>
    <tr>
      <th>Knob</th>
      <th>Old</th>
      <th>New</th>
      <th>Why</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Sub-batch size</td>
      <td>Fixed 20k inst</td>
      <td><code class="language-plaintext highlighter-rouge">PUMP_TINY = 2k</code> when PC is at a <code class="language-plaintext highlighter-rouge">key_poll</code> with bytes to feed; <code class="language-plaintext highlighter-rouge">PUMP_BIG = 50k</code> elsewhere</td>
      <td>Stop wasting cycles spinning in <code class="language-plaintext highlighter-rouge">key_poll</code> on cheap bytes; let real compile work run longer when it has actual work to do</td>
    </tr>
    <tr>
      <td>Tick batch</td>
      <td><code class="language-plaintext highlighter-rouge">BOOTSTRAP_BATCH = 500k</code></td>
      <td><code class="language-plaintext highlighter-rouge">BOOTSTRAP_BATCH = 600k</code> per tick</td>
      <td>Small bump; more work per scheduler wake</td>
    </tr>
    <tr>
      <td>Tick interval</td>
      <td><code class="language-plaintext highlighter-rouge">TICK_MS = 25</code> everywhere</td>
      <td><code class="language-plaintext highlighter-rouge">TICK_MS_BOOT = 5</code> during bootstrap; <code class="language-plaintext highlighter-rouge">TICK_MS_INTERACTIVE = 25</code> once ready</td>
      <td>Cut scheduler overhead during the one phase where it matters</td>
    </tr>
  </tbody>
</table>

<p><em>“Biggest single win.”</em> Combined with the kernel-side XMX + lookaside work, this dropped the phase-2 tab’s cold-bootstrap from ~10 seconds to <strong>subjectively instant</strong>.</p>

<h4 id="the-build-time-snapshot-infrastructure-shipped-gated-off">The build-time snapshot (infrastructure shipped, gated off)</h4>

<p>The snapshot idea — run the cold bootstrap natively at build time, embed a 64 KB memory + registers blob via <code class="language-plaintext highlighter-rouge">include_bytes!</code>, restore on load — is actually <em>implemented</em>: <code class="language-plaintext highlighter-rouge">build.rs</code> does the native bootstrap and writes <code class="language-plaintext highlighter-rouge">fif_snapshot.bin</code>, <code class="language-plaintext highlighter-rouge">src/snapshot.rs</code> parses and restores it, a <code class="language-plaintext highlighter-rouge">localStorage</code> cache keys on a content hash of <code class="language-plaintext highlighter-rouge">kernel.s</code> + <code class="language-plaintext highlighter-rouge">core/*.fth</code> so edits auto-invalidate.</p>

<p>But it’s shipped with a runtime feature flag, <code class="language-plaintext highlighter-rouge">SNAPSHOT_CACHE_ENABLED = false</code>. The reason is honest: with the pump-loop fix alone making the tab feel instant, <strong>turning on the snapshot would contaminate kernel-side perf measurements</strong>. Any future change to the hash, lookaside, or threading-layer primitives needs to be benchmarked against the slow-path boot, not the pre-warmed one. The flag flips on once the kernel-side optimization work is fully wrapped.</p>

<p>This also means the originally-planned CLI <strong>pre-load-and-dump-to-binary</strong> is now formally <strong>deferred</strong>. The rationale, recorded in <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/plan.md"><code class="language-plaintext highlighter-rouge">docs/plan.md</code></a>: it’s the biggest expected WASM win, but the same deliverable — a kernel that <em>starts</em> in the ready state, without replaying bootstrap — is exactly what phase 4 (<code class="language-plaintext highlighter-rouge">forth-from-forth/</code>) produces as its build artifact. Two paths to the same destination; doing both is wasteful. Revisit if the hash + lookaside + pump-loop stack proves insufficient once the snapshot flag is flipped on.</p>

<h4 id="the-speedups-that-shipped-stacked">The speedups that shipped, stacked</h4>

<table>
  <thead>
    <tr>
      <th>Speedup</th>
      <th>Mechanism</th>
      <th>Where it helps</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>First-char hashed FIND</td>
      <td>256-bucket table + <code class="language-plaintext highlighter-rouge">_start</code> populate</td>
      <td>Any host, marginal on CLI</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">a3a63f0</code>); CLI 0% gain</td>
    </tr>
    <tr>
      <td>Len-seeded mult33 hash</td>
      <td>Drop-in <code class="language-plaintext highlighter-rouge">compute_hash</code> subroutine</td>
      <td>Any host, marginal on CLI</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">9bd4b10</code> after revert <code class="language-plaintext highlighter-rouge">ab9817f</code>); WASM still slow</td>
    </tr>
    <tr>
      <td>2-Round 24-bit XMX hash</td>
      <td>~10 ops/char XMX avalanche</td>
      <td>WASM (cheaper bit ops) and denser dictionaries</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">fdae7dd</code>)</td>
    </tr>
    <tr>
      <td>1-entry FIND lookaside cache</td>
      <td>Memento keyed by full 24-bit hash</td>
      <td>Same-word-twice patterns in compile</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">4ea2f79</code>); +88 bytes</td>
    </tr>
    <tr>
      <td>Adaptive web pump-loop</td>
      <td>PC-aware <code class="language-plaintext highlighter-rouge">PUMP_TINY</code> / <code class="language-plaintext highlighter-rouge">PUMP_BIG</code> batches; shorter boot tick</td>
      <td>Web bootstrap; “biggest single win”</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">f757800</code>)</td>
    </tr>
    <tr>
      <td>Build-time snapshot + <code class="language-plaintext highlighter-rouge">localStorage</code> cache</td>
      <td><code class="language-plaintext highlighter-rouge">build.rs</code> + <code class="language-plaintext highlighter-rouge">snapshot.rs</code> in web crate</td>
      <td>Web, skipping cold boot entirely</td>
      <td><strong>Shipped, gated</strong> (<code class="language-plaintext highlighter-rouge">SNAPSHOT_CACHE_ENABLED=false</code>)</td>
    </tr>
    <tr>
      <td>CLI pre-load-and-dump-to-binary</td>
      <td>Native bootstrap → <code class="language-plaintext highlighter-rouge">.bin</code> → <code class="language-plaintext highlighter-rouge">cor24-run --load-state</code></td>
      <td>CLI scripts, CI</td>
      <td><strong>Deferred</strong> — phase 4 produces the same artifact</td>
    </tr>
  </tbody>
</table>

<p>Net effect: the <a href="https://sw-embed.github.io/web-sw-cor24-forth/">live phase-2 tab</a> boots as fast as the phase-1 tab now, even though it’s still doing the full “language builds itself” cold bootstrap — the snapshot fast-path isn’t even on.</p>

<h4 id="a-measurement-footnote">A measurement footnote</h4>

<p>CLI perf numbers look identical across all hash variants. <code class="language-plaintext highlighter-rouge">cor24-run</code> reports instruction timestamps quantized to 10,000 cycles; the per-FIND savings of XMX (~200 inst × 1000 lookups) and the lookaside (~30–50 inst × dozens of hits) both land below that resolution. The four-commit CLI iteration — <code class="language-plaintext highlighter-rouge">a3a63f0</code> → <code class="language-plaintext highlighter-rouge">9bd4b10</code> → <code class="language-plaintext highlighter-rouge">fdae7dd</code> → <code class="language-plaintext highlighter-rouge">4ea2f79</code> — all reports <strong>61.17M instructions</strong> for fib-demo compile. That’s not the optimizations doing nothing; it’s the measurement infrastructure not having the resolution to show it. WASM wall-clock at millisecond granularity over a multi-second boot is the authoritative metric, and there the stacked speedups are very visible.</p>

<h3 id="the-vocabulary-feels-thin--and-fills-in">The Vocabulary Feels Thin — and Fills In</h3>

<p><code class="language-plaintext highlighter-rouge">FIB</code> and the existing examples already worked on <code class="language-plaintext highlighter-rouge">forth-in-forth</code> before any of this — nothing was missing for <em>correctness</em>. What the web tab made obvious, once the phase-2 REPL sat next to standard Forth idioms in teaching material, was how much more <em>ergonomic</em> the same demos would read with a fuller vocabulary.</p>

<p>The <code class="language-plaintext highlighter-rouge">FIB</code> print loop used to look like:</p>

<pre><code class="language-forth">: FIB ... ;
: FIBS 0 BEGIN DUP FIB . 1 + DUP 21 = UNTIL DROP ;
FIBS
</code></pre>

<p>Every hand-rolled <code class="language-plaintext highlighter-rouge">BEGIN</code>/<code class="language-plaintext highlighter-rouge">UNTIL</code> counter is a small tax. In a fuller Forth the same thing reads as:</p>

<pre><code class="language-forth">: FIB ... ;
21 0 ?DO I FIB . LOOP
</code></pre>

<p>Not shorter by much — but no setup, no sentinel variable, no <code class="language-plaintext highlighter-rouge">DROP</code> at the end. Several files in <code class="language-plaintext highlighter-rouge">forth-in-forth/examples/</code> collapsed to one-liners once the vocabulary filled in.</p>

<p>The additions shipped into both the phase-2 and phase-3 kernels (scoped there — the phase 1 <code class="language-plaintext highlighter-rouge">forth.s</code> kernel stays as-is for its existing users):</p>

<table>
  <thead>
    <tr>
      <th>Group</th>
      <th>Shipped</th>
      <th>Landed in</th>
      <th>How</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Extra BEGIN-style flow</strong></td>
      <td><code class="language-plaintext highlighter-rouge">AGAIN</code>, <code class="language-plaintext highlighter-rouge">WHILE</code>, <code class="language-plaintext highlighter-rouge">REPEAT</code></td>
      <td><code class="language-plaintext highlighter-rouge">3b4f541</code></td>
      <td>Pure Forth in <code class="language-plaintext highlighter-rouge">core/minimal.fth</code>, built on <code class="language-plaintext highlighter-rouge">0BRANCH</code>/<code class="language-plaintext highlighter-rouge">BRANCH</code>. No new primitives.</td>
    </tr>
    <tr>
      <td><strong>Defining words</strong></td>
      <td><code class="language-plaintext highlighter-rouge">CONSTANT</code>, <code class="language-plaintext highlighter-rouge">VARIABLE</code></td>
      <td><code class="language-plaintext highlighter-rouge">3b4f541</code></td>
      <td>Pure Forth in <code class="language-plaintext highlighter-rouge">core/lowlevel.fth</code>, layered on <code class="language-plaintext highlighter-rouge">CREATE</code> + <code class="language-plaintext highlighter-rouge">,DOCOL</code> + <code class="language-plaintext highlighter-rouge">LIT</code>. <code class="language-plaintext highlighter-rouge">DOES&gt;</code> parked for later.</td>
    </tr>
    <tr>
      <td><strong>Counted loops</strong></td>
      <td><code class="language-plaintext highlighter-rouge">DO</code>, <code class="language-plaintext highlighter-rouge">LOOP</code>, <code class="language-plaintext highlighter-rouge">?DO</code>, <code class="language-plaintext highlighter-rouge">I</code>, <code class="language-plaintext highlighter-rouge">UNLOOP</code></td>
      <td><code class="language-plaintext highlighter-rouge">92cef7f</code></td>
      <td>New RS primitives <code class="language-plaintext highlighter-rouge">(DO)</code>, <code class="language-plaintext highlighter-rouge">(LOOP)</code>, <code class="language-plaintext highlighter-rouge">(?DO)</code>, <code class="language-plaintext highlighter-rouge">I</code>, <code class="language-plaintext highlighter-rouge">UNLOOP</code> in kernel; IMMEDIATE Forth wrappers in <code class="language-plaintext highlighter-rouge">core/lowlevel.fth</code>. Matching Forth examples <code class="language-plaintext highlighter-rouge">15-again.fth</code> through <code class="language-plaintext highlighter-rouge">19-do-loop.fth</code>.</td>
    </tr>
  </tbody>
</table>

<p>RS layout inside a <code class="language-plaintext highlighter-rouge">DO</code> loop body:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>top    [ index ]
       [ limit ]
deeper [ caller IP ]
</code></pre></div></div>

<p>Standard Forth convention — <code class="language-plaintext highlighter-rouge">UNLOOP</code> must precede an <code class="language-plaintext highlighter-rouge">EXIT</code> from inside a loop to restore the caller’s IP. The <code class="language-plaintext highlighter-rouge">(LOOP)</code> and <code class="language-plaintext highlighter-rouge">(?DO)</code> primitives stash the IP in the frame-pointer register during the compare, because this ISA’s <code class="language-plaintext highlighter-rouge">ceq</code> rejects <code class="language-plaintext highlighter-rouge">fp</code> as an operand and <code class="language-plaintext highlighter-rouge">sw</code> rejects <code class="language-plaintext highlighter-rouge">fp</code> as a source; that frees <code class="language-plaintext highlighter-rouge">r2</code> as a scratch register for the limit/index work.</p>

<p>A handful of additional conveniences (<code class="language-plaintext highlighter-rouge">+LOOP</code>, <code class="language-plaintext highlighter-rouge">J</code>, <code class="language-plaintext highlighter-rouge">LEAVE</code>, <code class="language-plaintext highlighter-rouge">DOES&gt;</code>, <code class="language-plaintext highlighter-rouge">RECURSE</code>, <code class="language-plaintext highlighter-rouge">PICK</code>, <code class="language-plaintext highlighter-rouge">ROLL</code>, <code class="language-plaintext highlighter-rouge">?DUP</code>, <code class="language-plaintext highlighter-rouge">MIN</code>/<code class="language-plaintext highlighter-rouge">MAX</code>, <code class="language-plaintext highlighter-rouge">&lt;=</code>/<code class="language-plaintext highlighter-rouge">&gt;=</code>/<code class="language-plaintext highlighter-rouge">&lt;&gt;</code>) are left for follow-up work. What’s shipped is enough for the demos the browser tab shows side-by-side with the phase-1 kernel.</p>

<p>Live demos in the web UI (<code class="language-plaintext highlighter-rouge">AGAIN</code>, <code class="language-plaintext highlighter-rouge">CONSTANT</code>, <code class="language-plaintext highlighter-rouge">DO LOOP</code>, <code class="language-plaintext highlighter-rouge">VARIABLE</code>) are already wired into both the phase-2 and phase-3 tabs, sharing one demo constant via the refactored component in <code class="language-plaintext highlighter-rouge">src/repl.rs</code>.</p>

<p>The general lesson: a language that only <em>feels</em> thin once it’s compared against a fuller one benefits from that comparison. Good that it surfaced before phase 3 cemented the primitive set.</p>

<h2 id="phase-3-forth-on-forthish--first-subsets-shipping">Phase 3: forth-on-forthish — First Subsets Shipping</h2>

<p><code class="language-plaintext highlighter-rouge">./forth-on-forthish/</code> scaffolded with a copy of phase 2’s kernel and core — the <em>current</em> phase-2 kernel with XMX hash + 1-entry lookaside carried forward (commit <code class="language-plaintext highlighter-rouge">4f5e8ab</code>), verified byte-identical to baseline on all 15 examples. Phase 3 work starts on the optimized substrate, not the pre-hash version. Then the first two subsets landed on top of it:</p>

<ul>
  <li><strong>Subset 12</strong> (<code class="language-plaintext highlighter-rouge">79f4350</code>): the <code class="language-plaintext highlighter-rouge">,DOCOL</code> primitive. Wraps the existing <code class="language-plaintext highlighter-rouge">do_colon_cfa</code> as a named dict entry, exposing the 6-byte far-CFA template emission so a Forth <code class="language-plaintext highlighter-rouge">:</code> can build headers without touching asm. First attempt at Forth <code class="language-plaintext highlighter-rouge">:</code> / <code class="language-plaintext highlighter-rouge">;</code> in a new <code class="language-plaintext highlighter-rouge">core/runtime.fth</code> also landed and was reverted — hit the classic SMUDGE-bit problem where <code class="language-plaintext highlighter-rouge">;</code> at the end of <code class="language-plaintext highlighter-rouge">: ; ... ; IMMEDIATE</code> resolves to the in-progress new <code class="language-plaintext highlighter-rouge">;</code> because <code class="language-plaintext highlighter-rouge">FIND</code> has no way to skip “being-compiled” entries. Documented three options to unblock (asm tweak to <code class="language-plaintext highlighter-rouge">:</code>/<code class="language-plaintext highlighter-rouge">;</code> that sets/clears <code class="language-plaintext highlighter-rouge">HIDDEN</code>, dedicated <code class="language-plaintext highlighter-rouge">HIDE-LATEST</code>/<code class="language-plaintext highlighter-rouge">UNHIDE-LATEST</code> primitives, or modify <code class="language-plaintext highlighter-rouge">CREATE</code> to always hide).</li>
  <li><strong>Subset 13</strong> (<code class="language-plaintext highlighter-rouge">a98b4b8</code>): Forth <code class="language-plaintext highlighter-rouge">:</code> and <code class="language-plaintext highlighter-rouge">;</code> shipping. Went with the “asm sets/clears HIDDEN inline” option — <code class="language-plaintext highlighter-rouge">colon_thread</code> now runs <code class="language-plaintext highlighter-rouge">do_hide_latest</code> between <code class="language-plaintext highlighter-rouge">do_colon_cfa</code> and <code class="language-plaintext highlighter-rouge">do_rbrac</code> (sets bit 6 on the new entry so <code class="language-plaintext highlighter-rouge">FIND</code> skips it during the rest of the definition). <code class="language-plaintext highlighter-rouge">do_semi</code> clears <code class="language-plaintext highlighter-rouge">HIDDEN</code> on <code class="language-plaintext highlighter-rouge">LATEST</code> before compiling <code class="language-plaintext highlighter-rouge">EXIT</code> and zeroing <code class="language-plaintext highlighter-rouge">STATE</code>. A new <code class="language-plaintext highlighter-rouge">core/runtime.fth</code> tier, loaded <em>first</em> (before <code class="language-plaintext highlighter-rouge">minimal.fth</code>), defines:</li>
</ul>

<pre><code class="language-forth">: : CREATE ,DOCOL LATEST @ 3 + DUP C@ 64 OR SWAP C! ] ;
: ; ['] EXIT , LATEST @ 3 + DUP C@ 191 AND SWAP C! 0 STATE ! ; IMMEDIATE
</code></pre>

<p>No <code class="language-plaintext highlighter-rouge">\</code> comments in <code class="language-plaintext highlighter-rouge">runtime.fth</code> — <code class="language-plaintext highlighter-rouge">\</code> is defined in <code class="language-plaintext highlighter-rouge">minimal.fth</code> which loads after. An initial draft that included comments parsed them as code.</p>

<p>All 15 examples/*.fth produce the same functional behavior as the first-char hash baseline; the only new UART output is two extra <code class="language-plaintext highlighter-rouge">" ok"</code> lines for the two new <code class="language-plaintext highlighter-rouge">runtime.fth</code> definitions. The phase-3 kernel now has Forth <code class="language-plaintext highlighter-rouge">:</code> and Forth <code class="language-plaintext highlighter-rouge">;</code> — every new colon definition from here on uses the Forth implementations.</p>

<p>The remaining subsets push further into the primitive set:</p>

<p>Specific moves enabled by the new substrate:</p>

<table>
  <thead>
    <tr>
      <th>Word(s)</th>
      <th>Strategy</th>
      <th>New primitive needed</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">:</code> and <code class="language-plaintext highlighter-rouge">;</code></td>
      <td><code class="language-plaintext highlighter-rouge">: : CREATE ,DOCOL ... ] ;</code> plus a tricky <code class="language-plaintext highlighter-rouge">;</code> that compiles <code class="language-plaintext highlighter-rouge">EXIT</code> and toggles <code class="language-plaintext highlighter-rouge">STATE</code>; both flip <code class="language-plaintext highlighter-rouge">HIDDEN</code> inline on <code class="language-plaintext highlighter-rouge">LATEST</code></td>
      <td><code class="language-plaintext highlighter-rouge">,DOCOL</code> + <code class="language-plaintext highlighter-rouge">HIDDEN</code>-bit handling in <code class="language-plaintext highlighter-rouge">colon_thread</code> / <code class="language-plaintext highlighter-rouge">do_semi</code></td>
      <td><strong>Shipped</strong> (subsets 12, 13)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">WORD</code></td>
      <td>Forth loop over <code class="language-plaintext highlighter-rouge">KEY</code> into a known word buffer</td>
      <td><code class="language-plaintext highlighter-rouge">WORD-BUFFER</code> (or a fixed address)</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FIND</code></td>
      <td>Walk <code class="language-plaintext highlighter-rouge">LATEST @</code> with <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">C@</code>, <code class="language-plaintext highlighter-rouge">=</code>, <code class="language-plaintext highlighter-rouge">AND</code></td>
      <td>None — uses existing primitives</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">NUMBER</code></td>
      <td>Digit-parsing on top of <code class="language-plaintext highlighter-rouge">*</code>, <code class="language-plaintext highlighter-rouge">+</code>, <code class="language-plaintext highlighter-rouge">&lt;</code>, <code class="language-plaintext highlighter-rouge">BASE @</code></td>
      <td>None</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">INTERPRET</code> / <code class="language-plaintext highlighter-rouge">QUIT</code></td>
      <td><code class="language-plaintext highlighter-rouge">BEGIN ... UNTIL</code> loops over <code class="language-plaintext highlighter-rouge">WORD</code> / <code class="language-plaintext highlighter-rouge">FIND</code> / <code class="language-plaintext highlighter-rouge">EXECUTE</code> / <code class="language-plaintext highlighter-rouge">NUMBER</code></td>
      <td>None</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">*</code>, <code class="language-plaintext highlighter-rouge">/MOD</code>, <code class="language-plaintext highlighter-rouge">-</code></td>
      <td><code class="language-plaintext highlighter-rouge">+</code>-loops or <code class="language-plaintext highlighter-rouge">NEGATE +</code></td>
      <td>None — can drop the asm versions</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">AND</code> / <code class="language-plaintext highlighter-rouge">OR</code> / <code class="language-plaintext highlighter-rouge">XOR</code></td>
      <td>Derivations from a single bit-primitive</td>
      <td><code class="language-plaintext highlighter-rouge">NAND</code> (replaces 3 primitives with 1)</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DUP</code> / <code class="language-plaintext highlighter-rouge">SWAP</code> / <code class="language-plaintext highlighter-rouge">OVER</code> / <code class="language-plaintext highlighter-rouge">&gt;R</code> / <code class="language-plaintext highlighter-rouge">R&gt;</code></td>
      <td><code class="language-plaintext highlighter-rouge">SP@</code>-based memory operations on the data stack</td>
      <td><code class="language-plaintext highlighter-rouge">SP!</code>, <code class="language-plaintext highlighter-rouge">RP@</code>, <code class="language-plaintext highlighter-rouge">RP!</code> (already have <code class="language-plaintext highlighter-rouge">SP@</code>)</td>
      <td>Planned</td>
    </tr>
  </tbody>
</table>

<p>After the refactor the irreducible asm primitives are approximately:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NEXT  DOCOL  EXIT  LIT  BRANCH  0BRANCH  EXECUTE
+  NAND  @  !  C@  C!  KEY  EMIT  SP@  RP@  SP!  RP!
LED!  SW?  HALT
</code></pre></div></div>

<p>About <strong>20 primitives</strong>, <strong>~600–800 asm lines</strong> (vs. ~2240 today). Projected progression:</p>

<table>
  <thead>
    <tr>
      <th>Approach</th>
      <th style="text-align: right">asm lines</th>
      <th style="text-align: right">Forth lines</th>
      <th style="text-align: right">asm primitives</th>
      <th>Self-hosting</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1: all-asm</td>
      <td style="text-align: right">~2983</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">~65</td>
      <td>100% asm</td>
    </tr>
    <tr>
      <td>2: today</td>
      <td style="text-align: right">2239</td>
      <td style="text-align: right">161</td>
      <td style="text-align: right">50</td>
      <td>93% asm</td>
    </tr>
    <tr>
      <td>3: forth-on-forthish</td>
      <td style="text-align: right">~700</td>
      <td style="text-align: right">~600</td>
      <td style="text-align: right">~22</td>
      <td>54% asm</td>
    </tr>
    <tr>
      <td>4: forth-from-forth</td>
      <td style="text-align: right">0 hand-written</td>
      <td style="text-align: right">~1000 Forth</td>
      <td style="text-align: right">~22 emitted</td>
      <td>0% hand-written asm</td>
    </tr>
  </tbody>
</table>

<h3 id="the-phased-plan">The Phased Plan</h3>

<p>Phase 3 breaks into subsets the same way phase 2 did:</p>

<table>
  <thead>
    <tr>
      <th>Subset</th>
      <th>Size</th>
      <th>Scope</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>12</strong></td>
      <td>small</td>
      <td>Add <code class="language-plaintext highlighter-rouge">,DOCOL</code> primitive</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">79f4350</code>)</td>
    </tr>
    <tr>
      <td><strong>13</strong></td>
      <td>medium</td>
      <td>Forth <code class="language-plaintext highlighter-rouge">:</code> and <code class="language-plaintext highlighter-rouge">;</code> via <code class="language-plaintext highlighter-rouge">core/runtime.fth</code> + inline <code class="language-plaintext highlighter-rouge">HIDDEN</code>-bit management</td>
      <td><strong>Shipped</strong> (<code class="language-plaintext highlighter-rouge">a98b4b8</code>)</td>
    </tr>
    <tr>
      <td><strong>14</strong></td>
      <td>medium</td>
      <td>Add <code class="language-plaintext highlighter-rouge">SP!</code>/<code class="language-plaintext highlighter-rouge">RP@</code>/<code class="language-plaintext highlighter-rouge">RP!</code>; move <code class="language-plaintext highlighter-rouge">DUP</code>/<code class="language-plaintext highlighter-rouge">SWAP</code>/<code class="language-plaintext highlighter-rouge">OVER</code>/<code class="language-plaintext highlighter-rouge">&gt;R</code>/<code class="language-plaintext highlighter-rouge">R&gt;</code> to Forth</td>
      <td>Next</td>
    </tr>
    <tr>
      <td><strong>15</strong></td>
      <td>medium</td>
      <td>Move <code class="language-plaintext highlighter-rouge">*</code>/<code class="language-plaintext highlighter-rouge">/MOD</code>/<code class="language-plaintext highlighter-rouge">-</code> to Forth as loops; <code class="language-plaintext highlighter-rouge">AND</code>/<code class="language-plaintext highlighter-rouge">OR</code>/<code class="language-plaintext highlighter-rouge">XOR</code> from a new <code class="language-plaintext highlighter-rouge">NAND</code> primitive</td>
      <td>Planned</td>
    </tr>
    <tr>
      <td><strong>16</strong></td>
      <td>large</td>
      <td>Move <code class="language-plaintext highlighter-rouge">WORD</code>/<code class="language-plaintext highlighter-rouge">FIND</code>/<code class="language-plaintext highlighter-rouge">NUMBER</code>/<code class="language-plaintext highlighter-rouge">INTERPRET</code>/<code class="language-plaintext highlighter-rouge">QUIT</code> to Forth — after this, kernel matches approach 3 (~700 asm lines)</td>
      <td>Planned</td>
    </tr>
  </tbody>
</table>

<p>Subset 16 is the scary one. The outer interpreter written in Forth is <em>slow</em> — every text token goes through Forth-coded dictionary walking instead of asm. Estimates: ~10× slower text-input path, but compiled colon definitions run at nearly the same speed.</p>

<h3 id="known-tradeoffs">Known Tradeoffs</h3>

<p>Phase 3 isn’t free. The comparison from phase 2 to phase 3:</p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>Phase 2 (today)</th>
      <th>Phase 3 (target)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Asm lines to maintain</td>
      <td>2239</td>
      <td>~700</td>
    </tr>
    <tr>
      <td>Asm primitive count</td>
      <td>50</td>
      <td>~22</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">WORD</code>/<code class="language-plaintext highlighter-rouge">FIND</code> speed</td>
      <td>asm (fast)</td>
      <td>Forth (~10× slower)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">:</code> and <code class="language-plaintext highlighter-rouge">;</code> speed</td>
      <td>asm</td>
      <td>Forth (slightly slower compile)</td>
    </tr>
    <tr>
      <td>Bootstrap complexity</td>
      <td>Low</td>
      <td>Higher — careful <code class="language-plaintext highlighter-rouge">.fth</code> load ordering required</td>
    </tr>
    <tr>
      <td>Retargeting effort</td>
      <td>Rewrite ~2240 lines of asm</td>
      <td>Rewrite ~700 lines of primitives + rebuild</td>
    </tr>
  </tbody>
</table>

<p>The payoff is dramatic: the kernel becomes easy to retarget to a different ISA, the <em>language</em> story becomes much cleaner (Forth doing Forth’s job), and phase 4 becomes tractable because the primitive set is already small and orthogonal.</p>

<h2 id="phase-4-forth-from-forth--on-the-horizon">Phase 4: forth-from-forth — On the Horizon</h2>

<p><strong>[UPDATE:</strong> follow-on discussion in <a href="/2026/04/23/rabbit-hole-deep-dive-forth-find-cost-of-a-name/">Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name</a><strong>]</strong></p>

<p><code class="language-plaintext highlighter-rouge">./forth-from-forth/</code>. Write a Forth-to-COR24-asm compiler <em>in Forth</em>. Run it on a host Forth (either a separate Forth, or phase 3’s kernel) to emit <code class="language-plaintext highlighter-rouge">kernel.s</code>. After bootstrap, no hand-written <code class="language-plaintext highlighter-rouge">.s</code> exists; <code class="language-plaintext highlighter-rouge">kernel.s</code> is a build artifact.</p>

<p>The cross-compiler has three pieces:</p>

<ul>
  <li><strong>Instruction encoder</strong>: each COR24 opcode → bytes.</li>
  <li><strong>Primitive registry</strong>: each Forth primitive defined as a small Forth word that emits the asm body. E.g. <code class="language-plaintext highlighter-rouge">: prim-+ asm-pop-r2 asm-pop-r0 asm-add-r0-r2 asm-push-r0 asm-next ;</code>.</li>
  <li><strong>Linker</strong>: lays out the dict chain and writes the final <code class="language-plaintext highlighter-rouge">.s</code>.</li>
</ul>

<p>This is the standard pattern behind <a href="https://en.wikipedia.org/wiki/EForth">eForth</a>, <a href="http://git.annexia.org/?p=jonesforth.git">JonesForth</a>, and several ITSY-style projects. Roughly 500–1000 lines of cross-compiler Forth plus a runtime specification.</p>

<p>At that point the kernel’s <code class="language-plaintext highlighter-rouge">.s</code> becomes a build artifact, not source. Retargeting to a different ISA means swapping the instruction encoder module. The self-hosting story goes from <em>“Forth is written in asm, except for the words that aren’t”</em> to <em>“Forth is written in Forth, including the compiler that produces the kernel.”</em></p>

<p>Estimated work from phase 3 to phase 4: ~2–3 weeks. Risk: medium — instruction-encoding bugs are silent.</p>

<h2 id="why-ship-the-phases-as-separate-directories">Why Ship the Phases As Separate Directories?</h2>

<p>Each phase is a snapshot. <code class="language-plaintext highlighter-rouge">./</code> is the canonical reference (stays untouched). <code class="language-plaintext highlighter-rouge">./forth-in-forth/</code> is today. <code class="language-plaintext highlighter-rouge">./forth-on-forthish/</code> is where work happens next. <code class="language-plaintext highlighter-rouge">./forth-from-forth/</code> is future. Keeping them as sibling directories means:</p>

<ul>
  <li>Regression tests for the original kernel keep passing against <code class="language-plaintext highlighter-rouge">./</code>.</li>
  <li>The web frontend can keep pointing at <code class="language-plaintext highlighter-rouge">./</code> while the next phase stabilizes.</li>
  <li>Each phase documents its own subset ordering and status (e.g., <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/forth-in-forth/docs/status.md"><code class="language-plaintext highlighter-rouge">forth-in-forth/docs/status.md</code></a>).</li>
  <li>The comparison tables from phase to phase stay honest — you can diff the asm line counts, binary sizes, and primitive tables directly.</li>
</ul>

<p>It also matches the <a href="https://github.com/sw-embed/web-sw-cor24-demos/blob/main/docs/language-building-tech.md">language-building pattern</a> used across other COR24 languages: build a reference, keep it, and iterate new variants beside it.</p>

<h2 id="vibe-coding-the-migration">Vibe Coding the Migration</h2>

<p>Every subset in phase 2 was a short conversation: <em>“Here’s the current kernel; move <code class="language-plaintext highlighter-rouge">=</code> and <code class="language-plaintext highlighter-rouge">0=</code> to Forth, deriving them from <code class="language-plaintext highlighter-rouge">XOR</code>. Add a <code class="language-plaintext highlighter-rouge">minimal.fth</code> line and a test.”</em> An AI agent proposed the edits, I reviewed and ran the regression harness, and the subset shipped as one commit. Eleven subsets in a day. That pace is only possible because each move is small, each test is fast, and the kernel stays buildable at every step.</p>

<p>The reward for that discipline is visible in the commits: every subset is a single logical change, every status.md update is a diff, and <code class="language-plaintext highlighter-rouge">SEE FIB</code> on the REPL reads back the Forth definition the AI agent wrote an hour earlier. Forth’s self-extending nature and vibe coding’s tight loop fit each other well — the language is already expected to grow incrementally, and the agent’s output is exactly one <code class="language-plaintext highlighter-rouge">.fth</code> addition at a time.</p>

<h2 id="what-to-watch-next">What to Watch Next</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">forth-on-forthish/</code> subset 14 — stack-pointer primitives (<code class="language-plaintext highlighter-rouge">SP!</code>, <code class="language-plaintext highlighter-rouge">RP@</code>, <code class="language-plaintext highlighter-rouge">RP!</code>) and moving <code class="language-plaintext highlighter-rouge">DUP</code>/<code class="language-plaintext highlighter-rouge">SWAP</code>/<code class="language-plaintext highlighter-rouge">OVER</code>/<code class="language-plaintext highlighter-rouge">&gt;R</code>/<code class="language-plaintext highlighter-rouge">R&gt;</code> into Forth on top of <code class="language-plaintext highlighter-rouge">SP@</code>.</li>
  <li>The first <em>visible</em> win in phase 3: <code class="language-plaintext highlighter-rouge">kernel.s</code> drops below 2000 lines. Likely around subset 15.</li>
  <li>Subset 16 — the big one. <code class="language-plaintext highlighter-rouge">WORD</code>/<code class="language-plaintext highlighter-rouge">FIND</code>/<code class="language-plaintext highlighter-rouge">NUMBER</code> in Forth; asm line count drops by hundreds.</li>
  <li>Eventually, <code class="language-plaintext highlighter-rouge">./forth-from-forth/</code> gets scaffolded, and the question becomes which Forth hosts the first cross-compile run.</li>
</ul>

<h2 id="hashing-references">Hashing References</h2>

<p>In-repo docs for the attempt sequence: <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/hashing-analysis.md"><code class="language-plaintext highlighter-rouge">docs/hashing-analysis.md</code></a> (measurement-driven comparison of 9 hash functions) and <a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/hashing.txt"><code class="language-plaintext highlighter-rouge">docs/hashing.txt</code></a> (Gemini-assisted survey of classical through 2025–2026 techniques).</p>

<p>Key external references:</p>

<table>
  <thead>
    <tr>
      <th>Topic</th>
      <th>Link</th>
      <th>Why it matters here</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Krapivin optimal open addressing (2025)</strong></td>
      <td><a href="https://www.quantamagazine.org/undergraduate-upends-a-40-year-old-data-science-conjecture-20250210/">Quanta Magazine</a> · <a href="https://arxiv.org/abs/2501.02305">arXiv:2501.02305</a></td>
      <td>New probe sequence keeps hash tables near-constant-time to 99% fill. Directly informs the secondary-probe formula in the attempt-3 design.</td>
    </tr>
    <tr>
      <td><strong>Perfect hash functions</strong></td>
      <td><a href="https://en.wikipedia.org/wiki/Perfect_hash_function">Wikipedia</a> · <a href="https://cmph.sourceforge.net/">CMPH library</a> · <a href="https://www.gnu.org/software/gperf/">GNU gperf</a></td>
      <td>Build-time generator for zero-collision lookup over the static kernel vocabulary (~90 words).</td>
    </tr>
    <tr>
      <td><strong>Learned index structures (2018)</strong></td>
      <td><a href="https://arxiv.org/abs/1712.01208">Kraska et al., “The Case for Learned Index Structures”</a></td>
      <td>Foundational paper on replacing static hash functions with data-aware models. Inspires the “one hash for the known set, another for user defs” split.</td>
    </tr>
    <tr>
      <td><strong>SSHash (order-preserving short-string hashing)</strong></td>
      <td><a href="https://github.com/jermp/sshash">jermp/sshash</a></td>
      <td>Cache-local hashing for short strings — Forth word names are the same shape as bioinformatics k-mers.</td>
    </tr>
    <tr>
      <td><strong>xxHash / XXH3</strong></td>
      <td><a href="https://xxhash.com/">xxhash.com</a> · <a href="https://github.com/Cyan4973/xxHash">Cyan4973/xxHash</a></td>
      <td>Current speed gold standard for non-cryptographic hashing. Benchmark baseline even when we can’t use it directly (too many registers for a 24-bit GPR-limited ISA).</td>
    </tr>
    <tr>
      <td><strong>FNV-1a</strong></td>
      <td><a href="https://en.wikipedia.org/wiki/Fowler%E2%80%93Noll%E2%80%93Vo_hash_function">Fowler/Noll/Vo hash — Wikipedia</a></td>
      <td>Classic short-string hash; one of the attempt-2 candidates, tied for second at 17 collisions.</td>
    </tr>
    <tr>
      <td><strong>djb2 hash</strong></td>
      <td><a href="http://cr.yp.to/cdb/cdb.txt">Dan Bernstein cdb docs</a> · <a href="http://www.cse.yorku.ca/~oz/hash.html">hash discussion</a></td>
      <td>Another attempt-2 candidate; <code class="language-plaintext highlighter-rouge">h = h*33 ^ c</code>. Inspired the <code class="language-plaintext highlighter-rouge">len-seeded mult33</code> winner.</td>
    </tr>
    <tr>
      <td><strong>PJW / ELF hash</strong></td>
      <td><a href="https://en.wikipedia.org/wiki/PJW_hash_function">PJW hash — Wikipedia</a></td>
      <td>Historical precedent for shift-based rolling hashes used in compilers and linkers.</td>
    </tr>
    <tr>
      <td><strong>JonesForth</strong></td>
      <td><a href="http://git.annexia.org/?p=jonesforth.git">git.annexia.org/jonesforth</a></td>
      <td>Reference Forth implementation covering dictionary layout tradeoffs.</td>
    </tr>
  </tbody>
</table>

<h2 id="resources">Resources</h2>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>GitHub</th>
      <th>Live Demo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Forth Interpreter (CLI)</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth">sw-embed/sw-cor24-forth</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>Web Demo</strong> (browser UI for the interpreter above)</td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-forth">sw-embed/web-sw-cor24-forth</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-forth/">COR24 Forth</a></td>
    </tr>
    <tr>
      <td><strong>Issue: hashed dictionary</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth/issues/1">#1</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>Issue: DO/LOOP etc.</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth/issues/2">#2</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>Approach Comparison Doc</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/docs/future.md">docs/future.md</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>Phase 2 Status</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-forth/blob/main/forth-in-forth/docs/status.md">forth-in-forth/docs/status.md</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>COR24 Assembler</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-assembler">sw-embed/sw-cor24-assembler</a></td>
      <td>—</td>
    </tr>
    <tr>
      <td><strong>COR24 Demo Hub</strong></td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub</a></td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>Forth sketches itself the way Escher’s hands do — each version a clean line drawing, each one pointing at the next.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="embedded" /><category term="programming-languages" /><category term="deep-dive" /><category term="embedded" /><category term="forth" /><category term="self-hosting" /><category term="cor24" /><category term="assembler" /><category term="kernel" /><category term="bootstrapping" /><category term="vibe-coding" /><category term="language-building" /><category term="threaded-code" /><summary type="html"><![CDATA[How much of a Forth kernel can be written in Forth instead of assembly? The question has an obvious answer (“as much as possible”) and a less obvious answer (“it depends on which phase of the bootstrap you’re in”). This post walks through four points along that spectrum for the COR24 Forth kernel: two phases shipped, a third in progress with its first subsets landing, and a fourth on the horizon. It’s a deep dive — every movement of a word from .s to .fth changes the bootstrap ordering, the primitive set, and what the next movement looks like. Forth is an unusually good language for showing its own self-extending nature, and reading the phases in sequence reads like one of Escher’s drawings: each hand sketching the other. Why this matters — Self-hosting is the final test that a language is expressive enough for systems work. Moving Forth words from assembly into Forth itself shows exactly where the irreducible floor is: the primitives that must be machine code. Everything above that floor can, in principle, live in .fth source. Resource Link Play in Browser COR24 Forth Demo — three tabs: forth.s (phase 1), forth-in-forth (phase 2, default), forth-on-forthish (phase 3 in progress) Forth Interpreter (CLI) sw-embed/sw-cor24-forth Web Demo sw-embed/web-sw-cor24-forth Approach Comparison docs/future.md Phase 2 Status forth-in-forth/docs/status.md Closed Issues #1 hashed dictionary · #2 DO/LOOP &amp; friends Prior Post Embedded (2/?): COR24 Assembly Emulator Follow-on post Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name Comments Discord The Four Approaches A single axis: what fraction of the kernel is hand-written assembly, and what fraction is Forth? Four labeled points along it: # Name Directory Where the kernel comes from 1 All-asm kernel ./ (repo root) Hand-written .s 2 Tiered Forth on a slimmed kernel ./forth-in-forth/ Hand-written .s + hand-written .fth 3 Minimal-primitive kernel ./forth-on-forthish/ Smaller .s (a Forth-ish primitive substrate) + larger .fth 4 Self-hosted via cross-compiler ./forth-from-forth/ Hand-written Forth compiler emits the .s The preposition family — in / on-ish / from — signals what the kernel is to the Forth code on top of it: In approach 2, Forth runs in a slimmed asm host. In approach 3, the substrate is so reduced it’s barely asm any more — Forth runs on something that’s already Forth-ish. In approach 4, the kernel itself comes from Forth (Forth source emits the .s). Phase 1: All-Asm Kernel — Where We Started forth.s as a single self-contained file. Every word is assembly, including IF/THEN, ., WORDS, .S, \, (, and so on. About 3000 lines of asm, 3879 bytes assembled. Still the canonical kernel for the web frontend and the existing reg-rs regression tests. This was the right starting point. A single-file kernel is easy to debug, easy to load, and explicit about every mechanism. The cost: it doesn’t show Forth’s most characteristic feature — self-extension — because everything is already defined in asm. There’s no moment where Forth makes itself bigger. Phase 2: forth-in-forth — Shipped Today forth-in-forth/kernel.s plus four tiered .fth files: core/minimal.fth, lowlevel.fth, midlevel.fth, highlevel.fth. The kernel keeps only what must be asm — the threading layer, ALU primitives, hardware I/O, the dict-text triplet (WORD/FIND/NUMBER), the outer loop (INTERPRET/QUIT), and :/;. Everything else moved to .fth. The migration happened in 11 subsets, each a single commit: Subset Description Commit 1 Baseline fib example, demo, reg-rs test 86edf74 2 Scaffold forth-in-forth/ directory 94e76b2 3 Move IF/THEN/ELSE/BEGIN/UNTIL to Forth 686c65f 4 Move \ and ( to Forth (add EOL! primitive) 71e1627 5 Stack &amp; arith helpers in core/lowlevel.fth 7d0037c 6 = and 0= via XOR ce57489 7 CR SPACE HEX DECIMAL to Forth 06a8dca 8 . to Forth (hide asm .) 12de5b1 9 DEPTH / .S to Forth (add SP@ primitive) d65ae26 10 WORDS VER SEE to Forth (add ', &gt;NAME) c908615 11 repl.sh and see-demo.sh 8c9104a The net movement was 18 words out of asm, 3 new asm primitives in, and 19 brand-new Forth words added on top: Category Words Moved asm → Forth (18) IF, THEN, ELSE, BEGIN, UNTIL, \, (, =, 0=, CR, SPACE, HEX, DECIMAL, ., DEPTH, .S, WORDS, VER New asm primitives (3) ['] (needed for Forth IF/THEN to compile BRANCH/0BRANCH at compile time), EOL! (needed for Forth \ to end the input line), SP@ (needed for Forth DEPTH/.S to inspect the stack pointer) New Forth words (19) NIP, TUCK, ROT, -ROT, 2DUP, 2DROP, 2SWAP, 2OVER, 1+, 1-, NEGATE, ABS, /, MOD, 0&lt; (lowlevel); ', PRINT-NAME, &gt;NAME, SEE (highlevel) The headline numbers after phase 2: Category Before After Δ asm dictionary entries 65 50 −15 asm lines (kernel.s) 2852 2239 −613 (−22%) Assembled binary bytes 3879 2786 −1093 (−28%) Forth colon defs (core/*.fth) 0 37 +37 Total vocabulary visible at REPL 62 86 +24 Forth words, by tier: Tier Count Words minimal.fth 9 BEGIN UNTIL IF THEN ELSE 0= = ( \ lowlevel.fth 15 NIP TUCK ROT -ROT 2DUP 2DROP 2SWAP 2OVER 0&lt; 1+ 1- NEGATE ABS / MOD midlevel.fth 5 CR SPACE HEX DECIMAL . highlevel.fth 8 DEPTH .S ' PRINT-NAME WORDS VER &gt;NAME SEE SEE SQUARE now prints DUP * ;. SEE CUBE prints DUP SQUARE * ;. The machinery for decompiling a colon definition lives in Forth, because SEE itself is Forth. That’s the self-extending story the all-asm kernel couldn’t tell. Why Phase 2 Stopped Where It Did Three categories of word resist moving to Forth, and together they explain the ~50 asm primitives left: Threading-layer primitives are below Forth’s level. NEXT, DOCOL, EXIT, LIT, BRANCH, 0BRANCH, EXECUTE define how threaded code runs. They can’t themselves be threaded code — the CPU has to jump to them. Some primitives need hardware/ALU/memory access. +, @, !, KEY, EMIT, LED!, SW? ultimately compile to native instructions. Forth can wrap them, but something has to execute the actual add, lw, sw, or memory-mapped UART access. Bootstrap-phase primitives need to exist before any .fth source loads. WORD, FIND, NUMBER, :, ;, INTERPRET, QUIT are all used by the outer interpreter that reads .fth source. They could be Forth in principle — but only if a smaller bootstrap interpreter runs first. Phase 2 sidesteps the recursion by keeping them in asm. Phase 3 doesn’t dodge category 3. It attacks it head-on. What the Web Port Taught Us Building the phase 2 tab in web-sw-cor24-forth — alongside the phase 1 forth.s tab, and now joined by a phase 3 forth-on-forthish tab — surfaced two categories of learning: performance and vocabulary. The performance thread spans three hash functions, a 1-entry lookaside cache, an adaptive web pump-loop, and a build-time bootstrap snapshot (infrastructure shipped but gated off for measurement cleanliness). The vocabulary thread surfaced when the phase-2 tab’s Forth sat side-by-side with standard Forth idioms in teaching material. Both threads shipped fixes, some explicit deferrals, and one feature-flagged fast-path that stays off until the kernel-side work finishes. Making It Fast, Part 1: Hashing FIND — Three Attempts The obvious suspect for slow bootstrap was FIND: a linear O(N) walk of the LATEST link chain, called for every token in every .fth line. At 90 dictionary entries (50 asm + 40 Forth colon defs), the constant factor should add up. That hypothesis drove three successive attempts, documented in detail in docs/hashing-analysis.md. A glance at the first-letter distribution explains why the first attempt was in trouble: First char Words (count) S SWAP STATE SW? SP@ SEE-CFA SEE SPACE (7) E EMIT EXIT EXECUTE EOL! ELSE (5) D DROP DUP DEPTH DUMP-ALL DECIMAL (5) C C@ C! C, CREATE CR (5) B BRANCH BASE BYE BEGIN (4) 2 2DUP 2DROP 2SWAP 2OVER (4) Only ~43 distinct first-letter classes across 90 words. Any scheme that hashes on first char alone saturates long before 256 buckets. Attempt 1 — First-char buckets (shipped) A 256-bucket first-character table (tracked in sw-cor24-forth#1, commit a3a63f0). Populated at _start by walking LATEST newest-first, maintained by do_create on every new header, with linear fallback on bucket miss. Correctness held — all reg-rs tests pass, SEE, DUMP-ALL, every example produced identical UART output. The measurement was humbling: CLI speedup on fib-demo compile: ~0% within timestamp resolution. cor24-run reports instruction timestamps rounded to 10K. Last UART TX for fib complete: 61.17M inst with hash vs 61.17M inst without. Profiling showed why. FIND is only ~0.3% of compile time. The other 99.7% splits between KEY’s UART busy-wait (spinning while cor24-run delivers the next input byte) and the threaded-code overhead of Forth-defined IMMEDIATE words (IF, BEGIN, UNTIL, \, (). Shrinking FIND from ~250 inst to ~50 inst per lookup saves ~200K inst, which disappears into the 61M total. Still, WASM might behave differently. And with EMIT/EXIT, OVER/OR, and similar first-letter twins all sharing buckets, the fallback was doing more work than it should have been. Time for a better hash. Attempt 2 — len-seeded mult33 (shipped, with a detour) An offline collision analysis ran nine hash functions against all 90 known dictionary words, at bucket sizes 64/128/256/512. Full data lives in docs/hashing-analysis.md; the summary: Hash function 64 128 256 512 first_char 47 47 47 47 len + first + last 47 34 34 34 len*31 + first + last 42 28 17 — djb2 44 29 17 — mult33 (no seed) 44 31 21 — fnv1a 44 28 17 — len-seeded mult33 34 25 11 9 2-Round XMX — 23 15 8 Len-seeded mult33 (h = length; for c in name: h = h*33 + c) won at 256 buckets with 11 collisions — a 35% improvement over the closest classical competitor. The length seed perturbs the initial state so short words spread out early in the iteration. The rollout itself was instructive. Commit 485f36f landed mult33 without the full example-suite check and broke the web agent. Commit ab9817f reverted. Commit 9bd4b10 re-landed it properly — all 15 examples byte-identical to the first-char version on CLI, then tested on WASM. WASM verdict: works, but wall-clock still not fast enough. A better hash doesn’t rescue a cold-boot that spends the majority of its time not in FIND at all. That measurement effectively closed out hashing as a standalone fix. If bootstrap speed mattered on WASM — and it did, because the “forth-in-forth” tab felt visibly slower than the forth.s tab — something more fundamental than a hash swap was needed. The “Build-Time Bootstrap Dump” section below describes that answer. Attempt 3 is still worth running, though, for reasons specific to this ISA. Attempt 3 — 2-Round 24-bit XMX (shipped) The updated docs/hashing.txt design notes — a Gemini-assisted survey of 2025–2026 hashing research — surface three recent developments that change the tradeoffs: Krapivin’s optimal open addressing (2025). Probe sequence keeps lookups near-constant-time even at 99% table occupancy. Probe 2 becomes (index + (hash &gt;&gt; 12) + 1) &amp; mask instead of +1 — a tiny asm change that avoids the clustering cliff classical linear probing hits when tables fill. Learned / data-aware hashing. For a static Forth core with a known vocabulary at build time, a perfect-hash-function generator can emit a hash with zero collisions on the core dictionary, lookup collapsing to a single multiply-shift. SSHash cache-locality hashing (2024–2026). Order-preserving hashing for short strings (Forth word names are shaped like bioinformatics k-mers). Keeps related words physically close in RAM so the CPU prefetcher stays effective. For COR24’s constraints — 24-bit words, ~8 GPRs, sometimes no hardware multiplier — the pick was 2-Round 24-bit XMX (Xor, Multiply, Xor), which shipped in commit fdae7dd: \ R0 = running hash (24 bits, native word size) \ R1 = next character (or temp during avalanche) \ R2 = MAGIC = 0xDEADB5, loaded once \ Per character: XOR \ R0 ^= R1 (mix char into hash) 24_BIT_MUL \ R0 *= R2 (native 24-bit truncation) DUP 12 RSHIFT \ R1 = R0 &gt;&gt; 12 XOR \ R0 ^= R1 (spread high bits into low bits) Two registers, no overflow waste (every bit of the 24-bit GPR carries signal), and the h ^ (h &gt;&gt; 12) avalanche step is the most bit-distributing operation tested. In the collision analysis XMX tied mult33’s worst-bucket depth at 256 (3) and beat it at 512 (2 vs 3). Per-character cost: ~10 COR24 ops vs ~4 for mult33 (roughly 2.5× slower per char), but for typical 4-character word names that’s ~24K extra instructions across a full bootstrap — noise against 61M. All 15 example files and scripts/see-demo.sh produce byte-identical UART output vs the first_char baseline. Verified correctness, shipped, moved on. Making It Fast, Part 2: A 1-Entry Lookaside Cache A better hash function still does compute_hash → bucket probe → name compare for every token. Most colon-def bodies repeat words back-to-back (DUP DUP, DROP DROP, a word used twice in the same definition). Why recompute? Commit 4ea2f79 added a 1-entry lookaside cache (classic memento pattern). After every successful FIND, the kernel stashes a single triple — (full 24-bit XMX hash, CFA, flag) — in fixed memory. The next FIND that produces the same full hash skips the bucket probe and the name compare entirely, pushes the cached (cfa, flag), and returns. Property Choice Why Cache size 1 entry Simplest possible memento; covers the “same word twice” pattern which is the common case. Cache key Full 24-bit XMX hash (not just the 8-bit bucket index) 24-bit keyspace is effectively collision-free across a 90-word dict. False positives (returning the wrong CFA on a spurious hit) are astronomically unlikely. Cache update In find_push_flag just before the NEXT jump Reads flag + CFA off the data stack via mov fp, sp; lw rX, off(fp) without disturbing DS. Three sws to store flag/cfa/hash. Cache NOT-FOUND? No Would cause incorrect stale hits when the user later defines the previously-failed word. Only successful lookups are cached. Invalidation Implicit — cfa=0 slot treated as empty; overwritten on next successful FIND Simple and correct; a user-defined FORGET that removes the cached word would need to clear the slot, but that isn’t currently implemented. Binary size went from 3893 → 3981 bytes (+88 bytes of asm). All 15 example files and scripts/see-demo.sh remained byte-identical. CLI measurement once again showed no improvement — cor24-run timestamps quantize to 10,000 cycles, and the per-FIND savings (~30–50 inst per cache hit, ~15–25K across ~1000 lookups) are below that resolution. But this is a measurement-infrastructure limitation, not evidence the cache does nothing: WASM wall-clock has millisecond resolution over a multi-second bootstrap, and that’s where the cumulative savings of hash + lookaside become visible. Implementation history Commit Hash Notes a3a63f0 first_char First hash landed. 47 collisions. Poor distribution but correct. 485f36f len-seeded mult33 First try at a better hash. Pushed without full test suite; web agent reported broken. ab9817f (revert) Reverted to first_char after bug report. 9bd4b10 len-seeded mult33 Re-landed after all 15 examples went byte-identical. WASM-tested: works, still not fast enough. fdae7dd 2-Round XMX Per hashing.txt recommendation for 24-bit GPR ISAs. Shipped. 4ea2f79 XMX + 1-entry lookaside Memento-pattern cache on top of XMX; +88 bytes. Shipped. Making It Fast, Part 3: The Web Tab Goes Snappy With the kernel-side hash + lookaside work landing, the web side had its own journey. The web agent tried two approaches in parallel — one shipped disabled, the other turned out to be the real winner. The adaptive pump-loop (shipped, the actual fix) web-sw-cor24-forth/src/repl.rs runs the emulator in batches between UART byte feeds. The old scheme was a fixed 20k instructions per byte — but for cheap-byte cases (where a single input byte triggers maybe 500 instructions of compile work before the next KEY poll), that meant burning ~19,500 cycles spinning in key_poll waiting for the next byte that the scheduler hadn’t delivered yet. Commit f757800 reworked the pump to inspect the CPU’s PC each iteration and adapt: Knob Old New Why Sub-batch size Fixed 20k inst PUMP_TINY = 2k when PC is at a key_poll with bytes to feed; PUMP_BIG = 50k elsewhere Stop wasting cycles spinning in key_poll on cheap bytes; let real compile work run longer when it has actual work to do Tick batch BOOTSTRAP_BATCH = 500k BOOTSTRAP_BATCH = 600k per tick Small bump; more work per scheduler wake Tick interval TICK_MS = 25 everywhere TICK_MS_BOOT = 5 during bootstrap; TICK_MS_INTERACTIVE = 25 once ready Cut scheduler overhead during the one phase where it matters “Biggest single win.” Combined with the kernel-side XMX + lookaside work, this dropped the phase-2 tab’s cold-bootstrap from ~10 seconds to subjectively instant. The build-time snapshot (infrastructure shipped, gated off) The snapshot idea — run the cold bootstrap natively at build time, embed a 64 KB memory + registers blob via include_bytes!, restore on load — is actually implemented: build.rs does the native bootstrap and writes fif_snapshot.bin, src/snapshot.rs parses and restores it, a localStorage cache keys on a content hash of kernel.s + core/*.fth so edits auto-invalidate. But it’s shipped with a runtime feature flag, SNAPSHOT_CACHE_ENABLED = false. The reason is honest: with the pump-loop fix alone making the tab feel instant, turning on the snapshot would contaminate kernel-side perf measurements. Any future change to the hash, lookaside, or threading-layer primitives needs to be benchmarked against the slow-path boot, not the pre-warmed one. The flag flips on once the kernel-side optimization work is fully wrapped. This also means the originally-planned CLI pre-load-and-dump-to-binary is now formally deferred. The rationale, recorded in docs/plan.md: it’s the biggest expected WASM win, but the same deliverable — a kernel that starts in the ready state, without replaying bootstrap — is exactly what phase 4 (forth-from-forth/) produces as its build artifact. Two paths to the same destination; doing both is wasteful. Revisit if the hash + lookaside + pump-loop stack proves insufficient once the snapshot flag is flipped on. The speedups that shipped, stacked Speedup Mechanism Where it helps Status First-char hashed FIND 256-bucket table + _start populate Any host, marginal on CLI Shipped (a3a63f0); CLI 0% gain Len-seeded mult33 hash Drop-in compute_hash subroutine Any host, marginal on CLI Shipped (9bd4b10 after revert ab9817f); WASM still slow 2-Round 24-bit XMX hash ~10 ops/char XMX avalanche WASM (cheaper bit ops) and denser dictionaries Shipped (fdae7dd) 1-entry FIND lookaside cache Memento keyed by full 24-bit hash Same-word-twice patterns in compile Shipped (4ea2f79); +88 bytes Adaptive web pump-loop PC-aware PUMP_TINY / PUMP_BIG batches; shorter boot tick Web bootstrap; “biggest single win” Shipped (f757800) Build-time snapshot + localStorage cache build.rs + snapshot.rs in web crate Web, skipping cold boot entirely Shipped, gated (SNAPSHOT_CACHE_ENABLED=false) CLI pre-load-and-dump-to-binary Native bootstrap → .bin → cor24-run --load-state CLI scripts, CI Deferred — phase 4 produces the same artifact Net effect: the live phase-2 tab boots as fast as the phase-1 tab now, even though it’s still doing the full “language builds itself” cold bootstrap — the snapshot fast-path isn’t even on. A measurement footnote CLI perf numbers look identical across all hash variants. cor24-run reports instruction timestamps quantized to 10,000 cycles; the per-FIND savings of XMX (~200 inst × 1000 lookups) and the lookaside (~30–50 inst × dozens of hits) both land below that resolution. The four-commit CLI iteration — a3a63f0 → 9bd4b10 → fdae7dd → 4ea2f79 — all reports 61.17M instructions for fib-demo compile. That’s not the optimizations doing nothing; it’s the measurement infrastructure not having the resolution to show it. WASM wall-clock at millisecond granularity over a multi-second boot is the authoritative metric, and there the stacked speedups are very visible. The Vocabulary Feels Thin — and Fills In FIB and the existing examples already worked on forth-in-forth before any of this — nothing was missing for correctness. What the web tab made obvious, once the phase-2 REPL sat next to standard Forth idioms in teaching material, was how much more ergonomic the same demos would read with a fuller vocabulary. The FIB print loop used to look like: : FIB ... ; : FIBS 0 BEGIN DUP FIB . 1 + DUP 21 = UNTIL DROP ; FIBS Every hand-rolled BEGIN/UNTIL counter is a small tax. In a fuller Forth the same thing reads as: : FIB ... ; 21 0 ?DO I FIB . LOOP Not shorter by much — but no setup, no sentinel variable, no DROP at the end. Several files in forth-in-forth/examples/ collapsed to one-liners once the vocabulary filled in. The additions shipped into both the phase-2 and phase-3 kernels (scoped there — the phase 1 forth.s kernel stays as-is for its existing users): Group Shipped Landed in How Extra BEGIN-style flow AGAIN, WHILE, REPEAT 3b4f541 Pure Forth in core/minimal.fth, built on 0BRANCH/BRANCH. No new primitives. Defining words CONSTANT, VARIABLE 3b4f541 Pure Forth in core/lowlevel.fth, layered on CREATE + ,DOCOL + LIT. DOES&gt; parked for later. Counted loops DO, LOOP, ?DO, I, UNLOOP 92cef7f New RS primitives (DO), (LOOP), (?DO), I, UNLOOP in kernel; IMMEDIATE Forth wrappers in core/lowlevel.fth. Matching Forth examples 15-again.fth through 19-do-loop.fth. RS layout inside a DO loop body: top [ index ] [ limit ] deeper [ caller IP ] Standard Forth convention — UNLOOP must precede an EXIT from inside a loop to restore the caller’s IP. The (LOOP) and (?DO) primitives stash the IP in the frame-pointer register during the compare, because this ISA’s ceq rejects fp as an operand and sw rejects fp as a source; that frees r2 as a scratch register for the limit/index work. A handful of additional conveniences (+LOOP, J, LEAVE, DOES&gt;, RECURSE, PICK, ROLL, ?DUP, MIN/MAX, &lt;=/&gt;=/&lt;&gt;) are left for follow-up work. What’s shipped is enough for the demos the browser tab shows side-by-side with the phase-1 kernel. Live demos in the web UI (AGAIN, CONSTANT, DO LOOP, VARIABLE) are already wired into both the phase-2 and phase-3 tabs, sharing one demo constant via the refactored component in src/repl.rs. The general lesson: a language that only feels thin once it’s compared against a fuller one benefits from that comparison. Good that it surfaced before phase 3 cemented the primitive set. Phase 3: forth-on-forthish — First Subsets Shipping ./forth-on-forthish/ scaffolded with a copy of phase 2’s kernel and core — the current phase-2 kernel with XMX hash + 1-entry lookaside carried forward (commit 4f5e8ab), verified byte-identical to baseline on all 15 examples. Phase 3 work starts on the optimized substrate, not the pre-hash version. Then the first two subsets landed on top of it: Subset 12 (79f4350): the ,DOCOL primitive. Wraps the existing do_colon_cfa as a named dict entry, exposing the 6-byte far-CFA template emission so a Forth : can build headers without touching asm. First attempt at Forth : / ; in a new core/runtime.fth also landed and was reverted — hit the classic SMUDGE-bit problem where ; at the end of : ; ... ; IMMEDIATE resolves to the in-progress new ; because FIND has no way to skip “being-compiled” entries. Documented three options to unblock (asm tweak to :/; that sets/clears HIDDEN, dedicated HIDE-LATEST/UNHIDE-LATEST primitives, or modify CREATE to always hide). Subset 13 (a98b4b8): Forth : and ; shipping. Went with the “asm sets/clears HIDDEN inline” option — colon_thread now runs do_hide_latest between do_colon_cfa and do_rbrac (sets bit 6 on the new entry so FIND skips it during the rest of the definition). do_semi clears HIDDEN on LATEST before compiling EXIT and zeroing STATE. A new core/runtime.fth tier, loaded first (before minimal.fth), defines: : : CREATE ,DOCOL LATEST @ 3 + DUP C@ 64 OR SWAP C! ] ; : ; ['] EXIT , LATEST @ 3 + DUP C@ 191 AND SWAP C! 0 STATE ! ; IMMEDIATE No \ comments in runtime.fth — \ is defined in minimal.fth which loads after. An initial draft that included comments parsed them as code. All 15 examples/*.fth produce the same functional behavior as the first-char hash baseline; the only new UART output is two extra " ok" lines for the two new runtime.fth definitions. The phase-3 kernel now has Forth : and Forth ; — every new colon definition from here on uses the Forth implementations. The remaining subsets push further into the primitive set: Specific moves enabled by the new substrate: Word(s) Strategy New primitive needed Status : and ; : : CREATE ,DOCOL ... ] ; plus a tricky ; that compiles EXIT and toggles STATE; both flip HIDDEN inline on LATEST ,DOCOL + HIDDEN-bit handling in colon_thread / do_semi Shipped (subsets 12, 13) WORD Forth loop over KEY into a known word buffer WORD-BUFFER (or a fixed address) Planned FIND Walk LATEST @ with @, C@, =, AND None — uses existing primitives Planned NUMBER Digit-parsing on top of *, +, &lt;, BASE @ None Planned INTERPRET / QUIT BEGIN ... UNTIL loops over WORD / FIND / EXECUTE / NUMBER None Planned *, /MOD, - +-loops or NEGATE + None — can drop the asm versions Planned AND / OR / XOR Derivations from a single bit-primitive NAND (replaces 3 primitives with 1) Planned DUP / SWAP / OVER / &gt;R / R&gt; SP@-based memory operations on the data stack SP!, RP@, RP! (already have SP@) Planned After the refactor the irreducible asm primitives are approximately: NEXT DOCOL EXIT LIT BRANCH 0BRANCH EXECUTE + NAND @ ! C@ C! KEY EMIT SP@ RP@ SP! RP! LED! SW? HALT About 20 primitives, ~600–800 asm lines (vs. ~2240 today). Projected progression: Approach asm lines Forth lines asm primitives Self-hosting 1: all-asm ~2983 0 ~65 100% asm 2: today 2239 161 50 93% asm 3: forth-on-forthish ~700 ~600 ~22 54% asm 4: forth-from-forth 0 hand-written ~1000 Forth ~22 emitted 0% hand-written asm The Phased Plan Phase 3 breaks into subsets the same way phase 2 did: Subset Size Scope Status 12 small Add ,DOCOL primitive Shipped (79f4350) 13 medium Forth : and ; via core/runtime.fth + inline HIDDEN-bit management Shipped (a98b4b8) 14 medium Add SP!/RP@/RP!; move DUP/SWAP/OVER/&gt;R/R&gt; to Forth Next 15 medium Move *//MOD/- to Forth as loops; AND/OR/XOR from a new NAND primitive Planned 16 large Move WORD/FIND/NUMBER/INTERPRET/QUIT to Forth — after this, kernel matches approach 3 (~700 asm lines) Planned Subset 16 is the scary one. The outer interpreter written in Forth is slow — every text token goes through Forth-coded dictionary walking instead of asm. Estimates: ~10× slower text-input path, but compiled colon definitions run at nearly the same speed. Known Tradeoffs Phase 3 isn’t free. The comparison from phase 2 to phase 3:   Phase 2 (today) Phase 3 (target) Asm lines to maintain 2239 ~700 Asm primitive count 50 ~22 WORD/FIND speed asm (fast) Forth (~10× slower) : and ; speed asm Forth (slightly slower compile) Bootstrap complexity Low Higher — careful .fth load ordering required Retargeting effort Rewrite ~2240 lines of asm Rewrite ~700 lines of primitives + rebuild The payoff is dramatic: the kernel becomes easy to retarget to a different ISA, the language story becomes much cleaner (Forth doing Forth’s job), and phase 4 becomes tractable because the primitive set is already small and orthogonal. Phase 4: forth-from-forth — On the Horizon [UPDATE: follow-on discussion in Rabbit-hole (2/?): FORTH — FIND and the Cost of a Name] ./forth-from-forth/. Write a Forth-to-COR24-asm compiler in Forth. Run it on a host Forth (either a separate Forth, or phase 3’s kernel) to emit kernel.s. After bootstrap, no hand-written .s exists; kernel.s is a build artifact. The cross-compiler has three pieces: Instruction encoder: each COR24 opcode → bytes. Primitive registry: each Forth primitive defined as a small Forth word that emits the asm body. E.g. : prim-+ asm-pop-r2 asm-pop-r0 asm-add-r0-r2 asm-push-r0 asm-next ;. Linker: lays out the dict chain and writes the final .s. This is the standard pattern behind eForth, JonesForth, and several ITSY-style projects. Roughly 500–1000 lines of cross-compiler Forth plus a runtime specification. At that point the kernel’s .s becomes a build artifact, not source. Retargeting to a different ISA means swapping the instruction encoder module. The self-hosting story goes from “Forth is written in asm, except for the words that aren’t” to “Forth is written in Forth, including the compiler that produces the kernel.” Estimated work from phase 3 to phase 4: ~2–3 weeks. Risk: medium — instruction-encoding bugs are silent. Why Ship the Phases As Separate Directories? Each phase is a snapshot. ./ is the canonical reference (stays untouched). ./forth-in-forth/ is today. ./forth-on-forthish/ is where work happens next. ./forth-from-forth/ is future. Keeping them as sibling directories means: Regression tests for the original kernel keep passing against ./. The web frontend can keep pointing at ./ while the next phase stabilizes. Each phase documents its own subset ordering and status (e.g., forth-in-forth/docs/status.md). The comparison tables from phase to phase stay honest — you can diff the asm line counts, binary sizes, and primitive tables directly. It also matches the language-building pattern used across other COR24 languages: build a reference, keep it, and iterate new variants beside it. Vibe Coding the Migration Every subset in phase 2 was a short conversation: “Here’s the current kernel; move = and 0= to Forth, deriving them from XOR. Add a minimal.fth line and a test.” An AI agent proposed the edits, I reviewed and ran the regression harness, and the subset shipped as one commit. Eleven subsets in a day. That pace is only possible because each move is small, each test is fast, and the kernel stays buildable at every step. The reward for that discipline is visible in the commits: every subset is a single logical change, every status.md update is a diff, and SEE FIB on the REPL reads back the Forth definition the AI agent wrote an hour earlier. Forth’s self-extending nature and vibe coding’s tight loop fit each other well — the language is already expected to grow incrementally, and the agent’s output is exactly one .fth addition at a time. What to Watch Next forth-on-forthish/ subset 14 — stack-pointer primitives (SP!, RP@, RP!) and moving DUP/SWAP/OVER/&gt;R/R&gt; into Forth on top of SP@. The first visible win in phase 3: kernel.s drops below 2000 lines. Likely around subset 15. Subset 16 — the big one. WORD/FIND/NUMBER in Forth; asm line count drops by hundreds. Eventually, ./forth-from-forth/ gets scaffolded, and the question becomes which Forth hosts the first cross-compile run. Hashing References In-repo docs for the attempt sequence: docs/hashing-analysis.md (measurement-driven comparison of 9 hash functions) and docs/hashing.txt (Gemini-assisted survey of classical through 2025–2026 techniques). Key external references: Topic Link Why it matters here Krapivin optimal open addressing (2025) Quanta Magazine · arXiv:2501.02305 New probe sequence keeps hash tables near-constant-time to 99% fill. Directly informs the secondary-probe formula in the attempt-3 design. Perfect hash functions Wikipedia · CMPH library · GNU gperf Build-time generator for zero-collision lookup over the static kernel vocabulary (~90 words). Learned index structures (2018) Kraska et al., “The Case for Learned Index Structures” Foundational paper on replacing static hash functions with data-aware models. Inspires the “one hash for the known set, another for user defs” split. SSHash (order-preserving short-string hashing) jermp/sshash Cache-local hashing for short strings — Forth word names are the same shape as bioinformatics k-mers. xxHash / XXH3 xxhash.com · Cyan4973/xxHash Current speed gold standard for non-cryptographic hashing. Benchmark baseline even when we can’t use it directly (too many registers for a 24-bit GPR-limited ISA). FNV-1a Fowler/Noll/Vo hash — Wikipedia Classic short-string hash; one of the attempt-2 candidates, tied for second at 17 collisions. djb2 hash Dan Bernstein cdb docs · hash discussion Another attempt-2 candidate; h = h*33 ^ c. Inspired the len-seeded mult33 winner. PJW / ELF hash PJW hash — Wikipedia Historical precedent for shift-based rolling hashes used in compilers and linkers. JonesForth git.annexia.org/jonesforth Reference Forth implementation covering dictionary layout tradeoffs. Resources Project GitHub Live Demo Forth Interpreter (CLI) sw-embed/sw-cor24-forth — Web Demo (browser UI for the interpreter above) sw-embed/web-sw-cor24-forth COR24 Forth Issue: hashed dictionary #1 — Issue: DO/LOOP etc. #2 — Approach Comparison Doc docs/future.md — Phase 2 Status forth-in-forth/docs/status.md — COR24 Assembler sw-embed/sw-cor24-assembler — COR24 Demo Hub sw-embed/web-sw-cor24-demos Demo Hub Forth sketches itself the way Escher’s hands do — each version a clean line drawing, each one pointing at the next.]]></summary></entry><entry><title type="html">Saw (7/?): Prolog, Many-Agent Isolation, Self-Hosting Assembler, and MLPL</title><link href="https://software-wrighter-lab.github.io/2026/04/19/saw-prolog-many-agents-self-hosting-mlpl/" rel="alternate" type="text/html" title="Saw (7/?): Prolog, Many-Agent Isolation, Self-Hosting Assembler, and MLPL" /><published>2026-04-19T09:30:00-07:00</published><updated>2026-04-19T09:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/19/saw-prolog-many-agents-self-hosting-mlpl</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/19/saw-prolog-many-agents-self-hosting-mlpl/"><![CDATA[<p><img src="/assets/images/posts/block-lion-unicorn-arch.png" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>Seventh Sharpen the Saw update. <a href="/2026/04/12/saw-agent-coordination-fuzzit-vendoring/">Last time</a> the theme was <em>independence</em>—agents coordinating without stepping on each other, tools testing other tools, compilers vendoring their dependencies. This week the theme is <em>controlled scale</em>: adding more agents, more languages, and more layers of the stack, but with infrastructure that keeps growth reliable instead of chaotic.</p>

  <p>Four threads, one idea: the way to scale vibe-coding isn’t to run harder—it’s to build the platform underneath so that running harder stays safe.</p>

</div>

<div class="aside-box">

  <p><strong>Why Sharpen the Saw?</strong> — The name comes from Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">Habit 7</a>: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains—so the feature work on top goes faster.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Rust-to-Prolog Demos</strong></td>
        <td><a href="https://sw-vibe-coding.github.io/rust-to-prolog/">sw-vibe-coding.github.io/rust-to-prolog</a></td>
      </tr>
      <tr>
        <td><strong>Repos &amp; Live Demos</strong></td>
        <td><a href="#repos-and-live-demos">Table below</a></td>
      </tr>
      <tr>
        <td><strong>Language-Building Pattern</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-demos/blob/main/docs/language-building-tech.md">language-building-tech.md</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/04/12/saw-agent-coordination-fuzzit-vendoring/">Saw (6/?): Agent Coordination, Fuzzing Tests, Vendoring, and Emacs Graphics</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="rust-to-prolog-from-lion-and-unicorn-to-a-full-demo-set">Rust-to-Prolog: From Lion and Unicorn to a Full Demo Set</h2>

<p><em>“The Lion lies on Mondays, Tuesdays, and Wednesdays… the Unicorn lies on Thursdays, Fridays, and Saturdays…”</em> Smullyan’s <a href="https://en.wikipedia.org/wiki/Raymond_Smullyan">Alice-in-the-Forest-of-Forgetfulness</a> puzzles are a canonical showcase for <a href="https://en.wikipedia.org/wiki/Prolog">Prolog</a>: facts about when each creature tells the truth, rules for what a statement implies given the day, and a query—“what day is it?”—that the engine answers by backward-chaining through the constraints. No procedural search code; just facts, rules, and unification. That’s the puzzle behind this week’s image—and <code class="language-plaintext highlighter-rouge">liar.pl</code> in the demo set.</p>

<p><a href="https://github.com/sw-vibe-coding/rust-to-prolog">Rust-to-Prolog</a> (<a href="https://sw-vibe-coding.github.io/rust-to-prolog/">live demo</a>) is a reference Prolog implementation in Rust. The in-browser demo ships a curated set of classic Prolog examples that each exercise a different feature of the interpreter:</p>

<table>
  <thead>
    <tr>
      <th>Demo</th>
      <th>What It Exercises</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ancestor</strong></td>
      <td>Recursion and pattern matching</td>
    </tr>
    <tr>
      <td><strong>append</strong></td>
      <td>List concatenation (the canonical Prolog program)</td>
    </tr>
    <tr>
      <td><strong>color</strong></td>
      <td>Backtracking across constraint choices</td>
    </tr>
    <tr>
      <td><strong>fib</strong></td>
      <td>Fibonacci with an accumulator</td>
    </tr>
    <tr>
      <td><strong>liar</strong></td>
      <td>Smullyan’s Lion Lies on Tuesdays puzzle</td>
    </tr>
    <tr>
      <td><strong>max</strong></td>
      <td><code class="language-plaintext highlighter-rouge">!</code> (cut) and commitment</td>
    </tr>
    <tr>
      <td><strong>member</strong></td>
      <td>List membership</td>
    </tr>
    <tr>
      <td><strong>neq</strong> (×2)</td>
      <td>Disequality—same atoms fail, distinct atoms succeed</td>
    </tr>
    <tr>
      <td><strong>path</strong> (×2)</td>
      <td>Graph reachability: yes/no and print-each-path</td>
    </tr>
    <tr>
      <td><strong>sum</strong></td>
      <td>Tail-recursive arithmetic</td>
    </tr>
  </tbody>
</table>

<p>Between them they cover unification, resolution, cut, backtracking, lists, arithmetic, and graph search—the core of what a Prolog implementation has to get right. The <code class="language-plaintext highlighter-rouge">liar.pl</code> puzzle is the showcase piece, but every demo is a focused test of one language feature.</p>

<p>The Rust interpreter is a reference—the starting point, not the destination. The COR24 port is <strong>one self-hosting port split across two languages</strong>, not two competing implementations:</p>

<ul>
  <li><strong>Runtime (Rust WAM → PL/SW LAM)</strong>: The <a href="https://en.wikipedia.org/wiki/Warren_Abstract_Machine">Warren Abstract Machine</a> at the heart of the interpreter—term representation, unification, choice points, and the backtracking trail—moves to PL/SW as a LAM. PL/SW is the right language for this layer: it compiles with <code class="language-plaintext highlighter-rouge">tc24r</code> and runs on COR24, so the runtime itself stops needing a host machine.</li>
  <li><strong>Front end (Rust lex/parse → SNOBOL4)</strong>: Prolog syntax is a pattern-matching and string-processing problem, which is <a href="https://en.wikipedia.org/wiki/SNOBOL">SNOBOL4</a>’s home turf. Tokenization and parsing move into <code class="language-plaintext highlighter-rouge">.sno</code> files and take natural advantage of SNOBOL4’s pattern idioms.</li>
</ul>

<p>Together the <code class="language-plaintext highlighter-rouge">.plsw</code> runtime and the <code class="language-plaintext highlighter-rouge">.sno</code> front end are a drop-in replacement for the current Rust (<code class="language-plaintext highlighter-rouge">.rs</code>) sources. The Rust implementation stays around as the oracle the ported version gets diffed against, but nothing in the on-device toolchain depends on it—the COR24 Prolog is self-hosting.</p>

<p>Why this split? The hard parts of a Prolog implementation—unification, backtracking, and parsing—are semantic decisions, not implementation-language decisions. Solve them once in Rust where the tooling is strong, then pick the right tool per layer for the port: SNOBOL4 for strings, PL/SW for the abstract machine, Rust for the high-confidence reference that keeps the other two honest.</p>

<h2 id="all-together-now-many-agent-isolation">All-Together-Now: Many-Agent Isolation</h2>

<p><a href="https://github.com/sw-vibe-coding/all-together-now">All Together Now</a> (ATN) continues to evolve toward running more agents, concurrently, with better session durability. Four changes landed this week:</p>

<ul>
  <li><strong>Many agents at once</strong>: Beyond the coordinator + workers demo from last week, ATN now supports a larger pool of concurrent agent sessions. Panels and mailboxes scale horizontally instead of hardcoding small counts.</li>
  <li><strong><a href="https://mosh.org">mosh</a> for the SSH layer</strong>: Mosh replaces plain SSH for the control connection to the host running agents. Roaming networks, laptop sleep/wake, and dropped Wi-Fi no longer kill sessions—mosh’s state sync and local echo keep the pipe alive across transient failures.</li>
  <li><strong><a href="https://github.com/tmux/tmux">tmux</a> for remote session management</strong>: Agent sessions live inside tmux so they survive disconnects, can be re-attached from any client, and can be inspected side-by-side on a remote host. The PTY streaming in ATN’s Web UI still works—tmux adds durability underneath.</li>
  <li><strong>Mac → Arch, one user per agent</strong>: Development is moving from macOS to <a href="https://archlinux.org">Arch Linux</a> on the agent host. Each agent gets its own Linux user account, so filesystem, process tree, resource limits (<code class="language-plaintext highlighter-rouge">ulimit</code>), and environment are isolated at the OS level—not just inside a coordinator process. An agent that misbehaves can only affect its own <code class="language-plaintext highlighter-rouge">$HOME</code>, its own cgroup, its own sandbox.</li>
</ul>

<p>The theme is real boundaries. Process-level isolation inside a single user is too porous for agents that can run arbitrary code. Per-user Linux accounts give OS-enforced separation for free, and standard Unix tools (<code class="language-plaintext highlighter-rouge">sudo -u</code>, <code class="language-plaintext highlighter-rouge">su</code>, <code class="language-plaintext highlighter-rouge">systemd-run --uid=</code>) manage the dispatch.</p>

<h2 id="self-hosting-the-cor24-assembler">Self-Hosting the COR24 Assembler</h2>

<div class="resource-box" style="float: left; margin: 0 1.2em 0.8em 0; max-width: 360px;">

  <p><strong>Why these patterns?</strong> — COR24 languages get built different ways. Some start as reference implementations in a high-level language (Prolog in Rust). Some are cross-compiled from the host toolchain (tc24r for C). Some are self-hosted from the start and build on top of already-self-hosted layers (the native assembler, then Forth on top of it). Vendoring, reference-first, and self-hosting each solve a different problem.</p>

  <p>The motivation, tradeoffs, and when to pick which technique are collected in one doc:</p>

  <p><strong><a href="https://github.com/sw-embed/web-sw-cor24-demos/blob/main/docs/language-building-tech.md">→ language-building-tech.md</a></strong></p>

  <p>Read it for <em>why</em> this post keeps mixing approaches across Prolog, the assembler, and the rest of the COR24 stack.</p>

</div>

<p><a href="https://github.com/sw-embed/sw-cor24-assembler">sw-cor24-assembler</a> is the <strong>native</strong> COR24 assembler—a two-pass assembler written in C that, once bootstrapped, runs directly on COR24 FPGA hardware. The naming convention is strict:</p>

<table>
  <thead>
    <tr>
      <th>Repo</th>
      <th>Role</th>
      <th>Written in</th>
      <th>Runs on</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-x-assembler"><code class="language-plaintext highlighter-rouge">sw-cor24-x-assembler</code></a></td>
      <td>Cross-assembler</td>
      <td>Rust</td>
      <td>Host (x86/ARM)</td>
    </tr>
    <tr>
      <td><a href="https://github.com/sw-embed/sw-cor24-assembler"><code class="language-plaintext highlighter-rouge">sw-cor24-assembler</code></a></td>
      <td>Native assembler</td>
      <td>C</td>
      <td>COR24 FPGA</td>
    </tr>
  </tbody>
</table>

<p>The <code class="language-plaintext highlighter-rouge">x-</code> prefix marks cross-tools. The plain name is the native tool that runs on the target. The bootstrap pipeline is short:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tc24r (Rust)                  compiles    cas24.c  →  cas24.s
sw-cor24-x-assembler (Rust)   assembles   cas24.s  →  cas24.bin
cas24.bin runs on COR24 FPGA  →  native assembler available on-device
</code></pre></div></div>

<p>Self-hosting the assembler isn’t about performance—it’s about removing the host PC from the inner loop. Once <code class="language-plaintext highlighter-rouge">cas24</code> runs on-device, COR24 can assemble code for itself. That unlocks every other assembly-based toolchain on the same hardware: a <a href="https://en.wikipedia.org/wiki/Forth_%28programming_language%29">Forth</a> system, a <a href="https://en.wikipedia.org/wiki/P-code_machine">p-code VM</a>, small interpreters—all buildable on COR24 without reaching back to a host.</p>

<p>This is the same motivation as last week’s <a href="/2026/04/12/saw-agent-coordination-fuzzit-vendoring/">vendoring</a>: each layer of the stack should be able to rebuild itself without reaching outside the COR24 ecosystem. Vendoring isolates compilers from each other in time; the native assembler isolates the on-device toolchain from the host machine in space.</p>

<h2 id="sw-mlpl-tiny-lm-complete-mlx-backend-started">sw-MLPL: Tiny LM Complete, MLX Backend Started</h2>

<p><a href="https://github.com/sw-ml-study/sw-mlpl">sw-MLPL</a> had its biggest week yet—three sagas moved forward.</p>

<p><strong>Saga 12 closed</strong> with the tokenizers release (<code class="language-plaintext highlighter-rouge">v0.9.0</code>): a byte-level BPE trainer (<code class="language-plaintext highlighter-rouge">train_bpe</code>), <code class="language-plaintext highlighter-rouge">apply_tokenizer</code> + <code class="language-plaintext highlighter-rouge">decode</code> for round-trip validation, the <code class="language-plaintext highlighter-rouge">experiment "name" { body }</code> scoped form, and an <code class="language-plaintext highlighter-rouge">:experiments</code> registry with <code class="language-plaintext highlighter-rouge">compare(a, b)</code> for side-by-side experiment inspection. Dataset-prep built-ins (<code class="language-plaintext highlighter-rouge">shuffle</code>, <code class="language-plaintext highlighter-rouge">batch</code>, <code class="language-plaintext highlighter-rouge">split</code>) and a <code class="language-plaintext highlighter-rouge">--data-dir</code> sandboxed loader rounded out the training-pipeline surface.</p>

<p><strong>Saga 13 completed end-to-end</strong> as <code class="language-plaintext highlighter-rouge">v0.10.0</code>—a tiny language model from embeddings to generation, all in MLPL:</p>

<table>
  <thead>
    <tr>
      <th>Step</th>
      <th>Feature</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>001</td>
      <td><code class="language-plaintext highlighter-rouge">embed(vocab, d_model, seed)</code> token embeddings</td>
    </tr>
    <tr>
      <td>002</td>
      <td><code class="language-plaintext highlighter-rouge">sinusoidal_encoding(seq_len, d_model)</code> positional encoding</td>
    </tr>
    <tr>
      <td>003</td>
      <td><code class="language-plaintext highlighter-rouge">causal_attention(d_model, heads, seed)</code> masked self-attention</td>
    </tr>
    <tr>
      <td>004</td>
      <td><code class="language-plaintext highlighter-rouge">cross_entropy(logits, targets)</code> fused loss</td>
    </tr>
    <tr>
      <td>005</td>
      <td><code class="language-plaintext highlighter-rouge">sample(logits, t, seed)</code> + <code class="language-plaintext highlighter-rouge">top_k(logits, k)</code> generation</td>
    </tr>
    <tr>
      <td>006</td>
      <td>End-to-end training demo</td>
    </tr>
    <tr>
      <td>007</td>
      <td>Generation loop + attention-map visualization</td>
    </tr>
    <tr>
      <td>008</td>
      <td>“Language Model Basics” and “Training and Generating” tutorials</td>
    </tr>
  </tbody>
</table>

<p>The saga also shipped a Criterion benchmark harness comparing the interpreter against compiled MLPL, a <code class="language-plaintext highlighter-rouge">:version</code> REPL command, a Workspace Introspection demo, seven new docs guides with a README index, and a <code class="language-plaintext highlighter-rouge">wasm32-unknown-unknown</code> panic fix for the <code class="language-plaintext highlighter-rouge">experiment</code> block.</p>

<p><strong>Saga 14 opened: an MLX backend.</strong> MLPL is gaining an <a href="https://ml-explore.github.io/mlx/">Apple MLX</a> runtime target so array ops can dispatch to Apple Silicon’s unified-memory GPU path. Progress in the last four days:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">mlpl-mlx</code> crate with MLX matmul (step 001)</li>
  <li>Elementwise ops and shape primitives on MLX (step 002)</li>
  <li>Reductions, softmax, and cross-entropy on MLX (step 003)</li>
  <li><code class="language-plaintext highlighter-rouge">device("mlx") { ... }</code> scoped form for switching the active backend (step 004)</li>
  <li>Model DSL dispatch + <code class="language-plaintext highlighter-rouge">to_device</code> for moving models between backends (step 005)</li>
</ul>

<p>The MLX work is the clearest example of this week’s <em>controlled scale</em> theme: MLPL already had a CPU runtime, a compile-to-Rust path, and a wasm build—adding an MLX backend means experiments can now scale to GPU without changing user code, just by wrapping a block in <code class="language-plaintext highlighter-rouge">device("mlx") { ... }</code>. Same scripts, more hardware.</p>

<h2 id="repos-and-live-demos">Repos and Live Demos</h2>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>GitHub</th>
      <th>Live Demo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Rust-to-Prolog</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/rust-to-prolog">sw-vibe-coding/rust-to-prolog</a></td>
      <td><a href="https://sw-vibe-coding.github.io/rust-to-prolog/">Prolog Demos</a></td>
    </tr>
    <tr>
      <td><strong>All Together Now</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/all-together-now">sw-vibe-coding/all-together-now</a></td>
      <td><em>in development</em></td>
    </tr>
    <tr>
      <td><strong>COR24 Native Assembler</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-assembler">sw-embed/sw-cor24-assembler</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>COR24 Cross-Assembler</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-x-assembler">sw-embed/sw-cor24-x-assembler</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>sw-MLPL</strong></td>
      <td><a href="https://github.com/sw-ml-study/sw-mlpl">sw-ml-study/sw-mlpl</a></td>
      <td><a href="https://sw-ml-study.github.io/sw-mlpl/">MLPL Demo</a></td>
    </tr>
    <tr>
      <td><strong>PL/SW</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-plsw">sw-embed/sw-cor24-plsw</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-plsw/">PL/SW Demo</a></td>
    </tr>
    <tr>
      <td><strong>SNOBOL4</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-snobol4">sw-embed/sw-cor24-snobol4</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-snobol4/">SNOBOL4 Demo</a></td>
    </tr>
    <tr>
      <td><strong>COR24 Demo Hub</strong></td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub</a></td>
    </tr>
  </tbody>
</table>

<h2 id="whats-next">What’s Next</h2>

<p><strong>Rust-to-Prolog</strong>: Begin the self-hosting COR24 port—PL/SW for the LAM runtime (the WAM analog) and SNOBOL4 for the lexer and parser. The Rust implementation stays around as the oracle; the ported version must pass the same demo set while running entirely on the COR24 toolchain.</p>

<p><strong>All Together Now</strong>: Full migration to the Arch host with per-user agent accounts. Campaign to run many worker agents concurrently on a shared long-running task, using mosh/tmux durability for multi-day runs.</p>

<p><strong>COR24 Assembler</strong>: Finish the two-pass native <code class="language-plaintext highlighter-rouge">cas24</code> implementation in C, boot it on COR24 FPGA, and validate by using it to rebuild other on-device tools (Forth, p-code VM experiments) without the host cross-assembler in the loop.</p>

<p><strong>sw-MLPL</strong>: Fill out the MLX backend (optimizers, autograd, remaining layer ops), then re-run the Tiny LM demo end-to-end on MLX and publish a backend-parity report against the CPU path.</p>

<hr />

<p><em>Scaling up without breaking down takes infrastructure. Follow for more Sharpen the Saw updates.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rust" /><category term="ai-agents" /><category term="compilers" /><category term="machine-learning" /><category term="sharpen-the-saw" /><category term="rust" /><category term="prolog" /><category term="all-together-now" /><category term="mosh" /><category term="tmux" /><category term="arch-linux" /><category term="cor24" /><category term="assembler" /><category term="self-hosting" /><category term="mlpl" /><category term="multi-agent" /><summary type="html"><![CDATA[Seventh Sharpen the Saw update. Last time the theme was independence—agents coordinating without stepping on each other, tools testing other tools, compilers vendoring their dependencies. This week the theme is controlled scale: adding more agents, more languages, and more layers of the stack, but with infrastructure that keeps growth reliable instead of chaotic. Four threads, one idea: the way to scale vibe-coding isn’t to run harder—it’s to build the platform underneath so that running harder stays safe. Why Sharpen the Saw? — The name comes from Covey’s Habit 7: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains—so the feature work on top goes faster. Resource Link Rust-to-Prolog Demos sw-vibe-coding.github.io/rust-to-prolog Repos &amp; Live Demos Table below Language-Building Pattern language-building-tech.md Prior Post Saw (6/?): Agent Coordination, Fuzzing Tests, Vendoring, and Emacs Graphics Comments Discord Rust-to-Prolog: From Lion and Unicorn to a Full Demo Set “The Lion lies on Mondays, Tuesdays, and Wednesdays… the Unicorn lies on Thursdays, Fridays, and Saturdays…” Smullyan’s Alice-in-the-Forest-of-Forgetfulness puzzles are a canonical showcase for Prolog: facts about when each creature tells the truth, rules for what a statement implies given the day, and a query—“what day is it?”—that the engine answers by backward-chaining through the constraints. No procedural search code; just facts, rules, and unification. That’s the puzzle behind this week’s image—and liar.pl in the demo set. Rust-to-Prolog (live demo) is a reference Prolog implementation in Rust. The in-browser demo ships a curated set of classic Prolog examples that each exercise a different feature of the interpreter: Demo What It Exercises ancestor Recursion and pattern matching append List concatenation (the canonical Prolog program) color Backtracking across constraint choices fib Fibonacci with an accumulator liar Smullyan’s Lion Lies on Tuesdays puzzle max ! (cut) and commitment member List membership neq (×2) Disequality—same atoms fail, distinct atoms succeed path (×2) Graph reachability: yes/no and print-each-path sum Tail-recursive arithmetic Between them they cover unification, resolution, cut, backtracking, lists, arithmetic, and graph search—the core of what a Prolog implementation has to get right. The liar.pl puzzle is the showcase piece, but every demo is a focused test of one language feature. The Rust interpreter is a reference—the starting point, not the destination. The COR24 port is one self-hosting port split across two languages, not two competing implementations: Runtime (Rust WAM → PL/SW LAM): The Warren Abstract Machine at the heart of the interpreter—term representation, unification, choice points, and the backtracking trail—moves to PL/SW as a LAM. PL/SW is the right language for this layer: it compiles with tc24r and runs on COR24, so the runtime itself stops needing a host machine. Front end (Rust lex/parse → SNOBOL4): Prolog syntax is a pattern-matching and string-processing problem, which is SNOBOL4’s home turf. Tokenization and parsing move into .sno files and take natural advantage of SNOBOL4’s pattern idioms. Together the .plsw runtime and the .sno front end are a drop-in replacement for the current Rust (.rs) sources. The Rust implementation stays around as the oracle the ported version gets diffed against, but nothing in the on-device toolchain depends on it—the COR24 Prolog is self-hosting. Why this split? The hard parts of a Prolog implementation—unification, backtracking, and parsing—are semantic decisions, not implementation-language decisions. Solve them once in Rust where the tooling is strong, then pick the right tool per layer for the port: SNOBOL4 for strings, PL/SW for the abstract machine, Rust for the high-confidence reference that keeps the other two honest. All-Together-Now: Many-Agent Isolation All Together Now (ATN) continues to evolve toward running more agents, concurrently, with better session durability. Four changes landed this week: Many agents at once: Beyond the coordinator + workers demo from last week, ATN now supports a larger pool of concurrent agent sessions. Panels and mailboxes scale horizontally instead of hardcoding small counts. mosh for the SSH layer: Mosh replaces plain SSH for the control connection to the host running agents. Roaming networks, laptop sleep/wake, and dropped Wi-Fi no longer kill sessions—mosh’s state sync and local echo keep the pipe alive across transient failures. tmux for remote session management: Agent sessions live inside tmux so they survive disconnects, can be re-attached from any client, and can be inspected side-by-side on a remote host. The PTY streaming in ATN’s Web UI still works—tmux adds durability underneath. Mac → Arch, one user per agent: Development is moving from macOS to Arch Linux on the agent host. Each agent gets its own Linux user account, so filesystem, process tree, resource limits (ulimit), and environment are isolated at the OS level—not just inside a coordinator process. An agent that misbehaves can only affect its own $HOME, its own cgroup, its own sandbox. The theme is real boundaries. Process-level isolation inside a single user is too porous for agents that can run arbitrary code. Per-user Linux accounts give OS-enforced separation for free, and standard Unix tools (sudo -u, su, systemd-run --uid=) manage the dispatch. Self-Hosting the COR24 Assembler Why these patterns? — COR24 languages get built different ways. Some start as reference implementations in a high-level language (Prolog in Rust). Some are cross-compiled from the host toolchain (tc24r for C). Some are self-hosted from the start and build on top of already-self-hosted layers (the native assembler, then Forth on top of it). Vendoring, reference-first, and self-hosting each solve a different problem. The motivation, tradeoffs, and when to pick which technique are collected in one doc: → language-building-tech.md Read it for why this post keeps mixing approaches across Prolog, the assembler, and the rest of the COR24 stack. sw-cor24-assembler is the native COR24 assembler—a two-pass assembler written in C that, once bootstrapped, runs directly on COR24 FPGA hardware. The naming convention is strict: Repo Role Written in Runs on sw-cor24-x-assembler Cross-assembler Rust Host (x86/ARM) sw-cor24-assembler Native assembler C COR24 FPGA The x- prefix marks cross-tools. The plain name is the native tool that runs on the target. The bootstrap pipeline is short: tc24r (Rust) compiles cas24.c → cas24.s sw-cor24-x-assembler (Rust) assembles cas24.s → cas24.bin cas24.bin runs on COR24 FPGA → native assembler available on-device Self-hosting the assembler isn’t about performance—it’s about removing the host PC from the inner loop. Once cas24 runs on-device, COR24 can assemble code for itself. That unlocks every other assembly-based toolchain on the same hardware: a Forth system, a p-code VM, small interpreters—all buildable on COR24 without reaching back to a host. This is the same motivation as last week’s vendoring: each layer of the stack should be able to rebuild itself without reaching outside the COR24 ecosystem. Vendoring isolates compilers from each other in time; the native assembler isolates the on-device toolchain from the host machine in space. sw-MLPL: Tiny LM Complete, MLX Backend Started sw-MLPL had its biggest week yet—three sagas moved forward. Saga 12 closed with the tokenizers release (v0.9.0): a byte-level BPE trainer (train_bpe), apply_tokenizer + decode for round-trip validation, the experiment "name" { body } scoped form, and an :experiments registry with compare(a, b) for side-by-side experiment inspection. Dataset-prep built-ins (shuffle, batch, split) and a --data-dir sandboxed loader rounded out the training-pipeline surface. Saga 13 completed end-to-end as v0.10.0—a tiny language model from embeddings to generation, all in MLPL: Step Feature 001 embed(vocab, d_model, seed) token embeddings 002 sinusoidal_encoding(seq_len, d_model) positional encoding 003 causal_attention(d_model, heads, seed) masked self-attention 004 cross_entropy(logits, targets) fused loss 005 sample(logits, t, seed) + top_k(logits, k) generation 006 End-to-end training demo 007 Generation loop + attention-map visualization 008 “Language Model Basics” and “Training and Generating” tutorials The saga also shipped a Criterion benchmark harness comparing the interpreter against compiled MLPL, a :version REPL command, a Workspace Introspection demo, seven new docs guides with a README index, and a wasm32-unknown-unknown panic fix for the experiment block. Saga 14 opened: an MLX backend. MLPL is gaining an Apple MLX runtime target so array ops can dispatch to Apple Silicon’s unified-memory GPU path. Progress in the last four days: mlpl-mlx crate with MLX matmul (step 001) Elementwise ops and shape primitives on MLX (step 002) Reductions, softmax, and cross-entropy on MLX (step 003) device("mlx") { ... } scoped form for switching the active backend (step 004) Model DSL dispatch + to_device for moving models between backends (step 005) The MLX work is the clearest example of this week’s controlled scale theme: MLPL already had a CPU runtime, a compile-to-Rust path, and a wasm build—adding an MLX backend means experiments can now scale to GPU without changing user code, just by wrapping a block in device("mlx") { ... }. Same scripts, more hardware. Repos and Live Demos Project GitHub Live Demo Rust-to-Prolog sw-vibe-coding/rust-to-prolog Prolog Demos All Together Now sw-vibe-coding/all-together-now in development COR24 Native Assembler sw-embed/sw-cor24-assembler N/A COR24 Cross-Assembler sw-embed/sw-cor24-x-assembler N/A sw-MLPL sw-ml-study/sw-mlpl MLPL Demo PL/SW sw-embed/sw-cor24-plsw PL/SW Demo SNOBOL4 sw-embed/sw-cor24-snobol4 SNOBOL4 Demo COR24 Demo Hub sw-embed/web-sw-cor24-demos Demo Hub What’s Next Rust-to-Prolog: Begin the self-hosting COR24 port—PL/SW for the LAM runtime (the WAM analog) and SNOBOL4 for the lexer and parser. The Rust implementation stays around as the oracle; the ported version must pass the same demo set while running entirely on the COR24 toolchain. All Together Now: Full migration to the Arch host with per-user agent accounts. Campaign to run many worker agents concurrently on a shared long-running task, using mosh/tmux durability for multi-day runs. COR24 Assembler: Finish the two-pass native cas24 implementation in C, boot it on COR24 FPGA, and validate by using it to rebuild other on-device tools (Forth, p-code VM experiments) without the host cross-assembler in the loop. sw-MLPL: Fill out the MLX backend (optimizers, autograd, remaining layer ops), then re-run the Tiny LM demo end-to-end on MLX and publish a backend-parity report against the CPU path. Scaling up without breaking down takes infrastructure. Follow for more Sharpen the Saw updates.]]></summary></entry><entry><title type="html">TBT (9/?): UNIVAC Startrek, TRS-80 Adventures, and COR24 BASIC</title><link href="https://software-wrighter-lab.github.io/2026/04/16/tbt-cor24-basic-startrek-trs80-robot-chase/" rel="alternate" type="text/html" title="TBT (9/?): UNIVAC Startrek, TRS-80 Adventures, and COR24 BASIC" /><published>2026-04-16T16:30:00-07:00</published><updated>2026-04-16T16:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/16/tbt-cor24-basic-startrek-trs80-robot-chase</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/16/tbt-cor24-basic-startrek-trs80-robot-chase/"><![CDATA[<p><img src="/assets/images/posts/block-spaceships-robots.png" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>Three BASIC games. Three eras. One retro-computing platform.</p>

  <p>Startrek on a UNIVAC 1108 teletype in the ’70s. A Trek text adventure typed in from a computer magazine on a TRS-80 in the ’80s. And Robot Chase, added just last week at a friend’s request. All three now run in the browser on an emulated <a href="https://github.com/sw-embed/sw-cor24-basic">COR24 BASIC</a>—a line-numbered, integer-only, 1970s-style time-sharing BASIC built on a p-code VM written in Pascal.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Play in Browser</strong></td>
        <td><a href="https://sw-embed.github.io/web-sw-cor24-basic/">COR24 BASIC Demos</a></td>
      </tr>
      <tr>
        <td><strong>Games</strong></td>
        <td><em>startrek</em>, <em>trek-adventure</em>, <em>robot-chase</em></td>
      </tr>
      <tr>
        <td><strong>BASIC Interpreter</strong></td>
        <td><a href="https://github.com/sw-embed/sw-cor24-basic">sw-embed/sw-cor24-basic</a></td>
      </tr>
      <tr>
        <td><strong>Web Runtime</strong></td>
        <td><a href="https://github.com/sw-embed/web-sw-cor24-basic">sw-embed/web-sw-cor24-basic</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/03/26/tbt-wiki-rs-six-wikis/">TBT (8/?): Wiki Systems</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="univac-1108-startrek-the-bell-that-gave-you-away">UNIVAC 1108 Startrek: The Bell That Gave You Away</h2>

<p>The first version of <a href="https://en.wikipedia.org/wiki/Star_Trek_%28text_game%29">Star Trek</a> I ever played ran on a <a href="https://en.wikipedia.org/wiki/UNIVAC_1100/2200_series">UNIVAC 1108</a> time-sharing system, dialed into from a <a href="https://en.wikipedia.org/wiki/Teleprinter">teletype</a> terminal. You typed commands, the ship’s status came back as ALL CAPS tabular output, and the teletype’s mechanical print head hammered out every line of the 8×8 galactic map one character at a time.</p>

<p>The game used an 8×8 galaxy of quadrants, each with its own 8×8 sector grid, populated with Klingons, starbases, and stars. You commanded the Enterprise: warp around, fire phasers and torpedoes, dock for repairs, and save the Federation before your energy or time ran out.</p>

<p>But the detail nobody forgets: <strong>the <code class="language-plaintext highlighter-rouge">BELL</code> character.</strong> Every teletype in the room had a solenoid-driven bell. When your ship entered a quadrant containing Klingons, the Star Trek program printed:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*** RED ALERT *** *** RED ALERT ***
</code></pre></div></div>

<p>…preceded by ASCII BEL (<code class="language-plaintext highlighter-rouge">CHR$(7)</code>), which rang the bell on your terminal. Loudly. Across the entire room.</p>

<p>Which meant everyone in the terminal room could tell exactly who was playing Star Trek. This was at college, where the UNIVAC 1108 was also where you did your programming homework—and the bell was supposed to be the game warning <em>you</em> about Klingons, but in practice it also let every other student in the room know you were not, in fact, finishing that assignment.</p>

<p>My COR24 version (<code class="language-plaintext highlighter-rouge">startrek.bas</code>) keeps the command loop, the galaxy, and the Red Alert banner. The one thing it sadly <em>doesn’t</em> reproduce is the bell itself: <code class="language-plaintext highlighter-rouge">CHR$(7)</code> in a browser tab is silent, and the solenoid-driven clack of a shared teletype doesn’t port. You’ll have to imagine the sound.</p>

<h2 id="trs-80-trek-adventure-the-vibe-of-a-magazine-listing">TRS-80 Trek Adventure: The Vibe of a Magazine Listing</h2>

<p>A half-decade later, the home computer era produced a different kind of Trek game: <strong>text adventures</strong> distributed as <strong>BASIC source listings in computer magazines</strong>. On a <a href="https://en.wikipedia.org/wiki/TRS-80">TRS-80</a>—in my case, my dad’s old Model I—you read the listing, typed it in line by line (<code class="language-plaintext highlighter-rouge">100 DIM A$(50)</code>, <code class="language-plaintext highlighter-rouge">110 PRINT "BRIDGE"</code>, on for hundreds of lines), saved to cassette, and prayed you hadn’t mistyped a variable name. Debug night was the same night you got the game. If it didn’t run, you walked back through every line number until you found the typo.</p>

<p>For <code class="language-plaintext highlighter-rouge">trek-adventure.bas</code>, I didn’t have an actual magazine listing to work from. So I did what the era didn’t have: <strong>vibe coding, baby.</strong> I described the game I wanted—a text adventure in integer-only line-numbered BASIC, starting on the bridge of the Enterprise, menu-driven in the 80s-magazine style, with a tight little puzzle about boarders, a phaser, a key card, a tribble, a relay coupler, and a decaying orbit—and the AI wrote one. No original source, no translation, no finger down a magazine page. Just a description and iteration.</p>

<p>The AI leaned into the bit. The resulting file opens with a REM block claiming a provenance that never existed:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>104 REM STAR TREK: DECAYING ORBIT - A TEXT ADVENTURE.
105 REM TRANSLATED FROM A QBASIC/GW-BASIC MAGAZINE LISTING
106 REM INTO INTEGER-ONLY COR24 BASIC V1. COMMANDS ARE
107 REM NUMERIC MENUS; TARGETS ARE NUMBERS SHOWN IN THE
108 REM ROOM DESCRIPTIONS.
</code></pre></div></div>

<p>There is no magazine listing. The vibe <em>is</em> the listing. Numbered menus, numeric targets pulled from room descriptions, a handful of endings, state on a 24-bit integer VM—the game feels exactly like something that could have shipped in a 1982 issue of <em>80 Micro</em>, because that’s what I asked for. No <code class="language-plaintext highlighter-rouge">?SN ERROR</code> at 3am, no cassette rewinding, no walking every line number hunting a typo. The grunt work moved up a layer of abstraction; the feel stayed put.</p>

<h2 id="robot-chase-added-on-request">Robot Chase: Added on Request</h2>

<p>Every retro project eventually picks up a side quest. A friend asked whether I could also do <a href="https://en.wikipedia.org/wiki/Robots_%28BSD_game%29">Robot Chase</a> (sometimes called <em>Daleks</em> or just <em>Robots</em>)—the classic 1980s terminal game where you’re trapped on a grid with robots that step one square toward you every turn, and your only hope is to make them collide into each other or into wreckage.</p>

<p>It’s a small game with a lot of character:</p>

<ul>
  <li>16×16 board, 12 robots.</li>
  <li>Numpad-style movement: <code class="language-plaintext highlighter-rouge">7 8 9 / 4 5 6 / 1 2 3</code>—<code class="language-plaintext highlighter-rouge">5</code> waits a turn.</li>
  <li>Three emergency teleports per game. <code class="language-plaintext highlighter-rouge">99</code> resigns.</li>
  <li><code class="language-plaintext highlighter-rouge">10</code> gives a 4×4 long-range-scan summary, collapsing the board into regional robot counts so you can plan routes.</li>
  <li>Collide a robot with another robot or with wreckage, and the tile becomes a <code class="language-plaintext highlighter-rouge">*</code> wreck. Touch a robot or a wreck yourself, and you lose.</li>
</ul>

<p>COR24 BASIC is integer-only and has no clock, so the <a href="https://en.wikipedia.org/wiki/Pseudorandom_number_generator">PRNG</a> seed comes from whatever the variable <code class="language-plaintext highlighter-rouge">R</code> holds when you start the game. First run, <code class="language-plaintext highlighter-rouge">R=0</code> → deterministic default seed 5237. Subsequent runs pick up the previous game’s residual <code class="language-plaintext highlighter-rouge">R</code>, which in practice gives you a different board each time. Pure, old-school deterministic pseudo-randomness. No <code class="language-plaintext highlighter-rouge">time()</code>, no <code class="language-plaintext highlighter-rouge">/dev/urandom</code>, just whatever integer happens to be sitting in <code class="language-plaintext highlighter-rouge">R</code>.</p>

<p>My version runs as <code class="language-plaintext highlighter-rouge">robot-chase.bas</code>.</p>

<h2 id="why-cor24-basic">Why COR24 BASIC?</h2>

<p>All three games run on <a href="https://github.com/sw-embed/sw-cor24-basic">COR24 BASIC v1</a>, which is deliberately <strong>time-sharing-era BASIC</strong>, not a modern dialect. The design target is the experience of UNIVAC 1100-series terminal BASIC: line numbers, integer arithmetic, single-letter variables, <code class="language-plaintext highlighter-rouge">GOSUB</code>/<code class="language-plaintext highlighter-rouge">RETURN</code>, <code class="language-plaintext highlighter-rouge">GOTO</code>, interactive <code class="language-plaintext highlighter-rouge">LIST</code> and <code class="language-plaintext highlighter-rouge">RUN</code>. No floats, no strings beyond <code class="language-plaintext highlighter-rouge">CHR$(n)</code> inside <code class="language-plaintext highlighter-rouge">PRINT</code>, no structured programming. Just enough to type a program into a terminal and watch it go.</p>

<p>The implementation is a four-layer stack:</p>

<table>
  <thead>
    <tr>
      <th>Layer</th>
      <th>What It Is</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Layer 3</strong></td>
      <td>BASIC interpreter (tokenizer, parser, dispatch)</td>
    </tr>
    <tr>
      <td><strong>Layer 2</strong></td>
      <td>BASIC runtime (I/O, line storage, stacks, PEEK/POKE)</td>
    </tr>
    <tr>
      <td><strong>Layer 1</strong></td>
      <td><a href="https://en.wikipedia.org/wiki/P-code_machine">P-code</a> virtual machine (language-neutral abstract machine)</td>
    </tr>
    <tr>
      <td><strong>Layer 0</strong></td>
      <td>COR24 hardware / emulator</td>
    </tr>
  </tbody>
</table>

<p>The interpreter is a <a href="https://en.wikipedia.org/wiki/Pascal_%28programming_language%29">Pascal</a> program compiled to p-code by <code class="language-plaintext highlighter-rouge">p24p</code>, assembled by <code class="language-plaintext highlighter-rouge">pa24r</code> into <code class="language-plaintext highlighter-rouge">.p24</code> bytecode, and run on <code class="language-plaintext highlighter-rouge">pv24t</code> (the p-code VM). The p-code machine handles arithmetic, stacks, calls, and memory; the BASIC runtime on top of it handles line-numbered statements and interactive editing.</p>

<p>This matters for the games because <strong>integer-only BASIC on a 24-bit word is a real constraint.</strong> No floating-point Klingon positioning, no shortcut RNGs, no lazy string parsing. You write the game like you’re writing it in 1975—arrays of integers, careful arithmetic, explicit line numbers, <code class="language-plaintext highlighter-rouge">GOSUB</code> instead of functions—and the emulator gives it back to you pixel-true in a browser tab.</p>

<h2 id="try-the-demos">Try the Demos</h2>

<p><a href="https://sw-embed.github.io/web-sw-cor24-basic/">sw-embed.github.io/web-sw-cor24-basic</a> runs the entire stack in WebAssembly. Pick a program from the examples list, click <strong>RUN</strong>, and the integer-only BASIC interpreter—running on a Pascal-compiled p-code VM—plays the game in your browser:</p>

<table>
  <thead>
    <tr>
      <th>Demo</th>
      <th>What You Get</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">startrek.bas</code></strong></td>
      <td>The 8×8 galaxy, phaser and torpedo combat, starbase docking, energy management. The command loop and the quadrant display are faithful to the 1970s teletype experience—minus the bell.</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">trek-adventure.bas</code></strong></td>
      <td>Numbered-menu text adventure starting on the bridge of the Enterprise. Save the ship, stop the boarders, keep the orbit from decaying.</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">robot-chase.bas</code></strong></td>
      <td>The 16×16 board with 12 robots, numpad movement, teleports, long-range scan. Collide the robots into each other and yourself into none of them.</td>
    </tr>
  </tbody>
</table>

<p>All three are line-numbered BASIC source you can inspect, edit, re-run, and (if you want) port to your own time-sharing-era BASIC. The listings are MIT-licensed in <a href="https://github.com/sw-embed/sw-cor24-basic/tree/main/examples">sw-cor24-basic/examples</a>.</p>

<p>Three eras, one interpreter, one browser tab. The bell is silent now, but the galaxy still needs defending.</p>

<hr />

<p><em>TBT: what we built with, what we still build from.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="tbt" /><category term="programming-history" /><category term="retrocomputing" /><category term="throwback-thursday" /><category term="basic" /><category term="star-trek" /><category term="trs-80" /><category term="univac-1108" /><category term="cor24" /><category term="p-code-vm" /><category term="pascal" /><category term="integer-basic" /><category term="retro-gaming" /><category term="teletype" /><category term="vibe-coding" /><summary type="html"><![CDATA[Three BASIC games. Three eras. One retro-computing platform. Startrek on a UNIVAC 1108 teletype in the ’70s. A Trek text adventure typed in from a computer magazine on a TRS-80 in the ’80s. And Robot Chase, added just last week at a friend’s request. All three now run in the browser on an emulated COR24 BASIC—a line-numbered, integer-only, 1970s-style time-sharing BASIC built on a p-code VM written in Pascal. Resource Link Play in Browser COR24 BASIC Demos Games startrek, trek-adventure, robot-chase BASIC Interpreter sw-embed/sw-cor24-basic Web Runtime sw-embed/web-sw-cor24-basic Prior Post TBT (8/?): Wiki Systems Comments Discord UNIVAC 1108 Startrek: The Bell That Gave You Away The first version of Star Trek I ever played ran on a UNIVAC 1108 time-sharing system, dialed into from a teletype terminal. You typed commands, the ship’s status came back as ALL CAPS tabular output, and the teletype’s mechanical print head hammered out every line of the 8×8 galactic map one character at a time. The game used an 8×8 galaxy of quadrants, each with its own 8×8 sector grid, populated with Klingons, starbases, and stars. You commanded the Enterprise: warp around, fire phasers and torpedoes, dock for repairs, and save the Federation before your energy or time ran out. But the detail nobody forgets: the BELL character. Every teletype in the room had a solenoid-driven bell. When your ship entered a quadrant containing Klingons, the Star Trek program printed: *** RED ALERT *** *** RED ALERT *** …preceded by ASCII BEL (CHR$(7)), which rang the bell on your terminal. Loudly. Across the entire room. Which meant everyone in the terminal room could tell exactly who was playing Star Trek. This was at college, where the UNIVAC 1108 was also where you did your programming homework—and the bell was supposed to be the game warning you about Klingons, but in practice it also let every other student in the room know you were not, in fact, finishing that assignment. My COR24 version (startrek.bas) keeps the command loop, the galaxy, and the Red Alert banner. The one thing it sadly doesn’t reproduce is the bell itself: CHR$(7) in a browser tab is silent, and the solenoid-driven clack of a shared teletype doesn’t port. You’ll have to imagine the sound. TRS-80 Trek Adventure: The Vibe of a Magazine Listing A half-decade later, the home computer era produced a different kind of Trek game: text adventures distributed as BASIC source listings in computer magazines. On a TRS-80—in my case, my dad’s old Model I—you read the listing, typed it in line by line (100 DIM A$(50), 110 PRINT "BRIDGE", on for hundreds of lines), saved to cassette, and prayed you hadn’t mistyped a variable name. Debug night was the same night you got the game. If it didn’t run, you walked back through every line number until you found the typo. For trek-adventure.bas, I didn’t have an actual magazine listing to work from. So I did what the era didn’t have: vibe coding, baby. I described the game I wanted—a text adventure in integer-only line-numbered BASIC, starting on the bridge of the Enterprise, menu-driven in the 80s-magazine style, with a tight little puzzle about boarders, a phaser, a key card, a tribble, a relay coupler, and a decaying orbit—and the AI wrote one. No original source, no translation, no finger down a magazine page. Just a description and iteration. The AI leaned into the bit. The resulting file opens with a REM block claiming a provenance that never existed: 104 REM STAR TREK: DECAYING ORBIT - A TEXT ADVENTURE. 105 REM TRANSLATED FROM A QBASIC/GW-BASIC MAGAZINE LISTING 106 REM INTO INTEGER-ONLY COR24 BASIC V1. COMMANDS ARE 107 REM NUMERIC MENUS; TARGETS ARE NUMBERS SHOWN IN THE 108 REM ROOM DESCRIPTIONS. There is no magazine listing. The vibe is the listing. Numbered menus, numeric targets pulled from room descriptions, a handful of endings, state on a 24-bit integer VM—the game feels exactly like something that could have shipped in a 1982 issue of 80 Micro, because that’s what I asked for. No ?SN ERROR at 3am, no cassette rewinding, no walking every line number hunting a typo. The grunt work moved up a layer of abstraction; the feel stayed put. Robot Chase: Added on Request Every retro project eventually picks up a side quest. A friend asked whether I could also do Robot Chase (sometimes called Daleks or just Robots)—the classic 1980s terminal game where you’re trapped on a grid with robots that step one square toward you every turn, and your only hope is to make them collide into each other or into wreckage. It’s a small game with a lot of character: 16×16 board, 12 robots. Numpad-style movement: 7 8 9 / 4 5 6 / 1 2 3—5 waits a turn. Three emergency teleports per game. 99 resigns. 10 gives a 4×4 long-range-scan summary, collapsing the board into regional robot counts so you can plan routes. Collide a robot with another robot or with wreckage, and the tile becomes a * wreck. Touch a robot or a wreck yourself, and you lose. COR24 BASIC is integer-only and has no clock, so the PRNG seed comes from whatever the variable R holds when you start the game. First run, R=0 → deterministic default seed 5237. Subsequent runs pick up the previous game’s residual R, which in practice gives you a different board each time. Pure, old-school deterministic pseudo-randomness. No time(), no /dev/urandom, just whatever integer happens to be sitting in R. My version runs as robot-chase.bas. Why COR24 BASIC? All three games run on COR24 BASIC v1, which is deliberately time-sharing-era BASIC, not a modern dialect. The design target is the experience of UNIVAC 1100-series terminal BASIC: line numbers, integer arithmetic, single-letter variables, GOSUB/RETURN, GOTO, interactive LIST and RUN. No floats, no strings beyond CHR$(n) inside PRINT, no structured programming. Just enough to type a program into a terminal and watch it go. The implementation is a four-layer stack: Layer What It Is Layer 3 BASIC interpreter (tokenizer, parser, dispatch) Layer 2 BASIC runtime (I/O, line storage, stacks, PEEK/POKE) Layer 1 P-code virtual machine (language-neutral abstract machine) Layer 0 COR24 hardware / emulator The interpreter is a Pascal program compiled to p-code by p24p, assembled by pa24r into .p24 bytecode, and run on pv24t (the p-code VM). The p-code machine handles arithmetic, stacks, calls, and memory; the BASIC runtime on top of it handles line-numbered statements and interactive editing. This matters for the games because integer-only BASIC on a 24-bit word is a real constraint. No floating-point Klingon positioning, no shortcut RNGs, no lazy string parsing. You write the game like you’re writing it in 1975—arrays of integers, careful arithmetic, explicit line numbers, GOSUB instead of functions—and the emulator gives it back to you pixel-true in a browser tab. Try the Demos sw-embed.github.io/web-sw-cor24-basic runs the entire stack in WebAssembly. Pick a program from the examples list, click RUN, and the integer-only BASIC interpreter—running on a Pascal-compiled p-code VM—plays the game in your browser: Demo What You Get startrek.bas The 8×8 galaxy, phaser and torpedo combat, starbase docking, energy management. The command loop and the quadrant display are faithful to the 1970s teletype experience—minus the bell. trek-adventure.bas Numbered-menu text adventure starting on the bridge of the Enterprise. Save the ship, stop the boarders, keep the orbit from decaying. robot-chase.bas The 16×16 board with 12 robots, numpad movement, teleports, long-range scan. Collide the robots into each other and yourself into none of them. All three are line-numbered BASIC source you can inspect, edit, re-run, and (if you want) port to your own time-sharing-era BASIC. The listings are MIT-licensed in sw-cor24-basic/examples. Three eras, one interpreter, one browser tab. The bell is silent now, but the galaxy still needs defending. TBT: what we built with, what we still build from.]]></summary></entry><entry><title type="html">Saw (6/?): Agent Coordination, Fuzzing Tests, Vendoring, and Emacs Graphics</title><link href="https://software-wrighter-lab.github.io/2026/04/12/saw-agent-coordination-fuzzit-vendoring/" rel="alternate" type="text/html" title="Saw (6/?): Agent Coordination, Fuzzing Tests, Vendoring, and Emacs Graphics" /><published>2026-04-12T09:30:00-07:00</published><updated>2026-04-12T09:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/12/saw-agent-coordination-fuzzit-vendoring</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/12/saw-agent-coordination-fuzzit-vendoring/"><![CDATA[<p><img src="/assets/images/posts/block-robots-card-table.png" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>Sixth Sharpen the Saw update. <a href="/2026/04/05/saw-saga-archiving-mlpl-plsw-cor24-compilers/">Last time</a> the theme was dependency chains—saga archiving, new languages, and compiler fixes cascading through the stack. This week the theme is <em>independence</em>: making agents coordinate without stepping on each other, testing tools without trusting their error handling, and letting compilers evolve without breaking their downstream consumers.</p>

  <p>Four projects, one idea: build the infrastructure so that parallel work stays parallel.</p>

</div>

<div class="aside-box">

  <p><strong>Why Sharpen the Saw?</strong> — The name comes from Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">Habit 7</a>: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains—so the feature work on top goes faster.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repos &amp; Live Demos</strong></td>
        <td><a href="#repos-and-live-demos">Table below</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/04/05/saw-saga-archiving-mlpl-plsw-cor24-compilers/">Saw (5/?): Sagas, Languages, and Compiler Chains</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="all-together-now-multi-agent-coordination-demo">All Together Now: Multi-Agent Coordination Demo</h2>

<p>All Together Now (ATN) is a program manager that orchestrates multiple AI agent sessions running in isolated PTY environments. The new milestone is a demo that shows <strong>Claude Code as coordinator</strong> delegating tasks to multiple <strong>opencode/GLM-5 worker agents</strong>, with the agents collaborating through mailboxes and a shared wiki.</p>

<p>The Web UI (Yew/WASM) now presents this as a <strong>multi-panel layout</strong>:</p>

<ul>
  <li><strong>Agent terminal panels</strong>: Each agent’s TUI runs in its own panel, with live PTY streaming so you see exactly what the agent sees</li>
  <li><strong>Tabbed views</strong>: Switch between agent-graphs (who’s talking to whom), wiki pages (shared durable state), agentrail Sagas per agent (workflow history), and mailbox events (messages flowing to and from the coordinator)</li>
</ul>

<p>The coordination model separates two planes: a <strong>terminal I/O plane</strong> (raw PTY bytes, keystroke forwarding) and an <strong>orchestration plane</strong> (structured JSON events for feature requests, completion notices, and status updates). The coordinator doesn’t type into worker terminals—it sends structured messages through mailboxes. Workers read their mailbox, do their work, and post results back. The wiki provides durable shared state: goals, decisions, and context that any agent can read without asking.</p>

<p>This matters because the alternative—agents talking through unstructured terminal paste—is fragile and loses context. Mailboxes give you a clean event log. The wiki gives you shared memory that survives agent restarts. Agentrail sagas give you per-agent workflow audit trails.</p>

<h2 id="fuzzit-testing-tools-with-extreme-inputs">Fuzzit: Testing Tools with Extreme Inputs</h2>

<p>Fuzzit is an LLM-guided fuzz testing tool built to discover crashes, hangs, panics, and unexpected behavior in CLIs, compilers, interpreters, REPLs, and APIs. The core idea: <strong>a tool that tests other tools</strong> by throwing extreme and random inputs at them to validate error handling.</p>

<p>Fuzzit runs four-layer fuzzing campaigns with budget allocation across:</p>

<table>
  <thead>
    <tr>
      <th>Layer</th>
      <th>Budget</th>
      <th>What It Does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Baseline</strong></td>
      <td>30%</td>
      <td>Deterministic edge cases—empty inputs, whitespace storms, deep nesting, invalid UTF-8, huge payloads</td>
    </tr>
    <tr>
      <td><strong>LLM Seeds</strong></td>
      <td>10%</td>
      <td>Targeted seed generation via local Ollama models that understand the tool’s grammar and API surface</td>
    </tr>
    <tr>
      <td><strong>Mutations</strong></td>
      <td>40%</td>
      <td>Bit flips, insertions, deletions, and crossover applied to seeds that previously triggered interesting behavior</td>
    </tr>
    <tr>
      <td><strong>Feedback</strong></td>
      <td>20%</td>
      <td>Retain and re-mutate inputs that produced new exit codes, slow responses, or unusual stderr output</td>
    </tr>
  </tbody>
</table>

<p>Each finding gets deterministically classified: panic, hang (wall-time timeout), segfault, unexpected exit code, or stderr anomaly. Interesting findings are automatically promoted to Rust regression tests, so once Fuzzit finds a bug, the fix stays tested forever.</p>

<p>No cloud dependencies—Fuzzit uses local Ollama for LLM-guided seeds, making it practical for testing proprietary tools and compilers that can’t be sent to external services. The nine-crate workspace (fz-core, fz-manifest, fz-corpus, fz-exec, fz-classify, fz-mutate, fz-llm, fz-artifacts, fz-cli) keeps concerns separated and testable.</p>

<p>The immediate use case: Fuzzit is already testing the COR24 compiler toolchain (tc24r, PL/SW, Pascal) to find codegen bugs that downstream languages would otherwise discover the hard way.</p>

<h2 id="vendoring-parallel-development-without-the-pain">Vendoring: Parallel Development Without the Pain</h2>

<p>The COR24 compiler chain has a dependency problem. PL/SW is written in C and compiled by <code class="language-plaintext highlighter-rouge">tc24r</code> (the C cross-compiler). SNOBOL4 is written in PL/SW. A new Fortran compiler will be written in SNOBOL4. Each layer depends on the one below it, and changes at any level can break everything above.</p>

<p><strong>Vendoring</strong> solves this by pinning stable snapshots:</p>

<ul>
  <li><strong>PL/SW vendors tc24r</strong>: The PL/SW repo includes a known-good version of the C compiler. I can add features or fix bugs in tc24r’s main branch without breaking PL/SW mid-development. When a tc24r improvement is ready and tested, PL/SW explicitly updates its vendored copy.</li>
  <li><strong>SNOBOL4 vendors PL/SW</strong>: Same pattern one level up. SNOBOL4 works against a stable PL/SW compiler. PL/SW macro system changes don’t destabilize the SNOBOL4 interpreter until deliberately promoted.</li>
  <li><strong>Fortran vendors SNOBOL4</strong>: The new Fortran compiler (upcoming) will vendor SNOBOL4, giving it a stable implementation language while SNOBOL4 continues evolving.</li>
</ul>

<p>This is the same idea behind Go’s <code class="language-plaintext highlighter-rouge">vendor/</code> directory or Rust’s <code class="language-plaintext highlighter-rouge">Cargo.lock</code>—except applied to entire compiler toolchains in an embedded ecosystem. Each project controls when it absorbs upstream changes, so three developers (or three agent sessions) can work on three layers simultaneously without coordination overhead.</p>

<p>The alternative was what we had before: fix a C compiler bug, rebuild PL/SW, discover the fix exposed a PL/SW assumption, fix that, rebuild SNOBOL4, discover <em>that</em> exposed a SNOBOL4 assumption. Vendoring breaks the cascade. Fix, test, promote when ready.</p>

<h2 id="emacs-graphics-paperbanana-style-visuals-in-emacs">Emacs Graphics: PaperBanana-Style Visuals in Emacs</h2>

<p><a href="https://github.com/sw-emacs/graphical-experiments">Graphical Experiments</a> is a new project exploring what happens when you bring PaperBanana-styled graphics into Emacs—SVG menu cards, inline charts, animated headers, and slide presentations, all rendered in native Emacs buffers.</p>

<p>The project is a kit of six Elisp packages:</p>

<table>
  <thead>
    <tr>
      <th>Package</th>
      <th>What It Does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>pb-menu</strong></td>
      <td>PaperBanana-style SVG menu cards with rounded corners, icons, and solarized color palette</td>
    </tr>
    <tr>
      <td><strong>pb-chart</strong></td>
      <td>Bar charts, sparklines, and scatter plots rendered as inline SVG</td>
    </tr>
    <tr>
      <td><strong>pb-media</strong></td>
      <td>Image viewing and an animated header-line “heat” indicator</td>
    </tr>
    <tr>
      <td><strong>pb-present</strong></td>
      <td>A minimal slide/presentation mode with keyboard navigation (<code class="language-plaintext highlighter-rouge">n</code>/<code class="language-plaintext highlighter-rouge">p</code>/<code class="language-plaintext highlighter-rouge">q</code>)</td>
    </tr>
    <tr>
      <td><strong>pb-web</strong></td>
      <td>Browser embedding helpers—xwidgets if available, EAF fallback, else external browser</td>
    </tr>
    <tr>
      <td><strong>pb-demo-init</strong></td>
      <td>Convenience loader and command index</td>
    </tr>
  </tbody>
</table>

<p>The immediate goal is a visual toolkit for Emacs that feels like PaperBanana’s aesthetic—warm card layouts, clean data visualizations, and interactive menus—without leaving the editor. The longer-term direction includes clickable menu actions, layout helpers for arrows and swimlanes, Org integration for slides rendered from headings, and Rust-backed SVG layout for more complex graph visualizations.</p>

<p>Everything is built on Emacs 29+’s native SVG support (<code class="language-plaintext highlighter-rouge">svg.el</code>), keeping the code small and hackable. No external rendering dependencies—if your Emacs build supports SVG images, the demos work.</p>

<h2 id="repos-and-live-demos">Repos and Live Demos</h2>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>GitHub</th>
      <th>Live Demo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>All Together Now</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/all-together-now">sw-vibe-coding/all-together-now</a></td>
      <td><em>in development</em></td>
    </tr>
    <tr>
      <td><strong>Fuzzit</strong></td>
      <td><a href="https://github.com/sw-cli-tools/fuzzit">sw-cli-tools/fuzzit</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>Emacs Graphics</strong></td>
      <td><a href="https://github.com/sw-emacs/graphical-experiments">sw-emacs/graphical-experiments</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>PL/SW</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-plsw">sw-embed/sw-cor24-plsw</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-plsw/">PL/SW Demo</a></td>
    </tr>
    <tr>
      <td><strong>SNOBOL4</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-snobol4">sw-embed/sw-cor24-snobol4</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-snobol4/">SNOBOL4 Demo</a></td>
    </tr>
    <tr>
      <td><strong>Fortran</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-fortran">sw-embed/sw-cor24-fortran</a></td>
      <td><em>future</em></td>
    </tr>
    <tr>
      <td><strong>tc24r</strong> (C compiler)</td>
      <td><a href="https://github.com/sw-embed/sw-cor24-x-tinyc">sw-embed/sw-cor24-x-tinyc</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-tinyc/">TinyC Demo</a></td>
    </tr>
    <tr>
      <td><strong>agentrail-rs</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/agentrail-rs">sw-vibe-coding/agentrail-rs</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>COR24 Demo Hub</strong></td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub</a></td>
    </tr>
  </tbody>
</table>

<h2 id="whats-next">What’s Next</h2>

<p><strong>All Together Now</strong>: Live multi-agent demo with Claude Code coordinating opencode/GLM-5 workers on a real task—likely a collaborative code review or multi-repo refactor. The Web UI will stream all panels simultaneously.</p>

<p><strong>Fuzzit</strong>: Expanding target coverage beyond compilers—next up are the COR24 embedded tools (monitor, shell, editor) and the agentrail-rs CLI. Campaign comparison reports to track regression across tool versions.</p>

<p><strong>Emacs Graphics</strong>: Clickable menu actions on the SVG cards, Org-mode integration so presentation slides render from headings, and exploring Rust-backed SVG layout helpers for more complex graph and swimlane diagrams.</p>

<p><strong>Vendoring</strong>: Establishing the Fortran-vendors-SNOBOL4 relationship as the Fortran compiler scaffolding begins. Documenting the vendoring protocol so agent sessions can update pinned versions without manual intervention.</p>

<hr />

<p><em>Parallel work needs parallel infrastructure. Follow for more Sharpen the Saw updates.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rust" /><category term="ai-agents" /><category term="testing" /><category term="compilers" /><category term="emacs" /><category term="sharpen-the-saw" /><category term="rust" /><category term="all-together-now" /><category term="fuzzit" /><category term="vendoring" /><category term="plsw" /><category term="snobol4" /><category term="cor24" /><category term="multi-agent" /><category term="fuzzing" /><category term="emacs" /><category term="svg" /><category term="paperbanana" /><summary type="html"><![CDATA[Sixth Sharpen the Saw update. Last time the theme was dependency chains—saga archiving, new languages, and compiler fixes cascading through the stack. This week the theme is independence: making agents coordinate without stepping on each other, testing tools without trusting their error handling, and letting compilers evolve without breaking their downstream consumers. Four projects, one idea: build the infrastructure so that parallel work stays parallel. Why Sharpen the Saw? — The name comes from Covey’s Habit 7: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—agent orchestration, testing infrastructure, compiler toolchains—so the feature work on top goes faster. Resource Link Repos &amp; Live Demos Table below Prior Post Saw (5/?): Sagas, Languages, and Compiler Chains Comments Discord All Together Now: Multi-Agent Coordination Demo All Together Now (ATN) is a program manager that orchestrates multiple AI agent sessions running in isolated PTY environments. The new milestone is a demo that shows Claude Code as coordinator delegating tasks to multiple opencode/GLM-5 worker agents, with the agents collaborating through mailboxes and a shared wiki. The Web UI (Yew/WASM) now presents this as a multi-panel layout: Agent terminal panels: Each agent’s TUI runs in its own panel, with live PTY streaming so you see exactly what the agent sees Tabbed views: Switch between agent-graphs (who’s talking to whom), wiki pages (shared durable state), agentrail Sagas per agent (workflow history), and mailbox events (messages flowing to and from the coordinator) The coordination model separates two planes: a terminal I/O plane (raw PTY bytes, keystroke forwarding) and an orchestration plane (structured JSON events for feature requests, completion notices, and status updates). The coordinator doesn’t type into worker terminals—it sends structured messages through mailboxes. Workers read their mailbox, do their work, and post results back. The wiki provides durable shared state: goals, decisions, and context that any agent can read without asking. This matters because the alternative—agents talking through unstructured terminal paste—is fragile and loses context. Mailboxes give you a clean event log. The wiki gives you shared memory that survives agent restarts. Agentrail sagas give you per-agent workflow audit trails. Fuzzit: Testing Tools with Extreme Inputs Fuzzit is an LLM-guided fuzz testing tool built to discover crashes, hangs, panics, and unexpected behavior in CLIs, compilers, interpreters, REPLs, and APIs. The core idea: a tool that tests other tools by throwing extreme and random inputs at them to validate error handling. Fuzzit runs four-layer fuzzing campaigns with budget allocation across: Layer Budget What It Does Baseline 30% Deterministic edge cases—empty inputs, whitespace storms, deep nesting, invalid UTF-8, huge payloads LLM Seeds 10% Targeted seed generation via local Ollama models that understand the tool’s grammar and API surface Mutations 40% Bit flips, insertions, deletions, and crossover applied to seeds that previously triggered interesting behavior Feedback 20% Retain and re-mutate inputs that produced new exit codes, slow responses, or unusual stderr output Each finding gets deterministically classified: panic, hang (wall-time timeout), segfault, unexpected exit code, or stderr anomaly. Interesting findings are automatically promoted to Rust regression tests, so once Fuzzit finds a bug, the fix stays tested forever. No cloud dependencies—Fuzzit uses local Ollama for LLM-guided seeds, making it practical for testing proprietary tools and compilers that can’t be sent to external services. The nine-crate workspace (fz-core, fz-manifest, fz-corpus, fz-exec, fz-classify, fz-mutate, fz-llm, fz-artifacts, fz-cli) keeps concerns separated and testable. The immediate use case: Fuzzit is already testing the COR24 compiler toolchain (tc24r, PL/SW, Pascal) to find codegen bugs that downstream languages would otherwise discover the hard way. Vendoring: Parallel Development Without the Pain The COR24 compiler chain has a dependency problem. PL/SW is written in C and compiled by tc24r (the C cross-compiler). SNOBOL4 is written in PL/SW. A new Fortran compiler will be written in SNOBOL4. Each layer depends on the one below it, and changes at any level can break everything above. Vendoring solves this by pinning stable snapshots: PL/SW vendors tc24r: The PL/SW repo includes a known-good version of the C compiler. I can add features or fix bugs in tc24r’s main branch without breaking PL/SW mid-development. When a tc24r improvement is ready and tested, PL/SW explicitly updates its vendored copy. SNOBOL4 vendors PL/SW: Same pattern one level up. SNOBOL4 works against a stable PL/SW compiler. PL/SW macro system changes don’t destabilize the SNOBOL4 interpreter until deliberately promoted. Fortran vendors SNOBOL4: The new Fortran compiler (upcoming) will vendor SNOBOL4, giving it a stable implementation language while SNOBOL4 continues evolving. This is the same idea behind Go’s vendor/ directory or Rust’s Cargo.lock—except applied to entire compiler toolchains in an embedded ecosystem. Each project controls when it absorbs upstream changes, so three developers (or three agent sessions) can work on three layers simultaneously without coordination overhead. The alternative was what we had before: fix a C compiler bug, rebuild PL/SW, discover the fix exposed a PL/SW assumption, fix that, rebuild SNOBOL4, discover that exposed a SNOBOL4 assumption. Vendoring breaks the cascade. Fix, test, promote when ready. Emacs Graphics: PaperBanana-Style Visuals in Emacs Graphical Experiments is a new project exploring what happens when you bring PaperBanana-styled graphics into Emacs—SVG menu cards, inline charts, animated headers, and slide presentations, all rendered in native Emacs buffers. The project is a kit of six Elisp packages: Package What It Does pb-menu PaperBanana-style SVG menu cards with rounded corners, icons, and solarized color palette pb-chart Bar charts, sparklines, and scatter plots rendered as inline SVG pb-media Image viewing and an animated header-line “heat” indicator pb-present A minimal slide/presentation mode with keyboard navigation (n/p/q) pb-web Browser embedding helpers—xwidgets if available, EAF fallback, else external browser pb-demo-init Convenience loader and command index The immediate goal is a visual toolkit for Emacs that feels like PaperBanana’s aesthetic—warm card layouts, clean data visualizations, and interactive menus—without leaving the editor. The longer-term direction includes clickable menu actions, layout helpers for arrows and swimlanes, Org integration for slides rendered from headings, and Rust-backed SVG layout for more complex graph visualizations. Everything is built on Emacs 29+’s native SVG support (svg.el), keeping the code small and hackable. No external rendering dependencies—if your Emacs build supports SVG images, the demos work. Repos and Live Demos Project GitHub Live Demo All Together Now sw-vibe-coding/all-together-now in development Fuzzit sw-cli-tools/fuzzit N/A Emacs Graphics sw-emacs/graphical-experiments N/A PL/SW sw-embed/sw-cor24-plsw PL/SW Demo SNOBOL4 sw-embed/sw-cor24-snobol4 SNOBOL4 Demo Fortran sw-embed/sw-cor24-fortran future tc24r (C compiler) sw-embed/sw-cor24-x-tinyc TinyC Demo agentrail-rs sw-vibe-coding/agentrail-rs N/A COR24 Demo Hub sw-embed/web-sw-cor24-demos Demo Hub What’s Next All Together Now: Live multi-agent demo with Claude Code coordinating opencode/GLM-5 workers on a real task—likely a collaborative code review or multi-repo refactor. The Web UI will stream all panels simultaneously. Fuzzit: Expanding target coverage beyond compilers—next up are the COR24 embedded tools (monitor, shell, editor) and the agentrail-rs CLI. Campaign comparison reports to track regression across tool versions. Emacs Graphics: Clickable menu actions on the SVG cards, Org-mode integration so presentation slides render from headings, and exploring Rust-backed SVG layout helpers for more complex graph and swimlane diagrams. Vendoring: Establishing the Fortran-vendors-SNOBOL4 relationship as the Fortran compiler scaffolding begins. Documenting the vendoring protocol so agent sessions can update pinned versions without manual intervention. Parallel work needs parallel infrastructure. Follow for more Sharpen the Saw updates.]]></summary></entry><entry><title type="html">Saw (5/?): Sagas, Languages, and Compiler Chains</title><link href="https://software-wrighter-lab.github.io/2026/04/05/saw-saga-archiving-mlpl-plsw-cor24-compilers/" rel="alternate" type="text/html" title="Saw (5/?): Sagas, Languages, and Compiler Chains" /><published>2026-04-05T10:30:00-07:00</published><updated>2026-04-05T10:30:00-07:00</updated><id>https://software-wrighter-lab.github.io/2026/04/05/saw-saga-archiving-mlpl-plsw-cor24-compilers</id><content type="html" xml:base="https://software-wrighter-lab.github.io/2026/04/05/saw-saga-archiving-mlpl-plsw-cor24-compilers/"><![CDATA[<p><img src="/assets/images/posts/bunny-saw-vert.jpg" class="post-marker no-invert" alt="" /></p>

<div style="overflow: hidden;">

  <p>Fifth Sharpen the Saw update. <a href="/2026/03/29/saw-pjmai-reg-emacs-all-together-now/">Last time</a> the focus was integration—Emacs packages, a multi-agent orchestrator. This week spread across multiple repos touching four themes: workflow infrastructure for agents, new programming languages, compiler-chain fixes that unblock downstream tools, and embedded infrastructure (monitor, shell, editor) that makes COR24 usable as a development platform.</p>

  <p>The common thread is <em>dependency chains</em>—saga archiving lets agentrail manage MLPL’s multi-phase development, MLPL draws on APL ideas validated on embedded hardware, that embedded APL needs the C compiler fixed, and BASIC needs the Pascal compiler extended. Every fix at the bottom of the stack unlocks something above it.</p>

</div>

<div class="aside-box">

  <p><strong>Why Sharpen the Saw?</strong> — The name comes from Covey’s <a href="https://en.wikipedia.org/wiki/The_7_Habits_of_Highly_Effective_People">Habit 7</a>: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—compilers, languages, agent infrastructure—so the feature work that sits on top of them goes faster. Five weeks in, the dependency chains are getting shorter.</p>

</div>

<div class="resource-box">

  <table>
    <thead>
      <tr>
        <th>Resource</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Repos &amp; Live Demos</strong></td>
        <td><a href="#repos-and-live-demos">Table below</a></td>
      </tr>
      <tr>
        <td><strong>Prior Post</strong></td>
        <td><a href="/2026/03/29/saw-pjmai-reg-emacs-all-together-now/">Saw (4/?): All Together Now — Emacs Meets the Multi-Agent Orchestra</a></td>
      </tr>
      <tr>
        <td><strong>Comments</strong></td>
        <td><a href="https://discord.com/invite/Ctzk5uHggZ">Discord</a></td>
      </tr>
    </tbody>
  </table>

</div>

<h2 id="saga-archiving-in-agentrail-rs">Saga Archiving in agentrail-rs</h2>

<p>Agentrail-rs tracks AI agent workflows as append-only saga records. Until now, one project meant one saga directory. That breaks down when a project has multiple concurrent workstreams—say, a language project where one saga covers the parser, another the runtime, and a third the test harness.</p>

<p>Saga archiving adds the ability to close out a completed saga and start a new one without losing history. Archived sagas move to a timestamped subdirectory, keeping the active saga directory clean while preserving the full trajectory for later analysis or ICRL replay. MLPL is the first project using saga archiving—each phase of the language (lexer, parser, interpreter, compiler) gets its own saga, archived when complete.</p>

<p>Not every project needs multiple sagas. The C compiler (<code class="language-plaintext highlighter-rouge">tc24r</code>) uses a single ever-growing saga that accumulates GitHub issues as they arrive from downstream projects like APL and PL/SW. When an APL feature hits a codegen gap, the issue lands in tc24r’s saga and stays there until fixed. One saga, one backlog—simple and appropriate for a project driven by external bug reports rather than internal phases.</p>

<h2 id="mlpl-an-apl2jbqn-inspired-ml-language">MLPL: An APL2/J/BQN-Inspired ML Language</h2>

<p>MLPL is a new Rust-based language inspired by APL2, J, BQN, and K, purpose-built for machine learning workflows. Unlike the <a href="#fixing-the-c-compiler-to-unblock-apl">integer APL on COR24</a>—which is a minimal subset running on embedded hardware—MLPL runs on Linux and Mac with full floating-point support, first-class tensors, visual debugging, and a contract-first compartmentalized architecture.</p>

<p>The language is being built in Rust with a phased approach: parser and AST first, then a tree-walking interpreter, then compilation. The two APL projects inform each other: operator semantics and reduction patterns tested in COR24’s constrained integer environment validate core ideas, while MLPL extends them into floating-point territory and higher-rank tensor operations that embedded hardware can’t touch. MLPL is also the proving ground for agentrail’s new saga archiving—each language phase gets its own saga with clean boundaries.</p>

<h2 id="plsw-macros-for-a-pli-inspired-systems-language">PL/SW: Macros for a PL/I-Inspired Systems Language</h2>

<p>PL/SW is a systems programming language inspired by PL/I, targeting COR24 today with an eye toward future FPGA soft CPUs. It compiles to human-readable COR24 assembler, which means you can inspect every instruction the compiler emits.</p>

<p>This week’s milestone: <strong>compile-time macro metaprogramming</strong>. PL/SW macros expand at compile time and generate COR24 assembly directly, enabling:</p>

<ul>
  <li><strong>Hardware abstraction</strong>: <code class="language-plaintext highlighter-rouge">%MMIO_WRITE(addr, val)</code> expands to the correct load/store sequence</li>
  <li><strong>Inline patterns</strong>: Loop unrolling and register allocation hints without runtime cost</li>
  <li><strong>BASED record templates</strong>: Structured memory access patterns that the macro system can verify at compile time</li>
</ul>

<p>The macro system is the bridge between PL/SW-as-a-language and PL/SW-as-a-systems-tool. Without it, every hardware interaction required inline assembly. With it, the language can express hardware patterns in its own syntax. The <a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">COR24 demo hub</a> hosts the emulator where PL/SW programs run.</p>

<p><img src="/assets/images/posts/sw-enabling-tech-mermaid-diagram2.png" alt="Enabling technology dependency diagram" class="post-image" /></p>

<h2 id="fixing-the-c-compiler-to-unblock-apl">Fixing the C Compiler to Unblock APL</h2>

<p>The COR24 APL interpreter is written in C and compiled by <code class="language-plaintext highlighter-rouge">tc24r</code> (a chibicc-inspired C compiler targeting COR24’s 24-bit RISC ISA). APL’s array operations hit several compiler gaps:</p>

<ul>
  <li><strong>Missing features</strong>: Certain C constructs that APL’s runtime relied on weren’t yet implemented in tc24r</li>
  <li><strong>Code generation bugs</strong>: Edge cases in pointer arithmetic and array indexing produced incorrect COR24 assembly</li>
</ul>

<p>Each fix in tc24r immediately unblocked APL features that were waiting on correct codegen. The <a href="https://sw-embed.github.io/web-sw-cor24-apl/">APL REPL</a> now handles more complex array expressions thanks to these fixes.</p>

<h2 id="extending-pascal-to-unblock-basic">Extending Pascal to Unblock BASIC</h2>

<p>A similar dependency chain on the Pascal side. The COR24 BASIC interpreter runs on a Pascal p-code VM: Pascal source compiles to p-code assembler (<code class="language-plaintext highlighter-rouge">pa24r</code>), links via <code class="language-plaintext highlighter-rouge">pl24r</code>, and executes on the COR24 virtual machine. BASIC features that need new p-code instructions require Pascal compiler work first.</p>

<p>The Pascal compiler extensions this round added capabilities that BASIC’s string handling and control flow were blocked on. Like the C/APL chain, every Pascal fix cascades into BASIC functionality.</p>

<h2 id="embedded-tools-monitor-shell-and-editor">Embedded Tools: Monitor, Shell, and Editor</h2>

<p>The COR24 ecosystem also gained progress on three infrastructure tools that make the platform usable as more than a compiler target.</p>

<p><strong>Monitor</strong> (<code class="language-plaintext highlighter-rouge">sw-cor24-monitor</code>) is the low-level system monitor—the first thing that runs on COR24 hardware. It provides memory inspection, register dumps, and direct machine-code entry. Think of it as the boot ROM’s interactive console.</p>

<p><strong>Script</strong> (<code class="language-plaintext highlighter-rouge">sw-cor24-script</code>) is a shell and scripting environment for COR24. It gives the platform a command-line interface for running programs, piping output, and automating tasks—the glue layer between the monitor and the language tools above it.</p>

<p><strong>Yocto-Ed</strong> (<code class="language-plaintext highlighter-rouge">sw-cor24-yocto-ed</code>) is an Emacs-inspired line editor for COR24. On embedded hardware without a full terminal, a line editor is the practical way to edit source files and configuration. Yocto-Ed borrows Emacs keybindings and concepts (kill ring, incremental search) scaled down to fit in COR24’s memory constraints.</p>

<p>All three are works in progress with live demos planned.</p>

<h2 id="repos-and-live-demos">Repos and Live Demos</h2>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>GitHub</th>
      <th>Live Demo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>agentrail-rs</strong></td>
      <td><a href="https://github.com/sw-vibe-coding/agentrail-rs">sw-vibe-coding/agentrail-rs</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>APL</strong> (integer, embedded)</td>
      <td><a href="https://github.com/sw-embed/sw-cor24-apl">sw-embed/sw-cor24-apl</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-apl/">APL REPL</a></td>
    </tr>
    <tr>
      <td><strong>BASIC</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-basic">sw-embed/sw-cor24-basic</a></td>
      <td><em>future</em></td>
    </tr>
    <tr>
      <td><strong>COR24 Demo Hub</strong></td>
      <td><a href="https://github.com/sw-embed/web-sw-cor24-demos">sw-embed/web-sw-cor24-demos</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-demos/#/">Demo Hub</a></td>
    </tr>
    <tr>
      <td><strong>MLPL</strong> (APL2/J/BQN, Rust, float)</td>
      <td><a href="https://github.com/sw-ml-study/sw-mlpl">sw-ml-study/sw-mlpl</a></td>
      <td><em>future</em></td>
    </tr>
    <tr>
      <td><strong>Monitor</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-monitor">sw-embed/sw-cor24-monitor</a></td>
      <td><em>future</em></td>
    </tr>
    <tr>
      <td><strong>Pascal</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-pascal">sw-embed/sw-cor24-pascal</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-pascal/">Pascal Demo</a></td>
    </tr>
    <tr>
      <td><strong>PL/SW</strong></td>
      <td><a href="https://github.com/sw-embed/sw-cor24-plsw">sw-embed/sw-cor24-plsw</a></td>
      <td><a href="https://sw-embed.github.io/web-sw-cor24-plsw/">PL/SW Demo</a></td>
    </tr>
    <tr>
      <td><strong>Script</strong> (shell)</td>
      <td><a href="https://github.com/sw-embed/sw-cor24-script">sw-embed/sw-cor24-script</a></td>
      <td><em>future</em></td>
    </tr>
    <tr>
      <td><strong>tc24r</strong> (C compiler)</td>
      <td><a href="https://github.com/sw-embed/sw-cor24-x-tinyc">sw-embed/sw-cor24-x-tinyc</a></td>
      <td>N/A</td>
    </tr>
    <tr>
      <td><strong>Yocto-Ed</strong> (line editor)</td>
      <td><a href="https://github.com/sw-embed/sw-cor24-yocto-ed">sw-embed/sw-cor24-yocto-ed</a></td>
      <td><em>future</em></td>
    </tr>
  </tbody>
</table>

<h2 id="whats-next">What’s Next</h2>

<p><strong>Agentrail-rs</strong>: Multi-saga coordination—archived sagas feeding context into new ones, so an agent starting a fresh workstream can learn from completed ones.</p>

<p><strong>MLPL</strong>: Parser completion and first interpreter pass. The APL operator semantics validated on COR24 hardware will inform which primitives make it into MLPL’s core.</p>

<p><strong>PL/SW</strong>: Macro library for COR24 peripheral access patterns, targeting the <a href="https://makerlisp.com">MakerLisp</a> hardware and eventually FPGA soft CPUs.</p>

<p><strong>COR24 compilers</strong>: Continuing to close gaps in tc24r and the Pascal toolchain as APL and BASIC push further into their respective feature sets.</p>

<hr />

<p><em>Sharpen the tools, shorten the chains. Follow for more Sharpen the Saw updates.</em></p>]]></content><author><name>Software Wrighter</name></author><category term="rust" /><category term="compilers" /><category term="languages" /><category term="ai-agents" /><category term="sharpen-the-saw" /><category term="rust" /><category term="agentrail" /><category term="mlpl" /><category term="apl" /><category term="plsw" /><category term="pascal" /><category term="basic" /><category term="c-compiler" /><category term="cor24" /><category term="fpga" /><category term="vibe-coding" /><summary type="html"><![CDATA[Fifth Sharpen the Saw update. Last time the focus was integration—Emacs packages, a multi-agent orchestrator. This week spread across multiple repos touching four themes: workflow infrastructure for agents, new programming languages, compiler-chain fixes that unblock downstream tools, and embedded infrastructure (monitor, shell, editor) that makes COR24 usable as a development platform. The common thread is dependency chains—saga archiving lets agentrail manage MLPL’s multi-phase development, MLPL draws on APL ideas validated on embedded hardware, that embedded APL needs the C compiler fixed, and BASIC needs the Pascal compiler extended. Every fix at the bottom of the stack unlocks something above it. Why Sharpen the Saw? — The name comes from Covey’s Habit 7: stop cutting long enough to sharpen the blade. This series tracks weekly investment in the tools themselves—compilers, languages, agent infrastructure—so the feature work that sits on top of them goes faster. Five weeks in, the dependency chains are getting shorter. Resource Link Repos &amp; Live Demos Table below Prior Post Saw (4/?): All Together Now — Emacs Meets the Multi-Agent Orchestra Comments Discord Saga Archiving in agentrail-rs Agentrail-rs tracks AI agent workflows as append-only saga records. Until now, one project meant one saga directory. That breaks down when a project has multiple concurrent workstreams—say, a language project where one saga covers the parser, another the runtime, and a third the test harness. Saga archiving adds the ability to close out a completed saga and start a new one without losing history. Archived sagas move to a timestamped subdirectory, keeping the active saga directory clean while preserving the full trajectory for later analysis or ICRL replay. MLPL is the first project using saga archiving—each phase of the language (lexer, parser, interpreter, compiler) gets its own saga, archived when complete. Not every project needs multiple sagas. The C compiler (tc24r) uses a single ever-growing saga that accumulates GitHub issues as they arrive from downstream projects like APL and PL/SW. When an APL feature hits a codegen gap, the issue lands in tc24r’s saga and stays there until fixed. One saga, one backlog—simple and appropriate for a project driven by external bug reports rather than internal phases. MLPL: An APL2/J/BQN-Inspired ML Language MLPL is a new Rust-based language inspired by APL2, J, BQN, and K, purpose-built for machine learning workflows. Unlike the integer APL on COR24—which is a minimal subset running on embedded hardware—MLPL runs on Linux and Mac with full floating-point support, first-class tensors, visual debugging, and a contract-first compartmentalized architecture. The language is being built in Rust with a phased approach: parser and AST first, then a tree-walking interpreter, then compilation. The two APL projects inform each other: operator semantics and reduction patterns tested in COR24’s constrained integer environment validate core ideas, while MLPL extends them into floating-point territory and higher-rank tensor operations that embedded hardware can’t touch. MLPL is also the proving ground for agentrail’s new saga archiving—each language phase gets its own saga with clean boundaries. PL/SW: Macros for a PL/I-Inspired Systems Language PL/SW is a systems programming language inspired by PL/I, targeting COR24 today with an eye toward future FPGA soft CPUs. It compiles to human-readable COR24 assembler, which means you can inspect every instruction the compiler emits. This week’s milestone: compile-time macro metaprogramming. PL/SW macros expand at compile time and generate COR24 assembly directly, enabling: Hardware abstraction: %MMIO_WRITE(addr, val) expands to the correct load/store sequence Inline patterns: Loop unrolling and register allocation hints without runtime cost BASED record templates: Structured memory access patterns that the macro system can verify at compile time The macro system is the bridge between PL/SW-as-a-language and PL/SW-as-a-systems-tool. Without it, every hardware interaction required inline assembly. With it, the language can express hardware patterns in its own syntax. The COR24 demo hub hosts the emulator where PL/SW programs run. Fixing the C Compiler to Unblock APL The COR24 APL interpreter is written in C and compiled by tc24r (a chibicc-inspired C compiler targeting COR24’s 24-bit RISC ISA). APL’s array operations hit several compiler gaps: Missing features: Certain C constructs that APL’s runtime relied on weren’t yet implemented in tc24r Code generation bugs: Edge cases in pointer arithmetic and array indexing produced incorrect COR24 assembly Each fix in tc24r immediately unblocked APL features that were waiting on correct codegen. The APL REPL now handles more complex array expressions thanks to these fixes. Extending Pascal to Unblock BASIC A similar dependency chain on the Pascal side. The COR24 BASIC interpreter runs on a Pascal p-code VM: Pascal source compiles to p-code assembler (pa24r), links via pl24r, and executes on the COR24 virtual machine. BASIC features that need new p-code instructions require Pascal compiler work first. The Pascal compiler extensions this round added capabilities that BASIC’s string handling and control flow were blocked on. Like the C/APL chain, every Pascal fix cascades into BASIC functionality. Embedded Tools: Monitor, Shell, and Editor The COR24 ecosystem also gained progress on three infrastructure tools that make the platform usable as more than a compiler target. Monitor (sw-cor24-monitor) is the low-level system monitor—the first thing that runs on COR24 hardware. It provides memory inspection, register dumps, and direct machine-code entry. Think of it as the boot ROM’s interactive console. Script (sw-cor24-script) is a shell and scripting environment for COR24. It gives the platform a command-line interface for running programs, piping output, and automating tasks—the glue layer between the monitor and the language tools above it. Yocto-Ed (sw-cor24-yocto-ed) is an Emacs-inspired line editor for COR24. On embedded hardware without a full terminal, a line editor is the practical way to edit source files and configuration. Yocto-Ed borrows Emacs keybindings and concepts (kill ring, incremental search) scaled down to fit in COR24’s memory constraints. All three are works in progress with live demos planned. Repos and Live Demos Project GitHub Live Demo agentrail-rs sw-vibe-coding/agentrail-rs N/A APL (integer, embedded) sw-embed/sw-cor24-apl APL REPL BASIC sw-embed/sw-cor24-basic future COR24 Demo Hub sw-embed/web-sw-cor24-demos Demo Hub MLPL (APL2/J/BQN, Rust, float) sw-ml-study/sw-mlpl future Monitor sw-embed/sw-cor24-monitor future Pascal sw-embed/sw-cor24-pascal Pascal Demo PL/SW sw-embed/sw-cor24-plsw PL/SW Demo Script (shell) sw-embed/sw-cor24-script future tc24r (C compiler) sw-embed/sw-cor24-x-tinyc N/A Yocto-Ed (line editor) sw-embed/sw-cor24-yocto-ed future What’s Next Agentrail-rs: Multi-saga coordination—archived sagas feeding context into new ones, so an agent starting a fresh workstream can learn from completed ones. MLPL: Parser completion and first interpreter pass. The APL operator semantics validated on COR24 hardware will inform which primitives make it into MLPL’s core. PL/SW: Macro library for COR24 peripheral access patterns, targeting the MakerLisp hardware and eventually FPGA soft CPUs. COR24 compilers: Continuing to close gaps in tc24r and the Pascal toolchain as APL and BASIC push further into their respective feature sets. Sharpen the tools, shorten the chains. Follow for more Sharpen the Saw updates.]]></summary></entry></feed>